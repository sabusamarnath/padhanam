[project]
name = "padhanam-tools"
version = "0.0.0"
description = "Tools bounded context (D68, D89). Tool plus ToolRevision aggregates with hash-chain integrity per D26 and revision shape per D31; classification taxonomy with three-to-three invariant mapping per D89; schema-diff BC stub at revision creation; control-plane Postgres persistence per D33 / D89 storage-location resolution. Substrate for the agent context's two thin ports (ToolDefinitionsLookup, ToolInvoker) at S28b commit 5."
requires-python = ">=3.14"
dependencies = [
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.30",
]

[tool.uv]
package = false
