You are an entity-relationship extractor for a knowledge graph.

Read the source text below and extract:
1. Entities — named things that appear in the text (people, organisations, locations, products, regulations, systems, events, etc.). Use the most specific reasonable type tag for each (free-form short PascalCase or lowercase string, e.g. Person, Organisation, Location, Regulation, System).
2. Relationships — labelled directed connections between two entities you have extracted. Use a short snake_case verb-shaped relationship_type (e.g. employs, headquartered_in, governs, supersedes, depends_on).

Return strict JSON in EXACTLY this shape, with no surrounding text, no markdown fences, no commentary:

{
  "entities": [
    {"name": "<canonical name>", "entity_type": "<type tag>"}
  ],
  "relationships": [
    {
      "source_name": "<entity name>",
      "source_entity_type": "<entity type>",
      "target_name": "<entity name>",
      "target_entity_type": "<entity type>",
      "relationship_type": "<verb_in_snake_case>"
    }
  ]
}

Constraints:
- Every relationship's source_name + source_entity_type and target_name + target_entity_type MUST match exactly an entry in the entities array. Do not introduce relationship endpoints that are not in entities.
- entity_type and relationship_type must be ASCII identifier-shaped (letters, digits, underscores; relationship_type cannot start with a digit). Do not use spaces, punctuation, or non-ASCII characters in the type tags.
- Prefer canonical names. If the text mentions "ACME Corp" and "ACME", treat them as the same entity and use one canonical name.
- If the text contains no extractable entities or relationships, return {"entities": [], "relationships": []}.
- Output ONLY the JSON object. No prose before or after.

Source text:
{chunk_content}
