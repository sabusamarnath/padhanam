"""matcher_policy application — none yet (D186).

The policy surface is read and written through its ports directly by the two
sides of the seam (optimization writes, the matcher reads); there is no
orchestration to host here. The package exists for hexagonal-layer uniformity.
"""
