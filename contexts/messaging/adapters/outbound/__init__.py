"""Messaging outbound adapters (D129).

``twilio/`` — the Twilio Python SDK delivery adapter (WhatsApp).
``local_echo/`` — the local-first echo delivery adapter.
``postgres/`` — the Message persistence adapter.
"""
