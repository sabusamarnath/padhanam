"""Messaging adapters layer (D129).

Outbound adapters: the Twilio Python SDK delivery adapter, the
LocalEcho local-first delivery adapter, and the Postgres
persistence adapter. Vendor SDKs enter the codebase only here.
"""
