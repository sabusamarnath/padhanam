# Session Log

One entry per session. Append-only. Old entries archive at phase audits, never delete.

Format:
```
## Session N (Package P)
- Produced: [what shipped]
- Decisions: [D-numbers from decisions.md, or "none"]
- Tests: [pass/fail summary]
- Reflection: [one or two sentences. What was learned, what should change.]
```

---

## Phase 2 design 7-Step arc Step 2 commit
roles: analyst, PM, technical writer
mode: strategic (commit session landing Step 2 deliverables from the Claude.ai Step 2 conversation as appended `## Step 2: Disaggregate` section at `charter/phase-2-design-7step.md` plus current-package transition plus brief preservation; no code changes)

- Produced: Two commits closed the session.
  - Commit 1 (`docs(charter)`, 46e10b5) appends a new `## Step 2: Disaggregate` section to `charter/phase-2-design-7step.md` after the Step 1 close paragraph. The section carries seven sub-sections per the brief's spec: an opener naming the Disaggregator role's discipline (function-focused system_prompt plus McKinsey MECE override; Posture 1.5 dogfooding continued from Step 1) plus the two structural insights that emerged during the conversation (Branch 6 addition for platform-to-user signal fidelity; four-stage temporal lifecycle elevation to cross-cutting discipline); the issue tree at six branches × five sub-branches (Branch 1 portfolio existence; Branch 2 pace calibration; Branch 3 action at the right moment; Branch 4 feedback on calibration; Branch 5 trust substrate; Branch 6 signal fidelity and methodology-fit — thirty terminal sub-problems); cross-cutting disciplines (detection at every platform-user interface; the four-stage temporal lifecycle); MECE self-challenge naming closest cross-branch pairs (4.5-and-6.5; 1.5-and-2.4) held as distinct work units plus items deliberately excluded (agent runtime substrate; mass-market UX as design constraint; user onboarding); the Step 2 dogfooding-evidence record (what the McKinsey 7-Step Disaggregator role authored at S26b informed; where the temporal-lifecycle discipline and orthogonal-dimension handling extensions surfaced outside the role's system_prompt scope; the role's extensibility-without-breaking as procurement-grade evidence; methodology-extension workitem candidate as either role-system_prompt addition or Phase 2 skills-per-role surface); carry-forward to Step 3 with four open questions (dogfooding-only versus broader-population framing as prioritisation input; substrate-type × integration-function matrix at sub-branch 1.1; dependency ordering across branches with Branch 5 as partially-independent foundation; lifecycle-stage prioritisation across all branches versus full-lifecycle for fewer branches); Step 2 close paragraph. Appends a Step 2 close marker paragraph to `charter/current-package.md` after the Step 1 close marker, preserving chronological order per the file's append-only pattern. Creates `briefs/phase-2/design-7step-step-2.md` as a synthetic retrospective brief (flagged in the file itself because Step 2 transitioned from Step 1 close within the same Claude.ai conversation; no separate opening prompt existed). Creates `briefs/phase-2/design-7step-step-2-commit.md` preserving this commit-session prompt verbatim.
  - Commit 2 (this commit) lands the session log entry.

- Decisions: None at this session per AC 7. No new D-entries land. The two D-entries the Step 2 deliverables cite (D82 intelligence-layer commitment at sub-branch 3.5; D85 McKinsey 7-Step methodology authoring placement in the dogfooding-evidence record) are existing summary lines in active `charter/decisions.md` with full content at `docs/archive/decisions/phase-1.md`. Drift items continue to defer per Out of scope: D93 supersession; four-functions framework expansion; captures portfolio reactivation; product-methodology.md v2 primitives-versus-templates principle; Disaggregator role temporal-lifecycle extension (Phase 2 methodology workitem candidate now at two instances of role-scope drift); stale `current-package.md` line 5 header (still pre-existing structural drift, out of scope here).

- Tests: No code changes at this session; no test deltas. 1213 unit tests pass (unchanged from Step 1 commit close); 148 tenant_isolation contract scenarios; 18 pagination/error contract scenarios; 33 integration api tests; 27 import-linter contracts kept.

- Enforcement layer: No new contracts. Commit session ships no architectural surface.

- Reflection prompt 1: **Pre-write reconciliation outcome.** Six reconciliation items, with drift at item 2 and a new pattern at item 5. Item 1 (D-entry count) was clean: D113 stands; D82 and D85 cited; no new D-entries. Item 2 (current-package.md current text) was the load-bearing finding. The commit prompt said "Replace the 'Step 1 closed; Step 2 next' framing with 'Step 2 closed; Step 3 next.'" The file's pattern across six prior close markers (P11 through Step 1 commit) is append-not-replace; each new close marker adds a new paragraph rather than editing the prior one. Per the methodology document v3's section-level append-only discipline ("a mistaken D-entry is corrected by a new D-entry that supersedes it, not by editing the original"), the structurally honest interpretation is append: the Step 1 paragraph at line 35 was not mistaken (it was accurate at its commit time); the Step 2 paragraph supersedes it by being chronologically later. Proceeded with append. A drafting error caused the Step 2 paragraph to land before the Step 1 paragraph on the first Edit (reverse chronological order); caught on file inspection and corrected by Edit-swap before commit 1 staged. The replace-versus-append ambiguity is methodology line 1 below. Item 3 (append point at phase-2-design-7step.md) was clean: Step 1 close was terminal content; new Step 2 section appended without modifying Step 1. Item 4 (briefs/phase-2/ directory) was clean: existed from Step 1 commit. Item 5 (synthetic-retrospective-brief framing) was the new-pattern finding. Step 2 transitioned from Step 1 close to Step 2 open within the same Claude.ai conversation; no separate opening prompt existed. The retrospective brief documents intent, inputs, and discipline that would have lived in a pre-conversation brief. Flagged in the brief file itself as honest framing; methodology line 2 below carries the recurrence-test discipline. Item 6 (stale "P11 framed; S39 next" header at line 5) was stable: no further drift; pre-existing structural drift; out of scope per AC 7.

- Reflection prompt 2: **The two structural insights from Step 2.** Branch 6 (signal fidelity and methodology-fit as platform-to-user direction) and the four-stage temporal lifecycle elevation (find rhythm, settle in, watch, adapt as cross-cutting discipline) are structurally distinct insights with a shared methodology signal. Both surfaced because the Disaggregator role's authored discipline did not accommodate them; both extended the role's output without breaking its MECE override. The pattern matches Step 1's finding (the ProblemFramer role's system_prompt had narrower discipline than the conversation's five-sub-prompt expansion required). Two roles now show the same pattern: authored discipline scope narrower than work scope, role accommodates extension, work produces richer output. The case for landing the methodology-extension workitem as an early Phase 2 package strengthens materially. Phase 1 evidence at Step 1 was single-instance; Phase 1 evidence at Step 2 is two-instance (across two distinct roles); if Steps 3 through 7 continue the pattern, by Phase 2 framing the methodology-extension workitem will have five-to-seven-instance recurrence evidence — well past any structural-promotion threshold. The specific workitem framing is also now sharper: the Disaggregator role would benefit from a temporal-lifecycle discipline addition plus orthogonal-dimension handling; the candidate landing surfaces (system_prompt addition versus Phase 2 skills-per-role surface) are concrete enough for Phase 2 strategic-mode opening to settle. The workitem moves from hypothetical (something the methodology authoring might need) to scoped (what specifically and where).

- Reflection prompt 3: **Methodology-fit lifecycle sub-branch cross-branch implications.** Sub-branch 6.4 surfaces dependencies on Branches 1 (portfolio composition shifts at key-change moments), 4 (mirror format differs in rhythm versus key-change states), and 5 (trust required to ask "is this a key-change moment"). The question: should these cross-branch dependencies land as explicit cross-references in the issue tree at a future Step's refinement, or stay implicit until Step 3's prioritisation surfaces them. Verdict: leave implicit at Step 2; surface during Step 3 prioritisation reasoning. Two reasons. First, MECE discipline at the sub-branch level holds without cross-references: each sub-branch carries distinct work units, and the temporal-lifecycle cross-cutting discipline already names the lifecycle-stage behaviour applies at every branch; adding explicit dependency edges would make the output graph-shaped rather than tree-shaped, which exceeds the McKinsey override's "issue tree" framing. Second, dependency ordering is one of the four Step 2 open questions explicitly named for Step 3; surfacing dependencies now would pre-empt Step 3 work. Step 4 (workplan) is where explicit dependency artefacts will likely live if the prioritisation produces them; the issue tree stays tree-shaped at Step 2.

- Reflection prompt 4: **Synthetic-retrospective-brief pattern.** First instance at this session. The pattern emerged because the operator's Claude.ai usage continued from Step 1 close into Step 2 work within the same conversation, so no separate Step 2 opening prompt existed. The retrospective brief documents intent, inputs, and discipline that would have lived in a pre-conversation brief. Recurrence test at Step 3: if Step 3 opens with a formal pre-conversation brief at Claude.ai (the target shape; pre-conversation brief drafts before substantive Step 3 work begins), the synthetic-retrospective pattern stays at single instance. If Step 3 also transitions mid-conversation, the pattern lifts to second instance and earns methodology-candidate promotion. Worth surfacing now as discipline: the same-conversation-transition shape is partly a function of the operator's Claude.ai usage pattern (whether each Step gets a fresh conversation versus continuing from prior Step's close). The discipline-correction is to draft Step 3's pre-conversation brief intentionally before Claude.ai opens for Step 3, regardless of whether the conversation continues from Step 2. The brief at `briefs/phase-2/design-7step-step-3.md` lands as the test of whether the briefs/ discipline survives operator vigilance now that it has been re-established at Step 1.

- methodology (line 1): **Replace-versus-append ambiguity in commit-prompt wording when the underlying file has append-only-at-section-level discipline.** The Step 2 commit prompt said "Replace the 'Step 1 closed; Step 2 next' framing with 'Step 2 closed; Step 3 next.'" The file's pattern across six prior close markers is append-not-replace per the methodology document's section-level append-only discipline. The structurally honest interpretation is append, with the latest paragraph serving as the source of truth and the audit trail preserved by the prior paragraphs standing. Future commit prompts editing current-package.md or any append-only-at-section-level file should use "append a new close marker after the prior one" wording rather than "replace the framing" to avoid the ambiguity. First explicit instance; recurrence test at Step 3 commit prompt drafting (does the operator's commit-prompt convention shift to append-shaped wording, or does the ambiguity recur).

- methodology (line 2): **Synthetic-retrospective-brief pattern.** First instance at this session. Pattern emerges when a strategic-mode block transitions mid-conversation (from one Step to the next within the same Claude.ai conversation) and no separate opening prompt exists for the second block. The retrospective brief documents intent/inputs/discipline at the commit session rather than before the conversation. Honest framing requires the brief file to flag the synthetic-retrospective shape explicitly (done at the top of `briefs/phase-2/design-7step-step-2.md`). Recurrence test at Step 3: pre-conversation brief at `briefs/phase-2/design-7step-step-3.md` before Claude.ai conversation opens for Step 3 keeps the pattern at single instance and validates the discipline-correction; same-conversation transition lifts the pattern to second instance and promotes the candidate accordingly. Discipline-correction is forward-looking: draft Step 3's pre-conversation brief intentionally before opening Claude.ai for Step 3, regardless of whether the conversation continues from Step 2.

- methodology (line 3): **Methodology-template extensibility-without-breaking as procurement-grade evidence signal across two roles.** The Disaggregator role at Step 2 and the ProblemFramer role at Step 1 both have narrower authored discipline scope than the work each Step's conversation needed. In both cases the role's structural discipline accommodated operator-driven extensions without breaking. Pattern now consistent across two roles. The methodology-extension workitem (expand role system_prompts versus skills-per-role surface deferred per briefs/p8/mckinsey-7-step.md) moves from single-instance hypothetical to two-instance pattern-based candidate for Phase 2 framing. Recurrence test continues at Steps 3 through 7 (Prioritiser, Workplanner, Analyst, Synthesiser, Storyteller roles); if all seven McKinsey 7-Step roles surface the same pattern, the workitem lifts to high-priority Phase 2 framing input with five-to-seven-instance recurrence evidence well past any structural-promotion threshold.

- **Phase 2 design 7-Step arc Step 2 commit closed.** The next strategic-mode block is Phase 2 design 7-Step arc Step 3 (Prioritisation), opening at Claude.ai. Step 3 takes the thirty sub-problems plus the four open questions and produces the impact-tractability prioritisation, ranking sub-problems and surfacing the prioritised set as Step 4 (workplan) input plus the bets that land as Phase 2 LVT structure at the v6 roadmap entry per D44. The Step 3 pre-conversation brief should land at `briefs/phase-2/design-7step-step-3.md` before the Step 3 Claude.ai conversation opens, restoring full briefs/ discipline and testing the synthetic-retrospective-brief pattern's single-instance verdict.

---

## Phase 2 design 7-Step arc Step 1 commit
roles: analyst, PM, technical writer
mode: strategic (commit session landing Step 1 deliverables from the Claude.ai Step 1 conversation as charter-grade specification at `charter/phase-2-design-7step.md` plus current-package transition plus brief preservation; no code changes)

- Produced: Two commits closed the session.
  - Commit 1 (`docs(charter)`, 453cd00) creates `charter/phase-2-design-7step.md` carrying an arc-introduction header (charter-grade placement per the Step 1 conversation's Decision 1 making the arc binding for Phase 2 LVT structure at Step 3 close; blank-sheet within the bet's success criteria per Decision 2; Posture 1.5 dogfooding framing of the McKinsey 7-Step methodology template authored at S26b per D85, structured per D81 with the override-mode space at D87) plus Step 1's deliverables: problem statement paragraph (busy professionals working twelve-plus hour days carrying a portfolio of work and personal goals; the calibration layer breaks down under load); two context paragraphs (current substitutes solving slices; CoS analogue and the population gap); dogfooding-evidence record at five paragraphs (Posture 1.5 lead-in; what the ProblemFramer role's function-focused system_prompt informed via scope/situation/complication/success-criteria plus SCQ override layering; where four of the five conversation sub-prompts extend beyond the template's authored scope; the methodology-extension workitem this surfaces; the bet's methodology-embedding claim now carrying structural-level evidence with behavioural dogfooding still untested); initial-disaggregation paragraph with seven sub-problem candidates (judgment layer, mirror function, whisperer function, portfolio view persistence, user-authored methodology surface, personal-domain integration, substrate integration) plus Step 2 disaggregator handoff note; two open questions (Step 2 substrate-integration cut; Step 3 dogfooding-vs-broader-population prioritisation framing); Step 1 close paragraph. Adds a close marker paragraph to `charter/current-package.md` after the pre-Phase-2 architecture synthesis paragraph naming the two commits, the deliverable shape, the brief-preservation restoration, no-new-D-entries, and the explicit handoff to Step 2 as the next strategic-mode block. Preserves the Step 1 conversation prompt verbatim at `briefs/phase-2/design-7step-step-1.md` and this commit-session prompt verbatim at `briefs/phase-2/design-7step-step-1-commit.md` per D43.
  - Commit 2 (this commit) lands the session log entry.

- Decisions: None at this session per AC 7. No new D-entries land. The eight D-entries the Step 1 deliverables cite (D14, D77, D78, D81, D82, D85, D87, D93) are existing summary lines in active `charter/decisions.md` with full content at `docs/archive/decisions/phase-1.md`. Drift items deferred per Out of scope: D93 supersession (Step 3 commit or earlier if Step 2 forces); four-functions framework expansion (Phase 2 strategic-mode opening proper or Step 3 commit); captures portfolio reactivation (Phase 2 strategic-mode opening proper); Phase 2 methodology-extension workitem (Phase 2 strategic-mode opening proper as deferred-decisions entry).

- Tests: No code changes at this session; no test deltas. 1213 unit tests pass (unchanged from pre-Phase-2 architecture synthesis close); 148 tenant_isolation contract scenarios; 18 pagination/error contract scenarios; 33 integration api tests; 27 import-linter contracts kept.

- Enforcement layer: No new contracts. Commit session ships no architectural surface.

- Reflection prompt 1: **Pre-write reconciliation outcome.** Three of four reconciliation items surfaced drift. Item 1 (D-entry count and latest entry) was clean: D113 stands as latest; the eight cited D-entries exist with the meanings the deliverables require; no new D-entries land. Item 2 (current-package.md text) surfaced two observations: the most recent close paragraph at line 33 ends with "The next strategic-mode block is Phase 2 packaging" which the new close paragraph supersedes by appending; and the file still carries a stale top header "## P11 framed; S39 next" at line 5 from the original P11 framing despite the body having since absorbed P11-close, P12 audit, post-P12 hygiene, pre-Phase-2 synthesis, and now this commit's close. The stale top header is pre-existing structural drift outside this session's AC 7 scope; flagged for future hygiene, not fixed. Item 3 (brief preservation path) was the load-bearing finding. The prior three strategic-mode blocks (pre-Phase-2 architecture synthesis, post-P12 charter-discipline hygiene, P12 audit) did not preserve their briefs at briefs/. The most recent strategic-block briefs preserved at briefs/ are `briefs/p8/topology-design.md` and `briefs/p8/mckinsey-7-step.md`, both pre-P9. The convention has lapsed across three blocks since P9 open. The operator's restoration of the convention establishes `briefs/phase-2/` as the unit-of-work directory analogous to `briefs/p8/` and names the strategic blocks by slug (`design-7step-step-1.md`, `design-7step-step-1-commit.md`) so future Steps extend without restructuring. The discipline-correction observation is methodology-relevant: brief preservation convention does not survive without explicit operator restoration when strategic-block work accumulates across phase boundaries; this is methodology line 1 below. Item 4 (session log entry shape) was clean: this entry matches the structure of the most recent strategic-mode entries (Produced commit-by-commit, Decisions, Tests, Enforcement, Reflection prompts, methodology lines, close marker paragraph), scaled to the smaller commit count (two commits versus ten). A separate content-payload gap surfaced at session open and was load-bearing: the commit-session prompt was a template with eight content placeholders ("[problem paragraph verbatim from the Step 1 conversation close]" and seven similar) rather than a content-bearing prompt; the Step 1 deliverables and the Step 1 conversation prompt verbatim were not in the commit-session prompt as paste-ready substitutions. The operator restored both via a follow-up message before commit 1 could execute. This is methodology line 2 below.

- Reflection prompt 2: **Step 1 close as Phase 2 design entry.** Charter-grade placement per Decision 1 of the Step 1 conversation makes the 7-Step arc binding specification for Phase 2's strategic shape. Step 3's prioritised bets become the Phase 2 LVT structure landing as v6 roadmap entry per D44; the arc's full close produces the bets that Phase 2 packaging organises around. The commitment pressure on subsequent Steps is real: each Step now operates under the discipline of producing charter-grade content that survives audit, rather than producing notes-grade content that can be discarded if it does not converge. Subsequent Steps may surface friction with this pressure — for example, if Step 4 (synthesis of root causes) does not converge cleanly within a single strategic-mode session, the arc holds the open questions explicitly rather than defer to "later phases." The charter-grade placement is the structural commitment that the arc converges to landing; deferral to a later phase becomes a charter-superseding act rather than a quiet defer. Forward-relevance: the placement also makes the arc auditable at the next phase audit (Phase 2 close); a procurement reader can trace the arc's outputs through the LVT structure to the substrate Phase 2 builds, with each Step's deliverables sitting in the same charter file as the binding specification chain.

- Reflection prompt 3: **Methodology dogfooding-evidence record as a Phase 1 deliverable.** The bet's methodology-embedding claim now has its first structural-level evidence in committed charter form. The dogfooding-evidence record names specifically what the McKinsey 7-Step ProblemFramer role authored at S26b informed (function-focused system_prompt with scope/situation/complication/success-criteria shape; SCQ override layering as light additional discipline; the "you frame, not analyse" framing-versus-analysis distinction) and where the template's scope did not cover the conversation's expanded discipline (four of the five conversation sub-prompts — Who has the problem, What gets worse if the problem persists, How would success be measured, What is the problem NOT — extend beyond what the role's system_prompt encodes; only "What is the problem" maps directly). The record's honest framing is what makes it procurement-grade rather than performative: it names where the template earned its place and where it did not, and it logs the methodology-extension workitem (expand the role's system_prompt to encode the five-sub-prompt discipline, or land via the recommended-skills-per-role surface deferred to Phase 2 per `briefs/p8/mckinsey-7-step.md`). Phase 2 packaging benefits from the workitem being concrete and structurally located: it absorbs either as part of the methodology-as-product UX surface or as a separate methodology-extension session early in Phase 2. The case for landing it as an early Phase 2 package strengthens because structural-level evidence is the minimum bar, behavioural dogfooding (an actual ProblemFramer agent producing the same problem statement through the runtime) is the higher bar, and the methodology-extension workitem is on the path to that higher bar.

- Reflection prompt 4: **Blank-sheet discipline scope verdict.** Decision 2 of the Step 1 conversation chose option (a): blank-sheet within the bet's success criteria. Step 1's deliverables surface drift relative to two Phase 2-specific commitments. First, D93's methodology-as-product framing positions the audience as senior product leaders and consultancies; the Step 1 problem statement names "busy professionals working twelve-plus hour days" with a portfolio integrating work and personal goals, which is broader. The drift is not catastrophic — methodology-as-product can apply to either audience — but it signals that D93's audience framing may need supersession or refinement at Step 3 when prioritised bets land. Second, the four-functions framework at `charter/product-methodology.md` (PM, Marketing, L&D, PgM) covers professional functions only; the Step 1 problem statement implies personal-life portfolio integration that the four functions do not cover. Either a fifth function or a framework expansion will be needed; this is the deferred-to-Phase-2-strategic-opening workitem named in the prompt's Out of scope. The blank-sheet discipline scope held: the bet's core claims remain intact; the Phase 2-specific commitments are the layer surfacing drift, which is exactly where Decision 2 placed the scope. Step 2 (Disaggregation) or Step 3 (Prioritisation) likely absorbs the refinement work; expanding scope to question the bet itself at this point would reframe the entire arc and is not warranted by the Step 1 evidence. The scope choice was sound.

- methodology (line 1): **Briefs/ preservation convention does not survive without explicit operator restoration across strategic-block sequences.** Three blocks since P9 open (pre-Phase-2 architecture synthesis, post-P12 charter-discipline hygiene, P12 audit) did not preserve their briefs at `briefs/`. The operator's restoration at this Step 1 commit re-establishes the convention with the `briefs/phase-2/` directory and slug-based naming. The lapse-and-restoration shape suggests the convention needs structural support beyond memory: either a methodology-document explicit prescription for strategic-mode blocks (the existing Session brief preservation sub-section names the `briefs/<package>/<session>.md` format but does not enforce it across all session types and does not name the strategic-block slug pattern explicitly), or a mechanical-enforcement upgrade. Patterns-observed candidate: "Briefs/ preservation convention lapse pattern at strategic-mode block sequences"; first explicit instance logged here.

- methodology (line 2): **Placeholder-versus-content-payload miss in commit-session prompts authored against Claude.ai-conversation deliverables.** This commit-session prompt was structured as a template with eight content placeholders for content the operator provided in a follow-up message. The placeholder shape was honest (the prompt explicitly named what each placeholder slot expected) but the content was not pasted into the prompt before commit execution. Two viable conventions for future commit-session prompts: (a) author the prompt after the Claude.ai conversation closes, pasting deliverables inline so the prompt is content-bearing at the moment Claude Code starts; (b) author the prompt as a template with explicit pre-execution instruction ("commit-session prompt is a template; before Claude Code begins, paste content into placeholders"). The first is the structurally honest shape because the commit-session prompt is itself preservation-bearing per D43; preserving a template-with-placeholders is preserving incomplete content. The second has a robustness advantage when commit-session prompt authoring lags Claude.ai conversation close. Recurrence test continues at the next Claude.ai-conversation-to-Claude-Code commit-session transition (likely Step 2's commit session).

- **Phase 2 design 7-Step arc Step 1 commit closed.** The next strategic-mode block is Phase 2 design 7-Step arc Step 2 (Disaggregation), opening at Claude.ai. Step 2 takes the seven initial-disaggregation candidates and produces a MECE-decomposed issue tree per the McKinsey 7-Step Disaggregator role's discipline, addresses the Step 2 open question on substrate-integration cut, and lands its artefacts as a Step 2 section in `charter/phase-2-design-7step.md` via a subsequent commit session. Step 3 prioritises and produces the bets that land as Phase 2 LVT structure at the v6 roadmap entry per D44 plus the Phase 2 PRD per D43.

---

## Pre-Phase-2 architecture synthesis and methodology refinement
roles: technical writer, architect, analyst
mode: strategic (synthesis session between post-P12 charter-discipline hygiene close and Phase 2 packaging open; produces architecture.md v1 plus three methodology document refinements; no code changes)

- Produced: Ten commits closed the session.
  - Commit 1 (`docs(charter)`, 6622668) appends an "Updates since P12 close" section at `charter/p12-phase-2-inputs.md` with four sub-sections: post-P12 charter-discipline hygiene completion (seven commits dated, three discipline conventions, 22 principles annotated, deferred-decisions reorganisation, D1-D113 archival, D106 reservation, one entry retired, one pre-write reconciliation finding); refreshed Phase 2 entry handoff artefact list (nine artefacts including the new archive and pending architecture.md); Phase 2-entry substrate hygiene workitems status refresh (gold-set audit-emission, graph-extract reliability, schema.md formalisation, doc-content rebrand); pre-Phase-2-framing pending work explicit (architecture.md authoring and methodology refinements at this session).
  - Commit 2 (`docs(charter)`, d5a491d) creates `charter/architecture.md` with seven-section skeleton (Overview, Architectural patterns, Tenancy and jurisdiction, Vendor and dependency posture, Domain primitives, The four-context substrate, Cross-document map) plus populated content for the Overview and Cross-document map sections. Overview synthesises bet plus procurement-grade-architecture-as-test-condition framing; Cross-document map establishes the three-document relationship (principles for binding rules with D-entry references; decisions for full Choice/Reasoning/Alternatives/Kano with per-phase archival; architecture for synthesis with diagrams) plus pointers to methodology and product-methodology.
  - Commit 3 (`docs(charter)`, 0b9f411) populates Architectural patterns and Tenancy and jurisdiction sections. Architectural patterns covers hexagonal architecture (with diagram of single-context layered structure), bounded contexts at the top of the codebase (with diagram of twelve bounded contexts plus padhanam/ plus shared_kernel/), observability as foundation, binding specifications live in charter. Tenancy and jurisdiction covers database-per-tenant topology (with diagram of control plane Postgres plus per-tenant data plane Postgres instances plus TenantContext propagation), jurisdiction as first-class architectural attribute, tenant onboarding as configuration not deployment, customer-specific behaviour as configuration.
  - Commit 4 (`docs(charter)`, 78095d5) populates Vendor and dependency posture and Domain primitives sections. Vendor and dependency posture covers vendor flexibility (with diagram of vendor port-adapter pattern showing domain ports, concrete adapters per vendor, external systems), LLM-provider-agnostic via LiteLLM, hybrid retrieval via pgvector and Neo4j, OTel as observability portability boundary, hash-chain primitive at the platform layer. Domain primitives covers role-first agent identity, methodology as defaults plus envelopes, four-layer constraint stack (with diagram showing platform invariants as universal floor, methodology opt-in via lineage, workflow opt-in via invocation context, agent layer per-instance choices), recommendation-shaped optimisation output.
  - Commit 5 (`docs(charter)`, 1063791) populates The four-context substrate section with the fifth Mermaid diagram showing producer contexts (retrieval_evaluation, run_history, audit, ingestion) producing evidence, optimization context consuming via reader ports, rules generating recommendations with evidence citations, HTTP transport with OpenAPI as procurement-grade documentation. Section covers the four producer contexts with their D-entry references and substrate shape, the optimization consumer with pluggable rule and metric abstractions, the HTTP transport surface combining S34/S37/S38/S42 routes, and end-to-end procurement-grade traceability through the platform's own tooling.
  - Commit 6 (`docs(charter)`, c7de15c) introduces Foundation super-section at the methodology document top grouping What Padhanam is, What's being investigated, and the new Hypothesis and iteration sub-section. What Padhanam is compressed for platform-side content via a pointer to architecture.md; methodology-side elaboration preserved. New Hypothesis and iteration sub-section establishes the iteration cycle (hypothesis → evidence → audit → revision) as the methodological scaffold, names every charter artefact as situated within the cycle, and names the methodology document's own version progression as a living hypothesis revised across phase boundaries.
  - Commit 7 (`docs(charter)`, 5c15117) introduces Work organisation super-section replacing the prior Work hierarchy + Frameworks standalone H2 sections plus consolidating the Session shape + Session shapes H2 content. Six H3 sub-sections: Strategic tree (existing Work hierarchy content); Phase lifecycle (NEW); Package lifecycle (NEW); Session lifecycle (NEW); Session shapes (existing v2 content preserved); Frameworks (existing LVT/Kano/RICE content reframed against the lifecycle vocabulary). Session brief preservation sub-section under Two-surface mode separation remains in place; Session lifecycle cross-references it.
  - Commit 8 (`docs(charter)`, 834f09a) adds explicit hypothesis-framing paragraph at the top of the Measurement model section establishing that the metrics are themselves part of the hypothesis being tested; the "What the numbers are not" sub-section cross-references the framing.
  - Commit 9 (`docs(charter)`, 7f7a9a1) adds three-document pointer at the top of `charter/principles.md` (architecture.md for synthesis with diagrams; decisions.md active phase plus per-phase archives for full reasoning; methodology.md for build discipline). Charter structure sub-section in methodology.md updates the Strategic constants list to include architecture.md as a fourth Strategic constant; list entries gain short reading-audience annotations. Methodology document version log gains v3 entry recording the Foundation, Work organisation, and Measurement model refinements.
  - Commit 10 (this commit) lands the close marker on `charter/current-package.md` plus this session log entry.

- Decisions: None at this session. Architectural commitments referenced in architecture.md are existing D-entries (D1-D113); no new D-entries land. The three methodology document refinements (Foundation hypothesis-iteration, Work organisation lifecycle expansion, Measurement model hypothesis-framing) are documented in the v3 version log entry rather than as new D-entries — methodology document refinements live at the document's version log per the established pattern.

- Tests: No code changes at this session; no test deltas. 1213 unit tests pass (unchanged from pre-P12 hygiene); 148 tenant_isolation contract scenarios; 18 pagination/error contract scenarios; 33 integration api tests; 27 import-linter contracts kept.

- Enforcement layer: No new contracts. Synthesis session ships no architectural surface.

- Findings outcomes per the brief's pre-write reconciliation framing:

  - **Finding 1 (architecture.md depth scope).** Operator-decision option (a) executed autonomously per the recommend-(a) framing. The four content commits (3 through 5) produced procurement-grade synthesis from inception across seven sections with five Mermaid diagrams. Architecture.md final size approximately 250 lines (Overview 25, Cross-document map 20, Architectural patterns 65, Tenancy and jurisdiction 45, Vendor and dependency posture 60, Domain primitives 50, four-context substrate 65 with the largest single diagram). The synthesis read-density is high enough that a procurement reader at Phase 2 entry can understand the architecture without consulting decisions.md; v2 evolution at Phase 2 close will accrete substrate-completion details (gold-set audit emission, graph-extract reclaim policy when those land) and Phase 2 substrate sections.

  - **Finding 2 (methodology refinement scope coupling).** Three refinements landed without surfacing deeper restructuring concerns. Foundation super-section creation absorbed the existing two H2 sections (What Padhanam is, What's being investigated) plus the new Hypothesis and iteration sub-section cleanly under one H2. Work organisation super-section restructure consolidated four H2s (Work hierarchy, Frameworks, Session shape, Session shapes) into six H3 sub-sections under one H2; existing content preserved verbatim within new sub-section structure; new Phase lifecycle, Package lifecycle, and Session lifecycle sub-sections drafted from scratch with substantive prose. Measurement model hypothesis-framing single-paragraph insertion plus cross-reference at "What the numbers are not". No operator engagement required; the disposition rule applied autonomously.

  - **Finding 3 (diagram syntax).** Mermaid chosen per the brief's autonomous-disposition rule; renders in GitHub markdown without server-side dependencies; markdown-embedded; accessible to non-technical readers viewing raw files. Diagrams use `flowchart TB` and `flowchart LR` shapes with subgraph clustering where load-bearing (the four-context substrate diagram has four subgraphs grouping producers, consumer, rules, transport).

  - **Finding 4 (cross-reference granularity to D-entries from architecture.md).** Each architectural commitment in architecture.md cross-references either the principle (for binding rules; see `charter/principles.md` ...) or the D-entry by number with the convention "per D<n>" — since D1-D113 are now archived to `docs/archive/decisions/phase-1.md`, the active surface that readers reach by following a D-entry number is the archive. Architecture.md does not include explicit `docs/archive/decisions/phase-1.md` path links per D-entry to keep the prose readable; the principles.md three-document pointer establishes the navigation convention at the entry surface and readers follow the D-entry references with the archive-path as canonical destination.

- Reflection prompt 1: **Architecture.md as procurement-grade synthesis verdict.** The seven-section structure held up under content authoring without surfacing structural refinements during writing. The five Mermaid diagrams carry the load-bearing structural information that prose alone would require multiple paragraphs each to convey: the twelve-bounded-context structure plus padhanam/ plus shared_kernel/ surfaces as a single diagram in Architectural patterns; the database-per-tenant topology with TenantContext propagation arrows surfaces in Tenancy and jurisdiction; the vendor port-adapter pattern with concrete adapters per vendor surfaces in Vendor and dependency posture; the four-layer constraint stack ordering (platform invariants as floor, methodology opt-in, workflow opt-in, agent layer per-instance) surfaces in Domain primitives; the four-context evaluation-to-recommendation pipeline with HTTP transport surfaces the bet's criterion-4 demonstration in The four-context substrate. The Cross-document map section is the structural anchor that makes architecture.md sit cleanly in the charter rather than overlapping unhelpfully with principles.md or decisions.md; the three-document relationship is clean (principles for binding rules; decisions for full reasoning; architecture for synthesis with diagrams) and the principles header pointer added at commit 9 plus the methodology Charter structure update establish the relationship across surfaces. Forward-relevance: Phase 2 close audit reviews architecture.md against the as-built Phase 2 substrate; v2 will accrete the workflow context substrate per D83, the four producer→consumer-UX-layer pipeline if Phase 2 surfaces a fifth context, and any Phase 2-period architectural commitments that don't fit current sections cleanly. Sections likely to need v2 refinement: The four-context substrate (Phase 2 workflow + UX layer additions); Domain primitives (Phase 2 may add UX-layer domain concepts). Sections likely to remain stable through Phase 2: Architectural patterns (the hexagonal-and-bounded-contexts shape is bet-grade architecture); Tenancy and jurisdiction (the database-per-tenant + jurisdiction commitments are bet-grade); Vendor and dependency posture (the vendor-flexibility commitment is bet-grade per D111).

- Reflection prompt 2: **Methodology refinements coherence verdict.** The three refinements integrated cleanly with v2 super-section grouping. Foundation super-section landed as a natural grouping for content that was already conceptually grouped but not structurally; the new Hypothesis and iteration sub-section reads as a natural extension of "What's being investigated" rather than as a substantial reframing — the iteration cycle was implicit throughout the v2 document and v3 makes it explicit. Work organisation super-section restructure was the most substantive change; the six-sub-section structure (Strategic tree, Phase lifecycle, Package lifecycle, Session lifecycle, Session shapes, Frameworks) makes lifecycle disciplines visible without bloating the document because the new sub-sections (Phase lifecycle, Package lifecycle, Session lifecycle) replace prose that was previously distributed across multiple H2s. Measurement model hypothesis-framing is a one-paragraph addition that operates as a meta-commitment about the model itself; the framing was implicit at v2 and v3 makes it explicit. The methodology document and architecture.md are properly delimited: methodology covers how Padhanam itself is built (operator discipline, build process, framing-prompt patterns, refactoring conventions, session shapes, measurement model); architecture covers what Padhanam is built as (architectural commitments, patterns, contexts, vendor posture, domain primitives, substrate scaffolding). No overlap; the Cross-document map in architecture.md and the Charter structure sub-section in methodology.md establish the boundary explicitly. Future hygiene at Phase 2 audit may surface additional refinement candidates if Phase 2 evidence demands; the v3 hypothesis is that Foundation + Work organisation + Measurement model refinements absorb the post-P12 evidence and the document is sustainable through Phase 2 without further structural change.

- Reflection prompt 3: **Three-document relationship operationalisation.** The three-document relationship holds up structurally at this session's close. The principles.md header pointer (architecture.md for synthesis; decisions.md for full reasoning; methodology.md for build discipline) establishes the navigation convention at the entry surface readers reach first. The methodology document's Charter structure sub-section names architecture.md as a fourth Strategic constant alongside bet.md, principles.md, methodology.md; the strategic-constants framing makes the four-document relationship visible. The architecture.md Cross-document map section names principles + decisions + architecture as the three reading-mode triad (session-open compact; cold-path audit-time; warm-path synthesis-time) and names methodology + product-methodology as adjacent surfaces (build methodology; product methodology layer for users). Together the three cross-references describe a coherent reading order: at session open, read principles.md (compact rules); for full reasoning, read decisions.md (active phase) or the per-phase archive; for synthesis, read architecture.md (with diagrams); for build discipline, read methodology.md; for product methodology, read product-methodology.md. Each document has a distinct audience and a distinct reading moment. Phase 2 framing benefits from reading architecture.md alongside the bet, the PRFAQ, principles.md, decisions.md (active), and the consolidated Phase 2 inputs file: architecture.md gives the procurement-grade-touch synthesis surface that Phase 2 framing's strategic-mode block reads as the structural anchor for "what we have built and how it relates to the proposition". Forward-relevance for Phase 2 close: architecture.md v2 absorbs Phase 2 substrate additions; methodology.md v4 absorbs Phase 2 discipline-pattern evidence; principles.md gains new principles as Phase 2 surfaces commitments worth promoting to binding rules; decisions.md (Phase 2 active) accumulates Phase 2 D-entries until Phase 2 close, at which point Phase 2 D-entries archive to `docs/archive/decisions/phase-2.md` per the per-phase archival pattern.

- methodology (line 1): **Pre-Phase-2 strategic-mode session is a third hygiene-shape instance.** First instance was pre-P12 hygiene at P11 close (six commits, structural-finding focus). Second instance was post-P12 charter-discipline hygiene at the post-audit boundary (seven commits, discipline-convention focus). Third instance is this session (ten commits, synthesis-and-refinement focus). The accumulation of three hygiene-shape sessions at three different temporal positions (mid-P11-close, post-audit, pre-Phase-2-framing) reinforces the methodology document v2's framing that hygiene sessions plan by default at phase boundaries. The three instances also surface a sub-shape distinction worth naming for future audit observation: pre-audit hygiene (P11 close) addresses substrate-session debt; post-audit hygiene (post-P12) addresses charter-discipline debt the audit surfaced but explicitly scoped out; pre-next-phase hygiene (this session) addresses synthesis-and-cleaned-state-for-framing needs that the audit-output state doesn't fully satisfy. The three sub-shapes may consolidate or stay distinct at Phase 2 close audit observation.

- methodology (line 2): **Architecture.md as new charter surface absorbs synthesis-density load that principles.md cannot carry without losing scannability and decisions.md cannot carry without losing per-D-entry independence.** The three-document relationship resolves a tension implicit at Phase 1 that surfaced explicitly at P12 close: principles.md is read every session and must stay tight; decisions.md carries 113 D-entries each with Choice/Reasoning/Alternatives/Kano and is structurally optimised for per-entry audit-trail not for cross-entry synthesis. Procurement-grade-touch reading requires synthesis with diagrams that neither surface naturally accommodates. Architecture.md as a third synthesis surface fills the gap. The Cross-document map in architecture.md plus the principles header pointer plus the methodology Charter structure update operationalise the relationship across all three surfaces; the convention scales to v2 of architecture.md at Phase 2 close. Phase 2 close audit reviews the relationship's coherence under Phase 2 evidence (does architecture.md still synthesise cleanly when Phase 2 adds UX layer concepts and workflow context substrate? does the principles header pointer remain accurate? does the methodology Charter structure list need expansion?).

- methodology (line 3): **Methodology document refinements absorbed structurally without v3 surfacing tensions.** The v3 hypothesis is that Foundation hypothesis-iteration, Work organisation lifecycle expansion, and Measurement model hypothesis-framing make the document's hypothesis-iteration nature visible at the structural level rather than only at the content level. v1 articulated the discipline; v2 absorbed Phase 1 evidence into Patterns-observed entries; v3 surfaces the iteration mechanic explicitly so that Phase 2's evidence has a structural place to land. The hypothesis-as-living-cycle framing in Hypothesis and iteration sub-section makes the methodology's revision posture explicit ("the methodology document is a living hypothesis revised across phase boundaries"); the Work organisation lifecycle structure makes phase-and-package-and-session boundaries visible at the document's level rather than only at the project's level; the Measurement model hypothesis-framing makes the metric model itself a hypothesis. Forward-relevance: Phase 2 close audit reviews whether v3's structural reframing actually absorbs Phase 2's evidence without further structural change, or whether v4 needs additional super-sections (e.g., a dedicated cross-cutting Patterns super-section grouping Patterns-observed plus Failure-modes-observed plus What-Padhanam-does-not-do plus the principle sub-sections at the document's tail).

- **Pre-Phase-2 architecture synthesis and methodology refinement closed.** The next strategic-mode block is Phase 2 packaging. Phase 2 framing reads the four-document charter (bet, principles with three-document pointer at top, architecture, methodology v3) plus the consolidated `charter/p12-phase-2-inputs.md` handoff section (which now includes the Updates since P12 close section listing all post-P12 work). Phase 2 framing produces a v6 roadmap entry per D44, a Phase 2 PRD per D43, and the first Phase 2 package's epic note per D43.

---

## Post-P12 charter-discipline hygiene
roles: technical writer, architect, analyst
mode: strategic (post-audit hygiene session; closes the ten-item charter-discipline punch list between P12 audit close and Phase 2 packaging open; no code changes)

- Produced: Seven commits closed the session.
  - Commit 1 (`docs(charter)`, 344569d) lands three new discipline conventions at `charter/methodology.md`. A "Per-phase decisions archival pattern" sub-section under Charter structure mirroring D107's per-package session-log archival pattern at phase granularity. A "Deferred-entry closure by D-entry" sub-section under Append-only discipline naming the Status header line + append-only body preservation convention. A "Principles-D-entry reference convention" sub-section under Charter structure codifying the parenthetical-reference shape where principles restate D-entry commitments.
  - Commit 2 (`docs(charter)`, 366c252) audits each methodology document section against "does this elaborate beyond what principles plus D-entry already commit?" Compression verdict: the document earns its place as the narrative companion to principles+decisions. The three named borderline candidates (Frameworks, Append-only discipline, session-shape vocabulary) all preserve verbatim because their reasoning content (the "why") doesn't duplicate the "what" of principles+D-entries. Cross-reference refactor lands a "Mechanical enforcement upgrades" sub-section under Architectural enforcement carrying the discipline articulation for each of the seven upgrades plus a cross-reference back to the deferred-decisions entry for activation triggers; phase-audit-template extension paragraph naming the edge-case-hunter procedural checklist under the Reflection and learning section; refined cross-reference at the fabrication-class Failure modes entry to point at the specific proper-noun-attribution-check item within the deferred entry.
  - Commit 3 (`docs(charter)`, 1298b3a) audits every principle in `charter/principles.md` for D-entry restatement and adds parenthetical references where the relationship is load-bearing. Twenty-two principles gained references; existing references (D40, D41, D42, D43, D44, D45, D46, D47, D81, D82, D86, D91, D107, D111, D113) preserved. Multi-D references at five principles (D4+D16 for hexagonal, D1+D32 for database-per-tenant, D4+D15 for LiteLLM+Ollama, D14+D76 for customer-specific behaviour, D16+D17+D28 for bounded contexts) reflect the principle restating commitments from multiple D-entries; the brief's Finding 2 anticipated this and the autonomous-disposition rule applied with the multi-D pattern as the structurally honest shape.
  - Commit 4 (`docs(charter)`, 143a53e) refreshes three stale-framing references in `charter/deferred-decisions.md` per the P12 audit Finding T2 disposition. The "Methodology metrics" entry (two occurrences of "pending operator authorship per D39") and the "Full DORA and CORE4 instrumentation" entry (one occurrence) refresh to D113's living-hypothesis framing. The "Methodology mechanical-enforcement upgrades" preamble refreshes the principle-landings language to point at the new methodology-document Mechanical enforcement upgrades sub-section. The preamble of the file gains a sentence naming the closure convention so future readers know to look for Status header lines. The "HTTP API for ingestion management" entry gains the Status header "Status: closed by D104, 2026-05-14". Pre-write reconciliation finding at commit time: the brief named a parallel "HTTP API for evaluation management" entry for closure marking with D112, but no dedicated entry exists in deferred-decisions.md — the topic is named only as a short-form carryover in `charter/current-package.md`. D112 absorbed the work; no Status marker landed because no entry exists to mark, which is the structurally honest state under the append-only discipline (retroactively adding an entry just to mark it closed would be ceremony).
  - Commit 5 (`docs(charter)`, 863eee8) reorganises `charter/deferred-decisions.md` into eight themed sub-sections with a Table of contents at top. Sub-sections in order: Architectural primitives awaiting activation; Phase 2 substrate completion; Production-deployment readiness; Workflow context extensions; Methodology and governance enhancements; Compliance and security; Tool registry and authoring; Phase 1 close audit findings. Entry content preserved verbatim per the brief's "append-only at entry level holds" framing; only structural reorganisation lands. Header depth shifted: sub-sections at H2, entries demoted from H2 to H3, the existing Data-plane ownership sub-sub-section demoted from H3 to H4 under Orchestration architecture. Per the brief's Finding 3, borderline placements (Per-tenant supply-chain surveillance → Compliance and security; Scheduled-runs primitive → Architectural primitives; Optimization-engine cost-per-successful-task threshold tuning → Phase 1 close audit findings) settled autonomously per the most-load-bearing cluster.
  - Commit 6 (`docs(charter)`, 0e02676) lands D106 reservation note inline between D105 and D107 summaries in the active `charter/decisions.md` and archives D1 through D113 to `docs/archive/decisions/phase-1.md` per the methodology document's per-phase archival pattern. The active file replaces D1-D113 detail with one-line summaries plus archive pointers; working size drops from 1589 lines to 136 lines (well below the working-files-stay-tight intent of <400 lines per AC 4). The archive preserves the full Choice / Reasoning / Alternatives / Kano content verbatim. The active file header explains the archival pattern; new Phase 2 D-entries land below the D113 entry until Phase 2 close per the per-phase archival cadence.
  - Commit 7 (this commit) lands the session log entry plus the close marker on `charter/current-package.md`.

- Decisions: None at this session. Charter-discipline hygiene work produces no new D-entries; the three discipline conventions land at the methodology document not as new D-entries per the brief's framing. The D106 reservation note is metadata, not a new commitment. No principles edits beyond reference parentheticals. No deferred-decisions entries beyond the existing P12-audit refreshes.

- Tests: No code changes at this session; no test deltas. 1213 unit tests pass (unchanged from pre-P12 hygiene); 148 tenant_isolation contract scenarios; 18 pagination/error contract scenarios; 33 integration api tests; 27 import-linter contracts kept.

- Enforcement layer: No new contracts. Charter-discipline hygiene ships no architectural surface.

- Findings outcomes captured per the brief's pre-write reconciliation framing:

  - **Finding 1 (methodology document compression scope).** The maintenance pass audit confirmed every section earns its place. The three named borderline candidates resolved cleanly without operator engagement: Frameworks elaborates the "why" behind D42 (decision-discipline reasoning at the framework-moment-of-the-work granularity, not D42's "what was decided"); Append-only discipline elaborates the "why" behind D29 (the S8 incident, the case-study audit-trail value, the symmetric-correction-of-errors framing); Session shape vs Session shapes address distinct topics (the cadence vs the named-shape vocabulary). No substantive compression landed; the substantive work was the three cross-reference additions (Mechanical enforcement upgrades sub-section under Architectural enforcement, phase-audit-template extension under Reflection and learning, refined Failure-modes cross-reference at the fabrication-class entry).

  - **Finding 2 (D-entry reference precision in principles audit).** Five principles required multi-D references reflecting combined commitments. The structurally honest pattern is to list both D-entries in the parenthetical (D4+D16 for hexagonal where D4 is no-vendor-SDKs and D16 is bounded-contexts-on-hexagonal; D1+D32 for database-per-tenant where D1 is the model and D32 is the per-tenant instance topology; D4+D15 for LiteLLM+Ollama where D4 is the gateway and D15 is the model default; D14+D76 for customer-specific behaviour where D14 is the original commitment and D76 is the forking-phrasing refinement; D16+D17+D28 for bounded contexts where D16 is the codebase structure, D17 is the cross-context communication shape, and D28 is the cross-cutting padhanam/ package). Single-D references at fourteen principles. The autonomous-disposition rule applied to all twenty-two reference additions without operator engagement because the load-bearing D-entry for each principle was unambiguous on inspection.

  - **Finding 3 (deferred-decisions thematic cluster placement edge cases).** Forty-three entries mapped cleanly to one of the eight clusters. Three placement edge cases the brief anticipated resolved autonomously: Per-tenant supply-chain surveillance for tenant-supplied tools placed at Compliance and security (the "surveillance" mechanism is the load-bearing concern, not the "tools" subject); Scheduled-runs primitive placed at Architectural primitives (the platform-vs-external-trigger choice is the load-bearing decision, not the consumer use case); Optimization-engine cost-per-successful-task threshold tuning placed at Phase 1 close audit findings (the entry was added during the P12 audit per audit-inputs entry 8 disposition, grouping it with other P12-audit-touched entries was the structurally honest cluster). No operator engagement on placement; the cluster-choice was clear at inspection.

- Pre-write reconciliation finding at commit 4: the brief named "HTTP API for evaluation management" alongside "HTTP API for ingestion management" for Status closure marking. Codebase inspection surfaced that no dedicated deferred-decisions entry exists for evaluation-management HTTP; the topic is named only as a short-form carryover in `charter/current-package.md`. D112 absorbed the work at S42. The structurally honest move is to mark only the ingestion-management entry (the entry that exists) rather than retroactively author an entry just to mark it closed. The finding mirrors the mid-build pre-write reconciliation sub-pattern from the methodology document v2 — brief-vs-codebase reconciliation surfacing at execution time, resolved with explicit disposition recorded in the commit message and this session log entry. Recurrence test continues: the methodology document v2's Patterns-observed entry on pre-write reconciliation absorbed this instance as the seventeenth-plus instance spanning S6 through this session.

- Reflection prompt 1: **Charter discipline conventions operationalisation.** Three new conventions landed at the methodology document as natural extensions of existing content. The per-phase decisions archival pattern reads as a clean parallel to D107's per-package session-log pattern (same shape at a different temporal granularity; the discipline is "archival cadence matches the natural review boundary"); the deferred-entry closure-by-D-entry convention reads as a natural specialisation of the existing append-only discipline (closed entries remain visible with explicit status rather than disappearing or editing); the principles-D-entry reference convention codifies a pattern already present at five principles before this session (D82, D86, D107, D111, D113) and generalises to the twenty-two newly-referenced principles at commit 3. None of the three required new sub-section structure beyond what the existing Charter structure / Append-only discipline sections accommodated; the conventions landed as sub-sub-sections within existing scope. Forward-relevance for Phase 2 inheritance: the conventions are structured well enough that Phase 2 sessions inherit them as discipline rather than as ceremonial pattern. The decisions-archival pattern in particular will fire at Phase 2 close when D-entries from Phase 2 archive to `docs/archive/decisions/phase-2.md`; the per-phase-summary-plus-archive-pointer format established at commit 6 carries forward as the canonical pattern. The conventions function as charter substrate the next phase audit reads against without re-derivation.

- Reflection prompt 2: **Methodology document maintenance pass observations.** Finding 1's "compression candidate" framing presupposed that some sections might fail the earning-place test. The audit found none. Three observations carry forward to the next methodology document maintenance moment. First, the document's earning-place posture is structurally durable because the document was authored from inception as the narrative companion to principles+decisions; the document doesn't repeat the "what" of D-entries (their Choice text is the canonical source) and doesn't repeat the "what" of principles (their concise statements are the canonical source); the document carries the "why" content (reasoning, recurrence counts, mechanism, examples) that the other two surfaces structurally exclude. Second, the document's size is sustainable through Phase 2 if the same earning-place posture holds; v2 grew by ~120 lines at the P12 audit through six new Patterns-observed entries plus the Session shapes sub-section, and v3 at Phase 2 close will grow by a similar magnitude through Phase 2's accumulated discipline-pattern observations. The compression pattern in the brief was anticipatory rather than necessary; future maintenance passes may surface real compression candidates if the document accretes elaboration that doesn't earn its place (e.g., a sub-section that restates a D-entry's reasoning rather than narrating the "why" beyond the D-entry). Third, the cross-reference refactor at commit 2 (Mechanical enforcement upgrades sub-section under Architectural enforcement) demonstrates a pattern worth naming: when the methodology document's discipline-articulation lives at a deferred-decisions entry, both surfaces benefit from bidirectional cross-references (the discipline articulation at the methodology document; the activation triggers at the deferred-decisions entry). The pattern generalises beyond mechanical-enforcement upgrades; any methodology discipline that has both a "what is the discipline" surface and a "when does it activate" surface fits the bidirectional-cross-reference pattern.

- Reflection prompt 3: **Decisions archival pattern observations.** First execution of the per-phase decisions archival pattern at Phase 1 close. The archival cleanly reduced the active `charter/decisions.md` from 1589 lines to 136 lines, well below the AC 4 target of <400 lines. The one-line-summary plus archive-pointer format reads well at session-open (every D-entry's title, package, session, and date are visible without scrolling more than a few screens; supersession relationships and revision relationships called out inline; archive pointer at the file header rather than per-line keeps the surface tight). The format proved expressive enough to call out supersession and revision relationships inline (D3 superseded by D52; D14's forking phrasing refined by D76; D28's package-name supersession by D38; D45's voice/audience supersession by D51 and D93; D88's sync signature revised by D90; D74's v2 at D81; D29's product-name supersession by D38; D39's pending-authorship framing superseded by D113). Three observations carry forward to Phase 2 close. First, the per-phase archival cadence is the right granularity for a project this size; per-session archival would be too fine, per-bet (every-N-phases) would be too coarse and would let the working file accrete unsustainably. Second, the summary format may evolve to be richer for entries with significant supersession or revision relationships (the inline "Superseded by ..." or "Revised at ..." annotations are useful but could become structured fields if Phase 2 introduces enough supersession to make the relationships hard to track); Phase 2 close audit reviews based on accumulated evidence. Third, the archival file at `docs/archive/decisions/phase-1.md` should not be edited after this commit per the append-only discipline; corrections to D-entry content from this point forward land via new D-entries that supersede the original, both in the archive (the original D-entry stands untouched) and as a new summary line in the active file pointing at the new D-entry's archive location. The active file's summary lines for archived D-entries are themselves append-only-at-the-summary-line level (the title and metadata may be corrected for accuracy but the entry is not removed or substantively edited; the canonical D-entry content lives at the archive).

- methodology (line 1): **Hygiene session at the phase boundary is the second instance of the hygiene-shaped session pattern.** The first instance was pre-P12 hygiene at P11 close (six commits, six items, structural-finding documentation). This second instance fires at the boundary between P12 audit close and Phase 2 packaging open (seven commits, ten items, charter-discipline focus). Recurrence rate: two instances at two phase boundaries (P11 close, post-P12-audit) within the same phase (Phase 1). The methodology document v2's Session shapes sub-section names hygiene as a recognized shape; the second instance reinforces the planning-by-default framing for Phase 2 (substrate-session work consistently produces hygiene-shaped end-of-package debt; planning hygiene sessions in advance is structurally honest). Distinguishing characteristic of the post-audit-hygiene sub-shape vs the end-of-package-hygiene sub-shape: post-audit-hygiene addresses charter-discipline debt surfaced *during* the audit but explicitly out-of-scope of the audit per the audit's no-code-changes framing; end-of-package-hygiene addresses substrate-session debt accumulated during the package's build sessions. Both qualify as hygiene-shaped sessions in the v2 methodology document's framing; the distinction is descriptive sub-classification rather than separate shape promotion.

- methodology (line 2): **Pre-write reconciliation fired once at commit 4 (HTTP API for evaluation management entry missing).** Mid-execution finding rather than session-open finding per the v2 methodology document's pre-write reconciliation sub-pattern framing. The brief named two Status-marker actions at commit 4 (ingestion-management closed by D104; evaluation-management closed by D112); codebase inspection at execution time surfaced that no dedicated entry exists for evaluation-management. Resolution: mark the one that exists, document the gap in the commit message, surface in the session log entry. The cost of the reconciliation moment was small (one paragraph in the commit message, two paragraphs here); the alternative of retroactively creating an entry just to mark it closed would have been ceremony against the append-only discipline. The methodology document's pre-write reconciliation pattern absorbed this instance as the seventeenth-plus instance; recurrence test continues at Phase 2 substrate sessions.

- methodology (line 3): **Charter-discipline conventions naturally fold into existing methodology document structure.** All three new conventions landed under existing sub-sections (Charter structure for two; Append-only discipline for one) rather than as standalone sub-sections. The structural pattern: discipline conventions that scale across multiple charter surfaces fit cleanly under the existing structural sub-sections that already organize those surfaces. Forward-relevance: future discipline conventions (Phase 2 may surface new ones during substrate or audit work) inherit the pattern; the methodology document's existing Charter structure / Append-only discipline / Architectural enforcement / Reflection and learning sub-sections accommodate convention-shaped content without sub-section proliferation. The methodology document scales by accreting content within existing sub-sections, not by adding new sub-sections at the top level; the top-level structure is the methodology's architectural shape and is stable across phases.

- **Post-P12 charter-discipline hygiene closed.** The next strategic-mode block is Phase 2 packaging. Phase 2 framing reads the cleaned charter surface (decisions.md at 136 lines, deferred-decisions.md reorganised into eight themed sub-sections with table of contents, methodology document v2 with the three new conventions and cross-references, principles.md with D-entry references throughout) plus the artefacts named at `charter/p12-phase-2-inputs.md` Phase 2 entry handoff section. Phase 2 framing produces a v6 roadmap entry per D44, a Phase 2 PRD per D43, and the first Phase 2 package's epic note per D43. The post-P12 hygiene work is complete; the charter surface is ready for Phase 2 framing to read against without re-derivation.

---

## P12 — Phase 1 close audit
roles: analyst, PM, architect, technical writer
mode: strategic (audit-mode session; closes P11 session-level work and Phase 1 substrate-completion)

- Produced: Eleven commits closed P12.
  - Commit 1 (`docs(charter)`, c73318f) lands the P12-in-flight marker on `charter/current-package.md` and creates `charter/p12-audit-findings.md` as the audit's consolidated deliverable with skeleton structure for three findings sections, an audit-surfaced-entries section, and a cross-reference index.
  - Commit 2 (`docs(audit)`, b89fd55) populates section 1 of the findings document with top-down verification of D1 through D112 against as-built reality. Nine findings surface (T1 through T9); two are substantive (T2 D39 stale framing requires new D-entry; T1 D109 correction preserves audit trail of pre-write reconciliation miss); T3 through T9 are non-action with existing supersession or deferred-decisions resolution already in place. Findings-not-surfaced summary confirms the bet's substrate commitments (hexagonal per D16, no-vendor-in-domain per D4, audit-as-context per D26, database-per-tenant per D32, jurisdiction-as-first-class per D12, tenant-isolation per D24, methodology evolution D74-D86, agent runtime D88-D90, four-context P11 substrate D108-D112) all ship matching D-entry shape.
  - Commit 3 (`docs(audit)`, 6661927) populates section 2 with bottom-up tour of `contexts/`, `apps/`, `padhanam/`, `tests/`. Seven findings (B1 through B7); none indicate code shipped without charter authorisation. B1 documents the twelve-bounded-context count; B2 names the operator-utility audit test-event endpoint; B3 surfaces TenantContext and ErrorResponse shape gaps in schema.md (deferred to Phase 2 entry hygiene); B4 verifies 27 import-linter contracts + 11 AST enforcement tests + 148 tenant-isolation contract scenarios + 18 HTTP contract scenarios + 33 integration API tests; B5 names accumulated test harness conventions; B6 confirms migration counts (13 control-plane + 15 per-tenant) match D-entry authorisation; B7 captures the load-bearing verdict that no code-level drift surfaced.
  - Commit 4 (`docs(audit)`, 7f5e8a2) populates sections 3 and 4 with explicit dispositions for all 14 audit-inputs entries plus three audit-surfaced entries. Disposition breakdown: 7 methodology-document (entries 1, 2, 4, 6, 7, 11, 14), 3 deferred-decisions-refresh (8, 10, 13), 1 deferred-decisions-hold (9), 3 explicit-non-action (3, 5, 12), 3 audit-surfaced (15 D39 stale framing, 16 twelve-bounded-context count, 17 schema.md gap). Cross-reference index maps every finding to its resolution location. Audit verdict summary closes against the bet's six load-bearing claims at Phase 1 close.
  - Commit 5 (`docs(charter)`, c8749ed) lands D113 superseding D39's pending-authorship framing without rewriting D39. The methodology document was authored at v1 at P3 post-close; the audit's commit 8 lifts to v2. Resolves P12 Finding T2 plus audit-surfaced entry 15.
  - Commit 6 (`docs(charter)`, b898ce0) refreshes `charter/principles.md` line 45 per D113 supersession, removing the stale "(pending operator authorship per D39)" framing in the engineering-practice principle naming the methodology measurement substrate.
  - Commit 7 (`docs(charter)`, 7455672) lands three deferred-decisions updates: new entry "Optimization-engine cost-per-successful-task threshold tuning" formalising D111's starter-value posture (entry 8); refresh "Gold-set aggregate-level audit emission" with Phase 2-entry-hygiene workitem framing (entry 10); refresh "Graph-extract pipeline reliability" with P12-audit-reviewed-and-confirms-hold note (entry 13).
  - Commit 8 (`docs(methodology)`, 0a384c2) lifts the methodology document from v1 to v2 with Phase 1's accumulated discipline-pattern data points. Two new content blocks: a "Session shapes" sub-section naming five shapes (substrate, bridge, transport, hygiene, audit) with distinguishing-characteristics paragraphs; six new dated Patterns-observed entries (pre-write reconciliation as architectural discovery with mid-build sub-pattern; principle-versus-framing drift; ship-tooling-with-smoke-exercise; metric-threshold expectations need structural-understanding grounding; reproducibility artefacts land at the session that ships them; methodology-candidate accumulation and audit-resolution rhythm). Version log v2 entry records the evolution.
  - Commit 9 (`docs(charter)`, f02bf0a) lands `charter/p12-phase-2-inputs.md` with deferred-item-to-package-candidate mapping, architectural rework observations, and strategic-tree input observations per the audit's Finding 3 high-level-granularity disposition.
  - Commit 10 (`docs(charter)`, 3f71a63) lands the P12-audit-closed marker on `charter/current-package.md` and updates `charter/packages.md` to reflect P11 closed and P12 as audit-mode session replacing the originally-planned active-testing scheduler build.
  - Commit 11 (this commit) lands the session log entry.

- Decisions: D113 landed at commit 5. No new principles; the vendor-flexibility principle at S41 plus the User safety section at D82 already cover Phase 1's binding-principle surface. Three deferred-decisions refreshes plus one new deferred-decisions entry. Methodology document v2 substantive update (six Patterns-observed entries plus Session shapes sub-section).

- Tests: No code changes at P12; no test deltas. 1213 unit tests pass (unchanged from pre-P12 hygiene); 148 tenant_isolation contract scenarios; 18 pagination/error contract scenarios; 33 integration api tests; 27 import-linter contracts kept.

- Enforcement layer: No new contracts. P12 is strategic-mode synthesis; no architectural surface ships.

- Reflection prompt 1: **Phase 1 close verdict.** The bet's six load-bearing claims hold end-to-end at Phase 1 close. (1) A single tenant runs locally with the full stack: verified via `make up` plus per-tenant database routing per D32; the live-stack smoke at every P11 session reinforces. (2) One agent can be configured, run, audited, and optimised through the platform's own tooling: S30b demonstrated the configure-and-run surface for the LVT PM agent against tenant alpha producing a SMART problem statement; the McKinsey 7-Step ProblemFramer at tenant alpha producing a full Lean Value Tree closes the run surface; S37 demonstrated the audit surface; S41 demonstrated the optimise surface with rule-derived recommendations citing producer-context records with full evidence traceability. (3) The evaluation harness produces meaningful quality signals: S40 retrieval evaluation runner against the S40b clean gold-set produces recall@k differentials that distinguish retrieval-quality state across vector_only and graph_only strategies, with the all-zero CaveatAnnotation on graph_only flagging the infrastructure substrate state transparently per S40b's S41-evidence verdict. (4) The trace capture layer surfaces optimization recommendations, not just data: S41 produced run `3b538acb-...` with 2 retrieval_strategy recommendations citing S40b's c168c2ba and S39b's ef58678a both carrying CaveatAnnotation on graph_only's all-zero aggregates; cost_optimization substrate-honest zero emission against local dev costs; model_choice and prompt_revision skipped with structured substrate-gap reasons. A procurement reader can trace prose → rule → citation → producer-context records without ambiguity. The criterion's full close requires the Phase 2 UX deliverable per D93. (5) The operator can explain every architectural decision and why it was made: the decisions file at 112 D-entries each carrying Choice + Reasoning + Alternatives Considered + Kano is the demonstration artefact; the audit reads against this surface and verifies it against the as-built codebase. (6) The methodology document captures the architect-implementer pattern with enough specificity that another senior product leader could read it and adopt the discipline: the methodology document's substantive v2 update at this audit's commit 8 lifts the document from v1 articulation to v2 with Phase 1's accumulated discipline-pattern data points integrated; the document is the proprietary insight the platform demonstrates. Phase 1 closes with the substrate intact, the architectural discipline holding under measurement, and the methodology evidence ready for Phase 2 to consume as the substrate it builds against per D93's methodology-as-product positioning.

- Reflection prompt 2: **Audit-as-strategic-mode-session-shape methodology observation.** This session is the first phase audit at this project. The strategic-mode synthesis session shape held up cleanly as distinct from build sessions and from other strategic-mode work (framing decisions, charter authorship). Three observations carry forward to Phase 2 audit. First, the three-track structure (top-down D-entry verification, bottom-up codebase tour, audit-input disposition) provided complementary signal density at different surfaces; top-down surfaced one substantive drift finding (T2 D39 stale framing) plus eight non-action confirmations, bottom-up surfaced no code-level drift (the load-bearing verdict B7) plus six documentation-shaped observations, audit-input disposition consolidated fourteen P11-session candidates into seven methodology-document promotions plus four deferred-decisions actions plus three non-action confirmations. The three tracks did not duplicate effort; they did not conflict; their outputs composed into the cross-reference index cleanly. Second, the commit-shape granularity per the brief's eleven-commit split mapped cleanly onto the synthesis work — the commits grouped by destination file rather than chronologically per the brief, which produced git-log coherence at audit time. The skeleton-first-then-progressive-section-population pattern at commits 1 through 4 preserved the audit-trail value of seeing the findings document's evolution rather than landing it as a single all-content commit. Third, the no-code-changes-at-P12 framing held cleanly through the synthesis work; code-level corrections from audit findings (gold-set audit-emission back-fill, graph-extract reclaim policy) forward to follow-on sessions per the deferred-decisions refreshes, with the audit-findings document and the Phase 2 inputs file naming them with operational specificity for those sessions to inherit. Forward-relevance to Phase 2 audit: the audit-shape session works as a session shape; the methodology document v2 names it as the fifth session shape alongside substrate, bridge, transport, and hygiene; Phase 2 audit at the next phase boundary inherits the three-track structure plus the eleven-ish-commit-shape plus the no-code-changes framing plus the disposition vocabulary plus the audit-trail discipline (supersession via new D-entry rather than in-place edit, append-only correction sections on D-entries with build-time deviation, audit-findings document as the consolidated deliverable). The shape is reusable without rework at Phase 2 boundary.

- Reflection prompt 3: **Audit findings synthesis observations — signal density across the two surfaces.** Top-down D-entry verification produced higher signal density than bottom-up codebase tour, against my session-open expectation that they would produce equivalent volume. Top-down surfaced two substantive findings (T1 D109 audit-trail discipline; T2 D39 stale framing) plus seven non-action confirmations, totalling nine entries. Bottom-up surfaced seven entries but only one rose to substantive-finding altitude (B3 schema.md formalisation gap), with the other six landing as documentation-shape observations (twelve-context count B1, operator-utility endpoint B2, enforcement counts B4, test harness conventions B5, migration counts B6, no-drift verdict B7). The bottom-up surface's lower substantive density reflects the codebase's actually-clean-state: no work shipped without charter authorisation; the principles file's commitments hold against the codebase; the methodology's "architectural discipline survives AI-assisted implementation" load-bearing claim verified at structural level. The categories anticipated in pre-write reconciliation (commitment-versus-execution differences, partial implementation, silent supersession, abandoned alternatives, schema drift) held up at top-down but each fired at lower frequency than expected — silent supersession surfaced once (D14 wording at D76; explicit supersession at the time it was made), partial implementation surfaced zero times, abandoned alternatives surfaced zero times in the audit-relevant scope, schema drift surfaced once (T2 D39 framing; the only stale-framing finding). The category surprise the audit surfaced was the D39-pending-authorship-framing-as-charter-binding-residue: doc-content rebrand at pre-P12 hygiene did not propagate to charter binding-specification surfaces, which surfaced as five-plus drift sites at audit time. The lesson generalises beyond methodology document: doc-content edits at hygiene moments need explicit propagation-or-quarantine framing for charter binding-specification surfaces. The methodology document v2 absorbs the lesson into the "session shapes" sub-section's hygiene-session paragraph naming structural-finding documentation as part of the shape's deliverable surface. Forward-relevance to future audits: the top-down-versus-bottom-up signal asymmetry holds when the codebase tracks the charter cleanly; the asymmetry would invert if the codebase ships ahead of charter (the inverse drift pattern), which is a different methodology concern than what Phase 1 produces. Phase 2 audit at the next phase boundary reads against this pattern and assesses whether Phase 2's substrate-completion-versus-consumer-UX shape produces a different signal-asymmetry profile.

- methodology (line 1): **Audit-shape session is the fifth session shape per the methodology document v2 update.** The session log entry's existence under audit-mode declaration is itself the first instance of the audit shape; the methodology document's session shapes sub-section names the shape's distinguishing characteristics (three-track structure, no-code-changes framing, commit-grouping-by-destination-file rather than chronological, the disposition vocabulary, the audit-trail discipline). Phase 2 audit inherits the shape vocabulary at next-phase-boundary entry.

- methodology (line 2): **Methodology-candidate accumulation and audit-resolution rhythm fired as designed.** P11's six sessions accumulated fourteen methodology candidates consolidated at `charter/p12-audit-inputs.md` by the pre-P12 hygiene session; the P12 audit's commit-4 disposition mapped each candidate to one of four shapes (seven methodology-document promotions, three deferred-decisions refreshes, one deferred-decisions hold, three explicit non-action, plus three audit-surfaced entries beyond the original fourteen). The pattern's first explicit instance verified that the accumulation-then-audit rhythm distributes candidate-recognition work across the package while compressing synthesis into a single strategic-mode block at the phase boundary; methodology document v2 absorbs this as a Patterns-observed entry. Phase 2 work inherits the rhythm.

- methodology (line 3): **D113 supersession-without-rewrite discipline reinforces the existing append-only commitment.** D39 stands; D113 records the supersession explicitly; the principles.md amendment at commit 6 brings the binding-specification surface into accuracy without editing D39 in place. The pattern mirrors D52 (Phase 2 deferral of identity foundation superseding D3) and D76 (forking-principle phrasing refinement superseding D14's prior "forbidden" wording). The audit-trail value compounds at each instance: future readers see what was true at each point in time, including what was authored as "pending" before authorship completed; the methodology's evidentiary surface is the supersession trail itself, not just the latest state.

- **P11 plus P12 close Phase 1.** The next strategic-mode block is Phase 2 packaging. The packaging conversation reads the six artefacts named at `charter/p12-phase-2-inputs.md` Phase 2 entry handoff section (audit-findings document, audit-inputs file, methodology document v2, decisions.md including D113, deferred-decisions.md including the three refreshes, this inputs file) plus the bet, the PRFAQ at the candidate Phase 2-direction articulation, the principles file, and the roadmap at v5. Phase 2 framing produces a v6 roadmap entry plus a Phase 2 PRD plus the first Phase 2 package's epic note.

---

## Pre-P12 hygiene — P11 close consolidation and structural-finding capture
roles: analyst, technical writer, engineer
mode: build (P11 hygiene session; closes P11 session-level work before P12 phase audit)

- Produced: Six commits closed the pre-P12 hygiene session.
  - Commit 1 (`docs(charter)`, 41f33d7) lands the in-flight marker on `charter/current-package.md` and creates `charter/p12-audit-inputs.md` consolidating fourteen methodology candidates and architectural-honesty observations forwarded from S39 / S39b / S40 / S40b / S41 / S42 plus the hygiene-session investigation outcomes. The file frames each entry as observation + evidence + decision shape; names four disposition types P12 audit may apply (charter amendment, methodology-document entry, deferred-decisions entry, explicit non-action). Input material for P12 audit; reduces audit's discovery surface so the audit can focus on resolution and methodology synthesis.
  - Commit 2 (`docs(readme)`, f9c41f1) expands the repository-root README per the four operator-confirmed dispositions. Methodology framing repositioned as living hypothesis revised by session data points and audit findings (replacing the stale "pending operator authorship per D39" framing). `charter/schema.md` and `charter/deferred-decisions.md` added to the charter reading order at positions 5 and 6 (schema after decisions per the binding-specification pairing; deferred-decisions after schema as the activation-trigger surface). Apache 2.0 repositioned under a new "The strategic commitment" section: "The platform is the demonstration; the methodology is the proprietary insight" — open-sourcing as load-bearing for the bet. Two new sections: "What the platform does at Phase 1 close" enumerating the six producer contexts (agent, ingestion, run_history, audit, retrieval_evaluation, optimization) plus the two applications (cli, api); "Operating the platform" covering install dependencies, start the stack via `make up`, apply migrations via `make migrate`, seed dev tenants via `make seed-tenants`, exercise the CLI surface via `docker compose exec padhanam-api python -m apps.cli.main` plus the three Makefile shortcuts (eval-run / ingest-run / ingest-worker), exercise the HTTP surface at `https://localhost/openapi.json` with reference to the S42 smoke walkthrough.
  - Commit 3 (`chore(p11/hygiene)`, db285f8) refreshes the compose.yaml padhanam-api digest pin from sha256:fa829a07277c... to sha256:aa21431690ab... via re-running `make build-api`. The post-S41 target's S42-fix (commit ac5a6bf, replacing `docker compose build` with direct `docker build` to avoid docker compose build's rejection of digest-tagged image directives) holds; the refresh is the verification step rather than a new fix. Same commit removes the stray `apps/api/{routers}/` literal-braces empty directory (dated 2026-04-30, surfaced at S42 Finding 1 investigation as a fat-finger artefact unrelated to the actual routers/ tree). The directory removal is invisible to git but the on-disk state is now clean.
  - Commit 4 (`docs(charter)`, 44f3897) lands the graph-extract pipeline reliability structural finding at `charter/deferred-decisions.md` per Finding 1 disposition. The EXTRACTING state in `contexts/ingestion/domain/state.py` has no reclaim-after-timeout transition; a worker dying mid-extraction (LLM-call hang, container kill, transaction-rollback edge case) leaves the source stuck in EXTRACTING indefinitely. The extraction LLM call at `contexts/ingestion/adapters/outbound/extraction/litellm_extractor.py` has no timeout enforcement at the worker boundary either. Three structural pieces missing: reclaim policy (EXTRACTING-for-longer-than-timeout → PENDING_EXTRACT with retry counter, terminal EXTRACTION_FAILED at reclaim_limit_exceeded), worker-side asyncio timeout enforcement around the extractor call, optimistic-concurrency-control on the reclaim transition for split-brain safety against concurrent workers. Operational mitigation at Phase 1: manual re-trigger via CLI (SELECT then forced UPDATE of stuck sources; procurement-grade-invisible because no audit event emission — part of the reliability finding the structural reclaim policy resolves). Activation trigger: any Phase 1 close or Phase 2 work that depends on graph retrieval producing entities reliably; production deployment with multiple worker processes amplifies the failure mode.
  - Commit 5 (`fix(p11/hygiene)`, 4dc4c8c) loosens the S37 audit reader validator at `contexts/audit/domain/audit_event_record.py` to accept empty correlation_id as legitimate "no inbound HTTP context" signal. The original S37/D102 commitment required non-empty correlation_id across all six text fields; cross-context audit semantics drift surfaced at S42 smoke time when audit rows emitted by the retrieval_evaluation runner (S40/D110) and the optimization engine (S41/D111) carried correlation_id="" because engine-internal events have no inbound HTTP request context. The S37 reader's list route returned 500 against any tenant_audit chain containing such rows. The loosening preserves all other invariants (five required-non-empty text fields, the type contract on correlation_id, the 64-lowercase-hex hash validation, the tenant_id shape commitment); two new unit tests verify the accept path and the still-raises-on-None path. The recommended-(a) disposition from the S42 captures entry held; the alternative-(b) backfill migration was rejected because the empty-string state is structurally meaningful and a backfill would inject false data to satisfy an overly-strict validator.
  - Commit 6 (this commit) lands the session log entry plus the close marker on `charter/current-package.md`.

- Decisions: None at this session. Hygiene work surfaces structural findings (graph-extract reliability lands at `charter/deferred-decisions.md` per the brief's disposition rule, not as a new D-entry) and methodology candidates (consolidated at `charter/p12-audit-inputs.md` for P12 to resolve). No new charter principles; no new D-entries.

- Tests: 1213 unit tests pass at session close (+1 from S42's 1212; one parametrize case removed for correlation_id, two new explicit tests added — net +1). 148 tenant_isolation contract scenarios unchanged; 18 pagination/error contract scenarios unchanged; 33 integration api tests unchanged. 27 import-linter contracts kept.

- Enforcement layer: No new contracts. The hygiene session ships no new architectural surface; the validator loosening is a one-line change preserving the type contract; the structural finding is documented rather than implemented.

- Findings outcomes captured per the brief's reflection prompts:

  - **Finding 1 (graph-extract investigation outcome).** Structural, not bounded. The investigation surfaced the EXTRACTING state's lack of reclaim-after-timeout transition as the root cause. A worker dying mid-extraction leaves the source stuck indefinitely; no other worker picks the row up. The extraction LLM call has no worker-boundary timeout either. The bounded-fix possibilities the brief named (worker timeout adjustment alone, retry semantics layered on top of existing transitions, queue-state edge case) were inadequate because they don't address the missing reclaim transition; a worker-side timeout adjustment alone leaves the row in EXTRACTION_FAILED on every transient failure (which inflates failure rates against real reliability), and retry semantics layered on EXTRACTION_FAILED would require a separate reset-failed-to-pending transition that doesn't exist. The structural reclaim policy is the structurally honest fix; bounded fixes would be incidental scope expansion masquerading as the right shape. The disposition's bounded-or-document rule held cleanly.

  - **Finding 2 (CLI/API wiring parity outcome).** Defensible asymmetry; no backfill needed. The brief's premise of `tests/integration/apps/api/test_wiring.py` doesn't exist (path mismatch; api tests live at `tests/integration/api/`), and neither side has a single dedicated `test_wiring.py` file. The actual surfaces:
    - CLI side: explicit cross-context adapter tests at `tests/integration/apps/cli/test_cross_context_adapters.py` (eight test functions covering the methodology/role/source/agent-retrieval adapters) and `tests/integration/apps/cli/test_cross_context_tool_adapters.py` (tool-definitions and tool-invoker adapters) plus dedicated reader-adapter tests at `test_run_history_reader_adapter.py` and `test_audit_event_reader_adapter.py`. The pattern: construct adapters with fake repositories and verify the consumer-port-plus-wiring-adapter boundary translates correctly.
    - API side: route-level integration tests at `tests/integration/api/test_*_routes.py` (eight test files post-S42) that exercise the wiring through TestClient plus FastAPI dependency_overrides. The pattern: substitute the adapter at the dependency-injection seam and verify the route behaves correctly end-to-end.
    The asymmetry is defensible because the two surfaces test wiring at different altitudes appropriate to each app's entry point. CLI-side tests verify adapter-construction correctness at the consumer-port-plus-wiring-adapter boundary (where the CLI's per-command construction model means adapter shape matters); API-side tests verify the same wiring through the full HTTP stack (where FastAPI dependency injection plus the request-scoped session resolution model means stack-shape matters more than construction-shape). No backfill needed for procurement-grade defensibility.

- methodology (line 1): **Hygiene-session-as-shape methodology observation.** This is the first explicit hygiene-shaped session in P11. Distinct from substrate sessions (ship architectural surface — S39, S40, S41), bridge sessions (verification-and-hygiene between substrate sessions — S39b, S40b), and transport sessions (HTTP layer atop existing substrate — S42). Hygiene sessions consolidate end-of-package debt: documentation expansion, methodology-candidate consolidation, dev-tooling verification, structural-finding documentation, stray-artefact cleanup. The shape's distinguishing characteristics that surfaced at execution: smaller per-commit scope than substrate sessions (six commits, each tightly scoped); mixed commit prefixes per work nature (`docs(charter)`, `docs(readme)`, `chore(p11/hygiene)`, `fix(p11/hygiene)`); no new D-entries (the hygiene session does not produce binding architectural commitments); structural findings forwarded to deferred-decisions rather than fixed in-session per the bounded-fix-or-document disposition rule. Open question for P12 audit: is the hygiene-shaped session pattern materially different from a bridge session, or is it a bridge session at a different temporal position (P11 close versus mid-substrate-package)? My read at session close: it's distinct because the temporal position changes what work qualifies as "hygiene". Mid-package bridges address substrate-session debt (corpus state, gold-set authoring contamination); end-of-package hygiene addresses cross-package debt (methodology-candidate consolidation, README expansion, structural findings that accumulated across multiple sessions). Forward-relevance for Phase 2: hygiene sessions at phase boundaries should be planned by default rather than reactive; the methodology candidate at `charter/p12-audit-inputs.md` entry 14 captures the question.

- methodology (line 2): **Investigation outcomes summary — bounded-fix-or-document rule held cleanly.** Two findings surfaced at this session; both followed the brief's disposition rule without operator engagement. Finding 1 (graph-extract) surfaced as structural after inspection of `contexts/ingestion/domain/state.py` and `contexts/ingestion/application/extract_source.py`; the bounded-fix-possibilities the brief named were genuinely inadequate, not surface-level "this looks bigger than expected" misjudgment. Finding 2 (CLI/API wiring parity) surfaced as the brief's premise being partially wrong (no `test_wiring.py` files at either side) but the underlying concern (parity coverage across the eighteen wiring class pairs) being met by different test surfaces at different altitudes. Both outcomes are structurally honest, not "this is too hard so document" deflection. The disposition rule's value at this session: it gave CC a clear escalation criterion (engage operator only if structural finding requires substantive refactor exceeding the bounded-fix budget OR if backfill scope exceeds ~50 lines) without which the temptation to bound the graph-extract fix at the worker-timeout-adjustment surface might have been substantial.

- methodology (line 3): **Methodology-candidates consolidation observations.** The `charter/p12-audit-inputs.md` file is the first structured input set for P12 audit. The fourteen-entry structured-entry format (observation + evidence + decision shape implied) surfaced a clean separation between entries that are clearly methodology-document candidates (entries 1, 2, 3, 4, 14: pre-write reconciliation, principle-versus-framing drift, scope-check-at-substrate-application-boundary, bridge-session pattern, hygiene-session-as-shape), entries that are clearly charter-amendment candidates (entries 5, 6: vendor-flexibility operationalisation verdict, container-image-lag pattern closed), entries that are clearly deferred-decisions candidates (entries 8, 9, 10, 13: cost-threshold tuning, parallel_rrf, gold-set audit emission, graph-extract reliability), and entries that are explicit-non-action candidates (entries 7, 11, 12: MRR threshold lesson is one-time, S39b corpus version-control gap is captured-not-repeating, CLI/API wiring asymmetry is defensible). The clean separation suggests the structured format does support P12 audit's resolution work without biasing toward certain disposition types. Forward-relevance: P12 audit may add audit-surfaced entries (e.g., observations the audit conversation itself surfaces about session-shape distribution or methodology-pattern recurrence rates); the file's append-only-between-audits convention with the P12-resolved archive section at audit close keeps the file useful across phases without unbounded growth.

- **P11 session-level work closed.** Pre-P12 hygiene is the last session of P11; the next strategic-mode block is the P12 phase audit. P12 acts on the fourteen entries at `charter/p12-audit-inputs.md` plus any audit-surfaced additions per the four disposition types. The audit's output artefact updates `charter/p12-audit-inputs.md` with per-entry disposition annotations plus any new entries; charter-amendment dispositions land at the appropriate charter file (decisions.md, principles.md, schema.md, deferred-decisions.md) in the same audit-close commit chain. The Phase 1 close audit then assesses Phase 1's procurement-grade-readiness across the full surface (P1 through P12); this hygiene session's outputs flow into that assessment.

---

## S42 — HTTP transport surface for the retrieval_evaluation and optimization producer-context substrates
roles: analyst, PM, architect, engineer, technical writer
mode: build (P11 final substrate-surface session; completes the four-context consumer HTTP surface)

- Produced: Nine commits closed S42.
  - Commit 1 (`docs(charter)`, f080b76) lands D112 at `charter/decisions.md` tail with five coupled commitments. Scope reduced from the brief's four-context framing to the two missing producer contexts (retrieval_evaluation + optimization) after the Finding 1 session-open investigation confirmed run_history HTTP routes shipped at S34 (D98) and audit HTTP routes shipped at S37 (D103); the brief's three scope options collapse to identical work. Flat-module router convention (routes at `apps/api/routers/<context>.py`, DTOs at `_<context>_dto.py`, query parsers at `_<context>_query.py`) adopted per Finding 5 deviation from the brief's per-context-subdirectory framing — five-of-five existing surfaces use the flat convention. JWT-resolved tenancy per the S15/S37 hardening pattern. Cursor pagination matching the reader-layer shape. OpenAPI specification as procurement-grade Phase 2 UX-consumer documentation. Nine alternatives considered. `charter/current-package.md` gains the S42-in-flight marker (transformed to S42-closed at commit 9).
  - Commit 2 (`feat(p11/s42)`, dc1d2e4) lands the Pydantic v2 DTOs and query parsers at the flat convention. Two DTO modules: `_retrieval_evaluation_dto.py` covers gold-set authoring shapes (CreateGoldSetRequest/Response, GoldSetWithRevision, AppendEntry, FinalizeRevision), the two-step discovery RetrievalCandidate response, and the evaluation-run snapshot envelope; `_optimization_dto.py` covers the Recommendation aggregate with a Pydantic discriminated union on evidence_citations keyed by `category` (retrieval_strategy / cost_optimization), the OptimizationRun with skipped_categories, and the lifecycle TransitionResponse. Two query parsers mirror the existing run_history/audit/ingestion shape with PAGE_SIZE_CEILING (50) from the domain layer (the brief's 200 framing was forward-affordance the domain layer does not support today; following codebase reality per Finding 6 deviation). `_errors.py` registers two new handler groups: `register_retrieval_evaluation_error_handlers` covers six error-code paths (gold_set_not_found 404, no_draft_to_finalize 409, empty_draft 400, gold_set_missing_finalized_revision 400, evaluation_run_not_found 404, malformed_retrieval_evaluation_cursor 400); `register_optimization_error_handlers` covers five (recommendation_not_found 404, recommendation_transition_not_permitted 409 with structured from_status/to_status details, optimization_run_not_found 404, malformed_optimization_cursor 400, invalid_optimization_filter 400). 46 new unit tests verify DTO round-trips and query-parser validation.
  - Commit 3 (`feat(p11/s42)`, 56c83a7) lands the retrieval_evaluation routes at three FastAPI routers. `gold_set_router` (/gold-sets) covers create, list, get, append-entry, finalize with principal subject from the JWT becoming `created_by_user_id`. `discovery_router` (/retrieval-candidates) is Stage 1 of the two-step decomposition surfacing ranked candidate chunks for the operator to pick from before posting the entry. `evaluation_run_router` (/evaluation-runs) covers synchronous kickoff (returns the completed snapshot in one response per Finding 3), list, and get. The kickoff threads principal.subject to invoked_by_user_id and BinaryRelevanceMetrics() as the default MetricCalculator. 18 integration tests cover happy paths, error-code paths, synchronous-kickoff audit-chain anchoring, and principal-subject threading.
  - Commit 4 (`feat(p11/s42)`, c6e0572) lands the optimization routes at two FastAPI routers. `optimization_run_router` (/optimization-runs) covers synchronous engine kickoff plus the read surface. The kickoff route wires `default_rules()` against an EvidenceContext built from the four producer-context reader ports pulled from app.state; with empty evidence the rules-engine produces zero recommendations plus structured CategorySkipReason entries for model_choice and prompt_revision per D111 commitment 5. `recommendation_router` (/recommendations) covers filtered/paginated list, single-resource read, and the three lifecycle transition routes (acknowledge / apply / reject); transition routes carry empty bodies and thread principal.subject as actor. The 409 path surfaces details on from_status / to_status. Recommendation responses surface evidence_citations as the discriminated union on the wire. 15 integration tests.
  - Commit 5 (`feat(p11/s42)`, d298c47) lands the wiring at the API composition root. `AppCompositions` gains ten new optional seams (gold_set repository + reader, evaluation_run repository + reader, optimization_run repository + reader, recommendation repository + reader, retrieval_client, retrieval_runner_port). `_agent_runtime_wiring.py` gains `build_retrieval_client` (returns a standalone TenantRoutingRetrievalClient for the discovery route) and `HttpRetrievalRunnerPort` (wraps an AgentRetrievalClient as a RetrievalRunnerPort for the synchronous evaluation-run kickoff — mirrors the CLI's _CliRetrievalRunnerPort shape). `_build_default_compositions` calls each existing S39/S40/S41 builder plus the new two; `create_app` includes the five new routers and registers the two new error-handler groups. 27 import-linter contracts kept; 1290 unit + API integration tests pass.
  - Commit 6 (`test(p11/s42)`, 75dd012) lands eleven new tenant_isolation contract scenarios. test_retrieval_evaluation_http_isolation.py has six (cross-tenant gold-set get → 404 with no info leakage, cross-tenant list → empty, cross-tenant append-entry → 404, cross-tenant evaluation-run get → 404, cross-tenant evaluation-run kickoff against cross-tenant gold-set → 404, unauthenticated → 401). test_optimization_http_isolation.py has five (cross-tenant optimization-run get → 404, cross-tenant recommendation get → 404, cross-tenant recommendation list → empty, cross-tenant acknowledge → 404, unauthenticated → 401). Privacy-preserving 404 policy from D103 inherits (no security event fire on cross-tenant 404; structurally indistinguishable from genuinely not-found at per-tenant adapter altitude). Harness count: 137 → 148.
  - Commit 7 (`test(p11/s42)`, 70f64a1) lands the new `tests/contract/http/` directory with two contract suites covering the consumer-visible HTTP contract that is not specific to any one bounded context. test_pagination.py has eight cursor scenarios across the four list endpoints (cursor round-trip, malformed-cursor 400, page-size-above-ceiling 422 per context). test_error_responses.py has ten scenarios covering the canonical ErrorResponse body shape (four not-found 404 paths, 409 recommendation_transition_not_permitted with structured details, 409 no_draft_to_finalize, 400 empty_draft, 400 invalid_optimization_filter, correlation-id header round-trip across two requests with distinct UUIDs).
  - Commit 8 (`docs(p11/s42)`, ac5a6bf) lands the live-stack smoke at `docs/smoke/p11_s42_http_transport_surface.md` plus the smoke script at `scripts/smoke_p11_s42.py`. Nine stages: gold-set authoring through HTTP (Stage 1; finalized revision hash head `94eb3b45011ac8e9` byte-identical to compute_revision_hash output); two-step discovery (Stage 2; 0 candidates against empty corpus, route surface holds); synchronous evaluation-run kickoff (Stage 3; completed in 152ms with both vector_only and graph_only producing aggregates); synchronous optimization-run kickoff (Stage 4; completed in 18ms with 2 retrieval_strategy recommendations citing S40b evidence; model_choice and prompt_revision surface as structured skipped_categories); recommendation read surface (Stage 5; discriminated-union citations confirmed on the wire); tenant isolation (Stage 6; cross-tenant get returns 404 with no information leakage); recommendation lifecycle (Stage 7; acknowledge → apply on one rec, reject on another, 409 on re-apply of terminal recommendation); existing run_history and audit read surfaces (Stage 8; run_history clean, audit list surfaces a pre-existing empty-correlation_id finding from S40/S41 audit drafts, captured for pre-P12 hygiene); OpenAPI verification (Stage 9; 17/17 S42 operationIds present in /openapi.json, total 30 operations). Two findings landed at log/captures.md: the post-S41 `make build-api` target was broken by docker compose build's rejection of digest-in-image-tag (fixed inline by using `docker build` directly; methodology candidate for smoke-time verification of dev-workflow tooling at the same session that ships it); the S40/S41 audit rows carry empty correlation_id which the S37 reader's AuditEventRecord validator rejects (recommend one-line validator loosening at pre-P12 hygiene).
  - Commit 9 (this commit) lands the session log entry plus the close marker on current-package.md.

- Decisions: D112 landed at commit 1 with five coupled commitments. No new `charter/deferred-decisions.md` entries at S42. Two new captures entries forwarded to pre-P12 hygiene per above.

- Tests: 1212 unit tests pass at session close (+46 from S41's 1166); 148 tenant_isolation contract scenarios (+11 from S41's 137); 18 new pagination+error contract scenarios at the new `tests/contract/http/` directory; 33 new integration api tests (18 retrieval_evaluation + 15 optimization). 27 import-linter contracts kept (no new contracts at S42 — the existing layers contracts plus cross-context independence contracts already cover the new routers via apps/api/routers/* being existing namespace).

- Enforcement layer: No new contracts at S42 — the existing import-linter shape (fastapi confined to apps/, cross-context independence, domain purity, layers-* per context) already covers the new HTTP surfaces because the routes import from contexts.*/application and contexts.*/ports without violating any of the architectural commitments. The HTTP layer is itself the inbound-adapter altitude per the hexagonal commitment from D16; routes consuming application use cases via Pydantic DTOs over JWT-resolved TenantContext is the standard inbound-adapter shape. The 27-contract count from S41 close is preserved.

- Operator-decided dispositions at pre-write reconciliation:
  - **Finding 1 (scope):** Investigation-confirmed retrieval_evaluation + optimization only. run_history shipped at S34 per D98 and audit shipped at S37 per D103; the three scope options the brief proposed collapse to identical work. Session size reduced from approximately 10 commits to 9.
  - **Finding 2 (gold-set write semantics):** Append-only confirmed at the HTTP surface per D109 inheritance. No edit routes, no delete routes; path forward for wrong entries is a new gold-set with corrected content.
  - **Finding 3 (run kickoff):** Synchronous for POST /evaluation-runs and POST /optimization-runs. Phase 1 engine latency is bounded against local models; async is Phase 2 evolution when consumer evidence demands it. Smoke confirmed at Stage 3 (152ms evaluation) and Stage 4 (18ms optimization).
  - **Finding 4 (audit chain-verify HTTP route):** Confirmed CLI-only. The existing /audit/events list route already embeds page-granularity chain integrity per S36/D102 inline on every response; separate chain-verify endpoint not added — confirms existing state, not a new commitment.
  - **Finding 5 deviation (DTO placement):** Operator-resolved to adopt the existing flat convention (routers/<context>.py + routers/_<context>_dto.py + routers/_<context>_query.py) from the five-of-five existing surfaces precedent. The brief originally framed per-context subdirectories; the structurally honest move was convention consistency over the brief's framing. Subdirectory adoption would have introduced inconsistency requiring a follow-up hygiene session to migrate the five existing surfaces.
  - **Finding 6 deviation (page-size ceiling):** The brief's 200 framing was forward-affordance the codebase domain layer does not support; followed codebase reality with PAGE_SIZE_CEILING=50.
  - **Findings 7, 8 (JWT tenancy + discriminated union):** Autonomous — both align with the brief and the existing patterns. JWT tenant_id resolution via the shared get_tenant_context dependency at apps/api/routers/inference.py per the S15 hardening pattern extended at S37 (D103/D104) with PrincipalType.TENANT discriminator. Pydantic discriminated union on evidence_citations keyed by `category` literal mirrors the domain-layer union shape.

- methodology (line 1): **Pre-write reconciliation surfacing brief-vs-codebase deviation reaches fifth instance in P11.** Finding 5 (DTO placement) deviation was the recurrence at S42. The pattern now has five P11 instances (S39 D109 vs codebase substrate; S40 D66 vs as-built strategies; S40b graph-extract; S41 D111 framing scope; S42 Finding 5 DTO placement). The previous methodology line at S41 already named this beyond promotion threshold; S42 reinforces.

- methodology (line 2): **Smoke-time verification of dev-workflow tooling at the same session that ships it.** New candidate at log/captures.md 2026-05-15 [S42] entry. The post-S41 `make build-api` target was authored at commit 0e8041f without being exercised against the production-shape flow (the commit's own validation used `docker build` directly, the established pattern). The first production-shaped use at S42 smoke surfaced the docker-compose-build-rejects-digest-tag bug. Structurally invisible to unit tests (Makefile is not test-covered); only smoke-time exercise catches the class. The candidate is "when a session ships dev-workflow tooling, the same session's smoke must exercise the new tooling end-to-end" — applies recursively when the tooling is itself smoke-time infrastructure. Recurrence test continues at the next dev-workflow tooling addition (likely pre-P12 hygiene or P12 territory).

- methodology (line 3): **Audit-row data shape inconsistency surfaces at smoke time as cross-context audit semantics drift.** New captures entry at log/captures.md 2026-05-15 [S42]. The S40 retrieval_evaluation runner and the S41 optimization engine emit audit events with `correlation_id=""` (no inbound HTTP request context for engine-internal work); the S36/S37 audit reader's `AuditEventRecord.__post_init__` validator rejects empty correlation_id. The list route's 500 at smoke-time Stage 8 is the surfaced symptom. The structural lesson: cross-context audit semantics need explicit thinking about "is every audit event tied to an inbound HTTP request?" — answer is no (engine internals, background work), so the validator's commitment to non-empty correlation_id is too strict. Recommended one-line validator loosening at pre-P12 hygiene. The captures entry carries the recommendation plus the alternative (backfill migration to set sentinel correlation_id values) and the structurally honest argument for the looser validator.

- methodology (line 4): **OpenAPI-as-procurement-grade-deliverable verdict.** S42 is the first session committing OpenAPI specification as a deliverable, not just a side-effect of FastAPI route definitions. Stage 9 of the smoke verified all 17 S42 operationIds present in /openapi.json; the spec mirrors the FastAPI route declarations 1:1 with no maintenance burden because the route definitions ARE the spec source. A Phase 2 frontend developer reads /openapi.json and integrates without needing source access. Procurement-grade defensibility: spec-vs-implementation divergence is structurally impossible because FastAPI generates the spec from the route definitions that drive runtime behaviour. Forward-relevance for Phase 2: when OpenAPI consumer-tooling matures (TypeScript codegen, contract testing), this substrate is ready without rework.

- methodology (line 5): **HTTP transport surface session shape distinct from substrate sessions.** Methodology candidate observation: S42 is the first session in the P11 sequence (after S39 / S39b / S40 / S40b / S41) that ships HTTP transport over an existing substrate rather than ship the substrate itself. Substantively different shape — smaller architectural surface (no new ports, no new domain types, no new adapters; just inbound adapter layer atop existing application use cases); more execution-shaped (routes, DTOs, query parsers, error handlers, wiring extensions, tests). The pre-write reconciliation discipline still mattered (Finding 1 scope investigation reduced session size; Finding 5 deviation caught the structural-consistency call); the substrate-application-boundary scope-check pattern from S41 did not fire because there was no substrate to land. The role-functions exercised distribute differently — heavy engineer-and-PM, lighter analyst-and-architect (no new architectural decisions beyond D112 itself), tech-writer normal. P12 audit input: should HTTP-transport sessions be a named session shape with distinct discipline expectations versus substrate sessions?

- **Procurement-grade UX-consumer surface verdict.** A Phase 2 frontend developer reads `/openapi.json` (17/17 S42 operationIds present at smoke Stage 9; total 30 operations across the full API) and integrates against the platform without needing to read source. The four-context P11 substrate scaffold's consumer surface is complete: run_history (S34 per D98), audit (S37 per D103), retrieval_evaluation (S42 commit 3), optimization (S42 commit 4). The two-step gold-set discovery decomposition preserves human-in-the-loop content-fit discipline at the HTTP layer per S40b precedent. The recommendation lifecycle through HTTP lands equivalently to the S41 CLI exercise. Tenant isolation through HTTP holds with no information leakage. Phase 2 frontend work can build directly against this substrate without rework at the HTTP layer.

- **P11 close.** S42 closes P11's build-session sequence: D108 framed the four-context-substrate-plus-HTTP-transport bet at P11 framing; D109 / D110 / D111 / D112 land the four substrates plus their consumer HTTP surface across S39 / S39b / S40 / S40b / S41 / S42. Pre-P12 hygiene is the next strategic-mode block: README expansion; methodology candidate consolidation (the captures entries from this run plus the strong P11 candidates: pre-write reconciliation, principle-versus-framing drift, scope-check-at-substrate-application-boundary, smoke-time-verification-of-dev-tooling, OpenAPI-as-procurement-deliverable, HTTP-transport-session-shape); audit-row correlation_id loosening; the stray `apps/api/{routers}/` empty directory hygiene; the captures-promote-to-methodology window before the P12 phase audit itself.

---

## S41 — Optimization layer substrate with pluggable rule and metric abstractions
roles: analyst, PM, architect, engineer, technical writer
mode: build (P11 final producer-context substrate session; closes the four-context scaffold)

- Produced: Twelve commits closed S41.
  - Commit 1 (`docs(charter)`, ecbf4c5) lands D111 at `charter/decisions.md` tail with eight commitments (`contexts/optimization/` placement; OptimizationRun aggregate; Recommendation aggregate; recommendation_status_transitions; RecommendationRule Protocol plus four default rules; MetricCalculator Protocol with two-method shape; discriminated evidence_citations with structured caveat annotations; audit-chain-absorbed lifecycle across both aggregates). Thirteen alternatives considered. Vendor-flexibility principle at `charter/principles.md`. Schema additions for three new per-tenant tables at `charter/schema.md`. S41-in-flight paragraph at `charter/current-package.md`.
  - Commit 2 (`refactor(p11/s41)`, fdf2635) extracts the metric primitives behind a two-method MetricCalculator Protocol per Finding 1 disposition. `BinaryRelevanceMetrics` at the new `binary_relevance_metrics.py` absorbs the previous `metrics.py` module's seven functions; the S40 runner refactors to take a MetricCalculator via required kwarg; CLI wiring injects `BinaryRelevanceMetrics()` as default. All 80 retrieval_evaluation unit tests pass — bit-identity preserved end-to-end. The two-method shape is the structurally honest cut per Finding 1's structural-honesty addition: a single-method Protocol would leave aggregation hardcoded against the default impl and make swap meaningless at the aggregation surface where Phase 2 graded-relevance implementations most legitimately differ.
  - Commit 3 (`feat(p11/s41)`, fa12fa9) lands the optimization domain layer: OptimizationRun aggregate with status lifecycle and skipped_categories JSONB; Recommendation aggregate with category discriminator, append-only content, mutable status, NOT NULL FK to optimization_runs; RecommendationStatusTransition append-only row; RecommendationStatus enum plus can_transition forward-only lifecycle map; EvidenceCitation discriminated union (RetrievalStrategyEvidenceCitation + CostOptimizationEvidenceCitation + CaveatAnnotation + StrategyComparison + CostAggregate); RecommendationCandidate intermediate VO; RecommendationRule Protocol + SubstrateGapError. 22 domain unit tests cover invariants and citation/category coherence. Operator-confirmed pause point at substrate-application boundary; resumed cleanly at commit 4.
  - Commit 4 (`feat(p11/s41)`, 11f824c) lands the four default rule implementations at `contexts/optimization/application/rules/` (placement deviation from the brief's `domain/rules/` per the structural-honesty layering call) plus the EvidenceContext interface at `application/evidence_context.py`. RetrievalStrategyRule iterates completed evaluation runs and triggers on pairwise recall@3 delta > 0.15 absolute with structured CaveatAnnotation for all-zero strategies per Finding 3a (ii). CostOptimizationRule aggregates over RunHistoryReader by agent_template_id over 14 days, triggers above $0.10 starter threshold. ModelChoiceRule and PromptRevisionRule raise SubstrateGapError naming scoring-sheet substrate gap per Finding 3 (β→δ) disposition. 15 rule unit tests against fake EvidenceContext fakes.
  - Commit 5 (`feat(p11/s41)`, 907f365) lands the application use cases and four ports: run_optimization engine orchestrator catching SubstrateGapError into the parent OptimizationRun's skipped_categories field; acknowledge/apply/reject lifecycle transitions via shared `_transition_helpers` enforcing can_transition gating; get + list for both aggregates with cursor pagination at `application/cursors.py` mirroring run-history; audit_events drafts seven event verbs (run start/complete/fail; recommendation generate/ack/apply/reject) embedding full evidence_citations per Finding 4. citation_serialization helper at the domain layer centralises discriminated-union ↔ JSON-dict round-trip. Query filter and cursor VOs at `domain/query_filters.py`. 26 new application unit tests (engine 7, lifecycle 10, cursors 5, reads 4) — 63 optimization tests total.
  - Commit 6 (`feat(p11/s41)`, 9e3da11) lands four Postgres adapters: PostgresOptimizationRunRepository (persist + mark_completed-with-skipped_categories + mark_failed); PostgresOptimizationRunReader; PostgresRecommendationRepository (persist_recommendation + atomic persist_status_transition); PostgresRecommendationReader. Bound-tenant-id defence-in-depth at construction; status-transition WHERE clauses pin prior status. `_tables.py` defines three SQLAlchemy Core tables with category/status CHECK constraints. Per S40 precedent, adapter unit tests deferred to live-stack smoke.
  - Commit 7 (`feat(p11/s41)`, bcfc393) lands Alembic migration `0015_optimization_substrate` (27 chars; under the 32-char S39b ceiling) creating the three per-tenant tables with CHECK constraints (status, category, jurisdiction non-empty, terminal-completed pairing, transition status-distinct, from/to status enum), four indexes for the read paths.
  - Commit 8 (`feat(p11/s41)`, 4545db9) lands the wiring: four new wiring adapter classes at `apps/cli/_cross_context.py` as #15-#18 (OptimizationRunRepositoryAdapter, OptimizationRunReaderAdapter, RecommendationRepositoryAdapter, RecommendationReaderAdapter); four new factories at `apps/api/_agent_runtime_wiring.py`. New `layers-optimization` import-linter contract plus contexts.optimization.* added to the three independence contracts, domain-purity contract, and three vendor-confinement contracts (litellm, langfuse, neo4j). Layer-violation surfaced at execution time: the initial TYPE_CHECKING import of EvidenceContext in `recommendation_rule.py` broke both the new layers-optimization contract and the cross-context domain independence contract; import-linter parses TYPE_CHECKING blocks as real imports. Fixed by dropping the import and using `object` as the Protocol parameter type. Methodology candidate at line 2 below.
  - Commit 9 (`feat(p11/s41)`, f7e8167) lands two typer sub-apps at `apps/cli/_optimization.py` registered in `apps/cli/main.py`: `padhanam optimization` (run/get/list/acknowledge/apply/reject) and `padhanam optimization-run` (get/list). `_build_dependencies` constructs every adapter the engine needs including the four producer-context readers wrapped in EvidenceContext.
  - Commit 10 (`test(p11/s41)`, e53bf22) lands `tests/contract/tenant_isolation/test_optimization_isolation.py` with eleven scenarios (3 structural × 2 tenants + control-plane absence + 7 behavioural). Synthetic dual-tenant fixture mirrors the S40 pattern. Harness count: 126 → 137 after migration applies at smoke.
  - Commit 11 (`docs(p11/s41)`, aedbfa6) lands `docs/smoke/p11_s41_optimization_engine.md` walking the full optimization flow against tenant_a end-to-end: optimization run `3b538acb-...` produces 2 retrieval_strategy recommendations citing both completed evaluation runs (S40b's c168c2ba and S39b's ef58678a); both citations carry the all-zero CaveatAnnotation on graph_only per the S40b graph-extract reliability deviation. cost_optimization rule exercised end-to-end with substrate-honest zero emission against local dev cost evidence ($0.000246 mean vs $0.10 threshold). Phase 1 zero categories surface structured substrate-gap reasons on the OptimizationRun aggregate. Lifecycle exercise: acknowledge → apply on one rec, reject on the other; 7 audit events emitted with full evidence_citations embedded in after_state at every transition. tenant_b stays empty (D32 isolation).
  - Commit 12 (this commit, session log entry plus `charter/current-package.md` close marker).

- Decisions: D111 landed at commit 1 with eight coupled commitments expanded from the original brief's seven via the (δ) push-back adding OptimizationRun as a coupled aggregate. New principle "Vendor flexibility" landed at `charter/principles.md` alongside the architectural-principles grouping. No new `charter/deferred-decisions.md` entries at S41 (D108's four-category commitment held without amendment; the substrate-gap surfacing on model_choice and prompt_revision is queryable transparency, not deferral).

- Tests: 1166 unit tests pass at session close (+61 from S40b's 1105). 63 new optimization unit tests across domain (22), rules (15), engine (7), lifecycle (10), cursors (5), reads (4). 11 new tenant_isolation contract scenarios. 27 import-linter contracts kept (+1 from the new `layers-optimization` hexagonal contract).

- Enforcement layer: New `layers-optimization` hexagonal contract at `.importlinter` plus `contexts.optimization.*` added to the three cross-context independence contracts (domain, application, adapters), the `domain-purity` contract, and the three vendor-confinement contracts (litellm, langfuse, neo4j). The optimization context's internal layers (adapters → application → ports → domain) are enforced; the cross-context domain-independence guarantee extends to optimization.domain. The contract additions caught a real layering violation at write time (TYPE_CHECKING import edge); the fix preserved the hexagonal-on-bounded-contexts shape.

- Operator-decided dispositions at pre-write reconciliation:
  - **Finding 1 (MetricCalculator extraction shape):** Option (a) — Protocol at domain + DI, with the structural-honesty addition of two methods on the Protocol (per-query plus per-strategy aggregation) so swap is meaningful at the aggregation surface where Phase 2 graded-relevance implementations most legitimately differ. Single-method shape would have left aggregation hardcoded.
  - **Finding 2 (RecommendationRule abstraction shape):** Option (a) — Protocol with code-defined rules registered at composition. Configuration-driven registry deferred per Phase 1 simplicity.
  - **Finding 3a (retrieval_strategy caveat-on-all-zero-strategy):** Option (ii) — rule-pure semantics with structured `CaveatAnnotation` on the citation per the structured-annotation-not-free-form-prose requirement. CaveatAnnotation carries `strategy_id`, `state`, `caveat_code` — queryable downstream. Phase 2 may add caveat_code values without schema migration since the citation is JSONB.
  - **Finding 3b (recall@k trigger primary k):** Concur with recall@3 primary, all-k deltas in the citation. Avoids spurious recall@1 triggers while preserving audit-trail richness. Honours the S40b verdict that recall@k differentials at @3 are the load-bearing procurement-grade surface.
  - **Finding 3 zero-recommendation surface (β → δ push-back):** Operator pushed back on the original (β) transient-run-summary disposition in favour of (δ) commit OptimizationRun as a persistent aggregate. Substrate-symmetric with EvaluationRun at retrieval_evaluation; queryable persistent invocation context for procurement-grade defensibility; the FK from Recommendation to optimization_runs becomes NOT NULL. Scope expansion ~15-20% at S41 (additional aggregate root + repository + reader + Postgres adapters + migration table + list/get use cases + CLI subcommands). Accepted for the substrate-completeness benefit.
  - **Finding 4 (audit-event payload shape):** Embedded full evidence_citation at every transition (not back-reference). Multiple chain-anchoring points for the same citation strengthen procurement-grade defence against tampering with the recommendations table directly.
  - **Finding 5 (EvidenceContext reader surface):** Autonomous. EvaluationRunReader and RunHistoryReader surfaces are sufficient for the two substantive rules; client-side aggregation in cost_optimization rule avoids extending RunHistoryReader. Surfaced one detail: RunRecord carries `agent_template_id` not `model_id`, so cost rollup is by template not by model. Aligned with D111 commitment 5's reasoning.
  - **Finding 6 (cross-context port import contract):** Autonomous. application → other-context-ports cross-context imports are permitted by the existing independence contracts. No contract modification beyond adding `layers-optimization` plus the three existing independence-contract module-list extensions.
  - **Finding 7 (BinaryRelevanceMetrics bit-identity):** Verified at commit 2 via 80 retrieval_evaluation unit tests passing post-refactor; math unchanged.
  - **Finding 8 (wiring composition root patterns):** Confirmed 14 existing wiring classes; new four (#15-#18) extend cleanly.

- methodology (line 1): **Pre-write reconciliation surfacing brief-vs-required-structure gaps reaches fourth instance in P11.** S39, S40, S40b previous instances; S41 Finding 3 (β → δ) push-back is the fourth. Different shape from the prior three — the operator's reconciliation surfaced a structural symmetry consideration (substrate-symmetric with EvaluationRun) rather than a brief-vs-codebase drift. Demonstrates the discipline's value extends beyond drift-catching into scope-shape reasoning. Strong P12 audit promotion candidate; the candidate was already at promotion threshold per S40b's three-instance count.

- methodology (line 2): **Principle-versus-framing drift surfaces as a distinct methodology candidate.** Captured at `log/captures.md` at session-mid (commit 4 close). The brief framed `contexts/optimization/domain/rules/` against no specific as-built reality; the framing simply contradicted the hexagonal layering principle the codebase commits to at D16. No pre-write reconciliation against codebase reality could catch it because there was no codebase reference to reconcile against; only writing the code and watching the import pattern surface the principle violation catches the drift. Mitigation surface different from the as-built-versus-as-framed drift previously surfaced — closer to "check the framing against the principles file before writing the prompt." Surfaced again at commit 8 when the TYPE_CHECKING import broke the layers-optimization contract; same root class (principle violation in framing that only surfaces at write time). Two instances in S41 alone reinforces the candidate. P12 audit input.

- methodology (line 3): **Scope-check-at-substrate-application-boundary as candidate methodology default.** Captured at `log/captures.md` mid-session. The original S41 brief framed 12 commits; the (δ) push-back at pre-write reconciliation expanded scope ~15-20%. Mid-execution operator pause caught the scope-versus-framing divergence at the natural substrate-application boundary (between commit 3 closing the domain layer and commit 4 opening the application work). Whether substrate sessions should plan a scope-check at the domain-application boundary by default rather than discovering scope creep at execution time is the P12 audit question. The candidate is the planned-scope-check moment, not the pause itself.

- methodology (line 4): **Container-image-lag pattern at smoke time.** Third instance in P11: S39 also had `make migrate` against an outdated container; S40 had the audit-adapter-connection-not-yet-in-container issue; S41 has the migration-file-not-in-container issue. The fixing-inline-at-smoke methodology established at S40 (operator's flagged-CLI-audit-connection-risk disposition: fix inline, capture as methodology finding) worked again here — `docker compose cp` the migration plus the new context tree plus modified CLI/wiring modules into the running container, then `make migrate`. Container-rebuild would be the production-shaped path; the dev-rig smoke pattern uses cp-into-running. Three instances in P11; promotion candidate — should the dev workflow include a documented "fast-path code sync into running container for smoke" step? Or should the operator-driven smoke loop assume container rebuild as baseline and accept the slower turnaround?

- methodology (line 5): **Vendor-flexibility principle operationalisation verdict.** S41 is the first session to operationalise the new vendor-flexibility principle beyond vendor ports via pluggable domain-layer abstractions (MetricCalculator + RecommendationRule). Substantive verdict: the abstractions feel structurally honest, not over-engineered. MetricCalculator's two-method shape was an explicit Finding 1 expansion away from the original single-method draft because the runner consumes both surfaces; the wider Protocol matches the actual consumption pattern. RecommendationRule's single `evaluate` method matches its actual consumption pattern (engine calls it once per registered rule). Phase 2 graded-relevance implementations (nDCG, MAP) and richer rule-trigger languages have clean swap surfaces without runner change. Forward-relevance for Phase 2: evaluation-unit shape for graded relevance is the next natural pluggable-abstraction candidate when scoring-sheet substrate ships; rule-trigger language is the candidate when user-defined-rules surfaces consumer demand.

- methodology (line 6): **Four-context P11 substrate scaffold close at S41.** Gold-set substrate (S39 + S39b) → runner (S40 + S40b clean-gold-set bridge) → optimization (S41). The bridge-session-shape pattern's second instance closed at S40b; S41 did not require its own bridge — the substrate-application boundary pause at commit 3 close functioned as an in-session structural-honesty checkpoint instead. Whether the bridge-session pattern can be absorbed into in-session checkpoints at the substrate-application boundary (per methodology line 3) is the P12 audit question. The four-context scaffold is procurement-grade complete: every producer context owns its consumer-defined reader port, the optimization layer wraps the four readers via EvidenceContext, audit-event-level tamper-evidence absorbs the optimization lifecycle per D110's three-regime distinction extended at D111 commitment 8.

- S41-close verdict on procurement-grade defensibility: **PASS.** A procurement reader looking at recommendation `9f3e311d-...` from the smoke can trace the full chain without ambiguity. Prose names the action and magnitude. Rule provenance is implicit in the category (`retrieval_strategy` → `RetrievalStrategyRule` per D111 commitment 5) and the rule's trigger semantics are explicit in code. Citation evidence carries `evaluation_run_id=c168c2ba` (S40b's contamination-broken run; verifiable directly) and `gold_set_id=3b001430` (S40b's clean gold-set). Caveat annotation flags graph_only's all-zero aggregates with `caveat_code=infrastructure_substrate_check_required` — substrate transparency, not silent masking. Audit chain (7 events × full evidence_citations embedded) ensures the lifecycle has not been tampered with. Substrate transparency for Phase 1 zero categories (model_choice, prompt_revision) is queryable at both the OptimizationRun aggregate and the audit event level. The bet's success criterion 4 (the trace capture layer surfaces optimization recommendations, not just data) is demonstrated end-to-end against tenant_a's real evidence; D108's four-category commitment is procurement-grade-honest about Phase 1 scope without amendment.

```
metrics:
  classification: new-context-substrate (final P11 producer-context; closes the four-context scaffold)
  brief_started: 2026-05-15
  session_started: 2026-05-15
  session_closed: 2026-05-15
  merged: 2026-05-15
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: charter/decisions.md (D111); charter/principles.md (vendor-flexibility); charter/schema.md (three optimization tables); charter/current-package.md (S41 close marker)
  corrects:
  corrected_by:
```

---

## S40b — Clean gold-set authoring with content-fit selection
roles: engineer, technical writer
mode: build (verification-and-hygiene bridge between S40 runner close and S41 optimization framing)

- Produced: Five commits closed S40b.
  - Commit 1 (`docs(charter)`, ad9d729) lands the S40b in-flight paragraph at `charter/current-package.md` and the `.gitignore` rule for `docs/archive/snapshots/` closing the AC 17 cleanliness gap S40 flagged.
  - Commit 2 (`feat(p11/s40b)`, db9cd2d) lands the Pacelane recovery-first wearable case-study corpus at `tests/fixtures/corpus/p11_s40b/pacelane_recovery_first_case.md`. ~1,750 words across 6 H2 sections plus title; produces 7 chunks at ingest. Vocabulary diverges from the LVTGuide system prompt on case-study-specific terms (Pacelane, recovery-first, HRV, Spearman correlation, retention, subscription) while keeping unavoidable LVT-conceptual overlap (bet, initiative, epic, story, success criteria). The corpus is version-controlled so chunk references are reproducible across DB rebuilds, closing the S39b corpus-not-in-repo gap prospectively for S40b; S39b's corpus stays uncommitted as a separate P12 audit input.
  - Commit 3 (`docs(p11/s40b)`, 221ef1c) lands the live-stack smoke at `docs/smoke/p11_s40b_clean_gold_set.md` with five-stage executed-evidence walkthrough. Stage 1 (corpus refresh): Neo4j cleanup per the operator's refined criterion (12 entities trimmed, 8 deleted), Postgres delete + new source ingest, graph-extract bypass via forced state='indexed' captured as methodology finding plus orphan-entity cleanup after a late-completing worker. Stage 2 (S39b rename): direct SQL UPDATE renaming the rank-selected gold-set; hash byte-identical post-rename per the canonical-payload-excludes-name proof. Stage 3 (clean gold-set authoring): create `3b001430-...`, three per-query content-fit selection blocks each capturing the chunks selected, the chunks rejected, and the criterion that drove each line including the transparent borderline-section judgment for Query 2; finalize with revision hash `8fec2553d9...`. Stage 4 (verification re-run): runner produces `c168c2ba-...` with substantive recall@k drops vs S39b. Stage 5 (S41-evidence verdict): contamination broke at the recall@k surface.
  - Commit 4 (this commit) lands the session log entry plus the S40b close marker at `charter/current-package.md`.

- Decisions: No new D-entries. Two `charter/deferred-decisions.md` entries from S40 (`parallel_rrf` retrieval-strategy implementation; gold-set aggregate-level audit emission) remain active without S40b touch. The methodology principle S40b demonstrates — selection function at authoring must differ from the function being evaluated — is forwarded to P12 audit as a candidate, not committed as a D-entry inside S40b per the brief's explicit out-of-scope item.

- Tests: 1105 unit tests pass at session close (matches S40 baseline; no test surface touched at S40b). 26 import-linter contracts kept. Tenant_isolation contract harness scenario count unchanged at 126 (no new ports or adapter shapes; the harness extension at commit 8f179fd covers what S40b exercises).

- Enforcement layer: No `import-linter` contract changes. No AST enforcement changes. The runtime fix from S40 (`PostgresAuditAdapter.from_settings` at commit 21acd36) is the only code surface the verification re-run depends on; S40b exercises the runner CLI end-to-end without further code modifications.

- Operator-decided dispositions at pre-write reconciliation:
  - **Finding 1 (corpus refresh scope):** Option (a) — replace `lvt_methodology_overview.md` with the Pacelane case study. Operator strengthening accepted: Query 2 reframing is load-bearing alongside corpus replacement because the word *framing* in the original Query 2 is system-prompt vocabulary. Reframed to "how did Pacelane apply LVT to its recovery-first wearable launch" using case-study-specific terms. Queries 1 and 3 unchanged.
  - **Finding 2 (authoring agent provenance):** Option (a) — CC autonomously authors with content-fit selection. The structural-honesty cut is selection-function-must-differ-from-evaluated-function, not selection-is-human; CC reading chunk content and judging answer-fit meets the cut.
  - **Finding 3 (S39b gold-set rename):** Option (a) — direct SQL UPDATE on `gold_sets.name`. Renamed to "P11 retrieval baseline (rank-selected, S39b)". Stored revision hash `9ee5aed07c7c...` byte-identical post-rename per the canonical-payload-excludes-name proof from S39b's smoke Stage 3.
  - **Finding 4 (corpus durability):** Option (a) — version-controlled at `tests/fixtures/corpus/p11_s40b/`. Closes the S39b corpus-not-in-repo gap prospectively.
  - **Finding 5 (Ollama state; autonomous):** Compose stack healthy at S40 smoke; `nomic-embed-text:v1.5` present. No drift since S39b.
  - **Finding 6 (replace-vs-additive; refined to refined-cleanup-criterion):** Ingest CLI is additive-only; cleanup of LVT chunks + entities required direct DB operations. Operator's refined Neo4j criterion landed: trim `source_chunk_ids` to remove deleted chunk references first, conditional-delete only when array is empty. Preserved "bet", "Padhanam", "PM agent", "agent" entities (co-derived from surviving sources); deleted 8 LVT-source-only entities.
  - **Finding 7 (gold-set audit-emission gap; autonomous informational):** Confirmed; no S40b touch.

- methodology (line 1): **Pre-write reconciliation surfacing brief-vs-required-structure gaps reaches third instance in P11.** S39 Finding 2 caught D105's scoring-sheet-precedent failure (sibling-in-placement-not-in-pattern). S40 Finding 2 caught D66's framing-vs-as-built drift (catalogue says three strategies, adapter executes two). S40b pre-write reconciliation caught the Query 2 reframing being load-bearing alongside corpus replacement — the operator's brief named the corpus refresh but did not name the query vocabulary as system-prompt-overlapping. Three instances inside one package. The methodology candidate from S40 line 1 strengthens at S40b: pre-write reconciliation routinely surfaces drift between strategic-mode framing and as-built or as-required structure; the discipline's value is now demonstrable at three instances in P11 alone. Strong P12 audit promotion candidate.

- methodology (line 2): **Metric-threshold expectations should derive from structural understanding of what each metric measures, not from gut intuition.** The brief stated MRR above 0.9 would trigger corpus revision. At Stage 4 the recall@k drops were substantive (28% relative at k=1, 20% at k=3) but MRR stayed at 1.0. Operator-owned at smoke time as a wrong implicit assumption: content-fit selection typically agrees with vector retrieval at rank-1 when vector retrieval is competent at top-1 ranking, so MRR=1.0 is a structural property of this metric on a competent-retrieval setup, not a contamination signal. The structural cut sharper than the brief framed: at S39b rank-1 is in expected BY CONSTRUCTION (rank-based selection); at S40b rank-1 is in expected BY JUDGMENT (content-fit happened to agree with vector's top-1 because vector's top-1 is genuinely relevant). The selection-must-differ-from-evaluated-function principle holds in both cases; MRR just isn't the surface where the difference surfaces. Recall@k is the contamination-sensitive surface in this evaluation setup. P12 audit candidate: metric-threshold expectations derive from structural understanding of what each metric measures, not from gut intuition about what "clean" looks like. The principle generalises to any evaluation context with multiple metric surfaces.

- methodology (line 3): **Graph-extract pipeline reliability is the gating constraint for any S40-style smoke that needs both retrieval legs.** The local Ollama qwen2.5:7b extract pipeline is slow (1-3 min per chunk) and unreliable on this dev rig — multiple worker invocations left the Pacelane source in `extracting` state without progress, and a late-completing worker wrote orphan entities for a deleted source that required separate cleanup. Per the S40 CLI runner's honest-empty graph_only leg, S40b's contamination-break test doesn't depend on Neo4j entity creation; bypassed by forcing `state='indexed'`. P12 audit input: invest in extract reliability (timeout handling, automatic retry semantics, observable worker progress) at a future hygiene session, or accept that graph_only metrics are honest-zero on this rig and document the expectation. Both paths reasonable; S40b operates under the second.

- methodology (line 4): **Bridge-session-shape pattern's second instance close.** S39 → S39b was verification-and-hygiene (LVTGuide absent, corpus empty, CLI not runtime-verified). S40 → S40b was methodologically-clean-artefact authoring (S39b gold-set was rank-selected; S40b corrected with content-fit). Both bridge sessions resolved specific structural-honesty gaps the substrate session surfaced but could not fix without scope creep. The pattern (S39b methodology line 5) now has two confirmed instances against different sub-substrate concerns. The candidate at S40 close was "one more instance at P11 close or P12 would promote"; S40b is the second instance. Promotion threshold met for P12 audit consideration: bridge sessions as a distinct session shape between substrate and audit with two named sub-substrate variants. Substantive paragraph for the audit: whether substrate sessions should plan bridges in by default rather than discovering them at substrate close is the question P12 should resolve; the brief flagged this explicitly.

- methodology (line 5): **S41 framing brief should privilege recall@k differentials over MRR explicitly in evidence-citation specs.** Operator commitment from the verdict-disposition disposition: when S41 framing happens, the evidence-citation specification for the `retrieval_strategy` recommendation category names recall@k and precision@k as the load-bearing metric surfaces, not MRR. A recommendation like "switch from vector_only to graph_only because vector_only's recall@3 of 0.80 underperforms graph_only's recall@3 of 0.92 on this gold-set" cites the right surface; the same recommendation citing MRR=1.0 differentials would not (MRR is structurally non-discriminating in this evaluation setup). Worth flagging in S41's brief explicitly so the evidence-shape commitment is correct from the start. This is a near-term carryover from S40b for S41 framing, not a P12 audit candidate.

- S40b-close verdict on S41-evidence credibility: **The S40b run `c168c2ba-328f-4163-b374-1f69d914b623` can be cited as procurement-grade evidence for retrieval-strategy recommendations.** The contamination at S39b (selection-function-equals-evaluation-function via rank-based authoring) broke at the recall@k surface where it should surface: recall@1 dropped from 0.555 to 0.400 (28% relative), recall@3 from 1.000 to 0.800 (20%), recall@5 from 1.000 to 0.867 (13%). Vector retrieval is now measured against an independent ground-truth selection function. MRR stayed at 1.0 for structural reasons (operator-owned threshold-setting error captured at methodology line 2); recall@k carries the contamination-break verdict instead. S41 proceeds with the load-bearing-metric commitment captured at methodology line 5.

```
metrics:
  classification: verification-and-hygiene bridge (methodologically-clean-artefact authoring)
  brief_started: 2026-05-15
  session_started: 2026-05-15
  session_closed: 2026-05-15
  merged: 2026-05-15
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: charter/current-package.md (S40b close marker); .gitignore (docs/archive/snapshots/); briefs/p11/s40b.md
  corrects: S40 close verdict (S39b gold-set unusable as S41 evidence)
  corrected_by:
```

---

## S40 — Retrieval-evaluation runner with per-query plus aggregated result substrate
roles: analyst, PM, architect, engineer, technical writer
mode: build (P11 runner substrate; second new-context-substrate session in P11)

- Produced: Eleven commits closed S40.
  - Commit 1 (`docs(charter)`, 9e062b7) lands D110 at `charter/decisions.md` tail with seven commitments (placement at `contexts/retrieval_evaluation/`; `EvaluationRun` aggregate root shape with status lifecycle; `EvaluationResult` and `EvaluationAggregate` value object shapes; agent-level `AgentRetrievalClientAdapter` invocation; all-executing-strategies-in-one-run; canonical-identifier projection rule for storage; audit-event-level tamper-evidence absorption with the three-regime distinction); seven alternatives considered including (h) implement-`parallel_rrf`-now and (i) rewrite-D66 (both rejected on scope-discipline grounds with operator's prose disposition reasoning). Schema additions for `evaluation_runs`/`evaluation_results`/`evaluation_aggregates` at `charter/schema.md`. Two new `charter/deferred-decisions.md` entries: `parallel_rrf` implementation and gold-set aggregate-level audit emission. S40-in-flight paragraph at `charter/current-package.md`. Brief preserved at `briefs/p11/s40.md`.
  - Commit 2 (`feat(p11/s40)`, a76919b) lands the domain layer: `EvaluationRun` aggregate root with status-lifecycle invariants (`__post_init__` enforces RUNNING ↔ completed_at-NULL pairing), `EvaluationResult` value object, `EvaluationAggregate` value object, metric primitives (recall@k, precision@k, MRR, mean/percentile aggregation; library-free implementations per Finding 4). Frozen dataclasses, stdlib only, per D16 domain purity. Domain-level unit tests covering invariants and metric edge cases.
  - Commit 3 (`feat(p11/s40)`, 8a86265) lands the application layer: `run_retrieval_evaluation` (orchestrates entry × executing-strategy invocation via `RetrievalRunnerPort`, persists per-query results and per-strategy aggregates, transitions the run aggregate through running/completed/failed, emits an `AuditEvent` for every write), `get_evaluation_run`, `list_evaluation_runs` with cursor pagination matching the S33 base64-of-JSON shape. Three new consumer-defined ports: `EvaluationRunRepository` (write side), `EvaluationRunReader` (read side), `RetrievalRunnerPort` (consumer surface over the AgentRetrievalClient adapter). `strategy_keys.py` projects canonical identifiers (`vector_only`, `graph_only`) to the adapter's free-form dispatch mapping per D110 commitment 6. `audit_events.py` drafts four `AuditEvent` shapes mirroring `_draft_event` from agent_loop_executor. Cursor codec at `application/cursor.py` refactored with shared helpers; `EvaluationRunListCursor` lands at `domain/query_filters.py`. 19 new unit tests at `tests/unit/contexts/retrieval_evaluation/application/test_runner.py` plus extended `_fakes.py` with `InMemoryEvaluationRunStore`, `FakeEvaluationRunRepository/Reader`, `FakeRetrievalRunner`, `RecordingAuditPort`.
  - Commit 4 (`feat(p11/s40)`, b4c44f3) lands `PostgresEvaluationRunRepository` and `PostgresEvaluationRunReader` against per-tenant data planes with the bound-tenant defence-in-depth pattern from S31/S33/S39. Three new SQLAlchemy Core table definitions at `_tables.py`. The reader's pagination uses the uuid<uuid cast fix from S33 (uuid<varchar fall-back broke ordering across pages at S33 smoke). Adapter unit tests against fake session intentionally skipped per the S39 retrieval_evaluation precedent (verification lives at the contract harness + live-stack smoke); the intra-context test-pattern asymmetry is flagged at methodology line 4 below.
  - Commit 5 (`feat(p11/s40)`, d9cb409) lands per-tenant Alembic migration `0014_eval_runner_substrate` creating the three tables with status CHECK, terminal-state pairing CHECK (status='running' → completed_at IS NULL; otherwise NOT NULL), MRR range CHECK, latency non-negative CHECK, UNIQUE on (run_id, entry_id, strategy) for results and (run_id, strategy) for aggregates, plus three indices on the read paths. Revision string `0014_eval_runner_substrate` (26 chars, well under the 32-char ceiling captured at S39b).
  - Commit 6 (`feat(p11/s40)`, e7c02b0) lands the wiring: `EvaluationRunRepositoryAdapter` (#13 on `apps/cli/_cross_context.py`) and `EvaluationRunReaderAdapter` (#14) following the consumer-port-plus-wiring-adapter pattern's thirteenth and fourteenth instances. `build_evaluation_run_repository` and `build_evaluation_run_reader` at `apps/api/_agent_runtime_wiring.py` mirror the S39 gold-set factories.
  - Commit 7 (`feat(p11/s40)`, b20262a) lands the CLI: a new `evaluation_run_app` typer sub-app registered under `padhanam evaluation-run` with three subcommands (`start`, `get`, `list`). Inline wiring helpers `_CliCompositeRetrievalClient` (PgVectorSearch + honest-empty graph leg per the Phase 1 limitation matching `AgentRetrievalClientAdapter`'s own Phase 1 best-effort comment), `_CliRetrievalRunnerPort` (wraps `AgentRetrievalClientAdapter` as a `RetrievalRunnerPort` with wall-clock latency capture per Finding 5, evaluation-appropriate defaults for `min_score`/`filter_tree`, and type-narrowed extraction of ranked chunk_ids from `RetrievalResult.citation_candidates`), and `_build_runner_dependencies` (assembles the runner repo, reader, gold-set reader, runner port, audit adapter — the first instance of audit emission at the CLI surface).
  - Commit 8 (`test(p11/s40)`, 8f179fd) lands `tests/contract/tenant_isolation/test_evaluation_run_isolation.py` extending the harness with twelve new scenarios (3 structural + 9 behavioural): per-tenant DB table presence × 2, control-plane absence, persist-run isolation, two defence-in-depth ValueError checks, cross-tenant get/list returns None/empty, cross-tenant mark_completed leaves status='running', cross-tenant result and aggregate persist blocked by FK, run-persist blocked when gold_set lives on the other tenant. Harness count: 114 → 126.
  - Commit 9 (`fix(p11/s40)`, 21acd36) corrects the `ControlPlaneSettings.url_async` AttributeError surfaced at S40 live-stack smoke: the CLI's `_build_runner_dependencies` was constructing the audit adapter inline with a non-existent attribute; switched to `PostgresAuditAdapter.from_settings(...)` matching the production composition path at `apps/api/main.py`. Methodology finding captured at line 5 below.
  - Commit 10 (`docs(p11/s40)`, 422d9c9) lands `docs/smoke/p11_s40_retrieval_evaluation_runner.md` walking the full runner flow against tenant_a end-to-end: migration 0014 applied to both tenants, run `ef58678a-...` produces 6 per-query results plus 2 per-strategy aggregates, 10 audit events emitted (1 run.start + 6 result.append + 2 aggregate.append + 1 run.complete) with chain integrity intact across all 35 tenant_a audit rows, cross-tenant tenant_b stays empty, S40-close verdict surfaces.
  - Commit 11 (this commit, session log entry plus `charter/current-package.md` close marker).

- Decisions: D110 landed at commit 1, revised per pre-write reconciliation. Seven commitments. Two new `charter/deferred-decisions.md` entries: `parallel_rrf` retrieval-strategy implementation (activates at P12 audit if procurement-grade three-strategy comparison surfaces, or at a Phase 2 retrieval-adapter-extension session) and gold-set aggregate-level audit emission (activates no later than the Phase 1 close audit). The two carryovers preserve the framing-vs-as-built divergence loops without rewriting D66 or back-filling S39's gold-set substrate inside S40 scope.

- Tests: 1105 unit tests pass at session close (+58 from S39b's 1047). 19 new runner-application unit tests; 39 new domain-level tests across `evaluation_run`/`evaluation_result`/`evaluation_aggregate`/`metrics` (committed at a76919b). 26 import-linter contracts kept. Tenant_isolation contract harness extends 114 → 126 (+12 new scenarios at S40).

- Enforcement layer: No new `import-linter` contracts; the retrieval_evaluation layers contract from S39 continues to govern the new application/ports/adapter modules. No new AST enforcement. The hexagonal layer order (adapters → application → ports → domain) holds across all new code; the new `RetrievalRunnerPort` and `EvaluationRunRepository`/`EvaluationRunReader` ports satisfy the `ports/` layer-purity expectation (Protocol classes, no SQLAlchemy, no asyncpg).

- Operator-decided dispositions at pre-write reconciliation:
  - **Finding 1 (run granularity):** Option (a) — one run × all executing strategies. Comparison surface co-located inside one EvaluationRun; optimization layer at S41 cites one run_id as evidence covering every executing strategy. Brief recommendation held.
  - **Finding 2 (strategy catalogue):** Option (a) — ship two strategies at S40; `parallel_rrf` deferred to Phase 2 via new `charter/deferred-decisions.md` entry. D110 commitment 6 reframes from "all three" to "every D66-registered strategy with an executing branch in `AgentRetrievalClientAdapter` at S40 close". D110 alternatives (h) and (i) explicitly reject implement-now and rewrite-D66 paths. Substantive operator-prose disposition; reshaped D110 inline. Methodology candidate at line 1 below.
  - **Finding 3 (invocation path):** Option (a) — runner invokes via `AgentRetrievalClientAdapter` callable as the single dispatch site. Canonical-identifier projection rule (`vector_only` → `{"primary": "vector"}`; `graph_only` → `{"primary": "graph"}`) lives at `application/strategy_keys.py`, not at the adapter; runner consumes, adapter unchanged.
  - **Finding 4 (metrics library; autonomous):** No existing implementation; custom land at `domain/metrics.py` per the brief recommendation.
  - **Finding 5 (latency capture; autonomous):** Wall-clock from invocation-start to result-return via `time.monotonic()`; not reading OTel spans (decouples runner from observability instrumentation, which the runner explicitly does not invoke per D110 commitment 5).
  - **Finding 6 (audit-event emission):** **Not autonomous; surfaced for operator decision.** Verification showed S39's retrieval_evaluation context emits zero audit events on gold-set writes; gold-set tamper-evidence sits on chain-self-contained revision hashes per D26. D110 commitment 7 introduces audit emission for the first time in the context, creating intra-context asymmetry. Operator confirmed option (A) with the three-regime distinction reasoning: tenant-authored content sits under hash-chain self-containment (D26's revision-shaped pattern); actor-driven events and platform-computed records sit under the audit-event chain (also D26). Gold-set audit emission gap activates as a separate carryover, not in-S40 scope. Methodology candidate at line 2 below.
  - **Finding 7 (wiring patterns; autonomous):** 12 existing wiring classes at `apps/cli/_cross_context.py`; the two new adapters extend cleanly as #13 and #14.

- methodology (line 1): **D66 strategy registry framing-vs-as-built divergence surfaced at pre-write reconciliation.** Finding 2 surfaced that D66 catalogues `vector_only` / `graph_only` / `parallel_rrf` as a three-strategy starter set, but `AgentRetrievalClientAdapter` at `apps/cli/_cross_context.py:322-462` dispatches on `{"primary": "vector"}` and `{"primary": "graph"}` only — the third "strategy" key (`{"primary": "vector", "secondary": "graph"}` in the adapter's docstring) is documented as Phase 1 best-effort with no fusion-merge code. The operator's prose disposition kept D66 unchanged and added a new `charter/deferred-decisions.md` entry naming the gap. This is the **third** instance of framing-vs-as-built drift surfacing at P11 pre-write reconciliation (after D105's scoring-sheet-precedent failure at S39 Finding 2 and the field-set-agnostic primitive misidentification at S39 mid-build reconciliation). Three instances is a pattern. Strong methodology candidate at the P12 audit for "pre-write reconciliation as the load-bearing discipline for catching framing-vs-as-built drift between sessions". Adjacent observation: the strategy-key surface at the adapter is stringly-typed (`Mapping[str, Any]`) — naming the keys in a structured way (enum or typed identifier) is a separate concern worth a P12 audit note but out of scope for S40.

- methodology (line 2): **First audit-event emission inside `contexts/retrieval_evaluation/` establishes the three-regime distinction.** D110 commitment 7 introduces audit emission to the context where gold-sets (S39) did not emit; the reasoning paragraph names three D26 regimes explicitly: tenant-authored content under hash-chain self-containment (methodology revisions, gold-set revisions), actor-driven events under the audit-event chain (tenancy registry mutations, agent-runtime mutations), and platform-computed records under the audit-event chain mirroring the actor regime (the runner's `evaluation_runs`/`results`/`aggregates`). The intra-context asymmetry (gold-set writes don't emit; runner writes do) is structurally honest under this regime distinction — gold-sets are tenant-authored, runner records are platform-computed. The gold-set audit-emission gap activates as a separate carryover. **P12 audit input:** the three-regime distinction generalises beyond retrieval_evaluation and may want explicit naming at `charter/principles.md` if a fourth bounded context surfaces the same regime question; current evidence sits at two contexts (audit + retrieval_evaluation), one more would promote.

- methodology (line 3): **Bridge-session-shape pattern confirms at second instance.** S39 surfaced an S39b verification-and-hygiene bridge carryover (LVTGuide absent, corpus empty, CLI not runtime-verified); S40 surfaces an S40b methodologically-clean-gold-set authoring bridge carryover (S39b gold-set is rank-based-not-content-fit so its metrics cannot discriminate between strategies). Both bridge sessions resolve specific structural-honesty gaps the substrate session surfaced but cannot fix without scope creep. The pattern (S39b methodology line 5) now has two instances against different sub-substrate concerns: verification-and-hygiene at S39b, methodologically-clean-artefact authoring at S40b. One more instance at P11 close or P12 would promote to a formal `charter/methodology.md` entry — bridge sessions as a distinct session shape between substrate and audit.

- methodology (line 4): **Substrate-plus-runner-co-located bounded-context discipline confirmed; runner-at-substrate-context is the right cut.** D110 commitment 1 placed the runner at `contexts/retrieval_evaluation/` alongside the gold-set substrate from S39, rejecting placement at `contexts/evaluation/` (scoring-sheet pattern) or a new context. The build experience confirms the choice: the runner reuses `GoldSetReader` directly via the existing port (no cross-context substrate coupling), the runner's tables sit on the same per-tenant DB as the gold-sets they exercise (FK enforcement makes cross-tenant association structurally impossible per the contract harness's run_persist_blocked test), and the consumer-port-plus-wiring-adapter pattern reaches the thirteenth and fourteenth instances without architectural impedance. The brief flagged "does runner-at-substrate-context produce friction, or is it the right cut?" — the cut is the right cut. **Methodology candidate:** P12 audit may want to name this as "co-locate substrate and orchestrator-against-substrate inside one bounded context" when the orchestrator reads its inputs from the substrate; the alternative (substrate at one context, orchestrator at a sibling) mirrors D109's sibling-in-placement-not-in-pattern failure mode. Adjacent observation: the no-adapter-unit-tests pattern from S39 (verification lives at the contract harness + smoke) continued at S40; this is now the retrieval_evaluation-specific test-pattern asymmetry against the API composition root, flagged at S39 methodology line 4 and still standing.

- methodology (line 5): **Verification-debt-from-smoke-carryovers pattern's second instance at retrieval_evaluation.** S39b methodology line 1 named the pattern: substrate-session CLI ACs cannot claim closure on import-shape verification alone; runtime verification at the smoke surface catches what import-shape checks structurally cannot. S40 smoke fired the second instance: `_build_runner_dependencies` constructed the audit adapter with `ControlPlaneSettings.url_async` (a non-existent attribute). Unit tests cannot catch this because the fakes (`RecordingAuditPort`) substitute the adapter wholesale. Fixed inline at commit 21acd36 by switching to `PostgresAuditAdapter.from_settings(...)` matching the production composition path. Adjacent observation: CLI wiring helpers that construct production-class adapters inline (vs delegating to existing factory functions) are a specific sub-pattern worth naming at the P12 audit; the API composition root at `apps/api/_agent_runtime_wiring.py` uses `from_settings` everywhere; the CLI's inline construction is the structural-honesty gap.

- methodology (line 6): **Operator feedback on AskUserQuestion option-list constraint for findings that reshape D-entries.** At Findings 1-3 surfacing I posed via AskUserQuestion with structured options; the operator rejected the tool call and responded in free-form prose that added load-bearing specificity my option labels could not have anticipated (exact deferred-decisions text, specific alternative-rejection text, canonical-identifier projection rule with placement, explicit non-rewrite-D66 commitment). The feedback saved to `~/.claude/projects/-Users-sabu-padhanam/memory/feedback_prose_over_option_lists_for_findings.md` per the auto-memory convention. Pattern: when a finding may change D-entry text or add a new charter entry, narrate in assistant message text and pause for free-form operator response; reserve AskUserQuestion for narrow self-contained decisions where labels fully cover the space. Signal-to-detect: if option descriptions become paragraphs rather than short labels, the question is too rich for the tool. The methodology line is captured here for the session record; the memory entry persists across conversations.

- S40-close verdict on the S39b gold-set's S41 usability: **An S40b clean-gold-set bridge session is required before S41 ships.** The runner substrate works structurally end-to-end (smoke Stage 1-5 evidence). The metrics it produced against the S39b real-corpus gold-set are not procurement-grade S41 evidence: the gold-set's `expected_chunk_ids` were rank-1 selected from vector retrieval at S39b authoring time, and the runner at S40 invokes the same vector retrieval at evaluation time, so MRR=1.0 and recall@1=high are the artefact of two identical functions on the same corpus, not retrieval-quality measurement. For S41's optimization engine to cite `recall_at_5 = 0.42` as evidence that a strategy underperforms, the gold-set's correct answers must be independent of the strategy being measured. Compounded by the LVT-feedback-loop caveat for one of three queries (corpus-system-prompt overlap from S39b). Recommended S40b shape: (a) human content-fit judgment over retrieval candidates instead of rank-based selection, plus (c) a re-ingested corpus that does not overlap with the agent system prompt. S41 framing at strategic-mode conversation post-S40 close commits the specific shape; the smoke document records the requirement, not the design.

```
metrics:
  classification: new-context substrate (runner-against-substrate, S39-twin shape)
  brief_started: 2026-05-15
  session_started: 2026-05-15
  session_closed: 2026-05-15
  merged: 2026-05-15
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: D110; charter/schema.md (3 new tables); charter/deferred-decisions.md (parallel_rrf + gold-set audit emission); charter/current-package.md; briefs/p11/s40.md
  corrects:
  corrected_by:
```

---

## S39b — Corpus re-ingest and real-corpus discovery-mode authoring
roles: engineer, technical writer
mode: build (verification-and-hygiene bridge between S39 substrate close and S40 runner work)

- Produced: Six commits closed S39b.
  - Commit 1 (`docs(charter)`, 9876bdd) lands the S39b in-flight paragraph at `charter/current-package.md`, captures the migration name-length convention at `log/captures.md` as a project-tooling constraint, and preserves the brief at `briefs/p11/s39b.md`.
  - Commit 2 ingestion-drift commit skipped per Finding 1 — the ingestion CLI runs clean post-S39.
  - Commit 3 (`feat(p11/s39b)`, 1926057) lands forward-only control-plane Alembic migration `0013_lvtguide_reseed` re-seeding LVTGuide with the retrieval tool reference pinned at insertion time and the LVT system prompt lifted verbatim from `briefs/p7/s25.md`. Idempotent; preserves `migration:0013_lvtguide_reseed` actor for provenance symmetry with the seven McKinsey 7-Step roles. One documented mechanical drift: S25 brief named the retrieval strategy "hybrid" citing D66; D66's actual registry name is `parallel_rrf`. Migration uses D66's canonical name.
  - Commit 4 (rename, executed as direct SQL UPDATE; no code commit). Renamed the S39 synthetic gold-set on tenant_a from "P11 retrieval baseline" to "P11 retrieval baseline (synthetic, S39)". Captured as execution evidence in the smoke document; rename is hash-chain-invariant because the canonical revision payload excludes the gold-set name (structurally honest proof recorded in the smoke document's Stage 3).
  - Commit 5 (`fix(p11/s39b)`, 5c7a7f2) corrects ChunkResult attribute access in the gold-set append-entry CLI (`.text` → `.content`, `.score` → `.similarity_score`). Surfaced at S39b stage 4 when discovery-mode authoring ran for the first time; S39's import-shape verification missed this because the typer registration imports cleanly without invoking the candidate-rendering loop. The fix is the methodology demonstration that justifies the bridge-session shape: import-shape verification does not substitute for runtime verification at the discovery-mode CLI altitude.
  - Commit 6 (`docs(p11/s39b)`, f241247) lands the smoke document at `docs/smoke/p11_s39b_corpus_reingest_and_real_authoring.md` with five-stage executed-evidence walkthrough (image rebuild + compose digest pin advancement, LVTGuide re-seed plus chain integrity verification, corpus re-ingest with substitution evidence and one extraction-failure retry path, S39 gold-set rename via SQL UPDATE, real-corpus discovery-mode authoring with the runtime-fix mid-smoke and hash-chain verification byte-identical). Includes four S40 / P12 carryovers explicitly named.
  - Commit 7 (this commit, session log entry).

- Decisions: No new D-entries. Migration name-length convention captured at `log/captures.md` per the brief disposition (vendor-tooling constraint, not architectural decision).

- Tests: 1047 unit tests pass at session close (matches S39 baseline; no test surface changed at S39b). 26 import-linter contracts kept. Tenant-isolation contract harness scenario count unchanged.

- Enforcement layer: No `import-linter` contract changes. No AST enforcement changes. The S39b runtime-fix at commit 5c7a7f2 modifies two attribute references in `apps/cli/_retrieval_evaluation.py`; the existing import-linter contracts continue to police the same cross-context boundaries. The CLI's import-shape verification at S39 passed and still passes; the runtime divergence was below the import-shape surface.

- Operator-decided dispositions at pre-write reconciliation:
  - **Finding 1 (corpus source and path):** Option (c) — substitute corpus drafted at execution time; S25's original synthetic sources never landed in repo and are not recoverable. Operator added shaping note: substitute corpus shape should plausibly answer S39's three queries so the real-corpus gold-set reuses those queries and produces a structural-comparison artefact with the S39 synthetic gold-set at P12. Substitution evidence captured at smoke Stage 2.
  - **Finding 2 (LVTGuide re-seed):** Option (b) — new control-plane migration `0013_lvtguide_reseed`. LVT system prompt lifted verbatim from briefs/p7/s25.md per operator's fidelity ask. One drift surfaced at lift: brief said `retrieval_strategy: hybrid` citing D66; D66's registry names this `parallel_rrf`; mechanical correction at the migration with rationale documented in the migration's docstring. No D-entry required (mechanical rename per D66, not a substantive choice).
  - **Finding 3 (S39 gold-set rename):** Option (a) — direct SQL UPDATE. Structural-honesty observation captured at smoke Stage 3: the rename is hash-chain-invariant because the canonical revision payload at `contexts/retrieval_evaluation/domain/hash_chain.py:70-94` spans only `{revision_number, entries[]}` and explicitly excludes the gold-set name. The S39 gold-set's stored hash `ad94611492299f23b76d7c3eb4602206e88a20223e9ea5983bb0568c43f465a4` still recomputes byte-identically post-rename. This is the structural proof that the rename does not break D109 commitment 4.
  - **Findings 4-6 (autonomous):** Embedding model `nomic-embed-text:v1.5` present on Ollama; dimensions match (768 ↔ 768); per-tenant chunks schema unchanged; discovery-mode CLI runtime verified end-to-end at smoke Stage 4 (with the runtime fix at commit 5).

- methodology (line 1): **Import-shape verification is not runtime verification at the discovery-mode CLI altitude.** S39's smoke document explicitly named that the typer-wiring imports were verified but the `--query` retrieval path was not exercised end-to-end. S39b's stage 4 authoring ran the path for the first time and immediately surfaced an AttributeError on ChunkResult that the import-shape check could not have caught (the typer command's module loads without invoking the candidate-rendering loop, where the bad attribute access lives). The methodology-shape observation worth recording: substrate-session CLI ACs cannot claim closure on import-shape alone; runtime verification against real producers is the closure gate. This is the verification-debt-from-smoke-carryovers pattern the brief named, fired in its first instance at S39b. Recommend P12 audit names this distinctly as "verification-and-hygiene bridge sessions catch CLI runtime divergences that substrate sessions cannot catch with import-shape verification". The bridge-session shape has now produced its first methodology-promoting observation; whether to formally promote depends on whether a second bridge session at P12 or P13 surfaces a similar pattern.

- methodology (line 2): **LVT-query feedback loop in the real-corpus gold-set.** The substitute corpus document `lvt_methodology_overview.md` paraphrases the LVTGuide system prompt being seeded by migration 0013 at the same session. S40's retrieval-evaluation runner against this gold-set will report misleadingly high metric scores on the LVT query because the retrieval corpus and the agent's system prompt share LVT framing content. The artefact is sufficient for the S39b CLI-flow-verification purpose but cannot be a methodologically-clean evaluation baseline. **Carryover to S40 explicitly named:** the methodologically-clean evaluation gold-sets S40 needs require queries whose answers exist in the per-tenant corpus but NOT in the agent's system prompt. The S39b real-corpus gold-set should be marked as "CLI-flow-verification artefact" in any S40 documentation that consumes it.

- methodology (line 3): **CC-autonomously authoring is explicit in the smoke document.** The discovery-mode CLI is interactive at human-operator-time; at this session, CC operates the CLI autonomously and selects "correct" chunk indices from the retrieval candidates without a human in the loop. The smoke document's "CC-autonomously authoring provenance" section names this explicitly so any reader at P12 audit understands that the ground-truth annotations on the real-corpus gold-set reflect CC's rank-based selection, not a human operator's content-fit judgment. Carried as a P12 audit observation.

- methodology (line 4): **Rank-by-similarity ≠ best semantic answer.** Entry 3 illustrates the distinction. Query "what does the bet say about procurement-grade architecture" surfaced ten retrieval candidates with similarity scores. CC selected indices 1, 2, 3 by rank; candidate 4 ("the compliance and architectural constraints") was substantively as relevant or more so than candidate 3 ("the LVT four-levels framing"). A human operator authoring this gold-set with content-fit judgment would likely select 1, 4, 2 instead. The methodology distinction between rank-based and content-fit-based annotation discipline is a P12 audit observation. Recommend the methodologically-clean gold-set at S40 re-authors with content-fit judgment rather than rank-based heuristic.

- methodology (line 5): **Verification-and-hygiene bridge sessions as a session shape.** S39b is Phase 1's first verification-and-hygiene bridge session — distinct from substrate sessions (commit code), feature sessions (commit feature surfaces), and audit sessions (review and consolidate). Trigger condition: substrate session closes with smoke-evidence carryovers that aren't blocking but are real (LVTGuide absent, corpus empty, CLI not runtime-verified). Resolution: a session that resolves the carryovers and tightens the smoke evidence before the next feature session opens. S39b's shape worked: minimal code commits (migration 0013, runtime fix), one substitute-corpus draft, executed-state evidence, runtime divergence caught and fixed mid-smoke. Worth flagging at P12 as a methodology-promoting candidate shape; one instance is observation, recurrence at P12 or P13 would promote.

```
metrics:
  classification: verification-and-hygiene bridge
  brief_started: 2026-05-15
  session_started: 2026-05-15
  session_closed: 2026-05-15
  merged: 2026-05-15
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: log/captures.md (migration name-length convention); charter/current-package.md (S39b paragraph); briefs/p11/s39b.md
  corrects: S39 commit 8 CLI runtime drift (ChunkResult attribute access)
  corrected_by:
```

---

## S39 — P11 allowlist closure plus contexts/retrieval_evaluation/ substrate
roles: analyst, PM, architect, engineer, technical writer
mode: build (P11 opening session, new bounded context substrate)

- Produced: Ten commits closed S39 (plus the brief preservation folded into commit 1).
  - Commit 1 (`docs(charter)`, 7c41c6d) lands D109 at `charter/decisions.md` tail (revision-with-hash-chain aggregate following the methodology-context precedent; lifecycle ships at application-layer granularity for the first time in Phase 1); schema additions at `charter/schema.md` for `gold_sets`, `gold_set_revisions`, `gold_set_entries`; S39-in-flight paragraph in `charter/current-package.md`; brief preserved at `briefs/p11/s39.md` per the build-session brief discipline.
  - Commit 2 (`feat`, aea44ed) lands forward-only Alembic migration `0012_role_allowlist_retrieval_closure` on the control plane UPDATEing the eight seeded role-revision rows (LVTGuide plus the seven McKinsey 7-Step roles) with the retrieval tool reference pinned to the well-known UUIDs from migration 0009. The UPDATE recomputes each role-revision's `this_revision_hash` per D26 chain-self-containment using the `padhanam.security.hash_chain.compute_revision_hash` helper that migration 0010 established as the recompute pattern. Idempotent on re-run.
  - Commit 3 (`feat`, 92425d7) lands the retrieval_evaluation context's hexagonal substrate skeleton: `contexts/retrieval_evaluation/{__init__.py, pyproject.toml, domain/, application/, ports/, adapters/}`. Domain layer ships `GoldSet`, `GoldSetRevision`, `GoldSetEntry`, `GoldSetRevisionStatus` as frozen dataclasses with `__post_init__` invariants. Hash-chain wrapper at `contexts/retrieval_evaluation/domain/hash_chain.py` exposes `compute_revision_hash` and `revision_canonical_payload`; delegates to `padhanam.security.hash_chain.compute_revision_hash` (the third consumer of that primitive). Workspace registration at `pyproject.toml`, `apps/api/Dockerfile` COPY directive. Six import-linter contract updates (new layers contract + cross-context independence × 3 + domain-purity + 3 vendor-SDK confinements). Seventeen new unit tests including a golden vector pinning the canonical serialisation rule. 26 import-linter contracts kept.
  - Commit 4 (`feat`, fc96d5e) lands the application layer (five use cases per D109 commitment 6: `create_gold_set`, `append_entry_to_revision`, `finalize_revision`, `list_gold_sets`, `get_gold_set`) plus the two consumer-defined ports (`GoldSetRepository` write-side, `GoldSetReader` read-side with three result DTOs). Cursor pagination at `contexts/retrieval_evaluation/domain/query_filters.py` plus base64-of-JSON codec at `application/cursor.py` mirroring the run_history S33 codec. Nine new unit tests against in-memory fakes covering the full authoring lifecycle including the `append-after-finalize-opens-new-draft` semantics from D109 commitment 6.
  - Commit 5 (`feat`, 3ea4d64) lands the Postgres adapters (`PostgresGoldSetRepository` write-side and `PostgresGoldSetReader` read-side) against per-tenant data planes with bound-tenant defence-in-depth mirroring the S31/S33 pattern. Shared table definitions at `adapters/outbound/postgres/_tables.py` keep the row-shape contract single-sourced. Adapter unit tests against fake session deferred to a future hygiene session; smoke at commit 10 covers query-shape verification end-to-end.
  - Commit 6 (`feat`, 4f6339e) lands per-tenant Alembic migration `0013_retrieval_evaluation_substrate` creating the three tables with all CHECK constraints (status pin, finalize-row-shape pairing CHECK, hash length, query non-empty, expected_chunk_ids non-empty), UNIQUE constraints, the DEFERRABLE INITIALLY DEFERRED FK on `gold_sets.current_revision_id` resolving the circular dependency at the schema layer, and the `(gold_set_id, status)` index supporting the find_current_draft_revision read path.
  - Commit 7 (`feat`, 6963f30) lands the wiring: `apps/cli/_cross_context.py` extends with `GoldSetRepositoryAdapter` (eleventh consumer-port-plus-wiring-adapter class) and `GoldSetReaderAdapter` (twelfth); `apps/api/_agent_runtime_wiring.py` extends with `build_gold_set_repository` and `build_gold_set_reader` factories closing over the existing `TenantSessionFactoryCache`.
  - Commit 8 (`feat`, 2de0596) lands the CLI at `apps/cli/_retrieval_evaluation.py` exporting `retrieval_evaluation_app` registered under the `padhanam gold-set` namespace in `apps/cli/main.py`. Five subcommands (`create`, `append-entry`, `list`, `get`, `finalize`) with `append-entry` supporting both interactive prompting and non-interactive `--correct-indices` for smoke scripting; discovery-mode authoring uses `PgVectorSearch` directly per D5/D65.
  - Commit 9 (`test`, 9f87617) lands `tests/contract/tenant_isolation/test_retrieval_evaluation_isolation.py` extending the harness with 13 new scenarios (4 structural + 9 behavioural): table presence per-tenant, control-plane absence, deferred FK property, cross-tenant write isolation in both directions, two defence-in-depth ValueError checks (TenantContext mismatch + GoldSet.tenant_id mismatch on persist), cross-tenant get / list / find-draft / get-revision returning None, cross-tenant append raising before any insert.
  - Commit 10 (`docs`, a052857) lands `docs/smoke/p11_s39_retrieval_evaluation_substrate.md` as a five-stage procedural walkthrough plan covering pre-flight migration upgrade, allowlist closure verification on the eight roles including chain integrity recompute, discovery-mode authoring against tenant_a's corpus, finalize with hash-chain integrity verification (recompute via the platform primitive and byte-identical comparison against the stored hash), and list/get for the final view.

- Decisions: D109 landed at commit 1, revised at pre-write reconciliation. Six commitments cover the aggregate root shape, revision value object shape (status + hash-chain fields), entry value object shape (entry_index + expected_chunk_ids ordered array), hash-chain reuse pattern, structural precedent (methodology context, not scoring-sheet), and the five-use-case application layer. Six alternatives considered including (a) methodology inheritance (rejected on cross-context substrate coupling), (c) audit-context refactor with thin extracted helper (committed before pre-write reconciliation surfaced the existing platform primitive; see methodology line 1 below). Kano: must-have on aggregate shape, hash-chain reuse, application-layer lifecycle, methodology-as-precedent framing; performance on use case shape; indifferent on naming conventions.

- Tests: 1047 unit tests pass at session close. 26 new unit tests across `tests/unit/contexts/retrieval_evaluation/` covering domain invariants, canonical-payload sort/render, hash stability across in-memory entry ordering, golden vector pinning the canonical serialisation rule, and full authoring-lifecycle scenarios via in-memory fakes. 26 import-linter contracts kept (one new layers contract for retrieval_evaluation plus four contracts extended with the new context). Tenant-isolation contract harness extends by 13 scenarios; new file `test_retrieval_evaluation_isolation.py` skips cleanly without docker reachable, mirroring run_history precedent.

- Enforcement layer: New `layers-retrieval-evaluation` contract at `.importlinter` enforces the hexagonal layer order (adapters → application → ports → domain). `contexts.retrieval_evaluation.domain` added to `domain-purity` (no Pydantic, no SDK imports, no SQLAlchemy in domain code). `contexts.retrieval_evaluation` added to `litellm-confined`, `langfuse-confined`, `neo4j-confined` (the context has no SDK dependencies beyond SQLAlchemy + asyncpg at the adapter; `fastapi-confined` covers `contexts/` already). Cross-context independence contracts (domain, application, adapters) all extended with the new context. AST enforcement at `tests/_enforcement/test_dockerfile_workspace_coverage.py` requires the new context's pyproject COPY directive in `apps/api/Dockerfile`; landed at commit 3.

- Operator-decided dispositions at pre-write reconciliation:
  - **Finding 1 (role-seed migration shape and location):** Option (a) — new forward-only Alembic migration on the control plane that UPDATEs the eight named roles' allowlists to include the retrieval tool reference + recomputes role-revision hashes per migration 0010's helper pattern. Commit 2 lands this; commit message body explicitly names the hash recompute step (operator's instruction at finding disposition).
  - **Finding 2 (scoring-sheet aggregate divergence):** Option (i) — re-anchor D109 on `contexts/methodology/` as the structural precedent for revision-with-hash-chain; `contexts/evaluation/` is sibling-in-placement only. D105's "sibling-contexts-sharing-patterns" framing rested on a scoring-sheet premise that did not survive reconciliation. D105 not revised at S39; the P12 audit decides whether D105 needs a successor D-entry or whether the gold-set's structural-first status absorbs the correction. See methodology line 2 below.
  - **Finding 3 (hash-chain primitive reuse):** Option (c) — extract `compute_chained_payload_hash` inside `contexts/audit/domain/events.py` as a precursor commit 2.5; commit 3's wrapper would consume it. **This disposition reversed mid-build**; see methodology line 1 below.
  - **Findings 4-8 (autonomous):** Chunk_id is UUID array per ingestion convention; CLI pattern is typer (existing); tenant context flows through `build_tenant_wiring` per the dev-only label-or-UUID convention; composition root wiring at both seams; deferrable FK supported by the existing alembic surface (verified at the migration write at commit 6).

- methodology (line 1): **Mid-build Finding 3 correction — pre-write reconciliation as architectural discovery fires *inside* the build.** Pre-write reconciliation at session open scoped Finding 3 to `contexts/audit/domain/events.py` per the brief's named surface; the Explore agent reported `compute_event_hash` and `GENESIS_HASH` and the operator approved option (c) extract-a-helper. Commit 1 landed D109 with that commitment. Commit 2 then needed to mirror migration 0010's hash recompute pattern, so I read migration 0010 — and the import `from padhanam.security.hash_chain import compute_revision_hash` surfaced. The field-set-agnostic primitive promoted at S24 per D75 already exposes exactly the canonical-JSON-plus-SHA-256-with-chain-link mechanism D109's commit 2.5 would extract. The audit refactor was therefore unnecessary; commit 2.5 did not land. Commit 3's wrapper uses the platform primitive directly. The gold-set context became the third consumer of `padhanam.security.hash_chain.compute_revision_hash` after methodology and role — strengthening D75's second-consumer-promotes pattern observation (now third-consumer-confirms). The structural lesson generalises beyond this instance: pre-write reconciliation at session open scopes its reads against the brief's named surface; if the brief's surface is too narrow (it named the audit context but not the platform helper module that sits at the same architectural altitude), a mid-build reconciliation fires when the *adjacent* file is read for a related reason. The triggering act here was mirroring migration 0010's pattern for commit 2's recompute; this is "the act of using an adjacent piece of code surfaces something the pre-write read missed". Methodology-promotion candidate for the Phase 1 close audit: **mid-build pre-write reconciliation as a distinct sub-pattern** of the broader pre-write-reconciliation-as-architectural-discovery pattern. One instance against a different sub-substrate (within-build vs at-open); awaiting recurrence before P12 audit promotes formally.

- methodology (line 2): **D105's "sibling-contexts-sharing-patterns" framing did not survive S39 pre-write reconciliation.** Finding 2 surfaced that the scoring-sheet aggregate at `contexts/evaluation/` is read-only at S16 with no hash-chain, no status lifecycle, no write use cases. The actual structural precedent for revision-with-hash-chain is `contexts/methodology/` (the methodology and role aggregates carry chain-self-contained revision hashes per D26, even if their lifecycle is migration-time rather than application-time). D109's commitments and reasoning re-anchored on the methodology precedent. The gold-set ships as the first context in Phase 1 with the full create/append/finalize/list/get lifecycle at application-layer granularity with revision-granularity hash-chain. The optimization context at S41 inherits from the gold-set pattern. **D105 is NOT revised at S39.** The P12 audit decides whether D105 needs a successor D-entry or whether the gold-set's structural-first status absorbs the correction; flagging here for the audit per the operator's instruction at Finding 2 disposition.

- methodology (line 3): **Consumer-port-plus-wiring-adapter pattern reaches eleventh and twelfth instances at S39** (`GoldSetRepositoryAdapter`, `GoldSetReaderAdapter` at `apps/cli/_cross_context.py`). The pattern continues to do load-bearing work at every new-context-substrate session; the methodology-document promotion candidate noted at S29b (four reinforcements then) is now at eight more reinforcements (S29b through S39). Strong P12 audit promotion candidate.

- methodology (line 4): **No CLI-side wiring integration test surface exists at `tests/integration/apps/api/`.** The brief at commit 7 said "if no CLI-side wiring test pattern exists, surface the asymmetry in the session log entry as a methodology-candidate observation for the Phase 1 close audit." The asymmetry: the API composition root has the rich integration-test surface (`tests/integration/api/`) that exercises real wiring at request boundaries through the FastAPI app; the CLI composition root has unit tests against the typer CliRunner per `_composition.py`'s test-fixture pattern but no dedicated wiring integration test surface. For the gold-set wiring at S39 the unit-suite + smoke-document combination is sufficient verification; for future wiring additions where the integration-test surface would catch shape mismatches that unit tests miss, the asymmetry is a real gap. Phase 1 close audit input.

- methodology (line 5): **Field-naming inconsistency: gold-set revisions use `previous_event_hash` / `this_event_hash` while methodology and role revisions use `previous_revision_hash` / `this_revision_hash`.** D109 commitment 2 named the audit-mirror convention (`_event_hash`) because the original commitment 4 named the audit-context primitive as the reuse target. The mid-build Finding 3 correction shifted the reuse target to `padhanam.security.hash_chain.compute_revision_hash` (which uses `_revision_hash` internally as the payload key), but the field names on the GoldSetRevision dataclass and the database columns stayed as committed at commit 1 (`_event_hash`). The structural correctness is unaffected — the platform primitive's internal payload-encoding-key is independent of the field names on consumer aggregates — but the cosmetic inconsistency is real. Not changing at S39 to avoid commit-1 churn; the inconsistency is flagged in the gold_set_revision.py docstring and surfaced here for the P12 audit's consideration alongside the D105 framing-versus-as-built question from methodology line 2.

```
metrics:
  classification: new-context substrate
  brief_started: 2026-05-15
  session_started: 2026-05-15
  session_closed: 2026-05-15
  merged: 2026-05-15
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: D109; charter/schema.md; charter/current-package.md; briefs/p11/s39.md
  corrects:
  corrected_by:
```

## S38b — Pre-P11 working-tree hygiene: D107 session-log per-package archival convention, P0-P10 backfill bootstrap, working-tree triage
roles: analyst, architect, technical writer
mode: build (pre-package hygiene)

- Produced: Six commits closed S38b. Commit 1 (`docs(charter)`, 68fa304) landed D107 at `charter/decisions.md` tail committing the cadence shift from per-phase to per-package archival for `log/sessions.md`, with the one-time bootstrap clause covering P0 through P10; same commit edited the Token discipline section at `charter/principles.md` to make the per-package cadence visible at every session-open. Commit 2 (`docs(archive)`, 903da62) backfilled `log/sessions.md` (1879 lines at session open) into per-package files at `docs/archive/sessions/p<n>.md` for n in 1..10; 65 `##` sections distributed per Finding 5's header-naming classification rule with strategic blocks between packages archiving to the prior package's file; live `log/sessions.md` reduced to the 15-line format-preamble header; same commit removed the untracked `docs/archive/sessions/p0-p7.md` combined draft (Finding 3 disposition: split) and the untracked `docs/archive/snapshots/` stale-script-log directory (Finding 4 disposition: delete). Commit 3 (`chore`, b7bcb1c) added the `charter-2026*/` rule to `.gitignore` so the charter-export tooling's dated snapshot directories stay out of `git status`; the matching `.zip` files were already covered by the existing `/*.zip` rule. Commit 4 (`docs(archive)`, b0d8555) committed the 78-line additive correction at `docs/archive/packages/p9.md` capturing the S35a + S35b carryover-hygiene fold-in, harness-integrity restoration, the mid-S35b deliberate-violation-cycle architectural-assumption finding forwarded as P12 audit input, and the Phase 1 substrate-completeness state after P9 plus carryover hygiene (Finding 1 disposition: kept as additive correction with explicit post-archive-append rationale in the commit message). Commit 5 (`docs(charter)`, c0fd3e1) landed `charter/deck.html` as a charter-grade case-study artefact per the D91 brand precedent (Finding 2 disposition: commit to `charter/` as-is). Commit 6 (this commit) landed this session log entry at `log/sessions.md` after the format-preamble header.

- Decisions: D107 lands at S38b (session-log archival cadence shifts to per-package on close, forward from P11 close, with one-time S38b bootstrap covering P0-P10 and a strategic-block-to-prior-package classification rule). No other new D-entries; the four working-tree triage findings are operator-decided dispositions rather than architectural commitments, recorded in commit-message rationale rather than as new D-entries.

- Tests: No test surface touched at S38b (hygiene against working-tree state and documentation surfaces only). Unit-test, contract-test, integration-test, import-linter, and AST-enforcement surfaces unchanged at S38b since the work touches only charter documents, archive files, and `.gitignore`.

- Enforcement layer: No `import-linter` contract changes. No AST enforcement changes. The new D107 convention is enforced socially through CLAUDE.md and `charter/principles.md`'s Token discipline section rather than through tooling; the cadence is reviewed at each package close as part of the close commit. The Acceptance criteria at the S38b brief stand as the bootstrap-event-specific verification surface; future per-package archival events at P11 close onward will follow the same pattern but at smaller scope (one package's worth of sessions per close).

- Operator-decided dispositions at pre-write reconciliation:
  - **Finding 1 (p9.md modification):** Kept as additive correction. The 78-line block captures S35a + S35b carryover-hygiene content that became visible after the original P9 archive landed; the content thematically belongs with P9's retrospective record (S35a/S35b are P9 carryover sessions per session-log headers); append-only spirit preserved (no existing text rewritten or removed). Commit message names the post-archive-append carve-out explicitly.
  - **Finding 2 (charter/deck.html):** Committed to `charter/` as-is. Case-study deck is brand-aligned (uses `charter/brand/tokens.css` palette and typography committed at D91) and sits alongside `bet.md`, `phase-1-prd.md`, and `prfaq.md` as a binding-narrative artefact. The D91 brand-as-charter-grade precedent supports placement here.
  - **Finding 3 (docs/archive/sessions/p0-p7.md):** Split into per-package files. Sourced p1.md through p7.md from `log/sessions.md` per D107's bootstrap clause; superseded untracked combined draft removed in the same commit (commit 2). The combined draft's title "P1 through P7 close sessions" indicated it was a prior strategic-block draft that prepared a combined archive but never landed; the per-package shape is the D107-aligned replacement.
  - **Finding 4 (docs/archive/snapshots/):** Deleted. Directory contents were `.DS_Store` plus a `.last-run.log` (stale charter-export script log from May 11 onward; the referenced zip files were never checked in to that path). Removed as part of commit 2's working-tree cleanup.

- methodology: Reflection prompt 1 — backfill mechanics did surface two small structural surprises in the live session log file worth recording for the Phase 1 close audit. First, the section count came in at 65, not the 51 I'd initially estimated from the section-header grep output. The grep output included the `## Session N (Package P)` format-example line inside the preamble (line 7), so the raw grep count was 66; subtracting the preamble's format example leaves 65 real sections. The under-estimate was a mechanical reading error at finding-classification time, not a structural mis-classification of any section. Worth noting for future archival bootstrap operations: distinguish format-example headers from real section headers explicitly when counting, particularly for files whose format preamble names the section shape with a `##` heading inside a fenced code block (which `grep -c "^## "` still matches because grep does not parse markdown fences). Second, Package P2's session ordering at `log/sessions.md` is S4, S6, S5, S7, S8 — not strictly monotonic, because S6 was committed before S5 at the original write time per the session-log shape (the session-numbering convention was looser at P2). This is a known historical artefact and not a backfill issue, but the per-package archive p2.md now preserves the non-monotonic ordering in line with the source. Surfaced here as a small archival-completeness note rather than as a classification ambiguity.

- methodology (second): Reflection prompt 2 — whether the pre-write reconciliation surfaced any working-tree state that contradicts charter expectations, particularly the `docs/archive/packages/p9.md` modification. The modification did not surface a discipline drift signal under the operator-decided disposition; the additive-correction reading holds because S35a and S35b are P9 carryover sessions per the session-log headers themselves, and the content captures genuine P9 retrospective material that became visible only after the original archive landed. The discipline-question worth recording is narrower: archives are normally append-only at write time, and the 78-line block sat as an in-place modification rather than a clearly-marked seam (no explicit "Post-archive S35a/S35b carryover fold-in" header preceded the block, and the placement is inside the document rather than at the tail). The commit message that lands the modification now names the post-archive-append carve-out explicitly, which retroactively documents the seam. For future post-package-close additions to archive files, the discipline that landed here implicitly is: name the post-archive append in the commit message; preserve append-only spirit in the file content (no rewrite, no remove); place the addition at a thematically coherent seam rather than strictly at the tail when the content thematically completes a preceding section. Whether to promote this to a D-entry or a `charter/methodology.md` line depends on recurrence; one instance is observation, not pattern. The Phase 1 close audit at P12 close is the natural place to revisit if a second post-close archive addition surfaces.

- methodology (third): Reflection prompt 3 — pre-write reconciliation count update and the substrate-extension question. S38b is the nineteenth instance of the pre-write reconciliation practice (S38a was the eighteenth per its session log entry). The substrate-extension question worth recording: prior reconciliation instances were against vendor documentation, file-path facts, interface shapes, and (at S38a) failure-mode causal structure. S38b's reconciliation was against working-tree state — modified-tracked-files, untracked-files, ignored-state, and the contents of dated scratch directories at the repo root. Working-tree state is a different reconciliation substrate from the prior instances because the canonical-state surface is `git status` plus actual file contents rather than vendor docs or test failure output. The pattern itself is identical (read the actual state, surface findings, pause for operator disposition on the decision-dependent items, proceed autonomously on the unambiguous items). The novelty is the substrate, not the mechanism. Per the captures-candidate threshold (don't propose candidates for "existing methodology applied correctly" without structural novelty or 3x recurrence), this is one instance against a different substrate — observation rather than promotion candidate. The Phase 1 close audit at P12 close is the natural place to consolidate the substrate-class taxonomy if more substrate variants surface (e.g., reconciling against external-system state, against historical-commit state, against tooling-output state).

- methodology (fourth): The S38b bootstrap operation itself is worth a brief methodology note. The brief structured the work as one charter commit, one archive backfill commit, one gitignore commit, then triage commits per the operator-decided dispositions. The shape held cleanly — each commit's diff is interpretable in isolation; the audit trail at `git log --oneline` reads as a coherent sequence of cadence-shift, content-relocation, scratch-hide, and triage commits. The pattern is reusable for future bootstrap-style operations (cadence shifts, convention adoptions, archival reorganisations): one commit lands the convention with its D-entry; one or more commits land the content moves that the convention requires; one commit handles working-tree scratch hygiene; one-commit-per-disposition handles operator-decided working-tree triage. The order matters because the convention commit makes the content-move commit interpretable in audit, and the triage commits are independent leaves that can land in any order after the convention is committed. Worth noting for future S-prefix hygiene sessions whose deliverable is a cadence shift or convention adoption.

```
metrics:
  classification: pre-package hygiene
  brief_started: 2026-05-15
  session_started: 2026-05-15
  session_closed: 2026-05-15
  merged: 2026-05-15
  close_state: clean
  tests_passing: not_applicable
  principles_intact: yes
  charter_touchpoints: updated
  corrects:
  corrected_by:
```

