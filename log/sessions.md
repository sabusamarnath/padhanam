# Session Log

One entry per session. Append-only. Old entries archive at phase audits, never delete.

Format:
```
## Session N (Package P)
- Produced: [what shipped]
- Decisions: [D-numbers from decisions.md, or "none"]
- Tests: [pass/fail summary]
- Reflection: [one or two sentences. What was learned, what should change.]
```

---

## S102 — the CDD authoring layer: per-goal LLM-draft + user-proof, with provenance (D200) (build mode)

roles: architect (the authored-layer graph shape — reuse :Outcome, extend :Lever not fork, add :Intermediary/:External, FEEDS/INFLUENCES; the lever-constraint reconciliation; the consumer-port bridge over OutcomeGraphPort), engineer (the migration + composed-label Cypher; the draft use case over the structured-output seam; the proof read/write paths; the CDD proof surface; the tests), analyst (Step 0 against the live graph + inference seam; the live draft-quality read), technical writer (schema.md, the current-package marker, this entry).

- Step 0 (the live-surface guard, all reconciled). Latest Neo4j migration **0004 → new 0005**; node/edge inventory confirmed (`:Outcome`/`:Lever`/`:Unit`/`:Facet`; `LEVER_FOR`/`SERVES`/`SAME_WORK`); the `StructuredOutputPort.generate_structured(StructuredOutputRequest{prompt, schema, latency_tier, temperature?, model_hint?})` signature confirmed with the **checkin context as the exact consumer precedent** (domain holds the schema dict + pure parse, adapter wires the port). **The authored-lever fork settled at brief-altitude, no D201:** extend `:Lever` with a stable `lever_id` (nullable `commitment_id`) + a new `lever_id_unique_per_tenant`; the existing `lever_unique_per_tenant` stays — Neo4j exempts nodes missing a constraint property, so authored levers (no commitment) and matcher levers (no lever_id) coexist. **Step-0 correction recorded:** the tenant Alembic revisions are **date-prefixed (`2026…`)**, not `0038` as the brief expected — immaterial (graph-only session, Postgres untouched).
- Reflection 1 — **did the CDD frame fit all 8 goals? Yes, cleanly, and the sequence goal best of all.** D200 holds that a goal that cannot be drafted as levers/intermediaries/externals/outcome is evidence the frame does not fit. On the live draft against Ollama, **all 8 goals drafted coherently** — 4 levers + 3–4 intermediaries + a proofable expected outcome each, no goal resisting. The interesting stress case, **Get-a-job (sequence), drafted as a textbook D198 funnel**: levers (Apply / Network / Prepare for interviews / Update résumé) feeding intermediaries that are exactly the pipeline metrics (Application response rate, Interview invitations, Offer acceptance rate) feeding "Receive a job offer." That is the process-stage-as-intermediary shape D198 predicted, drafted unprompted — strong evidence the frame fits the process-shaped goal, not just the homeostatic ones. The homeostatic goals (Strength, Litany, Stretch, Voice, Health) also drafted clean lever→intermediary→outcome chains. So the frame held across all three modes; no goal's resistance signalled a frame mismatch this round.
- Reflection 2 — **is the three-value provenance enum sufficient?** Yes for this exercise. Every drafted element was `llm_drafted`; `user_authored` is the correct-path flip; `system_suggested` is reserved for S103's emergent-suggestion loop. No fourth origin appeared — authoring, correction, and system-proposal cover the cases the model produces. Revisit only if S103's suggestion loop surfaces an origin these three cannot name.
- Reflection 3 — **was the Ollama dev model strong enough?** For **levers, intermediaries, and expected outcomes: yes** — coherent, goal-specific, proofable, not noise. The honest weakness: the model drafted **zero externals across all 8 goals**, even Get-a-job where a hiring freeze or a recruiter's inbound are real externals (D198's externals). The externals dimension is the weakest of the four; a sharper externals prompt or a stronger `model_hint` for the authoring call is **named here, not solved here** (the two-threshold rule — fix it when the externals gap actually bites the proof).
- Reflection 4 — **did the proof surface read true on the real corpus?** The **data** the surface renders is verified good (the drafted CDDs above are clearly worth proofing). The **render** itself (the CDD tab, accept/edit/reject) is **operator-gated** — the instance is Google-login wired, no headless backdoor — exactly the S101 idiom; the drafts are persisted and ready, the eyeballing is the operator's.
- methodology: Step 0 paid off as a cheap guard again — the highest-risk assumption (the lever fork) was settled first, against the live `:Lever` constraint, so commit 1 shipped a shape that coexists with the matcher rather than colliding with it. And the live draft (running the use case in-container against real Ollama + the real graph) turned reflection-3 from a guess into a measurement: the externals gap is now evidence, not anticipation.
- AC verdicts: AC1 (migration idempotent on the live graph) ✓ live; AC2 (8 goals drafted via StructuredOutputPort/Ollama, ≥1 lever + expected outcome, zero vendor SDK in domain) ✓ **live**; AC3 (provenance/proof/tenant on every element; cross-tenant read returns none) ✓ live red-team; AC4 (proof read + accept/reject/correct) ✓ test; AC5 (proof surface on /app, served) ✓ code+served, the live proof pass operator-gated; AC6 (tenant isolation on new node types; import-linter + AST green; suite green with the schema-shape guard) ✓; AC7 (matcher untouched — grep confirms no correlate/SERVES-write change) ✓; AC8 (schema.md committed charter-first ahead of code) ✓.
- Carry-forward (all S103+ per the brief): the **matcher rewrite** from goal-linking to element-evidence (an email attaches to the response-rate intermediary, not the goal whole); the **ongoing edit loop + correction-as-learning-signal** (v13 increment 2); the **co-evolving suggestion loop** (`system_suggested`, increment 3); the **externals draft-quality** gap (a model_hint/prompt question); cross-goal edges/traversal/ripple (D162 post-week); the Flow/portals + `step_state`-to-status (D198 post-week). The benign `INFLUENCES` UnknownRelationshipType warning clears once an external edge exists.
- Close state: **the authored CDD layer is live — 0005 applied, the 8 goals drafted and persisted `llm_drafted`/`pending`, the proof endpoints + surface served; no D201 (brief-altitude shape); matcher untouched; suite 2304 + enforcement + contract green, import-linter 48/0.** The proof eyeballing is operator-gated.

```
metrics:
  classification: build session (S102 — the CDD authoring layer: per-goal LLM-draft + user-proof + provenance; executes D200)
  session_started: 2026-06-18
  session_closed: 2026-06-18
  step0: live reconciliations — neo4j latest 0004 -> 0005; StructuredOutputPort signature + checkin consumer precedent confirmed; authored-lever fork settled brief-altitude (lever_id + nullable commitment_id; lever_id_unique_per_tenant alongside lever_unique_per_tenant; no D201); Postgres untouched (Alembic now date-prefixed 2026..., not 0038 — recorded)
  schema: :Intermediary + :External node types, :Lever extension (lever_id/label/provenance_origin/proof_state, nullable commitment_id), FEEDS/INFLUENCES edges; provenance_origin {llm_drafted,user_authored,system_suggested}; proof_state {pending,accepted}; migration 0005_authored_cdd.cypher (idempotent, tenant-scoped, behind the wrapper)
  draft: 8/8 live goals drafted via StructuredOutputPort against Ollama — 4 levers + 3-4 intermediaries + expected outcome each, llm_drafted/pending; Get-a-job = textbook D198 funnel (Apply/Network/Prepare/Update -> response-rate/interview-invites/offer-rate -> offer); ZERO externals across all 8 (the reflection-3 quality gap, named not solved)
  proof: read + accept (proof_state->accepted) + correct (origin->user_authored) + reject (user-initiated delete); two new permissions DAILY_DRIVER_CDD_READ/WRITE (D126 pattern), granted to operator; draft retrofit ASSESSMENT_READ -> CDD_WRITE
  surface: List/Map/CDD toggle on How-am-I-doing; per-goal fold -> expected outcome + levers/intermediaries/externals with provenance+proof badges + Accept/Edit/Reject; served (data-mode=cdd live on /app)
  tests: tests/unit 2304 passed; in-container red-team tenant-isolation on the new node types + live-constraint check; migration-shape guard (code whitelist <-> migration); import-linter 48/0; AST no-vendor-in-domain + no-raw-neo4j green
  matcher_untouched: confirmed (grep — no correlate_goal_facets / SERVES-write change in the S102 diff)
  live: image re-pinned 983b256 (make build-api + force-recreate); 0005 applied in-container via ops.migrate; 8 goals drafted+persisted on the live personal-tenant graph; proof eyeballing operator-gated (Google login)
  commits: 4f2e9b6 (D200 pivot, prior turn), 405c237 (charter schema + marker), 60c293f (graph layer), 8f2c3f1 (draft), 8c88426 (proof paths), 6cfbd52 (surface), 2a11a6d (tests), 0dde3af (digest bump)
  charter_touchpoints: charter/schema.md (authored CDD layer); charter/current-package.md (S102 marker; S101 closed); docs/smoke/p2b_s102_authored_cdd.md; log/sessions.md (this entry). No D201 (brief-altitude shape, no framing fork surfaced)
  numbering: S102; D200 the live decision max, D201 reserved but unused (no framing fork); schema in schema.md per D200
  corrects:
  corrected_by:
```

## S102m — Charter and log compaction: bound the living-state files, window the ledgers
roles: PM (the retention policy and its placement under D113), architect (the living-state-vs-ledger split and the size-check shape), technical writer (the methodology rule, the archive snapshots, the pointer block, this entry), analyst (the Step 0 reconciliation that caught the D107 conflict)
mode: maintenance and methodology, markdown only — no code, no graph, no vendor deploy. Numbered S102m (maintenance suffix, the a/b idiom): the product sequence stays S103+ untouched; the S102 marker's "(S103)" forward-ref stays verbatim per the v8 drift-safety rule and now resolves to the matcher product session under the split.

The charter and log totalled ~645k tokens and the read-at-start ritual no longer fit one context window. Two living-state files drove it: `log/sessions.md` at ~312k (half the total) and `charter/current-package.md` at ~72k against a one-package contract. This session bounds both and writes the rule that keeps them bounded — the rule first, ahead of the surgery, so policy precedes action.

- **Step 0 finding (corrected premise).** The session prompt assumed a new `log/archive/` directory, one file per phase. Reconciliation against the live tree found the established D107 archive convention already in place: `docs/archive/sessions/` (per-package, p1–p10), `docs/archive/packages/` (per-package snapshots), `docs/archive/decisions/` (per-phase `phase-1.md`), README-documented. Creating `log/archive/` would have forked a parallel scheme — the exact charter-vs-archive inconsistency the new rule prevents, and a conflict with a binding decision (D107) that CLAUDE.md says to surface before building. Surfaced; the operator confirmed the destination (`docs/archive/sessions/`, no `log/archive/`), overruled my per-phase lean to **per-package** (continue p1–p10 to the letter of D107), and confirmed the governing principle for edge cases: per-package where content buckets verbatim, a dated snapshot where it cannot. A second reconciliation: D107's literal cadence is **per-package-on-close**, so the hot window tightened from the phase boundary (S74) to the **open package** (S102 only), aligning "current" for both living-state files. No D-entry minted — resolving a conflict by complying with D107 is not a new decision (D113: operating-model discipline lives in methodology.md).

- **Reflection 1 — did the cut land cleanly?** The cut is by open-package, not by a phase-header parse, so no audit reads an entry under the wrong phase: each archived entry's home is documented in the pointer block. One entry was genuinely ambiguous — **S64** (the test-integrity / clock-seam slice), numbered in the P16 range, themed like Phase-2-B clock-seam work, but actually the Phase-2-A gate test-integrity precondition (roadmap v8) that ran during the dogfood-gate sprint. Bucketed to p19 by *when it ran*, recorded in p19's note. The other surprise the cut exposed: the hot file held the entire inter-phase **Phase-2-design 7-Step arc** (~13 entries, no package of its own), folded into p12 — the close-audit period that launched it — rather than minting a third file type.

- **Reflection 2 — post-compaction read-at-start cost (measured).** The two compacted files: **387k → 3.5k tokens** (−99.1%) — current-package 72.5k→0.8k, sessions 314.5k→2.7k. The read-at-start ritual (bet, principles, decisions-index, current-package, hot sessions): **~490k → ~106k tokens**, now fitting one window. Charter+log overall: ~645k → ~261k. `decisions.md` (92k) is now the dominant remaining read-at-start cost.

- **Reflection 3 — is decisions.md the next pressure point, and its trigger?** Yes — at 92k tokens it is now the single largest read-at-start file. Its body split into era files behind the index waits for the **two-threshold trigger**: (a) the index-only read *demonstrably blocks* a session — a session needs decision **bodies** and the 92k whole-file read no longer fits alongside the rest of the ritual — **and** (b) the file crosses a second size bound (~150k tokens). Until both fire, the index resolves every D-number to a title and the file stays whole, because the cross-reference risk of splitting bodies (D-entries reference and supersede each other by number; a split risks a cross-file reference going stale — the v3 charter-vs-archive drift class) is real and not paid before it must be.

- **AC verdicts.** AC1 (current-package = one package, fraction of 72k) ✓ — 783 tok, 1 marker, grep clean. AC2 (closed markers archived verbatim, session list intact) ✓ — 753 lines moved, diff byte-clean. AC3 (sessions.md open-package only, fraction of 312k; prior archived verbatim) ✓ — 2,690 tok; line multiset byte-identical to the pre-window file. AC4 (pointer block names each file + range, resolves any S-number) ✓ — verified S43→p13, S55a→p15, S74→phase2b. AC5 (decisions.md byte-unchanged) ✓ — `git diff` empty; index already complete through D200, no correction needed. AC6 (retention rule present, committed charter-first ahead of the moves) ✓ — commit `0fd6a03` precedes the three move commits. AC7 (size check runs, reports every file, no external dep) ✓ — stdlib-only, `make charter-size` green. AC8 (no content deleted; counts conserved) ✓ — 4659 archived + 48 kept = 4707 lines; 110 archived + 1 hot = 111 entries; multiset byte-identical.

- **methodology:** Step 0 paid for itself again — the prompt's load-bearing premise (a new `log/archive/`) was wrong against the live tree, and catching it turned the rule from a fork of D107 into an operationalisation of it. The pattern worth naming: a maintenance prompt that proposes new structure must reconcile against the existing archive tree first, because the cheapest place to catch a charter-vs-archive fork is before the file surgery, not after. The operator's per-phase→per-package override is the same lesson from the other side: consistency with the established scheme beats a smaller file count for a session whose entire purpose is consistency.

- Close state: **four commits — the methodology rule first (`0fd6a03`), then archive-current-package (`7275cab`), window-sessions (`6a509b1`), size-check (`4f1f180`).** current-package holds one open package; sessions holds the open package's one session (S102) plus this entry; S38b–S101 live under `docs/archive/sessions/` per-package (p11–p20) plus the Phase-2-B snapshot (phase2b.md); the closed current-package journal lives at `docs/archive/packages/current-package-history-through-s101.md`. Nothing deleted; full history pointer-reachable. The size check guards the bound at every future package close.

```
metrics:
  classification: maintenance + methodology session (S102m — charter and log compaction; operationalises D107, no D-entry)
  session_started: 2026-06-18
  session_closed: 2026-06-18
  retention_rule: charter/methodology.md "Charter and log retention (living-state versus ledger)" — living-state files bounded by contract (current-package = one package; schema.md = current-truth reference), ledgers windowed (sessions.md = open package, archived per-package on close per D107; decisions.md whole behind index until two-threshold trigger); archives append-only never pruned; size check at package close
  compaction: current-package.md 290,009B/~72.5k tok -> 3,149B/~0.8k tok; log/sessions.md 1,258,030B/~314.5k tok -> 10,881B/~2.7k tok; read-at-start ~490k -> ~106k tok
  destination: docs/archive/ tree (NOT a new log/archive/) — sessions per-package p11-p20 + phase2b.md snapshot (S74-S101, no P-numbers); packages snapshot current-package-history-through-s101.md
  conservation: 4659 archived + 48 kept = 4707 lines; 110 archived + 1 hot = 111 entries; line multiset byte-identical to pre-window file (verbatim, nothing deleted)
  size_check: ops/charter_size_check.py + `make charter-size` (stdlib-only; ARGS=--check gates); bounds current-package 20k / sessions 60k tok; decisions.md 92k surfaced as next pressure point
  next_pressure_point: charter/decisions.md ~92k tok — body split waits for the two-threshold trigger (index-only read blocks AND ~150k second bound)
  step0: corrected premise — prompt's log/archive/ per-phase forked from the live D107 docs/archive/sessions/ per-package tree; reconciled to docs/archive/ + per-package (operator-confirmed); D107 cadence per-package-on-close tightened hot window to the open package; ambiguous entry S64 bucketed to p19 by when it ran; Phase-2-design arc folded into p12
  ac: AC1-AC8 all ✓
  commits: 0fd6a03 (methodology rule, charter-first), 7275cab (archive current-package tail), 6a509b1 (window sessions.md), 4f1f180 (size check)
  charter_touchpoints: charter/methodology.md (retention rule); charter/current-package.md (windowed to S102); log/sessions.md (windowed to S102 + pointer block + this entry); docs/archive/packages/current-package-history-through-s101.md; docs/archive/sessions/p11.md..p20.md + phase2b.md; ops/charter_size_check.py; Makefile (charter-size target). decisions.md byte-unchanged.
  numbering: S102m (maintenance suffix); product sequence S103+ untouched; S102 marker's "(S103)" forward-ref left verbatim per v8 drift-safety
  corrects:
  corrected_by:
```

---

## S103a — authoring completion: surface the outcome, add elements, reclassify across types (D200, D201) (build mode)

roles: architect (the reclassify mechanic over the live label/id-property whitelist — identity-preserving label swap with id-move, the edge-grammar flag-not-drop semantic, D201; the outcome-as-proofable-stance shape with schemaless props and no migration), engineer (the three write paths across the full vertical — Cypher → bound session → repository → port → bridge → use case → router → DTO → surface; the modeling-fake unit tests + the live in-container reclassify guard), analyst (Step 0 against the live S102 layer — the id-property wrinkle, the no-migration finding, the matcher-untouched confirm), technical writer (D201, schema.md, the current-package marker, the smoke doc, this entry).

- **Step 0 (the live-surface guard, all reconciled; one premise sharpened).** Confirmed migration latest **0005** and that **no 0006 is needed** — the only new persisted state (the `:Outcome` authored-outcome `origin`/`proof_state` properties and the edge `needs_review` flag) is schemaless with no constraint. The brief flagged "the prompt assumes label; correct if wrong" — element type **is** a node label, so reclassify is a label swap, **but** the `_AUTHORED_NODE` whitelist keys `:Lever` by `lever_id` and `:Intermediary`/`:External` by `element_id`, so crossing the lever boundary **moves the id value between properties**, not just the label. That wrinkle is the heart of the reclassify Cypher and the load-bearing Step-0 catch. The drafted outcome **was** persisted (`o.authored_expected_outcome`) but with no proof/origin, so the read coalesces the 8 S102 drafts to `pending`/`llm_drafted`. `DAILY_DRIVER_CDD_READ`/`WRITE` (D126) cover the new paths — no new permission. Matcher untouched (graph-only, no Postgres).

- **Reflection 1 — the externals could-the-model-have-known test (the substantive paragraph).** The empirical per-external add-tally is the operator's gated live pass to produce (adding by hand on the authed 8-goal corpus needs the browser), so this is the reasoned analysis the corpus and the S102 draft already support, with the empirical confirmation marked as the gated follow-up. The S102 draft produced **zero externals across all 8 goals** while drafting levers/intermediaries/outcomes coherently — so the gap is uniform, not goal-specific, which already argues it is a **draft-prompt weakness, not eight separate emergent surprises**. Classifying by goal type: for the **process/sequence goal (Get-a-job)** the externals split cleanly into a knowable *category* and an emergent *instance* — "macro hiring conditions" and "inbound recruiter interest" are external *slots* any job search always has (knowable from the description; the draft prompt should always propose them), but "a hiring freeze hit this month" or "this recruiter emailed Tuesday" are *instances* that only become knowable from an event that arrives. For the **homeostatic goals (Strength, Health, Voice, and the cadence siblings)** standing externals are sparse and mostly emergent — an injury, an illness — events, not derivable from "hold the rhythm." So the honest tally is **dual-natured**: the *types* of external are largely knowable-from-description (a draft-prompt weakness S103b should fix by having the draft enumerate external categories per goal-type), while the *specific instances* are emergent (S104's no-match→propose loop fills them as events arrive). The finding is therefore **not** "strengthen the draft" *or* "route to emergent" but **both, at two different altitudes** — a one-line **D200 refinement candidate** (externals are knowable as category, emergent as instance) surfaced here for strategic promotion, not minted from this build session (D47). The operator's live add pass will confirm or adjust the category/instance split per goal.

- **Reflection 2 — reclassify frequency.** Also gated for the empirical count (reclassifying on the authed corpus is the browser pass), reasoned here from the S102 draft record. S102's reflection-1 found all 8 goals drafted **coherently** into levers/intermediaries/outcomes — the draft's error was **omission (zero externals)**, not **mis-classification**. So the predicted reclassify frequency across the lever/intermediary/external boundary is **low**, and the dominant authoring act is expected to be **add (externals)**, not reclassify. The most plausible reclassify, if any, is an **intermediary → external** on Get-a-job, where a drafted pipeline intermediary ("interview invitations") sits near the boundary with an external (the *other party's* decision to invite). That a mis-type would land exactly on the intermediary/external seam is itself a signal that D198's boundary is sharpest between lever↔intermediary (the draft got that right) and blurriest at intermediary↔external — which S103b's matcher inherits as the boundary most likely to carry correction signal.

- **Reflection 3 — is the model now proofable end to end?** The **capability** is complete and verified: every element type is reachable to add, the outcome is a proofable terminal element, and reclassify works end-to-end on the **live** surface (the in-container contract test reclassifies a lever to an external on real Neo4j — identity preserved, origin flipped, the FEEDS edge flagged not dropped, tenant-isolated). So no element type is unreachable; the unblock the session existed for is delivered. Whether **each of the 8 goals actually holds a coherent proofed CDD** — the per-goal state S103b's matcher needs as real targets — is the operator's gated authoring pass (add the missing externals, proof each outcome, reclassify any mis-type). The capability is done; the corpus-completion is the gate. Named for S103b: it should not open until the operator's pass confirms each goal holds a CDD worth proofing signal against.

- **methodology:** Step 0 paid for itself precisely on the one thing the brief told me to verify — "the prompt assumes label; correct if wrong." The label assumption held but the **per-kind id-property** asymmetry did not surface from the brief, only from reading the live whitelist; building reclassify as a naïve label swap would have produced nodes the read query silently can't find (a fixture-green / live-broken trap exactly of the class the "proof on fakes is not a proof on the real surface" principle names). The corrective that made it safe was running the reclassify against **live Neo4j** in the contract test, not only the modeling fake — the fake encodes my *belief* about the semantics; the live test proves the Cypher. The other note: the substantive reflections here are **reasoned, not empirical**, because the data they want (hand-added externals, observed reclassify frequency) lives behind the operator's gated browser pass — recording the reasoning with the empirical confirmation explicitly deferred is the honest form, rather than manufacturing a tally I did not run.

- **AC verdicts.** AC1 (outcome renders as a proofable terminal element with accept/edit/reject; Step 0 confirmed persisted, now surfaced) ✓ code+served, the browser eyeballing operator-gated. AC2 (add any of four types; user_authored/accepted, tenant+jurisdiction set, cross-tenant read returns none) ✓ — unit (origin/proof + default edge) + the add reuses `merge_authored_element` whose live isolation is covered; the merge Cypher sets tenant/jurisdiction/outcome_id/element_id. AC3 (reclassify preserves node + id, origin flips, invalid edge flagged not dropped, verified by test) ✓ **live** (the in-container reclassify guard) + unit. AC4 (every write through CDD_WRITE; cross-tenant denied; isolation covers add+reclassify) ✓ — decorators + live isolation (reclassify), add via the covered merge path. AC5 (matcher unchanged — `correlate_goal_facets`/`SERVES` untouched, grep-confirmed) ✓ — no matcher file in the diff. AC6 (lens shows add+reclassify+proofable outcome; live pass advances pin with `make build-api`; code+test passes without operator) ✓ served (pin `b1e136fa`), browser eyeballing operator-gated. AC7 (full authoring pass — every type reachable, outcome proofable, reclassify end-to-end, no type unreachable) ✓ capability complete (live contract test + served surface); the per-corpus live pass operator-gated. AC8 (suite green; import-linter 48/0 + AST green; charter-first commit ahead of code) ✓ — `f186ecc` precedes all code commits.

- Close state: **seven commits — charter-first (`f186ecc`: marker + schema + D201), then outcome-proof (`dea7633`), add (`49eb0ba`), reclassify (`73d5e13`), surface (`5ae3369`), tests (`b14e56e`), pin+smoke (`959063d`).** No migration (schemaless over 0005). D201 minted (framing, the reclassify correction semantic). Unit suite green; `tests/_enforcement` green; import-linter 48/0; the authored-CDD contract tests 3/3 live on the rebuilt image (incl. the new reclassify flag-not-drop guard). Image re-pinned `b1e136fa`; the full browser authoring pass on the personal corpus is operator-gated.

```
metrics:
  classification: build session (S103a — authoring completion: surface the outcome, add elements, reclassify across types; executes D200, mints D201)
  session_started: 2026-06-18
  session_closed: 2026-06-18
  step0: live reconciliations — neo4j latest 0005, NO 0006 needed (outcome proof/origin props + edge needs_review flag are schemaless, no constraint); element type IS a node label so reclassify is a label swap, BUT the _AUTHORED_NODE whitelist keys :Lever by lever_id and :Intermediary/:External by element_id, so crossing the lever boundary moves the id value between properties (the load-bearing catch); drafted outcome persisted as o.authored_expected_outcome with no proof/origin -> read coalesces 8 S102 drafts to pending/llm_drafted; CDD_READ/WRITE (D126) cover the new paths, no new permission; Postgres untouched
  schema: no migration; new schemaless persisted state on the 0005 shapes — :Outcome authored_outcome_origin + authored_outcome_proof_state (proofable outcome stance); FEEDS/INFLUENCES edge needs_review flag (D201, reclassify); documented in schema.md
  outcome_proof: read returns expected_outcome + origin + proof_state; accept (proof->accepted), correct (text + user_authored/accepted), reject (clears the stance, :Outcome node never deleted); proof_goal_cdd.accept/correct/reject_cdd_outcome behind CDD_WRITE
  add: author_cdd.add_cdd_element — user_authored/accepted + default edge to outcome (lever/intermediary FEEDS, external INFLUENCES); outcome type routes to the outcome stance; the path by which externals enter the model
  reclassify: author_cdd.reclassify_cdd_element (no-op same-kind rejected) -> _reclassify_authored_element_cypher (label swap, id-prop move when crossing lever boundary, drop lever-only commitment_id, flip origin to user_authored) + _flag_reclassified_edges_cypher (needs_review on now-ungrammatical outgoing edges, never deleted); D201
  surface: daily_driver.html CDD lens — proofable outcome row (accept/edit/reject), per-element "Move to..." reclassify select, add control (4 types), needs_review flag note; beside the S102 accept/edit/reject
  tests: tests/unit/contexts/daily_driver/test_cdd.py +11 (required_edge_type, add origin/proof+edge, reclassify identity/flip/flag-not-drop/no-op/compatible-no-flag, outcome accept/correct/reject) via a modeling fake; tests/contract/tenant_isolation/test_authored_cdd_isolation.py +1 live (reclassify flag-not-drop + isolation on real neo4j); unit suite green; tests/_enforcement green; import-linter 48/0
  matcher_untouched: confirmed (no matcher file in the diff; correlate_goal_facets / SERVES-write unchanged)
  live: image re-pinned b1e136fa (make build-api + force-recreate, healthy); authored-CDD contract tests 3/3 live on the rebuilt image; served HTML carries the add/reclassify/outcome affordances (grep in-container); full browser authoring pass on the personal corpus operator-gated (Google login)
  commits: f186ecc (charter-first: marker + schema + D201), dea7633 (outcome proof), 49eb0ba (add), 73d5e13 (reclassify), 5ae3369 (surface), b14e56e (tests), 959063d (pin + smoke doc)
  charter_touchpoints: charter/current-package.md (S103a marker); charter/schema.md (outcome proof/origin props, edge needs_review flag, authoring-completion note); charter/decisions.md (D201 index + entry); docs/smoke/p2b_s103a_authoring_completion.md; log/sessions.md (this entry). No migration (schemaless over 0005)
  numbering: S103a; D201 the live decision max (the reclassify correction semantic, settled as framing per the brief); schema in schema.md, no migration
  reflection_1_externals_tally: reasoned (empirical per-external add-tally is the operator's gated live pass) — uniform zero-externals draft across all 8 => draft-prompt weakness not 8 emergent surprises; externals are DUAL-natured: knowable as CATEGORY (slot, draft-prompt fix at S103b) but emergent as INSTANCE (fill, S104 loop); D200 refinement candidate surfaced for strategic promotion, not minted here (D47)
  reflection_2_reclassify_frequency: reasoned (gated for the empirical count) — predicted LOW (S102 drafted coherently; error was omission not mis-type); dominant act is add(externals) not reclassify; the boundary most likely to carry mis-type/correction signal is intermediary<->external (Get-a-job), the blurriest seam of D198
  reflection_3_proofable_end_to_end: capability complete + live-verified (every type addable, outcome proofable, reclassify end-to-end on real neo4j); per-goal coherent-proofed state is the operator's gated pass; S103b's matcher should not open until the operator confirms each goal holds a CDD worth proofing signal against
  d200_refinement_candidate: externals are knowable-as-category, emergent-as-instance (from reflection 1) — for strategic promotion, lands in S103b (strengthen draft to enumerate external categories) or S104 (emergent instance fill) per the operator's confirmed tally
  corrects:
  corrected_by:
```

---

## S103a-fix — expose the edit affordance and the draft-missing empty state (D200) (build mode, surface-only)

roles: engineer (the in-place edit control over the existing correct path; the empty-state read; the served-HTML guard), analyst (Step 0 against the running surface — the edit control was present-but-unrecognisable, not absent), technical writer (the current-package note, this entry).

- **Step 0 (correction recorded).** The brief framed the edit control as possibly absent; reconciliation against the running `daily_driver.html` found it **present but unrecognisable** — a `window.prompt`-based "Edit" ghost button on both the element row and the outcome row, already wired to the S102 `/correct` paths (which flip `provenance_origin` to `user_authored`). So the fix made it a **clear in-place editor**, not a new path. The draft-missing handler already returned the `results` with `skipped_existing` per goal, so the empty state reads the existing zero-count, no new field. No graph, no migration, no endpoint, no permission, no matcher.

- **The two gaps closed.** (1) Editing in place: clicking Edit swaps the row for a labelled input + Save/Cancel (Enter saves, Escape cancels) on each element **and** the proofable outcome, over the existing `/correct` path — so a mis-drafted label is corrected without the reject-then-re-add workaround that breaks element identity and incident edges before the matcher binds. (2) Draft-missing empty state: when every goal already has a CDD the lens shows "All goals already have a CDD" (reading `skipped_existing`), not a silent "Drafted 0 goals" that reads as failure.

- **Reflection — did in-place edit close the reject-and-re-add gap?** At the surface+test level, yes: the edit affordance is now an inline editor wired to the existing correct path (origin → `user_authored`), served on the rebuilt image (grep-confirmed), so correcting a label no longer requires reject-and-re-add and no longer disturbs element identity or incident edges. The in-browser confirmation that it *reads* clearly on the real corpus is the operator's, per the S101 idiom — the gap this patch closes is the *availability and clarity* of in-place edit, which is delivered and guarded; the eyeball is gated.

- **AC verdicts.** AC1 (each element + the outcome render a visible edit control; editing flows through the existing correct path and flips origin to user_authored; no new write path in the diff) ✓ — served + the surface guard; the diff adds no endpoint/use case. AC2 (draft-missing shows a clear empty state when all goals have a CDD; no new action) ✓ — served + guard. AC3 (surface-only: no schema, no migration, no new endpoint/permission; matcher and all S103a write paths untouched) ✓ — the diff is `daily_driver.html` + the marker + the surface test only (name-only grep clean). AC4 (suite green; import-linter 48/0 + AST green; served-HTML guard asserts the edit control; operator-gated live check on the rebuilt image) ✓ — unit suite exit 0, enforcement exit 0, import-linter 48/0; image re-pinned `c76dac4a`, served HTML confirmed; browser eyeball operator-gated.

- Close state: **five commits — marker first (`da2626b`), edit affordance (`6a48272`), empty state (`b165276`), served-HTML guard (`49fffad`), pin bump (`ad0bc83`).** Surface-only; no D-entry (exposing an existing path is not a decision); no schema, no migration. Step 0 correction recorded (control was present-but-unrecognisable, not absent).

```
metrics:
  classification: build session (S103a-fix — surface patch over S103a: in-place edit affordance + draft-missing empty state; D200, no decision)
  session_started: 2026-06-18
  session_closed: 2026-06-18
  step0: corrected premise — the edit control was PRESENT but unrecognisable (a window.prompt ghost button on element + outcome rows, wired to the S102 /correct paths), not absent; fix makes it a clear in-place editor over the same path. draft-missing already returns results+skipped_existing, so the empty state reads the existing zero-count (no new field). surface-only — no graph/migration/endpoint/permission/matcher
  gap_1_edit: in-place editor (inline input + Save/Cancel, Enter/Escape) on each element AND the outcome, over the existing /correct path (origin -> user_authored); replaces the unrecognisable window.prompt; closes the reject-and-re-add workaround that broke element identity + incident edges
  gap_2_empty_state: draft-missing shows "All goals already have a CDD" (reads skipped_existing) instead of a silent "Drafted 0 goals"; "No goals to draft" for the empty corpus
  surface_only: confirmed — diff is apps/api/static/daily_driver.html + charter/current-package.md + tests/unit/apps/api/test_daily_driver_surface.py (+ compose.yaml pin); no schema/migration/endpoint/permission; matcher + all S103a write paths untouched (name-only grep clean)
  tests: tests/unit/apps/api/test_daily_driver_surface.py +3 (in-place edit over /correct with no window.prompt fallback on element+outcome; draft-missing empty state; the S103a add/reclassify affordances); unit suite exit 0; tests/_enforcement exit 0; import-linter 48/0
  live: image re-pinned c76dac4a (make build-api + force-recreate, healthy); served HTML carries cddBeginEdit + cdd-edit-input + the empty-state string (grep in-container); in-browser eyeball operator-gated (Google login)
  commits: da2626b (marker, charter-first), 6a48272 (edit affordance), b165276 (empty state), 49fffad (served-HTML guard), ad0bc83 (pin bump)
  charter_touchpoints: charter/current-package.md (S103a-fix note under the S103a marker). No schema.md, no decisions.md (no D-entry — exposing an existing path is not a decision). log/sessions.md (this entry); compose.yaml (digest pin)
  numbering: S103a-fix (surface-patch suffix over S103a); no D-entry; S103b/S104 untouched
  corrects: (none — S103a's record was not wrong; both S102 and S103a deferred the browser eyeball to the operator, whose pass surfaced these surface gaps; this patches them, it does not correct a false claim)
  corrected_by:
```

---

## S103b — the element-evidence matcher: signal binds to authored elements, not goals (D202) (build mode)

roles: architect (the framing fork — Step 0 found direction blocked on a missing user-identity reference and no embedding tier in the live matcher, settled with the operator to defer both; the derive-at-`list_goal_edges` boundary so the readers stay untouched; the EVIDENCES edge over the authored-endpoint whitelist), engineer (the migration + graph layer + the matcher rewrite over the 905-line inference + the read-only display + the live re-match + the tests + the in-container guard), analyst (Step 0 against the live matcher/retrieval/ingestion; the live re-match read — 68.5% unbound and its token-overlap diagnosis), PM (the D202 + D200-addendum framing; the S103b/S103c/S104 split), technical writer (D202, the D200 addendum, schema 0006, the marker, the smoke doc, this entry).

- **Step 0 (the framing fork the brief reserved D203 for).** Two of the brief's spine assumptions did not hold against the live system, surfaced and settled with the operator before any code: **(1) direction is blocked on identity, not fields.** The direction fields (email `from`/`to`, calendar `organizer_email`/`attendees`) are ingested at source but not projected into `WorkFacet`, and — the real blocker — nothing stores *who the user is* (`Connection` carries no account email, `MeetingAttendee` no self-flag), so inbound-vs-user-initiated cannot be oriented. Direction's only consumer is the emergent loop and with one authored external it separates almost nothing, so **direction + the lever/external orientation + its configured-account-identity (D203) all moved to S104** (a signal lands where it informs a decision). **(2) No embedding tier exists in this matcher** — `infer_goal_edges` is lexical(lever-name)+alias only, and the email-embedding corpus is empty (S100) — so S103b extends lexical+alias to per-element targets honestly and names the embedding tier as later work; my spine's "lexical→alias→embedding" assumed a third tier that was not there (my error, corrected). **(3) The split:** S103b = rewrite + re-match + derive + read-only display; relink/unlink + correction capture → **S103c**; direction + emergent loop → **S104**. Live corpus confirmed: 1359 units, the matcher's readers (`group_units_by_goal`/`goal_status`) take `edges` as a parameter so deriving at `list_goal_edges` leaves them byte-unchanged.

- **Reflection 1 — the unbound rate, and what it means (the substantive paragraph).** After the live re-match the unbound rate is **68.5% (931 of 1359 units bound no element)**, and the headline question is whether that sends work back to authoring (elements under-described) or forward to a matcher fix (lexical recall too crude without embeddings). The spot-check splits it cleanly: of the 931 unbound, **44% share at least one token with some authored element label yet still did not bind** — these are the matcher/vocabulary candidates an embedding tier or a richer element label would catch (the pooled-token overlap is looser than the matcher's per-element `_keyword_match`, so a token shared with goal A's element doesn't bind work that belongs to goal B). The other **56% share no token with any element at all** — and the unbound sample is dominated by "3 mins Esperanto with Megan", a recurring language-practice series for which **no goal is authored** (the corpus has a German goal, not a Esperanto one). That is the decisive read: most of the unbound bucket is **genuinely uncovered work the model has no element for**, parked honestly (the coverage-honest law), not a broken matcher — so the rewrite worked, signal binds to elements where elements exist, and uncovered work parks rather than mis-binding. The tier mix corroborates it: **681 lexical_keyword, 166 lexical_exact, and zero alias** — the goal-name recall was *fully subsumed* by element-keyword matching (an element always matched before the alias fallback could fire), so the weak goal-whole signal has effectively disappeared, which is exactly the precision gain D202 wanted. The decision this drives: **do not pull the embedding tier forward yet** — the dominant cause is coverage/authoring (author a Esperanto goal; enrich thin element labels), and the 44% token-sharers are a real but not-yet-binding constraint; the embedding tier earns its place when authoring is saturated and the 44% becomes the ceiling. S103b's job — make the unbound rate *measurable and diagnosable* — is done.

- **Reflection 2 — direction cleanliness and the S104 queue size.** Direction was deferred to S104 this session, so there is no sender/recipient/organiser separation to grade yet; what S103b sizes is the queue direction will serve. **The S104 emergent queue is the 931 unbound units**, and the externals signal within it is reinforced by the element-kind distribution: **361 Lever, 297 Outcome, 189 Intermediary, and 0 External** evidence edges — only one external is authored and nothing matched it, so externals genuinely arrive from events, not from the draft (the D200 dual-nature addendum's instance altitude). The known cost of deferring direction, recorded honestly: without it an inbound email that lexically matches a lever binds to that lever rather than parking unbound, so the 931 figure slightly *under*-counts the true external queue; the S103c relink corrections on exactly those mis-binds are what will teach S104's direction. The loop closes the right way — bind, then correct, then learn to orient.

- **Reflection 3 — multi-attach legibility.** Multi-attach is the **common case, not the exception**: 276 of 428 bound units (64%) evidence two or more elements (deg2=161, deg3=91, deg4=22, deg6=2). It stayed legible because the goal-level derive **collapses a unit's many element bindings within a goal to one goal edge** (strongest status wins), so a single unit never double-counts into a goal's verdict — `goal_status`'s worst-status propagation runs over the deduped goal edges, unchanged. Within a goal the per-element display shows honest distinct-unit counts, so a unit serving three elements legibly appears on three element rows without muddying the goal's one verdict. The one watch-item for S103c: a unit binding 4–6 elements is often the loose-keyword case (a generically-titled unit brushing several element labels), which is precisely where the user's relink will sharpen the model — multi-attach is real signal where the work genuinely serves several elements and noise where it is keyword brush, and only the correction loop tells them apart.

- **methodology:** Step 0 did its highest-value work yet — it caught that the brief's load-bearing spine (tiered recall *including embeddings*, direction *computable*) did not match the live code, and surfacing that as a fork *before* writing turned a 7-commit overreach into a correctly-scoped 6-commit session with two clean deferrals. The operator's "a signal lands where it informs a decision" call (defer direction because nothing consumes it yet) is the observability rule applied to sequencing, and it is what kept the identity-scope creep out. The other lesson: the derive-at-the-boundary choice (reimplement `list_goal_edges` to roll up element evidence) let a fundamental matcher rewrite land with the entire coverage/grouping/status read layer byte-unchanged — the cheapest place to absorb a model change is the one seam every reader already goes through.

- **AC verdicts.** AC1 (0006 adds the EVIDENCES edge, idempotent live) ✓ — applied, re-run is a no-op, the relationship index lands. AC2 (binds nearest element(s), multi-attach) ✓ — live (multi-attach deg distribution) + unit tests. AC3 (direction computed; inbound-no-lever parks unbound) — **deferred to S104 per the Step-0 fork** (direction needs the unstored user identity); units matching no element do park unbound, but the lever/external orientation is S104. AC4 (goal-level derived on read; SERVES write removed; correct rollups) ✓ — live: 8 goals covered, 695 derived edges, SERVES_remaining 0; readers byte-unchanged. AC5 (re-match conserves every unit) ✓ **live** — 428 bound + 931 unbound = 1359, none lost. AC6 (lens shows element evidence + unbound bucket; relink/unlink behind CDD_WRITE; corrections captured) — **partially S103c**: the read-only element-evidence + unbound display ships and is served; relink/unlink + the append-only correction record are S103c per the split. AC7 (matcher behind interface, no vendor SDK, tenant-isolated) ✓ — import-linter 48/0 (neo4j-confined), live cross-tenant isolation test. AC8 (live in-container guard; suite/import-linter/AST green) ✓ — 4/4 contract guards live; unit + enforcement exit 0; import-linter 48/0. AC9 (charter-first ahead of code) ✓ — 32b2125 precedes all code commits.

- Close state: **seven commits — charter-first (`32b2125`: D202 + D200 addendum + schema 0006 + marker), element-evidence storage+derive (`7f909a7`), matcher rewrite (`79a0f12`), read-only display (`afacaf3`), tests (`46a0ea3`), pin+ops+smoke (`c521e0f`).** Migration 0006 applied; the live re-match wrote 847 EVIDENCES over 1359 units (428 bound, 931 unbound, SERVES retired); image re-pinned `d5f7197`; suite + enforcement + import-linter 48/0 green; 4/4 tenant-isolation contract guards pass live. D202 minted + the D200 dual-nature addendum landed; D203 reserved for S104. Deferred per the fork: direction + emergent loop (S104), relink/unlink + correction capture (S103c), the embedding tier (named).

```
metrics:
  classification: build session (S103b — the element-evidence matcher: signal binds to authored elements not goals; executes D202, lands the D200 dual-nature addendum, reserves D203 for S104)
  session_started: 2026-06-18
  session_closed: 2026-06-18
  step0: framing fork (the brief reserved D203) — (1) direction blocked on a missing user-identity reference (Connection has no account email, MeetingAttendee no self-flag; the from/to/organiser fields are ingested but unprojected), only consumer is the emergent loop, deferred to S104 with D203; (2) no embedding tier in the live matcher (infer_goal_edges is lexical+alias only; S100 email-embedding corpus empty) — lexical+alias per-element only, embedding named deferred; (3) split S103b/S103c/S104. Live: readers take edges as a param so derive-at-list_goal_edges leaves them byte-unchanged
  schema: migration 0006_element_evidence.cypher — (:Unit)-[:EVIDENCES]->(authored element) edge (tier/status/basis; NO direction) + EVIDENCES(tenant_id) relationship range index; goal-level SERVES write retired, derived on read; no constraint (MERGE-keyed idempotency, the SERVES precedent)
  matcher: infer_element_evidence — per-element lexical_exact (confirmed) / lexical_keyword (candidate) against element labels, outcome-on-expected_outcome, multi-attach, alias->outcome fallback only when no element matched (basis goal-name for the D185/D186 hooks); park-unbound; email-job-search->outcome analog; dedup per (unit, element) best tier; correlate_goal_facets rewritten to write element evidence (SERVES write removed), email/suppression(drops alias)/quality(measures derived rollup) hooks preserved
  derive: derive_goal_edges rolls element evidence to one GoalEdge per (unit, goal), strongest status wins; list_goal_edges reimplemented to derive; group_units_by_goal/goal_status/coverage byte-unchanged
  display: read-only element-evidence summary (GET /cdd/evidence, CDD_READ) — per-element unit counts + unbound bucket; CDD lens shows per-element "N units" badge + a "bound/unbound" line; relink/unlink + capture deferred to S103c
  live_rematch: make correlate-units — 1359 units, 847 EVIDENCES edges, SERVES_remaining 0; 428 bound / 931 unbound (68.5% unbound), CONSERVED (428+931=1359); derived: 8 goals covered, 695 goal edges, 166 confirmed
  reflection_1_unbound: 68.5% unbound; of the 931, 44% share a token with some element (matcher/vocabulary, embedding-tier candidates) but 56% share none — mostly genuinely uncovered work (e.g. "Esperanto with Megan", no goal authored), parked honestly; tiers 681 keyword / 166 exact / 0 alias (goal-name recall fully subsumed by element-keyword); decision: do NOT pull embedding tier forward yet (dominant cause is coverage/authoring), embedding earns its place when authoring saturates
  reflection_2_direction_queue: direction deferred to S104 (no cleanliness to grade); S104 emergent queue = the 931 unbound; element kinds 361 Lever / 297 Outcome / 189 Intermediary / 0 External (1 external authored, nothing matched — externals are emergent, the D200 addendum); known cost: without direction an inbound email lexically matching a lever binds to it, slightly under-counting the external queue; S103c relink corrects exactly those
  reflection_3_multi_attach: 64% of bound units multi-attach (deg2=161/deg3=91/deg4=22/deg6=2); legible because derive collapses a unit's per-goal bindings to one goal edge (no double-count in the verdict); per-element counts honest; deg4-6 often loose-keyword brush — the relink loop (S103c) sharpens it
  tests: tests/unit/contexts/daily_driver/test_element_evidence.py (binding tiers, multi-attach, alias-fallback, park-unbound, dedup, derive, summarise, conservation, email-analog, 0006 guard) + the two correlate tests moved to the element model; tests/contract/tenant_isolation/test_element_evidence_isolation.py (live binding + isolation on real Neo4j); unit + enforcement exit 0; import-linter 48/0
  live: image re-pinned d5f7197 (make build-api + force-recreate, healthy); 0006 applied in-container; re-match run; 4/4 tenant-isolation contract guards pass live; element-evidence display served; full browser eyeball operator-gated
  commits: 32b2125 (charter-first: D202 + D200 addendum + schema 0006 + marker), 7f909a7 (element-evidence storage + derive), 79a0f12 (matcher rewrite), afacaf3 (read-only display), 46a0ea3 (tests), c521e0f (pin + ops wording + smoke doc)
  charter_touchpoints: charter/decisions.md (D202 + D200 dual-nature addendum + index lines); charter/schema.md (EVIDENCES edge, migration 0006); charter/current-package.md (S103b marker); docs/smoke/p2b_s103b_element_evidence.md; log/sessions.md (this entry); compose.yaml (pin); ops/correlate_units.py (wording)
  numbering: S103b; D202 minted, D200 dual-nature addendum landed, D203 reserved for S104; schema in schema.md + migration 0006
  deferred: direction + lever/external orientation + config-identity (D203) + the emergent loop -> S104; relink/unlink + append-only correction capture -> S103c; the embedding recall tier (named, S100 empty corpus); draft-strengthening for external category slots (D200 addendum)
  corrects:
  corrected_by:
```

---

## S103c — editability and the learning signal: relink, unlink, capture, re-runnable re-match (D203) (build mode)

roles: architect (the Step-0 audit-versus-dedicated decision; correction precedence as a graph property the re-match respects by construction; the single-edge mutation + match-both-before-write relink), engineer (the relink/unlink Cypher + user-ownership; the audit-emit capture; the correction-respecting re-match; the interactive lens + bindings read; the tests + the live guard), PM (D203 + the unit-level grain call), analyst (Step 0 against the live matcher/audit; the live idempotence read), technical writer (D203, schema, the marker, the smoke doc, this entry).

- **Step 0 finding (the audit-versus-dedicated decision, settled for audit).** The load-bearing Step-0 call was where the correction record lives. Two candidates: the platform audit context (`AuditPort.emit`) or a dedicated `:Correction` graph node. Settled for **audit**: a relink/unlink is a state change on tenant-authored canonical data, which the platform's "audit logging captures every state change on tenant-scoped data, hash-chained" commitment already mandates; optimization/retrieval-evaluation set the exact application-layer emit precedent; the API already constructs the `audit_adapter`; import-linter's cross-context independence is **domain-only**, so the `daily_driver.application → contexts.audit.domain` emit is clean (48/0); and the faceted audit reader serves the consume session's read (filter on `cdd.relink`/`cdd.unlink`). So the compliance record and the learning signal are the **same artefact** — a parallel un-chained store would have split them and violated audit-as-source-of-truth. Other Step-0 reconciliations held: user-ownership is a schemaless `:Unit.user_owned` property (survives `correlate_units`' deterministic MERGE), the re-match endpoint runs `correlate_goal_facets` from app.state seams, **no migration** (0007 not needed). Numbering: D202 had tentatively reserved "D203" for S104's direction; correction precedence takes D203, direction takes the next free number at S104.

- **Reflection 1 — the correction rate and the corrected-versus-confirmed split (the substantive paragraph).** The empirical count is the operator's gated browser pass (making real corrections mutates the authored model, which is the operator's to own), so this is the reasoned read off S103b's live data plus the instrumentation S103c ships to *measure* the split. The decisive distinction the prompt poses — correcting the matcher's cautious near-misses (the learning signal's first real data) versus correcting bindings it got right (the matcher is wrong, not cautious) — is now **directly readable on the surface**: every bound unit in the interactive expand shows its **tier badge** (`lexical exact` / `lexical keyword`), so a relink off a `lexical_keyword` binding reads as "the matcher was loosely cautious here" while a relink off a `lexical_exact` binding reads as "the matcher was confidently wrong." S103b's distribution predicts the former will dominate: **681 keyword bindings to 166 exact** (4:1), and 44% of the *unbound* share a token with some element — a matcher that binds loosely and parks the genuinely-disconnected is cautious by construction, so the operator's corrections should cluster on the keyword tier and on *authoring missing goals* (the 56% of unbound that share no token, e.g. the Esperanto recurring series with no goal) rather than on overturning exact bindings. If the live pass instead finds exact-tier relinks common, that is the signal the prompt names — the matcher is wrong and the embedding tier is not the fix — and the tier badge is exactly the instrument to tell the two apart without guessing. The corrected-versus-confirmed split is therefore *measurable now*; the number is the operator's to record, and it is the verdict on whether S103b's rewrite needs the embedding tier sooner.

- **Reflection 2 — coverage recovery on re-match.** The empirical "how many previously-unbound units bound to a newly-authored goal" is gated on the operator authoring the goal, but the **mechanism is live-verified**: the re-match is idempotent on the real corpus (re-running produced **847 EVIDENCES again, stable, SERVES 0, 0 owned**) and the contract guard proves a newly-authored element binds previously-unbound work while user-owned units are skipped. So author-then-re-match is confirmed as the coverage workflow, and the obvious first target is the **Esperanto recurring series** S103b surfaced as the dominant unbound work (no goal authored) — authoring a Esperanto goal and clicking Re-match should bind its ~120 recurring instances in one pass, which is also where the recurring-versus-one-off measure the next move needs begins. The number is the operator's to read off the unbound count's drop.

- **Reflection 3 — did unit-level ownership bite?** Gated on real multi-attach corrections, but S103b's data flags it as the **most likely refinement to be needed**: 64% of bound units are multi-attach (deg≥2), so the coarse unit-level grain is exercised heavily, and the named cost — a corrected multi-attach unit will not gain a *new* matcher binding on a later re-match — will land on exactly those units. Whether it bites depends on whether the operator, having corrected one of a multi-attach unit's bindings, then authors a new element they *want* the re-match to also attach to that same unit; unit-level ownership blocks that, forcing a manual relink. If the live pass hits that wish even once, **per-element ownership is the refinement to schedule** (D203 named it as the deferred grain). The high multi-attach rate makes this a live possibility, not a theoretical one — it is the watch-item the next session inherits.

- **methodology:** the Step-0 audit-versus-dedicated decision is the session's clearest instance of a charter principle resolving an architecture choice that looked like a toss-up: a dedicated graph node was *lighter* and co-located with the learning target, but "the audit trail is the canonical record of every state change" is binding, and following it gave the compliance record and the learning signal for free as one artefact. The general lesson: when a new store is tempting, check whether an existing canonical store's principle already claims the data — here it did, and the "lighter" option would have been a parallel-store drift the platform explicitly forbids. The second note: building the re-match's correction-respect as a *graph property* (`user_owned` gating the delete) rather than as application-timing logic means the guarantee holds for any caller of the re-match, not just the one endpoint — precedence by construction, the D203 reasoning made mechanical.

- **AC verdicts.** AC1 (relink moves the binding, unlink removes it, both mark user-owned + user-corrected, behind CDD_WRITE) ✓ **live** (the contract guard) + unit. AC2 (each emits an append-only correction record with prior→new provenance) ✓ unit (the emit + before/after states); the live audit-Postgres write is the audit adapter's own tested path. AC3 (re-match idempotent — no dup, no change) ✓ **live** (847 stable on re-run) + unit. AC4 (re-match skips user-owned — a correction survives) ✓ **live** (the guard: a re-match-shaped delete keeps the owned edge, drops the non-owned) + unit. AC5 (coverage-recovery: a newly-authored element binds previously-unbound work, owned units untouched) ✓ unit (model graph); the live mechanism proven (the guard binds a new element, idempotence stable); the empirical per-goal count operator-gated. AC6 (interactive display: relink, unlink, Re-match) ✓ served (pin `bc1de6b`); the browser eyeball operator-gated. AC7 (tenant-scoped + isolation tested; matcher behind the interface, no vendor SDK) ✓ — 5/5 live isolation guards, import-linter 48/0 (neo4j-confined + the audit emit clean). AC8 (charter-first ahead of code; consume + S104 untouched) ✓ — `1150c6b` precedes all code; nothing consumes the captured corrections, no direction/emergent-loop work.

- Close state: **eight commits — charter-first (`1150c6b`: D203 + marker + schema, no migration), relink/unlink+ownership (`0654986`), capture (`7428882`), correction-respecting re-match (`e22ca96`), interactive display (`2fbf13e`), tests (`a039110`), pin+smoke (`d679bac`).** No migration. D203 minted (the audit-store decision recorded). Unit + enforcement exit 0; import-linter 48/0; 5/5 tenant-isolation contract guards pass live; the live re-match is idempotent (847 stable). Image re-pinned `bc1de6b`. Deferred: consuming the captured corrections (the learning half — next session or folded into S104), direction + config-identity + the emergent loop (S104), the embedding tier, per-element ownership (the reflection-3 refinement). The three reflections' empirical numbers are the operator's gated browser pass; the tools to measure them are shipped.

```
metrics:
  classification: build session (S103c — editability and the learning signal: relink/unlink/capture + re-runnable correction-respecting re-match; executes D203, completes increment 2's editability half)
  session_started: 2026-06-18
  session_closed: 2026-06-19
  step0: the audit-versus-dedicated decision SETTLED FOR AUDIT — a correction is a state change on tenant-authored canonical data (the audit-trail-as-source-of-truth commitment mandates it), optimization sets the application-layer AuditPort.emit precedent, the API already builds audit_adapter, import-linter cross-context independence is domain-only so daily_driver.application -> contexts.audit.domain is clean, and the faceted reader serves the consume read; user-ownership = schemaless :Unit.user_owned (survives correlate_units' MERGE); re-match runs correlate_goal_facets from app.state; NO migration (0007 not needed); D203 takes the number D202 had tentatively reserved for direction (direction -> next free number at S104)
  schema: no migration — :Unit.user_owned marker (unit-level grain), the user-corrected EVIDENCES edge (tier=user/basis=user-corrected), the correction audit-event shape (cdd.relink/cdd.unlink, before/after binding) documented in schema.md
  relink_unlink: single-edge mutation — unlink deletes one EVIDENCES edge; relink matches both endpoints before any write then retargets to the new element; both SET :Unit.user_owned=true; behind CDD_WRITE; touches the one named binding (multi-attach safe)
  capture: each relink/unlink emits an append-only correction AuditEvent via AuditPort (cdd.relink/cdd.unlink; before_state prior binding, after_state new binding/empty); the same hash-chained record is the learning signal; capture only, no consume; audit_adapter wired onto app.state.daily_driver_audit_port
  rematch: correlate_goal_facets reads user_owned ids + excludes those units from the inference; replace_element_evidence delete skips user_owned EVIDENCES -> idempotent + correction-respecting; POST /cdd/rematch from app.state seams
  display: interactive over the S103b read-only display — GET /cdd/bindings (per-unit binding + title + user_owned); per-element "N units" expands to bound units with Relink-to (goal's other elements + outcome) + Unlink + a "yours" badge; a Re-match trigger on the CDD bar
  tests: tests/unit/contexts/daily_driver/test_correction_loop.py (relink/unlink mutate+capture with prior->new provenance, no-op no capture; re-match idempotent / skips owned / recovers coverage via a model graph); tests/contract/tenant_isolation/test_correction_isolation.py (live: relink marks owned, a re-match-shaped delete keeps owned + drops non-owned, isolation); the two correlate fakes gain list_user_owned_unit_ids; unit + enforcement exit 0; import-linter 48/0
  live: image re-pinned bc1de6b (make build-api + force-recreate, healthy); 5/5 tenant-isolation contract guards pass live; the live re-match is idempotent (847 EVIDENCES stable on re-run, SERVES 0, user_owned 0); the interactive lens served (grep)
  reflection_1_correction_rate: empirical count operator-gated (real corrections mutate the operator's model); the corrected-vs-confirmed split is now MEASURABLE on the surface (each binding shows its tier badge — a relink off lexical_keyword = cautious near-miss, off lexical_exact = matcher wrong); S103b's 4:1 keyword:exact + 44% token-sharing unbound predicts keyword-tier + author-missing-goals corrections dominate, not exact overturns
  reflection_2_coverage_recovery: empirical count gated on authoring a goal; mechanism live-verified (re-match idempotent 847 stable; the guard binds a new element while skipping owned); author-then-re-match confirmed as the workflow; first target = the Esperanto recurring series (no goal, S103b) — authoring it + Re-match binds ~120 instances in one pass; begins the recurring-vs-one-off measure
  reflection_3_ownership_grain: gated on real multi-attach corrections; S103b's 64% multi-attach flags unit-level as the most likely refinement to bite (a corrected multi-attach unit gains no new matcher binding on re-match); per-element ownership is the named D203 refinement to schedule if the live pass hits the wish once
  commits: 1150c6b (charter-first: D203 + marker + schema, no migration), 0654986 (relink/unlink + ownership), 7428882 (capture), e22ca96 (correction-respecting re-match), 2fbf13e (interactive display), a039110 (tests), d679bac (pin + smoke doc)
  charter_touchpoints: charter/decisions.md (D203 + index line); charter/schema.md (user_owned marker, user-corrected edge, correction audit-event shape); charter/current-package.md (S103c marker); docs/smoke/p2b_s103c_correction_loop.md; log/sessions.md (this entry); compose.yaml (pin). No migration.
  numbering: S103c; D203 minted (correction precedence; the audit-store decision recorded); S104's direction-and-configured-account-identity decision takes the next free number when it runs
  deferred: consuming the captured corrections to adjust matching (the learning half — next session or folded into S104); direction + config-identity + the emergent loop (S104); the embedding recall tier; per-element user-ownership (the reflection-3 refinement); authoring the missing recurring goals (operator work on the shipped surfaces)
  corrects:
  corrected_by:
```

---

## S103c-fix — legible bindings: the why, match strength, and a workable unlink (D203) (build mode, surface-mostly)

roles: engineer (the recompute-on-read rationale + strength; the triage + in-place unlink + bulk; the tests), analyst (Step 0 — the edge records tier+basis not the matched term; the live strength distribution + the classifier-why find), technical writer (the marker, the smoke doc, this entry).

- **Step 0 finding.** The matcher records `tier` + `basis` on the EVIDENCES edge but **not the matched term**, so the why and the strength are **recomputed on read** from the deterministic lexical/alias match against the current element vocabulary — no storage change, no migration (confirming the brief's premise). Honest framing held throughout: what is shown is **lexical match strength, not correctness** (the matcher is lexical+alias only; semantic confidence is the deferred embedding tier).

- **The three gaps closed.** (1) **The why** — each binding shows the matched term (`matched on "practice"`), recomputed on read. (2) **Match strength** — a strong/medium/weak band from tier + discriminativeness (a distinctive matched token outranks an incidental shared one), labelled strength-not-correctness. (3) **Workable unlink** — an unlink/relink re-renders the bound-units list **in place** (a sub-local refresh) instead of collapsing it, with a weak-only **triage** filter (weakest-first) and a **bulk** "Unlink selected" (each its own correction record).

- **Reflection — did the why + strength let me triage, and did strength mislead?** The empirical triage *experience* is the operator's browser pass, but the live recompute on the real corpus gives the substantive read: **421 weak / 275 medium / 166 strong** — 49% weak, so the triage target is real and large, and the weak whys are legibly the trap ("Music practice Megan" → matched **"practice"**, an incidental token shared across elements; multi-attaching on it). The decisive find: **all 166 "strong" bindings are the D183 job-search classifier, not lexical exact — the lexical matcher produced zero exact title==label matches on this corpus.** So lexical strength here tops out at *medium* (a distinctive keyword), and the only *strong* signal is the rule. That sharpens the "did strength mislead" question structurally: a high-strength-relink (where lexical strength and semantic correctness part) can only come from the medium tier on this corpus, and it is the embedding-tier signal — the empirical count of such relinks is the operator's to record. The live pass also caught a self-inflicted honesty bug: the 166 classifier bindings first showed `matched "Receive a job offer"` (a string match that never happened); fixed in-session to render **"job-search classifier"** so the why never misleads.

- **methodology:** the live read earned its keep twice — it produced the reflection's headline (strong == classifier-only, lexical-exact empty) *and* caught that S103c-fix's own first cut mislabelled the classifier binding's why; running the recompute against the real corpus, not a fixture, is what surfaced both. The honest-framing discipline (strength is string-match, not correctness) is what made the mislabel visible as a bug rather than passing as a plausible badge.

- **AC verdicts.** AC1 (tier + matched term + a strength band labelled match-strength, recomputed on read, no storage change) ✓ — live recompute + the honest-why fix. AC2 (triage weakest-first / weak-only filter) ✓ served. AC3 (unlink keeps the list expanded; bulk works; each unlink one record) ✓ — `subRefresh` in place + the bulk loop; unit-tested (N calls → N records). AC4 (surface + read-side only: no migration, no new write path beyond batching, matcher + consume untouched) ✓ — grep-confirmed (goal_assessment additions only, `correlate_goal_facets` not in the diff). AC5 (suite/import-linter/AST green; served-HTML guard; operator-gated live check via `make build-api`) ✓ — unit + enforcement green, import-linter 48/0, served on pin `8a4078c`, 5/5 contract guards pass live.

- Close state: **six commits — marker (`eaf5a84`), rationale+strength (`a23a919`), triage+workable-unlink (`1dc7cb3`), tests (`89be65e`), the honest-why fix (`b77bfb2`), pin+smoke (`e03c7db`).** Surface-mostly (recompute-on-read + front-end); no migration, no D-entry. Commits 2+3 (triage, workable unlink) landed together — interleaved in one render function. The triage *experience* and the high-strength-relink count are the operator's gated browser pass.

```
metrics:
  classification: build session (S103c-fix — legible bindings: the why + match strength + a workable unlink; surface-mostly over S103c, no decision)
  session_started: 2026-06-19
  session_closed: 2026-06-19
  step0: the EVIDENCES edge records tier+basis, NOT the matched term -> the why + strength are recomputed on read from the deterministic lexical/alias match against the current element vocabulary; no storage change, no migration; honest framing = lexical match strength not correctness (matcher is lexical+alias only)
  gap_1_why: each binding shows the matched term (recompute on read via binding_rationale)
  gap_2_strength: strong/medium/weak band from tier + discriminativeness (element_token_counts); labelled match-strength not a trust score; classifier-fed (D183) binding shows "job-search classifier" (live-found honesty fix)
  gap_3_unlink: in-place subRefresh keeps the list expanded through unlink/relink; weak-only triage filter (weakest-first); bulk "Unlink selected" (each its own correction record)
  surface_only: confirmed — goal_assessment.py additions only (matcher inference unchanged), read_element_evidence.py, the DTO, /cdd/bindings, daily_driver.html; no migration, correlate_goal_facets not in the diff, consume side untouched
  live: read_element_bindings recompute on the personal tenant — 421 weak / 275 medium / 166 strong; ALL 166 strong are the D183 classifier (lexical exact == 0 on this corpus); the why honest after the fix; image re-pinned 8a4078c; 5/5 tenant-isolation contract guards pass live; served HTML carries the affordances
  tests: tests/unit/contexts/daily_driver/test_correction_loop.py (binding_rationale bands + bulk one-record-each) + tests/unit/apps/api/test_daily_driver_surface.py (why/strength/triage/subRefresh/bulk guards); unit + enforcement exit 0; import-linter 48/0
  reflection: triage target is real (49% weak); weak whys legibly expose the incidental-token trap; strong == classifier-only (lexical-exact empty) so a high-strength relink can only come from the medium tier and is the embedding-tier signal; the empirical triage experience + high-strength-relink count are operator-gated
  commits: eaf5a84 (marker), a23a919 (rationale+strength), 1dc7cb3 (triage + workable unlink), 89be65e (tests), b77bfb2 (honest-why fix), e03c7db (pin + smoke)
  charter_touchpoints: charter/current-package.md (S103c-fix note). No schema.md, no decisions.md (no D-entry — exposing match properties + an honest read-side proxy is not a decision). log/sessions.md (this entry); compose.yaml (pin); docs/smoke/p2b_s103c_fix_legible_bindings.md
  numbering: S103c-fix (legibility-patch suffix over S103c); no D-entry; D203 stands as the live decision max; S104's direction decision takes the next free number when it runs
  corrects:
  corrected_by:
```

---

## S103c-fix-2 — view coherence: one corrected truth across List, Map, CDD (D202/D203) (build mode, surface + read-side)

roles: analyst (Step 0 — the load-bearing read-path reconciliation that corrected the brief's premise), engineer (the per-goal corrections section + cross-goal relink picker in List/Map; the binding `outcome_id`; the consistency guard + tests), technical writer (the marker, the smoke doc, this entry).

- **Step 0 finding (the brief's premise corrected).** The brief's load-bearing question was whether List and Map read a separate model (stop-and-flag) or a bypassable seam (small re-point). Neither — **they already derive from element evidence.** Since S103b, `/units-by-goal` → `list_units_by_goal` → `unit_graph.list_goal_edges()`, and the bridge's `list_goal_edges` derives from `list_element_evidence` + `derive_goal_edges`; the remaining `SERVES` references were stale docstrings, not a live read. So the read-coherence gap was already closed at S103b — the re-point is a **no-op**, no separate-model reconciliation, no scoping fork, no D-entry. The genuine gap was **write-reach**: List and Map were read-only, lacking the unlink/relink the CDD view got at S103c. The session is therefore smaller and cleaner than framed: expose the corrections, don't re-plumb the reads.

- **Reflection 1 — did corrections propagate cleanly across all three views?** The empirical "without a manual refresh" is the operator's browser pass, but the architecture makes propagation **structural, not best-effort**: all three views read one shared cache (`assessData` + `cddBindings`, loaded once in `loadAssess`), and every correction — from any view — calls `loadAssess()`, which re-reads that one cache and re-renders. There is no second data path to go stale, and the live coherence is proven: on the real corpus the List/Map goal edges (688) equal `derive_goal_edges` of the CDD element evidence (840). A stale binding in one view would require the views to read diverging sources, which the consistency guard now fails on. So a stale view is structurally precluded; the operator's eyeball confirms the feel.

- **Reflection 2 — was element-level relink the right grain from the goal-level views?** Operator-gated, reasoned. The picker is goal-then-element (cross-goal), which keeps D202's element-level model intact — relinking from a goal-level view still lands on a specific element, so the matcher's learning signal stays element-grained rather than collapsing to a goal-whole link. The open question the prompt names — did you want to move an email to a goal *without* naming an element (a goal-level default) — is the deferred refinement; if it recurs, the natural default is the goal's outcome element (so a goal-default is sugar over the element model, not a model change). Named, not built, pending the live pass.

- **methodology:** Step 0 paid for itself by *subtraction* — it caught that the brief's central premise ("List and Map read a stale model") was already false, turning a feared re-plumb (possibly a read-architecture decision) into a no-op plus a small write-reach exposure. The discipline that surfaced it: trace the actual read path to its source (`list_goal_edges` → `derive_goal_edges`) rather than trusting the docstrings, which still said `SERVES`. The stale-docstring lag is itself the lesson — a prior session re-pointed the read but left the prose, so the next brief inherited a wrong mental model from the comments; refreshing the docstring this session closes that lag.

- **AC verdicts.** AC1 (List/Map derive from the same element-evidence truth as CDD; a correction reflects in all three; proven by the consistency guard) ✓ — the unit guard (`list_goal_edges == derive_goal_edges(list_element_evidence)`) + **live** (688 == derive(840)). AC2 (unlink in List/Map — orphans to unbound, marks user-owned, emits a record via the S103c path) ✓ served + reuses the path (unit-tested). AC3 (element-level relink via a goal-then-element picker, cross-goal, edge moves element-level per D202) ✓ served + reuses the path. AC4 (the consistency guard fails if the views diverge) ✓ — `test_view_coherence`. AC5 (surface + read-side: no schema, no new write path beyond more entry points, matcher + consume untouched) ✓ — grep-confirmed (no migration; `infer_element_evidence`/`correlate_goal_facets` not in the diff). AC6 (suite/import-linter/AST green; served-HTML guard; operator-gated live check via `make build-api`) ✓ — unit + enforcement green, import-linter 48/0, served on pin `79276dd`, 5/5 contract guards live, cross-view coherence live-verified.

- Close state: **six commits — marker (`f3aba55`), binding-carries-its-goal + docstring refresh (`cfd5467`), unlink + element-level relink from List/Map (`25dcede`), tests (`269daaf`), pin+smoke (`f2cad36`)** (and the marker). Surface + read-side; no migration, no D-entry. Commits 2+3 (unlink, relink) landed together — interleaved in the one shared `renderGoalCorrections` / `relinkPicker`. The cross-view propagation feel and the relink-grain question are the operator's gated browser pass.

```
metrics:
  classification: build session (S103c-fix-2 — view coherence: one corrected element-evidence truth across List/Map/CDD + write-reach to List/Map; surface + read-side, no decision)
  session_started: 2026-06-19
  session_closed: 2026-06-19
  step0: brief premise CORRECTED — List/Map are NOT reading a stale goal-level model; since S103b they derive from element evidence (units-by-goal -> list_units_by_goal -> list_goal_edges -> derive_goal_edges over list_element_evidence); remaining SERVES refs are stale docstrings; read-coherence gap already closed at S103b (re-point is a no-op, no separate-model fork, no D-entry); genuine gap = write-reach (List/Map read-only)
  gap_closed: List/Map gain the S103c unlink + element-level cross-goal relink, over the same element-evidence bindings the CDD view uses (one shared cache: assessData + cddBindings loaded once in loadAssess); a consistency guard so the three views cannot silently diverge
  read_side: ElementBinding + DTO gain outcome_id (lets the goal-level views resolve a unit's bindings per goal); list_units_by_goal docstring refreshed from SERVES to element-evidence-derived
  write_reuse: List/Map unlink/relink POST the S103c /cdd/evidence/unlink and /relink — same use cases, mark user-owned + emit a correction record (D203); relink is element-level via a goal-then-element picker, cross-goal allowed (D202); no new write path
  live: image re-pinned 79276dd; cross-view coherence verified on the real corpus (element evidence 840, goal edges 688, List/Map == derive_goal_edges(CDD evidence) True); served HTML carries renderGoalCorrections + relinkPicker; 5/5 tenant-isolation contract guards pass live
  tests: tests/unit/apps/api/test_view_coherence.py (list_goal_edges == derive_goal_edges(list_element_evidence) — the consistency guard; multi-attach rollup coherent) + served-HTML guards (List/Map corrections, cross-goal picker, S103c-path reuse, one shared source); unit + enforcement exit 0; import-linter 48/0
  surface_only: confirmed — daily_driver.html, the binding DTO+read (outcome_id), list_units_by_goal docstring, ElementBinding; no migration, no matcher logic change (infer_element_evidence/correlate_goal_facets untouched), no schema, consume side untouched
  reflection_1_propagation: structural — one shared cache, every correction calls loadAssess() to re-read; no second path to go stale; live-proven coherent (688 == derive(840)); empirical "no manual refresh" feel is operator-gated
  reflection_2_relink_grain: element-level picker (goal-then-element, cross-goal) keeps D202's element grain; a goal-level default (relink to a goal without naming an element) is the deferred refinement, natural default = the outcome element; built only if reflection-2 shows it bites
  commits: f3aba55 (marker), cfd5467 (binding carries its goal + docstring refresh), 25dcede (unlink + element-level relink from List/Map), 269daaf (tests), f2cad36 (pin + smoke)
  charter_touchpoints: charter/current-package.md (S103c-fix-2 marker). No schema.md, no decisions.md (no D-entry — reuses D202's element model + D203's correction semantics; the re-point was a no-op). log/sessions.md (this entry); compose.yaml (pin); docs/smoke/p2b_s103c_fix_2_view_coherence.md
  numbering: S103c-fix-2 (view-coherence patch over S103c); no D-entry; D203 stands as the live decision max; S104's direction decision takes the next free number when it runs
  note: charter/current-package.md is at ~18.3k/20k tokens (many open-package markers) — approaching the size bound; flagged as a capture for windowing at package close [CORRECTED by S103c-fix-3: this conflated bytes with tokens — the file is ~5.2k/20k tokens, not near the bound]
  corrects:
  corrected_by: S103c-fix-3 (the Map unlink shipped here was broken — the outcome-kind 422 — and the served-HTML guard checked render not function; and this size note mistook bytes for tokens); S103c-fix-4 (the List/Map corrections it added copied the CDD display not the interaction — collapse-on-unlink, no bulk — single-sourced there)
```

---

## S103c-fix-3 — make the bug and the why honest: working unlink everywhere, a truthful match basis (D202/D203) (build mode, surface + read-side)

roles: analyst (Step 0 — root-causing the Map 422, classifying the why and the activity mislabel; the live no-basis count), engineer (the kind relaxation + the honest-why recompute + the functional/live guards), technical writer (the marker, the smoke doc, this entry).

- **Step 0 (reproduce, root-cause, classify).** (1) **The Map unlink fault is the outcome-kind 422.** `ElementKind` is `{lever, intermediary, external}` with no `outcome`; the weak alias bindings bind to the **outcome** element, and List/Map's `renderGoalCorrections` sends every binding's kind to `/cdd/evidence/unlink|relink`, so `ElementKind("outcome")` raised → 422. The graph endpoint already supported outcome (`_AUTHORED_ENDPOINT`); only the router's `ElementKind` validation rejected it. The S103c-fix-2 served-HTML guard passed because it checked the affordance *renders*, not that it *works* — the lesson the functional guard now closes. It is not Map-specific in the code (List shares `renderGoalCorrections`); it bit on Map because the spurious Brighton-Marathon bindings the operator was working are alias→outcome. (2) **The "goal name" why is a placeholder** — classified by whether the recompute reproduces a real unit∩goal-name token. (3) **"2 applications under Wide World Marathon" is a symptom of spurious binding, not a label change** (the kind label is correct; job emails are spuriously alias-bound to the marathon goal) → deferred and named, Commit 4 omitted.

- **Reflection 1 — what was the Map unlink fault?** The root cause is a **type-narrowing mismatch across a layer boundary**: the EVIDENCES edge can target four endpoint kinds (lever/intermediary/external/**outcome**, the graph's `_AUTHORED_ENDPOINT` whitelist), but the router validated the unlink/relink kind against `ElementKind`, the *three* authored-element kinds — so the one endpoint kind that is not an authored element, the outcome, 422'd. The class is "a narrower domain type guarding a wider capability"; the fix names the wider set explicitly (`EVIDENCE_KINDS = {lever, intermediary, external, outcome}`) and relaxes the unlink/relink path to it (`str`), while `reclassify`/`add` keep `ElementKind` (outcome genuinely excluded there, D201). The functional guard now covers it: unlink must *complete* for every kind a view can send, incl. outcome, with the edge actually removed — not merely render.

- **Reflection 2 — why bug or matcher bug? The no-basis count.** Both, and the split is now measured. Most weak "goal name" bindings had a **real token** the recompute surfaces (a why bug — the placeholder hid it), so the honest why now shows it. But **12 of 839 bindings (1.4%), all alias-tier, recompute to "no clear basis"** — job emails alias-bound where no unit∩goal-name token reproduces. The reason is precise: the matcher's `_keyword_match` accepts a **substring** match *or* a shared token, while the recompute only dignifies a shared significant token — so these 12 rest on substring coincidences, not real overlaps. That 1.4% is the **spurious-binding rate**, and it is the matcher-quality input to the strategic call on the weak/alias tier and the embedding tier — held outside this session, the matcher binding logic unchanged (grep-confirmed). The honest why does double duty: it shows the real token where there is one, and flags the 12 as "no clear basis" so they read as exactly the unlink candidates they are.

- **methodology:** the prior guard's gap is the durable lesson — "the affordance renders" is not "the affordance works," and a served-HTML presence check passed over a control that 422'd on its main case. The live-surface law (a real-surface pass, not a fixture) is now extended from *presence to function*: the functional guard exercises the use case for every kind and the contract guard unlinks an outcome-bound unit on real Neo4j. Also worth recording: the honest why turned a *display* fix into a *diagnostic* — making the recompute tell the truth surfaced the 1.4% spurious rate as a number, which is exactly the matcher-quality signal the strategic call needs, at no extra instrumentation cost. (And a self-correction: the S103c-fix-2 "current-package approaching its bound" capture conflated the size tool's bytes column with tokens; the file is ~5.2k/20k tokens — corrected in captures this session.)

- **AC verdicts.** AC1 (unlink completes from Map; unlink+relink from List/CDD; Map fault root-caused + recorded) ✓ — the kind relaxation + the recorded outcome-422 root cause; live outcome-unlink. AC2 (a functional guard that unlink+relink succeed end-to-end, edge removed/moved, not only rendered) ✓ — the unit functional guard (every kind incl. outcome) + the live outcome-unlink contract. AC3 (the why shows the real token where found and "no clear basis" where none, no "goal name" placeholder) ✓ — `binding_rationale` + the no-placeholder source guard. AC4 (the no-basis count reported as the spurious-binding signal; matcher logic unchanged, grep-confirmed) ✓ — **12/839 (1.4%), all alias**; `infer_element_evidence`/`correlate_goal_facets` not in the diff. AC5 (surface + read-side + the Map wiring; no schema, no migration, no matcher-logic change, consume untouched) ✓ — grep-confirmed. AC6 (suite/import-linter/AST green; functional + served-HTML guards; operator-gated live check via `make build-api`) ✓ — unit + enforcement green, import-linter 48/0, served on pin `2171607`, 3/3 correction guards live. AC7 (activity-label fix present only if one-line, else named) ✓ — named in out-of-scope (it is a binding symptom, not a label change).

- Close state: **six commits — marker (`d5bff08`), Map unlink/relink accept outcome (`3588b2f`), honest match basis (`e79c9b0`), functional + why guards (`e3de7f6`), pin+smoke (`9296fb1`)** (and the marker). Surface + read-side; no migration, no D-entry, no matcher-logic change. The spurious-binding rate (1.4%, all alias) is the strategic input recorded for the held matcher-looseness call.

```
metrics:
  classification: build session (S103c-fix-3 — working unlink everywhere + a truthful match basis; bug fix + read-side honesty, no matcher-logic change, no decision)
  session_started: 2026-06-19
  session_closed: 2026-06-19
  step0: (1) Map unlink fault = the outcome-kind 422 — ElementKind {lever,intermediary,external} has no outcome, but the weak alias bindings bind to the outcome element and List/Map send every kind to /cdd/evidence/unlink|relink; the graph endpoint supported outcome (_AUTHORED_ENDPOINT), only the router's ElementKind validation rejected it; the prior guard checked render not function. (2) "goal name" why = placeholder, classify by recompute. (3) "2 applications under Wide World Marathon" = symptom of spurious binding, not a label change -> deferred, Commit 4 omitted
  bug_fix: EVIDENCE_KINDS = {lever,intermediary,external,outcome} (the EVIDENCES endpoint kinds); unlink/relink endpoints validate against it, use case + port + bridge relax ElementKind -> str; graph unchanged (already _authored_endpoint); reclassify/add keep ElementKind (outcome excluded, D201)
  honest_why: binding_rationale shows the real overlapping token where the recompute finds one and NO_CLEAR_BASIS ("no clear basis") where it cannot; alias uses the unit∩goal-name token (read_element_bindings builds goal_tokens per goal); replaces the "goal name" and "(substring)" placeholders; read-side only
  no_basis_count: 12 of 839 bindings (1.4%) recompute to no-clear-basis, ALL alias-tier — job emails alias-bound where no unit∩goal-name token reproduces (the matcher's _keyword_match accepts substring, looser than the recompute's shared-token); this is the spurious-binding rate, the matcher-quality input; matcher binding logic UNCHANGED (grep)
  tests: tests/unit/contexts/daily_driver/test_correction_loop.py (functional: unlink completes for every kind incl outcome with the edge removed; relink to/from outcome; EVIDENCE_KINDS-includes-outcome regression; alias real-token + no-clear-basis + no-placeholder source guard) + tests/contract/tenant_isolation/test_correction_isolation.py (live outcome-bound unlink on real Neo4j); unit + enforcement exit 0; import-linter 48/0
  live: image re-pinned 2171607; 3/3 correction contract guards pass live (incl. outcome unlink); no-basis count read on the real corpus = 12/839 (1.4%) all alias; served HTML carries the why + corrections
  surface_only: confirmed — no migration, no matcher-logic change (infer_element_evidence/correlate_goal_facets untouched), no schema, consume side untouched; diff = cdd.py (EVIDENCE_KINDS), correct_cdd_evidence, unit_graph port, bridge, router, goal_assessment (binding_rationale), read_element_evidence, daily_driver.html(none this session beyond prior), tests
  reflection_1_map_fault: a narrower domain type (ElementKind, 3 kinds) guarding a wider capability (EVIDENCES endpoints, 4 kinds incl outcome); fix names the wider set + relaxes to str; functional guard covers it (completion per kind, not render)
  reflection_2_why_vs_matcher: both — most weak bindings had a real token (why bug, placeholder hid it, now shown); 12 (1.4%, all alias) have no reproducible token (matcher-quality, rest on substring matches) — the spurious-binding rate for the strategic call
  out_of_scope: matcher looseness / the 12 spurious alias bindings (strategic, informed by 1.4%); the activity mislabel (binding symptom); embedding tier; consume side; S104; current-package windowing (NOT near bound — the S103c-fix-2 alarm conflated bytes with tokens; ~5.2k/20k tokens, corrected in captures)
  commits: d5bff08 (marker), 3588b2f (Map unlink/relink accept outcome), e79c9b0 (honest match basis), e3de7f6 (functional + why guards), 9296fb1 (pin + smoke)
  charter_touchpoints: charter/current-package.md (S103c-fix-3 marker); log/captures.md (corrected the current-package size alarm). No schema.md, no decisions.md (no D-entry). log/sessions.md (this entry); compose.yaml (pin); docs/smoke/p2b_s103c_fix_3_bug_and_why_honesty.md
  numbering: S103c-fix-3 (bug+honesty patch over S103c); no D-entry; D203 stands as the live decision max; S104's direction decision takes the next free number when it runs
  corrects: S103c-fix-2 (the Map unlink shipped broken — outcome 422 — and its served-HTML guard checked render not function; this fixes the bug and extends the guard to function. Also corrects the S103c-fix-2 current-package size capture: bytes mistaken for tokens)
  corrected_by:
```

---

## S103c-fix-4 — one correction interaction across all three views (D202/D203) (build mode, surface-only)

roles: engineer (the consolidation — the shared renderCorrectionList + thin renderers; the structural guard), analyst (Step 0 inventory + the shape decision), technical writer (the marker, the smoke doc, this entry).

- **Step 0 shape decision (the finding).** The two correction renderings — CDD's `renderBindings` (groups by element; had in-place re-render, bulk, triage, same-goal relink select) and List/Map's `renderGoalCorrections` (groups by the goal's bound units; had the why/strength/relink but full-reload, no bulk, no triage) — differ in two context dimensions: **grouping** (by element vs by goal) and the **relink target space** (same-goal element select vs cross-goal goal-then-element picker). So the chosen shape is **a shared interaction behind two thin renderers** (Step 0 option 2), not a single merged renderer: one `renderCorrectionList` owns the interaction (triage, bulk select, in-place re-render, the why/strength/unlink row), and the **relink control** is the one context-specific piece passed in (`relinkControlFor`). The unlink uses `b.element_kind` uniformly — correct for both, since a binding's kind is its element's kind.

- **Reflection 1 — is it genuinely single-sourced, and which protection landed?** **Structural, not just functional parity.** The guard asserts the unlink action is **one POST site** (`unlinkBinding`), the bulk control + the select checkbox live **only** in `renderCorrectionList`, and both `renderBindings` and `renderGoalCorrections` **delegate** to it (their bodies contain `renderCorrectionList(`). A future view that re-implemented its own correction handling would either add a second unlink POST site or a second bulk control — both caught by the count assertions — or skip the delegate call — caught by the body check. So a determined divergence is *prevented*, not merely unlikely; this is the UI analogue of the read-consistency guard, the protection the brief asked for in its strongest expressible form.

- **Reflection 2 — what context difference did the consolidation absorb?** Two, both pushed to the caller as parameters: **grouping** is the caller passing the already-filtered `bindings` (CDD: by `element_id`; List/Map: by `outcome_id`), and the **relink target space** is the caller passing `relinkControlFor` (CDD: a same-goal element `<select>`; List/Map: the cross-goal goal-then-element picker). The shared source knows nothing of either — it takes a list of bindings, a refresh, and a relink-control factory. So the next correction surface inherits the shape directly: pass your bindings and your relink control, get triage / bulk / in-place re-render / why / strength for free. (One normalization fell out: the consolidated row standardises on **element_kind + strength + why** — the CDD view's old raw tier badge was dropped as redundant with the strength band.)

- **methodology:** the recurring-fix signal is the lesson — the same in-place + bulk fix was needed a third time because S103c-fix-2 copied the *display* of the CDD corrections without the *interaction*, and the guard then checked the display rendered, not that the interaction matched. Single-sourcing the interaction + a structural guard that the views *delegate* (not just render the same strings) is what converts "fix it again next surface" into "can't diverge." The general rule: when a behaviour has been re-patched in N places, the (N+1)th fix should be the consolidation, and the guard should assert the shared *source* is used, not that the behaviour is present.

- **AC verdicts.** AC1 (the interaction single-sourced; CDD/List/Map use it; verified by the guard) ✓ — the structural single-source guard. AC2 (List/Map re-render in place on unlink/relink, no collapse, matching CDD) ✓ — the sub-local refresh; served. AC3 (bulk unlink in all three; each emits its own record) ✓ — the shared bulk loop over `unlinkBinding`; the one-record-per-binding behaviour stands from S103c-fix-3 (the shared call routes every view through it). AC4 (why + strength consistent across views) ✓ — rendered from the one source (guard). AC5 (the guard fails if a view re-implements its own correction interaction) ✓ — the unlink-site/bulk-control uniqueness + delegate checks. AC6 (surface-only: no schema, no new write path, matcher + consume untouched) ✓ — the diff is `daily_driver.html` + the marker + tests, grep-confirmed. AC7 (suite/import-linter/AST green; functional + served-HTML guards; operator-gated live via `make build-api`) ✓ — unit + enforcement green, import-linter 48/0, served on pin `6a5067e`, correction contract guards live.

- Close state: **six commits — marker (`3e04c27`), single-source the interaction (`e61a102`), List/Map in-place + bulk (`5b014e7`), single-source guards (`b5398e8`), pin+smoke (`490d3fc`)** (and the marker). Surface-only; no write-path, matcher, schema, or D-entry change. The correction interaction is now one component; the structural guard stops the divergence from recurring. The live feel is the operator's gated browser pass.

```
metrics:
  classification: build session (S103c-fix-4 — single-source the correction interaction + List/Map parity; surface-only UI consolidation, no decision)
  session_started: 2026-06-19
  session_closed: 2026-06-19
  step0: two correction renderings — CDD renderBindings (group by element; in-place/bulk/triage/same-goal relink) and List/Map renderGoalCorrections (group by goal; why/strength/relink but full-reload, no bulk/triage); they differ in grouping + relink target space, so the shape is a SHARED INTERACTION BEHIND TWO THIN RENDERERS (option 2), not a single merged renderer; the relink control is the parameterised context piece
  consolidation: renderCorrectionList (+ unlinkBinding) is the one correction interaction (triage weakest-first + weak-only, bulk select + "Unlink selected" each its own record, per-binding why/strength/unlink, in-place re-render); CDD renderBindings + List/Map renderGoalCorrections are thin renderers passing {bindings, refresh, relinkControlFor}; relink control: cddElementRelink (same-goal select) vs relinkPicker (cross-goal); unlink uses b.element_kind uniformly; row standardised on element_kind + strength + why (tier dropped, redundant with strength)
  parity_gain: List/Map now re-render in place on unlink/relink (sub-local refresh, no fold collapse) + bulk select + triage — identical to CDD, by pointing them at the shared source
  protection: STRUCTURAL single-source guard — unlink action is one POST site (unlinkBinding); bulk control + checkbox only in renderCorrectionList; both renderers delegate (body contains renderCorrectionList(); a re-implementing view would duplicate the unlink site/bulk control or skip the delegate -> caught
  tests: tests/unit/apps/api/test_daily_driver_surface.py (single-source guard: unlink-POST count==1, bulk control + cdd-pick only in the shared source, both renderers delegate; AC4 why+strength from the one source); functional bulk one-record-each stands from S103c-fix-3; unit + enforcement exit 0; import-linter 48/0
  surface_only: confirmed — diff is apps/api/static/daily_driver.html + charter/current-package.md + tests only; no Python write-path, no matcher, no schema, consume side untouched
  live: image re-pinned 6a5067e; served HTML carries renderCorrectionList (one source) used by all three views; correction contract guards pass live; the in-place/bulk feel on List/Map is operator-gated
  reflection_1_single_source: structural (not just functional parity) — the guard catches a future view re-implementing its own correction handling (duplicate unlink site / bulk control, or missing delegate)
  reflection_2_context_difference: grouping (by element vs by goal) absorbed by the caller passing filtered bindings; the relink target space (same-goal vs cross-goal) by the caller passing relinkControlFor; the shared source is context-free
  commits: 3e04c27 (marker), e61a102 (single-source the interaction; CDD rewired), 5b014e7 (List/Map in-place + bulk), b5398e8 (single-source guards), 490d3fc (pin + smoke)
  charter_touchpoints: charter/current-package.md (S103c-fix-4 marker). No schema.md, no decisions.md (no D-entry). log/sessions.md (this entry); compose.yaml (pin); docs/smoke/p2b_s103c_fix_4_one_correction_interaction.md
  numbering: S103c-fix-4 (correction-interaction consolidation over S103c); no D-entry; D203 stands as the live decision max; S104's direction decision takes the next free number when it runs
  corrects: S103c-fix-2 (the List/Map corrections it added copied the CDD display not the interaction — collapse-on-unlink, no bulk; single-sourced here so it cannot diverge again)
  corrected_by:
```

---

## S103d — align the matcher's match rule to the read-side basis rule (D204) (build mode, matcher logic)

roles: architect (D204 — the align-and-single-source decision; single-source vs align call), engineer (the `significant_tokens` helper, the substring-branch drop, the tie/precision/recall tests, the measurement script), analyst (Step 0 premise reconciliation + the live before/after re-measure), technical writer (D204 text, the marker, the smoke doc, this entry).

- **Step 0 — three brief-premise corrections, all toward the brief's own single-source preference.** (1) **Stamping stale**: the brief's "number off S100" reflects the lagged strategic snapshot; live max is S103c-fix-4, so this stamped **S103d** / **D204** (S104 stays reserved for direction). (2) **`_keyword_match` is one shared function** used by *both* the keyword and alias tiers — not the "alias path" the brief named. So dropping substring aligns both tiers, which is the true single-source; the keyword tier had **zero** substring-only binds live, so only the alias substring-only binds change in practice. (3) **Baseline drifted** to **10 of 745** (all alias, all substring-only) from the brief's 12 of 839 — same pattern. Single-source is **feasible** (matcher + read side in one module, no import boundary), so the stronger "one shared symbol" form was taken, not just align-and-tie-test. None blocked the build; they refined it.

- **The change.** A new `significant_tokens(text)` / `shared_significant_tokens(a, b)` helper is the single place the non-stopword token set is computed. `_keyword_match` drops the bare substring branch and binds only on a shared significant token — exactly the rule `binding_rationale` already enforces on read. `binding_rationale` and `element_token_counts` now read through the same helper. So the matcher no longer creates the binds the honest-why narrates as `NO_CLEAR_BASIS`. Principled-subtractive: removes flagged-wrong behaviour, adds none.

- **Reflection 1 — opening the matcher sub-series.** This is the first touch on binding *logic* since S103b stood the matcher up; the whole S103c sub-series before it was read-side and surface (legibility, corrections, view coherence, single-sourcing the correction UI). The discipline that made this first logic touch safe was that the read side was built *first*: the honest-why (S103c-fix-3) gave a precise, already-shipped definition of "baseless," so the logic change had a target to align *to* rather than a judgment call to make. Touching the matcher became "make the bind rule equal the basis rule the read side already applies" — a subtraction with an oracle — instead of "decide what the matcher should do." The sequencing (legibility before logic) is what turned a risky matcher edit into a tight, provable one.

- **Reflection 2 — single-source vs align.** The brief offered both: one shared symbol if no import boundary forces a copy, else align-both-and-tie-test. Step 0 found matcher and read side in the same module with no boundary between them, so single-source was feasible and taken — one `significant_tokens` helper both read through. But the tie-test was kept *on top* of the single-source, not as the fallback alternative to it. The reason: single-sourcing makes the rules equal *today*; the tie-test makes them *provably unable to diverge* — it would fail if a future edit re-introduced substring into one rule but not the other. That is the "guard the invariant, not the proxy" rule (promoted to methodology.md last commit) applied to the matcher: the invariant is *matcher binds ⟺ read side has a basis*, and the tie-test asserts exactly that, not the proxy of "both call the same function."

- **Reflection 3 — pre-pass timing.** The settled matcher-quality sequence is coverage authoring first (operator work on the shipped surfaces) → this bind-rule tightening now → the correction-consumption pass next → the embedding recall tier deferred (D197 two-threshold). Tightening *before* the operator's coverage pass is the right order: a looser matcher would have the operator correcting binds the rule itself calls baseless, polluting the very correction signal the consume pass will learn from. Removing the baseless binds first means the corrections the operator makes during coverage authoring are about genuine matches, not matcher noise — a cleaner teaching signal for the pass that consumes it.

- **Reflection 4 — the structural-test / single-source binding count (methodology).** The tie-test binds the matcher and the read side at the *behaviour* level (bind ⟺ basis), which is one count higher than the S103c-fix-4 structural guard that bound the correction views at the *call-site* level (both renderers delegate to one source). Both are single-source guards, but they sit at different rungs: the UI guard asserts the shared symbol is *used*; the matcher tie-test asserts the shared rule *holds as an equivalence*. The behaviour-level guard is the stronger form — it survives a refactor that changes how the rule is shared, because it checks the invariant the sharing exists to protect. The methodology note: when single-sourcing a *rule* (not just a UI component), prefer the behavioural equivalence guard over the call-site-delegation guard; the latter is the right tool when the shared thing is a rendering, the former when it is a decision.

- **AC verdicts.** AC1 (substring branch dropped; matcher binds only on a shared significant token) ✓ — `_keyword_match` is now `bool(shared_significant_tokens(...))`. AC2 (single-sourced to the read-side rule) ✓ — one `significant_tokens` helper; matcher + `binding_rationale` + `element_token_counts` all read through it. AC3 (tie-test pins matcher-binds ⟺ read-side-has-basis) ✓ — `test_match_rule_ties_to_the_read_side_basis_rule`, both tiers, across exact/shared/substring-only/disjoint. AC4 (precision: substring-only no-shared-token does not bind) ✓ — helper + `infer_element_evidence` alias-fallback tests. AC5 (recall: shared significant token still binds) ✓ — both directions + the alias outcome fallback. AC6 (live: baseless binds gone, zero alias no-clear-basis) ✓ — 10→0, idempotent across two re-correlates. AC7 (suite/import-linter green; no schema, no migration) ✓ — daily_driver unit + enforcement green, import-linter 48/0, re-runnable via `make correlate-units`.

- Close state: **four commits — charter-first D204 + marker (`197e468`), the rule alignment + helper (`8217bbe`), the tie/precision/recall tests (`33c0457`), and this measurement + pin + close**. The matcher and the read side now share one rule that cannot diverge; the baseless binds are gone with zero residual. The live read across List/Map/CDD (no baseless alias "no clear basis" anywhere) is the operator's gated browser pass.

```
metrics:
  classification: build session (S103d — align/single-source the matcher's keyword/alias match rule to the read-side basis rule; matcher logic, first binding-logic touch in the S103c sub-series)
  session_started: 2026-06-22
  session_closed: 2026-06-22
  step0_stamping: brief's "number off S100" stale (lagged strategic snapshot); live max S103c-fix-4 -> stamped S103d / D204; S104 reserved for direction
  step0_shared_function: _keyword_match is ONE function used by both the keyword AND alias tiers (not an "alias path"); dropping substring aligns both -> true single-source; keyword tier had 0 substring-only binds live so only alias changes in practice
  step0_baseline: drifted to 10/745 (all alias, all substring-only) from the brief's 12/839; same pattern; single-source feasible (one module, no import boundary) -> stronger "one shared symbol" form taken
  change: significant_tokens / shared_significant_tokens helper is the single token-set source; _keyword_match drops the substring branch, binds only on a shared significant token; binding_rationale + element_token_counts read through the helper; principled-subtractive (removes flagged behaviour, adds none)
  tie_test: test_match_rule_ties_to_the_read_side_basis_rule asserts matcher binds (unit,target) iff binding_rationale finds a basis, for keyword AND alias tiers, across exact/shared-token/substring-only/disjoint pairs (the divergence guard, behaviour-level invariant)
  precision_recall: substring-only no-shared-token does NOT bind (helper + infer_element_evidence alias fallback); a genuine shared token still binds, both directions + alias outcome fallback
  live_before: 745 bindings / 431 bound units / 10 no-clear-basis (ALL alias) — accumulated state on old image (ops/measure_alias_basis)
  live_after: 727 bindings / 421 bound units / 0 no-clear-basis — re-correlate on tightened matcher, idempotent across two runs; baseless binds gone, zero residual
  live_delta_honesty: net drop -18 bindings exceeds the 10 baseless because the 745 baseline was accumulated live state (corrections + prior runs), NOT the old matcher's fresh fixed point; rule change is purely subtractive (a shared-token bind cannot be dropped by requiring a shared token; precision + tie tests prove it, 0 residual confirms nothing baseless survived); ids-only reported, no content (D21)
  tests: tests/unit/contexts/daily_driver/test_goal_assessment.py (tie-test + precision + recall + significant_tokens), tests/unit/contexts/daily_driver/test_element_evidence.py (alias substring-only no-bind + shared-token still-binds); daily_driver unit + enforcement exit 0; import-linter 48/0
  no_schema: confirmed — no migration; re-runnable via make correlate-units; matcher re-derives the edges
  reflection_1_opening_matcher_subseries: read side built first (honest-why S103c-fix-3) gave a shipped definition of "baseless" to align TO; logic touch became a subtraction with an oracle, not a judgment call; legibility-before-logic sequencing made the first matcher edit tight and provable
  reflection_2_single_source_vs_align: single-source feasible (one module) -> taken; tie-test kept ON TOP (not as the fallback) because single-source makes rules equal today, the tie-test makes them provably unable to diverge — "guard the invariant, not the proxy" applied to the matcher
  reflection_3_prepass_timing: tighten BEFORE the operator's coverage-authoring pass so corrections are about genuine matches not matcher noise — a cleaner teaching signal for the consume pass that follows
  reflection_4_structural_test_rung: tie-test binds at the BEHAVIOUR level (bind <=> basis), one rung above S103c-fix-4's call-site delegation guard; the behavioural equivalence guard is the stronger form for single-sourcing a RULE (vs the call-site guard for a rendering)
  commits: 197e468 (charter-first D204 + marker), 8217bbe (rule alignment + significant_tokens helper), 33c0457 (tie + precision + recall tests), <this commit> (measurement script + pin + smoke + close)
  charter_touchpoints: charter/decisions.md (D204 + index line), charter/current-package.md (S103d marker + settled matcher-quality sequence), log/sessions.md (this entry), compose.yaml (pin), docs/smoke/p2b_s103d_alias_tier_shared_token.md, ops/measure_alias_basis.py (re-runnable before/after measure)
  numbering: S103d (first matcher-logic touch over the S103c read/surface sub-series); D204 the live decision max; S104's direction decision takes the next free number when it runs
  corrects:
  corrected_by:
```

---

## S103e — re-scope the dogfood to get-a-job by archiving the other goals (D205) (build mode, clean-data)

roles: architect (D205 — the archive-not-erase choice + the single-seam scope decision), engineer (the `:Outcome.archived_at` write path, the read filter, the coverage-consistency fix, the ops script, the tests), analyst (Step 0 premise reconciliation + the live before/after re-measure + the tie-out arithmetic), technical writer (D205 text, schema.md, this entry).

- **Step 0 — two premises refined, neither contradicted; the plan held.** (1) **Goals carried no archive/status field.** Goal "status" is a computed-on-read verdict (`goal_status.py`), not persistence; the Revisable Protocol (D114) / `archived_at` pattern covers tools/roles/templates in **Postgres**, not the `:Outcome` **graph** node. So a status add was needed — but Neo4j-schemaless (no Alembic, no Cypher constraint, the S102/S103a schemaless-prop precedent), sizing the session as a small graph change, not config and not a DB migration. The brief anticipated exactly this and pre-authorised it. (2) **Moving house exists in neither form** — not authored, and the "House purchase" class is only a deferred goal-discovery *nomination* (D197, cross-ref D184's domain), not a built latent record (the brief's "D184 latent" pointer was imprecise; the conclusion — don't create — was identical). The live enumeration confirmed it: 8 authored goals, no house/moving-house among them. Recorded for its later framework step: **latent-nomination-only**.

- **The change.** A reversible `:Outcome.archived_at` marker, set by `archive_outcome` / removed by `unarchive_outcome` (session + repo + `OutcomeGraphPort`). `_LIST_OUTCOMES` scopes to active goals (`WHERE o.archived_at IS NULL`) — the **single `list_goals()` chokepoint** that feeds both the assess surface (List/Map/CDD) and the matcher (`correlate_goal_facets` reads `list_goals`), so one filter scopes both reads. The one gap the filter alone left: `assess_goals`/`group_units_by_goal` counted a unit "linked" on *any* edge without intersecting the active goal set (unlike `goals_with_edge`, which did intersect) — a latent inconsistency that would have hidden archived-goal units instead of swelling the unlinked pile. Fixed: `units_with_edge` intersects `goal_ids`, so an archived-goal bind reads **unlinked**, the honest coverage read (D171). Nothing is deleted; the archive is the only mechanism.

- **Reflection 1 — archive versus erase (substantive).** The mechanism honoured invariant 4 and originals-never-erased by construction: archiving sets one property and touches nothing else — the `:Outcome` node, its authored CDD elements, its `EVIDENCES` binds and its audit history all stay in the graph. The live reversibility proof was unambiguous: from the re-scoped state (1 active / 7 archived), `--reactivate` restored all 8 and coverage returned to the **exact** baseline — 8/8 goals, **425/1359 units linked**, byte-identical to the before-read — so the archived goals came back whole, binds intact. The Revisable Protocol (D114) did **not** cover goals (it lives at the Postgres entity level for tools/roles/templates), so a status add *was* needed; but the add was schemaless and minimal because the goal is a graph node, not a constrained table. The deeper point: archive-not-erase is not a courtesy here — it is the only legible path to a clean corpus, because the whole value of the dogfood is the *real* job-search inbox, and erasing the other goals' work would have meant erasing the very signal that later proves coverage (a marathon email orphaned today is a goal authored tomorrow recovering it).

- **Reflection 2 — the clean-corpus read (the read the re-scope was for).** This is the first inspectable single-goal read. Before: 8 goals, 425/1359 linked, 145 distinct orphans (934 instances). After re-scope + re-correlate: **1 goal (get-a-job), 190/1359 linked, 195 distinct orphans (1169 instances)**. Get-a-job's binds were **unchanged** — 190 routines / 166 confirmed, identical to the eight-goal read — so scoping the corpus did not perturb the goal under study. The arithmetic ties out exactly: the 235 units that were linked to the 7 archived goals (425−190) now read unlinked (orphan instances 1169−934 = 235), and the matcher wrote 299 EVIDENCES edges, all to get-a-job's elements (no bind against an archived goal). The orphan sample now surfaces the archived goals' work honestly as *coverage* — "Wide World Marathon", the medications, "Megan's articulation warm-ups" — under "no goal edge", never as neglect (D171). What get-a-job's binds look like by eye now that the cross-goal collisions are archived out: legible. 190 routines, 166 of them confirmed (the rule-confirmed job-search emails + the lever-step commitments), against a single authored model — exactly the inspect-and-correct surface D200's authored-and-corrected loop needs, instead of eight goals' binds interleaved on a two-thirds-unbound corpus.

- **Reflection 3 — moving house.** Latent-nomination-only: not an authored goal and not a built latent record — the deferred D197/D156 goal-discovery nomination (cross-referencing D184's job-search domain). Its framework step **creates** it (it is not waiting to be activated); nothing exists to un-archive. Recorded so that step does not mistake a nomination for an authored goal awaiting scope.

- **AC verdicts.** AC1 (every authored goal except get-a-job archived; get-a-job active) ✓ — live read: 8 active → 7 archived → 1 active (get-a-job `…6300a1`). AC2 (no data deleted; links + audit intact; reversible) ✓ — archive sets one property (session test: SET not DELETE/REMOVE); reactivate restored all 8 and coverage returned to the exact baseline (425/1359). AC3 (assess surface renders get-a-job only) ✓ — `_LIST_OUTCOMES` filter (session test) + live coverage shows 1/1 goal. AC4 (matcher writes no bind against an archived goal) ✓ — re-correlate on the clean state wrote 299 edges, all to get-a-job (the only target). AC5 (coverage shows the larger unlinked pile as coverage not neglect) ✓ — 145→195 distinct orphans, the archived goals' work under "no goal edge" (D171); domain tests pin a unit bound only to an archived goal reads unlinked. AC6 (suite green; import-linter kept; schema.md updated) ✓ with one honest caveat — import-linter 48/0, enforcement + all touched unit suites green, schema.md carries `:Outcome.archived_at` in the same commit as the code; the 6 pre-existing live-stack integration/e2e failures (optimization `matcher_suppression`, calendar/methodology/tools/p3) fail **identically on the base commit** and are unrelated to this change (no migration was needed, so no migration to enforce).

- Close state: **five commits — charter-first D205 (`a291b77`), the archive mechanism (`950c117`), the read-scope + coverage-consistency fix (`6a4422d`), the reactivate-robustness fix the live pass surfaced (`4b2bb71`), and this re-measure + close**. The personal tenant is left in the clean single-goal state (get-a-job active, 7 archived, reversibly). The next step is slice one — authoring the get-a-job CDD from the framework against this clean corpus.

- methodology: the live re-measure surfaced a real gap the unit tests could not — `--reactivate` rebuilt the id set from seed modules and silently missed Health regimen (a goal seeded by a module the ops script did not import), restoring 7 of 8. The fix (read the archived set from the graph, not from seed internals) is a small instance of "a proof on fakes is not a proof on the real surface": the reversibility *guarantee* was sound in the unit test, but the reversibility *operation* was incomplete until the live corpus exercised it. Recorded as confirmation of the live-surface law, not a new pattern.

```
metrics:
  classification: build session (S103e — re-scope the dogfood to get-a-job by archiving the other goals; clean-data move before the framework CDD authoring; schemaless graph change)
  session_started: 2026-06-24
  session_closed: 2026-06-24
  step0_no_archive_field: goals carried no archive/status field (status is a computed-on-read verdict); the Revisable Protocol/archived_at pattern covers tools/roles/templates in Postgres, NOT the :Outcome graph node -> a status add was needed, schemaless (no Alembic, no Cypher constraint, the S102/S103a precedent); small graph change, not config, not a DB migration
  step0_single_seam: list_goals() -> _LIST_OUTCOMES is the single chokepoint feeding both the assess surface and the matcher; one WHERE o.archived_at IS NULL scopes both
  step0_coverage_inconsistency: assess_goals/group_units_by_goal counted units_with_edge on ANY edge without intersecting the active goal set (unlike goals_with_edge, which intersected) -> a unit bound only to an archived goal would vanish from view instead of swelling the unlinked pile; corrected so units_with_edge intersects goal_ids
  step0_moving_house: exists in neither form — not authored, and the "House purchase" class is only a deferred goal-discovery nomination (D197/D184), not a built latent record; created at its own framework step later; live enumeration confirmed 8 authored goals, no house
  change: :Outcome.archived_at marker (archive_outcome sets, unarchive_outcome removes) on session+repo+OutcomeGraphPort; _LIST_OUTCOMES scopes active; units_with_edge intersects goal_ids; ops/rescope_dogfood_to_get_a_job.py + make rescope-dogfood / -reactivate
  live_before: 8/8 goals linked, 425/1359 units linked, 145 distinct orphans (934 instances)
  live_after: 1/1 goal linked (get-a-job 190 routines / 166 confirmed, unchanged), 190/1359 units linked, 195 distinct orphans (1169 instances); 299 EVIDENCES edges, all to get-a-job
  live_tieout: 425-190 = 235 units that were linked to the 7 archived goals now read unlinked; orphan instances 1169-934 = 235; exact tie-out; counts/ids only, no content (D21)
  live_reversibility: from re-scoped state (1 active / 7 archived), --reactivate restored all 8 and coverage returned to the EXACT baseline (8/8 goals, 425/1359 units linked); archived goals came back whole, binds intact, nothing erased
  tests: tests/unit/contexts/ingestion/test_neo4j_outcome_graph.py (list scopes active; archive SETs marker not DELETE; unarchive REMOVEs; missing->False; archived-ids Cypher filters IS NOT NULL), tests/unit/contexts/daily_driver/test_goal_assessment.py (unit bound only to an absent/archived goal reads unlinked in both projections; archived goal renders nowhere; get-a-job still renders + counts linked); daily_driver + ingestion unit suites green; enforcement green; import-linter 48/0
  suite_caveat: 6 pre-existing live-stack integration/e2e failures (optimization matcher_suppression, calendar wiring, methodology lvt round-trip, tool repository, p3 full slice, agent methodology flow) fail IDENTICALLY on the base commit 5c1f98e; unrelated to this change; no new failures introduced
  no_migration: confirmed — :Outcome.archived_at is a schemaless Neo4j property; schema.md updated in the same commit as the code (Commit 1)
  reflection_1_archive_vs_erase: archiving sets one property, touches nothing else; reactivate restored 8/8 to the exact baseline -> whole, reversible; D114 did NOT cover goals (Postgres entity level) so a status add was needed but schemaless+minimal; archive-not-erase is the only legible clean-corpus path because the real job-search inbox IS the dogfood
  reflection_2_clean_corpus_read: first inspectable single-goal read; get-a-job 190 routines / 166 confirmed UNCHANGED from the eight-goal read; the 235 archived-goal units now orphan honestly under "no goal edge" (D171); legible inspect-and-correct surface for D200's loop, replacing eight goals' interleaved binds
  reflection_3_moving_house: latent-nomination-only (D197/D156 goal-discovery, cross-ref D184); its framework step CREATES it, nothing to activate
  methodology: live re-measure surfaced --reactivate's seed-id gap (restored 7/8, missed Health regimen) that unit tests could not; fix reads the archived set from the graph not seed internals; a small confirmation of "a proof on fakes is not a proof on the real surface" — the guarantee was sound, the operation incomplete until the live corpus exercised it
  commits: a291b77 (charter-first D205 + index line), 950c117 (archive mechanism + schema.md + ops script), 6a4422d (read-scope + coverage-consistency fix + domain tests), 4b2bb71 (reactivate-robustness fix the live pass surfaced), <this commit> (live re-measure + session log)
  charter_touchpoints: charter/decisions.md (D205 + index line), charter/current-package.md (S103e marker + the three-slice framework build order), charter/schema.md (:Outcome.archived_at), log/sessions.md (this entry), Makefile (rescope-dogfood targets)
  numbering: S103e (the clean-data move before slice one); D205 the live decision max; S104 reserved for direction
  corrects:
  corrected_by:
```

---

## Archive pointer

Prior sessions are windowed out at package close per the charter-and-log retention rule
(`charter/methodology.md`, "Charter and log retention (living-state versus ledger)"; D107,
per-package-on-close). This hot file holds only the open package's sessions (the Phase 2-B
v13 CDD authoring-and-correction layer; S102 and forward). Everything earlier lives under
`docs/archive/sessions/`, append-only, never pruned:

- `docs/archive/sessions/p1.md` … `p10.md` — Phase 1 sessions S1–S38a (archived pre-S102m).
- `docs/archive/sessions/p11.md` — S38b–S42 + P11-close hygiene.
- `docs/archive/sessions/p12.md` — P12 Phase-1-close audit + the Phase-2-design 7-Step arc + post-P12 hygiene.
- `docs/archive/sessions/p13.md` — S43–S48b + P13 framing/hygiene + post-P13 hygiene S49–S50.
- `docs/archive/sessions/p14.md` — S51–S52.
- `docs/archive/sessions/p15.md` — S53–S57 + Ciborra audit / corrections / methodology-upgrade / prune-and-reframe / Nango + P15-audit-residue.
- `docs/archive/sessions/p16.md` — S58–S63 + dogfood-setup.
- `docs/archive/sessions/p17.md` — S65.
- `docs/archive/sessions/p18.md` — S66.
- `docs/archive/sessions/p19.md` — S67, S69–S72, S64 (test-integrity), the dogfood-first-read charter write.
- `docs/archive/sessions/p20.md` — S68, S73.
- `docs/archive/sessions/phase2b.md` — Phase 2-B snapshot, S74–S101.
