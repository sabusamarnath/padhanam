# Archive — P10 sessions

## P10 — S36 — P10 open + audit read substrate (port, adapter, wiring, contract harness, live smoke)
roles: architect, engineer, technical writer
mode: build

- Produced: Eight commits closed S36 per the brief. Commit 1 (`docs(charter)`) landed the P10 framing charter touch-points ahead of code: `charter/packages/p10-epic.md` new with the full P10 scope and three-to-four-session forecast; `charter/current-package.md` transitioned from "P9 closed; P10 framing next" to "P10 open" with the corrective note on the misrouted P10 framing inputs from S35 close (S35's "P10 is the evaluation territory" line is correctly routed to the retrieval-evaluation design session pre-P11 per D92's audit-log-viewer scope for P10) plus a new audit-port-location-asymmetry carryover entry recording the operator's option-A selection at finding 1; `charter/decisions.md` appended D102 with eight alternatives (a-h, including alternative (h) on why `verify_chain` is not reused wholesale) and Kano category (must-have on substrate + page-level chain integrity, delighter on full-chain verification, must-have on ingestion management absorption at P10); `briefs/p10/s36.md` preserved per the convention. Commit 2 (`feat(p10/s36)`) landed four domain value-object modules at `contexts/audit/domain/`: `destination.py` (`AuditDestination` Literal), `audit_event_record.py` (13-field record including `id` which the write-side `AuditEvent` omits — two coexisting value objects mirroring the `AgentRunRecord`/`RunRecord` pattern at run_history), `chain_integrity.py` (`ChainIntegrityStatus` Literal plus `ChainIntegrityVerification` with `broken_at_id` required iff `status == 'broken_at_row'`), `query_filters.py` (seven filter dimensions `timestamp_range` + `actor` + `action_verbs` + `resource_type` + `resource_id` + `correlation_id` + `jurisdiction` with `resource_id` requires `resource_type` invariant at construction time; `AuditEventListCursor` on `(timestamp, id, page_size)`; `AuditEventListPage` envelope; `MalformedCursorError`; `PAGE_SIZE_CEILING=50` mirroring run_history); 39 new unit tests. Frozen dataclasses (not Pydantic v2 as the brief drafted) per sibling-symmetry — finding 8 in Appendix D. Single `timestamp_range` (not the brief's `started_at_range`/`ended_at_range` pair) per the schema's single-timestamp column — finding 9. Commit 3 (`feat(p10/s36)`) landed the consumer-defined port: `contexts/audit/ports/__init__.py` + `contexts/audit/ports/reader.py` defining `AuditEventReader` Protocol with three async methods (`get_audit_event`, `list_audit_events_with_filters`, `verify_chain_segment`) plus `AuditQueryRoutingError` raised when destination/tenant_context disagree at port-method entry; `contexts/audit/application/cursor.py` base64-of-JSON codec mirroring `contexts/run_history/application/cursor.py`; `.importlinter` layers-audit contract extended with `contexts.audit.ports` between application and domain so the new ports/ layer is enforced; 22 new unit tests (7 port-surface + 15 cursor round-trip and error paths). Commit 4 (`feat(p10/s36)`) landed `PostgresAuditEventReader` at `contexts/audit/adapters/outbound/postgres/reader.py` implementing the port against both destinations through destination-parameter routing per D102 alternative (b); constructor mirrors the write-side `PostgresAuditAdapter` (control-plane sessionmaker + per-tenant resolver callback); chain integrity verifier reuses `compute_event_hash` and `GENESIS_HASH` from `contexts/audit/domain/events.py` as primitives (the existing `verify_chain` walker is NOT reused because it walks from genesis and a page may begin mid-chain — D102 alternative (h)); page-granularity verification surfaces `verified` / `broken_at_row` / `partial` per D102; tuple-comparison cursor pagination with `sa.cast(..., pg.UUID)` per S33's defence-in-depth; 20 new unit tests. Commit 5 (`feat(p10/s36)`) landed wiring at both composition roots: `AuditEventReaderAdapter` class as the tenth consumer-port-plus-wiring-adapter on `apps/cli/_cross_context.py`; `build_audit_event_reader` factory at `apps/api/_agent_runtime_wiring.py`; 6 new wiring tests at `tests/integration/apps/cli/test_audit_event_reader_adapter.py`. Commit 6 (`test(p10/s36)`) landed seven tenant-isolation contract scenarios at `tests/contract/tenant_isolation/test_audit_reader_isolation.py` covering the four brief scenarios (cross-tenant get returns None; cross-tenant list returns empty; per_tenant without context raises; control_plane with context raises) plus three defence-in-depth extensions to the list method. Commit 7 (`docs(p10/s36)`) landed the live-stack smoke at `docs/smoke/p10_s36_audit_reader.md` plus the reproducible smoke script at `scripts/smoke_p10_s36.py`; padhanam-api image rebuilt with the S36 code, compose.yaml digest pin updated to `sha256:ce8ef7df...`; the smoke walks five scenarios + two routing-guard pairs end-to-end against tenant_a's 20-row chain and a seeded one-event control-plane chain. Commit 8 (this entry) appends the S36 session-log entry, lands Appendix D findings 8 + 9 + Appendix E close-state at `briefs/p10/s36.md`, and transitions `charter/current-package.md` to "S36 closed; S37 next."

- Decisions: One new D-entry at S36 — D102 (P10 audit log read substrate framing: extension of existing audit context, two-destination model via port-method parameter per alternative (b), chain integrity verified on read at page granularity reusing `compute_event_hash` + `GENESIS_HASH` primitives per alternative (h), HTTP transport with separately authorized routes deferred to S37, ingestion management HTTP API absorbed at S38 from the P6 carryover). Eight alternatives considered including (a) new bounded context (rejected on existing-principle grounds), (b) separate ports per destination (rejected as ceremony), (c) cheap chain integrity (rejected on Kano grounds), (d) full-chain verification inside Phase 1 (rejected on RICE-versus-Kano asymmetry), (e)/(f) deferring control-plane route to Phase 1 close / Phase 2 (both rejected on audit-evidence-claim grounds), (g) deferring HTTP API for ingestion management to Phase 2 substrate completion (rejected on admin-grade-clustering grounds with fallback honoured), (h) reuse `verify_chain` walker wholesale (rejected on structural-correctness grounds: it walks from genesis and reports `break_index=0` for mid-chain pages). Kano: must-have on substrate + two-destination model + page-level chain integrity; delighter on full-chain verification (deferred); must-have on ingestion management absorption at P10.

- Tests: 1200 passed + 17 skipped + 31 deselected (live_llm) at S36 close under stable ordering — net +94 from S35b's 1106. Breakdown: 39 new audit unit tests at commit 2 + 22 at commit 3 + 20 at commit 4 = 81 audit unit tests added at S36 (audit unit suite size went from 50 pre-S36 to 92 at S36 close, with 7 of the 50 staying covered by existing files and 81 net new); 7 contract harness scenarios at commit 6; 6 wiring tests at commit 5. 25/25 import-linter contracts kept after the `layers-audit` contract extended with `contexts.audit.ports`. AST enforcement passes. One intermittent test failure surfaced under random ordering — `tests/unit/contexts/audit/adapters/test_postgres_audit.py::test_empty_sentinel_writes_to_control_plane` — but passes when run alone or in targeted combinations; this is a pre-existing test-ordering flake (the test predates S36 since S12), not a regression caused by any S36 commit. Recording at the methodology document Patterns-observed list if it recurs at a future session.

- Enforcement layer: `.importlinter` `layers-audit` contract extended at commit 3 to insert `contexts.audit.ports` between `application` and `domain` so the new ports/ layer is policed by import-linter (previously the contract did not name a ports layer because the audit context's port lived at `contexts/audit/domain/ports.py`). The contract now reads `adapters → application → ports → domain` symmetric to other contexts (inference, tenancy, methodology, agent, evaluation, ingestion, tools). The intra-context port-location asymmetry — write-side `AuditPort` at `domain/ports.py`, read-side `AuditEventReader` at `ports/reader.py` — is recorded as a carryover at `charter/current-package.md` with activation trigger named (next port added to the audit context). The tenant-isolation contract harness extended from 20 scenarios at S35b close to 27 scenarios at S36 close (+7 audit reader). The seven new scenarios cover (a) cross-tenant get returns None, (b) cross-tenant list returns empty page, (c) positive complement (tenant_a sees only tenant_a rows), (d) per_tenant without context raises pre-routing on both get and list, (e) control_plane with context raises pre-routing on both get and list. The four-direction routing defence covers both destination directions on both query methods.

- methodology: Pre-write reconciliation fired its fifteenth instance at S36 session-open, the first P10 instance and the first substrate-shaping altitude after the S35a/S35b carryover-hygiene tail. The three brief-specified reconciliation passes (`contexts/audit/` tree, schema, principal pattern) plus four additional brief-shape findings produced one operator-decision finding (finding 1: audit-context port-location asymmetry — operator selected option (A), keep write-side port at `domain/ports.py`, place new reader at `ports/reader.py`, intra-context asymmetry recorded as carryover), two clarifications to brief language (finding 2: chain-helper reuse is partial — `compute_event_hash` + `GENESIS_HASH` reusable, `verify_chain` walker is structurally inapplicable to mid-chain pages; finding 3: `AuditEventRecord` is a new value object distinct from `AuditEvent` because the latter omits `id`), one brief-shape correction (finding 4: schema is 13 columns including `id`, not 12), and three confirmations (findings 5-7: D102 numbering clean, 404 behaviour confirmed, tuple-comparison defence-in-depth pattern exists). Findings 8 + 9 surfaced at commit-2 write time and landed as Appendix D additions: finding 8 (Pydantic v2 vs frozen dataclass — frozen dataclass chosen for sibling-symmetry with `AuditEvent` and `RunRecord`), finding 9 (single `timestamp_range` vs the brief's two-bound `started_at_range`/`ended_at_range` shape — the audit schema has one timestamp column). Find-rate distribution at S36: one structural-honesty finding requiring operator decision at session-open (port-location asymmetry); two clarifications at session-open absorbed mechanically; one schema-shape correction at session-open absorbed mechanically; three confirmations; two findings surfaced at write-time (commits 2-4) and absorbed in-line with Appendix D updates. The trajectory across the P9 + P10-open window: S31 (3 structural at session-open) → S32 (1) → S33 (1) → S34 (1) → S35 (0 session-open + 1 mid-session) → S35a (1) → S35b (2 session-open + 1 mid-session) → S36 (1 session-open + 2 write-time). The substrate-shaping altitude at S36 surfaced findings at three altitudes: brief-versus-as-built (session-open), brief-language-clarity (session-open absorbed), and write-time-symmetry-with-siblings (Appendix D commits 2-4). Pre-write reconciliation surfaces some kinds of drift; write-time surfaces others; the three-altitude pattern is methodology signal worth recording at the Phase 1 close audit's methodology document.

- methodology (second): Consumer-port-plus-wiring-adapter pattern reinforced at S36 with a new variant. Eleven-plus reinforcement continues to hold — `AuditEventReaderAdapter` is the tenth wiring-adapter class on `apps/cli/_cross_context.py` and adds a new variant on top of the cross-context, intra-context, transport, and per-tenant-adapter altitudes the pattern has already covered. The new variant is "cross-destination within a context" — the audit reader handles two destinations (per-tenant and control-plane) through a destination parameter on port methods rather than two separate ports. The pattern's altitude-agnostic claim now spans: cross-context (S26a-1 through S28b: methodology, role, retrieval, tool); intra-context (S28b: tool invoker); transport (S29b: SSE); same-altitude cross-composition-root re-use (S30b: agent runtime composition); per-tenant adapter wiring (S31, S33: run-history writer and reader); and now cross-destination within a context (S36: audit reader). The destination-parameter shape compresses two surfaces into one port without losing structural clarity at the adapter — the same routing logic that the write-side `PostgresAuditAdapter` uses (control-plane sessionmaker + per-tenant resolver callback selected by `tenant_id` sentinel) is mirrored on the read side. The mechanism stays the same: define the port with the consumer's DTO, never import the producer's aggregate, wire the impedance mismatch at the application composition layer.

- methodology (third): Chain-helper reuse semantics at S36 surfaced a methodology distinction worth recording — "primitive reuse" vs "wholesale reuse" of existing domain helpers. The brief at commit 4 read "Chain verification reuses existing helpers at `contexts/audit/domain/events.py`" which is ambiguous between two readings: (a) reuse all the helpers including `verify_chain`, (b) reuse the primitive components (`compute_event_hash`, `GENESIS_HASH`) and build new logic on top. Pre-write reconciliation finding 2 surfaced that `verify_chain` is structurally inapplicable to mid-chain pages (walks from genesis, reports `break_index=0` for any input whose first row's `previous_event_hash != GENESIS_HASH`), so reading (a) fails; reading (b) is what the page-granularity verifier needs. D102 alternative (h) records the rejection of wholesale reuse explicitly. The methodology recording: when a brief specifies "reuse existing helpers" and the helpers compose at multiple altitudes (per-row recomputation primitive vs full-chain walker), the brief should specify primitive-vs-wholesale to avoid downstream ambiguity. The Phase 1 close audit's methodology document could surface this as a brief-authoring discipline.

- methodology (fourth): What S36 leaves for S37 framing. P10's read substrate is now structurally complete at the port + adapter + wiring + contract harness + live smoke level. S37 anchors directly from the P10 epic note at `charter/packages/p10-epic.md` without needing a strategic-mode framing conversation unless structural drift surfaces. S37 work: HTTP routes at `apps/api/routers/audit.py` exposing both destinations through `/audit` (per-tenant under the existing principal-derived tenant context dependency at `apps/api/routers/inference.py`) and `/platform/audit` (control-plane under a new platform-operator claim extending D23's signed-token backend). The dependency resolver gains a branch on principal type: tenant principals route to tenant-context; platform-operator principals route to control-plane scope without tenant context. The D23 extension is the load-bearing piece S37 needs to settle structurally; the HTTP DTO shape and query-string vocabulary mirror S34's run-history HTTP work with the destination dimension added. Two carryovers from S36 worth flagging at S37 framing: (1) the audit-context port-location asymmetry — if S37 adds a third audit port (e.g., a query port for a new analytics consumer), the symmetrize-or-accept decision activates; (2) the brief-shape distinction between "primitive reuse" and "wholesale reuse" from finding 2 — if S37's brief specifies helper reuse, it should disambiguate which altitude. Both are inputs for the brief authoring rather than S36 commitments; the S37 strategic-mode framing conversation owns S37 scope decisions if it runs.

```
metrics:
  classification: new_work
  brief_started: 2026-05-14
  session_started: 2026-05-14
  session_closed: 2026-05-14
  merged: 2026-05-14
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: updated
  corrects:
  corrected_by:
```

## P10 — S37 — HTTP transport for audit reader + platform-operator principal type
roles: architect, engineer, technical writer
mode: build

- Produced: Eight commits closed S37 per the brief. Commit 1 (`docs(charter)`, 924171a) landed D103 in `charter/decisions.md` anchoring the platform-operator principal type as a discriminator-field claim on the D23 signed-token backend plus the HTTP transport for the audit reader with two route trees plus the 403 path extension; alternative (b) revised at pre-write reconciliation finding 1 user-question resolution to acknowledge the existing `Principal.roles` field and frame `principal_type` as structurally complementary rather than alternative to role-list (which is already production code via `OPERATOR_ROLE` at `tenant_audit.py:91`); ten alternatives a-j including (i) parallel `register_audit_error_handlers` and (j) dev-mint helper omits `OPERATOR_ROLE` by default per the other reconciliation findings; charter/current-package.md transitioned to "S37 in flight"; briefs/p10/s37.md preserved with Appendix D capturing the four findings + four mechanical absorptions. Commit 2 (`feat(p10/s37)`, a18fddf) extended the D23 backend at `padhanam/security/auth.py` with `PrincipalType` StrEnum, `Principal.principal_type` field defaulting to TENANT for back-compat, `PlatformOperatorPrincipal` thin frozen-dataclass scope marker, `issue_dev_token` extended with `principal_type` kwarg, new `issue_platform_operator_dev_token(subject, roles=None)` helper omitting `OPERATOR_ROLE` by default; new `get_platform_operator_principal` dependency at `apps/api/middleware.py`; `get_tenant_context` at `apps/api/routers/inference.py` extended with the discriminator check at entry. Both 403 paths raised `HTTPException(403)` at this commit so the auth surface was testable in isolation; commit 4 refactored to the typed exception. 15 new unit tests (11 auth + 2 middleware + 2 inference dependency). The commits 3-5 ordering swapped from the brief's forecast (brief had routes-then-DTOs-then-errors; landed DTOs-then-errors-then-routes) for tree-integrity reasons — the route layer imports both DTOs and the registered error handlers, so the substrate landed first. Commit 3 (`feat(p10/s37)`, 7da5bc0) landed `apps/api/routers/_audit_dto.py` (three Pydantic v2 DTOs mirroring `AuditEventRecord`, `ChainIntegrityVerification`, `AuditEventListPage` 1:1; hash field validators) and `apps/api/routers/_audit_query.py` (`parse_audit_list_query` returning `(filters, cursor, page_size)` three-tuple — different from run-history's two-tuple because the audit port takes `page_size` as a separate parameter; eight filter parameters mapping to the seven-filter port surface from S36; `InvalidAuditFilterError` typed exception subclass of ValueError). 16 new unit tests (7 DTO round-trip + 9 query parser). Module naming `_audit_dto.py` + `_audit_query.py` (underscore-prefixed singular) per finding 2 user-question resolution mirroring `_run_history_dto.py`/`_run_history_query.py`. Commit 4 (`feat(p10/s37)`, f5e1cbf) extended `apps/api/_errors.py` with five new error-code paths: 403 `principal_type_mismatch` (typed `PrincipalTypeMismatchError` carrying required/actual principal-type values; handler fires AUTHZ_DENIAL security event with attempted route, principal_ref, tenant_id-when-tenant-typed for forensic traceability), 404 `audit_event_not_found` (typed `AuditEventNotFoundError`; no security event fires because per-tenant destination scopes the query and cross-tenant invisibility is privacy-preserving), 400 `invalid_audit_filter` (typed `InvalidAuditFilterError` from the parser), 400 `malformed_audit_cursor` (typed `AuditMalformedCursorError` from the domain; distinct code from run-history's `malformed_cursor` for clarity), 400 `invalid_audit_routing` (typed `AuditQueryRoutingError` defence-in-depth from the port surface). Parallel `register_audit_error_handlers(app)` function alongside the existing `register_run_history_error_handlers` per finding 4 user-question resolution. Refactored commit 2's dependencies to raise `PrincipalTypeMismatchError` instead of `HTTPException(403)`. 7 new error handler tests plus commit-2's middleware and tenant-context tests updated to assert the typed exception. Commit 5 (`feat(p10/s37)`, c218a3e) landed `apps/api/routers/audit.py` with two FastAPI routers (`tenant_router` under `/audit/*`; `platform_router` under `/platform/audit/*`) and four routes, each declaring its required principal-type dependency at route signature per D103's visibility-grounds reasoning; `get_audit_event_reader` dependency pulls the reader off `app.state` (returns 503 if not wired, mirroring run_history precedent); single-event routes raise `AuditEventNotFoundError` on reader None-return; list routes consume `parse_audit_list_query` and encode `next_cursor` via the S36 cursor codec. Composition wiring at `apps/api/main.py`: `AppCompositions` extended with `audit_event_reader` field; `_build_default_compositions` calls `build_audit_event_reader` (existing factory from S36) with the audit writer's `control_plane_sessionmaker` so reads and writes pool against the same engine — `PostgresAuditAdapter` gains a `control_plane_sessionmaker` property to expose the shared sessionmaker publicly without reaching into the underscore-prefixed attribute. 10 integration tests at `tests/integration/api/test_audit_routes.py`. Commit 6 (`test(p10/s37)`, dfdce5b) extended the D24 tenant-isolation harness from 20 scenarios (run-history HTTP isolation closed at S34) to 26 scenarios via six new audit HTTP cases at `tests/contract/tenant_isolation/test_audit_http_isolation.py`: cross-tenant get returns 404 no security event (privacy-preserving); tenant token on platform route returns 403 + AUTHZ_DENIAL; platform-operator token on tenant route returns 403 + AUTHZ_DENIAL with `tenant_id=null` because the offending token's empty sentinel does not propagate; platform-operator on platform route succeeds; unauthenticated request returns 401 from the middleware no route-level event; resource_id-without-resource_type returns 400 reader never invoked. Commit 7 (`docs(p10/s37)`, ca76e9e) landed the live-stack smoke at `docs/smoke/p10_s37_audit_http.md` plus the reproducible script at `scripts/smoke_p10_s37.py`; padhanam-api image rebuilt with the S37 code, compose.yaml line 380 digest pin updated to `sha256:39b0d026...`; the smoke walks all ten scenarios end-to-end against tenant_a's 23-row chain and a seeded one-event control-plane chain; four security events captured in `logs/security.jsonl` (one AUTH_FAILURE for scenario 8, two AUTHZ_DENIAL for scenarios 9 and 10, plus one PRIVILEGED_ACTION from the per-tenant session-factory cache resolving tenant_a credentials). Commit 8 (this entry) appends the S37 session-log entry, lands Appendix E close-state at `briefs/p10/s37.md`, and transitions `charter/current-package.md` to "S37 closed; S38 next."

- Decisions: One new D-entry at S37 — D103 (platform-operator principal type as discriminator-field claim on D23 signed-token backend, complementary to existing `Principal.roles`; HTTP transport for audit reader with two route trees and 403 path extension). Ten alternatives considered (a-j) including the load-bearing (b) revision at pre-write reconciliation: role-list claim was named as hypothetical Phase 2 future in the drafted D103, but `Principal.roles` is already production code via `OPERATOR_ROLE` at the tenant_audit operator endpoint. Reasoning revised in five lines to acknowledge the existing field and frame `principal_type` as structurally complementary because the discriminator anchors `tenant_id`'s conditional validity at decode time (which role-list cannot do cleanly because `verify_credential` requires `tenant_id` unconditionally today; introducing role-conditional decode would commingle permission evaluation with token-shape validation at the most-trusted chokepoint). Other alternatives rejected on usual grounds: (a) boolean discriminator scales poorly; (c) single route tree with body-conditional hides auth decision; (d) three-tree shape YAGNI; (e) standalone verify endpoint deferred per D102; (f) inline DTOs rejected on file-size grounds with module naming `_audit_dto.py` per S37 reconciliation finding 2; (g) combined `get_principal()` discriminated-union rejected on FastAPI ergonomics; (h) HTTP Basic/API-key auth for platform-operator rejected on consistency grounds; (i) extend `register_run_history_error_handlers` in place rejected on coupling grounds; (j) issue platform-operator dev tokens with `OPERATOR_ROLE` by default rejected on conceptual-separation grounds per finding 3. Kano: must-have on the discriminator-field claim shape, two-tree route shape, 403 path extension, parallel registration shape; performance on module naming; delighter deferred on standalone verify endpoint + Phase 2 permission model.

- Tests: 1251 passed + 17 skipped + 31 deselected (live_llm) at S37 close — net +51 from S36's 1200. Breakdown: 15 new unit tests at commit 2 (11 auth + 2 middleware + 2 inference dependency) + 16 at commit 3 (7 DTO + 9 query parser) + 7 at commit 4 (audit-side error handlers) + 10 at commit 5 (audit route integration) + 6 at commit 6 (audit HTTP isolation contract) = 54 net new at S37. One intermittent test-ordering flake (`tests/unit/contexts/audit/adapters/test_postgres_audit.py::test_empty_sentinel_writes_to_control_plane`, pre-existing since S12) surfaced under random ordering during a verification run but passes under sequential ordering; recorded again as the same flake S36 close noted. 25/25 import-linter contracts kept after the audit router's transitive imports landed. AST enforcement passes.

- Enforcement layer: No new `import-linter` contract additions at S37 (the audit context's port + ports/ layer landed at S36; the new files at S37 are app-layer at `apps/api/routers/` which fall under the existing `fastapi confined to apps/` contract). The tenant-isolation contract harness extended from 27 scenarios at S36 close to 33 scenarios at S37 close (+6 audit HTTP scenarios). The new scenarios cover the four-cell cross-product of (tenant principal × platform principal) × (tenant route × platform route) plus unauthenticated request plus filter-validation 400. The discriminator branch in `get_tenant_context` and `get_platform_operator_principal` is enforced by integration tests (commit 5) AND through the full auth stack by contract tests (commit 6); both layers' tests must pass before the 403 path is considered enforced.

- methodology: Pre-write reconciliation fired its sixteenth instance at S37 session-open. Three brief-specified reconciliation passes (D23 backend as-built, run_history HTTP route precedent, get_tenant_context dependency confirmation) plus the implicit fourth pass (module naming, error registration shape) produced four findings — one structural (finding 1: existing `Principal.roles` + `OPERATOR_ROLE` contradicts D103 alternative (b) framing; user-question resolved to revise the alternatives-considered framing rather than the choice; the discriminator stands as drafted) and three mechanical (finding 2: DTO and query-parser module naming `_audit_dto.py`/`_audit_query.py` per precedent; finding 3: platform-operator dev-mint helper omits `OPERATOR_ROLE` by default per conceptual-separation; finding 4: parallel `register_audit_error_handlers` function per additive composition discipline). Plus four mechanical absorptions absorbed without user-question (error handler at `_errors.py` not `errors.py`; "eleven-path" referring to verification scenarios not eleven error codes; auth tests at `tests/unit/padhanam/security/` not `tests/unit/apps/api/auth/`; `AuditQueryRoutingError` already exists at the S36 port with the 400 translation already committed). Find-rate distribution at S37: one structural-honesty finding at session-open requiring user-question resolution + three mechanical findings + four mechanical absorptions. The structural finding's load-bearing quality: D103's drafted alternative (b) framed role-list as hypothetical Phase 2 future when in fact it is production code today; without pre-write reconciliation, the charter entry would have shipped with the inaccuracy. The trajectory continues to hold: pre-write reconciliation catches cross-document architectural inconsistencies that prose review at prompt-drafting time misses, and the structural-honesty finding's relevance grows as the codebase accumulates ground truth that briefs may inherit at the wrong altitude. Methodology-document promotion candidate at the Phase 1 close audit window.

- methodology (second): Reflection prompt 1 — the discriminator-field claim shape held in practice during commit 2. The choice was structurally valuable rather than ceremony. The conditional validation on `tenant_id` fell out cleanly at the `verify_credential` chokepoint as a single branch (required-iff-TENANT, prohibited-iff-PLATFORM_OPERATOR), with no friction in test fixtures because the default value `PrincipalType.TENANT` on the `Principal` dataclass kept every existing constructor call site working unchanged. The counterfactual — a role-list approach where `tenant_id` is conditionally required based on `"platform_operator" in roles` — would have forced the decode chokepoint to inspect string content (membership in a frozenset) before deciding whether `tenant_id` is required, which commingles permission evaluation with token-shape validation. The discriminator anchors a strictly categorical concern (what shape of claim payload is valid) at the smallest, most-trusted piece of code; roles continues to anchor a permission concern at higher layers (policy.check, route-handler gating). The two layers serve distinct purposes and coexist cleanly. The pre-write reconciliation finding shifted what D103's alternatives-considered section said, not what the implementation did.

- methodology (third): Reflection prompt 2 — the two-tree route shape during commit 5. The declarative `Depends(get_platform_operator_principal)` vs `Depends(get_tenant_context)` distinction made the authorization decision visible at route signature exactly as D103's visibility-grounds reasoning argued. Each route's signature names the required principal type as part of the FastAPI dependency declaration; an inspector reading `apps/api/routers/audit.py` sees four routes split across two routers, each declaring its required principal-type dependency in line 1 of its signature. The counterfactual — a single route tree with route-body conditional logic on `principal.principal_type` — would have hidden the auth decision inside the body, making the route tree visually homogeneous (all four routes look the same at signature) and forcing the auditor to read the body to discover the gate. The two-tree shape also mirrors the two-destination shape on the read port from S36; the HTTP layer's structure echoes the port layer's structure, and that symmetry is methodology-worth (visible-at-signature gates pair with visible-at-port-method-entry destination invariants). Reflection prompt 3 — the 403 path extension during commit 4. The `principal_type_mismatch` path collapsed cleanly into the existing error response handler with no structural drift. The handler shape mirrored `_handle_bound_tenant_mismatch` (request-state principal access + security-event emission + ErrorResponse body) and reused the typed-exception-to-handler-to-JSON pattern. The parallel `register_audit_error_handlers` per finding 4 worked cleanly without restructuring the existing run-history registration; the additive shape preserves the option to consolidate at a future cleanup moment without forcing the cleanup now. The 403 path's security-event metadata (required + actual principal type, attempted route, tenant_id-when-tenant-typed) captures the forensic signal a SIEM downstream would need to distinguish a tenant principal attempting platform access from a platform-operator principal attempting tenant access from a fabricated token attempting either; the AUTHZ_DENIAL category is the right semantic.

- methodology (fourth): Reflection prompt 4 — the consumer-port-plus-wiring-adapter pattern at S37. S37 reinforced the pattern at the HTTP-as-consumer altitude that S29b (SSE transport) and S34 (run-history HTTP routes) already covered, with one new variant worth recording: the two-tree route shape introduces "two consumers of the same port at different auth gates." The `AuditEventReader` Protocol port from S36 is the single port; the two FastAPI routers consume it through the same `get_audit_event_reader` dependency but declare different auth gates at route signature. The pattern's mechanism stayed identical (consumer-defined port surface, no producer-aggregate import, wiring impedance at composition layer); the new observation is that auth-gate-difference does not require port-shape-difference. A future variant — three or more route trees per principal type — would still consume the same port. The pattern is recurrence-test positive at six reinforcements across the P8 → P10 window and continues to do load-bearing work at every session that wires a new consumer surface; methodology-document promotion candidate at the Phase 1 close audit.

- methodology (fifth): Commit-ordering drift surfaced at S37 — brief forecast had commit 3 (routes), 4 (DTOs), 5 (error handler); landed order was commit 3 (DTOs + query parser), 4 (error handler), 5 (routes) because the route layer imports both the DTOs and the registered error handlers. The drift was caught at commit-3 write-time when the route module's import-statement scan surfaced the dependency direction; resolution was a same-session re-ordering with explanatory notes on each commit message. The methodology observation: brief commit-ordering is a forecast against an assumed dependency direction; write-time module-import scanning surfaces the actual direction and may force re-ordering. Pre-write reconciliation does not catch this class because the dependency surfaces only after the module is being drafted; this is a new failure-mode for the pre-write-reconciliation pattern. The Phase 1 close audit could record commit-ordering drift as a complement to the structural-drift category.

```
metrics:
  classification: new_work
  brief_started: 2026-05-14
  session_started: 2026-05-14
  session_closed: 2026-05-14
  merged: 2026-05-14
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: updated
  corrects:
  corrected_by:
```

## P10 — S38 — Ingestion management HTTP API + P10 close
roles: architect, engineer, technical writer
mode: build

- Produced: Eight commits closed S38 and closed P10. Commit 1 (`docs(charter)`, 00e1385) landed D104 in `charter/decisions.md` anchoring two architectural shapes: the auth error handler relocation from `register_audit_error_handlers` (where it landed at S37 per finding 4 resolution) to a new `register_auth_error_handlers` module at `apps/api/_auth_errors.py`; and the HTTP transport for ingestion management with three read routes consuming the ingestion application layer directly via Path A (extend `SourceRepositoryPort` with `list_sources` plus add a `list_sources` application use case rather than stand up a separate consumer-defined reader port). D104 carries seven alternatives (a-g) plus the Path-A-vs-Path-B fourth reasoning paragraph the operator's user-question resolution added. Brief preserved at `briefs/p10/s38.md` with Appendix D capturing the three findings + one side observation. Commit 2 (`refactor(p10/s38)`, 34a989f) extracted the auth-cross-cutting machinery: new module `apps/api/_auth_errors.py` with `PrincipalTypeMismatchError`, `_handle_principal_type_mismatch`, `register_auth_error_handlers`; removed from `apps/api/_errors.py`; updated two production import sites (`apps/api/middleware.py:161` and `apps/api/routers/inference.py:108`); wired `register_auth_error_handlers(app)` at `apps/api/main.py:293` ahead of `register_audit_error_handlers` per the cross-cutting-wires-first ordering; migrated three test-file imports; extracted three principal-type test cases from `tests/unit/apps/api/test_errors.py` to a new `tests/unit/apps/api/test_auth_errors.py` plus added two new registration-shape tests asserting that `register_auth_error_handlers` wires the handler and `register_audit_error_handlers` no longer does. 21 auth/error unit tests pass post-relocation. Commit 3 (`feat(p10/s38)`, 4a5a32b) landed the Path A substrate: new domain value objects at `contexts/ingestion/domain/source_list.py` (`SourceListCursor`, `SourceListPage`, `MalformedCursorError`, `PAGE_SIZE_CEILING=50`); cursor codec at `contexts/ingestion/application/cursor.py` mirroring the audit base64-of-JSON shape; `SourceRepositoryPort.list_sources(tenant_id, cursor, page_size)` returning `SourceListPage`; `PostgresSourceRepository.list_sources` with cursor pagination on `(created_at DESC, id DESC)` via tuple comparison with `pg.UUID` cast on `id` per the S33 finding; `list_sources` application use case as a signature-thin wrapper; HTTP DTOs at `apps/api/routers/_ingestion_dto.py` (`SourceDTO` mirroring `Source` 1:1 minus `raw_content`; `SourceListPageDTO` envelope; `SourceStatusDTO` projection; two conversion helpers); query parser at `apps/api/routers/_ingestion_query.py` exposing `cursor` and `page_size` with FastAPI's `ge`/`le` validators. 31 new unit tests pass. Commit 4 (`feat(p10/s38)`, 7980145) landed the HTTP routes at `apps/api/routers/ingestion.py` (three routes: list, get-by-id, status), the typed `IngestionSourceNotFoundError` exception + handler + parallel `register_ingestion_error_handlers` function at `apps/api/_errors.py`, the routing wrapper `TenantRoutingSourceRepository` + `build_source_repository` factory at `apps/api/_agent_runtime_wiring.py`, and the composition wiring at `apps/api/main.py` (new `source_repository` field on `AppCompositions`, attachment on `app.state`, router include). The routing wrapper mirrors `TenantRoutingRetrievalClient`'s shape: resolves the per-tenant session factory at call time via the shared `TenantSessionFactoryCache`, constructs a fresh `PostgresSourceRepository` per call, exposes only the two read methods the HTTP routes need. Commit 5 (`test(p10/s38)`, 2bfd58d) extended the D24 tenant-isolation harness from 33 scenarios at S37 close to 39 scenarios via six new ingestion HTTP cases at `tests/contract/tenant_isolation/test_ingestion_http_isolation.py`: cross-tenant get returns 404 no security event; cross-tenant status returns 404 same shape; list returns own-tenant only; platform-operator on ingestion returns 403 + AUTHZ_DENIAL via the relocated `_auth_errors.py` handler; unauthenticated returns 401; malformed cursor returns 400. Commit 6 (`docs(p10/s38)`, 9904986) landed the P10 close demo at `scripts/smoke_p10_s38.py` + `docs/smoke/p10_s38_close_demo.md`; padhanam-api image rebuilt with the S38 code, compose.yaml line 380 digest pin updated to `sha256:6479c9d0ed541b51471d81deb8624e01cf2b86a58ccea6dd516224cae14dc358`; the smoke walks eleven verification paths end-to-end against tenant_a's 25-row chain (the +2 over S37's 23 came from the smoke's own per-tenant audit reads firing `tenant.audit.test_event` audit writes per the S11/D26 audit-read-as-state-change discipline) and a seeded one-event control-plane chain and one seeded tenant_a source; three AUTHZ_DENIAL security events captured in `logs/security.jsonl` from the three cross-destination 403 paths (tenant→platform-audit, operator→tenant-audit, operator→ingestion); the auth handler relocation from commit 2 fires identically across all three route trees, confirming the cross-cutting extraction holds at runtime. Commit 7 (`docs(p10/s38)`, eaa934b) landed the P10 retrospective addendum to `charter/packages/p10-epic.md` mirroring the P9 epic's S35 close addendum shape with six sections (three sessions shipped, three D-entries landed, delta from framing, substrate as Phase 2 UX consumer surface, Phase 1 substrate-completeness observations, methodology candidate reinforcements, plus the new architectural-extension-rather-than-greenfield observation specific to P10's mode); transitioned `charter/current-package.md` to "P10 closed; retrieval-evaluation design session next." with the original P10 open text preserved verbatim under "P10 — final history (preserved below for traceability)" per the append-only discipline at D43. Commit 8 (this entry) appends the S38 session-log entry.

- Decisions: One new D-entry at S38 — D104 (auth error handler cross-cutting registration via `register_auth_error_handlers` at `apps/api/_auth_errors.py`; HTTP transport for ingestion management with three read routes consuming the ingestion application layer directly via Path A). Seven alternatives considered (a-g): (a) keep `PrincipalTypeMismatchError` handler inside audit module rejected because the second-router threshold fired at S38; (b) duplicate handler in both audit and ingestion modules rejected as linear duplication; (c) Path B — consumer-defined `IngestionReader` port mirroring `AuditEventReader` from S36 — rejected on duplication-versus-refactor grounds (`IngestionReader` and the existing `SourceRepositoryPort` would both need `get_source`, forcing real duplication across two ports or an unbounded refactor of `SourceRepositoryPort` touching existing consumers at S25's cross-context `SourceLookup` and three worker stages); (d) upload-over-HTTP at S38 rejected on YAGNI; (e) reindex-over-HTTP rejected for the same reason; (f) defer ingestion management to Phase 2 rejected because P10 framing absorbed it; (g) add `list_sources` to the port but skip a separate application use case (HTTP route calls port directly) rejected on consistency grounds — the existing read access pattern at `get_source` flows through an application use case so list follows the same shape. Kano: must-have on auth error handler relocation (D103 alternative (i) trigger fired); must-have on three read endpoints (P10 absorbed the P6 carryover at framing); performance on Path A over Path B (reversible to a separate reader port if reconciliation in a future session surfaces shape mismatch the existing port cannot accommodate); delighter deferred on upload-over-HTTP and reindex.

- Tests: All apps/api + ingestion unit tests pass plus the six new contract harness scenarios. New tests at S38: 21 auth/error unit tests at commit 2 (was 18 pre-relocation; +3 from the new registration-shape tests at `test_auth_errors.py`) + 31 new unit tests at commit 3 (14 source_list + 3 list_sources + 9 ingestion_dto + 4 ingestion_query) + 6 contract harness scenarios at commit 5 = 60 net new tests at S38. 207 unit tests pass across `tests/unit/apps/api` and `tests/unit/contexts/ingestion`; 101 contract tests pass across `tests/contract/tenant_isolation`. 25/25 import-linter contracts kept; the contexts.ingestion package hexagonal-layer ordering preserved (domain → ports → application → adapters). AST enforcement passes.

- Enforcement layer: No new `import-linter` contract additions at S38. The tenant-isolation contract harness extended from 33 scenarios at S37 close to 39 at S38 close (+6 ingestion HTTP scenarios). The discriminator branch in `get_tenant_context` is now enforced by integration tests across THREE route trees (run-history HTTP from S34, audit HTTP from S37 commit 6, ingestion HTTP from S38 commit 5) plus the cross-cutting `register_auth_error_handlers` registration-shape tests at `test_auth_errors.py`. The S38 commit 2 relocation surface is fully enforced: a test asserts `register_auth_error_handlers` wires `PrincipalTypeMismatchError`, a test asserts `register_audit_error_handlers` no longer does, and a contract scenario across each of three route trees verifies the runtime 403 path fires identically through the relocated handler.

- methodology: Pre-write reconciliation fired its seventeenth instance at S38 session-open. Three brief-specified reconciliation passes (ingestion application use-case shape, auth error handler current registration, HTTP-route precedent across run_history.py and audit.py) plus the implicit fourth pass (file path naming and deferred-decisions entry expectation) produced three findings plus one side observation — one structural (finding 1: `list_sources` exists neither as a use case nor as a port method; the brief's "direct invocation of existing application use cases" path has a gap; user-question resolved to Path A — extend the existing port plus add the use case — over Path B's IngestionReader port mirroring S36's audit pattern); one mechanical (finding 2: brief named `apps/api/_audit_errors.py` but the actual built state co-locates all error machinery in a single `apps/api/_errors.py` module; the relocation extracts to `_auth_errors.py` as the brief intends); one mechanical (finding 3: HTTP-route pattern stable across audit.py and run_history.py; pattern can be followed directly); plus one side observation (the brief's referenced `PrincipalTypeMismatchError` handler-location deferred-decisions entry does not actually exist in `charter/deferred-decisions.md`; the architectural seed is at D103 alternative (i) "preserves the option to refactor to a generic register_error_handlers at a future cleanup moment"; the relocation proceeded per the architectural intent the brief described). Find-rate distribution at S38: one structural-honesty finding at session-open requiring user-question resolution + two mechanical findings + one mechanical absorption. The structural finding produced a substantive D104 revision: alternative (c) reasoning shifted from "incidental-scope grounds" to "duplication-versus-refactor grounds" naming the duplication problem specifically (both ports would need `get_source`, creating either real duplication across two ports or an unbounded refactor of `SourceRepositoryPort` touching existing consumers). The trajectory holds: pre-write reconciliation catches brief-versus-built-state mismatches at substrate-extension moments. The methodology candidate at the Phase 1 close audit gains one more datapoint: the pattern's altitude continues to track the codebase's maturity arc — early packages established context boundaries (architectural-drift detection), P10 extends them (brief-versus-built-state detection), Phase 2 will exercise them.

- methodology (second): Reflection prompt 1 — the auth error handler relocation in commit 2 paid for itself. The cross-cutting module shape is cleaner than the audit-coupled shape at three observable points. First, the `apps/api/_errors.py` docstring no longer mentions auth-cross-cutting concerns; the audit-specific surface there is conceptually coherent (audit event lookup, audit query routing, audit cursor decode). Second, the `apps/api/_auth_errors.py` module is six functions tight (class + handler + register + helper) and reads as a single concern, with the docstring naming the cross-cutting role explicitly. Third, the test surface mirrors the module boundary — `test_auth_errors.py` carries the auth-cross-cutting tests including two new registration-shape tests asserting the relocation contract; `test_errors.py` retains the audit-specific tests. The deferred-decisions activation trigger fired in spirit if not in letter — D103 alternative (i)'s "future cleanup moment" language anchored the architectural intent the relocation now satisfies; the absent deferred-decisions entry is a brief-versus-built-state observation rather than a missed trigger. The wiring at composition root ordered cross-cutting-first (auth) before per-router-specific (audit), which is the right semantic and produced no friction at commit time.

- methodology (third): Reflection prompt 2 — the ingestion management API at Path A. Reconciliation surfaced the real structural gap (list_sources missing entirely from both port and application layers) but Path A's surface stayed bounded once the operator selected it: one new method on the existing port + one new application use case + one new adapter method + the HTTP layer + the routing wrapper. The Path-B counterfactual (consumer-defined IngestionReader port mirroring AuditEventReader) would have required either real method-shape duplication of `get_source` across two ports on the same context with identical tenant_id scoping semantics OR an unbounded refactor of `SourceRepositoryPort` touching three worker stages plus the cross-context `SourceLookup` adapter from S25. The operator's reasoning revision in D104 alternative (c) named this specifically as "duplication-versus-refactor" rather than the brief's "incidental-scope" framing, and that revised framing is a cleaner architectural argument: incidental scope is a quantity; the duplication-versus-refactor binary is a quality. The methodology observation: Kano-must-have status held on the three read endpoints (P10 absorbed the P6 carryover at framing); Kano-performance status on the path choice (reversible to a reader port if a future session needs UX-shaped semantics the application use cases cannot provide); the reversibility is real, not nominal, because Path A's surface area is the smaller, structurally-honest minimum and a future port refactor would land into a context whose write paths are unchanged. The status endpoint as thin DTO projection (no separate `get_source_status` use case) also held cleanly — the route reuses the existing `get_source` use case and projects the state-relevant fields; the HTTP boundary owns the projection. The smoke at commit 6 confirmed the projection at scenario 6.

- methodology (fourth): Reflection prompt 3 — the P10 close demonstration at commit 6 surfaced three observations the per-session tests had not. First, the chain integrity verifier returns `partial` rather than `verified` on a single-row page — this is the correct page-granularity-verifier shape per D102's three-state surface, but the per-session tests at S36 had not exercised the one-row case explicitly because the fake data they used always carried enough rows for a verifiable chain. The smoke surfaced the structurally-honest "I cannot verify two-row chain links on a one-row page" answer; the observation is recorded for the Phase 2 UX audit-viewer designer so the three-state surface is visible from day one. Second, the per-tenant chain count went from 23 events at session-open to 25 at smoke close — the +2 delta came from the smoke's own per-tenant `get_audit_event` calls firing `tenant.audit.test_event` audit writes per the S11/D26 audit-read-as-state-change discipline. The reading-amplifies-writes shape is a Phase 1 commitment and worth verifying continues to fire end-to-end as the smoke proves. Third, the cross-cutting auth handler from commit 2 fired identically across all three route trees (audit-per-tenant, audit-platform, ingestion) in the security event log — three AUTHZ_DENIAL events with the expected `action`, `metadata`, and `principal_ref` shape, distinguishable only by the `tenant_id` field (null for platform-operator-typed, populated for tenant-typed). The relocation's correctness is observable at the security event capture, not just the response status code. No gaps surfaced that need new deferred-decisions entries; the chain-integrity-partial observation is a design-surface flag for Phase 2 rather than a substrate gap.

- methodology (fifth): Reflection prompt 4 — Phase 1 close trajectory. P10 closes with three packages remaining in Phase 1 (the retrieval-evaluation design session in strategic-mode Claude.ai, then P11 recommendation engine, then P12 Phase 1 close audit). What P10 close tells us about Phase 1 close readiness: every substrate-completeness claim from the bet that P10 was responsible for is now honoured by working artefacts. The audit-evidence claim per D92 ("hash-chained audit on a per-tenant basis with a working read substrate verifying chain integrity") is honoured end-to-end: the per-tenant chain reads via HTTP carry `chain_integrity=verified` against the live stack, the control-plane chain reads work under platform-operator gating, and the cross-destination isolation paths fire as expected with security events. The ingestion-observability claim per D93 ("Phase 2 UX consumes the ingestion management API directly") is honoured: the three read endpoints are operational and the cross-tenant + malformed-input failure modes all behave as documented. What remains to be proved at P11 and P12: the optimisation-layer claim (P11 — recommendations distinguishing retrieval failures from reasoning failures) depends on the queued retrieval-evaluation design session; the trace-id propagation gap from P9 close stays open and needs revisit before Phase 1 close audit can pass; the audit-context port-location asymmetry from S36 inherits and will activate at the next port added to the audit context. The architectural-extension-rather-than-greenfield characteristic specific to P10's mode (every session extended an existing bounded context rather than shaping a new one) is itself signal: it suggests Phase 1 has reached the maturity arc where new substrate fits into existing shapes rather than requiring fresh ones, and Phase 2 UX work should find the consumer surfaces already in place rather than needing to bargain with substrate authors.

- methodology (sixth): Brief-versus-built-state mismatches as a sub-class of pre-write reconciliation findings. S38 surfaced two: finding 2 (file path `_audit_errors.py` named in the brief, actual `_errors.py`) and the side observation (PrincipalTypeMismatchError handler-location deferred-decisions entry referenced by the brief, does not exist). Neither required user-question resolution because the brief's intent was clear and the mechanical translation to actual paths was bounded. Both worth recording as a sub-observation on the pre-write reconciliation methodology candidate: at substrate-extension moments the brief decays against the actual built state at the rate the codebase moves between brief authoring and brief execution. The S37 brief at the same authoring window had similar drift (the S37 close noted "error handler at `_errors.py` not `errors.py`" as a finding-4 absorption). This is the second consecutive session where the brief-versus-built-state drift was named explicitly at the session log. The Phase 1 close audit could record the drift rate (findings per session) as a methodology signal: how rapidly briefs decay against built state tracks how much the codebase changed between framing and execution.

- methodology (seventh): Wiring-as-routing-wrapper sub-variant of the consumer-port-plus-wiring-adapter pattern. The `TenantRoutingSourceRepository` at `apps/api/_agent_runtime_wiring.py` reinforced the pattern at a new altitude: a routing wrapper rather than a port-defined-elsewhere-wired-here adapter. The wrapper holds the per-tenant routing impedance (resolves session factories at call time via the shared cache), exposes the same port shape the HTTP layer consumes, and matches the pattern previously seen at `TenantRoutingRetrievalClient` (S30b) and at the audit/run-history reader factories (S33/S36/S37). The methodology observation: the pattern operates at three sub-shapes at apps/api: (a) consumer-defined port elsewhere wired at composition root (audit reader, run-history reader); (b) consumer-aggregate-shape port owned by the consumer context with cross-context adapter (run-history writer, S25 SourceLookup); (c) routing-wrapper at composition root resolving per-tenant impedance (S30b retrieval, S38 source repository). All three sub-shapes share the mechanism (consumer sees a single clean port, impedance lives at the composition layer) but differ in where the port is defined and what kind of impedance the wiring resolves. Six reinforcements at S38 close across these three sub-shapes; methodology-document promotion candidate at the Phase 1 close audit gains one more datapoint with the routing-wrapper sub-shape named explicitly.

```
metrics:
  classification: new_work
  brief_started: 2026-05-14
  session_started: 2026-05-14
  session_closed: 2026-05-14
  merged: 2026-05-14
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: updated
  corrects:
  corrected_by:
```

## S38a — Post-P10 hygiene: TRUNCATE CASCADE for ingestion integration fixtures
roles: engineer, technical writer
mode: build (post-package hygiene)

- Produced: Two commits closed S38a. Commit 1 (`fix(s38a)`, 051f09b) applied the explicit-list TRUNCATE shape (`TRUNCATE TABLE chunks, sources, run_chunk_citations;`) at three sites in `tests/integration/contexts/ingestion/`: `test_ingest_e2e.py:112` (`_truncate_ingestion_tables` helper), `test_extract_e2e.py:105` (`_truncate_tenant_a` helper), `test_retrieval_e2e.py:110` (`_truncate_tenant` helper). Inline comment at each site documents the `run_chunk_citations.chunk_id` FK reference + TRUNCATE FK-action-trigger bypass + explicit-list-over-CASCADE intent reasoning + cross-reference to S35b's a555902 precedent at `test_concurrent_workers.py:191-197`. Verified by running the three integration test files end-to-end against the live stack with the `live_llm` marker enabled: 16/16 tests pass in 558s (was 8 ERRORs + 2 FAILUREs + 6 passes pre-fix). Commit 2 (this commit) appended the S38a note to `charter/current-package.md` under the existing "P10 closed; retrieval-evaluation design session next" section, and landed this session log entry.

- Decisions: No new D-entry at S38a. The fix shape mirrors S35b's existing precedent at D95 / commit a555902; no new architectural commitment lands. S38a is hygiene applying a known fix shape to additional sites surfaced by reconciliation.

- Tests: 16 previously-affected integration tests now pass; 8 ERRORs and 2 FAILUREs at `tests/integration/contexts/ingestion/` cleared. Full integration tier under the `live_llm` marker: 16 passed across the three named files. Unit-test surface, contract-test surface, import-linter contracts (25/25), and AST enforcement unchanged at S38a since the work touches only test fixture helpers.

- Enforcement layer: No `import-linter` contract changes at S38a. No tenant-isolation contract harness changes; the harness count remains at 39 scenarios from S38. The fix is enforced by the integration tier — `live_llm` runs catch any future re-introduction of the broken TRUNCATE pattern at any of the three sites.

- methodology: Pre-write reconciliation fired its eighteenth instance at S38a session-open. The three brief-specified passes (fixture as-built at `test_ingest_e2e.py`, S35b fix precedent shape, sweep for latent occurrences across the test suite) produced one structural finding — the sweep found **three** unfixed sites, not one. The brief's framing of "1 ERROR-batch + 2 downstream FAILUREs" predicted a single root cause with two cascading effects; the sweep contradicted that prediction. All three sites carry independent broken fixture helpers and would each fail independently of the others. The apparent grouping at session-open into "primary + two downstream" was an artefact of test ordering (whichever fixture fired first against an empty-chunks state surfaced the error message that the operator-visible failure output captured), not failure causality. User-question resolved with operator confirming the three-site scope; the brief's framing was acknowledged as incorrect at brief-drafting time. The finding is a different kind from S38's brief-versus-built-state mismatches (file path naming) because the brief's incorrect prediction here was about causal structure of the failures rather than about file/path facts. Worth distinguishing in the pre-write reconciliation methodology candidate at the Phase 1 close audit: brief-versus-built-state drift at file-path altitude vs brief-versus-failure-mode drift at causal-structure altitude. Both are sub-classes of the same methodology pattern; the causal-structure variant is rarer (one instance at S38a) and harder to predict against, which is precisely the value the sweep step adds.

- methodology (second): Reflection prompt 1 — why S35b missed three specific sites. The substantive answer is about S35b's scoping decision at the time, generalised from the brief's "why specifically test_ingest_e2e.py" to "why specifically three sites." Looking at the S35b commit a555902 message: S35b named the two fixed sites by their failing test names (`test_two_concurrent_workers_no_double_processing_full_pipeline`, `test_full_role_aware_clone_and_edit_flow_against_live_stack`). The framing was visible-failures-driven: which tests are erroring against the live stack at S35b session-open? Answer: those two specifically because those were the tests whose execution had populated chunks via the retrieval tool path at S35a's trace_id propagation demo. The other three sites (`test_ingest_e2e.py`, `test_extract_e2e.py`, `test_retrieval_e2e.py`) carry the same broken fixture pattern but were not in the visible-failure-list at S35b session-open because their own runs did not happen to populate chunks at the right window. S35b's scope was specifically narrow on the named failures, not systemically narrow on the FK class. The miss is a structural finding worth recording: scope-by-visible-failure is an under-detection mode for fixture-class defects because fixture defects activate only when prior state happens to be load-bearing, and the load-bearing state is intermittent across test ordering. The fix is the sweep step the S38a brief introduced — at hygiene-session boundaries, grep for the failure pattern (here: `TRUNCATE.*chunks` without `run_chunk_citations`) and surface all sites, not just the visibly-failing ones.

- methodology (third): Reflection prompt 2 — the reconciliation sweep result. The sweep across `tests/**.py` for `TRUNCATE.*chunks\|TRUNCATE TABLE chunks\|TRUNCATE.*sources` returned six sites total. Three carry the fixed shape (`TRUNCATE TABLE chunks, sources, run_chunk_citations;` — `test_concurrent_workers.py:199`, `test_ingestion_isolation.py:436`, `test_create_from_methodology_flow.py:153`, all from S35b). Three carry the unfixed shape (`TRUNCATE TABLE chunks, sources;` — the three files S38a fixed). The sweep did not surface additional surfaces beyond what the brief named, but the structural correction it produced is more valuable than a count of additional sites would have been: the brief predicted a 1+2 causal structure, the sweep produced a 3-independent-sites structure. The richer answer than "more sites exist" is that the brief's causal-structure assumption was wrong. This is the substantive payoff of the sweep step's first deliberate instance. The sweep was thorough: it covered `tests/integration/`, `tests/contract/`, `tests/e2e/`, and `tests/unit/` (only the integration and contract paths had matches; unit had no matches and e2e had one match already in the fixed shape). The grep pattern was conservative — any TRUNCATE mentioning chunks or sources without further filtering on the column-list shape — so false-positives were possible but did not materialise; the six matches were all the intended pattern.

- methodology (fourth): Reflection prompt 3 — should post-package hygiene sessions include a proactive sweep step as standard practice. S38a is one instance, not a settled pattern, but the instance is strong evidence. The sweep step's payoff at S38a was: (a) caught the brief's incorrect 1+2 causal-structure framing; (b) produced a clean three-site fix in one commit rather than a one-site fix followed by a discovery commit; (c) provided structural understanding that the FK class is hygiene-bounded at three additional sites with no further latent surfaces. The cost: ten minutes of greppy work plus the user-question round-trip to confirm scope. The cost-benefit favours the practice unambiguously at S38a. Whether to promote: one instance is observation, not pattern. The next post-package hygiene session (whenever it lands — likely S39 or a P12-close hygiene tail) is the next test. If that session's sweep step produces another structural correction or a clean confirmation, that becomes the second instance and the methodology promotion candidate earns enough evidence to lift to a methodology-document entry at the Phase 1 close audit. For now, S38a records the observation with the strength of the first-instance payoff named explicitly. The S38a note in `charter/current-package.md` captures it; the session log entry here captures it; both surfaces feed the Phase 1 close audit's methodology-evidence corpus.

- methodology (fifth): Reflection prompt 4 — the pre-write reconciliation count. S38a is the eighteenth instance of the practice. The meta-reinforcement worth recording: the sweep step that the S38a brief introduced is itself an output of pre-write reconciliation operating at a higher altitude — the brief author looked at the S35b commit history, recognised that the three named failures at S38 close fit the same FK pattern S35b fixed, and asked "have we covered every site." The brief-side reconciliation produced the sweep prompt. Then the session-side reconciliation executed the sweep and produced the structural correction. Two levels of reconciliation acting in series, each catching gaps the other would have missed alone. The eighteenth instance shows the pattern operating at brief-authoring altitude (the brief asks for a sweep) AND build-session altitude (the build runs the sweep). The Phase 1 close audit could record this as a meta-observation: when reconciliation operates at both altitudes serially, the find-rate improves over single-altitude operation. This is a refinement worth tracking but not a new pattern — same mechanism, longer-running across the brief-then-build seam.

- methodology (sixth): The brief's framing-error correction (1+2 causal structure → 3 independent sites) is itself a useful contribution to the Phase 1 close audit's pre-write reconciliation methodology evidence. Most prior pre-write reconciliation findings in the P8 → P10 window were about file paths or interface shapes (S37's `_audit_errors.py` vs `_errors.py`, S38's `_audit_errors.py` vs `_errors.py` again, S38's `deferred-decisions.md` entry that did not exist). Those are mechanical-translation findings. The S38a finding is at a different altitude — about how the brief reasoned about the failure mode itself. The methodology candidate gains another sub-class: brief-versus-actual-causal-structure mismatches alongside brief-versus-actual-file-paths and brief-versus-actual-failure-modes. Three sub-classes named so far at the Phase 1 close audit window; whether they consolidate into a single methodology principle or stay distinguished as sub-classes is a question for the audit itself.

```
metrics:
  classification: carryover
  brief_started: 2026-05-14
  session_started: 2026-05-14
  session_closed: 2026-05-14
  merged: 2026-05-14
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: updated
  corrects: a555902
  corrected_by:
```

