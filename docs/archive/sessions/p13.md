# Archive — P13 sessions

P13 (Phase 2-A Wave 1 foundational substrate: portfolio/intake/messaging/manual-entry/intent-class-eval), framing and hygiene, plus the post-P13 hygiene sessions S49/S50. Entries appear in source-file order (the descending S48b->S43 block precedes the ascending S49/S50). Archived at S102m under the retention rule (D107 per-package-on-close).

---

## S48b — Intent-classification evaluation substrate
roles: analyst, PM, architect, engineer, technical writer
mode: build session, executing briefs/p13/s48b.md

S48b closed Thread 2 of the parallel post-S47 forward path with the intent-classification evaluation substrate at `contexts/intent_classification_evaluation/` per D137. The substrate answers component-quality questions ("does model X classify Padhanam's intent surface reliably") in minutes against a fixed reference set rather than through multi-minute integration smokes. The S46 (qwen2.5:7b intent-extraction blind-spot) and S47 (qwen2.5:14b hardware-viability) findings were both surfaced by integration smokes asked to do work the architecture is structurally designed to split; S48b closes that methodology gap by building the dedicated evaluation surface the port-and-adapter shape already supported. Six commits land Option B (slim load-bearing version): charter (D137 plus architecture plus three captures methodology entries plus brief preservation); shared_kernel intent-classification primitive extraction plus single-source-of-truth structural test; substrate code (domain plus application plus ports plus Postgres plus Alembic 0022); CLI sub-app plus YAML gold-set fixture plus loader; tenant-isolation harness plus new layers-intent-class-eval import-linter contract; live-stack exercise verifying the substrate against tenant_a with two models. The full P11 mirror's revision-lifecycle gold sets and cross-run-comparison CLI subcommand defer to Phase 2-B+ at named activation triggers per D137 alternatives (c) and (d).

- Produced: Six commits. (1) `docs(charter): land P13 S48b intent-classification evaluation substrate (D137)` (b2e4970) covering D137 at decisions.md, the architecture.md sub-section, three captures methodology candidate entries (component-quality vs integration smoke promotion-ready at two-instance evidence; scope-discipline-at-brief-authoring first-instance; structural-test single-source-of-truth second-instance), the placeholder S48b in-flight marker, and the brief preservation at briefs/p13/s48b.md. (2) `feat(p13/s48b): extract intent-classification prompt and schema to shared_kernel` (91511dd) moving INTENT_EXTRACTION_SCHEMA + EXTRACTION_PREAMBLE + build_extraction_prompt to `shared_kernel/intent_classification.py`, refactoring the cell to consume the shared primitive, plus a four-scenario contract harness at `tests/contract/intent_classification/test_single_source_of_truth.py` asserting Python-identity (`is`) across consumers. (3) `feat(p13/s48b): intent-classification evaluation substrate (domain, ports, app, Postgres, Alembic 0022)` (cfd3450) landing 24 new files covering domain (gold_set, evaluation_run, evaluation_result, metrics), application (3 use cases plus audit_events), ports (3 Protocols), Postgres adapters (repository + reader + _tables) with bound-tenant defence-in-depth, Alembic migration 0022_intent_class_eval_substrate creating three per-tenant tables, and 20 domain-layer unit tests. (4) `feat(p13/s48b): CLI sub-app, YAML gold-set fixture, and fixture loader` (7aa8d8f) landing the typer sub-app at `apps/cli/_intent_classification_eval.py` with `eval start/get/list` subcommands registered under `padhanam intent-classification-eval`, plus the 40-entry gold-set fixture at `tests/fixtures/intent_classification/gold_set.yaml` (12 CreateCase, 12 AddDataPoint including the 4 S46 blind-spot phrasings, 8 ReviseDataPoint, 8 Unclear), plus the YamlGoldSetReader adapter. (5) `test(p13/s48b): tenant_isolation harness + layers-intent-class-eval import-linter contract` (8749442) landing six isolation scenarios (3 structural across per-tenant DBs plus control-plane, 3 behavioural across bound-tenant defence-in-depth ValueError surfaces) and four import-linter contract additions (new layers-intent-class-eval hexagonal; intent_classification_evaluation added to three cross-context independence contracts plus the domain-purity contract). (6) `docs(p13/s48b): live-stack intent-classification evaluation against tenant_a` (this commit) landing the smoke document at `docs/smoke/p13_s48b_intent_classification_eval.md`, this session log entry, and the S48b close marker.

- Live-stack exercise outcome — three evaluation runs against tenant_a. (a) `gpt-4o-mini` ran to completion in ~75 seconds across all 40 entries: 39/40 correct (97.50% overall accuracy); per-class create_case 100%, add_data_point 100% (including all 4 S46 blind-spot phrasings classified correctly), revise_data_point 87.5% (1 miss; debatable gold-set entry), unclear 100%; latency profile min 970ms / avg 1.89s / p50 1.63s / p95 3.43s / max 6.14s; zero parse failures; total spend <$0.001 against the OpenAI account at published rates. (b) `qwen2.5:7b` failed twice at first-call cold-start timeout (91.5s and 92.2s vs the 30s REAL_TIME_REQUIRED tier budget); the substrate's runner-level failure-capture absorbed both timeouts, marked the runs failed with structured failure_reason, emitted the run.fail audit event. The qwen2.5:7b operational verdict is "not viable at REAL_TIME_REQUIRED tier on this hardware" — the production cell uses the same tier budget, so operator-facing cold-start cascades would face identical timeouts. (c) Audit chain extended with 6 events (one start + one terminal-transition per run); chain integrity holds at 245 events / 245 distinct hashes / single genesis from 2026-05-12.

- Decisions: D137 (intent-classification evaluation substrate at contexts/intent_classification_evaluation/) plus the architecture.md addition plus captures methodology candidates. No D-entries beyond D137 at S48b.

- Tests: 1776 unit + contract tests pass (+22 from S48a close baseline of 1754: +4 single-source-of-truth contract harness, +12 domain-layer unit tests on the substrate, +6 tenant-isolation contract scenarios). 33 of 33 import-linter contracts kept (+1 layers-intent-class-eval). AST enforcement green. Alembic migration 0022 applied successfully to both tenant data planes.

- Reflection (substrate placement verification, prompt 1): The recommended cut (a) at pre-write reconciliation Surface 1 — new bounded context at `contexts/intent_classification_evaluation/` per D127 alternative (d) — held without revision. Intent-classification evaluation and retrieval evaluation are distinct architectural concerns (different evidence source; different metric shapes; different gold-set authoring); a single evaluation umbrella context would have absorbed too much for one instance per the build-at-second-instance discipline. The placement decision was clean and did not require a D-entry alternative-considered update beyond what D137 already records.

- Reflection (gold-set authoring discipline, prompt 2): The 40 entries authored cleanly across the four intent classes with the four S46 blind-spot phrasings landing as explicit AddDataPointIntent entries at index 12-15. The S40b methodology entry ("metric-threshold expectations need structural-understanding grounding") fired weakly at S48b — the single revise_data_point miss at entry 29 ("The hiring search status changed: offer extended yesterday" classified as add_data_point) surfaces a gold-set entry whose expected-class is debatable. The phrase "status changed: offer extended yesterday" could legitimately be read as adding a new status note rather than revising an existing one; the gold-set author chose revise_data_point because "changed" + "yesterday" implies update, but the model's add_data_point reading is also coherent. Two structural improvements possible at future work: (a) tighten gold-set entries at the operator-authoring layer to remove expected-class ambiguity; (b) extend the schema to ask the model for a disambiguation discriminator between add vs revise on borderline phrasings. Neither is in scope at S48b. The discipline observation: even a 40-entry gold set surfaces at least one ambiguity at the operator-authoring layer; future per-tenant gold sets (D137 alternative (c) trigger) should expect the same and provision authoring-time disambiguation tools.

- Reflection (per-model evaluation outcome, prompt 3): **Substantive paragraph because the result is load-bearing.** The two-model comparison directly validates the S48a swap decision component-side. gpt-4o-mini classified the four S46 blind-spot phrasings correctly (4/4 add_data_point) at warm-call latency 1.5-2.3 seconds, dropped one borderline revise_data_point entry (debatable gold-set entry), and ran the full 40-entry evaluation in ~75 seconds for <$0.001 against the OpenAI account. qwen2.5:7b could not complete a single classification within the 30-second REAL_TIME_REQUIRED tier budget on this hardware (both runs failed at 91-92 second cold-start timeouts). The component-quality verdict for the current Phase 2-A operator-dogfooding model choice is unambiguous: gpt-4o-mini is operationally fit; qwen2.5:7b is not. The substrate's load-bearing claim ("answer model-choice questions in minutes") delivered in 75 seconds for a positive verdict and in 92 seconds for a clean failure verdict. Both outcomes are procurement-grade evidence the audit chain captures.

- Reflection (confidence-value calibration evidence, prompt 4): gpt-4o-mini populated the `confidence` field consistently across all 40 entries (no nulls); confidence values clustered in the 0.7-0.95 band for correct classifications. The single revise_data_point miss had confidence 0.85 — the model was confident in the wrong classification, which is exactly the failure mode confidence-aware composition is most exposed to (Case 1 high-confidence-but-wrong). The architectural defense at D134 remains the right cut: pattern 2 (suggestion-as-question) handles medium-confidence wrong classifications; only the high-confidence wrong cases (~2.5% of the gold set here) bypass that defense. Forward calibration substrate evidence: a per-model confidence-vs-correctness scatter plot at substrate altitude would surface model-specific calibration patterns; the substrate's data is now in place at `intent_class_evaluation_results` to compute that. Brief observation; not in scope at S48b.

- Reflection (methodology surfaces, prompt 5): **Substantive paragraph because three methodology candidates land.** First, the **scope-discipline-at-brief-authoring** observation from the operator's framing-time methodology line: the S48b brief committed the full P11-mirror as default scope; Surface 1 reconciliation treated the mirror as precedent rather than testing whether each piece served a current need; the scope question surfaced only when the full file count emerged at the brief-versus-codebase reading. Captured at log/captures.md as first-instance, adjacent to but distinct from interface-versus-implementation. Second, the **structural-test single-source-of-truth binding** pattern fired its second instance at S48b commit 2 (Python-identity assertion across consumer imports for the shared intent-classification primitive). The first instance was the no-numeric-threshold-literals AST test at S47 addendum commit 4f74509; the second-instance promotion threshold is met. Third, the **component-quality-vs-integration-smoke** discipline has now landed its first concrete implementation (D137 at S48b) — two-instance evidence from S46 and S47 plus the structural surface from D137. All three forward to the post-S48 hygiene methodology review.

- Reflection (path-naming discipline operational evidence, prompt 6): Surface 2 caught no path-naming drift at S48b framing or commit-drafting time. The brief's named paths (`contexts/intent_classification_evaluation/`, `apps/cli/_intent_classification_eval.py`, `tests/fixtures/intent_classification/`, `tests/contract/tenant_isolation/test_intent_class_eval_isolation.py`) all matched the actual conventions verified against the existing eight contexts and the existing apps/cli/_* file convention. Brief observation: the path-naming discipline is now mechanical enough that drift is rare at framing-time when the operator's brief has been authored against the actual repo state. Recurrence test continues.

methodology: Three captures-promotion-ready candidates land at S48b — the component-quality-vs-integration-smoke pattern now has two-instance evidence plus its first structural implementation in code (D137); the structural-test-binding pattern lands its second instance (symbol-identity at S48b commit 2 after the no-numeric-thresholds AST test at S47 addendum); the scope-discipline-at-brief-authoring pattern lands as first-instance observation captured during framing. All three forward to the post-S48 hygiene methodology review; the component-quality-vs-integration-smoke discipline is promotion-ready for `charter/methodology.md` per the existing two-instance-promotion convention.

```
metrics:
  classification: build session
  brief_started: 2026-05-26
  session_started: 2026-05-26
  session_closed: 2026-05-26
  merged: 2026-05-26
  close_state: clean (operational unblock verified at the component-quality layer; substrate ready for forward model-choice decisions)
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: charter/decisions.md (D137); charter/architecture.md (substrate sub-section); charter/current-package.md (S48b close marker); log/captures.md (three methodology candidates); log/sessions.md (this entry); briefs/p13/s48b.md (brief preservation); shared_kernel/intent_classification.py (new primitive); contexts/intent_classification_evaluation/ (new bounded context); alembic/tenant/versions/0022_intent_class_eval_substrate.py; apps/cli/_intent_classification_eval.py; apps/cli/main.py (sub-app registration); tests/fixtures/intent_classification/gold_set.yaml; tests/contract/intent_classification/test_single_source_of_truth.py; tests/contract/tenant_isolation/test_intent_class_eval_isolation.py; .importlinter (new contract + cross-context extensions); docs/smoke/p13_s48b_intent_classification_eval.md
  corrects:
  corrected_by:
```

---

## S48a — REAL_TIME_REQUIRED swap to gpt-4o-mini and smoke revalidation
roles: engineer, technical writer
mode: build session, executing briefs/p13/s48a.md

S48a — Thread 1 of the parallel post-S47 forward path — swapped the REAL_TIME_REQUIRED tier model pin from `qwen2.5:14b` to the hosted `gpt-4o-mini` per D133's gateway-as-resolution-point shape, then re-executed the S47 multi-turn cell smoke against tenant_a. The S47 smoke had surfaced `qwen2.5:14b`'s commodity-hardware viability gap (progressive 28s→361s slowdown); S48a verifies whether the convergence's Response A disposition closes operationally at a hosted model rather than a local one. **Verdict: operational dogfooding loop unblocked at `gpt-4o-mini`.** 4/4 AddDataPointIntent phrasings classified correctly at 1.5-3 s warm latency; convergence concern 5 closes operationally.

- Produced: One feat commit (`feat(p13/s48a): swap REAL_TIME_REQUIRED tier pin to gpt-4o-mini`, d620692) covering three configuration surfaces: `padhanam/config/inference.py` line 70 pin swap (qwen2.5:14b → gpt-4o-mini); `ops/litellm/config.yaml` model_list gains gpt-4o-mini routing entry referencing `os.environ/OPENAI_API_KEY`; `compose.yaml` litellm service gains `OPENAI_API_KEY: ${OPENAI_API_KEY:-}` env passthrough. Plus `.env.example` OPENAI_API_KEY block surfacing the new credential to operators, the model_registry docstring refresh acknowledging qwen2.5:14b's local-development-fallback role, and three test updates (one rename + assertion swap, two comment refreshes). The brief's intended chore commit shape upgraded to feat because the brief's surface-3 reconciliation found that the one-line pin change wouldn't work operationally without the parallel gateway and compose changes (the exact drift class the S47 smoke captured at `log/captures.md`). Plus one `docs(p13/s48a)` commit (this commit) landing the executed-evidence smoke document append at `docs/smoke/p13_s47_multi_turn_cell_end_to_end.md` under a new "## S48a re-execution at `gpt-4o-mini`" section, the qwen2.5:14b captures-entry resolution-line update (resolved via swap to gpt-4o-mini), three new captures entries (resolver disambiguation rendering on duplicate-title collision; cancel-then-fresh-turn double-cost behaviour; auto-expire-prior-pending positive substrate observation), this session-log entry, and the current-package S48a close marker.

- Smoke outcome — all stages verified end-to-end. Stage 1 environment: ngrok, Twilio Sandbox, gateway-lists-gpt-4o-mini, end-to-end probe through gateway → OpenAI in 2.15 s. Stage 2 CreateCaseIntent regression: cell classified at medium confidence, rendered Case 2 shape-aware clarification, cell-call latency 2.19 s. Stage 4 (first instance) confirmation resolution: pending RESOLVED in 16 ms (deterministic confirm path), case `acb449a0` created, cited confirmation reply rendered (`Recorded a new case: Q3 portfolio review. — ref acb449a0 · intake ea1563fc · 10:06 UTC`). **Stage 3 four-phrasing battery: 4/4 classifications correct** with `intent_type=add_data_point`, `data_point_type=GOAL`, `value_text="ship Wave 1 by end of May"`, and case_reference variants preserving the user's literal phrasing (`Q3 review` / `Q3 portfolio review` / `Q3 portfolio review case` / `Q3 portfolio review`); warm LLM call latency 1.5-2.3 s, cold 2.9-4.1 s. Stage 4 (second instance) surfaced the duplicate-title resolver-disambiguation finding (three cases titled `Q3 portfolio review` from S46/S47/S48a; the resolver's rendering uses only `title` and produced three identical disambiguation strings; no data_point was written). Stage 5 cancellation surfaced the cancel-then-fresh-turn double-cost behaviour (deterministic resolve in 14 ms + fresh-turn LLM in 3.77 s on the bare `no`). Stage 6 audit chain integrity: 239 events / 239 distinct hashes / single genesis from 2026-05-12; chain holds. Final tenant_a state: intakes 29→40 (+11), cases 7→8 (+1, `acb449a0`), data_points 3→3 (zero, resolver bailed), messages 36→56 (+20), pending_clarifications 2→7 (+5), tenant_audit 196→239 (+43).

- Decisions: No new D-entries. The session executes against D133's existing commitment (gateway-as-resolution-point with static configuration at Phase 2-A) and the model registry's pre-seeded `gpt-4o-mini` entry from S47 commit 4db4476.

- Tests: 1750 unit + contract passing post-swap (matches S47 close baseline exactly; the swap is configuration-only, no new test assertions added beyond the one renamed test asserting the new pin); 32 of 32 import-linter contracts kept; AST enforcement green.

- Reflection (classification reliability at `gpt-4o-mini` against the S46 blind-spot phrasings, prompt 1): **substantive paragraph because the result is load-bearing.** `gpt-4o-mini` classified `add_data_point` correctly across all four structural variants: template (`Add a goal to the Q3 review: …`), verb-first-with-full-title (`Add this goal to the Q3 portfolio review: …`), target-front-loaded (`For the Q3 portfolio review case, add a goal: …`), and the structurally-hardest no-verb-noun-phrase (`Ship Wave 1 by end of May — goal for the Q3 portfolio review.`). Each cascade populated all four intent slots correctly. S46's `qwen2.5:7b` was 0/2 on the template phrasing; S47's `qwen2.5:14b` couldn't validate because of latency timeouts. The intent-extraction reliability finding from S46/S47 closes at S48a via the configuration-only path the convergence's Response A described, with the hosted-inference variant resolving the hardware-viability gap. Phase 2-A operator-dogfooding's AddDataPointIntent surface is operationally usable.

- Reflection (latency profile against operator dogfooding viability, prompt 2): warm cell-call latency at `gpt-4o-mini` landed in the 1.5-2.3 s band; cold (first-call-after-restart) landed in the 2.9-4.1 s band. End-to-end inbound→outbound including DB writes and WhatsApp delivery lands at 1.7-4.1 s. Confirm-path orchestration (no LLM call) lands at 271-294 ms. Compared to S46's `qwen2.5:7b` warm at 8-9 s / cold at 22.95 s and S47's `qwen2.5:14b` at 28 s → 361 s progressive, `gpt-4o-mini` is comfortably in the operator-dogfooding-viable band. The dogfooding loop is unblocked operationally.

- Reflection (confidence-aware composition under the new model, prompt 3): the cell's self-reported confidence field is populated consistently across all classifications, landing in the medium band (0.5 ≤ c < 0.8) for both CreateCaseIntent and all four AddDataPointIntent phrasings. This is the same band the S47 smoke observed at `qwen2.5:14b` on the same phrasing classes, which suggests the medium-band landing is a calibration property of the cell's prompt-and-schema design rather than the model's capability. The cell's Case 2 (shape-aware clarification) renders correctly throughout; D134's three-case discipline operates as intended. The ConfidenceCalculator port's swap-readiness holds: a future adapter (e.g. one that calibrates against a held-out set rather than reading self-reported confidence) could swap in via the port's adapter slot without cell-side change. No captures entry added for the calibration observation — single instance, not promotion-threshold.

- Reflection (cost profile at operator-dogfooding volume, prompt 4): the smoke session consumed approximately 10 LLM cascades (Stage 2 + 4 fresh-turn classifications at Stage 3 + 1 cancel-fresh-turn at Stage 5 + a few corrections); each cascade carries ~$0.0001 input + output cost at gpt-4o-mini's published $0.150 input / $0.600 output per 1M tokens. Total smoke spend: well under $0.01. Operator dogfooding at one user's volume (estimated 50-200 cascades/day) projects to <$1/day, well within the static-configuration commitment's budget bracket. The model_registry's cost-per-call metadata is honest at the gpt-4o-mini row (verified at smoke close; no drift). The brief's reflection prompt 4 verdict: cost profile validates the architectural expectation.

- Reflection (methodology surfaces, prompt 5): substantive paragraph because three observations land. **First**, the swap that the brief framed as "one configuration line, chore commit shape" required three configuration surfaces (inference.py pin + gateway model_list entry + compose env passthrough) plus an `.env.example` addition for operator-facing documentation. The brief's pre-write reconciliation surface 3 (LiteLLM API for gpt-4o-mini) correctly anticipated this — the reconciliation guidance "if more is needed at the configuration surface, the swap is not a pure chore and the commit shape upgrades to feat" fired exactly as designed. The interface-versus-implementation discipline (now at 6+ instances of evidence) caught the discrepancy at framing rather than commit-draft time; the brief was honestly framed and the upgrade was clean. **Second**, the smoke-document append-not-replace protocol (brief's surface 5) was the first application of the pattern at a model-choice surface; the append cleanly preserved S47's executed evidence as historical record while the S48a section captured the new evidence. **Pattern recurrence note** at first instance only — a future model-choice surface change (S48c or beyond) is the test. **Third**, the smoke surfaced three substrate findings (resolver disambiguation rendering; cancel-then-fresh-turn double-cost; auto-expire positive observation) that are structurally separable from the model-choice question. Smoke discipline: per-stage executed evidence keeps substrate findings cleanly distinct from the model-swap verdict, which is exactly what allows the verdict to be honest ("operational unblock at `gpt-4o-mini`") without conflating the resolver finding into the verdict. No methodology promotion at S48a; observation forwarded to phase-2-audit-inputs at session close per the existing discipline.

methodology: The brief's pre-write reconciliation surface 3 firing correctly at draft time — the LiteLLM gateway routing question caught the "swap is more than one line" reality before any commit landed — is the seventh interface-versus-implementation instance the run has now recorded, and the first at a model-choice configuration surface (the pattern's prior surfaces were code path, schema location, adapter shape). The interface-versus-implementation discipline now has demonstrated coverage across charter altitude (the convergence conversation), code altitude (S47 framing), and configuration altitude (S48a framing). Phase 2-A close hygiene's methodology-promotion path inherits a candidate with broad surface coverage.

```
metrics:
  classification: build session
  brief_started: 2026-05-26
  session_started: 2026-05-26
  session_closed: 2026-05-26
  merged: 2026-05-26
  close_state: clean (operational unblock verified; three substrate findings forwarded to captures)
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: padhanam/config/inference.py; ops/litellm/config.yaml; compose.yaml; .env.example; padhanam/config/model_registry.py; docs/smoke/p13_s47_multi_turn_cell_end_to_end.md; log/captures.md; log/sessions.md; charter/current-package.md
  corrects:
  corrected_by:
```

---

## S47 smoke — UX convergence substrate live-stack verification
roles: engineer, technical writer
mode: strategic-mode-continuation (operational execution evidence continuing the S47 + addendum close)

The S47 substrate (D133 dispatch port + gateway-as-resolution-point + model registry; D134 confidence-aware response composition + ConfidenceCalculator + PendingClarification; D135 domain-content channel-format rendering; D136 multi-channel UX primitives; S47 addendum's ThresholdResolver port) verified end-to-end against tenant_a on a freshly-rebuilt `padhanam-api` image (digest `sha256:be1e2c09…`, advanced from the S47-base `sha256:ce2928c2…` for three smoke-driven substrate fixes). Stages 1+2 landed the full multi-turn cascade live; Stages 3-8 closed-with-findings under separately-captured operational constraints. Three substrate gaps surfaced and committed at d63f8a5; two operational findings captured at `log/captures.md` rather than fixed in-session.

- Produced: One smoke-driven substrate-fixes commit (`feat(p13/s47): smoke-driven substrate fixes`, d63f8a5) covering the cell `originating_intake_id` threading from webhook through to the PendingClarification insert (closing the FK-violation crash the brief comment had anticipated but not resolved against the migration's `fk_pending_clar_intake_id` constraint); the LiteLLM gateway `model_list` seed for qwen2.5:14b at `ops/litellm/config.yaml` (closing the model-registry-vs-gateway-routing drift the smoke surfaced on the first cell invocation); the `REAL_TIME_REQUIRED_TIMEOUT_SECONDS` env passthrough on the `padhanam-api` compose block plus the new image digest pin. Plus one `docs(p13/s47-smoke)` commit (this commit) landing the executed-evidence smoke document at `docs/smoke/p13_s47_multi_turn_cell_end_to_end.md`, two new captures entries at `log/captures.md` (qwen2.5:14b operator-dogfooding viability on commodity hardware; model-registry-vs-LiteLLM-gateway-routing drift class methodology candidate), this session-log entry, and the current-package S47 smoke close annotation.

- Smoke outcome — Stages 1+2 verified end-to-end. Multi-turn cascade with PendingClarification persistence + lifecycle confirmation: inbound `Start a case for the Q3 portfolio review.` (00:14:09 UTC) → cell classified at medium confidence → shape-aware clarification rendered `I think you want to start a case for 'Q3 portfolio review'. Is that right? (yes / no)` (00:14:58 UTC) → PendingClarification `c612afe5-…` persisted with `originating_intake_id = 1c1203cc-…` (real intake row, FK satisfied) → operator's `yes` reply (00:16:51 UTC) → cell consulted PendingClarificationReader at turn-open, found active PENDING, classified reply as confirm, resolved pending, executed create_case orchestration → cited confirmation `Recorded a new case: Q3 portfolio review. — ref 5d4c3092 · intake 874eb027 · 00:16 UTC` (00:16:53 UTC). Case `5d4c3092-…` created; pending transitioned PENDING→RESOLVED with `resolved_at` populated; two PendingClarification lifecycle audit events emitted (`messaging.pending_clarification.create` + `…resolve`). Stage 3 (cancellation) closed-with-findings: attempt 3a hit Case 3 cleanly (de-facto Stage 4 evidence); attempt 3b's cell call latency exceeded the bumped 120s tier timeout (361s LiteLLM-observed; dispatch port's failure-capture worked correctly at the boundary). Stage 4 (low-confidence/parse-failure) de-facto verified by attempt 3a's clean Case-3 rendering. Stage 5 webhook contract verified at every recorded inbound (2xx in 27-282ms regardless of cell-run duration). Stage 7 audit-chain integrity verified at smoke close (192 events, 192 distinct hashes, zero duplicates). Final tenant_a state: intakes 17→28 (+11), cases 6→7 (+1), messages 21→34 (+13), pending_clarifications 0→1, tenant_audit 165→192 (+27).

- Decisions: No new D-entries. The smoke produces operational evidence for existing commitments (D133, D134, D135, D136, S47 addendum's ThresholdResolver) plus two captures entries forwarded for post-S47 hygiene or next strategic-mode session disposition.

- Tests: 1750 unit + contract passing post-fixes (+175 from S47 close baseline of 1575, reflecting the addendum's ThresholdResolver tests plus the smoke-driven fixes' assertions); 32 of 32 import-linter contracts kept (no new contracts); AST enforcement green.

- Reflection (substrate verification, prompt 1): Stages 1+2 validate D133's dispatch port, D134's three-case discipline + PendingClarification lifecycle, D135's rendering pattern at the WhatsApp channel adapter, and the S47 addendum's ThresholdResolver port consumption — all at the live stack against tenant_a. The combined multi-turn cascade (inbound → clarification → confirmation → cited orchestration reply) lands as a single coherent flow with audit-chain integrity intact across all six lifecycle events. The load-bearing D134 commitment — pattern-2 binding at the intent-classification gate via confidence-aware composition — operationally activates: the cell renders `Is that right? (yes / no)` rather than acting confidently on an uncertain classification, exactly the failure mode the convergence's Response C addresses. The substrate works.

- Reflection (FK-violation finding, prompt 2): The smoke's first significant finding surfaced at the first cell Case 2 invocation. The S47 commit-5 cell's `_dispatch_clarify_with_pending` minted a fresh UUID for `PendingClarification.originating_intake_id`; the migration's `fk_pending_clar_intake_id` FK constraint rejected the insert with `ForeignKeyViolationError`. The brief comment had anticipated the limitation ("Phase 2-A compromise: mint a fresh UUID as the pending's originating_intake_id slot when no upstream intake id has been threaded through") but had not resolved it against the migration's FK constraint. The dispatch-port's failure-capture worked correctly at the boundary — the exception logged structurally with the caller's context dict, the webhook stayed 2xx, the FK violation surfaced cleanly in the api logs. Fix landed at d63f8a5: the webhook handler now passes the inbound `intake_id` to the cell's constructor; the cell uses it as the pending's `originating_intake_id`, satisfying the FK and giving the pending a structurally-honest audit anchor back to the inbound message. This is the kind of structural gap a unit-test cannot catch — the unit test for the cell's Case 2 path can construct a fake repository and accept any UUID, while the migration's FK constraint operates at the persistence layer outside the unit-test boundary. Contract harness extension (a per-tenant integration test verifying PendingClarification lifecycle against the real `pending_clarifications` table) would close this gap class; deferred to post-S47 hygiene.

- Reflection (model-registry-vs-gateway-routing drift, prompt 3): The second significant finding was the model-registry seed at `padhanam/config/model_registry.py` (per D133's audit-and-future-policy substrate) not propagating to the LiteLLM gateway's `model_list` at `ops/litellm/config.yaml`. The smoke caught the drift on the first cell call after the S47-base image was rebuilt — `litellm.BadRequestError: Invalid model name passed in model=qwen2.5:14b`. The fix at d63f8a5 added the gateway routing entry, but the underlying drift class remains structurally unprotected: nothing enforces that model_registry entries also appear in the gateway's model_list. Captured at `log/captures.md` as a methodology candidate; the proposed structural test (parse both files, assert set intersection) lives at `tests/contract/inference/test_model_registry_gateway_sync.py` per the captures entry's forward-fix candidate.

- Reflection (qwen2.5:14b hardware-viability finding, prompt 4): The third significant finding has S47-Response-A implications. The model bump per D133 commit 6 to qwen2.5:14b is operationally non-viable on this hardware: progressive call latency 28s → 48s → 67s → 361s suggests memory pressure or cache thrashing at the 9.7 GB resident model against the 16 GB total RAM. Phase 2-A operator dogfooding requires 10-30 second responsiveness at the WhatsApp surface; 6-minute latencies break the dogfooding loop. The S47 substrate is unaffected — even at the 14b pin, the cell at medium-confidence classification renders shape-aware clarification rather than acting confidently (D134 Response C operating correctly regardless of which model the gateway routes to). What does not validate is **Response A's static-configuration disposition** on this machine. Captured at `log/captures.md` with three forward paths (hosted inference; per-model tier-timeout overrides; constrained classification surface). The captures entry stays as forward observation rather than triggering a charter change; the existing convergence captures-entry from 2026-05-22 [S46 smoke] names all three responses and is not superseded.

- Reflection (Stage 3 cancellation closure-with-findings, prompt 5): The intended Stage 3 cancellation path was not exercised live because both attempts hit findings rather than the cancellation flow itself. Attempt 3a (`Maybe…`) routed to Case 3 cleanly — useful de-facto Stage 4 evidence but not the cancellation. Attempt 3b (`Add a goal…`) blew the tier timeout. The cancellation path is structurally identical to Stage 2's confirmation path with one transition difference (resolve as cancelled rather than confirmed; cell falls through to fresh-turn handling on the correcting inbound rather than executing the proposed orchestration). The unit test at `tests/unit/contexts/messaging/application/test_manual_entry_cell.py::test_cancellation_resolves_pending_and_falls_through` covers the exact path. The smoke verdict: substrate-verified by Stage 1+2 plus unit-test coverage of the cancellation transition; live exercise of cancellation defers to a follow-up smoke against a viable inference backend.

- Reflection (executed-evidence discipline at smoke close, prompt 6): The S46 smoke close annotation at `docs/smoke/p13_s46_manual_entry_end_to_end.md` carried executed evidence stage-by-stage; this S47 smoke annotation follows the same discipline. The discipline produces a structurally-honest audit artefact for any procurement-grade reader: the substrate claims at D133/D134/D135/D136 plus the S47 addendum's ThresholdResolver port are not only documented in charter and committed in code, they are operationally executed end-to-end against tenant_a with timestamps, IDs, audit-chain integrity counts, and per-stage verdicts. The smoke document's structural-honesty extends to the closed-with-findings stages — Stage 3+ findings are recorded as findings rather than as failures-of-substrate, with the substrate-verification status separated from the model-viability status.

- Reflection (post-smoke hygiene candidates, prompt 7): Three forward items the smoke surfaces for post-S47 hygiene or a follow-up session: (a) the model-registry-vs-LiteLLM-gateway-routing drift structural test per the captures-entry forward-fix candidate; (b) the per-tenant integration test for PendingClarification lifecycle against the real `pending_clarifications` table (to close the FK-violation gap class at the test boundary); (c) the inference-model-selection strategic block (hosted vs local; per-model tier-timeout; possibly Response B's constrained classification surface). None of these block P14 framing; all defer to either post-S47 hygiene or the next strategic-mode session as appropriate. The S47 substrate ready-for-P14 verdict from the original S47 close (`current-package.md`) holds at smoke close.

methodology: The model-registry-vs-LiteLLM-gateway-routing drift is a first-instance of a class — two configuration surfaces that must stay in sync with no structural test enforcing the invariant. Recurrence test: any future addition to one of the two surfaces (model_registry or gateway model_list) without parallel addition to the other will produce the same failure mode. The qwen2.5:14b hardware-viability finding is also a first-instance — operator dogfooding's latency budget is a load-bearing constraint that the S47 strategic-mode framing did not surface explicitly. The convergence's Response A disposition was made under the assumption that "static-configuration-at-Phase-2-A bumps the model" closes the reliability finding; the smoke evidence shows that the model choice is constrained by hardware as well as by classification accuracy. Both forward to post-S47 hygiene methodology review.

```
metrics:
  classification: live-stack smoke (operational execution evidence)
  brief_started: 2026-05-25
  session_started: 2026-05-26
  session_closed: 2026-05-26
  merged: 2026-05-26
  close_state: closed-with-findings (Stages 1+2 verified end-to-end; Stages 3-8 closed-with-findings; three substrate fixes committed; two captures forwarded)
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: docs/smoke/p13_s47_multi_turn_cell_end_to_end.md; log/captures.md; log/sessions.md; charter/current-package.md
  corrects:
  corrected_by:
```

---

## S47 — UX convergence substrate (multi-turn manual entry cell, dispatch port, confidence-aware composition)
roles: analyst, PM, architect, engineer, technical writer
mode: build session, executing briefs/p13/s47.md

S47 — the UX convergence substrate bridging P13 Wave 1 close and P14 epic framing — landed the convergence's five architectural settlements as charter (D133-D136 plus architecture, principles, deferred-decisions, captures additions) and built the substrate end-to-end across six code commits: the CellDispatch port plus Path A in-process adapter unhitching the Twilio webhook from the LLM call chain; the ConfidenceCalculator port at shared_kernel with the self-reported placeholder adapter plus D130's StructuredOutputParseFailure extension; the PendingClarification domain entity with Postgres persistence and three-stage audit lifecycle (create / resolve / expire); the cell's three-case decision logic plus PendingClarification multi-turn consumption plus WhatsApp shape-aware-clarification rendering; the static configuration bump for REAL_TIME_REQUIRED to `qwen2.5:14b` plus the model registry seed per D133. Seven commits total.

- Produced: charter commits landing D133-D136 plus architecture.md, principles.md, deferred-decisions.md, captures.md, current-package.md, and `briefs/p13/s47.md` (28127d9); CellDispatch port plus InProcessCellDispatchAdapter plus cell-failure logging plus webhook handler refactor plus _SyncCellDispatch test fake (5341c59); ConfidenceCalculator port at `shared_kernel/confidence_calculator.py` plus SelfReportedConfidenceAdapter at `contexts/inference/adapters/` plus StructuredOutputParseFailure typed exception at shared_kernel and LiteLLM adapter (a7163f2); PendingClarification domain entity plus PendingClarificationRepository port plus consumer reader port plus three lifecycle use cases plus Postgres adapter plus Alembic 0021 with partial unique index plus three new permissions (8f7fd02); cell three-case decision logic plus PendingClarification multi-turn consumption plus confidence cut-off configuration at `padhanam/config/messaging.py` plus WhatsApp rendering (df20d9c); REAL_TIME_REQUIRED model pin bump to `qwen2.5:14b` plus model registry seed at `padhanam/config/model_registry.py` with four entries per D133 (4db4476); this commit landing the procedural smoke at `docs/smoke/p13_s47_multi_turn_cell_end_to_end.md` plus this session-log entry plus the current-package S47 close annotation.

- Decisions: D133 (gateway-as-resolution-point plus webhook dispatch port plus model registry), D134 (confidence-aware response composition plus ConfidenceCalculator port plus PendingClarification), D135 (domain-decides-content channel-decides-format rendering pattern), D136 (multi-channel UX architectural primitives). Plus the channel-shaped-rendering principle at `charter/principles.md` Phase 2 principles section. Plus three deferred-decisions entries (multi-channel UX entry updated as activated by D136; shared-kernel CitedResponse structural enforcement entry; cost-aware gateway routing policies entry).

- Tests: ~31 new tests; the full unit-plus-contract run is 1575 passed (+87 from S45 close baseline of 1488, +5 from S46 close), 178 skipped (docker/empty-registry), no failures. 32 of 32 import-linter contracts kept (no new contracts); AST enforcement green. New test files: `tests/unit/apps/api/test_cell_dispatch_inprocess.py` (4 scenarios — dispatch returns quickly, cell runs in background, exception captured + logged with context, concurrent dispatches independent), `tests/unit/shared_kernel/test_confidence_calculator.py` (7 scenarios — self-reported reads response.confidence, missing-value default, default tunability, clamp to [0,1], default range validation, ConfidenceCalculator Protocol conformance), `tests/unit/contexts/messaging/domain/test_pending_clarification.py` (9 scenarios — happy path, six invariant rejections, resolve + expire transitions), `tests/unit/contexts/messaging/application/test_pending_clarification_use_cases.py` (7 scenarios — create persists + audits, create expires prior PENDING, resolve + expire transitions, three authorisation-denial paths). Plus updates to manual_entry_cell unit tests, structured-output parse-failure assertion changes, operator-permission test updates for the three new pending_clarification permissions, latency-tier test for the qwen2.5:14b pin, and ConversationFlow harness stub updates.

- Reflection (convergence settlement integrity, prompt 1): each architectural commitment from D133-D136 landed cleanly in the substrate. The interface-versus-implementation pre-write reconciliation surface 9 caught two draft-time drifts at framing: (a) the brief named `apps/inference/adapters/` as the SelfReportedConfidenceAdapter home, but no such directory exists — the adapter lands at `contexts/inference/adapters/confidence_self_reported.py` (sibling of `outbound/`), recorded at the commit-3 message; (b) the dispatch port shape was named as sync `dispatch(...)` in the brief but the test seam required async-friendly semantics (a test fake awaits the cell synchronously while production schedules a task), so the port is `async def dispatch(...)` — the production adapter still returns promptly via `asyncio.create_task`, the in-flight `await` is trivial. Both shifts preserve the convergence's architectural intent at D133/D134; neither required charter rework.

- Reflection (interface-versus-implementation discipline at draft time, prompt 2): surface 9 was the new pre-write reconciliation surface at S47 framing; it caught both deviations above at commit-draft time rather than at operator review. The discipline addition recorded at the captures methodology entry now has first-instance operational evidence — the framing-time check "am I committing the interface or the implementation?" successfully directed both decisions back to the interface. The CellDispatch port stayed the architectural commitment with InProcessCellDispatchAdapter as the Phase 2-A adapter behind it; the ConfidenceCalculator port stayed the commitment with SelfReportedConfidenceAdapter as the placeholder. Without the surface, the brief's literal paths would have produced an adapter at a nonexistent directory and a port shape that did not test cleanly.

- Reflection (dispatch port and cell-failure logging integration, prompt 3): the bare-`except Exception: pass` at the prior synchronous shape (`apps/api/routers/messaging.py:267`) is closed by the dispatch port's contract. The integration produced no unexpected interactions with the existing Twilio signature verification (the webhook still verifies before dispatch) or the outbound REST API delivery (the cell's `send_message` still runs through the existing path after the dispatch's background task completes). The clean separation comes from the dispatch port carrying the cell-failure logging at *its* contract: callers don't see the cell's exceptions; the structured `cell-run failed in background task` log line emerges with the caller's `context` dict (intake_id, tenant_id, external_id) surfacing as `extra={"context": ...}`. The test for it (`test_dispatch_captures_cell_exception_and_logs`) verifies the contract end-to-end.

- Reflection (PendingClarification audit-chain integrity, prompt 4): the lifecycle events chain cleanly across the multi-turn cascade. The three audit drafts (`messaging.pending_clarification.create / resolve / expire`) follow the same draft-then-recompute shape as `draft_message_event`, so the Postgres audit adapter recomputes `previous_event_hash` and `this_event_hash` inside its locking transaction per D37. Across three lifecycle paths (create→resolve confirmed, create→resolve cancelled, create→expire), the audit chain at the unit-test altitude carries the right shape: one chain-anchoring per lifecycle event, three after-state shapes (creation carries `expires_at` plus `proposed_action_summary`; resolution carries `resolution: confirmed|cancelled` plus `resolved_at`; expiration carries the `resolved_at` only). The procurement-grade defensibility question — can an auditor distinguish "platform acted confidently on a low-confidence guess" from "platform asked for confirmation before acting" — answers yes via the audit chain: every Case 2 emission produces a `create` event, and every confirmed action carries a `resolve` event immediately before the portfolio write. Phase 2-A operator dogfooding will exercise the chain at live volume.

- Reflection (confidence-aware composition operator-facing reality, prompt 5): the smoke is procedural; the operator-facing UX records live. The shape-aware clarification at Case 2 reads as a suggestion-as-question per principle pattern 2 (`I think you want to <action>. Is that right? (yes / no)`). The hex-citation line per D131 Shape 1 holds on the confirming-reply confirmation. Whether the cut-off pair (0.8 / 0.5) hits the right balance — too many Case 1 false confidences feel pushy, too many Case 2 clarifications feel pedantic — is calibration that emerges with dogfooding data; the substrate is in place to tune without code change. The cut-offs threaded through MessagingSettings means a smoke-time recalibration is `MESSAGING_CONFIDENCE_HIGH_CUTOFF=0.85` away.

- Reflection (static config bump validation, prompt 6): the bump from `qwen2.5:7b` to `qwen2.5:14b` is a substantive Phase 2-A reliability commitment, not a cosmetic config nudge. The S46 smoke surfaced 0/7 AddDataPointIntent classifications across four phrasings at the 7B model; the 14B variant has substantially stronger discriminative capability on structured-output tasks per published benchmarks. Whether the bump closes the operational problem in the operator's actual deployment requires the live smoke — if `qwen2.5:14b` also misses AddDataPointIntent under the operator's hardware (or under load), Response A's static-configuration disposition has not closed the convergence's concern 5, and the operator should override to a hosted alternative (gpt-4o-mini, claude-haiku-4-5) via `INFERENCE_REAL_TIME_REQUIRED_MODEL`. The model registry at `padhanam/config/model_registry.py` carries the four candidate models with cost-per-call so the audit chain records which model fired and procurement-grade defensibility holds across the bump. The smoke's stage-2 medium-confidence cascade is the empirical test for `qwen2.5:14b`'s classification accuracy; if every attempt classifies at confidence ≥ 0.8, the cell skips Case 2 entirely and the convergence's principle-2-binding surface stays untested in practice.

- Reflection (methodology surfaces, prompt 7): the interface-versus-implementation methodology candidate (promotion-ready at the captures entry from this session's charter commit) earned its first operational instance during S47's commit-draft cycles, as recorded above. No additional methodology candidates from S47 itself warrant captures entries — the substrate work largely followed the established patterns (consumer ports per S46 precedent; intake-canonical orchestration per S44b; audit-event drafts per S45). One observation worth tracking but not yet promotion-threshold: the cell now consumes seven dependencies at construction (structured_output_port, portfolio_gateway, actor, confidence_calculator, pending_clarification_reader, pending_clarification_repository, audit_port plus two cutoffs). Phase 2-B+ ConversationFlow implementers landing in P14 with similar dependency counts will trigger the question of whether a CellComposition value object captures the cross-cutting infrastructure separately from per-cell deps. Single instance; not a captures entry.

methodology: Surface 9 (interface-versus-implementation discipline) — the captures-promotion-ready candidate's *first operational application* — caught two draft-time drifts at framing (the apps/inference/adapters/ nonexistent path and the sync-vs-async dispatch port shape). The candidate's promotion at the captures entry has structurally-honest evidence the discipline does load-bearing work at S47, supporting Phase 2-A close hygiene's methodology-document promotion path. The four-instance evidence base from the convergence conversation plus the two S47 framing instances brings the candidate to six-instance pattern with two distinct surfaces (charter-altitude drafting at the convergence; code-altitude drafting at S47).

```
metrics:
  classification: build session
  brief_started: 2026-05-25
  session_started: 2026-05-25
  session_closed: 2026-05-25
  merged: 2026-05-25
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: charter/decisions.md; charter/architecture.md; charter/principles.md; charter/deferred-decisions.md; charter/current-package.md; log/captures.md; log/sessions.md
  corrects:
  corrected_by:
```

---

## S46 smoke — manual entry cell live-stack verification
roles: engineer, technical writer
mode: strategic-mode-continuation (operational execution evidence continuing the S46 close)

The S46 manual entry cell (D115 first ConversationFlow implementer, D122 latency-tier routing, D132 four-layer model ontology) verified end-to-end against tenant_a on the rebuilt `padhanam-api` image (digest `sha256:523e9419…`, pin advanced from S45's `sha256:77ad47bf…`) across the operator-WhatsApp-to-portfolio-state cascade. Two CreateCaseIntent cascades clean (stages 1 and 8e-i); seven AddDataPointIntent attempts across four phrasings all classified to UnclearIntent (the cell behaved contract-correctly throughout — clarification, no write, no citation line); two structural-honesty findings surfaced and captured at `log/captures.md` rather than fixed in-session.

- Produced: Three commits across the smoke-execution arc. `chore(compose)` `padhanam-api` image re-pin to `sha256:523e9419…` after `make build-api` rebuilt the image to carry S46 code (the running container had been S45's `sha256:77ad47bf…`); `docs(charter)` two captures entries at `log/captures.md` (webhook synchronous-cell-run-vs-Twilio-timeout finding with cold-vs-warm latency paragraph; intent-extraction reliability at REAL_TIME_REQUIRED with `qwen2.5:7b` finding with instrumentation-gap paragraph); `docs(p13/s46-smoke)` landing the executed-evidence smoke document, this session-log entry, and the current-package smoke-complete annotation.

- Smoke outcome: two CreateCase cascades (stage 1 Case `fa6401b0` "Q3 portfolio review"; stage 8e-i Case `4fac3ae0` "Q3 budget review" against the now-similar-named existing case, no conflation); one AddDataPoint attempt at 8a using the smoke document's own template phrasing produced UnclearIntent; three structural rephrasings exercising distinct surface-cue patterns (verb-first-full-title, target-front-loaded, no-verb-noun-phrase) each sent twice (six runs total) all produced byte-identical UnclearIntent clarifications. Final tenant_a state: intakes 17 (+11 from baseline 6), cases 6 (+2), data_points 3 (unchanged — no Add succeeded), assertions 6 (unchanged), messages 21 (+18), tenant_audit 165 (+31). Audit chain holds: 165 events, exactly 1 chain entry point (genesis), 165 distinct `this_event_hash` values (zero duplicates), 0 broken links. D122/D132 trace attributes verified at the stage-1 LLM-call observation: all four `gen_ai.model.*` dimensions present (provider=ollama, account=default, version=qwen2.5:7b, configuration=`latency_tier=real_time_required;temperature=0.0;structured_output_schema=present`), scope `padhanam.inference.litellm`. D131 citation contract held in all 9 outbound replies (2 cited per Shape 1 short-hex-plus-timestamp; 7 clarifications carrying no citation line).

- Decisions: No new D-entries. The smoke produces operational evidence for existing commitments (D115, D122, D128, D129, D130, D131, D132) plus two structural-honesty findings preserved at `log/captures.md` (the webhook-contract violation finding feeds a future build session for Path A in-process asyncio dispatch plus cell-failure logging; the intent-extraction reliability finding is first-class convergence-session input).

- Tests: No test surface changed — operational execution evidence. The S46 build-session suite (`tests/unit`, `tests/contract`, `tests/integration/api` green at close) stands.

- Reflection (LLM cold-vs-warm latency dynamic, prompt 1): the synchronous-LLM-vs-Twilio-timeout finding surfaced at stage 1 (LLM call 22.95s, ngrok status 0) — initially framed as "the 23s LLM blows Twilio's 15s webhook timeout, retry-duplication risk." The round-2 rephrasings produced a richer empirical picture: the same LLM call ran at 18.62s (cold after 13min idle), 9.07s (warm 35s later), 8.05s (warm 18s later). Ollama's 5-min keep-alive unloads the model on idle; the next call pays cold-start cost. The webhook-contract finding holds on grounds independent of which side of the timeout a given run falls (a synchronous handler is wrong on contract grounds even at 8s — it ties up a server worker for the full duration and writes its response to an already-closed connection), but the empirical urgency calibrates by usage pattern. The captures entry now incorporates both endpoints honestly.

- Reflection (intent-extraction reliability and N=1-vs-N=4 catch, prompt 2): stage 8a's AddDataPointIntent attempt classified to UnclearIntent using the smoke document's own template phrasing. The initial disposition drafting in the cross-thread captures wording overclaimed three rephrasings as "having run" before they actually ran — Claude Code caught the N=1-versus-N=4 conflation at captures-entry drafting time and surfaced the discrepancy; the operator chose Path 1+2 (actually run the rephrasings then close) over Path 2 (close at N=1 with softer wording). The rephrasings ran (verb-first, target-front-loaded, no-verb-noun-phrase; each sent twice for six runs total); all classified to UnclearIntent. Phrasing-sensitivity empirically falsified; intent-class blindness on AddDataPoint at `qwen2.5:7b` / REAL_TIME_REQUIRED is the surviving claim, supported by N=4 distinct phrasings across 7 runs. Three architectural responses for the convergence session captured: raise the REAL_TIME_REQUIRED tier model (Response A), constrain the classification surface or expand the prompt (Response B), render model uncertainty as user-facing clarification more honestly (Response C). Phase 2-A operator-dogfooding-via-WhatsApp viability opens as a first-class convergence input rather than mid-stream optimisation.

- Reflection (D131 first-instance operator-facing reality, prompt 3): the cited replies in both CreateCase cascades carried the Shape 1 line cleanly — `— ref fa6401b0 · intake 85622595 · 21:45 UTC` and `— ref 4fac3ae0 · intake 583c7c36 · 22:30 UTC`. The short-hex prefixes match the persisted IDs exactly. The citation line sat below a blank line beneath the confirmation prose, scanning as a single short line. The clarification-path replies (8a + rephrasings) carried no citation line — D131 contract held across all 7 clarification cases. Whether operators find the citations useful, intrusive, or ignorable at any volume is convergence-session-relevant; the smoke confirms the rendering shape without stressing operator preference.

- Reflection (instrumentation gap on prompt/output capture, prompt 4): the Langfuse trace captures the D122/D132 dimensions and token counts but does not capture prompt content or model output. The smoke cannot distinguish whether `qwen2.5:7b` returned clean UnclearIntent JSON (the model knows it doesn't know) or whether `parse_intent` coerced malformed output to UnclearIntent (the model produced something Padhanam can't parse). The two cases have different architectural implications for confidence-aware composition. Folded as a paragraph inside the intent-extraction reliability captures entry rather than as a separate finding because the gap is structurally linked to the intent-extraction finding's analytical surface.

- Reflection (discipline-working signals during smoke execution, prompt 5): three discipline-working signals at this smoke worth indexing for Phase 2-A close hygiene methodology assessment. First, the N=1-versus-N=4 catch at captures-entry drafting where the build-execution surface caught a strategic-altitude overclaim before it landed in repo state — pre-write reconciliation at a non-traditional surface (cross-thread captures wording rather than within-brief acceptance criteria). Second, the cold-vs-warm latency finding emerging late through the round-2 rephrasings rather than at the initial stage-1 single data point — empirical evidence can sharpen mid-smoke if the test surface generates richer measurement, even when not planned. Third, three actually-distinct phrasings exercising different surface-cue patterns producing falsification evidence rather than three near-variants producing redundant noise — phrasing-pattern variation as a test-design discipline at the smoke surface. All three are convergence-input-shaping signals; not promoted to methodology entries at this commit because single-instance observations.

methodology: The three discipline-working signals (pre-write reconciliation across surfaces; mid-smoke empirical sharpening from richer measurement; phrasing-pattern variation as test-design discipline) are observation-level recordings, not promotion-threshold-crossing recurrence. The N=1-versus-N=4 catch is structurally similar to the pre-write reconciliation pattern at brief altitude (Phase 1's pre-write reconciliation pattern at 19+ instances), but the surface (cross-thread captures wording) is novel — worth tracking whether the surface recurs at Phase 2-A close methodology assessment. The cold-vs-warm finding's late-emergence shape recurs at the third-instance threshold if Phase 2-A smoke sessions produce more empirical-sharpening-mid-smoke instances.

```
metrics:
  classification: live-stack smoke (operational continuation)
  brief_started: 2026-05-22
  session_started: 2026-05-22
  session_closed: 2026-05-22
  merged: 2026-05-22
  close_state: clean (two structural findings captured rather than fixed; both feed convergence-session inputs)
  tests_passing: not_applicable
  principles_intact: yes
  charter_touchpoints: docs/smoke/p13_s46_manual_entry_end_to_end.md; log/captures.md; charter/current-package.md
  corrects:
  corrected_by:
```

---

## S46 — Manual entry cell (first ConversationFlow implementer) plus latency-tier routing plus four-layer model ontology
roles: analyst, PM, architect, engineer, technical writer
mode: build session, executing briefs/p13/s46.md

S46 — the P13 Wave 1 end-to-end exercise — landed the manual entry cell as the first ConversationFlow implementer (D115), the D122 latency-tier routing, and the D132 four-layer model ontology, end-to-end from a WhatsApp message through structured-output intent extraction, target identifier resolution, and an intake-canonical portfolio write to a cited confirmation rendered back to WhatsApp. Twelve commits. P13 Wave 1 closes at S46.

- Produced: charter updates landing D132 plus the D131 first-instance Expected-exercise update (14e7d0e); intent value objects with the four-variant discriminated union (9dd832f); the target identifier resolution helper (5b162d0); the four-layer model ontology and LatencyTier at `shared_kernel/inference.py` (1f7033b); the InferenceSettings `latency_tier_config` extension (3f40c32); the tier-aware LiteLLM adapter plus the `model_ontology` sibling plus the call-site retrofit (f257e14); the PortfolioGateway consumer port plus the apps/-layer adapter (7351911); the manual entry cell plus `cell_response` (a49a34a); the ConversationFlow harness registration (64bf0d8); the composition-root wiring plus end-to-end integration tests (fad90d7); the procedural live-stack smoke (0977ca9); this commit landing the session-log entry and the P13 Wave 1 close marker.

- Decisions: D132 (four-layer model ontology at the inference port). D131's Expected-exercise line updated for the S46 first-instance exercise. No other new D-entries — the cell, the intent value objects, the resolution helper, and the latency-tier routing land under existing commitments (D115 ConversationFlow, D122 latency-tier, D129 messaging, D130 structured output, D131 provenance).

- Tests: roughly 55 new and updated tests; `tests/unit`, `tests/contract`, and `tests/integration/api` green; 32 of 32 import-linter contracts kept; AST enforcement green. The ConversationFlow contract harness's five conformance scenarios — dormant at S45 with an empty registry — now run against the manual entry cell as the first registered implementer.

- Reflection (pre-write reconciliation, prompt 1): nine surfaces. Five absorbed mechanically (D-number, ActorContext synthesis, the three intake orchestration signatures, CLI-out-of-scope, type-identifier search). Four were structural-honesty findings, all operator-approved before code: (A) the brief's Path B — a *required* tier parameter — contradicted D122's committed "Phase 1 call sites preserve current behaviour with opt-in retrofit", resolved to Path A (a defaulted `latency_tier`); (B) the cell could not sit at the domain layer the brief named — a cell holding ports breaches the `layers-messaging` hexagonal contract — and moved to the application layer; (C) "ModelIdentifier at the call signature" was wrong in kind (call sites carry no Provider/Account knowledge, the Configuration layer overlaps existing request fields), resolved to adapter-boundary composition; (D) surface 8's consumer-port shape settled to a single `PortfolioGateway` carrying both the resolution reads and the orchestration writes, and surface 9's call-site count (four) confirmed Path A was tractable. The cleanest reconciliation cycle of P13 — four structural findings, none of which reached a code commit.

- Reflection (methodology candidate, prompt 2): S46 Item 4 verification found that "four-layer model ontology" (D132) is *new* substrate, distinct from the pre-existing "four-layer constraint stack" at `architecture.md` (D80) — two unrelated "four-layer" concepts. The verification did real work (it caught the distinction before the brief drafted against the wrong one) but produced new substrate, not recurrence of the "prose already landed" pattern. The framing-conversation pre-write-reconciliation methodology candidate stays at two-instance evidence (S44 Item 4, S45 Item 4) for the P13-close hygiene assessment.

- Reflection (D131 first-instance, prompt 3): the citation discipline felt natural at the cell altitude — `CellResponse` carries the three citation tuples, the cell populates `cited_intake_records` and `cited_artefacts` from the orchestration-result IDs, and `render_for_whatsapp` renders the compact Shape-1 line. The one honest gap: `cited_audit_events` stays empty because the intake-owned write-result DTOs (`CaseWriteResult`, `DataPointWriteResult`) do not surface audit-event IDs. The convention-versus-structural-enforcement gap is real but caused no implementation friction — the cell simply cites what it holds in scope. Recorded at `charter/captures.md` for the Phase 3 close audit.

- Reflection (ConversationFlow harness, prompt 4): the harness's five scenarios fit the cell cleanly — no contract-scenario gaps. `make_instance` needed offline stubs (a structured-output stub returning an UnclearIntent extraction so a harness `turn` runs without an LLM and never reaches the gateway); the `@runtime_checkable` `isinstance` check and the open/turn/close lifecycle held without adjustment. One genuine adjustment: S45's `test_harness_is_ready_at_s45` asserted an *empty* registry — that was an S45-state test, not a permanent contract, and it was replaced with `test_manual_entry_cell_is_registered`. Second-instance evidence for the harness machinery design: it absorbed the first implementer with no harness change.

- Reflection (D132 Configuration scope, prompt 5): the Configuration layer settled to a *narrower* scope than "everything affecting the call." It carries `latency_tier`, `temperature`, `max_tokens`, and the structured-output schema — the per-call *parameters* — but not the system prompt or message list, which already live first-class on the request surface. Finding C drove this: a Configuration layer that re-homed existing request fields would create two homes per field. `ModelIdentifier` composes at the LiteLLM adapter boundary and `audit_dimensions()` captures the four `gen_ai.model.*` span attributes per call; the audit capture worked cleanly in the adapter unit tests.

- Reflection (latency-tier operational evidence, prompt 6): the S46 smoke is procedural — the build environment cannot reach docker or the Twilio Sandbox — so the per-tier latency baseline records at the operator's live execution. The cell's only LLM call is the `REAL_TIME_REQUIRED` intent extraction (a small structured generation); no `ASYNC_TOLERANT` call fires on the cell path. The two evaluation appliers opt into `ASYNC_TOLERANT` per D122's substrate-analysis classification; the agent loop and `request_completion` stay on the `REAL_TIME_REQUIRED` default — both are user-invoked surfaces. S45's 97 ms inbound cascade remains the reference for the webhook leg.

- Reflection (convergence-session activation, prompt 7): P13 Wave 1 closes at S46. The UX convergence strategic-mode session named in the S45 `charter/captures.md` entry — combining multi-channel UX, provenance-aware response composition, and confidence-aware response composition into one coherent design — activates now, between this close and P14 epic framing. It is the next strategic-mode block.

methodology: Finding A is the brief-conflicts-with-a-committed-D-entry pattern recurring — the framing brief's Path-B disposition was reasoned from the call-site count without cross-checking D122's committed text. Pre-write reconciliation caught it at brief altitude. Cumulative evidence (with the S45 SMS-vs-WhatsApp and D-number findings) that the framing-conversation discipline surfaces alternatives without always reconciling against existing D-entry commitments; carried to the Phase 2-A close methodology assessment.

```
metrics:
  classification: build session
  brief_started: 2026-05-22
  session_started: 2026-05-22
  session_closed: 2026-05-22
  merged: 2026-05-22
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: charter/decisions.md; charter/architecture.md; charter/schema.md; charter/packages/p13-epic.md; charter/current-package.md; log/captures.md
  corrects:
  corrected_by:
```

---

## S45 smoke — messaging substrate live-stack verification (stages 0-9)
roles: engineer, technical writer
mode: strategic-mode-continuation (operational execution evidence continuing the S45 close)

The S45 messaging substrate (D129), structured-output discipline (D130), and ConversationFlow shape (D115) verified end-to-end against tenant_a on the rebuilt `padhanam-api` image — including a real Twilio WhatsApp Sandbox round trip in both directions. Ten smoke stages (0-9) walked at `docs/smoke/p13_s45_messaging_substrate.md`.

- Produced: Five commits across the smoke-execution arc. `chore(p13/s45)` Twilio messaging env wiring into the compose stack (12f9716); `chore(p13/s45)` image re-pin to `sha256:77ad47bf` after `make build-api` (0f50c80); `chore(compose)` loopback-only `127.0.0.1:8000:8000` binding for the webhook tunnel — a dev-only exception per S5, matching the postgres-control-plane precedent (7af8e88); `chore(compose)` `WEBHOOK_*` passthroughs (6079b9d); this commit landing the executed-evidence smoke document, this entry, and the current-package transition.

- Smoke outcome: all ten stages green. Stage 0 — Twilio Sandbox setup plus the ngrok-binding methodology-gap fix. Stage 1 — `make build-api` plus `make migrate` (0019/0020 on both tenant planes; the `messages` table and the `WHATSAPP_INBOUND` enum extension verified). Stage 2 — outbound via LocalEcho (Message `2cb6d146`, `external_id=local-echo-…`, DELIVERED, no Twilio call). Stage 3 — outbound via Twilio (Message `97e5d4c5`, `external_id=SM81b3ed…`, QUEUED, delivered to the operator's WhatsApp). Stage 4 — inbound webhook: a real Twilio WhatsApp message produced IntakeRecord `b165f334` (WHATSAPP_INBOUND) then Message `c4ae90e6` (INBOUND, `intake_id` linking), 97 ms cascade. Stage 5 — invalid signature rejected 403, zero side effects, `AUTH_FAILURE` security event. Stages 6-7 — GET routes plus cross-tenant 404s. Stage 8 — audit chain: 134 events, one genesis entry point, zero broken links, zero duplicate hashes. Stage 9 — pagination plus direction/channel filters.

- Decisions: No new D-entries. The smoke produces operational evidence for existing commitments (D119, D128, D129, D130).

- Tests: No test surface changed — operational execution evidence. The S45 build-session suite (`tests/unit` plus `tests/contract` green at close) stands.

- Reflection (LocalEcho operator-dev-cycle, prompt 1): LocalEcho's substrate-depth dividend is real and bounded. It removed the external dependency for substrate iteration — route, persistence, and the audit chain are verifiable in-process with no credentials and no Twilio call — but it cannot verify actual WhatsApp delivery. First-instance evidence for the YAGNI monitoring discipline: the D129 substrate-depth justification is *partially* exercised at Phase 2-A; whether it was load-bearing versus a Twilio-only adapter plus heavier test setup is a Phase 3 close audit evaluation.

- Reflection (D128 second-instance, prompt 2): the cross-context shape worked cleanly end-to-end with no structural friction — `record_intake_and_record_inbound_message` at the intake context, the `MessageWriter` consumer port, the `apps/`-layer adapter — and the IntakeRecord-then-Message-with-`intake_id` ordering held across a real Twilio webhook. The orchestration shape generalises across context-pairs (S44b portfolio writes first instance; messaging-to-intake second).

- Reflection (D131 first-instance accountability, prompt 3): no Phase 2-A surface implements a ConversationFlow consumer; provenance-aware response composition is not exercised. Recorded honestly — the substrate-depth-overrides-YAGNI commitment is on the record for the Phase 3 close audit.

- Reflection (methodology recurrence, prompt 4): the smoke-document stage-0 ngrok-binding gap — the original draft named `ngrok http 8000` without verifying that port 8000 is host-bound (it is not, per S5's only-Caddy-binds rule). Brief-vs-codebase-actuality drift at the smoke-document altitude — the same shape as the SMS-vs-WhatsApp finding at the S45 brief, scaled down to a procedural concern. Recurrence evidence; deferred to the Phase 2-A close methodology assessment per the philosophical pruning discipline.

- Reflection (cascade latency, prompt 5): stage 4's Twilio-POST-to-200-response measured 97 ms upstream. The inbound cascade — signature verification, dual-decorator authorisation, IntakeRecord plus Message persistence, two audit events — sits well inside a real-time latency budget. An empirical reference point for S46's latency-tier-routing framing.

```
metrics:
  classification: live-stack smoke (operational continuation)
  brief_started: 2026-05-22
  session_started: 2026-05-22
  session_closed: 2026-05-22
  merged: 2026-05-22
  close_state: clean
  tests_passing: not_applicable
  principles_intact: yes
  charter_touchpoints: docs/smoke/p13_s45_messaging_substrate.md; charter/current-package.md
  corrects:
  corrected_by:
```

---

## S45 — Messaging substrate (WhatsApp via Twilio Sandbox) plus ConversationFlow Protocol plus structured output
roles: analyst, PM, architect, engineer, technical writer
mode: build (P13 Wave 1 Session 3 — new bounded context plus two shared_kernel primitives)

- Produced: Fourteen commits closed S45 (the brief's thirteen plus a session-close test correction).
  - Commit 1 (`docs(p13/s45)`, db5274d) landed the charter ahead of code: D129 (messaging substrate plus inbound-as-intake-orchestration plus LocalEcho adapter), D130 (structured-output discipline at shared_kernel), D131 (provenance-aware response composition); the `charter/architecture.md` provenance prose; the p13-epic Session 3 line; `charter/schema.md` messaging-substrate plus shared-kernel additions; two `charter/deferred-decisions.md` entries (structured-output-to-agent-tool convergence; multi-channel UX readiness); the `log/captures.md` UX-convergence forward-strategic-block entry; the `charter/current-package.md` in-flight marker; the updated brief at `briefs/p13/s45.md`.
  - Commit 2 (`feat`, b5e543f) messaging domain layer — Message aggregate plus MessageDirection/MessageChannel/MessageStatus enums — plus the `layers-messaging` import-linter contract and `contexts.messaging.*` added to the cross-context independence, domain-purity, and vendor-confinement contracts.
  - Commit 3 (`feat`, b5f9721) MessageRepository and MessageDeliveryPort ports, query filters, the Postgres adapter, Alembic 0019 (messages table per-tenant), twelve tenant-isolation contract scenarios.
  - Commit 4 (`feat`, 5c05874) Twilio and LocalEcho delivery adapters plus the `twilio-confined` and `twilio-confined-apps` import-linter contracts; messaging gained its own `pyproject.toml` and workspace registration to scope the twilio vendor dep.
  - Commit 5 (`feat`, 07a3a19) the four messaging use cases, the four messaging permissions (operator role 8 → 12), and the MessagingSettings config class.
  - Commit 6 (`feat`, 05f8259) Alembic 0020 extending intake_source with WHATSAPP_INBOUND plus the IntakeSource enum value and the intake adapter's payload-dispatch widening.
  - Commit 7 (`feat`, 6cba23e) the `record_intake_and_record_inbound_message` orchestration at the intake context plus the `MessageWriter` consumer port.
  - Commit 8 (`feat`, 7cf4295) `shared_kernel/conversation_flow.py` (ConversationFlow Protocol plus five value objects) and `shared_kernel/structured_output.py` (the structured-output discipline).
  - Commit 9 (`feat`, 8161ce5) the inference adapter's additive `generate_structured` method implementing StructuredOutputPort.
  - Commit 10 (`test`, ccbaabc) the ConversationFlow and StructuredOutputPort contract harnesses.
  - Commit 11 (`feat`, a3c2292) the four messaging HTTP routes including the Twilio webhook receiver, the DTOs, query parser, `_messaging_errors.py`, `_messaging_wiring.py`, and the composition-root wiring.
  - Commit 12 (`docs`, c068c87) the live-stack smoke at `docs/smoke/p13_s45_messaging_substrate.md`.
  - Commit 13 (`fix`, 5cb4df9) corrected two pre-existing tests that hard-assert the operator permission set — the commit-5 8 → 12 change broke them, surfaced at the session-close full run.
  - Commit 14 (this commit, `docs`) the session log entry plus the close marker.

- Decisions: D129, D130, D131 landed at commit 1. D129 references the existing D119 for the WhatsApp channel and Twilio vendor commitments rather than restating them — the central pre-write-reconciliation correction (the original brief's "D119 messaging substrate" collided with D119's existing WhatsApp-via-Twilio decision; the updated brief reassigned to D129/D130/D131). D131 commits an architectural posture (provenance-aware response composition) that no Phase 2-A surface exercises; the substrate-depth-overrides-YAGNI justification is on the record for the Phase 3 close audit.

- Tests: `tests/unit` plus `tests/contract` green at session close — 1488 passed, 183 skipped, pytest exit 0. The skips are the docker-dependent tenant-isolation suites plus the five ConversationFlow contract scenarios (parametrised over the empty registry — no implementer at S45). Roughly 104 new tests across the S45 surface: 14 Message domain, 12 messaging tenant-isolation (skip-without-docker), 18 delivery-adapter, 10 messaging use-case, 5 inbound-orchestration, 15 shared-kernel (ConversationFlow plus structured-output), 10 inference structured-output, 12 contract-harness, 7 messaging HTTP integration, plus the intake-source domain test. The two pre-existing environmental failures carried from S44a/S44b did not recur in this docker-less run.

- Enforcement layer: import-linter grew from 29 to 32 contracts — `layers-messaging` (the hexagonal layer order for the new context) plus `twilio-confined` and `twilio-confined-apps` (the vendor-SDK confinement pair mirroring litellm/langfuse/neo4j; `import twilio` resolves only inside `contexts/messaging/adapters/outbound/twilio/`). `contexts.messaging.*` was added to the three cross-context independence contracts, the domain-purity contract, and the litellm/langfuse/neo4j confinement source lists. AST enforcement (`test_dockerfile_workspace_coverage`) holds — messaging is a workspace member with its Dockerfile COPY directive. The orchestration placement at the intake context (D127 alternative (d)) keeps the cross-context contracts clean: the `MessageWriter` consumer port at `contexts/intake/application/ports/` imports nothing from `contexts/messaging`.

- Pre-write reconciliation: the eight surfaces were verified in the prior framing cycle and dispositioned into the updated brief (SMS → WhatsApp; D-numbers → D129/D130/D131; orchestration at intake per D127(d); import-linter count → 32; CLI deferred; DTO/query paths → `apps/api/routers/`; the `Message` name reused across bounded contexts; `twilio-confined` pair added). Session-open verification found the working tree clean and HEAD unchanged at `e322dc4` — nothing had shifted since the disposition conversation. One new finding surfaced mid-build: the twilio SDK was neither installed nor in the lockfile, so messaging became the first context since retrieval_evaluation to carry its own `pyproject.toml` — the per-context manifest is the vendor-dep-scoping mechanism, and messaging genuinely needs to scope `twilio` (optimization/portfolio/intake skipped a manifest because they added no new third-party dep). One path correction: the brief said `charter/captures.md`; the file is `log/captures.md`.

- methodology (line 1): **Brief-vs-existing-D-entry drift as a distinct reconciliation class.** The original S45 brief said "SMS" throughout and proposed "D119" for the messaging substrate — but D119 was already the WhatsApp-via-Twilio channel decision, and the P13 epic commits WhatsApp. This is neither brief-path-drift (file naming) nor brief-vs-domain-model drift (type identifiers) nor as-built-versus-as-framed drift — it is the brief contradicting an *existing committed D-entry*. The mitigation is the same pre-write reconciliation discipline but the surface is the decisions.md tail: a brief that names a D-number or a channel/vendor must reconcile against the actual decisions.md state. Caught cleanly in the prior cycle; the updated brief absorbed the correction. Second-instance evidence for the framing-conversation pre-write reconciliation methodology candidate at a new substrate (decisions.md as the reconciliation surface).

- methodology (line 2): **The new protocol-selection principles did load-bearing analytical work at first application.** D129's protocol disposition — direct Twilio Python SDK at the messaging adapter, Twilio MCP server rejected on Alpha-stage vendor-readiness with a named re-evaluation trigger — maps directly onto principles.md lines 22-23 (scenario-driven protocol selection; architectural commitments evolve). The principle supplied the analytical frame: "service-to-service runtime integration defaults to vendor SDK, modulated by vendor readiness." Without it the disposition would have read as an ad-hoc justification; with it, it is a principled application carrying an explicit re-evaluation trigger. First-instance evidence that principles.md lines 22-23 produce real reasoning value, not ceremony.

- methodology (line 3): **The intake-canonical commitment generalised cleanly across context-pairs.** D128's second-instance test was whether the intake-then-X orchestration shape holds when the downstream context is not portfolio. `record_intake_and_record_inbound_message` is a field-for-field analogue of `record_intake_and_create_case` — same dual-decorator shape, same `record_intake`-then-consumer-port structure, same `intake_id` propagation — with a `MessageWriter` consumer port replacing `PortfolioWriter`. D127 alternative (d) pre-settled the placement, so the "critical framing question" the original brief posed was already charter-resolved. The consumer-port pattern is altitude- and context-pair-agnostic.

- methodology (line 4): **A permission-set change ripples to every enumeration site — a type-altitude drift the build did not catch until the session-close run.** Commit 5 grew the operator authorisation_set from eight to twelve but did not update the two tests that hard-assert the exact set; the full `tests/unit`-plus-`tests/contract` run at session close surfaced both failures (commit 13 corrected them). This is a variant of the S44a brief-vs-domain-model drift candidate at the value altitude: a change to an *enumerated set* (the operator's permissions) must update every site that asserts the set's membership, and a grep for the changed constant's siblings at change time would have caught it. The discipline addition: when a use case or policy adds to an enumerated collection, grep for existing exact-set assertions before the commit closes.

- methodology (line 5): **Vendor-SDK-plus-local-development-adapter as a named pattern.** The Twilio adapter (production) plus the LocalEcho adapter (local-first development, no credentials) behind one `MessageDeliveryPort`, selected by `MESSAGING_ADAPTER`, parallels the LiteLLM-gateway-plus-Ollama-default precedent (D4/D15) and the structural shape of vendor-flexibility (D27/D30). The pattern: a vendor adapter and a local-first adapter implementing the same port, with configuration selecting between them, so the local-first principle holds without the vendor account. First-instance naming; the recurrence test fires at the next external-vendor integration (P14 calendar/email).

- Reflection — LocalEcho substrate-depth (brief prompt 5): the LocalEcho adapter carries unit-test coverage and is the configured `MESSAGING_ADAPTER` default; the live-stack smoke names it the default development path at stage 2. Whether it is exercised in a genuine operator dev cycle (the laptop stack without a Twilio account) awaits the operator running the smoke — docker was not reachable from the build environment. The adapter is not inert: it is the default selection and the messaging HTTP integration tests run the send path against a delivery double, so the LocalEcho substrate is load-bearing for local development rather than speculative.

- Reflection — D131 first-instance accountability (brief prompt 6): no Phase 2-A surface implements a ConversationFlow consumer, so D131 (provenance-aware response composition) commits an architectural posture S45 does not exercise. Recorded honestly: the substrate-depth-overrides-YAGNI commitment is on the record; first exercise lands at P14 when the audit-conversation and mirror-conversation ConversationFlow implementers land; verification is a Phase 3 close audit input.

```
metrics:
  classification: new-context substrate plus shared-kernel primitives
  brief_started: 2026-05-22
  session_started: 2026-05-22
  session_closed: 2026-05-22
  merged: 2026-05-22
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: D129; D130; D131; charter/architecture.md; charter/schema.md; charter/packages/p13-epic.md; charter/deferred-decisions.md; log/captures.md; charter/current-package.md; briefs/p13/s45.md
  corrects:
  corrected_by:
```

---

## 2026-05-22 — Charter principles addition — protocol selection plus evolution
roles: technical writer
mode: strategic (charter commit; no code changes; strategic-conversation outcome appending two principles to `charter/principles.md`)

Strategic-conversation outcome from the Phase 2-A S45 framing approach. Two new principles append to the Architectural section of `charter/principles.md`, extending the existing line 21 meta-position (committed standards require demonstrated cross-vendor consolidation). The first makes protocol selection scenario-driven and vendor-readiness-modulated: agent-to-tool integration defaults to MCP, service-to-service runtime integration defaults to vendor SDK, cross-vendor LLM abstraction is via LiteLLM, observability is via OpenTelemetry — and vendor readiness at the chosen protocol determines whether the scenario's architectural default is the actual framing-time disposition. The second makes architectural commitments explicitly evolving rather than permanent, and names the operational commitment — ongoing port-level verification (contract harnesses, configuration-driven swap tests, vendor-agnostic integration evidence) — that keeps "built to evolve" real rather than aspirational. The conversation surfaced from the S45 messaging substrate's MCP-versus-direct framing question; the Twilio MCP server's Alpha status and AI-coding-agent design orientation made the case for direct vendor SDK at the messaging adapter, which generalised into the two principles. S45 framing inherits the principles as reference points for the Item 1 protocol-choice surface.

- Produced: One commit (`docs(charter)`, this commit) — two new principles appended to the Architectural section of `charter/principles.md` immediately after the line 21 protocol-commitments bullet; this session log entry. The commit is referenced by its conventional-commit message rather than a SHA per the same-commit-self-reference discipline (pre-Step-7 hygiene, 2026-05-20).
- Decisions: No new D-entries. The principles extend the existing line 21 meta-position; they make the scenario-driven default and the evolution discipline explicit without selecting between alternatives.
- Tests: None. Documentation-only change.

---

## 2026-05-22 — Pre-S45 hygiene — Revisable Protocol contract-test harness
roles: engineer, technical writer
mode: hygiene session (charter-vs-code-honesty; closes the load-bearing finding LSP1 from the pre-S45 principles assessment with a contract-test harness; test plus docs commit prefixes per the hygiene-session shape; no new D-entries)

Closes the one load-bearing finding of the pre-S45 principles assessment: the Revisable Protocol contract-test harness that D114, D125, and `charter/architecture.md` line 274 all assert exists but never landed. `tests/contract/revisable/` now carries a parametrised conformance harness; `contexts/portfolio` DataPoint is registered as the first implementer; the three charter surfaces that claimed CI-enforceable conformance are now accurate.

- Produced: Five commits closed the session.
  - Commit 95c2b2e (`docs(p13/pre-s45-harness)`): the charter touchpoint — `charter/phase-2-audit-inputs.md` (the Revisable harness entry marked resolved; `apps/cli/_cross_context.py` added to the close-hygiene list per assessment finding SRP3); the `charter/current-package.md` in-flight marker; `briefs/p13/pre-s45-revisable-harness.md` preserved.
  - Commit 66f6a09 (`test(contract)`): the harness scaffold — `tests/contract/revisable/__init__.py` and `conftest.py` carrying `RevisableImplementerFixture`, the `register_revisable_implementer` mechanism, and the `pytest_generate_tests` hook.
  - Commit da3bf5d (`test(contract)`): the five parametrised contract scenarios at `test_revisable_contract.py`.
  - Commit cb32e53 (`test(contract)`): `test_data_point_revisable.py` registering DataPoint as the first implementer.
  - Commit this commit (`docs(p13/pre-s45-harness)`): this session log entry; the current-package close marker.

- Decisions: No new D-entries. The session closes existing charter commitments (D114, D125, `architecture.md` line 274) by landing the artefact they assert; it makes no new architectural commitment.

- Tests: 6 new tests at `tests/contract/revisable/` — the five conformance scenarios (signature, return type, append-only, ordering, genesis) parametrised over `portfolio.DataPoint`, plus a `@runtime_checkable` sanity test. `tests/unit` and `tests/contract` run green (pytest exit 0). The harness adds only test files — no production code changed — so the integration and e2e tiers are structurally unaffected; the two pre-existing environmental failures in `contexts/methodology` and `contexts/tools` carried from S44a/S44b are unrelated and unchanged. 29 of 29 import-linter contracts kept (the harness adds no import-direction surface); AST enforcement unaffected.

- Enforcement layer: The harness *is* the new enforcement layer. Before this session, Revisable conformance was verified only by `tests/unit/shared_kernel/test_revisable.py` unit tests plus the `@runtime_checkable` decorator — which checks method *names* only. `tests/contract/revisable/` now runs five behavioural conformance scenarios against every registered implementer at every test cycle. The registration mechanism (`register_revisable_implementer` plus the `test_*_revisable.py` glob in `conftest.py`) is the extension surface: P14's methodology-application revision and a later Case-level revision register through it with no harness modification.

- Pre-write reconciliation: Five surfaces verified before the scaffold drafted; all five were mechanical absorptions — none surfaced a structural-honesty finding. Surface 1: `tests/contract/` carries `http/` and `tenant_isolation/` only, unchanged since the principles assessment. Surface 2: the Revisable Protocol's `revise(change, actor) -> Revisable[RevisionT]` and `revision_history() -> list[RevisionT]` signatures and the append-only docstring semantics re-read; what `@runtime_checkable` does not cover (signature shape, return type, behaviour) named before the scenarios drafted. Surface 3: `DataPoint.revise` carries the `intake_id` optional-with-default third parameter per LSP2 — the signature scenario was written to permit exactly that. Surface 4: contract-test convention is a package directory with `__init__.py`, optional `conftest.py`, flat `test_*.py` — matched. Surface 5: file topology budget — `conftest.py` 84 lines, `test_revisable_contract.py` 116, `test_data_point_revisable.py` 75, all within the brief's targets.

- Reflection prompts answered:

  1. *Pre-write reconciliation outcomes.* All five surfaces were mechanical absorptions — the brief's framing matched the codebase at every surface, which is the expected shape for a tightly-scoped hygiene session drafted one session after the principles assessment that surveyed the same ground. The one surface that carried design weight was surface 3: confirming `DataPoint.revise`'s `intake_id` extension shape before the signature scenario drafted, so the scenario verifies the LSP-compatible optional-extension rule rather than a rigid two-parameter equality.

  2. *What `@runtime_checkable` does not cover that the harness now does.* `@runtime_checkable` permits `isinstance(x, Revisable)` but verifies only that the named methods (`revise`, `revision_history`) *exist* — not their signatures, return types, or behaviour. The harness fills each gap with a named scenario: the **signature scenario** verifies `revise` presents the `(change, actor)` shape with any extra parameter carrying a default; the **return-type scenario** verifies `revise` returns the same concrete class (the covariant narrowing of `Revisable[RevisionT]`); the **append-only and ordering scenarios** verify the semantic core — `revise` appends exactly one revision without mutating the original, and sequential revises append at the tail in call order; the **genesis scenario** verifies a fresh instance carries a stable genesis revision at index 0. The `@runtime_checkable` membership check itself is retained as the sanity test in `test_data_point_revisable.py`, so the harness layers behavioural verification on top of structural membership rather than replacing it.

  3. *Cross-reference to LSP1 and LSP2.* LSP1 — the load-bearing finding of `audits/p13-principles-baseline.md` — is closed in code at this session: the codebase's one cross-context architectural-primitive Protocol now carries CI-enforceable conformance, and the charter surfaces that asserted it (D114, D125, `architecture.md` line 274) become accurate. LSP2 — marginal — observed that `DataPoint.revise` extends the Protocol signature with `intake_id`; the signature scenario verifies this extension is contract-compatible (the parameter is optional with a default, so the Protocol's two-argument call stays valid), converting LSP2's prose analysis into an executable check that every future implementer's signature extensions must also pass.

  4. *Discipline observation.* The harness converts Revisable's verification from aspirational to CI-enforceable: before this session, three charter surfaces asserted "CI-enforceable conformance via contract tests" while the only enforcement was unit tests plus a name-only `@runtime_checkable` decorator — the charter claimed more than the tree delivered. This is the first instance of the CI-enforceable-versus-aspirational pattern the principles assessment named receiving a closure. Whether the pattern (a charter claim of mechanical enforcement should be backed by the named artefact, and a missing artefact is a charter-vs-code honesty gap) earns methodology-document promotion evaluates at Phase 2-A close per the disposition; this entry records the first-instance evidence without promoting it, per the build-versus-strategic discipline.

- methodology (line 1): **A charter claim of mechanical enforcement is a charter-vs-code honesty gap until the named artefact exists.** D114/D125/`architecture.md`:274 asserted "CI-enforceable conformance via contract tests at `tests/contract/revisable/`" for two sessions while the directory did not exist. The principles assessment caught it as a code-altitude finding; this session closed it. First-instance closure of the CI-enforceable-versus-aspirational pattern; promotion evaluates at Phase 2-A close.

- methodology (line 2): **A contract-test harness verifies what `@runtime_checkable` cannot, and the gap is large.** `@runtime_checkable` verifies method names only; signature shape, return type, and the append-only/ordering/genesis semantics all needed explicit scenarios. The Protocol-surface-only discipline (scenarios touch `revise` and `revision_history`, never implementer internals) is what keeps the harness valid for P14's future implementers without modification.

- **Pre-S45 Revisable harness closed; the load-bearing finding LSP1 from the principles assessment is resolved in code. `tests/contract/revisable/` carries a parametrised conformance harness, `contexts/portfolio` DataPoint is verified against it, and the registration mechanism is ready for P14's methodology-application revision implementer. The remaining material findings from `audits/p13-principles-baseline.md` (the cross-context structural-duplication cluster, the composition-layer file splits including the newly-listed `_cross_context.py`, the YAGNI baseline-evidence forward-pointers) stay queued for Phase 2-A close hygiene per the disposition. S45 — the messaging substrate and conversational protocol per the p13-epic Session 3 line — is next.**

---

## 2026-05-22 — Pre-S45 hygiene — Software engineering principles baseline assessment
roles: analyst, architect, technical writer
mode: strategic (post-substrate-completion assessment; closes no in-flight build work; produces an evidence document that informs the subsequent disposition conversation; no code, test, or enforcement-layer changes)

The first code-altitude software-engineering-principles audit the project has run. It assesses the Phase 2-A codebase against KISS, DRY, the five SOLID letters individually, YAGNI, and Tell Don't Ask, and lands a findings document at the new `audits/` directory. It is an evidence document, not a verdict; disposition of findings happens at the subsequent disposition conversation.

- Produced: Three commits closed the session.
  - Commit e66059c (`docs(audits)`): the findings document at `audits/p13-principles-baseline.md` (a new top-level directory) plus `briefs/p13/pre-s45-principles-assessment.md` preserved at v1.
  - Commit 0c7cc07 (`docs(charter)`): three methodology-observation entries appended to `log/captures.md`.
  - Commit this commit (`docs(p13/pre-s45-principles-assessment)`): this session log entry; the current-package close marker.

- Decisions: No new D-entries. Per assessment-shape discipline — the session produces an evidence document; architectural decisions and dispositions belong to the subsequent conversation.

- Tests: None. No edits under `contexts/`, `apps/`, `shared_kernel/`, `padhanam/`, `tests/`, or migrations. The assessment ran `import-linter` (29/29 kept) and a throwaway stdlib-`ast` cyclomatic-complexity sampler; it did not re-run the pytest suite.

- Pre-write reconciliation: Eight surfaces verified before assessment work began.
  - Surface 1 (output path): no `audits/` directory existed; `charter/p12-audit-findings.md` is the package-altitude precedent but lands *in* `charter/`, which collides with the brief's AC 5 (no charter changes beyond captures and session log). Resolved to `audits/p13-principles-baseline.md` — the brief's own lead candidate — a new directory that keeps the cross-cutting assessment off the charter surface.
  - Surface 2 (substrate-depth D-entry inventory): D124–D128 are all Kano must-have; D126, D127, D128 carry explicit cost-now-versus-cost-later / cost-of-pivot substrate-depth reasoning. That trio is the YAGNI assessment's primary substrate; D124 is the base substrate and D125 inherits D114.
  - Surface 3 (tools): `import-linter` and `pytest` installed; `ruff`, `mypy`, `radon` **absent**. No operator approval sought to install tooling per the brief. The CC dimension was covered with a stdlib-`ast` sampler; `mypy` type-coverage and `ruff` lint-surface remain named measurement gaps in the findings document's method section.
  - Surface 4 (import-linter): 29 contracts, 29 kept, 0 broken — matches S44b's 29/29.
  - Surface 5 (file inventory): 441 non-test code files; 24 at ≥400 lines, 9 at ≥600, 5 at ≥800. Buckets recorded in the findings document.
  - Surface 6 (audit precedent): `charter/p12-audit-findings.md` uses a three-section + cross-reference-index + verdict shape; the principles assessment is principle-organised instead (cross-cutting, not finding-type-organised) but matches the evidence-citation and per-finding-disposition conventions.
  - Surface 7 (methodology overlap): the file topology budget ↔ SRP; pre-write reconciliation and the substrate-application boundary check ↔ KISS/DRY at the discipline altitude; the consumer-port pattern ↔ DIP. Overlaps cited in the findings document, not duplicated.
  - Surface 8 (no conflicting work): working tree clean at session open; `current-package.md` showed "S44b closed; S45 next" and had not advanced. One mechanical correction: the brief's context-reading item 7 cited `charter/captures.md`; the file lives at `log/captures.md` (the brief's own commit structure cited it correctly).

- Reflection prompts answered:

  1. *Pre-write reconciliation surface outcomes.* Of the eight surfaces, two were structural-honesty findings (surface 1's path collision with AC 5, resolved by the new `audits/` directory; surface 8's `charter/captures.md` vs `log/captures.md` mechanical correction), three were tools-and-paths verifications (surfaces 3, 4, 6), and three were mechanical absorptions feeding the assessment (surfaces 2, 5, 7). No surface blocked the assessment; surface 1 was the only genuine disposition and the brief pre-resolved it by naming `audits/p13-principles-baseline.md` as the lead candidate. Surface 3 is the one carrying forward weight — the absence of `ruff` and `mypy` means type-coverage and lint-surface were not measured, named as gaps for a future principle audit.

  2. *Charter-altitude versus code-altitude finding deltas.* Three relationships. **Aligned:** the file topology budget findings (`_errors.py`, `_agent_runtime_wiring.py`) and the import-linter contracts holding (29/29) — the code-altitude pass confirmed what the discipline already tracked. **Code-altitude found what charter-altitude could not:** `apps/cli/_cross_context.py` at 1704 lines (the largest file in the codebase, on no hygiene list, in no charter document); the `if "tenant" in str(exc)` exception-message-sniffing repeated ten times; the audit-event-draft inline-versus-helper drift between two sibling contexts one session apart. **Code-altitude found the charter overstates:** three charter surfaces (D114, D125, `architecture.md:274`, `schema.md`) assert a Revisable contract-test harness at `tests/contract/revisable/` that does not exist. On the specific orchestrations-DRY question the charter-altitude audit flagged as unverifiable: the code-altitude assessment surfaced a **different pattern** than feared — the three intake orchestrations are thin (~25 lines each) with only a ~7-line shared prologue, mild duplication consciously kept per D127(d); the real DRY weight is not in the orchestrations at all but in the cursor-codec / pagination / audit-draft / wiring cluster (the CP1 cross-principle finding).

  3. *Most surprising findings.* The single most surprising: `_cross_context.py` at 1704 lines — nearly double any file the brief's SRP watch-points named, and tracked nowhere. The brief's watch-points were drawn from session-log findings, so a file no recent session touched stayed invisible to charter-altitude framing. Second: `_errors.py` grew from the 729 lines the phase-2-audit-inputs entry recorded (S44a) to 828 — the very session after it was flagged for splitting added ~100 lines of intake/portfolio handlers to it. Confirming what the discipline already knew: the Phase 2-A code-altitude simplicity is genuinely strong (82 of 87 functions at CC ≤ 5), the import-linter contracts hold, and the file topology budget is doing real work — the surprises were all in the `apps/` composition layer, which no file-topology budget governs.

  4. *Severity classification balance.* The distribution skews toward the lighter end: 1 load-bearing, 9 material (plus 1 Phase-1-scoped), 12 marginal. The single load-bearing finding (LSP1, the absent Revisable contract-test harness) is already a tracked phase-2-audit-inputs entry, so the substrate's structural integrity is not in question — the load-bearing finding is a verification gap, not a structural breach. The material cluster is dominated by two things: composition-layer SRP (three over-800-line files) and the cross-context DRY cluster (cursor codec et al.). The marginal tail is mostly conscious, charter-reasoned trade-offs (D127(d) orchestration placement, S44a boundary-visibility, aggregate-cohesion ports). The shape says the substrate is sound and the debt is concentrated, addressable hygiene — which is the expected shape for a codebase this far into disciplined incremental build, not a skew in either alarming direction.

  5. *Methodology observations from the assessment process.* Three, all captured at `log/captures.md` this session. The per-principle section structure (one section per principle) forced roughly four genuinely cross-principle findings to be split and cross-referenced rather than stated once — a principle is a lens, not a partition. The quantitative metrics were load-bearing for KISS, SRP, and DIP and thin-to-noise for the other four principles — a uniform metrics-then-findings template over-promises measurability for DRY, OCP, LSP, ISP, YAGNI, and TDA. And the code-altitude pass surfaced a class of finding charter-altitude inference structurally cannot reach (un-charted structure plus charter overclaim). The brief format mostly worked — its watch-points were accurate and its pre-resolution of the output-path question was correct — but its SRP watch-points inherited the session-log blind spot that hid `_cross_context.py`.

  6. *Disposition forward-pointers.* The post-assessment disposition conversation opens with this queue. Hygiene-item candidates: the Revisable contract-test harness (LSP1, also closes a charter-vs-code honesty gap); the cursor-codec / pagination shared-primitive extraction (D1+D2, which also asks the disposition to revisit P12 Finding B5's now-strained non-action); the audit-event-draft helper (D3); the per-tenant-routing adapter primitive (D4); the typed bound-tenant exception replacing the message-sniffing (D7); and `apps/cli/_cross_context.py` added to the composition-layer split list alongside the already-listed `_errors.py` and `_agent_runtime_wiring.py` (SRP1–3). Forward-to-Phase-3-audit: the three inert forward-compat affordances (Y1 `role_list`, Y2 `certainty`, Y3 the `Revisable` Protocol) as YAGNI baseline evidence — this is the queue the held YAGNI-monitoring draft text addresses. Methodology candidates: the three captures entries. Cross-principle CP1 (structural shapes reimplemented per context) is the disposition's highest-value single thread; CP2 (SRP debt concentrates in the composition layer) motivates considering whether the file-topology budget should extend to `apps/`.

- methodology (line 1): **Code-altitude assessment surfaces findings charter-altitude inference structurally cannot.** The first code-altitude principle audit found un-charted structure (`_cross_context.py` at 1704 lines, on no list) and charter overclaim (the Revisable harness three charter surfaces assert but the tree does not carry). Charter-altitude auditing verifies decisions against intent; it cannot catch what was never written down or audit a claim against its absent artefact. First-instance observation; captured at `log/captures.md`.

- methodology (line 2): **A principle is a lens, not a partition.** The per-principle audit structure forced cross-principle findings to be split and cross-referenced; future principle audits may be better served finding-first with the principles as tags. Paired with the observation that metrics are load-bearing for only three of the seven principles. Both captured at `log/captures.md`.

- methodology (line 3): **The brief format held, with one inherited blind spot.** The brief's watch-points and its pre-resolution of the output-path question were accurate; its SRP watch-points, drawn from session-log findings, inherited the session log's blind spot — the largest file in the codebase was invisible because no recent session touched it. Watch-points sourced from session logs see what sessions touched, not what sessions left alone.

- **Pre-S45 principles assessment closed; the findings document at `audits/p13-principles-baseline.md` carries one load-bearing, nine material, and twelve marginal findings plus four cross-principle patterns, all evidence-anchored. The substrate is sound; the debt is concentrated, addressable hygiene in the `apps/` composition layer and the cross-context structural-duplication cluster. Disposition belongs to the subsequent conversation. S45 — the messaging substrate and conversational protocol per the p13-epic Session 3 line — is next.**

---

## 2026-05-22 — S44b — Intake context substrate plus write-side HTTP plus intake-canonical orchestration
roles: analyst, PM, architect, engineer, technical writer
mode: build (P13 Wave 1 fourth build session; the write-path substrate and transport, planned-bridge sub-variant)

metrics:
  classification: substrate session (new `contexts/intake/` bounded context plus write-surface transport; D127/D128)
  brief_started: 2026-05-22
  session_started: 2026-05-22
  session_closed: 2026-05-22
  merged: 2026-05-22
  close_state: clean (12 commits; the brief's eleven-commit plan grew to twelve at pre-write reconciliation)
  tests_passing: yes (S44b surface green; 1746 passed in the full unit+contract+integration run; the two pre-existing environmental failures in contexts/methodology and contexts/tools are carried from S44a and unrelated to S44b)
  principles_intact: yes
  charter_touchpoints: charter/decisions.md (D127, D128); charter/schema.md (Intake context substrate section); charter/packages/p13-epic.md (Session 2b line); charter/current-package.md; charter/phase-2-audit-inputs.md (two close-hygiene entries); briefs/p13/s44b.md
  corrects:
  corrected_by:
  paired_with: S44a

S44b lands the `contexts/intake/` bounded context and the intake-canonical write surface per D127/D128: IntakeRecord as the canonical-entry aggregate, three standalone use cases, three orchestration use cases composing intake with a portfolio write, write-side HTTP for both the portfolio and intake surfaces, and the CLI write path. The session opened with an eight-surface pre-write reconciliation (the seven from the brief plus the operator-added cross-context-contract verification surface) that reshaped the orchestration design before any code — the load-bearing event of the session.

- Produced: Twelve commits closed the session.
  - Commit eda60de (`docs(p13/s44b)`): D127 and D128; the p13-epic Session 2b line; the schema.md Intake context substrate section; the current-package transition; `briefs/p13/s44b.md` preserved with a pre-write reconciliation annotation.
  - Commit bd0f9b1 (`feat`): the `contexts/intake/` domain layer — IntakeRecord, IntakeSource, ManualEntryPayload, the IntakePayload alias — plus the hexagonal skeleton and the `layers-intake` import-linter contract.
  - Commit 0993e20 (`feat`): the IntakeRepository port, the Postgres adapter, Alembic 0017, and 11 tenant-isolation contract tests.
  - Commit a003749 (`feat`): the three standalone use cases (record_intake, get_intake, list_intakes), the three intake permissions, and the intake audit-event helper.
  - Commit 429ec7f (`feat`): Alembic 0018 plus `intake_id` on Case and Assertion; the portfolio use cases and adapter thread it through.
  - Commit ff16b2d (`feat`): the `PortfolioWriter` consumer port and the create-case and revise orchestrations.
  - Commit a9a89db (`feat`): the `record_intake_and_create_data_point` orchestration.
  - Commit affa966 (`feat`): the write-side portfolio HTTP routes, `_portfolio_write_dto.py`, and the `_intake_wiring.py` wiring adapters.
  - Commit 181f86e (`feat`): the intake HTTP routes, `_intake_dto.py`, `_intake_query.py`, and the intake error handlers.
  - Commit 1d0ccf4 (`feat`): the CLI write path via the orchestrations.
  - Commit 190c346 (`docs`): the eight-stage live-stack smoke.
  - Commit this commit (`docs(p13/s44b)`): this session log entry; the current-package close marker; five captures entries; two `charter/phase-2-audit-inputs.md` close-hygiene entries.

- Decisions: D127 (intake context substrate plus the three orchestration use cases on a consumer-defined `PortfolioWriter` port) and D128 (intake-canonical commitment as a cross-context architectural posture, scoped to the platform's write surfaces). Both landed at commit 1, reshaped at pre-write reconciliation from the framing-time brief: D127 gained alternative (e) (the rejected direct cross-context import) and grew from two orchestrations to three; D128 dropped the "single transaction" claim for two-transaction intake-first ordering and rescoped from "every persisted state change" to "every persisted state change at the platform's write surfaces." Kano: both must-have.

- Tests: roughly 90 new tests across the intake domain, persistence, use cases, orchestrations, the portfolio intake_id surface, and the two HTTP write surfaces. The full unit+contract+integration run is 1746 passed (+61 from S44a's 1685), 17 skipped. 29 of 29 import-linter contracts kept; AST enforcement green. The two pre-existing environmental failures (`contexts/methodology` lvt round-trip; `contexts/tools` `test_list_roles_using_tool_returns_empty_pre_commit_4`) are carried unchanged from S44a — both fail in isolation against the live control-plane DB state, and S44b touched zero files in either context's dependency closure.

- Enforcement layer: One new import-linter contract — `layers-intake` (the hexagonal layer order for the new context). The three cross-context independence contracts, `domain-purity`, and the three SDK-confinement contracts each gain the intake context: 29 contracts kept. The cross-context independence contract did load-bearing work this session — it is the contract the brief's orchestration design violated, and the consumer-port resolution keeps `contexts.intake.application` independent of `contexts.portfolio.application` (verified at 29/29). No AST enforcement change — the intake context carries no per-context `pyproject.toml`, so `COPY contexts ./contexts` covers it. The intake-canonical enforcement layer S44b adds: the orchestrations' dual `@requires_authorisation` decorators (fail-fast on both the intake and portfolio permissions before any write), and the `intake_id` foreign key with ON DELETE RESTRICT on `cases` and `assertions`.

- Pre-write reconciliation: Eight surfaces verified before commit 1. Surfaces 1 and 7 were clean (D-number sequence; the type-identifier search found no IntakeRecord / IntakeSource / ManualEntryPayload / intake_id collisions). Three surfaces surfaced structural findings that reshaped the build, all surfaced to the operator as questions before any code: Finding 1 — the brief's orchestrations directly importing `contexts/portfolio/application/` violate the import-linter cross-context-independence contract, and AC 13 asserted the inverse of the rule; Finding 2 — `create_data_point` was an uncovered write surface, so D128's "every persisted state change" claim could not hold; Finding 3 — the brief's "single transaction across two contexts" is not achievable with the per-method-session adapter shape. Four mechanical corrections (the Assertion file path, the `_agent_runtime_wiring.py` 912-line overage, the 0018 revision-string length, D128's scope wording). The operator confirmed all dispositions and added the cross-context-contract verification as standing surface 8.

- Reflection prompts answered:

  1. *Pre-write reconciliation surfaces.* Eight surfaces; the discipline did its heaviest architectural work yet. Finding 1 is the headline: the brief inverted the cross-context import-linter rule despite the consumer-port pattern carrying 12-plus reinforcements in the codebase — the brief drafter reasoned from intent ("the orchestration belongs at the intake boundary") without checking the mechanism against the binding contract. The new surface 8 (cross-context-contract verification) is the discipline addition; it caught Finding 1 and is added standing from S44b forward. Surface 6 (file topology budget, second instance) caught `_agent_runtime_wiring.py` at 912 lines and redirected the intake wiring to a new file. The cumulative reconciliation count keeps climbing; S44b is the second consecutive session (after S44a) where reconciliation reshaped a charter decision rather than absorbing drift.

  2. *Compose-shape and ActorContext propagation at the larger surface.* The S44a compose-shape commitment held cleanly across the much larger S44b surface — the new intake context, three orchestrations, the CLI synthesis, the dual-decorator pattern all consume ActorContext, and `actor.tenant_context` extraction reads as the same deliberate boundary at every adapter call seam. The `actor.actor_id` → `ActorReference` derivation (D126's persisted-identity separation) extended naturally to the IntakeRecord's `authored_by`. Second-instance evidence that the compose-shape decision is structurally durable: it cost nothing at the larger surface and the dual-decorator stacking (two `@requires_authorisation` decorators on each orchestration) composed without friction.

  3. *File topology budget second-instance evidence.* The discipline caught a second genuine overage — `_agent_runtime_wiring.py` at 912 lines, past the brief's own 600-line trigger — and redirected the intake write-surface wiring to a new `apps/api/_intake_wiring.py`. Two instances (S44a `_errors.py` at 729; S44b `_agent_runtime_wiring.py` at 912), two redirected placement decisions. The discipline does not merely measure; it changes where new code lands. Strong P13-close promotion candidate with two-instance load-bearing evidence; the `_agent_runtime_wiring.py` split forwarded to the Phase 2-A close hygiene list.

  4. *Brief-vs-domain-model drift recurrence test.* The S44a type-altitude drift candidate's recurrence test fired at surface 7 (the type-identifier search). The search was clean — no collisions for the new intake types or the `intake_id` field. So the drift did not recur at the type-identifier altitude; the S44a candidate stays at one instance. A different brief-vs-codebase drift class surfaced instead — Finding 1's cross-context-contract inversion — captured as its own methodology candidate (the cross-context-contract verification surface), not a recurrence of the type-altitude pattern.

  5. *Forward-commitment-evaluation pattern recurrence test.* The S44a candidate's recurrence test fired at surface 2. No D-entry forward commitment was evaluated-and-superseded at S44b: D127 and D128 are new entries, not evaluations of a prior entry's forward commitment. So the forward-commitment-evaluation pattern stays at one instance (S44a's D126 superseding D124). No recurrence at S44b; the candidate holds at single-instance pending.

  6. *Orchestration-at-intake disposition validation.* Item 4 of the framing settled the orchestrations at `contexts/intake/application/`. The implementation validated the choice once the consumer-port resolution landed: the orchestration genuinely reads as originating at the intake boundary (record the intake, then drive the downstream write), and the `PortfolioWriter` consumer port made the cross-context dependency explicit and unit-testable against a fake. The friction the framing did not anticipate was purely the import-linter contract — resolved structurally, not by relocating the orchestration. The intake-canonical conceptual flow (intake is the canonical entry; downstream actions flow from it) is faithfully expressed by an orchestration that depends inward on a consumer port rather than outward on the portfolio context.

  7. *Methodology line opportunities.* Five candidates surfaced, each captured at `log/captures.md` and carried as a methodology line below. The dual-decorator-at-orchestration-boundary idiom and the orchestration naming convention (`record_intake_and_X`) are clean patterns but expected consequences of the settled D127 design, not structural novelty — not promoted, per the captures-candidate threshold.

- methodology (line 1): **File topology budget second-instance load-bearing evidence.** The discipline caught `_agent_runtime_wiring.py` at 912 lines and redirected the intake wiring to a new file — the second instance after S44a's `_errors.py`. Two-instance evidence; strong P13-close promotion candidate. Captured at `log/captures.md`.

- methodology (line 2): **Cross-context-contract verification as a pre-write reconciliation surface.** The brief inverted the import-linter cross-context-independence contract despite the consumer-port pattern's 12-plus reinforcements; even well-established patterns surface drift at brief-drafting time. Surface 8 (verify import-linter contract conformance for any new cross-context dependency) added standing from S44b. First-instance candidate; captured at `log/captures.md`.

- methodology (line 3): **Scope-completeness for D-entry architectural-posture commitments.** D128's "every persisted state change" claim overclaimed; Finding 2 caught the `create_data_point` write-surface gap. A D-entry asserting a platform-wide posture needs explicit enumeration against all currently-implemented write surfaces before its prose drafts. First-instance candidate; captured at `log/captures.md`.

- methodology (line 4): **Adapter-level transaction-semantics verification before commitment.** The brief asserted a single shared transaction the adapter shape cannot provide; Finding 3 caught it. Structural claims about transaction semantics or unit-of-work boundaries need adapter-level capability verification before a D-entry commits the claim. First-instance candidate; captured at `log/captures.md`.

- methodology (line 5): **Distinct architectural concern, not instance count, triggers a new bounded context.** D127 alternative (d)'s "third orchestration" trigger was clarified: three orchestrations of one pattern are one concern; the `contexts/orchestration/` trigger is a distinct orchestration concern. A forward-looking discipline note; captured at `log/captures.md`.

- **S44b closed; the portfolio context is complete end-to-end through both the read and write surfaces, and the intake context substrate is in place. The five S44b-framing settlements absorbed: the intake-canonical posture held (with D128 honestly rescoped), compose-shape held at the larger surface, the file topology budget did second-instance load-bearing work, the orchestration-at-intake placement validated, and the consumer-port resolution kept the cross-context boundary intact. What diverged was the brief's code-side assumptions — the cross-context import mechanism, the create_data_point coverage, the transaction semantics — each caught by reconciliation before any code landed. S45 (the messaging substrate and conversational protocol) is next per the p13-epic Session 3 line.**

---

## 2026-05-22 — S44a — ActorContext value object plus authorisation decorator at the use-case boundary
roles: analyst, PM, architect, engineer, technical writer
mode: build (P13 Wave 1 third build session; identity-and-permissions substrate)

metrics:
  classification: substrate session (shared-kernel value object plus use-case-boundary decorator; D126)
  brief_started: 2026-05-21
  session_started: 2026-05-21
  session_closed: 2026-05-22
  merged: 2026-05-22
  close_state: clean (8 commits; the brief's nine-commit plan dropped to eight at pre-write reconciliation)
  tests_passing: yes (S44a surface green; 1685 passed in the full unit+contract+integration run; two pre-existing environmental failures in contexts/methodology and contexts/tools are unrelated to S44a)
  principles_intact: yes
  charter_touchpoints: charter/decisions.md (D126); charter/schema.md (ActorContext sub-section); charter/packages/p13-epic.md (Session 2 / 2b split); charter/current-package.md; charter/phase-2-audit-inputs.md (two close-hygiene entries); briefs/p13/s44a.md
  corrects:
  corrected_by:

S44a lands the Phase 2-A actor-identity and authorisation substrate per D126: ActorContext as a request-scoped shared-kernel value object, the `requires_authorisation` decorator at the use-case boundary, and `AuthorisationDenied` translated to 403. The session opened with a six-surface pre-write reconciliation that reshaped the framing-time D126 bundle before any code landed — the load-bearing event of the session, and the reason the brief's nine-commit plan closed at eight.

- Produced: Eight commits closed the session.
  - Commit 4254c43 (`docs(p13/s44a)`): D126; the p13-epic Session 2 → Session 2 (S44a) / Session 2b (S44b) split; the schema.md ActorContext sub-section under Cross-cutting binding shapes; the current-package transition; `briefs/p13/s44a.md` preserved with a pre-write reconciliation annotation recording the operator-confirmed reshape.
  - Commit b98c226 (`feat(p13/s44a)`): the ActorContext frozen dataclass at `shared_kernel/actor_context.py` — composes TenantContext plus `actor_id`, `role_list`, `authorisation_set`; 12 unit tests.
  - Commit fa5c9cf (`feat(p13/s44a)`): the `requires_authorisation` decorator, the `AuthorisationDenied` exception, and the Phase 2-A role-to-authorisation policy (five permission constants plus the `authorisations_for_roles` resolver) at `shared_kernel/authorisation.py`; the `AuthorisationDenied` → 403 handler at `apps/api/_auth_errors.py`; 23 tests.
  - Commit e1060de (`feat(p13/s44a)`): the `get_actor_context` FastAPI dependency at `apps/api/middleware.py`; 4 tests.
  - Commit 2b8539c (`feat(p13/s44a)`): the five portfolio use cases consuming ActorContext and applying the decorator; the CLI's ActorContext synthesis; use-case tests updated plus per-use-case deny-path tests.
  - Commit 8222813 (`feat(p13/s44a)`): the portfolio router wiring `get_actor_context`; router / HTTP-contract / tenant-isolation / integration test updates plus one new isolation scenario.
  - Commit 908c88a (`docs(p13/s44a)`): the six-stage live-stack smoke at `docs/smoke/p13_s44a_actor_context.md`; the compose.yaml padhanam-api digest pin advanced to the rebuilt image.
  - Commit this commit (`docs(p13/s44a)`): this session log entry; the current-package close marker; three captures entries; two `charter/phase-2-audit-inputs.md` close-hygiene entries.

- Decisions: D126 landed at commit 1, reshaped at pre-write reconciliation from the framing-time two-sub-commitment bundle to three sub-commitments — (1) the ActorContext value object, (2) the authorisation decorator plus `AuthorisationDenied`, (3) ActorReference retention as persisted authoring identity. D126 **supersedes D124's forward commitment** that `authored_by` and the Revisable Protocol's `actor` parameter become ActorContext — the first instance of a D-entry superseding a forward commitment carried by an earlier D-entry; D124 is not rewritten, the supersession is recorded in D126. Alternative (e) was added to the analysis (replace ActorReference per D124's literal commitment, rejected on the persisted-identity-versus-request-scoped-capability conflation). Kano: must-have.

- Tests: roughly 41 new tests across the shared_kernel, apps/api, and portfolio surfaces (ActorContext 12; authorisation decorator and policy 14; auth-error handler +4; get_actor_context +4; use-case deny-path +6; one new HTTP tenant-isolation scenario), plus signature-replacement updates to the portfolio router / HTTP-contract / integration suites. The full unit+contract+integration run is 1685 passed, 17 skipped. 28 of 28 import-linter contracts kept; AST enforcement green. Two pre-existing environmental failures (`contexts/methodology` lvt round-trip; `contexts/tools` `test_list_roles_using_tool_returns_empty_pre_commit_4`) fail in isolation against the live control-plane DB state — the `contexts/tools` test asserts a pre-migration-0012 empty binding set against a control plane that has 0012's eight seeded role-tool bindings applied. S44a touched zero files in either context's dependency closure; the failures are not S44a regressions.

- Enforcement layer: No new import-linter contracts. The two new shared_kernel modules (`actor_context.py`, `authorisation.py`) are policed by the existing `shared-kernel-policed` contract — both are framework-free (stdlib plus the sibling value objects only; no Pydantic, no `contexts/` imports), confirmed at 28/28. No AST enforcement change — S44a adds no bounded context or workspace package, so no Dockerfile COPY directive is required. The authorisation enforcement layer S44a adds is the `requires_authorisation` decorator at the use-case boundary: each of the five portfolio use cases carries a `@requires_authorisation(...)` line, and the decorator is the single grep-able surface a procurement auditor uses to verify completeness of authorisation enforcement across the codebase (the Decision 7 trivial-check-at-one-boundary commitment). Future use cases in P14+ inherit the decorator; the permission policy is single-sourced at `shared_kernel/authorisation.py`.

- Pre-write reconciliation: Six surfaces verified before commit 1; one was mechanical, five surfaced findings, and the findings reshaped D126 and the commit sequence. Surface 1 (D-number) confirmed D126 mechanically. Surface 2 (TenantContext consumption) verified the five use-case signatures. Surface 3 found no `apps/api/_auth.py` exists — TenantContext is built by the `get_tenant_context` dependency in `routers/inference.py`, not by middleware. Surface 4 found `tests/contract/revisable/` does not exist. Surface 5 — the central finding — found ActorReference consumed far beyond the brief's assumed "four use cases plus the Revisable Protocol": it is the declared type of `DataPoint.authored_by` and `Assertion.authored_by` (persisted domain-entity fields, round-tripped through a single `authored_by_user_id` text column) plus the adapter, the CLI, the DTO, the audit drafts, and five test files. Surface 6 (the new file topology budget surface) caught `apps/api/_errors.py` at 729 lines. All findings were surfaced to the operator as questions before commit 1; the operator confirmed five recommendations, reshaping D126 to retain ActorReference as the persisted authoring-identity value object and introduce ActorContext as new for request-scoped capability. The cumulative reconciliation count keeps climbing; S44a is the instance where reconciliation reshaped a charter decision rather than only absorbing drift.

- Reflection prompts answered:

  1. *Pre-write reconciliation surfaces.* The six surfaces split one mechanical (D-number) and five findings. Three were structural-honesty findings that became user-question resolutions: surface 5's ActorReference consumer surface (which falsified the brief's surface-5 assumption and forced the D126 reshape), surface 3's absent `_auth.py` (which re-homed the auth-middleware work to `middleware.py` plus `get_tenant_context`), and surface 6's 729-line `_errors.py`. Two were lighter — surface 4's absent contract-test directory (deferred) and surface 2 (confirmation plus the fifth-use-case discovery). The session is the clearest instance to date of pre-write reconciliation doing architectural work: it did not merely catch drift, it superseded a charter forward commitment (D124's). The discipline's value at S44a sits at the decision altitude, not the documentation-drift altitude.

  2. *Compose-shape decision validation.* The S44-framing Item 1 settlement on compose-shape (ActorContext wraps TenantContext as a field) held cleanly. Writing the code, the `tenant_context = actor.tenant_context` extraction appears once per use case — five sites — and reads as a deliberate boundary, not ceremony: the use case receives the rich request envelope and hands the adapter exactly the narrow value object it needs. The extraction is the structural boundary made visible, as Item 1 framing claimed. The stronger validation came at surface 5: compose-shape is *why* the reshape was clean. Because ActorContext composes rather than subsumes, ActorReference could stay exactly as it was for the persisted field while ActorContext took the request-scoped role — two single-concern value objects, no refactor of the domain entities, the adapter, or the DTO. A subsume-shape ActorContext would have forced the persisted-identity problem to be solved by either growing the schema or faking a reconstruction. First-instance evidence: compose-shape is structurally honest, and it bought the clean separation that resolved finding 1.

  3. *File topology budget first-instance evidence.* The budget did catch an overage, and it was load-bearing. Surface 6 found `apps/api/_errors.py` at 729 lines — far past a ~400-line ceiling for the router-error-handler file class — and the budget table's `_errors.py` row carried no current size, so absent the verification surface the overage would have gone unseen and the AuthorisationDenied handler would have landed there (the path the brief named), growing the file further. The discipline did two things at once: it redirected the handler to `apps/api/_auth_errors.py` (also the structurally correct home per D104's auth-cross-cutting placement) and surfaced the existing overage for forward triage. The discipline's load-bearing claim sits at first-instance validation, not only at recurrence. The `_errors.py` split is forwarded to the Phase 2-A close hygiene list; promotion of the discipline to the methodology document evaluates at P13 close per the framing settlement.

  4. *Framing-conversation pre-write reconciliation methodology observation.* The brief inherited D124's `authored_by`-becomes-ActorContext forward commitment without verifying it against the live domain model — the brief's surface-5 framing searched for ActorReference at the application-layer altitude only. The framing-conversation reconciliation candidate (framing conversations verify forward commitments against the live charter and codebase state before decomposing work) is reinforced here at a sharper edge: the thing that needed verifying was not charter prose but a *forward commitment's structural soundness against the persistence layer*. The mitigation surface is the same — read the live state before drafting the decomposition — but the lesson is that a D-entry's forward commitment is a hypothesis, not a settled instruction. Captured as a distinct methodology candidate (the forward-commitment-evaluation pattern; see methodology line 1).

  5. *Substrate-application boundary check.* The S41 substrate-application boundary candidate fired cleanly at S44a between commits 3 and 4. Commits 1-3 closed the shared_kernel additions (the value object, the decorator, the policy) — substrate that imports nothing from `contexts/` or `apps/`. Commits 4-6 opened the existing-code modifications (the middleware dependency, the five use cases, the router). The transition surfaced no scope drift: the substrate landed exactly as the reshaped D126 specified, and commits 4-6 were signature-and-wiring changes within the framing-time projection. The boundary is a genuine shape-change point — substrate is additive and self-contained; application work is modification with a blast radius — but at S44a it was a clean checkpoint, not a re-scoping moment. Recurrence holds the candidate at its existing threshold.

  6. *Methodology line opportunities.* Three candidates surfaced, each captured at `log/captures.md` and carried as a methodology line below. The compose-shape extraction idiom (`actor.tenant_context` at the adapter call seam) is a clean pattern but is the expected consequence of a settled framing decision, not structural novelty — not promoted, per the captures-candidate threshold. The authorisation-keys-must-match concern the brief flagged was dissolved rather than managed: the five permission strings are named once as constants in `shared_kernel/authorisation.py` and referenced by both the decorator applications and the lookup, so there is no parallel-drafting fragility to discipline.

- methodology (line 1): **Forward-commitment-evaluation pattern.** A forward commitment embedded in a D-entry is a hypothesis about future structure, not a binding instruction. D124 committed that ActorReference would be superseded by ActorContext at the `authored_by` field; S44a pre-write reconciliation evaluated that against the persistence layer, found it structurally unsound, and D126 supersedes it with reasoning. First instance of a D-entry superseding an earlier D-entry's forward commitment. Recurrence test: the next D-entry carrying a forward commitment evaluated at a future session. Captured at `log/captures.md`.

- methodology (line 2): **Brief-vs-domain-model drift at the type altitude.** A sub-class of the brief path-drift pattern (promoted at pre-S44 hygiene), but about which layers a type identifier reaches rather than file-location naming. The brief asserted ActorReference's consumer surface from the S44-framing mental model; the live surface spanned the domain entities, adapter, CLI, DTO, audit drafts, and tests. The same drift omitted the fifth use case (`create_data_point`). Discipline addition: a brief that names a type or a use-case set verifies it with an explicit identifier search across every layer. First-instance candidate; captured at `log/captures.md`.

- methodology (line 3): **File topology budget first-instance load-bearing evidence.** The discipline caught `apps/api/_errors.py` at 729 lines and redirected the AuthorisationDenied handler to its structurally-correct home, validating the discipline's load-bearing claim at first instance rather than at recurrence. Promotion to the methodology document evaluates at P13 close per the S44-framing settlement, with this evidence cited. Captured at `log/captures.md`.

- **S44a closed; the Phase 2-A identity-and-permissions substrate is in place end-to-end. The four S44-framing settlements held: compose-shape ActorContext was structurally honest and bought the clean reshape; the D126 bundle held with its body restructured to three sub-commitments; the file topology budget did load-bearing first-instance work; the D116 collapse (no architecture.md edit) was correct. What diverged was the brief's code-side assumptions about the live ActorReference surface — caught by reconciliation before any code landed. S44b lands the write-path substrate and transport per `briefs/p13/s44b.md`.**

---

## 2026-05-21 — Pre-S44 hygiene — brief path-drift methodology promotion
roles: technical writer, architect
mode: strategic (post-substrate-completion hygiene; promotes the methodology candidate queued at S43b close; no code changes)

- Produced: Two commits closed the session — one fewer than the brief's three-commit plan, because pre-write reconciliation found the captures-backfill commit unnecessary.
  - Commit e3f9457 (`docs(charter)`): the *Path-naming sub-pattern* paragraph under the pre-write reconciliation Patterns-observed entry at `charter/methodology.md`; the captures path-drift entry re-triaged as resolved at `log/captures.md`; the pre-S44 hygiene close marker at `charter/current-package.md`; `briefs/p13/pre-s44-hygiene.md` preserved at v1.
  - Commit this commit (`docs(p13/pre-s44-hygiene)`): this session log entry.

- Decisions: No new D-entries. Per hygiene-shape discipline — the promotion converts an already-surfaced methodology candidate to a methodology-document sub-paragraph; no architectural decision is made.

- Tests: None. Documentation-only changes. No edits under `contexts/`, `apps/`, `shared_kernel/`, `padhanam/`, `tests/`, or migrations.

- Pre-write reconciliation: Three surfaces verified before commit 1; all three surfaced findings, two of them material enough to surface to the operator before any commit landed.
  - Surface 1 (instance count): The S43b close framing carried three path-drift instances (S40, S43, S43b). Verbatim reading of each session's methodology lines found S40 records strategy-enumeration drift — D66 catalogues three retrieval strategies, the adapter executes two — not path naming. The "S40 adjacent adapter shape" citation in the S43 and S43b entries is a loose framing-time reference not borne out by S40's actual methodology lines. Verified count: two (S43, S43b).
  - Surface 2 (captures state): All three S43/S43b methodology candidates already exist at `log/captures.md`, landed at the S43b close commit. The captures-backfill commit the brief planned collapsed to empty per the brief's own conditional logic; only the path-drift entry's triage lines needed editing.
  - Surface 3 (methodology placement): The existing pre-write reconciliation entry carries the Mid-build sub-pattern paragraph as expected; the path-naming sub-paragraph landed adjacent to it per the brief's placement instruction.
  - A further finding outside the three named surfaces: the brief's own Entry B template and methodology sub-paragraph parenthetical inverted the S43 drift direction — they framed the S43 brief as naming `adapters/outbound/postgres/` against a flat convention, when the S43 session-log entry and the live codebase both show the reverse. The correct direction was used in the landed sub-paragraph (see methodology line 1).

- Reflection prompts answered:

  1. *Instance-verification outcome.* The three-instance framing did not hold. S40 reclassified: its methodology line 1 records D66 strategy-registry framing-vs-as-built drift (the catalogue lists three retrieval strategies, the adapter executes two) — strategy enumeration, not path naming — and none of S40's six methodology lines records an adapter-path-naming drift. The verified count is two: S43's flat-adapter-path drift against the `adapters/outbound/postgres/` convention, and S43b's `tests/contract/http/portfolio/` subdirectory drift against the flat `tests/contract/http/` convention. Promotion still fires: the corrective-discipline-on-first-instance precedent — the metric-threshold and reproducibility-artefacts Patterns-observed entries both promoted at a single instance, because corrective discipline-additions land on a single observed drift event rather than paying further drift instances while waiting for recurrence — covers a two-instance promotion comfortably. The brief anticipated exactly this reclassification at its reconciliation surface 1; the verification discipline worked as designed.

  2. *Sub-paragraph-versus-sibling-entry placement.* The path-naming sub-pattern landed as a sub-paragraph under the existing pre-write reconciliation entry, not as its own sibling Patterns-observed entry. Reasoning: the mitigation surface is identical (pre-write reconciliation reading at session open), the entry already carries a sub-pattern paragraph (Mid-build sub-pattern) so the structural precedent for sub-paragraph altitude is in place, and a sub-class of an already-promoted pattern fits the sub-paragraph altitude. A sibling entry was rejected — it would duplicate the parent entry's mitigation surface and inflate the document's pattern count without structural distinction. A discipline-addition paragraph under the document-tail "Pre-write reconciliation against vendor docs" section was rejected — that section addresses vendor-SDK drift, not codebase-convention drift.

  3. *Captures-as-audit-trail discipline.* All three S43/S43b methodology candidates were captured at `log/captures.md` at the S43b close commit even though only one promotes now; the other two stay pending recurrence with explicit second-instance thresholds. This is the right shape: the captures surface is the audit-trail record of methodology-candidate surfacing, independent of immediate promotion status. Promotion happens at a recurrence threshold or at corrective-discipline-on-first-instance qualification; capture happens at first surfacing. The discipline reinforcement for future session-close work: land every surfaced methodology candidate at captures, not only the ones at promotion threshold, so recurrence tracking has a complete trail.

  4. *Two-versus-three accuracy discipline, and the captures entry as canonical citation source.* The methodology sub-paragraph cites the verified instance count, not the framing-time count carried from the S43b close reflection. The structural-honesty discipline: methodology promotions verify their instance enumeration against the actual session-log methodology lines at every promotion event, and the captures entry's enumeration is the canonical citation source — itself verified against the session log. The captures path-drift entry's append-only body still carries the three-instance framing; its triage resolution line, the designated mutable surface, records the S40 reclassification, so the audit trail shows both the framing-time count and the verified count without falsifying the original capture.

- methodology (line 1): **The pre-write reconciliation discipline caught brief-vs-codebase drift in the very brief drafted to promote it.** The hygiene brief's Entry B template and methodology sub-paragraph parenthetical inverted the S43 path-drift direction — they framed the S43 brief as naming `adapters/outbound/postgres/` against a flat convention, when the S43 session-log entry and the live codebase (`contexts/portfolio/adapters/outbound/postgres/portfolio_repository.py`) both show the reverse: the S43 brief named a flat path, and `adapters/outbound/{vendor}/` is the actual convention. The discipline being promoted caught a drift instance of its own class on the way to being articulated — the cleanest available validation of what the methodology entry claims.

- methodology (line 2): **Prior-conversation summaries reconcile against repo-committed content before becoming brief source material.** The inverted S43 direction traces to cross-conversation brief drafting working from this conversation's loose summaries rather than reconciling against `log/captures.md`, which already carried the correct direction. The discipline addition: when a brief is drafted in a conversation other than the one that produced the source material, the repo-committed artefact — not the prior conversation's summary — is the authority, and the brief drafter reconciles against it before treating the summary as source.

- **Pre-S44 hygiene closed; the brief path-drift sub-pattern is promoted at two verified instances. S44 next.**

---

## 2026-05-21 — S43b — Portfolio context HTTP transport surface plus live-stack smoke (bridge session)
roles: architect, engineer, technical writer
mode: build (bridge session — the transport half of a planned substrate-and-transport split)

metrics:
  classification: planned-bridge-session (substrate-and-transport split)
  brief_started: 2026-05-21
  session_started: 2026-05-21
  session_closed: 2026-05-21
  merged: 2026-05-21
  close_state: clean
  tests_passing: yes (16 new tests at S43b; all S43 tests still green; 28 of 28 import-linter contracts; AST green)
  principles_intact: yes
  charter_touchpoints: charter/current-package.md (S43-close plus S43b-in-flight markers at commit 1, S43b-close marker at commit 5); briefs/p13/s43b.md
  corrects:
  corrected_by:
  paired_with: S43

S43b is the transport half of the planned substrate-and-transport split begun at S43. It is the first instance of a *planned* bridge session: the two prior bridge instances at P11 (S39 → S39b verification-and-hygiene; S40 → S40b methodologically-clean-artefact authoring) were unplanned corrections discovered at substrate close, and carried `corrects` / `corrected_by` relationships. S43 → S43b carries `paired_with` — S43b completes a planned split, it does not correct S43. No new D-entries: D124 and D125 already commit the architectural surface; the HTTP transport is mechanical absorption from the run_history and optimization router precedents.

- Produced: Five commits closed the session.
  - Commit aba4053 (`docs(p13/s43b)`): the retroactive S43 session-log entry; current-package S43-close plus S43b-in-flight markers; `briefs/p13/s43b.md` preserved.
  - Commit f2a8ec8 (`feat(p13/s43b)`): the HTTP transport surface — two GET routes (`/api/v1/portfolio/cases`, `/cases/{case_id}`), the DTOs, the query parser, the error handlers, and the API composition-root wiring (`AppCompositions.portfolio_reader`, `build_portfolio_reader`, the `PortfolioReaderAdapter`).
  - Commit 00c0f5f (`feat(p13/s43b)`): the HTTP contract tests, the HTTP tenant-isolation scenarios, and the integration lifecycle test.
  - Commit 11c059f (`feat(p13/s43b)`): the image rebuild, the migration verification, the live-stack smoke, and the re-added structural deployment tests.
  - Commit this commit (`docs(p13/s43b)`): this session log entry plus the current-package close marker.

- Decisions: No new D-entries. Per the bridge-session shape — the architectural surface is already committed at D124/D125.

- Tests: 16 new tests at S43b (5 HTTP route tests; 1 integration lifecycle test; 4 HTTP tenant-isolation scenarios; 3 HTTP contract tests; 3 structural deployment-check instances). All S43 tests still green; 28 of 28 import-linter contracts kept; AST enforcement green.

- Pre-write reconciliation: Six surfaces verified before commit 1. Surfaces 1-5 were mechanical — the S43 substrate present and green (28/28); the run_history/optimization router precedents; the AppCompositions plus `_agent_runtime_wiring.py` wiring pattern; the error-handler registration convention; the Dockerfile `COPY contexts ./contexts` picking up `contexts/portfolio/` whole (no per-context `pyproject.toml`, matching the optimization precedent). Surface 6 surfaced one drift — the brief named a `tests/contract/http/portfolio/` subdirectory but the actual S42 convention is flat files; absorbed per the brief's reflection prompt 5 (path drift reconciles against the codebase). Surface 5's migration-deployed structural check resolved at commit 4 (see reflection prompt 3).

- Reflection prompts answered:

  1. *Pre-write reconciliation surfaces.* Surfaces 1-5 mechanical; surface 6 a path drift absorbed per the brief's own discipline. The standout finding sits at surface 5: the migration-deployed-to-tenant-containers structural-honesty check (deferred from S43 commit 9) confirmed the `padhanam-api` image at S43 close predated the `0016` migration file — the S43 `make migrate` (which runs inside that container) applied only through `0015`. The cumulative reconciliation count keeps climbing; the discipline keeps earning its place, and at S43b it caught a genuine deployment-honesty gap rather than only documentation drift.

  2. *Planned-bridge-session sub-variant, first-instance evidence.* The split produced real signal. The S43b transport work — HTTP routes, DTOs, query parser, the API wiring across `_errors.py` / `_agent_runtime_wiring.py` / `main.py`, the three test surfaces, and the live smoke — ran to roughly 1,500 lines across thirteen files. Landing that as commits 8-10 of a single S43 would have been the fatigue-taxed tail of an already-very-long session; the operator's pause at the substrate-completion boundary, and the re-scoping into S43b, meant the transport work got a fresh reconciliation pass, un-rushed wiring, and a live smoke (with its image-rebuild variance) that received full attention rather than being the last push before exhaustion. The planned-bridge sub-variant is distinct from the unplanned S39→S39b / S40→S40b corrections; the `paired_with` metrics relationship captures the distinction structurally. First-instance evidence; second-instance promotion threshold at a future bounded-context substrate session producing the same split shape.

  3. *Image-rebuild structural-honesty check outcome.* The check revealed the image *was* stale at S43 close. `make migrate` runs `python -m ops.migrate` inside the `padhanam-api` container; that container's image predated the `0016` file, so the S43 migrate applied only through `0015`. The S43 contract tests passed regardless because they provision their own synthetic databases and run alembic from the host (which had `0016`) — but the real tenant containers did not carry the portfolio tables. This is exactly the substrate-completion-versus-deployment drift the structural-honesty discipline exists to catch: a migration committed to the source tree is not a migration deployed to the running stack. S43b commit 4 resolved it (rebuild + recreate + migrate) and the structural check is now a durable committed test. The container-image-lag pattern has now recurred across S41, S42, and S43→S43b — well past any promotion threshold for a documented dev-workflow fast-path.

  4. *Operator-pause-at-substrate-close as methodology candidate.* The S43 pause sat at the session-close boundary (substrate complete, transport remaining); the S41 scope check sat at a mid-session boundary (substrate-application). S43b confirms the session-close-boundary variant produced a clean outcome — the planned bridge ran well end to end. The two are sub-variants of one scope-versus-fatigue-management discipline: a scope check is a checkpoint where the remaining work is measured against the current session's fatigue budget, and either fits or earns its own session. Each sub-variant carries its own second-instance promotion threshold.

  5. *HTTP transport mechanical-absorption verification.* Mostly mechanical, with two divergences. The router, DTO, query-parser, and error-handler patterns absorbed cleanly from the run_history and optimization precedents. (a) The test-path drift — brief said `tests/contract/http/portfolio/` subdir, actual convention is flat files — is the *third* instance of the brief-path-drift pattern (S40's adjacent adapter shape; S43's `adapters/outbound/postgres/`; S43b's flat test files), crossing the promotion threshold the S43 entry set. (b) The `PortfolioReaderAdapter` placement diverged from the optimization precedent: the optimization reader adapter lives in `apps/cli/_cross_context.py`, but the portfolio one landed in `apps/api/_agent_runtime_wiring.py` because it is API-only (the portfolio CLI builds `PostgresPortfolioReader` directly) and that module already defines wiring classes locally (`TenantRoutingSourceRepository`) — a defensible placement call, flagged in the commit message.

- methodology (line 1): **Planned-bridge-session sub-variant confirmed at first instance.** A deliberate substrate-and-transport split produced controlled, un-fatigue-taxed transport work; the `paired_with` metrics relationship distinguishes it from the unplanned correction bridges (`corrects`/`corrected_by`). Promotion to `charter/methodology.md` at second instance; see reflection prompt 2.

- methodology (line 2): **Brief path-drift at third instance — crosses the promotion threshold.** Brief drafts naming paths (adapter, test, contract) must reconcile against the actual `adapters/outbound/{vendor}/` and flat-test-file conventions before commit 1. Three instances (S40, S43, S43b) — the S43 entry set the third-instance threshold; this is now a methodology-line promotion candidate for the next hygiene window. See reflection prompt 5.

- methodology (line 3): **Container-image-lag: substrate-completion is not deployment.** A migration committed to the source tree is not a migration on the running stack; `make migrate` runs inside an image that may predate the migration file. The pattern recurred across S41, S42, and S43→S43b; the structural-honesty check at the bridge session is the reliable catch point. Well past promotion threshold for a documented dev-workflow fast-path. See reflection prompt 3.

- **S43b closed; the portfolio context is complete end-to-end through the read surface. S44 next.**

---

## 2026-05-21 — S43 — Portfolio context foundational substrate (substrate-and-transport split)
roles: architect, engineer, technical writer
mode: build (P13 Wave 1 first build session; substrate half of a planned substrate-and-transport split)

metrics:
  classification: substrate session (closed at 7/10 commits per planned substrate-and-transport split)
  brief_started: 2026-05-21
  session_started: 2026-05-21
  session_closed: 2026-05-21
  merged: 2026-05-21
  close_state: clean (7 of 10 commits per planned split; remainder ships at S43b)
  tests_passing: yes (62 new tests; 28 of 28 import-linter contracts; AST green)
  principles_intact: yes
  charter_touchpoints: charter/decisions.md (D124, D125); charter/schema.md (Portfolio context substrate section; Revisable Protocol and AssertionChange sub-sections in cross-cutting binding shapes); charter/packages/p13-epic.md; charter/current-package.md; briefs/p13/s43.md
  corrects:
  corrected_by:
  paired_with: S43b

This entry is retroactive — it lands at S43b commit 1 alongside the current-package close-marker housekeeping, because S43 closed at 7/10 commits without its own close. The `metrics:` block above is a new session-log-entry shape introduced at S43/S43b per the `briefs/p13/s43b.md` instruction; no prior entry carries one. The `paired_with` field is new at this commit — it captures the S43–S43b planned-split relationship without implying correction (S43b completes the split; it does not correct S43, so `corrects`/`corrected_by` stay empty).

- Produced: Seven commits landed the portfolio context substrate end-to-end through the CLI surface.
  - Commit d0ada39 (`docs(p13/s43)`): D124 + D125; `charter/schema.md` "Portfolio context substrate" section plus Revisable Protocol and AssertionChange sub-sections; p13-epic S43 line; current-package transition; `briefs/p13/s43.md` preserved.
  - Commit 0484657 (`feat(p13/s43)`): Revisable Protocol — a generic `Protocol[RevisionT]` — and the ActorReference placeholder at `shared_kernel/`.
  - Commit 70b94e1 (`feat(p13/s43)`): Case/DataPoint/Assertion domain entities, the `contexts/portfolio/` hexagonal skeleton, and the import-linter delta (the new `layers-portfolio` contract plus the independence / domain-purity / vendor-confinement extensions; 27 → 28 contracts).
  - Commit 121ecc1 (`feat(p13/s43)`): Alembic per-tenant migration `0016_portfolio_substrate`.
  - Commit 801264e (`feat(p13/s43)`): the two consumer-defined ports plus the Postgres repository and reader adapters; 13 behavioural contract tests.
  - Commit ff87b96 (`feat(p13/s43)`): five application use cases, the cursor codec, and the audit-event draft helpers; 11 unit tests.
  - Commit 3ddff81 (`feat(p13/s43)`): the `padhanam portfolio` CLI subcommand; 4 CLI-surface tests.

- Decisions: D124 (portfolio context substrate — Case/DataPoint/Assertion plus three per-tenant tables) and D125 (Revisable Protocol shape — generic `Protocol` in `shared_kernel/`) landed at commit 1. No D-entries beyond those.

- Tests: 62 new tests (9 shared_kernel, 25 domain, 11 application, 4 CLI, 13 contract); 28 of 28 import-linter contracts kept; AST enforcement green.

- Pre-write reconciliation: The S43 six-surface reconciliation surfaced one mechanical placement (schema.md section ordering), four code-state drifts from the P12 audit-finding snapshots absorbed into the schema.md content (TenantContext.tenant_id and ErrorResponse.correlation_id typed `str` not `UUID`; ErrorResponse a flat `BaseModel`; the cursor-codec sites a three-singular / one-plural split with the audit codec originating at S36), a repository-adapter path drift (`adapters/outbound/postgres/`, not the brief's flat path), the protocol-location question (resolved to `shared_kernel/` in prose ahead of commit 1), and the import-linter delta (27 → 28). Three further adjustments surfaced at execution time, each flagged in its commit message: commits 2 and 3 swapped because the domain entities import the `shared_kernel` types the brief's commit 3 created (dependency graph); `jurisdiction` was restored to all three portfolio tables and entities because the brief's field lists omitted it and D12 binds it as a column from inception; the ports landed with the adapter commit rather than the use-case commit because the adapter structurally implements them.

- Reflection prompts answered:

  1. *Pre-write reconciliation surfaces.* The pattern across S43 reconciliation: the brief was accurate on architecture and intent, drifted on paths and field-completeness. The four audit-finding-snapshot drifts were mechanical absorptions into the binding spec; the three execution-time adjustments (commits 2/3 swap, jurisdiction restoration, ports-with-adapter) were dependency-graph and charter-compliance corrections, not architectural disagreements. None required a course change — the substrate landed as designed.

  2. *Forward-compat substrate-depth discipline, first build instance.* Case/DataPoint/Assertion landed in full; the Revisable Protocol shape committed without being exercised across the full Phase 2-A substrate (only DataPoint implements it at S43). Two forward-compat affordances landed unused: `DataPoint.certainty` (nullable, reserved for the D117 tiered-by-salience implementation at P15) and the ActorReference placeholder (superseded at S44 by ActorContext). Neither felt premature — both are single-column / single-field additions now versus refactor-forcing retrofits later, the D12 column-from-inception reasoning generalised. First-instance evidence that the forward-compat substrate-depth discipline holds at build time, not only at framing time.

  3. *Charter-touchpoint discipline at session-1 of a new package.* Commit 1 front-loaded D124, D125, the schema.md section, the epic-note line, the current-package transition, and the brief preservation before any code. Heavier than a mid-package session — and it served the audit-trail discipline rather than burdening it: the binding specification (schema.md table shapes, the D-entry alternatives analysis) landed before the code, so every subsequent commit reconciled against a committed spec rather than the spec chasing the code.

  4. *CLI-as-smoke-write-path discipline.* The `padhanam portfolio` CLI landed ahead of any HTTP write surface specifically to give the (S43b) live-stack smoke an honest write path — without it the smoke would seed Cases via raw SQL, bypassing the audit-events port. The CLI's command surface drove a substrate change: the `create-data-point` action required a fifth use case (`create_data_point`) the brief's four-use-case list had not named. Methodology candidate: substrate sessions that ship a read-side HTTP surface without the matching write side should plan a CLI write path by default, and the CLI command surface sanity-checks the use-case set before the use cases are considered complete.

  5. *Operator-pause-at-substrate-completion as methodology candidate.* S43 closed at 7 of 10 commits at operator decision, after a Claude Code check-in surfaced the remaining three commits' scope. The pause sat at the substrate-completion boundary — domain, application, adapter, and CLI layers complete and green; the HTTP transport surface and the live-stack smoke remaining. Two forces drove it: the ~600 lines of intricate, interdependent API-wiring code still to write (routes, DTOs, query parser, the `_cross_context.py` wiring adapter, the `_agent_runtime_wiring.py` factory, `AppCompositions`, error handlers), and commit 9's image-rebuild step carrying real environmental variance. The operator re-scoped the remainder as S43b, a planned bridge session. This is the first instance of a deliberate substrate-and-transport split: the two prior bridge instances at P11 (S39 → S39b verification-and-hygiene; S40 → S40b methodologically-clean-artefact authoring) were unplanned corrections discovered at substrate close, and carried `corrects` / `corrected_by` relationships. S43 → S43b carries `paired_with` instead — S43b completes a planned split, it does not correct S43. The planned-bridge sub-variant is the load-bearing methodology data point of this session; first-instance evidence is captured here, and promotion to a `charter/methodology.md` line waits on a second instance of a future bounded-context substrate session producing the same split shape. The session-close-boundary pause also pairs with the S41 mid-session scope check at the substrate-application boundary: two sub-variants — mid-session and session-close — of one scope-versus-fatigue-management discipline, each with its own second-instance promotion threshold.

  6. *Methodology line opportunities.* Four candidates surfaced. The protocol-file-location convention settled at `shared_kernel/` — a cross-context Protocol referencing no context type belongs there, not at `padhanam/protocols/`, because the `platform-no-contexts` import-linter contract plus the shared-kernel-as-cross-context-type-home charter both point there. The path-drift pattern reached its second instance (S40's adjacent retrieval_evaluation adapter shape; S43's `adapters/outbound/postgres/` versus the brief's flat path) — brief drafts naming adapter and test paths must reconcile against the actual `adapters/outbound/{vendor}/` and flat-test-file conventions before commit 1; a third instance promotes it. The Revisable Protocol is the first cross-context Protocol in the codebase — the generic-Protocol-imports-no-context-type discipline is a first-instance observation. Case-as-substrate-vocabulary: the karma portfolio-of-relationships domain language landed as the context's first-class vocabulary (`portfolio` over `state_persistence`).

- methodology (line 1): **Planned-bridge-session sub-variant.** A deliberate substrate-and-transport split, distinct from the unplanned verification-and-hygiene (S39→S39b) and methodologically-clean-artefact (S40→S40b) sub-variants; first instance at S43→S43b, carrying `paired_with` rather than `corrects`. See reflection prompt 5. Paired with the substrate-completion-boundary scope-check candidate; promotion at second instance.

- methodology (line 2): **Brief path-drift at second instance.** Brief-named adapter and test paths must reconcile against the actual `adapters/outbound/{vendor}/` and flat-test-file conventions before commit 1. Second instance (S40, S43); a third promotes it. See reflection prompts 1 and 6.

- methodology (line 3): **CLI-as-smoke-write-path.** Substrate sessions shipping a read-side HTTP surface without the write side should plan a CLI write path by default; the CLI command surface sanity-checks the use-case set. See reflection prompt 4.

- **S43 substrate closed at 7/10 commits per the planned substrate-and-transport split. S43b lands HTTP transport plus live smoke per `briefs/p13/s43b.md`; the paired S43b entry lands at S43b close.**

---

## 2026-05-21 — Charter-export tooling commit (parallel-block close before S43)
roles: engineer, technical writer, analyst
mode: build (tooling commit; closes parallel-block work surfaced at pre-P13 hygiene close)

- Produced: One substantive commit plus this session log entry.
  - Commit c3f5040 (`feat(scripts/charter-export)`): allowlist-based charter snapshot tooling at `scripts/charter-export.py` reading `docs/charter-archive-manifest.md` as single source of truth; `Makefile` `charter-export` target; manifest pending-confirmation items resolved per operator positions (`p13-epic.md` kept; `framing-landing-commit.md` kept; `s43.md` added with deferred drop at S43 close) and the "Pending operator confirmation" section removed; manifest file-count tally corrected to 48. Dry-run and live-run verified at 48 files; live-run produced `charter-20260521-1403/` plus `charter-20260521-1403.zip`, both git-ignored.
  - Commit this commit (`docs(p13)`): this session log entry.

- Decisions: No new D-entries. Tooling commit; no architectural commitment.

- Tests: None. Tooling commit; dry-run plus live-run verify the script behaves; no test-suite extension.

- Pre-write reconciliation: Five surfaces verified before commit 1. Surface 1 found `scripts/` is a pre-existing directory (seven P9-P11 live-stack smoke scripts), not new as the brief framed it — commit-body paragraph 3 and reflection prompt 3 were rewritten to the honest framing on operator disposition. Surface 2's dry-run reported 47 files (48 after the `s43.md` allowlist addition), not the brief's 45/46 — the manifest tally and the brief both carried an arithmetic error (the component breakdown summed to 47); manifest tally corrected to 48 and `.gitignore` confirmed `/*.zip` is the existing repo-root pattern. Surface 3 found `.gitignore` already covers all three output patterns — no `.gitignore` edit; the commit landed three files, not four. Surfaces 4 (manifest pending-items) and 5 (no scope creep) were as expected. Two operator dispositions resolved in prose ahead of commit 1.

- Reflection prompts answered:

  1. *Parallel-block-converges as session shape.* This session's work shape — parallel work the operator builds incrementally outside the session sequence, converging into a single commit when ready — is structurally distinct from the four session shapes named in `charter/methodology.md` (substrate, bridge, transport, hygiene). It is not substrate (no bounded context or domain layer), not bridge (no verification-and-hygiene gap between two build sessions), not transport (no HTTP surface), and not cleanly hygiene (hygiene closes accumulated carryover or drift; this closes a discrete capability built in parallel). The closest precedent is the brand-guidelines-plus-`tokens.css` block discussed at the S30b conversation: parallel work converging into a commit when ready. This is the first instance to land with a session-log entry that names the shape explicitly. Whether it earns a fifth shape name (parallel-block-close) or absorbs under hygiene at the lighter end is a second-instance question; the distinguishing property worth tracking is the convergence pattern — artefacts accumulate in the working tree outside the session sequence, then land as a micro-block when the operator judges them ready, rather than being scoped into a brief up front. Recurrence test fires at the next parallel-block convergence; promotion threshold at second instance.

  2. *Manifest pending-items as commit-time discipline.* The manifest was drafted with three open items parked in a "Pending operator confirmation" section at its tail rather than blocking the draft until every allowlist decision was settled. The pattern carries audit-trail value: the draft captured what was genuinely uncertain at draft time (two files added beyond the original spec, one held out), the operator's resolution landed at commit time, and the draft-uncertainty → resolution trail is legible. The alternatives are worse — blocking the draft slows the parallel-block work, and silently folding the uncertain items in hides the judgement call. The discipline's shape: park genuinely-open questions visibly in the artefact, resolve at commit time, then migrate the resolved audit trail into the commit message and session log so the artefact itself does not carry permanent low-value pending-state content (the "Pending operator confirmation" section was deleted at this commit for exactly that reason). First-instance observation; recurrence test fires at the next document drafted with open questions at its tail.

  3. *scripts/ widens from smoke-only to smoke-plus-repo-discipline.* The directory has carried live-stack smoke-verification scripts (`smoke_p9_s32.py` through `smoke_p11_s42.py`) since P9. `charter-export.py` is the first non-smoke entry, extending the directory's scope to repo-discipline tooling. The two-altitude convention itself (`ops/` as importable runtime package; `scripts/` as standalone-executable directory) is pre-existing and stable; this commit widens what `scripts/` carries, not what altitudes exist. The widening is mechanically clean (standalone executable, no package import contract change), so the directory's structural contract held without modification. Methodology observation candidate: the two-altitude convention deserves documentation at a future hygiene window if a third widening or third altitude surfaces; without documentation, the next tooling addition risks landing under the wrong altitude. Not promoted to a binding commitment at this session; recurrence test fires at the next tooling addition.

  4. *Brief-authoring-time verification gap — triple-instance evidence.* The pre-write reconciliation discipline has now caught a brief-asserted factual error about repo state in each of the last three sessions. P13 framing landing: the brief asserted the D116/D117/D118 architecture-primitive numbers without verifying against current `charter/decisions.md` and `charter/architecture.md`; reconciliation caught the D-number scramble. Pre-P13 hygiene: the brief asserted four field shapes from P12 audit-finding snapshots without verifying against current code; reconciliation caught four divergences. This session: the brief asserted `scripts/` as a new directory and inherited a 45-file tally without verifying against repo state or the breakdown's own arithmetic; reconciliation caught both. The pattern generalises from D-numbers to any factual claim about repo state. The P13 framing landing methodology line set a recurrence test at P14 framing; this session fires that test early and in a broader shape — the gap is not D-reference-specific. The structural reading: strategic-mode brief-authoring either verifies factual claims about repo state against available sources (project knowledge for charter content; explicit pre-write-reconciliation marking for working-tree state a Claude.ai conversation cannot reach), or carries the assertion as a known-to-need-verification marker rather than as a bare fact. The current bare-fact pattern produces structural-honesty findings reliably — the downstream reconciliation works — but it pushes verification cost to the commit session, where reversal is more expensive (a false claim in a drafted brief becomes a false claim in a commit body unless caught). Charter methodology promotion candidate at the next hygiene window; this is the strongest of the three instances, because it is the third and because it shows the gap is general rather than D-reference-specific.

- methodology (line 1): **Parallel-block-close as a candidate fifth session shape.** Parallel work accumulated in the working tree outside the session sequence, landed as a micro-block when ready — distinct from substrate, bridge, transport, and hygiene. First instance named in a session log; see reflection prompt 1. Promotion threshold at second instance.

- methodology (line 2): **The brief-authoring-time verification gap is triple-instance evidenced and general.** Strategic-mode briefs assert factual claims about repo state without verification; pre-write reconciliation has caught one such error per session across the last three sessions, spanning D-numbers, code field-shapes, and directory/arithmetic state. Charter methodology promotion candidate at the next hygiene window; see reflection prompt 4.

- **Charter-export tooling committed; working tree clean entering S43.**

---

## 2026-05-21 — Pre-P13 hygiene — schema.md formalisation plus old-platform-name find-replace
roles: technical writer, analyst
mode: build (P13 pre-substrate hygiene; closes two carryover items before S43 opens)

- Produced: Six commits closed the session — five per the brief's planned shape, plus one corrective after the post-session verification grep.
  - Commit e1067ee (`docs(charter)`): schema.md "Cross-cutting binding shapes" H2 section with three sub-sections (TenantContext, ErrorResponse, canonical cursor codec), field tables, and the cross-cutting-shape audit-trail role mirroring the database-table sections.
  - Commit 1eea111 (`docs(p13/hygiene)`): old-platform-name find-replace across three non-allowlist files (`charter/methodology.md`, `charter/packages.md`, `charter/packages/p13-epic.md`); post-edit `(Quorum|Zephyr)` grep returns only allowlist matches plus the Q1-exempt dated snapshot.
  - Commit 43c3603 (`docs(charter)`): phase-2-audit-inputs.md "Phase 2-A close hygiene list" section with the roadmap stale-headers entry as first item.
  - Commit fe18198 (`docs(charter)`): current-package.md pre-P13 hygiene close marker; pre-hygiene prompt preserved verbatim at `briefs/p13/pre-hygiene.md`.
  - Commit acf6cd7 (`docs(p13)`): this session log entry.
  - Commit this commit (`docs(p13/hygiene)`): corrective — the post-session `(Quorum|Zephyr)` verification grep flagged `charter/current-package.md`; the commit-4 close marker had carried the two former platform names as a use-mention. Close marker rephrased to a structural descriptor and this entry updated.

- Decisions: No new D-entries. Per hygiene-shape discipline — the three formalised shapes are already code-committed, and the find-replace plus accumulator-surface work is documentation hygiene.

- Tests: None. Documentation-only changes. No edits under `contexts/`, `apps/`, `shared_kernel/`, `padhanam/`, `tests/`, `infra/`, `migrations/`, `ops/`.

- Pre-write reconciliation: Five surfaces verified before commit 1. Surfaces 1, 4, 5 (schema.md structure, phase-2-audit-inputs.md state, current-package.md close marker) were mechanical — all three files in the assumed state, new content appends. Surface 2 (code substrates) was structural-honesty: four points of drift between the P12 audit-finding snapshots and current code, all absorbed into the schema.md content per AC 2 — `TenantContext.tenant_id` and `ErrorResponse.correlation_id` are typed `str` (UUID-shaped) not `UUID`; `TenantContext.__post_init__` validates all three fields, not two; `ErrorResponse` is a flat `BaseModel`, not a Pydantic discriminated union; the cursor-codec sites split three-singular `cursor.py` / one-plural `cursors.py` (optimization alone), not two/two, and the audit codec originates at S36, not S37. Surface 3 (grep) returned non-zero — four non-allowlist files — so commit 2 was a substantive find-replace, not the no-op the brief flagged as possible; the planned session held at five commits; a sixth corrective commit followed (see Produced). Three operator questions (status-snapshot exemption, Zephyr grep-widening, use-mention rephrasing) were resolved in prose ahead of commit 1.

- Reflection prompts answered:

  1. *Hygiene-session-as-shape, fourth instance.* The shape vocabulary holds cleanly. Commit counts across the four instances — pre-P12 hygiene (six), post-P12 charter-discipline hygiene (seven), pre-Phase-2 synthesis-and-refinement (ten), pre-P13 (five planned, six with a post-verification corrective) — track focus: carryover-closure is a tighter unit of work than structural-finding capture, discipline-convention establishment, or synthesis-and-refinement, and the five-commit planned shape is the smallest of the four. The third-instance temporal-position taxonomy (pre-audit, post-audit, pre-next-phase hygiene) does not absorb this instance: all three of those positions sit at phase boundaries, while this hygiene session sits intra-package — after P13 framing closed but before S43, the package's first build session, opens. It warrants a fourth sub-position, pre-package-first-session hygiene. A distinguishing structural property reinforces the new sub-position: this is the first hygiene session whose work surface was set by an explicit framing decision (P13 framing Decision 2 bundled the two items) rather than by carryover triaged at hygiene-open time. First-instance evidence for the pre-package-first-session sub-shape; recurrence test fires at the next package framing that bundles hygiene into a pre-first-session session.

  2. *Pre-write reconciliation, twelfth-plus instance.* The five surfaces split three mechanical (schema.md structure, phase-2-audit-inputs.md state, current-package.md close marker — each file in its assumed state) and one structural-honesty (surface 2's code-substrate verification) plus one that drove commit-shape (surface 3's grep). Surface 2 surfaced four points of drift between the P12 audit-finding snapshots and current code state — every field-table type and structural claim in the brief's schema.md content needed at least one correction, and the cursor-codec sites table had both a wrong path and a wrong originating session. The discipline earned its place decisively here: surface 2 alone caught four documentation errors that would otherwise have shipped into a binding-specification surface and propagated forward as ground truth. Surface 3's grep returned non-zero (four non-allowlist files), so commit 2 stayed substantive rather than compressing to the brief's contingency no-op. The twelfth-plus instance confirms the pattern is past any promotion threshold; the open question is no longer whether to keep it but whether to push verification one step upstream (the framing-landing session's methodology line already proposed a brief-authoring-time D-number checkpoint; the same logic applies to code-substrate snapshots — a brief that cites code field-shapes should verify them against current code at brief-authoring time).

  3. *Grep allowlist as cross-context discipline.* The S13 verification-grep allowlist began as a rebrand-sub-shape playbook element — a literal three-path list (`charter/decisions.md`, `log/sessions.md`, `docs/archive/`). This session generalises it on three axes, each a first-instance observation with a recurrence test at the next narrative-cleanup operation. (a) The allowlist is not a closed enumeration but an expression of an append-only-protected *category* defined by a structural property: any file that records what was true at a specific past moment, regardless of directory. Dated point-in-time snapshots (`docs/notes/status-*.md`) qualify by construction; so do verbatim-by-mandate build-session briefs under `briefs/` — surfaced at commit 4 when preserving this very prompt, whose title carries the old platform name. Rephrasing such a file would falsify its record, a worse violation of append-only spirit than the literal grep-rule violation. (b) The rebrand-cleanup discipline operates at the principle level — old-platform-name scrubbing from current-facing narrative — not the literal-string level; this is why the verification grep widened from `Quorum` to `(Quorum|Zephyr)`, since Zephyr is structurally equivalent (an old platform name, archived by D38, legitimate in historical content only). One operation, not two. (c) Use-mention occurrences require structural-descriptor rephrasing, not direct find-replace: `p13-epic.md` named this hygiene task itself, where a literal replace produces nonsense and allowlist-extension creates a permanent stale-name carrier. The S13 rebrand playbook did not anticipate use-mention because S13 was the rebrand event, not a post-rebrand cleanup; this session is the first post-rebrand cleanup operation.

  4. *Phase 2-A close hygiene list as new convention surface.* Commit 3 established the "Phase 2-A close hygiene list" section in `charter/phase-2-audit-inputs.md` as the accumulator for closure-housekeeping items triaged to the Phase 2-A close per P13 framing Decision 2. It is structurally distinct from two adjacent surfaces: the substrate-completion list and `charter/deferred-decisions.md` both carry activation-gated deferred work (substrate awaiting explicit trigger conditions), whereas this list carries closure-housekeeping with no activation gate — items clear at the Phase 2-A close hygiene session regardless of trigger. The roadmap stale-headers refresh lands as the first entry. First-instance evidence for the convention; recurrence test fires at the first item added through Phase 2-A package execution beyond this opening entry. One placement observation: the section co-locates with four audit-input sections in a file oriented to the Phase 2 close audit; close-hygiene and audit-input are different categories, and the co-location holds per the brief's explicit instruction (both are Phase 2 forward-accumulators) but is the kind of surface a future hygiene session might split if the list grows.

- methodology (line 1): **Pre-package-first-session hygiene is a fourth temporal sub-position for the hygiene-session shape.** The prior three-position taxonomy (pre-audit, post-audit, pre-next-phase) is phase-boundary-scoped; this instance sits intra-package between package framing close and the first build session, with its work surface set by an explicit framing decision rather than accumulated carryover. First-instance evidence; see reflection prompt 1.

- methodology (line 2): **The narrative-cleanup verification-grep allowlist is an append-only-protected category defined by structural property, not a literal path list.** Any file that records what was true at a past moment — `docs/archive/`, dated `docs/notes/status-*.md` snapshots, verbatim-preserved `briefs/` prompts — qualifies; rephrasing one would falsify its record. First-instance generalisation; see reflection prompt 3(a).

- methodology (line 3): **Rebrand-cleanup operates at the principle level — old-platform-name scrubbing — not the literal-string level.** The verification grep widened to every former platform name (`Quorum` and `Zephyr`) because the workitem's principle is drift removal, not a single search term. First-instance evidence; see reflection prompt 3(b).

- methodology (line 4): **Use-mention occurrences in rebrand-cleanup require structural-descriptor rephrasing, not direct find-replace.** Where text names the cleanup task or the rename event rather than referring to the platform by its old name, a literal replace produces nonsense; the generic descriptor preserves clarity without carrying the literal. First post-rebrand-cleanup instance; see reflection prompt 3(c).

- methodology (line 5): **The post-session verification grep caught drift the session introduced in its own close marker.** Commit 4's close marker named the two former platform names as a use-mention; the post-commit-5 `(Quorum|Zephyr)` grep flagged `charter/current-package.md`, corrected at commit 6. The verification step is not redundant with careful authoring — it caught an own-goal authoring missed. A narrative-cleanup session should run its verification grep after every commit, including the commits that describe the cleanup.

- **Pre-P13 hygiene closed; S43 next per `briefs/p13/s43.md`.**

---

## 2026-05-21 — Phase 2-A P13 framing landing commit session
roles: technical writer, PM, analyst
mode: strategic (charter commit; no code changes; P13 framing landing)

- Produced: Nine commits closed the session, landing the six drafted artefacts plus the two close deliverables from the P13 framing substantive conversation.
  - Commit 4dd618e (`chore(docs)`): karma prior-art spec relocated from `docs/notes/spec-private-assistant-platform.md` to `docs/notes/prior-art-karma/spec.md`, creating the prior-art-karma directory as partial Block 2 Part B closure per Decision 6 Sub-decision A.
  - Commit 196d7c7 (`docs(p13)`): P13 epic note at `charter/packages/p13-epic.md` carrying the three-mode framing lens, package contents, substrate-to-mode mapping, four-session forecast, out-of-scope list, and the forward-compat substrate-depth classification table.
  - Commit 08ff56a (`docs(charter)`): four karma-extracted Phase 2 principles plus the PA communication discipline principle at `charter/principles.md`; the Phase 2 domain vocabulary sub-section at `charter/architecture.md`; a new `Phase 2-A P13 framing deferrals` section at `charter/deferred-decisions.md` (two defer-with-trigger plus six flag-for-future-testing entries) with the TOC updated.
  - Commit ccf1567 (`docs(charter)`): roadmap v6 entry — Phase 2 packaging commitment.
  - Commit 782638d (`docs(charter)`): Phase 2 PRD section at `charter/prfaq.md` (post-v2 extension).
  - Commit cf186e8 (`docs(briefs)`): S43 first-session prompt at `briefs/p13/s43.md`.
  - Commit df58ef8 (`docs(charter)`): current-package P13-framing-closed marker.
  - Commit c5930c5 (`docs(briefs)`): landing commit prompt preserved at `briefs/p13/framing-landing-commit.md`.
  - Commit this commit (`docs(p13)`): this session log entry plus the captures triage marker on the 2026-05-20 Decision 6 capture.

- Decisions: No new D-entries. Per the P13 framing settlement the Step 6 D-entries (D114-D123) already commit the load-bearing decisions; D124+ surface at S43 onward during build. The spec-extracted Phase 2 principles land at `charter/principles.md` as charter additions without numbered D-entries per the extract-not-decide framing.

- Tests: None. Documentation-only changes.

- Pre-write reconciliation: Five surfaces verified before any commit landed; three findings surfaced and were operator-dispositioned in prose ahead of execution. Finding A: the three architecture-primitive D-numbers were rotated in the drafted artefacts (artefacts said D116 tiered-by-salience / D117 two-vector decay / D118 three-tier consent; `charter/decisions.md` and `charter/architecture.md` bind D116 three-tier consent / D117 tiered-by-salience / D118 two-vector decay). Primitive names and descriptions were correct; only the parenthetical numbers were wrong. Corrected on landing across `p13-epic.md` (five references), `architecture.md` domain vocabulary (one), and `principles.md` (one). Finding B: the drafted roadmap v6 used a `## v6` heading with labelled paragraphs; the existing "## Version log" convention is single-paragraph bullets — v6 reformatted to a `- **v6**` bullet, all content preserved. Finding C: the deferred-decisions entries were not supplied verbatim; the eight entries were authored from the Artefact 1 classification table into a new `## Phase 2-A P13 framing deferrals` section with a TOC entry per the TOC-maintenance discipline. Minor corrections folded in: s43.md AC 11 import-linter count 25 to 27; the flag-for-future-testing count corrected to six (the classification table is authoritative; reflection prompt 3 had said seven); epic-note title harmonised to the `# P{n} Epic — {title}` convention.

- Reflection prompts answered:

  1. *Pre-conversation decisions audit.* The brief carried seven pre-conversation recommendations; the substantive conversation revised or refined five and adopted two as written (per close deliverable 1). The brief-carries-recommendations discipline holds: write the brief carrying the recommendation, allow the substantive conversation to revise, record the revisions as audit-trail surface at close. Recurrence test fires at P14 framing.

  2. *Three-mode picture as framing evolution.* The product picture sharpened from "Phase 2-A as messaging-first delivery" to "three modes sharing one substrate (attentional, workflow, observation-and-suggestion), meta-layer over the user's existing apps." The sharpening preserved every prior commitment — the eight-package structure, the six Step 6 architectural primitives, the senior-leader ICP, the messaging-first delivery constraint — and surfaced no drift; the three-mode lens reframes the same substrate, not the scope. First-instance evidence for product-picture sharpening at package framing time.

  3. *Forward-compat substrate-depth discipline at first instance.* The discipline produced six build-now items, six defer-with-trigger items, and six flag-for-future-testing items. The classification absorbed substantive operator pushback at three placement decisions — Gate entity, methodology-as-workflow data model, and governance artefact hierarchy all moved from P13 to P14. First-instance evidence for the deferral-forces-refactor methodology candidate; recurrence test fires at P14 framing.

  4. *Karma prior-art reference set continuation.* Decision 6 reframed from "spec-as-architectural-input absorption" to "karma prior-art reference set completion plus partial Block 2 Part B closure" per the 2026-05-20 captures routing entry. The spec relocated to `docs/notes/prior-art-karma/spec.md`, creating the prior-art-karma directory that Block 2 Part B planned. Two pattern notes (`authoring-contract.md`, `taps-and-dispatcher.md`) remain deferred to their activation triggers per the existing deferred-decisions entries. The 2026-05-20 capture is triaged at this commit.

- methodology (line 1): **D-number verification belongs to brief-authoring time, not only to commit-session reconciliation.** The D-number scramble (Finding A) surfaced at the landing session's pre-write reconciliation, not at the strategic-mode substantive conversation that drafted the artefacts; the artefacts referenced D116/D117/D118 without verification against current `charter/decisions.md` and `charter/architecture.md` state. Methodology candidate: when strategic-mode artefacts reference D-numbers, verification against current charter state belongs to brief-authoring time as a drafting-discipline checkpoint, not solely to the downstream commit session's reconciliation. The commit-session reconciliation caught it cleanly here, but a brief-authoring checkpoint would catch it one step earlier and cheaper. Recurrence test fires at P14 framing artefact-authoring.

- **Phase 2-A P13 framing closed; the six drafted artefacts plus two close deliverables landed across nine commits. Pre-P13 hygiene session next (schema.md formalisation plus doc-content rebrand per Decision 2 triage); P13 Wave 1 first build session (S43) follows per `briefs/p13/s43.md`.**

---

## 2026-05-20 — Phase 2-A P13 framing brief commit
roles: technical writer, PM, analyst
mode: strategic (charter commit; no code changes; P13 framing brief pre-conversation landing)

- Produced: Two commits closed the session.
  - Commit 83b7bd5 (`docs(briefs)`): P13 framing brief at `briefs/p13/framing.md` carrying brief opening framing, what P13 framing opens (committed substrate plus forward-compat dimension plus carryover hygiene), four methodology streams plus forward-compat substrate-depth as discrete discipline candidate, six produced artefacts plus two close deliverables, reading-order context with spec inclusion, seven pre-conversation decisions (substrate-completion carryover; hygiene workitems; session granularity at four sessions; roadmap v6 entry; Phase 2 PRD shape; spec-as-architectural-input commitment; forward-compat substrate-depth classification), three substantive work-streams (epic note authoring; first-session prompt drafting; roadmap plus PRD plus spec-extracted charter additions), six reflection prompts at session close, explicit out-of-scope list.
  - Commit this commit (`docs(charter)`): Commit prompt preservation at `briefs/p13/framing-brief-commit.md`; operator-supplied Private Assistant Platform spec preservation at `docs/notes/spec-private-assistant-platform.md` with header-note framing per Decision 6 option (c); current-package.md close marker append for P13 framing brief landing; this session log entry.

- Decisions: No new D-entries. No content in `charter/decisions.md`, `charter/principles.md`, `charter/architecture.md`, `charter/packages.md`, `charter/deferred-decisions.md`, `charter/phase-2-design-7step.md`, or any other charter file modified at this commit. D-entries and substantive charter additions defer to the Claude Code commit session following P13 framing substantive conversation.

- Tests: None. Documentation-only changes.

- Reflection prompts answered:

  1. *Briefs/ discipline check, sixth instance by analogy.* P13 framing brief authored at Step 7 close thread; pattern continues. Six-instance evidenced as analogy carry from arc-step framing (Steps 3, 4, 5, 6, 7 brief authored pre-conversation in prior step close thread) to package framing (P13 framing brief at Step 7 close thread). The pattern carries; the convention extends from arc-step to package framing without modification. Worth noting that the discipline's portability across altitude shifts (arc-step framing to package framing) is itself a discipline-test signal; first instance of altitude-shift carry; recurrence test fires at P14 framing.

  2. *Brief carries pre-conversation recommendations across seven decisions.* The P13 framing brief expanded from five pre-conversation decisions to seven during iterative drafting with the operator. Decisions 6 (spec-as-architectural-input) and 7 (forward-compat substrate-depth classification) absorbed substantive operator pushback that surfaced the spec absorption discipline plus the deferral-forces-refactor rule. Pattern continues from Steps 4 through 7 briefs; brief authoring is a drafting plus iteration loop, not a one-shot artefact. The operator's rule (do not build now if can build later without major refactor) sharpened Decision 7's classification framework from open-ended forward-compat list to three-category disciplined classification.

- methodology (line 1): **Forward-compat-without-major-refactor as build-methodology discipline candidate, first instance.** The P13 framing brief introduces the discipline as Phase 2-A architectural posture: build now only if deferring forces major refactor; defer with named activation trigger otherwise; flag built-now substrate that operator dogfooding does not exercise. The discipline produces a three-category classification (build-now; defer-with-trigger; flag-for-future-testing) the P13 framing substantive conversation populates. First-instance candidacy; recurrence test fires at P14 framing and subsequent Phase 2-A packages. Promotion to charter-methodology line lands at Phase 2-A close audit if the discipline holds across multiple packages.

- methodology (line 2): **Spec-as-architectural-input absorption pattern, first instance.** The operator-supplied product specification at `docs/notes/spec-private-assistant-platform.md` enumerates Private Assistant Platform substrate primitives across the platform lifecycle. The P13 framing brief absorbs the spec via Decision 6 option (c): specific extracts as charter additions (frontend/backend boundary rule; audit-trail-as-source-of-truth; originals-never-erased; authority plus certainty independence); spec preservation at `docs/notes/spec-private-assistant-platform.md`; substrate vocabulary aligned across charter prose where the substrate matches; surface architecture explicitly excluded from absorption because Phase 2-A's messaging-first commitment plus the senior-leader ICP differ from the spec's Studio plus Portal SPA plus customer-organisation framing. First-instance candidacy; recurrence test fires if Phase 2-B or Phase 3+ framing absorbs additional external specifications.

Worth observing whether forward-compat-without-major-refactor recurs as charter-methodology discipline candidate across Phase 2-A packages.

- **Phase 2-A P13 framing brief landed; P13 framing substantive conversation opens at Claude.ai in a fresh thread per the briefs/ discipline pattern.**

---

## S49 — Post-S48 hygiene: methodology promotions and captures formalizations
roles: analyst, PM, technical writer
mode: strategic-mode hygiene, executing briefs/p13/s49.md (charter-only; two commits)

S49 closed the post-S48 hygiene discipline-altitude work between the S48a/S48b parallel-thread close and the resolver-disambiguation build session opening. Two commits landed without any code, test, or D-entry surfaces: commit 1 promoted three methodology candidates accumulated across the post-S46 architectural arc to `charter/methodology.md`'s Patterns observed section (interface-versus-implementation discipline at seven instances across three surfaces; component-quality-versus-integration-smoke discipline at two-instance evidence plus first structural implementation D137; structural-test SSOT binding at two-instance evidence) plus appended PROMOTED resolution lines to the three contributing captures entries (append-only per the audit-trail-integrity discipline). Commit 2 appended two new captures entries (scope-discipline-at-brief-authoring at first-instance observation; gold-set authoring plus confidence-aware composition connection observation) plus this S49 session-log entry plus the S49 close marker at `charter/current-package.md`. The two-commit shape separates the methodology-document load-bearing promotions from the forward-looking captures formalizations for clean audit trail.

- Produced: Two commits. (1) `docs(charter): promote three disciplines to methodology.md from S47-S48b evidence` (da56b0e) — three new sub-sections under Patterns observed at `charter/methodology.md` (interface-versus-implementation, component-quality-versus-integration-smoke, structural-test SSOT binding) plus three PROMOTED resolution lines appended to the contributing captures entries at `log/captures.md` (2026-05-25 [S47/UX convergence] interface-versus-implementation; 2026-05-26 [S48b] component-quality; 2026-05-26 [S48b] structural-test SSOT binding). (2) `docs(captures): formalize scope-discipline and gold-set/confidence observations` (this commit) — two new captures entries appended to `log/captures.md` (2026-05-26 [S48b framing] scope-discipline drift at brief authoring with the pattern-family observation alongside interface-versus-implementation; 2026-05-26 [S48b reflection 2] gold-set authoring plus confidence-aware composition with forward-relevance bound to D137, D110, and D134 extensions) plus this session-log entry plus the S49 close marker at `charter/current-package.md`.

- Decisions: No new D-entries; three methodology promotions to `charter/methodology.md`; three captures-entry resolution-line updates at `log/captures.md`; two captures formalizations at `log/captures.md`.

- Tests: No change. 1776 unit + contract tests hold from S48b close baseline; 33/33 import-linter contracts kept; AST enforcement green. Charter-only session; no test surface touched.

- Reflection (methodology-entry composition under Patterns observed, prompt 1): The three new sub-sections read as distinct patterns when placed adjacent in the section. Interface-versus-implementation is about committing implementations when the architecture should commit interfaces; component-quality-versus-integration-smoke is about using the wrong evaluation surface for component-quality questions; structural-test SSOT binding is about discipline commitments that admit structural tests landing those tests at the commitment's commit. The three pattern bodies inhabit different altitudes (framing-altitude drafting discipline; evaluation-surface selection discipline; commitment-binding discipline) and reference different mechanisms (interface-versus-implementation question; port-and-adapter substrate; structural test surfaces). No revision needed at draft time; the cross-pattern non-overlap pre-write reconciliation surface 3 closed cleanly.

- Reflection (pattern-density observation closure, prompt 2): The interface-versus-implementation entry's pattern-density paragraph carries the observation and its resolution at load-bearing weight without restating the convergence-conversation history in detail. The seven-instance enumeration carries the evidence; the pattern-density paragraph names the open question the five-instance captures entry posed (whether strategic-mode altitude inherently produces the drift or whether the convergence's particular shape produced it) and answers it (the seven instances across three surfaces close the question: strategic-mode altitude predictably produces the drift; the discipline binds at every drafting surface when explicitly named as a reconciliation surface). The load-bearing-versus-historical balance is appropriate; the methodology entry serves forward-relevance more than retrospective narration.

- Reflection (captures-entry resolution shape, prompt 3): The three resolution-line updates appended PROMOTED bullets to the original entries rather than replacing the prior resolution text. This composes cleanly with future recurrence accumulation: subsequent instances of the same pattern can land at the captures entry's recurrence log (as a new appended observation or as a sub-bullet under the existing entry) without contradicting the resolution; the PROMOTED line names the methodology document's forward-relevance discipline as the binding mechanism for future recurrence. The pattern matches the existing convention for entries with multiple resolution lines (the qwen2.5:14b entry at line 670 carries `resolution (initial)` and `resolution (S48a close, 2026-05-26)`; the S49 entries follow the same `resolution (S49, 2026-05-26)` shape).

- Reflection (forward-relevance bindings, prompt 4): **Substantive paragraph because the forward-relevance bindings vary in operational concreteness.** The interface-versus-implementation entry's forward-relevance is the most concrete: pre-write reconciliation surfaces at session prompts include an explicit interface-versus-implementation check from S49 forward; the discipline composes with the existing path-naming discipline (S43b) and the build-at-second-instance discipline (D127 alternative (d)) as the three pre-write reconciliation disciplines that catch the most common strategic-mode drafting drift classes. P14 epic framing inherits this directly. The component-quality entry's forward-relevance names four specific future component-quality substrates (prompt-revision, structured-output reliability per class, retrieval-strategy quality, confidence-calibration) with their activation triggers; each future substrate inherits the D137 shape (bounded context or substrate-altitude, gold set, metric calculator, audit chain integration, CLI). The structural-test SSOT binding entry's forward-relevance names five structural-test surfaces (AST-pattern detection, symbol-identity assertions, import-linter contracts, file-topology budget assertions, path-naming verification) and three future commitment classes that admit structural tests (D136 multi-channel UX primitives at activation triggers; future ConversationFlow implementers at P14+; future gold-set authoring disciplines per the captures observation from the same session). The structural-test entry's forward-relevance leans on the activation-trigger pattern more heavily than the other two; this is appropriate because the entry's discipline is about *evaluation at draft time* (does the commitment admit a structural test?) rather than about a specific test surface, so the forward-relevance must enumerate the surfaces and the commitment classes rather than commit a specific Phase 2-A test. All three forward-relevance bindings are operationally concrete enough to act on at the relevant future surfaces.

- Reflection (scope-discipline-at-brief-authoring recurrence test naming, prompt 5): The captures entry names the recurrence test at the next strategic-mode brief-drafting moment that proposes substantive substrate work, with concrete test surfaces (the next P14 ConversationFlow implementer brief or the next post-resolver-disambiguation build session brief). The test fires if a future brief commits maximal scope by precedent rather than testing each piece against Phase-current YAGNI; the test passes if the brief commits only the load-bearing minimum with deferrals named at activation triggers (the S48b Option B shape). The recurrence test is concrete enough to detect at the next strategic-mode brief-drafting moment.

- Reflection (gold-set-plus-confidence composition reflection, prompt 6): The captures entry is the right home for the observation at first-instance. The connection between gold-set authoring discipline and D134's confidence-aware composition primitive is structurally significant because the substrate at D137 measures model behaviour against gold-set expectations, but production behaviour at the same input class consumes D134's port; if gold-set authoring treats ambiguity as model failure where D134 treats it as architectural opportunity, the substrate's signal and the production cell's behaviour diverge on the same class of input. The forward-relevance binding names extensions at D137, D110, and D134; future evaluation substrates or D134 extensions inherit the connection as captures-entry reference rather than requiring D-entry-level prose changes at the current sessions. If recurrence at gold-set-authoring or D134-extension surfaces strengthens the observation, second-instance promotion to `charter/methodology.md` would warrant updating D134's prose or D137's prose with explicit forward-references to the methodology entry; first-instance does not.

- Reflection (methodology surfaces, prompt 7): One new methodology candidate surfaced at S49 itself — the **shape-of-hygiene-session** observation. S49 is the second hygiene-session-shape instance in P13 (after the pre-S45 principles assessment / pre-S45 Revisable harness hygiene block plus pre-S44 hygiene), but the first that is purely charter-only (no code; no tests; no enforcement layers) and sized at exactly two commits with a clear two-commit shape (load-bearing-promotions + forward-looking-formalizations-plus-close). The discipline of "charter-only sessions sized at one to two commits with methodology promotions and captures formalizations as the two commit-shape components" may be promotion-candidate if a second instance lands at the next hygiene session (post-P13 close hygiene or pre-P14 hygiene). Not captured as a new captures entry at S49 because the second-instance test is genuinely open (the next hygiene session may take a different shape if it has substantive code workitems); recorded here in the methodology line for future-session inheritance.

methodology: Three Patterns observed promotions land at `charter/methodology.md` (interface-versus-implementation discipline at seven-instance evidence across three surfaces; component-quality-versus-integration-smoke discipline at two-instance evidence plus first structural implementation D137; structural-test SSOT binding at two-instance evidence). Two captures formalizations land at `log/captures.md` (scope-discipline-at-brief-authoring at first instance with the pattern-family connection to interface-versus-implementation; gold-set authoring plus confidence-aware composition at first instance with forward-relevance bound to D137, D110, and D134 extensions). Two methodology candidates at first-instance pending recurrence (scope-discipline-at-brief-authoring; gold-set-plus-confidence connection). One shape-of-hygiene-session observation pending recurrence at the second hygiene session (post-P13 close hygiene or pre-P14 hygiene as candidate trigger).

```
metrics:
  classification: strategic-mode hygiene session
  brief_started: 2026-05-26
  session_started: 2026-05-26
  session_closed: 2026-05-26
  merged: 2026-05-26
  close_state: clean (three methodology promotions land cleanly under Patterns observed; two captures observations land for forward recurrence testing; no code/test/D-entry surfaces touched)
  tests_passing: not_applicable
  principles_intact: yes
  charter_touchpoints: charter/methodology.md (three new Patterns-observed sub-sections); log/captures.md (three PROMOTED resolution lines on contributing entries; two new captures formalizations); log/sessions.md (this entry); charter/current-package.md (S49 close marker); briefs/p13/s49.md (brief preservation pending at commit 2 if operator preserves)
  corrects:
  corrected_by:
```

---

## S50 — Resolver disambiguation on duplicate-title cases
roles: engineer, architect, technical writer
mode: build session, executing briefs/p13/s50.md (two commits: feat + docs)

S50 closed the S48a forward finding on resolver behaviour when multiple cases share the same title via cell-layer extension. The architectural disposition committed at the captures entry: resolution-ambiguity (high-confidence intent + multi-match resolve_target) routes to D134's existing Case 2 shape-aware clarification surface, not to a new fourth case or a separate disambiguation path. PendingClarification accommodates the sub-case via the existing `proposed_intent: dict[str, Any]` field carrying a `resolution_candidates` sidecar that `parse_intent` ignores; operator's positional reply ("1", "2", "3") resolves the pending and executes the original action against the chosen case via a `_dispatch_proceed_against_resolved` short-circuit. The PortfolioGateway.CaseSummary DTO extends with three discriminator fields (created_at, last_activity_at, data_point_count) computed eagerly at `find_cases` for Phase 2-A scale; TargetCandidate carries optional discriminators forward through ResolutionOutcome.candidates; the cell formats raw datetimes/ints to short human strings (relative-time helper with absolute-date fallback beyond 30 days) per the channel-decides-format pattern at D135.

- Produced: Two commits. (1) `feat(p13/s50): resolver multi-match with disambiguating-signal rendering` (a05f7fe) touching seven files across the four hexagonal layers — port DTO extension at `contexts/messaging/application/ports/portfolio_gateway.py`; TargetCandidate.discriminators + ResolutionOutcome.candidates at `contexts/messaging/application/resolve_target.py`; adapter eager enrichment via `get_case_detail` at `apps/api/_portfolio_gateway_wiring.py`; cell helpers and dispatch paths at `contexts/messaging/application/manual_entry_cell.py` (relative-time formatter; discriminator composition; positional-reply parser; numbered candidate renderer; `_dispatch_resolution_clarify_with_pending` for the multi-match dispatch; `_handle_resolution_selection` for the positional-selection branch; `_dispatch_proceed_against_resolved` for the id-hint short-circuit; positional-reply branch in `turn()` between active-pending check and confirm/cancel classification); 11 new unit tests across `test_resolve_target.py` (2: AMBIGUOUS carries candidates with discriminators; TargetCandidate.discriminators defaults empty) and `test_manual_entry_cell.py` (9: 4 helper tests; 5 cell-flow tests covering multi-match → pending creation, positional-selection executes against chosen id, out-of-range re-renders without resolving, correcting reply cancels the resolution pending, NO_MATCH keeps simpler clarification path). (2) `docs(p13/s50): live-stack smoke verifying resolver disambiguation` (this commit) landing the procedural smoke document at `docs/smoke/p13_s50_resolver_disambiguation.md` (five-stage walk against tenant_a's three accumulated "Q3 portfolio review" cases), the captures entry at `log/captures.md` recording the resolution-ambiguity disposition with the P14 second-instance recurrence test named, the S50 brief preservation at `briefs/p13/s50.md` per D43, this session-log entry, and the S50 close marker at `charter/current-package.md`.

- Decisions: No new D-entries; one captures-entry formalisation recording the resolution-ambiguity-routes-to-D134-shape-aware-clarification disposition with the P14 second-instance trigger named for explicit architecture.md prose addition. The disposition operates within D134's existing three-case discipline as a sub-case of Case 2; the cell-layer extension is application of an existing primitive rather than a new architectural commitment.

- Tests: 1787 unit + contract tests pass (+11 from S49/S48b baseline of 1776: 2 in test_resolve_target.py for the new candidates shape and discriminator carry-through; 9 in test_manual_entry_cell.py for helpers, multi-match dispatch, positional selection, out-of-range re-render, correcting cancel, no-match clarification). 33 of 33 import-linter contracts kept (no new contracts; no contract changes — the extension stays within the existing layers-messaging hexagonal contract). AST enforcement green. No Alembic migration at S50 (PendingClarification schema accommodates the sub-case via the existing dict-typed field).

- Reflection (D134 sub-case accommodation, prompt 1): The pre-write reconciliation Surface 3 hypothesis held cleanly. PendingClarification's `proposed_intent: dict[str, Any]` field tolerates the `resolution_candidates` sidecar key without `parse_intent` rejection (verified by reading `parse_intent` at `contexts/messaging/domain/intent.py:152-188` — it uses `raw.get("intent_type", "")` and reads specific known fields, ignoring unrecognised keys). `proposed_action_summary` accommodates the ambiguity phrasing. No schema change required. The build-at-second-instance discipline closed on the right side: extending the entity now would have built substrate for a single use case before evidence accumulates that other ConversationFlow implementers need a different sidecar shape.

- Reflection (discriminator computation surface, prompt 2): The trade-off landed at eager computation in `find_cases` (the adapter calls `get_case_detail` per case). Cost analysis at framing: ~50-200 ms across N case-detail loads at Phase 2-A scale (≤5 cases per tenant during dogfooding), well under the LLM call's 1.5-3 s baseline. The lazy alternative (compute only on AMBIGUOUS via a new enrich-by-id port method) would have added a port method and an extra adapter pass at the AMBIGUOUS path; the gain (N+1 reduction in find_cases path) does not materialise at Phase 2-A scale. Phase 2-B+ swap to lazy enrichment activates when tenant case counts grow; the adapter is the single change site, no cell-layer or port-shape change required. The choice composes with the existing `find_data_points` adapter pattern which already does N case-detail loads without optimisation.

- Reflection (operator reply pattern coverage, prompt 3): Positional-only as confirmed at framing. The cell handles bare positive integers ("1", "2", "3", "12", etc.) — including whitespace-padding and trailing punctuation per the `_normalise` helper. Descriptive forms ("the older one", "the new one", "the empty one") deferred to second-instance need at P14 or operator-dogfooding signal. The positional parser scope is narrow (whole-message integer only — not "1 please" or "first one") to avoid collision with fresh-turn replies that legitimately start with a digit; the trade-off costs operator friction at the edge (operator must reply with just the number) but eliminates a class of false-positive selections.

- Reflection (first operational application of S49's interface-versus-implementation discipline, prompt 4): **Substantive paragraph because the discipline caught two latent shape decisions at draft time and the methodology evidence is load-bearing.** S50 is the first session from S49 forward to exercise the standing pre-write reconciliation Surface 6. The discipline fired twice productively: (a) at the CaseSummary DTO extension — the interface commitment is "the case-lookup port returns multi-match results with disambiguating signals" (CaseSummary gains discriminator fields); the implementation behind it is the specific signals chosen (`created_at`, `last_activity_at`, `data_point_count`). Future Phase 2-B+ signals (e.g., `case_priority`, `tenant_owner_last_referenced`) extend the DTO without breaking the resolution path; the interface stays stable; (b) at the PendingClarification reuse — the interface commitment is "proposed_intent is the cell's structured-intent envelope at pending creation and re-parse-able at resolution"; the implementation behind it is the specific dict shape (which key carries `resolution_candidates`). Any future Phase 2-B+ resolution-disambiguation surface that needs richer ambiguity context (e.g., for DataPoint resolution-ambiguity, or for cross-context resolution-ambiguity at P14 implementers) extends the dict shape without changing the entity schema. The discipline operationally proved its forward-relevance binding from S49 commit 1 — both shape decisions were committed at the interface altitude with implementation choices explicitly separable. Forward methodology evidence: the standing pre-write reconciliation surface binds productively at the framing-time check; the discipline's recurrence test from S49 closes positively at first instance.

- Reflection (resolution-ambiguity first-instance shape, prompt 5): The captures entry frames the disposition as convention-altitude at one instance with P14 second-instance triggering architecture.md prose addition. S50 did not surface any architectural-altitude observation that would justify committing the disposition at architecture.md now. The disposition lives within D134's three-case discipline as a sub-case of Case 2; the cell-layer extension is application of an existing primitive. If P14 audit-conversation or mirror-conversation implementers face the same sub-case at their resolution surfaces, the cross-implementer commitment language at architecture.md becomes load-bearing; until then, the captures entry is the right home. Build-at-second-instance discipline (D127 alternative (d), promoted as part of S49's three pre-write reconciliation disciplines) binds here.

- Reflection (methodology surfaces, prompt 6): **Substantive paragraph because two methodology candidates surface at S50 worth recording.** First, the **first operational application of S49's interface-versus-implementation discipline** is forward methodology evidence the discipline binds at the framing-time pre-write reconciliation surface as the S49 promotion forecast. Caught two latent shape decisions at draft time (CaseSummary DTO discriminator-field extension as interface; PendingClarification dict shape as interface) before code drafts. The discipline composes cleanly with the existing path-naming and build-at-second-instance disciplines as the three standing pre-write reconciliation surfaces. Second, the operator surfaced a **sub-shape of the scope-discipline-at-brief-authoring pattern** at S50 framing — the brief left reply-pattern coverage as "framing's choice" rather than committing the load-bearing minimum at brief drafting. Neither the maximal-by-precedent drift the S48b first instance recorded nor the load-bearing-minimum the discipline aims at; this is a third mode, functionally similar to maximal-by-precedent because both push the scope-narrowing work to framing reconciliation rather than handling it at the brief. The S49 captures entry (formalized at S49 commit 2) may want refinement at next hygiene to acknowledge the three modes: load-bearing-minimum-committed (target shape); maximal-by-precedent (S48b first instance); deferred-to-framing (S50 sub-shape). Both observations forward to the next hygiene methodology review.

methodology: First operational application of S49's interface-versus-implementation discipline as a standing pre-write reconciliation surface; the discipline caught two latent shape decisions at draft time (CaseSummary DTO discriminator-field extension committed as interface; PendingClarification dict shape committed as interface). Resolution-ambiguity disposition recorded at captures with P14 second-instance trigger named for architecture.md prose addition per the build-at-second-instance discipline. A sub-shape of scope-discipline-at-brief-authoring surfaced at S50 framing (deferred-to-framing mode alongside the maximal-by-precedent mode S48b first instance recorded); the S49 captures-entry formalization may want refinement at next hygiene to acknowledge the three modes (load-bearing-minimum-committed; maximal-by-precedent; deferred-to-framing).

```
metrics:
  classification: build session
  brief_started: 2026-05-26
  session_started: 2026-05-26
  session_closed: 2026-05-26
  merged: 2026-05-26
  close_state: clean (substrate disposition holds at the load-bearing surface; live-stack smoke is procedural for operator execution; 1787 tests pass; 33/33 import-linter contracts kept; first operational application of S49 interface-versus-implementation discipline binds productively)
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: contexts/messaging/application/ports/portfolio_gateway.py (CaseSummary discriminator-field extension); contexts/messaging/application/resolve_target.py (TargetCandidate.discriminators + ResolutionOutcome.candidates); apps/api/_portfolio_gateway_wiring.py (find_cases eager enrichment); contexts/messaging/application/manual_entry_cell.py (helpers + multi-match dispatch + positional-reply branch + handlers); tests/unit/contexts/messaging/application/test_resolve_target.py (2 new tests); tests/unit/contexts/messaging/application/test_manual_entry_cell.py (9 new tests + helper); tests/unit/contexts/messaging/application/test_portfolio_gateway_port.py (CaseSummary fake gateway update); docs/smoke/p13_s50_resolver_disambiguation.md (procedural smoke document); log/captures.md (S50 disposition entry); log/sessions.md (this entry); charter/current-package.md (S50 close marker); briefs/p13/s50.md (brief preservation per D43)
  corrects:
  corrected_by:
```



