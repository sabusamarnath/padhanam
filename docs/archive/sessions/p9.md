# Archive — P9 sessions

## P9 — S31 — contexts/run_history bounded context, three-table substrate, runs-row write path, tenant-isolation harness extension
roles: architect, engineer
mode: build

- Produced: Nine commits closing S31 per the brief. Commit 1 (`docs(p9/s31)`, 547f3ce) landed D95 at `charter/decisions.md` with the three-table schema, FK-deletion-behaviour commitments, write-timing commitment (shape B), and audit-chain partial-state shape; `charter/schema.md` extended with the run-history per-tenant section; `charter/packages/p9-epic.md` S31 line replaced; `charter/current-package.md` transitioned to "P9 active — S31 in flight"; `briefs/p9/s31.md` preserved verbatim with a Reconciliation-outcomes appendix recording the brief-shape-versus-as-landed delta. Commit 2 (`feat(p9/s31)`, 8eeffa0) opened the `contexts/run_history/` bounded context with the hexagonal-layer convention and a new `layers-run-history` import-linter contract (25 contracts kept, up from 24). Commit 3 (`feat(p9/s31)`, d396b16) defined the consumer port `RunHistoryWriter` plus DTO `AgentRunRecord`, the producer-side `RunHistoryRepositoryPort`, the `record_run` use case with D75-style auth posture, and the `api.py` re-export. Commit 4 (`feat(p9/s31)`, f02ed59) shipped Alembic revision `0011_create_run_history` plus the `PostgresRunHistoryAdapter`; `make migrate` applied to both tenants; the six `\d <table>` queries confirmed the as-landed schema matches D95 byte-for-byte. Image rebuilt via direct `docker build` (the brief's `make build-api` target does not exist; absorbed). Commit 5 (`feat(p9/s31)`, fd71758) wired `RunHistoryWriterAdapter` at `apps/cli/_cross_context.py` (the eighth wiring adapter class on the file) plus the AgentRuntimeComposition extension at `apps/api/routers/agent.py` and `apps/api/_agent_runtime_wiring.py`. Commit 6 (`feat(p9/s31)`, 2eb2bed) extended `invoke_agent` with the accumulator plus the `_assemble_agent_run_record` helper mapping terminal event class to run record per D95's audit-chain partial-state shape, plus the post-terminal-yield writer call. Commit 7 (`feat(p9/s31)`, e90f798) extended the D24 tenant-isolation harness with nine new scenarios. Commit 8 (`test(p9/s31)`, e66ec38) ran the live-stack smoke against tenant_a producing one runs row with audit hashes byte-for-byte matching the audit chain; image digest pinned at `sha256:838e3ec94e2b...`. Commit 9 (`docs(p9/s31)`, c0fc3c3 + the follow-up commit landing this entry) transitions `charter/current-package.md` to "S31 closed; S32 next" and appends this session-log entry.

- Decisions: D95 lands at S31 commit 1 per the brief's Appendix A with three structural-honesty revisions absorbed through the user-question-at-session-open path: (a) `audit_end_hash` nullable under `termination_reason='failed'` with paired CHECK, mapping the executor's `InvocationFailed.partial_audit_chain_state` 0/1/2-element shapes to skip-write / write-with-null-end-hash / write-with-both-hashes respectively; (b) `termination_reason` CHECK with six values (the five `TerminationReason` enum values plus synthesised `failed` for the InvocationFailed terminal event class which has no `termination_reason` field); (c) migration revision number `0011_create_run_history` (brief assumed `0010_*`). D95 alternatives (i), (j), (k) record the rejected shapes: shape A (write before terminal yield), schema-strict NOT NULL with skip-write for <2 hashes, enum-exact-mirror collapsing InvocationFailed onto ERROR. No further D-entries at S31; subsequent reconciliation findings (Makefile build-api gap, .importlinter location at repo root not in pyproject.toml) were absorbed without commit per the brief's framing-prompt-as-recommendation pattern.

- Tests: 731 tests pass at session close across unit + the new tenant-isolation contract harness. 62 net new tests at S31: 29 RunRecord invariant tests, 4 record_run use-case tests, 3 api-surface tests, 5 Postgres adapter tests, 5 RunHistoryWriter port tests at agent context, 7 invoke_agent run-record assembly tests, 9 tenant-isolation contract tests. 25 import-linter contracts kept (the new `layers-run-history` contract added at commit 2; the existing seven contracts extended to cover `contexts.run_history`). AST enforcement passes (9 tests). The pre-existing `psql`-missing-in-padhanam-api-image failures from S30b carry over to S31.

- Enforcement layer: `layers-run-history` import-linter contract added enforces the hexagonal layer ordering for the new context. Seven existing contracts extended to cover `contexts.run_history`: cross-context independence (domain, application, adapters), domain-purity, litellm-confined, langfuse-confined, neo4j-confined. AST-level cross-context independence enforced at `tests/unit/contexts/agent/application/ports/test_run_history_writer.py` (the writer port module imports nothing from `contexts.run_history`) and at `tests/unit/contexts/run_history/test_api_surface.py`. The tenant-isolation contract at `tests/contract/tenant_isolation/test_run_history_isolation.py` extends the D24 harness: structural (information_schema query confirms tables exist on tenant DBs and not on control plane; pg_constraint queries confirm the chunk_id FK's SET NULL behaviour and the run_id FK existence) and behavioural (synthetic dual-tenant fixture confirms cross-tenant write isolation, read isolation, and the adapter's bound_tenant_id defence-in-depth pre-routing check).

- methodology: Pre-write reconciliation fired its eighth instance at S31 session-open and surfaced more structural-honesty deviations than any prior instance. Three required user-question resolution: the `InvocationFailed.partial_audit_chain_state` variable-length shape from `agent_loop_executor.py`'s three try-block emission sites; the `TerminationReason` enum's five values vs the brief's five with two mismatches plus the InvocationFailed event class's lack of a termination_reason field; the audit emission location (inside the executor, not in `invoke_agent`). Three more were absorbed without user question per the framing-prompt-as-recommendation pattern: migration revision number, AgentRuntimeComposition dataclass location, ContentDelta field name. Two surfaced after the work began: missing `make build-api` target (direct `docker build` worked); `.importlinter` at repo root not in `pyproject.toml`. Pattern continues to do load-bearing work; eighth instance is the pattern's largest find-rate yet.

- methodology (second): Consumer-port-plus-wiring-adapter reached its seventh reinforcement at S31. The S31 instance is the first against a brand-new producer context — `contexts/run_history/` had no prior wiring history at session-open. The pattern applied symmetrically without per-context refinement. Cross-app re-use also held: `apps/api/_agent_runtime_wiring.py` imports `RunHistoryWriterAdapter` from `apps/cli/_cross_context.py` per the S30b precedent.

- methodology (third): Framing-prompt-as-recommendation held at multiple S31 surfaces. The 1-hash InvocationFailed case forced a Question-1 moment with the executor-code reading driving the audit_end_hash nullable decision; the Q2 termination_reason CHECK revised the prior two-column proposal in favour of single-column six-value mapping with synthesised `failed`. The migration number, AgentRuntimeComposition location, ContentDelta field name absorbed without question. The Makefile gap and .importlinter location absorbed without commit; the workaround is documented in the smoke markdown.

Reflection (four prompts per the brief):

1. **Pre-write reconciliation outcomes — eighth instance.** Six brief-named surfaces verified at session-open. (a) `alembic/tenant/` track: confirmed the next migration number is `0011_create_run_history` per the brief's anticipated deviation. CHECK-constraint and index-naming conventions confirmed against `0005_create_sources_and_chunks`. `pg.JSONB()` and `sa.text("gen_random_uuid()")` idioms confirmed. (b) `tests/contract/tenant_isolation/` harness: confirmed each test file defines its own function-scoped `isolation_setup` fixture; `conftest.py` only provides principal fixtures. (c) `apps/cli/_cross_context.py`: confirmed seven existing adapter classes; the S31 `RunHistoryWriterAdapter` lands as the eighth. (d) `apps/api/_agent_runtime_wiring.py`: confirmed the AgentRuntimeComposition dataclass lives at `apps/api/routers/agent.py` (not at `_agent_runtime_wiring.py` as the brief said); absorbed without question. (e) `contexts/agent/application/use_cases.py` `invoke_agent`: the load-bearing surface; the docstring revealed audit emission happens inside the executor (resolving the brief's reflection-prompt-3 ambiguity on auth-failure-path audit-emission ordering: auth-failed produces no audit row), and the event types confirmed `ContentDelta.text_fragment`, `InvocationFailed.partial_audit_chain_state: tuple[str, ...]` variable-length, and `TerminationReason` enum's five values differing from the brief's five. (f) `charter/schema.md`: confirmed per-tenant tables convention. The pattern's eighth instance produced more structural findings than any prior — the InvocationFailed partial-state shape was the largest single find because it forced a real schema decision (audit_end_hash nullable with paired CHECK) rather than a mechanical adjustment.

2. **Consumer-port-plus-wiring-adapter at the agent-to-run-history altitude — seventh reinforcement.** The new context's brand-new state at session-open made the pattern straightforward to apply: no prior wiring history to reconcile, no existing port shapes to migrate. The agent context defined `RunHistoryWriter` against its own `AgentRunRecord` DTO without importing from `contexts.run_history`; the run-history context exposed `record_run` at `api.py`; the wiring adapter did the DTO-to-domain translation. The pattern's altitude-agnostic shape continued to do load-bearing work: cross-context, cross-composition-root re-use, and AST-enforcement-layer (the writer port module imports nothing from `contexts.run_history` per the test_run_history_writer.py AST parse). The S30b first-instance cross-composition-root re-use generalised naturally to S31's new wiring adapter; no per-context refinement required. Seven instances now, eligible for promotion to `charter/methodology.md` at the Phase 1 close audit per the existing captures entry.

3. **The invoke_agent use case as post-stream write seam, with the write-timing commitment exercised.** Shape B held cleanly in code. The accumulator block before the loop initialised four values (`invocation_id`, `started_at`, `iteration_count`, `cost_accumulator`); per-event updates inside the loop fired exactly where expected; the post-terminal-yield write call invoked the `_assemble_agent_run_record` helper which returned None for the 0-hash InvocationFailed case and an AgentRunRecord otherwise. The unit-test ordering assertion at `test_terminal_event_yields_before_writer_called` enforced shape B via a shared call-ordering list — terminal event reaches the consumer first; writer call follows; the assertion `ordering == ["consumer:InvocationCompleted", "writer.record_run"]` passed first time. The four termination paths assembled cleanly: InvocationCompleted reads termination_reason from `event.termination_reason.value`, audit hashes from `event.audit_chain_hashes`, output_content from `event.final_result`, total_cost from `event.total_cost_usd`; InvariantBlocked synthesises `termination_reason='invariant_blocked'`; InvocationFailed at 1-hash takes `partial[0]` as audit_start_hash and None as audit_end_hash; InvocationFailed at 2-hash (forward-affordance) takes both. No structural friction surfaced between the two responsibilities that argues for refactoring at S32 — the seam is bounded and the helper extraction kept invoke_agent's main body readable.

   The audit_start_hash captured at the start audit emission and the audit_end_hash on terminal events made the chain-linkage commitment straightforward — the smoke-time `\d` query confirmed byte-for-byte match between the runs row's hash columns and the tenant_audit chain's `agent.invoke.start` / `agent.invoke.end` rows. Phase 2 UX's missing-row reconciliation path has a concrete operational signature: the audit chain's `this_event_hash` matches the runs row's `audit_end_hash` for clean invocations; mismatch or runs-row-absence with audit-presence is the "run executed but projection missing" surface D95 names.

   **State matrix (future-self map without re-deriving from code).** Six client-visible states; four populated database states:
   - Client state 1, auth-failed: no events at all, no audit row, no runs row. The auth check at first `__anext__` fires before `executor.execute` is invoked.
   - Client state 2, pre-start-audit failure (InvocationFailed with empty `partial_audit_chain_state`): the executor's start-audit-emission failure site at `agent_loop_executor.py:152-164`. Emits InvocationFailed event; no audit row exists; no runs row written (`_assemble_agent_run_record` returns None).
   - Client state 3, post-start-audit failure with 1-hash (loop-body unhandled exception at `agent_loop_executor.py:434-441`; end-audit emission failure at `agent_loop_executor.py:449-465`): InvocationFailed event with `partial_audit_chain_state=(start_hash,)`; audit chain has the start row only; runs row exists with `termination_reason='failed'`, `audit_start_hash=partial[0]`, `audit_end_hash=NULL`.
   - Client state 4, post-end-audit failure with 2-hash (forward-affordance, currently no executor codepath fires this): InvocationFailed event with both hashes; both audit rows exist; runs row exists with `termination_reason='failed'`, both hash columns populated.
   - Client state 5, clean terminal events (InvocationCompleted with one of CONTENT/MAX_ITERATIONS/TOOL_NOT_REGISTERED/ERROR termination_reason, or InvariantBlocked): both audit rows exist; runs row exists with termination_reason matching the event class.
   - Client state 6, runtime-succeeded at writer-failure: terminal event reaches the SSE client; the writer call raises post-yield; no runs row exists; audit chain has both rows; Phase 2 UX must reconcile (the audit chain's `agent.invoke.end` row's `this_event_hash` is the join key the missing runs row would have carried in `audit_end_hash`).

   Database states (collapsed): No runs row (states 1, 2, 6); runs row with `termination_reason='failed'` and `audit_end_hash=NULL` (state 3 — the operationally-fired failure case at S31); runs row with `termination_reason='failed'` and both hashes (state 4 — forward-affordance, never fires at S31); runs row with `termination_reason ∈ {content, max_iterations, tool_not_registered, error, invariant_blocked}` and both hashes (state 5 — the dominant success case).

4. **The FK-deletion-behaviour commitment in D95.** ON DELETE SET NULL on `run_chunk_citations.chunk_id` held cleanly against the migration shape — the `pg_constraint` query at the contract test confirmed `confdeltype='n'` (SET NULL) on both tenant_a and tenant_b after `make migrate`. No test-fixture complications surfaced because S31 doesn't write citation rows (population lands at S32); the FK constraint state is structurally defined but not exercised under fixture teardown at this session. The same shape (FK to producer table, SET NULL on producer deletion, snapshot columns carry the rendering payload) generalises to other forthcoming citation surfaces: any future audit-evidence-bearing reference to a deletable producer entity can adopt this pattern. The asymmetry with `run_entity_citations` (no FK to Neo4j is possible; the composite `(entity_tenant_id, entity_name, entity_type)` is the documented join key, and snapshot columns carry the rendering payload regardless of entity merge/removal) is the natural extension: where Postgres FKs are available, use them with SET NULL on producer-side deletion; where they're not (cross-store references), commit to documented join keys plus snapshot columns. The audit-evidence-by-default property D94 names is structurally enforceable for chunks and operationally maintainable for entities.

```
metrics:
  classification: new_work
  brief_started: 2026-05-13
  session_started: 2026-05-13
  session_closed: 2026-05-13
  merged: 2026-05-13
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: updated
  corrects:
  corrected_by:
```

## P9 — S33 — UX-shaped query port, RunRecord-as-aggregate read DTO, four-filter list vocabulary, cursor pagination
roles: architect, engineer
mode: build

- Produced: Seven commits closing S33 per the brief. Commit 1 (`docs(p9/s33)`, 65f098d) landed D97 at `charter/decisions.md` with the four-shapes choice (RunHistoryReader port at run_history producer context; RunRecord-as-aggregate read DTO; four-filter RunListFilters vocabulary; cursor pagination on `(started_at, id)` with base64-of-JSON opaque cursor); twelve alternatives recorded including (k) the RunWithCitations wrapper rejection per the session-open reconciliation and (l) the citation sort-key reasoning. `charter/packages/p9-epic.md` S33 line replaced with as-landed scope; `charter/current-package.md` transitioned to "S33 in flight" with the framing paragraph naming the read-DTO-symmetry call. `briefs/p9/s33.md` preserved with Appendix D capturing the reconciliation outcomes (one structural-honesty finding plus six mechanical absorptions plus one operational-no-impact). Commit 2 (`feat(p9/s33)`, 401ff15) shipped the `RunHistoryReader` Protocol port plus `RunListPage` envelope at `contexts/run_history/ports/reader.py`; `RunListFilters`, `RunListCursor`, and `MalformedCursorError` at `contexts/run_history/domain/query_filters.py`; the base64-of-JSON cursor codec at `contexts/run_history/application/cursor.py`; 36 unit tests fencing every invariant including the bool-disguised-as-int page-size rejection. Commit 3 (`feat(p9/s33)`, 61ac6f2) the `PostgresRunHistoryReader` adapter at `contexts/run_history/adapters/outbound/postgres/reader.py` implementing the port via three queries (runs by id; chunk citations by run_id; entity citations by run_id) rather than a single LEFT JOIN to avoid the cartesian-product blowup; 21 unit tests cover the brief's commit-3 scenarios. Commit 4 (`feat(p9/s33)`, c2038d8) wired `RunHistoryReaderAdapter` at `apps/cli/_cross_context.py` (the ninth wiring class on that file) plus `build_run_history_reader` factory at `apps/api/_agent_runtime_wiring.py`; 3 wiring tests. Commit 5 (`test(p9/s33)`, fdacc97) extended the D24 tenant-isolation contract harness to 16 scenarios with three D97 read-side scenarios (cross-tenant `get_run` returning None; cross-tenant `list_runs_with_filters` returning empty; bound-tenant-id pre-routing defence on both methods). Commit 6 (`test(p9/s33)`, f4be15c) ran the live-stack smoke against tenant_a producing all four scenarios end-to-end (get_run with citations; list with no filters; list with `termination_reasons=("content",)`; cursor pagination at page_size=1 across two pages); the smoke surfaced one real-Postgres finding the unit tests' fake session did not catch (tuple comparison `uuid < varchar` operator-resolution failure) and the fix landed in the same commit (`sa.cast(..., pg.UUID)` on the cursor.id parameter); image rebuilt at `padhanam-api:dev@sha256:a05b87b9...` and compose digest pin updated. Commit 7 (`docs(p9/s33)`, this commit) transitions `charter/current-package.md` to "S33 closed; S34 next" and appends this session-log entry.

- Decisions: D97 lands at S33 commit 1 per the brief's Appendix A with one structural revision absorbed through the user-question-at-session-open path: the brief's drafted `RunWithCitations` wrapper would have duplicated the citation tuple fields already present on `RunRecord` from S32's D96 extension; the brief's own Choice text named "the read DTO is symmetric with the write side: the same record types travel both directions," which points to reusing `RunRecord` directly. Operator resolution at session-open: drop the wrapper; `get_run` returns `RunRecord | None`. The sub-finding on citation sort key (`created_at` not on the domain records) absorbed alongside via the same user-question moment: sort by `id ASC` rather than adding `created_at` to the records. D97 alternative (k) records the wrapper rejection; alternative (l) records the sort-key reasoning. No further D-entries at S33; six mechanical reconciliation findings (port path at `ports/reader.py` not `application/ports/run_history_reader.py`; adapter path at `adapters/outbound/postgres/reader.py` not `adapters/outbound/postgres_run_history_reader.py`; DTO-module placement at `domain/query_filters.py` plus `ports/reader.py`; latest migration head verified at `0012_revise_citation_snapshots`; read session pattern without `session.begin()`; wiring shape mirroring writer's class+factory split) absorbed without commit per the framing-prompt-as-recommendation pattern. One operational-state finding (tenant_a's S32 rows still present, no smoke recovery needed) verified at smoke time.

- Tests: 831 unit tests pass at session close, up from S32's 774. Net delta at S33 unit suite: **+57 net new tests**. Breakdown: 16 RunListFilters/RunListCursor/MalformedCursorError invariant tests at `test_query_filters.py`; 18 cursor codec tests at `test_cursor.py` covering round-trip plus 11 malformed-input scenarios; 3 reader-port surface tests at `test_reader_port_surface.py` (protocol method count, RunListPage shape, AST import-isolation); 21 PostgresRunHistoryReader unit tests at `test_postgres_reader.py`. Plus 3 wiring tests at `tests/integration/apps/cli/test_run_history_reader_adapter.py` (Protocol satisfaction; session factory called per call with TenantContext; round-trip through per-call PostgresRunHistoryReader). Plus 3 tenant-isolation contract scenarios extending the D24 harness from 13 to 16 scenarios. 25/25 import-linter contracts kept (the existing `layers-run-history` contract from S31 covers the new files cleanly because the read port sits at the ports/ layer between application and domain per hexagonal ordering). AST enforcement passes. Pre-existing failures from before S31 (methodology/tool CLI command tests; ingestion e2e tests) carry over unchanged.

- Enforcement layer: No new import-linter contracts at S33. The existing 25 contracts (including `layers-run-history` from S31) extend cleanly to the new files: `contexts/run_history/ports/reader.py` sits at the ports/ layer; `contexts/run_history/domain/query_filters.py` at the domain/ layer; `contexts/run_history/application/cursor.py` at the application/ layer; `contexts/run_history/adapters/outbound/postgres/reader.py` at the adapters/ layer. AST cross-context-independence at the new `test_reader_port_surface.py` confirms the reader port module imports nothing from forbidden cross-context modules. The tenant-isolation contract harness at `tests/contract/tenant_isolation/test_run_history_isolation.py` now has 16 scenarios (13 from S31+S32 + 3 from S33) covering: per-tenant table existence; control-plane has no run-history tables; FK constraints on chunk_id (SET NULL) and run_id (CASCADE); cross-tenant write isolation on runs/citations; bound-tenant-id pre-routing defence on writer; cross-tenant `get_run` returns None; cross-tenant `list_runs_with_filters` returns empty; bound-tenant-id pre-routing defence on both reader methods.

- methodology: Pre-write reconciliation fired its tenth instance at S33 session-open with a further compressed find-rate from S32. S33's distribution: **one structural-honesty (with one sub-decision absorbed alongside via the same user-question moment) plus six mechanical absorptions plus one operational-no-impact.** The compression continues the substrate-stability trajectory: S31 (new bounded context, three structural-honesty findings) → S32 (citation extension, one structural-honesty finding) → S33 (read port at existing context, one structural-honesty finding). The structural-honesty finding at S33 was the `RunWithCitations` wrapper redundancy with `RunRecord`'s existing citation fields; the brief's own D97 Choice text named the symmetry-with-write-side argument that resolved the question. The pattern's load-bearing value at S33 was less about catching architectural drift (substrate is stable) and more about catching brief-versus-built-state mismatch (the brief was written against the abstract D94/D95 framing; it did not fully account for S32's actual extension of `RunRecord` with citation tuples). Worth recording as a sub-observation: pre-write reconciliation's find-rate compresses as substrate stabilises, but the pattern still does load-bearing work because briefs are written at framing time and built-state evolves between framings.

- methodology (second): Consumer-port-plus-wiring-adapter reached its eighth-and-a-half reinforcement at S33 with an interesting variant. The eighth instance at S32 grew the AgentRetrievalClient port from `tuple[RetrievedChunk, ...]` to a `RetrievalResult` envelope; the S33 variant inverts the relationship — the producer-side port `RunHistoryReader` at `contexts/run_history/ports/reader.py` is the consumer-shape (UX-shaped) but lives at the producer context, mirroring S22's `RetrievalClient` pattern rather than S31's `RunHistoryWriter` (which lives at the consumer agent context). The two altitudes the pattern now operates at: (a) consumer-side port + producer-aggregate translation at wiring adapter (S22 `RetrievalClient`, S26a-1 `MethodologyLookup`, S26a-2 `RoleLookup`, S27b `AgentRetrievalClient` + `MethodologyOverridesLookup`, S28b `ToolDefinitionsLookup` + `ToolInvoker`, S29b SSE transport, S31 `RunHistoryWriter`); (b) producer-side port shaped to consumer needs + wiring adapter as the per-tenant-routing wrapper (S22 `RetrievalClient` first instance; S33 `RunHistoryReader` second instance). The structural distinguishing factor between altitudes (a) and (b): whether the consumer is a bounded context (altitude a) or a composition surface (altitude b). The pattern's altitude-agnostic shape continues to hold; the half-step at S33 is the second instance of altitude (b), suggesting the pattern has structural variance worth surfacing at Phase 1 close audit.

- methodology (third): Live-stack-only-catches finding class. Real-Postgres surfaced one finding the unit tests' fake session structurally could not catch: tuple comparison `(timestamptz, uuid) < (param_timestamptz, param_varchar)` raises `operator does not exist: uuid < character varying` at the Postgres operator-resolver step. The unit tests passed because the fake session does not enforce Postgres-strict operator resolution; the finding only surfaced at the live-stack smoke. Worth recording as a class of finding: type-strict operator resolution at the database boundary is something fake-session unit tests structurally cannot catch. The live-stack smoke is doing load-bearing work at altitudes the unit tests' fake-session strategy cannot reach. Phase 1 close audit candidate for promoting the live-stack smoke pattern as the strictly-required complement to fake-session unit tests for any database-touching adapter.

Reflection (four prompts per the brief):

1. **Pre-write reconciliation, tenth instance.** S33's find-rate distribution (one structural-honesty + one sub-decision absorbed alongside + six mechanical + one operational-no-impact) compressed further from S32's baseline ("one structural-honesty + four mechanical + one operational-recovery"). The trajectory S31 → S32 → S33 (three+three+two → one+four+one → one+six+one) tracks substrate stability cleanly: each successive session has less structural surface to verify because the bounded context's commitments have settled. The single structural finding at S33 was a brief-versus-built-state mismatch rather than an architectural-drift discovery: the brief was written at framing time against D94/D95's abstract substrate; D96's actual extension of `RunRecord` with citation tuples at S32 made the brief's drafted `RunWithCitations` wrapper structurally redundant. The pattern still does load-bearing work because briefs decay between framings — but its scope shifts from "catch architectural inconsistencies" (S28b shape) to "catch brief-versus-built-state mismatches" (S33 shape). Worth recording as a sub-observation on the methodology promotion candidate at Phase 1 close audit: the pattern operates at two distinct altitudes (architectural-drift detection at substrate-shaping moments; brief-versus-built-state reconciliation at substrate-extension moments); the cadence of which altitude fires tracks where the codebase is in its evolution cycle.

2. **Aggregate DTO and filter vocabulary as built.** `RunRecord`-as-aggregate held the load cleanly. The three-query approach to `get_run` (runs by id, then chunk citations by run_id, then entity citations by run_id) produced clean separation of concerns at the assembly layer; no cartesian-product blowup, no row-deduplication code at the application layer. The structural cost (three round-trips instead of one) is bounded by Phase 1 citation cardinality (single-digit per run for retrieval) so the trade-off is neutral at Phase 1. The four-filter vocabulary held: each filter compiled to clean SQL (IN clauses for multi-value, range comparison for `started_at_range`), and the combination matrix worked without per-pair special handling (all four AND together as the SQL conjunction). `agent_template_version` paired with `agent_template_id` naturally as two independent IN clauses; the alternative (single combined `(template_id, version)` tuple match) would have been over-engineered for the Phase 1 cardinality. The absence of `jurisdiction` filter was not felt at smoke because tenant_a is single-jurisdiction; the deferral until multi-jurisdiction tenants surface holds.

3. **Cursor pagination at first product-form use.** The cursor-on-`(started_at, id)`-tuple-comparison shape landed correctly under SQLAlchemy + asyncpg with one real-Postgres finding the unit tests did not catch: tuple comparison `(uuid_column, ts_column) < (param_ts, param_str)` fails at the Postgres operator-resolver step with "operator does not exist: uuid < character varying" because the asyncpg parameter binding defaults to VARCHAR for string values. The unit tests passed because the fake session does not enforce type-strict operator resolution. The fix is one line — `sa.cast(str(cursor.id), pg.UUID(as_uuid=False))` on the right-hand side of the tuple — but the finding is structural: the unit tests cannot catch this class of error because the failure mode lives at the database boundary. Equal-timestamp boundary cases were not exercised at smoke (tenant_a's two runs have distinct timestamps), but the SQL shape `(started_at, id) < (cursor_ts, cursor_id)` is the same row-comparison shape that handles equal-timestamps correctly under Postgres's standard semantics; the unit test `test_list_equal_timestamps_paginate_via_id_tiebreaker` exercises this via the fake-session pathway. The cursor encoding survived the SQL round-trip at smoke (verified by the `next_cursor_encoded` field's base64 string in the output). No divergence from the brief's recommended shape beyond the type-cast fix.

4. **What S33 leaves for S34.** Build evidence at S33 points more strongly toward S34 pivoting to the HTTP route layer surfacing the run-history reader than staying on the ingestion-management track. Three reasons converged. First, the reader substrate is now complete (port + adapter + wiring) and one more session (HTTP routes + handler) closes the end-to-end story Phase 2 UX needs to consume. The ingestion-management API is closer to operational tooling than to Phase 2 UX's primary consumer story. Second, the cursor codec at `contexts/run_history/application/cursor.py` is the substrate the HTTP layer's request/response boundary already needs; landing the HTTP layer at S34 exercises the codec at its intended boundary rather than leaving it as a partially-exercised module. Third, the four-filter vocabulary at `RunListFilters` is the natural input to a `GET /runs?agent_template_id=X&termination_reason=Y` query-string parser; the HTTP layer at S34 turns the structured filter object into a usable web surface. The ingestion-management API can land at S35 or P9 close without prejudice; the carryover from P6 is HTTP-shape generic and the substrate (the ingestion use cases) is fully in place. Recommend S34 framing pivots to the HTTP routes for run history; the strategic-mode conversation at S34 framing settles the call against operator priority.

```
metrics:
  classification: new_work
  brief_started: 2026-05-13
  session_started: 2026-05-13
  session_closed: 2026-05-14
  merged: 2026-05-14
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: updated
  corrects:
  corrected_by:
```

## P9 — S32 — citation linking, snapshot population, single-transaction completion seam, citation schema revision
roles: architect, engineer
mode: build

- Produced: Eleven commits closing S32 per the brief. Commit 1 (`docs(p9/s32)`, e4503f1) landed D96 at `charter/decisions.md` with the four-shapes choice (candidate types, ToolCallCompleted extension, schema revisions, single-transaction multi-table write), ten alternatives considered, and a Kano breakdown; `charter/schema.md` updated for both citation tables (source_citation→source_snapshot jsonb, entity_display_label→source_chunk_ids text[]); `charter/packages/p9-epic.md` S32 line replaced; `charter/current-package.md` transitioned to "S32 in flight"; `briefs/p9/s32.md` preserved verbatim with Appendix D capturing the six pre-write reconciliation outcomes. Commit 2 (`feat(p9/s32)`, 7d35283) opened `contexts/agent/domain/citation_candidates.py` with `ChunkCitationCandidate`, `EntityCitationCandidate`, and the discriminated union `CitationCandidate`; 22 unit tests fence every invariant. Commit 3 (`feat(p9/s32)`, fe25143) extended `ToolCallCompleted` with `citation_candidates: tuple[CitationCandidate, ...] = ()`. Commit 4 (`feat(p9/s32)`, 5ed284c) grew the `AgentRetrievalClient` port shape from `tuple[RetrievedChunk, ...]` to a `RetrievalResult` envelope so the same wiring adapter produces both LLM-facing chunks and citation candidates single-pass; ingestion-side `ChunkResult` extended with `chunk_index` + `source_snapshot`; pgvector SQL projects `c.chunk_index`, `s.file_name`, `s.file_type`. Commit 5 (`feat(p9/s32)`, c2b401e) the executor populates `ToolCallCompleted.citation_candidates` from `result.citation_candidates`. Commit 6 (`feat(p9/s32)`, 62de165) extended `invoke_agent` with chunk_citation_accumulator + entity_citation_accumulator plus within-run deduplication first-seen-wins; `AgentRunRecord` carries the two citation tuples; five new use-case tests. Commit 7 (`feat(p9/s32)`, dc11463) shipped Alembic revision `0012_revise_citation_snapshots`; applied to both tenants via direct alembic-upgrade-by-URL (the registry-driven `make migrate` path was non-operational at session-open). Commit 8 (`feat(p9/s32)`, eb977d4) the new `ChunkCitationRecord` and `EntityCitationRecord` at run-history domain mirror the agent candidates one-for-one per D54; `RunRecord` carries citation tuples; `PostgresRunHistoryAdapter.persist` grew `async with session.begin()` writing all three tables with partial-failure rollback and citation-row tenant-id defence-in-depth; six new adapter tests cover the three-table commit, two failure-rollback cases, and two tenant-id-mismatch defences; fourteen record-invariant tests. Commit 9 (`feat(p9/s32)`, 9580e2d) extended the D24 tenant-isolation harness to 13 scenarios. Commit 10 (`test(p9/s32)`, 2b31735) live-stack smoke against tenant_a wrote one runs row + two chunk citations + one entity citation with `source_snapshot` JSONB populated; image rebuilt and recreated at `sha256:9d1aca6f6464...`. Commit 11 (this entry) transitions `charter/current-package.md` to "S32 closed; S33 next" and appends this session-log entry.

- Decisions: D96 lands at S32 commit 1 per the brief's Appendix A with the Choice naming the four committed shapes (citation candidate value objects with chunk_id/source_id/chunk_index/content_snapshot/source_snapshot/tenant_id/jurisdiction for chunks; entity_tenant_id/entity_name/entity_type/source_chunk_ids/tenant_id/jurisdiction for entities; discriminated union; ToolCallCompleted citation_candidates field; schema revisions to JSONB source_snapshot and text[] source_chunk_ids; single-transaction multi-table write in async with session.begin(); within-run dedup first-seen-wins). Reasoning hangs on three load-bearing arguments: storage-versus-render separation (JSONB shape over baked-in display text keeps render shape a Phase 2 read-time concern); audit-evidence-fidelity over render-commitment-at-storage (every retrieved chunk lands a row because no honest signal distinguishes retrieved-and-used from retrieved-and-discarded); chunk-level granularity sufficient at Phase 1 (sub-chunk precision deferred until extraction tools enter). Ten alternatives recorded: keep source_citation text (rejected per storage-versus-render); leave entity_display_label text (same); single polymorphic citation type with kind discriminator (rejected per per-type-invariants clarity); record only LLM-cited (rejected per LLM-discard-signal honesty); discard-explanation layer (rejected per research-territory); sub-chunk precision now (rejected per no-Phase-1-producer); two-transaction write (rejected per partial-failure leak); adapter-level dedup (rejected per projection-assembly-layer ownership); citations on every tool-related event (rejected per event vocabulary muddying); typed source_snapshot Pydantic shape (rejected per evolving-metadata-catalogue coupling). Kano: must-have on the four committed shapes; performance on the within-run dedup policy; delighter on JSONB shape's anticipation of NotebookLM-style ingestion enrichment.

- Tests: 774 unit tests pass at session close across the agent, run-history, ingestion, and tools contexts. Net delta at S32: +22 (citation_candidates), +3 (ToolCallCompleted), +3 (AgentRetrievalClientAdapter translation), +3 (executor citation population), +5 (invoke_agent accumulator dedup), +6 (PostgresRunHistoryAdapter multi-table single-transaction), +14 (ChunkCitationRecord / EntityCitationRecord invariants), +4 (tenant-isolation harness extensions) ≈ **+60 net new tests** (S31 closed at 731; S32 closes at 774 in the unit suite; the gap reflects a few existing tests refactored without count delta). One test name updated (`test_agent_run_record_field_set_matches_d95` → `_matches_d95_d96`). 25 import-linter contracts kept; 9/9 AST enforcement tests pass. Pre-existing failures (not caused by S32 work): methodology/tool CLI command tests carry forward from before S31 due to config shape drift (verified via git-stash regression check); ingestion e2e tests fail because of the tenant-registry-wipe class S30b identified.

- Enforcement layer: No new import-linter contracts at S32. The existing 25 contracts (including the `layers-run-history` contract from S31) extend cleanly: the new `contexts.run_history.domain.citation_records` module sits at the inner-most layer per the hexagonal ordering; the run-history adapter imports the new record types via the layer-respecting path. AST cross-context independence at `tests/unit/contexts/agent/application/ports/test_run_history_writer.py` continues to forbid `contexts.run_history` imports from the agent ports module; the citation-candidate imports are within-agent-context (agent.domain → agent.application.ports) so they pass. The tenant-isolation contract harness at `tests/contract/tenant_isolation/test_run_history_isolation.py` now has 13 scenarios (9 from S31 + 4 from S32) covering: per-tenant table existence on each tenant DB; control-plane DB has no run-history tables; ON DELETE SET NULL on chunk_id; FK on run_id; cross-tenant write isolation on the runs table (both directions); cross-tenant query isolation; bound-tenant-id pre-routing defence on the runs table; cross-tenant write isolation on chunk citations; cross-tenant write isolation on entity citations; bound-tenant-id pre-routing defence on both citation tables; FK ON DELETE CASCADE on run_id deleting all citation children.

- methodology: Pre-write reconciliation fired its ninth instance at S32 session-open with a substantially smaller find-rate than S31 (one structural-honesty surface, four mechanical absorptions, one operational-recovery finding) — the find-rate compression is the methodology signal the brief's reflection prompt anticipated. The single structural-honesty finding (surface 1: ChunkResult/SQL/AgentRetrievalClient triple extension to surface chunk_index + source_snapshot from retrieval) was absorbed without user-question because the brief's intent ("from joining chunks to sources") was unambiguous about what the source-level fields should be. The S31 baseline of "three structural-honesty plus three mechanical plus two post-work" reflected new-bounded-context substrate; S32's substrate extends existing surfaces with no new context boundary, no new aggregate, no audit-chain shape change — so the find-rate naturally compresses. Worth recording as a methodology observation: the reconciliation pattern's find-rate is itself a proxy for substrate stability around the bounded context's commitments; high find-rate sessions are substrate-shaping, low find-rate sessions are substrate-extending.

- methodology (second): Consumer-port-plus-wiring-adapter reached its eighth reinforcement at S32 — but with a new sub-observation: the adapter pattern now demonstrably scales to envelope-shaped return types when the producer needs to surface multiple projections from the same producer-side data set. The `AgentRetrievalClient` port grew from `tuple[RetrievedChunk, ...]` to `RetrievalResult(chunks=..., citation_candidates=...)`. The pattern's altitude-agnostic shape held: the consumer (agent context) defines what it needs (an envelope carrying both projections); the producer (ingestion context) is untouched (still returns raw `ChunkResult`/`EntityResult` from its own port); the wiring adapter at `apps/cli/_cross_context.py` does both translations single-pass. The structural cost of the D17 boundary is that envelope shape evolution is a port change rather than a data change — but the structural benefit is that both projections derive from the same producer-side query, avoiding the double-fetch the alternative (two separate ports) would have required.

- methodology (third): Mirror-types-at-context-boundaries (D54 precedent) earned its second concrete application at S32. S31 mirrored `AgentRunRecord` (agent-context DTO) to `RunRecord` (run-history domain). S32 mirrored `ChunkCitationCandidate`/`EntityCitationCandidate` (agent-context value objects) to `ChunkCitationRecord`/`EntityCitationRecord` (run-history domain). In both instances the wiring adapter at `apps/cli/_cross_context.py` did the field-for-field translation including the additional binding (run_id at S32, which the agent-context candidate does not carry because it is run-identity-implicit). The pattern's structural duplication is the intentional cost of D17's boundary; the structural benefit is independence of context-internal type evolution. Worth tracking toward methodology-document promotion at Phase 1 close audit if a third instance lands.

Reflection (four prompts per the brief):

1. **Pre-write reconciliation, ninth instance.** The S32 find-rate distribution (one structural-honesty plus four mechanical plus one operational-recovery) vs S31's baseline (three structural-honesty plus three mechanical plus two post-work) confirms the find-rate-as-stability-proxy hypothesis the methodology observation above names. S32's substrate-extension nature means the structural decisions are mostly settled (no new bounded context, no new aggregate, no audit-chain shape, no new event type — `ToolCallCompleted` just gets a new field). The single structural finding (ChunkResult/SQL/AgentRetrievalClient triple extension) was a derivative of the brief's commitment to "from joining chunks to sources" rather than a new architectural decision; absorbed without user-question. The four mechanical absorptions (migration number, empty citation tables, structured ToolInvocationResult, executor yield site) were all anticipated by the brief explicitly. The single operational-recovery finding (no seeded role has retrieval in allowlist; tenant registry wiped) anticipated the smoke variant where SSE-driven invocation was structurally infeasible without registry recovery; the direct-adapter smoke landed the same acceptance criterion. Pre-write reconciliation at the ninth instance continues to do load-bearing work; the find-rate compression is the substrate-stability signal worth flagging at the Phase 1 close audit.

2. **Citation as defined versus citation as built.** The storage-versus-render distinction the strategic conversation committed to held cleanly in build. The JSONB `source_snapshot` shape carried the load it was designed to carry: at the pgvector SQL surface, `s.file_name` and `s.file_type` project naturally onto the column; at the citation-candidate construction surface, the dict-shape default-factory absorbs whatever keys ingestion enrichment surfaces in the future without code change; at the persistence surface, asyncpg's JSONB serialisation lands the dict as expected (verified by the smoke's SQL query showing `{"file_name": "03_customer_interviews.md", "file_type": "markdown"}` round-tripping byte-for-byte). The within-run deduplication policy worked cleanly: the unit-test scenario covering same-chunk-retrieved-twice produced one row with first-seen content (`FIRST` over `SECOND`); the entity-composite dedup correctly distinguished `(Acme, Organization)` from `(Acme, Person)` as separate rows. Chunk-level granularity held for the smoke's retrieval case — but the smoke did not exercise the substantive-data-point case the strategic conversation deferred (structured extraction), so the chunk-level boundary's stress test is queued for whenever extraction tools enter Phase 2. No pushback from the build evidence on the structured-versus-text storage decision; if anything, the structured shape simplified persistence (a dict pasted into the insert payload vs constructing a rendered string at write time).

3. **Single-transaction multi-table write at first product-form use.** The seam landed cleanly. The implementation matches the brief's commit 8 expectations exactly: `async with session.begin()` wraps the `runs` insert plus optional `run_chunk_citations` bulk insert plus optional `run_entity_citations` bulk insert; SQLAlchemy 2.0's bulk-insert via `[dict(...), ...]` parameter list works for the citation tables; partial-failure rollback verified by two unit tests injecting `RuntimeError` on the second and third inserts respectively. No implementation surprises surfaced at the seam itself. The two operational variants I had to navigate are recorded above: (a) the FK constraint on `run_chunk_citations.chunk_id` requires referenced chunks to exist in `chunks` table; the smoke used real chunk_ids from tenant_a's existing corpus so the constraint resolves naturally (synthetic chunk_ids would need either prior chunk insertion or accepting `chunk_id=None`, which is structurally valid per the migration but operationally unusual at write time); (b) the migration applied via direct alembic-against-each-tenant rather than `make migrate` because the tenant registry is currently empty. The seam at three tables is the precedent every later run-history write inherits — and the precedent is exactly the brief's recommended shape with no divergence to record. Phase 2 UX consuming a runs row plus its citation rows can rely on atomicity: either the run record + all its citations land, or none of them do.

4. **What S32 leaves for S33.** S33 lands the UX-shaped query port and the Postgres adapter implementing it. Build evidence at S32 anticipated three S33 framing decisions: (a) the citation-row structure with `run_id` foreign key and `ON DELETE CASCADE` makes a `get_run`-with-citations-attached shape natural — Phase 2 UX consuming a run will typically want its citations rendered alongside, and the FK relationship makes the join cheap; the alternative `get_citations_for_run` separate method works if the read pattern is "list runs first, fetch citations on demand," but the citation cardinality at Phase 1 (single-digit citations per run for retrieval) does not justify the separate-method shape. (b) The within-run deduplication at write time means the read-side does not need to de-dup; a chunk appears once per run regardless of retrieval call count, and the read can return the citation tuple directly. (c) The `source_snapshot` JSONB shape on chunk citations means the read-side query can either return the JSONB blob directly (Phase 2 UX renders) or project specific keys (`source_snapshot->>'file_name'`) for filtering — flexibility that the prior `source_citation text` shape would have foreclosed. The substantive S33 framing question, informed by S32: does the UX-shaped query port expose `get_run(run_id) -> RunWithCitations` returning the run plus its chunk_citations plus its entity_citations as a single aggregate DTO, or does it expose `get_run(run_id) -> Run` plus a separate `get_citations_for_run(run_id) -> CitationsBundle`. Build evidence at S32 leans toward the aggregate DTO because the citation cardinality is bounded and the consumer wants both together; the strategic conversation at S33 framing settles this.

```
metrics:
  classification: new_work
  brief_started: 2026-05-13
  session_started: 2026-05-13
  session_closed: 2026-05-13
  merged: 2026-05-13
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: updated
  corrects:
  corrected_by:
```

## P9 — S34 — HTTP routes for run-history reader, request/response DTOs, query-string parsing for filters, principal-derived tenant context
roles: architect, engineer
mode: build

- Produced: Eight commits closing S34 per the brief. Commit 1 (`docs(p9/s34)`, 584593f) landed D98 at `charter/decisions.md` with the four-shapes choice (response DTO as 1:1 mirror of `RunRecord`; six-parameter query vocabulary; error response body shape with eleven-path map; principal-derived tenant context per S29b precedent); twelve alternatives recorded including (k) the brief-versus-built-state finding that resolved scenario 20's stated 401 to the existing `get_tenant_context` 404 behaviour; `charter/packages/p9-epic.md` S34 line replaced; `charter/current-package.md` transitioned to "S34 in flight"; `briefs/p9/s34.md` preserved with Appendix D drafted for session-close reconciliation outcomes. Commit 2 (`feat(p9/s34)`, fe3d1bf) shipped four Pydantic response DTOs at `apps/api/routers/_run_history_dto.py` (`RunResponse`, `ChunkCitationResponse`, `EntityCitationResponse`, `RunListResponse`) with `ConfigDict(from_attributes=True)` so `model_validate(run_record)` works without manual field mapping; 14 unit tests fence the 1:1 mirror, tuple-to-list serialisation, Decimal-as-string monetary shape, ISO 8601 datetimes, nullable fields, and schemaless `source_snapshot`. Commit 3 (`feat(p9/s34)`, b332a4f) shipped the query-string parser at `apps/api/routers/_run_history_query.py` mapping six query params to `(RunListFilters, RunListCursor | None)` with the sentinel-cursor mechanism for page_size threading on the initial page (cursor=None); 18 unit tests cover each filter dimension, the three date-range failure modes, the cursor decode round-trip, and the sentinel-cursor construction. Commit 4 (`feat(p9/s34)`, 0137784) shipped the two FastAPI routes at `apps/api/routers/run_history.py` (`GET /runs/{run_id}` returning `RunResponse` with citations; `GET /runs` returning `RunListResponse` with paginated cursor); `AppCompositions` extended at `apps/api/main.py` to carry `run_history_reader: RunHistoryReader | None` and `security_events: SecurityEventLogger | None`; `_build_default_compositions` wires both via `build_run_history_reader`; the new router mounted alongside the existing four; 9 integration tests at `tests/integration/api/test_run_history_routes.py` cover happy paths and 401/422 paths. Commit 5 (`feat(p9/s34)`, 11286fe) shipped the `ErrorResponse` shape at `apps/api/_errors.py` with five exception handlers (`MalformedCursorError`, `InvalidFilterRangeError`, `RunNotFoundError`, `BoundTenantIdMismatchError`, `RequestValidationError`); `CorrelationIdMiddleware` at `apps/api/middleware.py` generates `uuid4()` per request and returns `X-Correlation-Id` header; the route handlers raise the typed exceptions (`RunNotFoundError`, `BoundTenantIdMismatchError`) instead of HTTPException so the handlers produce the new shape; 8 new integration tests plus 8 new unit tests at `tests/unit/apps/api/test_errors.py` cover each handler's status code, body shape, security-event emission on the defence-in-depth path, and correlation-id population. Commit 6 (`test(p9/s34)`, d7bfe8f) extended the D24 tenant-isolation harness to 20 scenarios with four HTTP-layer scenarios at `tests/contract/tenant_isolation/test_run_history_http_isolation.py` (cross-tenant `GET /runs/{id}` → 404 plus security event; cross-tenant `GET /runs` → empty list without security event; missing auth → 401; tenant-not-in-registry → 404). Commit 7 (`test(p9/s34)`, 8146cde) ran the live-stack smoke against tenant_a producing all eleven verification paths plus two happy paths end-to-end through the FastAPI app inside `padhanam-padhanam-api-1`; `compose.yaml` pin updated to `padhanam-api:dev@sha256:583f986993f6...`; the smoke uses `httpx.AsyncClient(transport=ASGITransport(app=app))` for in-process HTTP requests so the substituted-dependency approach (override `get_tenant_context` and `app.state.run_history_reader` to bypass the empty tenant registry from the S30b carryover) preserves the HTTP boundary exercise end-to-end. Commit 8 (this entry) transitions `charter/current-package.md` to "S34 closed; S35 next" and appends this session-log entry.

- Decisions: D98 lands at S34 commit 1 per the brief's Appendix A with one structural-honesty revision absorbed through the user-question-at-session-open path: the brief's stated scenario 20 ("principal whose tenant does not exist returns 401") contradicted the existing `get_tenant_context` dependency at `apps/api/routers/inference.py:113-118` which returns 404 from registry-lookup failure. Resolution at session-open: match existing behaviour (404). D98 alternative (k) records the rejection; Appendix D of `briefs/p9/s34.md` records the brief-versus-built-state finding. No further D-entries at S34; eight mechanical reconciliation findings (FastAPI 0.136.1 / Pydantic 2.12.5 versions; SSE-handler-shape reuse pattern; AppCompositions extension with run_history_reader and security_events fields; HTTPException-vs-ErrorResponse scope decision; correlation-id middleware introduction; security-event taxonomy; TestClient convention; router prefix `/runs`) absorbed without commit. One operational-no-impact verification (cursor URL-safety confirmed via codec's `urlsafe_b64encode` and decode-time regex check).

- Tests: 883 unit tests pass at S34 close (up from S33's 831, +52 net new). S34 unit test additions: 14 at `tests/unit/apps/api/routers/test_run_history_dto.py`; 18 at `tests/unit/apps/api/routers/test_run_history_query.py`; 8 at `tests/unit/apps/api/test_errors.py` (the eight extra above the named-40 likely reflect existing unit-test surface refactors counted independently). 17 integration tests at `tests/integration/api/test_run_history_routes.py` cover the route happy paths plus the five 4XX paths plus the 500 defence-in-depth path plus the cursor encoding/decoding. 4 contract tests at `tests/contract/tenant_isolation/test_run_history_http_isolation.py` extend the D24 harness to 20 scenarios. 25/25 import-linter contracts kept. Pre-existing failures from `test_agent_sse_endpoint.py` (the `run_history_writer` field missing on the test's `AgentRuntimeComposition` construction since S31) carry forward unchanged; methodology/tool CLI command tests and ingestion e2e tests likewise carry from S30b.

- Enforcement layer: No new import-linter contracts at S34. The existing 25 contracts (including `layers-run-history` from S31, `fastapi confined to apps/` from earlier) extend cleanly: the new modules (`apps/api/_errors.py`, `apps/api/routers/_run_history_dto.py`, `apps/api/routers/_run_history_query.py`, `apps/api/routers/run_history.py`) all sit at the `apps/api/` altitude and import from `contexts.run_history.ports/.domain/.application` at the layer-respecting paths. The `fastapi` import is confined to `apps/` per existing contract. AST cross-context independence at the new DTO module (no imports from forbidden cross-context modules); the route module imports from `contexts.run_history.ports.reader.RunHistoryReader` (the Protocol) but not from any context's domain or adapter modules.

- methodology: Pre-write reconciliation fired its eleventh instance at S34 session-open. Find-rate distribution: **one structural-honesty (the brief-vs-built-state scenario-20 finding) plus eight mechanical absorptions plus one no-impact verification.** The vendor-doc reconciliation surface (FastAPI 0.136.1 / Pydantic 2.12.5 idioms, the S4 Langfuse-precedent step the brief flagged explicitly) caught nothing the brief had wrong — the conventions are stable at the pinned versions and the brief had them right. A no-find at the vendor-doc surface is itself information: the pattern's value at the eleventh instance has shifted from architectural-drift detection (the early-pattern shape) and brief-versus-built-state mismatch (the S33 shape) toward vendor-stability confirmation when the vendor surface is in fact stable. The S4 precedent caught a real Langfuse SDK drift in early Phase 1; S34's no-find confirms the same step can produce zero findings in a session where the framework versions are recent and conventional. Worth surfacing as a sub-observation: pre-write reconciliation's value distribution across instances includes "confirms-stability" outcomes alongside "catches-drift" outcomes; both warrant the step.

- methodology (second): Consumer-port-plus-wiring-adapter reached its tenth reinforcement at S34, but at a new altitude — the HTTP transport layer consuming the producer-side `RunHistoryReader` port. The pattern altitudes now span four: (a) cross-context with consumer-shaped port at consumer context (S26a-1 through S29b plus S31's `RunHistoryWriter`); (b) intra-context wiring (S28b's ToolInvoker); (c) transport (S29b's SSE adapter); (d) producer-side-port consumed by composition surface (S22's `RetrievalClient`, S33's `RunHistoryReader`, S34's HTTP route layer). Altitude (d) at S34 is the second instance against `RunHistoryReader` specifically — S33 wired the adapter; S34 consumes it through the HTTP layer via dependency injection from `app.state.run_history_reader`. The pattern's altitude-agnostic shape continues to hold across the four altitudes without per-context refinement.

- methodology (third): Brief-versus-built-state reconciliation pattern (sub-altitude of pre-write reconciliation) accumulated its third instance at S34 — the scenario-20 401-vs-404 finding. Prior instances: S32 (citation-cardinality-vs-storage-cost framing in D96 alternative (e)), S33 (`RunWithCitations` wrapper vs `RunRecord`-as-aggregate in D97 alternative (k)). All three instances share the shape: the brief was drafted at strategic-mode altitude against the prior session's substrate; the build-time read of the actual built-state surfaced a small structural mismatch the brief author would not have caught at framing time. Three instances earns the prescriptive promotion criterion the methodology document's pre-write-reconciliation-as-architectural-discovery captures entry already documents.

- methodology (fourth): Storage-versus-render discipline extended cleanly to the HTTP boundary. D96 committed the discipline at the schema layer (JSONB `source_snapshot` over baked-in display text); D97 extended to the read-port surface (`RunRecord`-as-aggregate, no projection); D98 at S34 extended to the HTTP wire format (Pydantic DTOs mirror domain records 1:1, no flattening, no derived fields). Three altitudes of the same discipline in three consecutive sessions on the same substrate. The pattern's structural payoff at S34: the Phase 2 UX consumer reads the HTTP response and renders it; if Phase 2 UX evidence later demands a derived field (e.g., `audit_chain_complete: bool` computed from `audit_end_hash != null`), the addition happens at the consumer's render layer, not at the API's wire format. The discipline accumulates rather than dilutes across altitudes.

Reflection (four prompts per the brief):

1. **Pre-write reconciliation, eleventh instance.** S34's find-rate distribution (one structural-honesty + eight mechanical + one no-impact) tracks the substrate-stability trajectory: S31 (three structural + three mechanical + two post-work) → S32 (one structural + four mechanical + one operational) → S33 (one structural + six mechanical + one operational-no-impact) → S34 (one structural + eight mechanical + one no-impact). The structural-honesty count holds steady at one; the mechanical count grows because each successive session adds more surfaces to verify (S34 is the first HTTP transport session for run-history and has more vendor-doc, error-shape, middleware, and routing surfaces than S33's read-port-only session had). The vendor-doc surface — the headline reconciliation step the brief flagged via the S4 Langfuse precedent — caught nothing. That is itself information: FastAPI and Pydantic v2 conventions are stable at the versions pinned, the brief was written against current conventions, and the no-find is the confirming evidence. The S4 precedent was a real drift catch; S34 is a real confirm. Both are pre-write reconciliation working as intended. Worth recording as a sub-observation in the methodology document at Phase 1 close audit: the pattern's value distribution includes confirms-stability outcomes alongside catches-drift outcomes, and the cadence of which fires is itself a signal about the vendor ecosystem's stability at the framework boundary.

2. **HTTP layer at first run-history surface.** The cursor opacity survived the HTTP query-string boundary cleanly because the codec at S33 already used `urlsafe_b64encode` (the reconciliation #10 verification confirmed this without code change). The Pydantic v2 tuple-to-list serialisation held for nested citation tuples; the smoke's HAPPY_PATH_1 output showed two chunk citations and one entity citation surfacing as JSON arrays with their `source_snapshot` dicts intact. `model_validate(run_record)` from domain to response worked cleanly with `ConfigDict(from_attributes=True)` — no manual field mapping needed; the discipline of using `from_attributes=True` plus matching field types (UUID, datetime, Decimal, dict[str, Any]) absorbed every domain record without exception. The `Annotated[list[X] | None, Query()]` idiom for repeated query params held under FastAPI 0.136.1 cleanly; empty repeated params (e.g., the URL `?` with no params) collapse to `None` and the parser's tuple-vs-None invariant from S33 applied without modification. One unexpected surface during the smoke: the `X-Correlation-Id` header IS returned on the 401 from the auth middleware AND on the 405 from FastAPI's default method-not-allowed handler — because the `CorrelationIdMiddleware` is added last (after `AuthenticationMiddleware`) and therefore sits outermost in the middleware chain at request time per Starlette's reverse-application order. This was anticipated by the brief's correlation-id design but worth recording as as-built evidence: the correlation_id is present for forensic correlation on every response shape including the ones that don't use the new `ErrorResponse` body.

3. **Error response shape and security events at HTTP boundary.** The eleven-path error map covered everything that arose at S34. One small adjustment of the as-built reality vs the brief's wording: PATH_9 (unexpected exception / 500 internal_error) is not safely reproducible in the live stack without deliberately bypassing the route's principal check, so the smoke records it as "exercised at integration tests" and references the specific test `test_get_run_returns_500_on_bound_tenant_mismatch`. PATH_8 (missing run on principal's own tenant) is structurally indistinguishable from PATH_7 (cross-tenant attempt) at the HTTP layer per D98's privacy-preserving 404 commitment, so the smoke records PATH_8 as a known-run-200 verification rather than a missing-run-404 verification (the missing-run case is covered by PATH_7's nonexistent run-id). The security event firing policy held cleanly: cross-tenant 404s fire `TENANT_SCOPE_VIOLATION` synchronously to the file-backed logger; cross-tenant empty-list does not fire (the asymmetry is by design per D98). The noise-versus-signal tradeoff under production load is still untested — at Phase 1 single-tenant smoke loads the security log carries occasional 404s with principal-and-resource metadata, but at multi-tenant production load with bots and stale links the noise-versus-signal balance may need revision; the methodology document at Phase 1 close audit should record the policy as a candidate for production-evidence-driven adjustment. The CorrelationIdMiddleware addition was bounded (one new class, one new constant, three lines in `create_app`) and reached every response shape without further changes.

4. **What S34 leaves for S35.** Build evidence at S34 close points toward P9 close at S35 (Phase 1 substrate completeness for run-history) rather than the ingestion-management API carryover. Three convergent reasons. First, the substrate is structurally complete at S34 end: storage at S31, citation surface at S32, read port at S33, HTTP transport at S34. Phase 2 UX has every consumer surface it needs to render the run history end-to-end. Second, the ingestion-management API was deferred from P6 "until a UI consumer arrives at P9 or P10" per the carryover; under D93 the UI consumer is Phase 2 UX, which exists in narrative but not yet in build. No Phase 2 UX session is currently scheduled ahead of P10/P11 optimisation substrate; the ingestion-management API can land at "Phase 1 close substrate-completion" alongside browser-based auth and the frontend stack decision without holding up P10/P11. Third, an end-to-end demonstration session at S35 (agent invocation via the SSE endpoint → run completion writes record and citations via the writer adapter → HTTP routes retrieve the rendered surface) closes the bet's "the platform proves the proposition" loop at P9 in a way the ingestion-management API does not. The demonstration is also the first session that exercises the full P9 stack as a Phase 2 UX consumer would experience it; the integration findings (latency, cursor stability under concurrent inserts, security-event volume at multi-run scale) would surface there rather than at later phases. The strategic-mode S35 framing conversation settles the call against operator priority; the build evidence recommendation is (b) P9 close via end-to-end demonstration.

```
metrics:
  classification: new_work
  brief_started: 2026-05-14
  session_started: 2026-05-14
  session_closed: 2026-05-14
  merged: 2026-05-14
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: updated
  corrects:
  corrected_by:
```

## P9 — S35 — P9 close via end-to-end demonstration; test_agent_sse_endpoint carryover fixed; deferred-decisions extended; package archived
roles: architect, engineer, technical writer
mode: build

- Produced: Five commits closing S35 per the brief. Commit 1 (`docs(p9/s35)`, cdf64d2) landed the charter touch-points ahead of code: `charter/current-package.md` transitioned to "S35 in flight (P9 close)" with pre-write reconciliation outcomes summarised in the framing paragraph; `charter/deferred-decisions.md` extended with two new entries (integration-test slowness from real-LLM inference; HTTP API for ingestion management absorbed from P6→P9 and not landed within P9); `docs/notes/integration-test-slowness-investigation.md` committed verbatim from the working tree so the deferred-decisions entry has a stable reference target; `briefs/p9/s35.md` preserved per the session-preservation convention. Commit 2 (`fix(p9/s35)`, 16bb63e) resolved the `run_history_writer` carryover at `tests/integration/api/test_agent_sse_endpoint.py:181-189` — the `_runtime_with_script` helper was missing the field that D95 added to `AgentRuntimeComposition` at S31, causing all five tests in the file to raise `TypeError` since S31; fix shape adds `_FakeRunHistoryWriter` alongside the existing fakes and threads it through the helper. Commit 3 (`test(p9/s35)`, 06c28a7) executed the end-to-end demonstration against tenant_a and recorded the smoke at `docs/smoke/p9_s35_e2e_demo.md`: tenant registry recovery via `make seed-tenants`; fresh agent template authored via `agent create-from-role` against the ProblemFramer role (`5a6b54b5-476a-4ca1-b333-e4e8462e7382`) to avoid methodology-lineage on the new agent (the S30b methodology-wipe class otherwise causes `methodology_overrides_lookup` to fail); revision 2 added via `agent update` with `tool_allowlist=[retrieval]`; demo trigger via `padhanam agent run` produced `termination_reason=content` in ~58 seconds; one runs row landed at `5226925f-bd76-47c2-8c9b-fdb4b370e3ab` with audit_start_hash and audit_end_hash linked byte-for-byte to `tenant_audit` rows for the `agent.invoke.start` and `agent.invoke.end` actions; HTTP routes `GET /runs/{id}` and `GET /runs?termination_reasons=content&page_size=10` returned the rendered surface with X-Correlation-Id headers populated. Commit 4 (`docs(p9/s35)`, 38fe3c3) landed the P9 retrospective: addendum to `charter/packages/p9-epic.md` recording the as-built five-session shape against the framing four-to-five-with-ingestion-API forecast; P9 entry appended to `log/packages.md` per the P8 precedent with measured outcomes (5/5 clean-close, 0/5 CFR, 128 net new tests pushing total from 755 to 883); `docs/archive/packages/p9.md` created as a copy of the retrospectively-augmented epic per the P8 archive convention. Commit 5 (this entry) transitions `charter/current-package.md` from "S35 in flight (P9 close)" to "P9 closed; P10 framing next" and appends this session-log entry.

- Decisions: No new D-entries at S35. All P9 substrate decisions had landed at D94 through D98 across S31-S34. The eight pre-write reconciliation surfaces all absorbed mechanically per the brief's recommendations: surface 1 (test_agent_sse_endpoint failure mode) was the obvious carryover with the fix landing at commit 2; surface 2 (tool_allowlist empty on tenant_a agents) absorbed via option (a) seeding ahead of the demo; surface 3 (tenant registry empty) absorbed via `make seed-tenants` at the smoke; surface 4 (image pin) matched S34 close; surface 5 (Langfuse health) returned 200 OK; surface 6 (archive directory) had the P8 precedent; surface 7 (log/packages.md shape) followed the P8 structure; surface 8 (deferred-decisions.md shape) mirrored existing entries. One mid-session structural finding surfaced during commit 3 demo execution: the existing Flowstate ProblemFramer agents have methodology lineage pointing to a methodology row wiped by the S30b fixture-leak class, so they cannot be invoked — resolved by authoring a fresh agent via `create-from-role` rather than reviving the methodology row. This sub-class of the S30b wipe issue is recorded at the smoke for the Phase 1 close audit window.

- Tests: Test count holds at 871 passed + 12 skipped = 883 total, matching S34 close. The S35 work added zero net new tests: commit 2 restored 5 previously-failing tests at `test_agent_sse_endpoint.py` to passing (the carryover fix). 25/25 import-linter contracts kept. AST enforcement passes. The S35 end-to-end demonstration at commit 3 is a markdown record, not a test file; it exercises the full P9 stack against tenant_a's data plane and verifies through `psql` queries and `httpx` HTTP requests captured in the smoke document. Pre-existing failures carry forward unchanged: 11 methodology/tool CLI command tests (config shape drift carryover from S30b/S31); ingestion e2e tests (the tenant-registry-wipe class still firing in the test path, separate from the production-side recovery).

- Enforcement layer: No new import-linter contracts at S35. The existing 25 contracts from S31 through S34 hold without modification. The session changes are scoped to: test helper fixes at `tests/integration/api/`; charter file edits; markdown documents at `docs/smoke/`, `docs/notes/`, `docs/archive/packages/`. No production code or context-boundary changes; the import-linter and AST enforcement surface is unchanged.

- methodology: Pre-write reconciliation fired its twelfth instance at S35 session-open. Find-rate distribution: zero structural-honesty (the eight reconciliation surfaces all absorbed mechanically per the brief's clear guidance), one mid-session structural finding (the methodology-lineage-pointing-to-wiped-row sub-class of the S30b fixture wipe; surfaced at demo trigger time rather than at session open because the surface was downstream of the brief's surface 2 framing), zero no-impact verifications. The trajectory across P9: S31 (three structural + three mechanical + two post-work) → S32 (one structural + four mechanical + one operational) → S33 (one structural + six mechanical + one operational-no-impact) → S34 (one structural + eight mechanical + one no-impact) → S35 (zero structural at session-open + eight mechanical + one mid-session sub-class). The substrate-stability proxy across the package: structural-honesty count dropped from three at S31 to zero at S35 session-open while the mechanical count grew because each successive session adds more surfaces to verify. The S35 mid-session sub-class is a different shape: it surfaced not at brief-versus-built-state reconciliation time but at trigger-execution time when the runtime tried to look up a row the brief and reconciliation surfaces had no specific reason to query. This is a new variant of the pattern worth recording at Phase 1 close audit: pre-write reconciliation catches some kinds of structural drift; trigger-execution catches others; the two surfaces are complementary rather than substitutive.

- methodology (second): End-to-end demo as P9 close artefact yielded three substantive findings the substrate sessions alone could not surface. First, the methodology-lineage-pointing-to-wiped-row sub-class above. Second, the trace-id propagation gap — the agent runtime accumulator path does not capture the OTel trace context into the run record despite the column existing and Langfuse-web being healthy. Substrate complete at column + read-port level; capture path needs revisit for the Phase 1 close audit. Third, the model-behaviour gap — qwen2.5:7b does not choose to invoke the retrieval tool reliably even when explicitly prompted to do so in the system prompt. This is a known carryover but the demo surfaces it cleanly across two consecutive invocations on the same agent. The audit-chain linkage held byte-for-byte: `audit_start_hash`/`audit_end_hash` on the runs row match `tenant_audit.this_event_hash` for `agent.invoke.start`/`agent.invoke.end` actions, with the end event's `previous_event_hash` matching the start hash exactly. D95's audit-chain-partial-state shape executes correctly end-to-end across a real invocation for the first time. The SSE event stream observed across the demo: `InvocationStarted`, `IterationStarted`, `ContentDelta` (streamed), `IterationCompleted`, `InvocationCompleted` — five of D90's eleven event types, with the remaining six (`ToolCallProposed`, `ToolCallStarted`, `ToolCallCompleted`, `ToolCallSkipped`, `InvariantBlocked`, `InvocationFailed`) not exercised because no tool invocations or failures occurred on this path.

- methodology (third): Audit-chain-to-run-record linkage as integrity claim was verified end-to-end across a real invocation for the first time. The verification protocol: read the runs row's `audit_start_hash` and `audit_end_hash` columns; query `tenant_audit` for rows where `this_event_hash` matches; verify the chained hashes (the end event's `previous_event_hash` equals the start event's `this_event_hash`). All three properties held under the demo. The integrity claim is tamper-evident in both directions: modification of either audit event would break the chain (the SHA256 hash function fails the chain link); modification of the runs row's `audit_*_hash` columns would break the join from runs to tenant_audit. What a malicious actor would need to do to break the linkage: have write access to BOTH the per-tenant runs table AND the per-tenant tenant_audit table simultaneously, recompute the hash chain consistently across both tables (including downstream `previous_event_hash` references on every subsequent audit event), and either (a) rewrite the entire downstream chain or (b) accept that the chain breaks at the modification point. The append-only audit storage discipline plus per-tenant DB ownership means the threat model requires control-plane or per-tenant-DB write access at minimum; field-level encryption via envelope encryption (per the security principle) further raises the bar. Sharp edges in the verification protocol worth capturing at the methodology document for Phase 1 close audit: the verification is read-only across two tables on the same tenant DB; under multi-replica or read-replica deployment, the verification must run against the primary or against a snapshot at the same logical time so the two reads are consistent; the verification cadence at Phase 2 (continuous? on-demand? periodic sampling?) is an audit-shape question the methodology document should commit to.

- methodology (fourth): What P9 leaves for P10 framing. P9 closes the run-history substrate as the Phase 2 UX consumer surface and the substrate-completeness-as-bet-proof artefact per D92. Phase 2 UX has every consumer surface it needs to render runs end-to-end: write path (S31), citation surface (S32), read port (S33), HTTP transport (S34), demonstrated execution (S35). P10 is the evaluation territory per the roadmap. Two P9 substrate threads carry forward directly into P10: the run-history substrate becomes the data source for P10's per-tenant retrieval-evaluation harness (the deferred retrieval-evaluation design session at the P10 boundary will commit the evaluation surface against P9's persisted run records); the citation surface from S32 becomes the gold-set anchor for retrieval evaluations (the captured `source_snapshot` JSONB plus `source_chunk_ids` provenance is exactly what an evaluator needs to compare retrieved-versus-expected chunks). Three carryovers reinforce at the P9→P10 boundary: the 11 methodology/tool CLI test failures (config shape drift; pre-existing from S30b/S31 and not on the P9 critical path); the integration-test slowness (now formally captured at the new deferred-decisions entry with `docs/notes/integration-test-slowness-investigation.md` as reference); the ingestion-management HTTP API (deferred to early Phase 2 substrate-completion). Two P10 framing inputs surfaced from P9 execution worth weighing at the strategic-mode P10 conversation: first, the retrieval-not-exercised gap is more impactful at P10 than P9 because the evaluation harness needs reliable tool invocation as its foundation — option (c) Phase-2-preview override may need to land at P10 even if Phase 2 UX is the natural consumer; second, the trace-id propagation gap impacts P10's evaluation harness more than P9's run history because cross-store correlation with Langfuse is part of how the harness will deep-dive into per-run cost and latency. Both are framing inputs for the strategic-mode P10 conversation rather than P9 close commitments; the strategic conversation owns the P10 scope decisions.

Reflection (four prompts per the brief):

1. **Pre-write reconciliation, twelfth instance.** S35's session-open reconciliation produced zero structural-honesty findings — all eight surfaces absorbed mechanically per the brief's clear guidance. Surface 1 (test_agent_sse_endpoint failure) was the obvious carryover the brief flagged; surface 2 (tool_allowlist empty) was the brief's anticipated option-(a)-seeding moment; surface 3 (tenant registry empty) was the brief's anticipated wipe-class moment requiring `make seed-tenants`; surfaces 4-8 absorbed without question. The trajectory across the P9 window — three structural at S31 dropping to one at S32, S33, S34, and zero at S35 — tracks substrate stability as the package matures. By S35 the brief author can write against a known-stable substrate and the implementer's session-open reconciliation finds only the carryover surfaces the brief itself flagged. The mid-session sub-class finding (methodology-lineage-pointing-to-wiped-row at demo trigger time) is a different shape: it surfaced at trigger execution, not at brief-versus-built-state reconciliation. This is the pattern's new sub-observation at the twelfth instance: pre-write reconciliation catches some kinds of structural drift (brief-versus-built-state at session open); trigger-execution catches others (runtime-dependency-on-wiped-row at first invocation). The two surfaces are complementary; recording the distinction at the Phase 1 close audit's methodology document promotion entry is the right capture point.

2. **End-to-end demo as P9 close artefact.** The demo surfaces three findings the substrate sessions alone could not. First, the methodology-lineage-pointing-to-wiped-row sub-class above — invisible to S31-S34 because they all used substituted dependencies or freshly-authored substrate that did not exercise the existing-agent-with-methodology-lineage path through a real LLM invocation. Second, the trace-id propagation gap — invisible to S31-S34 because the substrate sessions verified the column exists and the read-port returns it correctly, but did not verify the agent-runtime accumulator captures the OTel trace context into the run-record-shaped state. The capture path runs through the runtime composition but the trace context threading was not exercised end-to-end against a real Langfuse-receiving trace pipeline until S35. Third, the model-behaviour gap reproducible-at-demo-time — already known from S30b but never reproduced across two consecutive invocations on the same agent until S35. The audit-chain linkage held byte-for-byte across both runs row and tenant_audit; the SSE event vocabulary matched D90's documented eleven types with five exercised on this path. The path executed cleanly with no integration friction — the substrate proved its substrate-completeness claim per D92 in product form against a real LLM with audit-chain integrity holding through the entire path. The demo is the bet-proof artefact P9 was supposed to produce.

3. **Audit-chain-to-run-record linkage as integrity claim.** D95 committed the linkage between the run record and the audit chain via shared hashes; S35's demo is the first verification of the linkage end-to-end across a real invocation. The verification protocol is mechanical: read the runs row, read the two tenant_audit rows linked by hash, verify three properties (hash equality at both ends, chained hash from end's `previous_event_hash` to start's `this_event_hash`). All three held byte-for-byte. A malicious actor would need simultaneous write access to BOTH the per-tenant runs table AND the per-tenant tenant_audit table plus recomputation of the entire downstream hash chain (or acceptance that the chain breaks at the modification point); the threat model requires per-tenant-DB write access at minimum, which the per-tenant data plane discipline plus per-tenant-DB credentials makes a meaningful barrier. Sharp edges in the verification protocol worth capturing at the methodology document for Phase 1 close audit: the two-table read consistency requirement (primary vs replica at multi-replica deployment); the verification cadence at production (continuous integrity check on every run? periodic sampling at the audit cadence?); the response-to-tamper-detection (alert + halt? quarantine? notify which actor?). All three are Phase 2 deployment questions; D95 commits the substrate; the operational policy lands at the methodology document at Phase 1 close audit or Phase 2 framing. The audit-chain-as-integrity-claim has moved from architectural commitment (D95 framing) through substrate verification (S31 commit 9 first runs row link) to end-to-end verification across a real invocation (S35 demo). The trajectory of evidence at each step is itself the methodology promotion candidate for the integrity-evidence-shape that bet-native artefacts produce.

4. **What P9 leaves for P10 framing.** Three substrate threads land directly into P10. First, the persisted run records become the data source for P10's per-tenant retrieval-evaluation harness — the deferred retrieval-evaluation design session at the P10 boundary will commit the evaluation surface against P9's substrate. Second, the citation surface from S32 (`source_snapshot` JSONB plus `source_chunk_ids text[]` provenance) becomes the gold-set anchor for retrieval evaluations: the captured snapshot is exactly what an evaluator needs to compare retrieved-versus-expected chunks. Third, the audit chain plus the per-tenant data plane discipline gives P10 the integrity surface its evaluations will run within — cross-tenant evaluation isolation is structurally gated by the same enforcement that S31's tenant-isolation contract harness extended. Three P9 carryovers carry forward: the 11 methodology/tool CLI test failures (config shape drift from S30b/S31, not on P9 critical path); the integration-test slowness (formally captured at the new deferred-decisions entry); the ingestion-management HTTP API (deferred to early Phase 2 substrate-completion). Two P10 framing inputs surfaced from P9 execution that the strategic-mode P10 conversation should weigh: the retrieval-not-exercised gap is more impactful at P10 than P9 because the evaluation harness needs reliable tool invocation as its foundation, so option (c) Phase-2-preview override on tool_allowlist may need to land at P10 even with Phase 2 UX as the natural consumer; the trace-id propagation gap impacts P10's evaluation harness more than P9's run history because cross-store correlation with Langfuse is part of how the harness will deep-dive into per-run cost and latency. Both are framing inputs for the strategic-mode P10 conversation; the strategic conversation owns the P10 scope decisions.

```
metrics:
  classification: new_work
  brief_started: 2026-05-14
  session_started: 2026-05-14
  session_closed: 2026-05-14
  merged: 2026-05-14
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: updated
  corrects:
  corrected_by:
```

## P9 carryover — S35a — live-LLM tier separation; CLI composition root; tenant registry actor provenance; trace_id propagation
roles: architect, engineer, technical writer
mode: build

- Produced: Six commits closing S35a per the brief, expanding scope at reconciliation time based on a structural finding the brief anticipated. Commit 1 (`docs(p9/s35a)`, 481358e) landed the charter touch-points ahead of code: D99 (two-tier integration test strategy) plus D100 (CLI settings flow through composition root) plus D101 (tenant registry actor provenance) appended to `charter/decisions.md`; `charter/deferred-decisions.md` cleaned of the slowness entry now that D99 commits the convention; `charter/schema.md` extended with the new `tenant_registry.created_by_user_id` column row; `charter/current-package.md` transitioned to "S35a in flight"; `briefs/p9/s35a.md` preserved per the session-preservation convention. Commit 2 (`feat(p9/s35a)`, b11e4e3) landed the live_llm marker tier separation: `pyproject.toml`'s `[tool.pytest.ini_options]` gained the `live_llm` marker definition plus `addopts = "-q -m 'not live_llm'"`; 10 integration test files at the live-LLM tier gained `pytestmark = pytest.mark.live_llm` (31 tests across `tests/integration/contexts/agent/`, `tests/integration/contexts/ingestion/`, `tests/integration/evaluation/`, `tests/integration/test_s15_tenant_context_e2e.py`); new `make test-live-llm` Makefile target plus help-line. Commit 3 (`fix(p9/s35a)`, 3640085) landed D101 end-to-end: control-plane Alembic revision `0011_tenant_actor_provenance` adds the column via server-side default `'migration:0001'` for backfill (then drops the default so subsequent writes supply value explicitly); `PostgresTenantRegistry.register_tenant` signature gains `created_by_user_id: str`; `TenantRegistryPort` Protocol and the async-shape Protocol both extend; `contexts/tenancy/application/use_cases.py`'s `register_tenant` derives the value from `principal.subject`; `ops/seed_tenants.py` switches its operator subject to `'migration:ops/seed_tenants'` so seeded rows match the guard sentinel; four wipe-fixture sites gain `.where(notlike('migration:%'))`. Commit 4 (`fix(p9/s35a)`, 6d749ea) closed the trace_id propagation gap: new `_current_otel_trace_id_hex()` helper in `contexts/agent/application/use_cases.py` lifts the active OTel span's trace_id via `trace.get_current_span().get_span_context()` and formats as 32-character lowercase hex per the OTel convention; `_assemble_agent_run_record` calls it instead of hardcoding `trace_id=None`; three unit tests at `tests/unit/contexts/agent/application/test_trace_id_lift.py` cover the no-provider, valid-span, and INVALID_TRACE_ID paths; live-stack smoke at `docs/smoke/p9_s35a_trace_id_propagation.md` verified runs row 486af46b carries `trace_id=b7e677a03e28afd51c5f691055545022`. Commit 5 (`refactor(p9/s35a)`, 697315e) landed D100: new `apps/cli/_composition.py` defines `CliCompositions` dataclass plus `set_compositions`/`get_compositions` accessors; `apps/cli/main.py` gains a `@app.callback()` constructing default compositions on first invocation; five `ControlPlaneSettings()` construction sites across `_methodology.py`, `_role.py`, `_tool.py`, `_agent.py` refactor to read from `get_compositions().control_plane_settings`; test fixtures at `test_methodology_commands.py` and `test_tool_commands.py` install loopback-shaped CliCompositions via `set_compositions(...)` before invoking CliRunner and reset on teardown; all 17 methodology + tool CLI integration tests pass (brief named 11 failing; actual collected count is 17 across the two files). Commit 6 (this entry) transitions `charter/current-package.md` from "S35a in flight" back to "P9 closed; P10 framing next" and appends this session-log entry.

- Decisions: Three new D-entries at S35a — D99 (two-tier integration test strategy: file-level `pytestmark = pytest.mark.live_llm` plus pyproject.toml addopts exclusion plus `make test-live-llm` target; six alternatives considered including full mocking, pytest-xdist, shared-corpus rewrite, accept-the-slowness, per-test toggle, per-test marker placement); D100 (CLI settings flow through composition root mirroring `apps/api`'s `AppCompositions` pattern; five alternatives considered including environment monkeypatch, settings-aware-proxy wrapper, module-level globals, env-var-only mechanism, per-invocation callback-construction); D101 (tenant registry carries actor provenance via `created_by_user_id text NOT NULL` column with server-default backfill plus adapter derivation from `principal.subject`; five alternatives considered including fixture-tracked tenant_ids, status-field discriminator, defer-to-its-own-session, explicit-parameter-not-derived-from-principal, stricter-sentinel-value). Reconciliation surfaced the brief's anticipated structural finding at surface 9 (tenant_registry had no `created_by_user_id` column) — operator selected option (a) at the session-open user question (add column via migration with backfill plus adapter-API change plus seed-script subject change), which expanded commit 3's scope from "one WHERE-clause fix" to "schema migration plus adapter signature plus four-site fixture refactor plus charter alignment." A reconciliation-at-execution-time finding caught the alembic_version varchar(32) limit on the originally-drafted revision id `0011_add_tenant_registry_actor_provenance` (40 chars); renamed to `0011_tenant_actor_provenance` (28 chars) matching the implicit length convention across prior migrations.

- Tests: 1100 passed + 17 skipped + 31 deselected (live_llm) in 34.88s default-tier run. Net new tests at S35a: three at `test_trace_id_lift.py` (no-provider, active-span-32-hex, format-conversion-matches-OTel-spec). The S30b/S31-class methodology/tool CLI failures the brief named as "11 failing" — actual collected count is 17 across `test_methodology_commands.py` (9) and `test_tool_commands.py` (8) — now ALL pass after the D100 composition-root threading. Six pre-existing carryover failures remain unchanged at S35a close and confirmed not caused by any S35a commit via `git checkout b11e4e3 -- . && pytest` round-trip: four already-named carryovers (`test_streaming_endpoint_threads_correct_tenant_per_bearer_token`, `test_streaming_endpoint_unauthenticated_rejected_before_executor`, `test_search_against_empty_tenant_returns_no_results[a/b]`) plus two newly-named carryovers surfaced at S35a verification (`test_two_concurrent_workers_no_double_processing_full_pipeline`, `test_full_role_aware_clone_and_edit_flow_against_live_stack`); the latter two fail because TRUNCATE on the chunks table now violates the run_chunk_citations FK introduced at S32 — latent at S35 close when tenant_a chunks were empty, load-bearing now that the demo runs populated rows. 25/25 import-linter contracts kept; AST enforcement passes (9 tests).

- Enforcement layer: No new import-linter contracts at S35a. The existing 25 contracts hold without modification across the new code (D99 marker registration in pyproject.toml; D100 composition root at `apps/cli/_composition.py`; D101 schema migration plus adapter API change; D27 join-key fix at `contexts/agent/application/use_cases.py`). The AST enforcement at `tests/_enforcement/` continues to assert the existing invariants (no plaintext credentials reference assigned to adapter `self`; per-tenant DB isolation contracts; the package-rename invariant tracking `padhanam` as the canonical package name).

- methodology: Pre-write reconciliation fired its thirteenth instance at S35a session-open, the first instance at carryover-hygiene altitude. The 12 reconciliation surfaces produced one structural-honesty finding (surface 9: tenant_registry's missing `created_by_user_id` column) plus 11 mechanical absorptions. The structural finding required a session-open user question with three options ((a) add column via migration, (b) refactor fixtures to track inserted tenant_ids, (c) defer to its own session); operator selected (a), which expanded commit 3 from a one-WHERE-clause fix to a multi-file schema-plus-adapter migration. The trajectory across the entire P9-plus-carryover window: S31 (three structural at session-open) → S32 (one structural) → S33 (one structural) → S34 (one structural) → S35 (zero structural at session-open; one mid-session sub-class) → S35a (one structural at session-open). The find-rate distribution holds: substrate-shaping sessions surface more structural drift than substrate-extension sessions; substrate-extension sessions surface more than carryover-hygiene sessions. S35a's one structural finding at carryover-hygiene altitude is the smallest count in the trajectory; it surfaced because the brief explicitly anticipated and asked about it ("If not, the guard pattern needs revision"), making the structural finding less a discovery and more a confirmation of a flagged-ahead-of-session uncertainty. The carryover-hygiene altitude reveals that the pattern's load-bearing claim ("altitude-agnostic") holds at this altitude too; the find-rate is smaller because the substrate is more stable, but the catch is still real and worth a user question.

- methodology (second): CLI composition-root migration as architectural symmetry surfaced exactly the cleanup the apps/api precedent suggested it would. apps/api has had composition-root settings construction since S2 (`_build_default_compositions()` at `apps/api/main.py`); apps/cli had been constructing settings per command at five sites since the methodology CLI landed at S23. The migration revealed no additional asymmetries between the two composition roots beyond settings: both now follow the dataclass-plus-callback-plus-shared-state pattern. One sub-observation worth recording at the Phase 1 close audit: the pragmatic choice to use a module-level singleton at `apps/cli/_composition.py` rather than threading `typer.Context` through every command signature traded idiomatic typer.Context-passing for surface-area-minimisation (21 command signatures unchanged). The brief named typer.Context as the path; the structurally-equivalent module-singleton achieved the architectural property (single construction site, consumed everywhere) without the 21-signature refactor cost. Future strategic-mode work may want to revisit whether the singleton-vs-context choice carries any downstream cost; at S35a close none is apparent.

- methodology (third): trace_id propagation as the first operational use of D27's join-key claim closed cleanly with one helper function plus three unit tests plus a one-line change at the assembly path. The OTel span context propagated through `invoke_agent`'s async-generator yield boundary cleanly without explicit propagation — Python's contextvars semantics in asyncio tasks preserved the span-as-current state across the yield, so `_assemble_agent_run_record` (called from inside `invoke_agent`'s `async for event in stream` loop after the terminal yield) observed the span the `AgentLoopExecutor`'s `with _tracer.start_as_current_span(...)` block had set. The substrate had been complete since S31 (the column existed; the field existed on AgentRunRecord and RunRecord; the adapter wrote it); the only gap was the assembly-path's hardcoded `trace_id=None`. The structural cost of the fix was bounded — D27's join-key claim was effectively a one-line bug for ~14 days between substrate completion at S31 and end-to-end exercise at S35a. The pattern is worth recording at the Phase 1 close audit: substrate-completeness verification needs end-to-end exercise of every committed join-key, not just structural assertion that the column/field/adapter exists. The audit prompt: "Are there other D-entries committing operational behaviour that is currently unexercised and could harbour similar gaps?" — surfaces at least one named candidate (the retrieval-not-exercised model-behaviour gap, which is a different shape but in the same operational-claim-vs-runtime-exercise class).

- methodology (fourth): What S35a leaves for P10 framing. Three substrate threads carry forward to P10 directly. First, the live-LLM tier separation is in place; P10's evaluation harness work will likely consume the live tier (real LLM behaviour is exactly what an evaluation harness needs to assert against), and the default tier's restored iteration speed lets the harness development itself run fast. Second, the trace_id propagation closure means P10's evaluation runs can correlate to Langfuse traces from inception via the runs row's join key; the cross-store deep-dive surface (cost-per-task, latency, model-output divergence) becomes immediately available rather than requiring a P10-internal substrate fix. Third, the tenant registry actor provenance plus wipe-guard symmetry means P10's evaluation harness work can establish per-evaluation-run isolation against an evaluation-author principal subject without risking accidental wipes of the platform's seed tenants. Three carryovers persist into P10 framing: the four pre-existing test failures from the `psql missing` carryover plus the agent-streaming pattern (separate from S35a's scope, no path-of-least-resistance fix at this commit); the two newly-named TRUNCATE-without-CASCADE failures from chunks ↔ run_chunk_citations FK (latent since S32, surfaced at S35a's live demo populating chunks); the retrieval-not-exercised model-behaviour gap (still load-bearing at P10's evaluation harness work per S35's reflection). All three are inputs for the strategic-mode P10 conversation rather than S35a close commitments; the strategic conversation owns the P10 scope decisions.

```
metrics:
  classification: carryover_hygiene
  brief_started: 2026-05-14
  session_started: 2026-05-14
  session_closed: 2026-05-14
  merged: 2026-05-14
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: updated
  corrects:
  corrected_by:
```

## P9 carryover — S35b — TRUNCATE+CASCADE fix; in-tenant-container psql truncation; streaming-isolation fixture-staleness fold-in
roles: architect, engineer
mode: build

- Produced: Five commits closing S35b per the brief, with two reconciliation findings reshaping the path. Commit 1 (`docs(p9/s35b)`, 893e62c) landed the charter touch-points: `charter/current-package.md` transition to "S35b in flight (P9 carryover hygiene continued)" framing the three workstreams; `briefs/p9/s35b.md` preserved. Commit 2 (`fix(p9/s35b)`, a555902) landed the TRUNCATE+CASCADE fix: two fixture sites — `tests/integration/contexts/ingestion/test_concurrent_workers.py:191` and `tests/e2e/agent/test_create_from_methodology_flow.py:145` — extended their `TRUNCATE TABLE chunks, sources;` to `TRUNCATE TABLE chunks, sources, run_chunk_citations;` per the S32 FK chain (`run_chunk_citations.chunk_id REFERENCES chunks(id) ON DELETE SET NULL` per D95). The explicit-table-list shape over the CASCADE keyword (per brief recommendation) documents intent at the call site; production ORM-deletion semantics are unaffected because TRUNCATE bypasses FK-action triggers while ORM deletions go through the DELETE path where SET NULL fires. Commit 3 (`refactor(p9/s35b)`, 373ea52) migrated `tests/contract/tenant_isolation/test_ingestion_isolation.py`'s S22/D65 retrieval-surface block from the failing padhanam-api psql shell-out to the in-tenant-container psql pattern (after reconciliation surface 2 caught that the brief's "SQLAlchemy from host mirroring the methodology fixture" target was structurally infeasible for per-tenant DBs — see below). The new `red_team_state` module-scoped fixture truncates both tenants' `chunks, sources, run_chunk_citations` via `docker compose exec -T postgres-tenant-<label>` then seeds each tenant with a distinct marker chunk (placeholder 768-dim unit-vector embedding, `sources.state='indexed'`). The two restructured tests assert own-marker-present AND other-marker-absent in result.stdout, pass-because-isolated per D24. `compose.yaml`'s `padhanam-api` digest pin refreshed to `sha256:125c0b0f858d0791ef5c2069f5e756880ff6371d0e7c1d6971dc1d8c5e5cb952` from the deliberate-violation rebuild cycle (source unchanged from pre-rebuild; rebuild not byte-for-byte reproducible). Commit 4 (`fix(p9/s35b)`, c242f50) added `_NullRunHistoryWriter` to `_build_app` in `tests/contract/tenant_isolation/test_agent_streaming_isolation.py` so the `AgentRuntimeComposition` instantiates with the `run_history_writer` field S31 added — identical class to the S35 carryover fix on `test_agent_sse_endpoint.py::_runtime_with_script`. Commit 5 (this entry) appends the session log entry, lands Appendix D in `briefs/p9/s35b.md` capturing the reconciliation findings, and transitions `charter/current-package.md` back to "P9 closed; P10 framing next" with closing paragraph.

- Decisions: No new D-entries at S35b. D24 (tenant-isolation contract harness discipline) already commits the principle the fixes restore; the in-tenant-container psql pattern at commit 3 is established codebase convention from `test_concurrent_workers.py::_exec_psql_tenant_a` and `test_create_from_methodology_flow.py::_exec_psql_tenant`. The contingent D102 named at the brief (in case streaming-isolation surfaced structural ground) did not land — the streaming-isolation diagnostic absorbed as fixture-staleness at session-open per the user-question protocol. The reconciliation findings (surface 2's methodology-fixture-pattern-non-transferability; the mid-commit-3 deliberate-violation finding) are preserved in `briefs/p9/s35b.md` Appendix D as methodology signal for the Phase 1 close audit rather than promoted to D-entries.

- Tests: 1106 passed + 17 skipped + 31 deselected (live_llm) in 180.69s default-tier run. Net change: +6 passing tests vs S35a's 1100. All six pre-existing carryover failures resolved: `test_two_concurrent_workers_no_double_processing_full_pipeline` (TRUNCATE+CASCADE), `test_full_role_aware_clone_and_edit_flow_against_live_stack` (TRUNCATE+CASCADE), `test_search_returns_only_own_tenant_chunks[a]` and `[b]` (replaces the failing `test_search_against_empty_tenant_returns_no_results[a]/[b]` with red-team-shaped variants), `test_streaming_endpoint_threads_correct_tenant_per_bearer_token`, `test_streaming_endpoint_unauthenticated_rejected_before_executor`. Zero regressions. 25/25 import-linter contracts kept; AST enforcement (9 tests) passes. No new unit tests at S35b — the changes are at fixture/test-scaffolding altitude, and the fixture's correctness is exercised by the contract tests themselves passing.

- Enforcement layer: No new import-linter contracts at S35b. The 25 existing contracts hold across the test-fixture-only changes. The AST enforcement at `tests/_enforcement/` continues to assert the package-rename invariant, per-tenant DB isolation contracts, and the no-plaintext-credentials-to-adapter-self pattern. The contract test harness at `tests/contract/tenant_isolation/` is itself a structural-enforcement layer (D24 commits the discipline); the harness now scales to 20 scenarios with the four red-team-shaped retrieval-surface tests (two search, two traverse) replacing the four pass-because-empty variants. The brief's AC #6 ("the contract harness tests pass because isolation is enforced, not because tables are empty") holds for the resolver-routing and seed-into-wrong-DB failure modes; the deliberate-violation finding (see methodology below) refined which failure modes the harness catches versus which are guaranteed by D32's per-DB topology.

- methodology (first): Pre-write reconciliation, fourteenth instance, second carryover-hygiene altitude. Five reconciliation surfaces at session-open produced one structural-drift finding (surface 2: methodology fixture's SQLAlchemy-from-host pattern doesn't transfer to per-tenant DBs because per D5 they carry no host-port bindings by design; the brief's "mirror the methodology fixture pattern" instruction was right in spirit but wrong in specifics) plus three mechanical absorptions (surfaces 1, 4, 5) plus one surface that surfaced its own user-question (surface 3: streaming-isolation diagnostic shape was a third class not named by the brief's empty-tables-or-FK-TRUNCATE alternatives — operator-resolved to fold-in as fixture-staleness identical to the S35 test_agent_sse_endpoint.py pattern). Plus one mid-commit-3 structural finding (deliberate-violation cycle, see below). Find-rate distribution at S35b: 2-of-5 surfaces required user-questions (surface 2 reconciliation-resolution, surface 3 streaming diagnostic) plus 1 mid-session structural finding (deliberate-violation outcome) = 3 structural findings total. S35a's distribution: 1 structural at session-open plus 0 mid-session. S31 (substrate-shaping): 3 structural. S32 (substrate-extension): 1 structural. S33-S34 (substrate-completion): 1-1. S35 (P9 close): 0 at session-open + 1 mid-session. S35a (carryover-hygiene v1): 1 structural at session-open + 0 mid-session. S35b (carryover-hygiene v2): 2 structural at session-open + 1 mid-session. The find-rate at carryover-hygiene altitude is NOT uniformly small — S35b's count exceeded several substrate-extension sessions. The hypothesis from S35a ("carryover-hygiene altitude reveals stable substrate so find-rate is smaller") does not hold across two instances. Updated hypothesis: find-rate scales with the brief's reliance on architectural assumptions that may not hold — S35b's brief assumed the methodology fixture pattern was transferable (it wasn't) and assumed the deliberate-violation cycle would catch in-DB-predicate failures (the per-DB topology means it doesn't). Both assumptions were caught and reframed in-session. The pattern's load-bearing claim remains intact ("altitude-agnostic"); the find-rate signal is about the brief's-assumption-density, not about the substrate's stability. This is worth surfacing at the Phase 1 close audit's methodology review.

- methodology (second): Harness integrity as a class of finding. The "pass-because-empty rather than pass-because-isolated" framing at S35a close was the load-bearing reason to do S35b before P10 framing. Commit 3's red-team restructuring (seed each tenant with a distinct marker, assert own-marker-present AND other-marker-absent) addresses the framing at the search-path contract. The mid-commit-3 deliberate-violation cycle exposed a second-order class of harness finding: D32's per-database topology is the *primary* isolation mechanism; the SQL predicate filter is defense-in-depth within each per-tenant DB. The contract test catches catastrophic failures (resolver mis-routing, seed-into-wrong-DB, predicate-removal-with-shared-data) but NOT in-DB-predicate-only failures — because the per-tenant-DB topology means each tenant's search never sees the other tenant's data regardless of in-DB predicate. The harness's red-team posture is now honest: it asserts what the architecture can guarantee at the user-visible surface, not all components of the isolation stack. Other contract tests in the harness may harbour the same shape — tests that pass-because-architecturally-guaranteed rather than tests that catch all variants of the stated invariant. The class of finding to surface at the Phase 1 close audit: contract tests at per-DB topologies have a structurally-bounded ability to verify within-DB defense-in-depth; the test design should acknowledge which layer it verifies and which is architecture-guaranteed. Promote to a methodology document Patterns-observed entry: "Verify-the-mechanism vs verify-the-outcome distinction in contract tests." Recurrence test continues at the next contract-test design moment.

- methodology (third): Fixture pattern unification — partial. The brief intended commit 3 to converge the ingestion-isolation fixture on the methodology-fixture's SQLAlchemy pattern (architectural symmetry analogous to apps/cli converging on apps/api at D100). Reconciliation surface 2 caught that the convergence is structurally impossible at this altitude (the methodology fixture works against control-plane Postgres with a host-port binding; per-tenant DBs have none by D5). The actual convergence at commit 3 is the in-tenant-container psql pattern, which is a DIFFERENT kind of unification: the four codebase fixtures that need to truncate per-tenant DBs (the two from commit 2 plus the two newly-migrated at commit 3) now all use the same shape — `docker compose exec -T postgres-tenant-<label> sh -c 'psql ...'`. Sub-observation worth recording: pattern unification is altitude-dependent. The methodology-fixture pattern unifies fixtures-against-control-plane (where the D5 exception enables host SQLAlchemy); the in-tenant-container-psql pattern unifies fixtures-against-per-tenant-DBs (where D5's default no-binding rule constrains the path). Two patterns, two altitudes, each unified at its altitude. Phase 1 close audit candidate for the "fixture pattern catalogue" if the harness grows to require explicit pattern selection at fixture authoring time.

- methodology (fourth): What S35b leaves for P10 framing. P10 is evaluation territory per the roadmap. Three direct contributions to P10's foundation: (1) the tenant-isolation contract harness at 20 scenarios with the four red-team-shaped retrieval tests now passing — P10's evaluation harness can assume tenant isolation is verified at the contract layer and focus on per-tenant evaluation runs without re-validating isolation; (2) the in-tenant-container psql pattern at commit 3 plus the explicit-table-list TRUNCATE shape at commit 2 give P10's evaluation harness a precedent for state-reset between evaluation runs (truncate by table list including FK descendants); (3) the deliberate-violation cycle's finding (D32 per-DB is the primary isolation, predicate is defense-in-depth) tells P10's harness design that evaluation runs need not re-test the in-DB predicate as a per-evaluation invariant — they can assume D32's physical separation. Carryover list after S35b close: all six previously-named carryover failures resolved; carryover-hygiene tail (S35a + S35b) folds into the P9 archive at P10 strategic-mode framing per operator direction; broader carryover items (retrieval-not-exercised model-behaviour gap; trace_id-propagation gap — partially addressed at S35a; HTTP API for evaluation management; Frontend stack decision; Browser-based authentication; etc.) carry forward per `charter/current-package.md`'s carryovers section. None block P10 framing structurally; the strategic-mode P10 conversation owns scope decisions.

- methodology (fifth, reconciliation-finding-specific): The brief's "deliberate-violation cycle" verification step is methodology signal worth refining. The cycle assumes the test's failure modes can be exercised by introducing faults in the code path the test traverses; for a contract test that asserts user-visible outcomes against an architecturally-guaranteed property (D32 per-DB → cross-tenant non-leak at search), the cycle exposes which faults the test catches versus which are guaranteed by architecture. The cycle's value is in the exposition, not in the verification per se. At Phase 1 close audit: surface this as a refinement to the brief authoring discipline — when a brief names a deliberate-violation cycle, name which mechanism the cycle is exercising (architectural-guarantee vs in-component-implementation) so the brief sets honest expectations. The S35b cycle was framed as predicate-removal which is a within-component implementation; the architectural-guarantee at this contract surface is D32 per-DB. The brief's framing was implicitly assuming the predicate was the primary mechanism; the cycle's outcome corrected that assumption.

```
metrics:
  classification: carryover_hygiene
  brief_started: 2026-05-14
  session_started: 2026-05-14
  session_closed: 2026-05-14
  merged: 2026-05-14
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: updated
  corrects:
  corrected_by:
```

