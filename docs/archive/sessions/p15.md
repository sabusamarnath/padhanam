# Archive — P15 sessions

P15 (outbound-initiation + messaging substrate + calendar/email surfaces) plus the standalone Ciborra audit / post-Ciborra corrections / methodology-upgrade / prune-and-reframe / Nango-provisioning sessions and the P15-audit-residue corrective, all in the P15 active period. Archived at S102m under the retention rule (D107 per-package-on-close).

---

## S53 — P15 opens: Outbound initiation substrate
roles: engineer, technical writer, architect
mode: build (substrate foundation; P15 opens)

S53 opens P15 with the outbound initiation substrate foundation per the P15 framing strategic-mode session (2026-05-27). Six pre-write reconciliation findings landed at brief altitude before code drafting (one substantive finding plus five no-divergence confirmations); the brief-altitude finding count holds at one for the second session running (S52 first-application evidence at one finding; S53 brief at one finding). The seven-commit substrate landed end-to-end: BroadcastFlow Protocol at shared_kernel (D142) parallel to ConversationFlow; BroadcastDispatch substrate at messaging context (D143) symmetric to CellDispatch; BroadcastFlow contract harness with four parametrised conformance scenarios plus synthetic-implementer baseline registration; ChannelResolver Protocol with the StaticConfigChannelResolverAdapter (D144) activating D136 Primitive 2 at the Phase 2-A degenerate-static shape; reactive outbound use case refactor consulting ChannelResolver before send. **P15 substrate foundation in place**; S54 (HTTP trigger endpoint per D145 + daily-briefing BroadcastFlow implementer), S55 (calendar-read + calendar-conversation), S56 (email-read + email-conversation), S57 (threshold-briefing + ThresholdEvaluator + P15 close) consume the substrate to ship the user-facing surfaces.

- Produced: Seven commits. (1) `docs(charter): D142 + D143 + D144 + D145 + architecture.md + packages.md + deferred-decisions.md + p15-epic.md + captures.md for P15 framing close` (5a85535) — D142-D145 at decisions.md tail plus summary lines; architecture.md additions for BroadcastFlow Protocol + BroadcastDispatch substrate + ChannelResolver Protocol + HTTP trigger endpoint sub-sections; packages.md P15 line revised to medium scope across five sessions plus P16 tightened to wider Wave 4 surfaces; deferred-decisions.md updates (calendar tool service and email tool service activations both routed through Path B / Nango self-hosted; D136 Primitive 2 P15 S53 activation note; new P15 framing deferrals section with three entries: background sync; MCP transport swap; Path A migration); p15-epic.md (new file); five P15 framing captures entries; brief preserved at briefs/p15/s53.md per D43; current-package.md S53 in-flight marker. (2) `feat(shared_kernel): BroadcastFlow Protocol + TriggerContext + BroadcastResponse satisfying CitedResponse` (2c8b00f) — runtime-checkable Protocol with single ``fire`` method; TriggerContext frozen value object with the five Phase 2-A BroadcastTriggerType discriminator values; BroadcastResponse runtime-checkable Protocol satisfying CitedResponse via the three citation tuple fields; lives at separate ``shared_kernel/broadcast_flow.py`` per pre-write reconciliation Finding 4 (BroadcastFlow architecturally parallel to ConversationFlow, not an extension); imports CitedResponse and ArtefactCitation from conversation_flow.py. 10 unit tests. (3) `feat(messaging): BroadcastDispatch port + InProcessBroadcastDispatchAdapter + registry mechanism` (6bad6a4) — BroadcastDispatch port plus BroadcastFlowRegistry port at messaging.application.ports; InProcessBroadcastDispatchAdapter composite implementing both Protocols; deterministic routing on trigger_type; NoRegisteredBroadcastImplementerError on missing implementer (no silent drop); structured failure logging on implementer ``fire`` exception with caller-supplied context extension. Composition root wires one composite adapter and publishes as both broadcast_dispatch and broadcast_flow_registry fields. 11 unit tests + 3 tenant-isolation contract scenarios. (4) `test(contract): BroadcastFlow contract harness + four conformance scenarios` (a773d18) — harness conftest globs ``test_*_broadcast_flow.py`` registration modules mirroring S45 ConversationFlow pattern; synthetic harness implementer registered at ``test_synthetic_broadcast_flow.py``; four parametrised conformance scenarios: fire-method signature, BroadcastFlow Protocol isinstance, BroadcastResponse satisfies CitedResponse, tenant_id and user_id pass-through. (5) `feat(messaging): ChannelResolver Protocol + StaticConfig adapter + ChannelDestination + MessageIntent + MessagingSettings extension` (9e3c4cc) — ChannelResolver Protocol; ChannelDestination + ChannelType (at shared_kernel for cross-platform/contexts reference per build-mode Finding A; messaging-domain re-export preserved); MessageIntent enum at shared_kernel; StaticConfigChannelResolverAdapter; MessagingSettings extended with operator_default_channel + operator_default_address (both authored fresh per pre-write reconciliation Finding 1; the brief's "reuse the existing operator phone number" framing was structurally false because twilio_whatsapp_from is the platform's sender, not the operator's destination). 14 new unit tests across ChannelType/ChannelDestination/MessageIntent (5), the static-config adapter (5), and the MessagingSettings extension (4). (6) `refactor(messaging): reactive outbound consults ChannelResolver before send` (315e2b3) — send_message use case gains channel_resolver + message_intent kwargs; consultation happens before delivery; Phase 2-A identity routing preserves existing behaviour. dispatch_inbound gains channel_resolver threading through three helper sites; four router call sites updated. FakeChannelResolver added to test fakes; six unit-test send_message call sites and six dispatch_inbound.execute call sites inject the resolver. Integration-test MessagingComposition gains broadcast_dispatch + broadcast_flow_registry + channel_resolver fields. (7) `docs(p15/s53): outbound substrate smoke + session log + current-package transition` (this commit) — procedural smoke at docs/smoke/p15_s53_outbound_substrate.md walking six stages (baseline, synthetic implementer registration, dispatch routing, missing-implementer failure mode, reactive outbound ChannelResolver consultation, integration-test parity, dispatch-task lifecycle); this session-log entry; current-package.md S53 close + S54 next marker.

- Decisions: D142 (BroadcastFlow Protocol at shared_kernel); D143 (BroadcastDispatch substrate at messaging context); D144 (ChannelResolver Protocol with static-configuration adapter at Phase 2-A; D136 Primitive 2 structural activation); D145 (HTTP trigger endpoint substrate with audit chain integration; architecture committed at S53, code lands at S54). All four committed at commit 1 (charter commit); no additional D-entries at S53.

- Tests: 1959 unit + contract tests pass at S53 close (+43 from S52 close baseline of 1916). 35 of 35 import-linter contracts kept (no new contracts; the existing layered architecture absorbs the new modules cleanly). AST enforcement green. Three pre-existing environmental failures (contexts/methodology test_lvt_round_trip, contexts/tools test_tool_repository, test_p3_full_slice) carry forward from S44a/S44b/S52 unchanged — S53 introduces zero regressions. New test counts: 10 shared_kernel BroadcastFlow tests; 11 BroadcastDispatch adapter unit tests; 3 BroadcastDispatch tenant-isolation contract scenarios; 5 BroadcastFlow contract conformance scenarios (against synthetic implementer); 5 ChannelType/ChannelDestination/MessageIntent value-object tests; 5 StaticConfigChannelResolverAdapter unit tests; 4 MessagingSettings extension tests; 1 ChannelResolver consultation assertion at send_message test. The 11 messaging integration tests pass after MessagingComposition extension absorption.

- Reflection prompts answered:

  1. **BroadcastFlow Protocol second-instance pattern (prompt 1).** D115 ConversationFlow + D138 CitedResponse + D142 BroadcastFlow are three instances of the shared_kernel cross-cutting Protocol pattern. At S53 the pattern held operationally exactly: BroadcastFlow Protocol is runtime-checkable per the ``@runtime_checkable`` decorator; parallel to ConversationFlow rather than inherited (separate ``shared_kernel/broadcast_flow.py`` file per Finding 4); structural typing without inheritance coupling (the synthetic harness implementer satisfies via duck-typing, no base-class import). The captures entry recording the pattern as third-instance observation is the right indexing: the pattern is implicit-but-coherent; no methodology.md elevation needed; future architectural decisions naming new shared_kernel Protocols inherit the shape structurally.

  2. **BroadcastDispatch substrate symmetry with CellDispatch (prompt 2).** S47's CellDispatch port handles inbound-triggered cell execution with classifier-driven routing (the dispatch_inbound use case at D140 routes via MetaClassifier). S53's BroadcastDispatch port handles trigger-driven implementer execution with deterministic routing on ``trigger_type``. The symmetric architecture held operationally cleanly: both ports have async ``dispatch`` methods; both adapters are composite (CellDispatch handles task lifecycle; BroadcastDispatch handles both registry + dispatch); both emit structured failure logs with caller-supplied context. The structural difference is routing semantics — deterministic vs classifier-driven — which is the conceptual difference between known-source triggers (scheduler, ThresholdEvaluator) versus unknown-shape inbound messages. No divergence surfaced; the architecture committed at D143 matched the implementation cleanly.

  3. **ChannelResolver static-config adapter correctness (prompt 3).** D144 commits the static-config adapter at Phase 2-A. The reactive outbound refactor at commit 6 is the first consumer. Identity routing worked cleanly: the static-config adapter returns the operator-default WhatsApp channel + operator_default_address regardless of input; the existing call sites pass ``channel=WHATSAPP`` and ``to_address=reply_to``; at Phase 2-A the resolved channel matches the kwarg by construction; behaviour preserved. No edge cases surfaced. The consultation is the structural forward-compat seam at which multi-channel activation later swaps the adapter; the consultation does happen on every send (verified by the new FakeChannelResolver consultation-count assertion in test_send_message_delivers_persists_and_audits).

  4. **Interface-versus-implementation discipline at S53 (S49 standing surface, fifth recurrence test instance) (prompt 4).** S50 first instance (two findings caught at draft time); S51 second instance (three brief-altitude findings plus one build-mode finding); S52 third instance (one brief-altitude finding plus one schema.md hygiene gap); S52 framing fourth instance (interface-vs-implementation at every architectural commitment shape); S53 brief is the fifth exercise. At S53 brief altitude the discipline produced clean interface decisions: D142-D145 commit interface decisions (Protocol shapes, port shapes, value object shapes, enum values, registry mechanism); implementation decisions stayed at code commits (specific TriggerMetadata shape per trigger type at commit 2; StaticConfigChannelResolverAdapter implementation shape at commit 5; reactive outbound consultation-vs-authoritative shape at commit 6). No interface decisions caught at pre-write reconciliation that should have deferred. The discipline at fifth recurrence is operationally enforced as standing pre-write reconciliation; the methodology candidate's promotion-readiness for methodology.md continues to accumulate evidence.

  5. **Substrate-inheritance survey at S53 brief (prompt 5).** S52 framing first-application evidence (five surveys at framing altitude; brief-altitude reconciliation surface reduced from S51's five findings to S52's one). S53 brief altitude carries one substantive finding (Finding 1: MessagingSettings has no operator phone number field to reuse) plus five no-divergence confirmations (Finding 2 MessagingSettings location; Finding 3 send_message exact name + call-site count; Finding 4 shared_kernel single-file convention + BroadcastFlow placement; Finding 5 no Alembic migration at S53; Finding 6 messaging/ports vs messaging/application/ports layering). Plus one build-mode finding (Finding A at commit 5: ChannelType lifted to shared_kernel because the platform → contexts import-linter contract forbids padhanam.config.messaging from importing contexts.messaging — the brief's placement at contexts/messaging/domain/channel_type.py was structurally false at the import-linter boundary; the messaging-domain path stays as a re-export to honor the brief's API). Two-finding total at S53 (one brief, one build); S52 had two-finding total (one brief, one schema.md hygiene). The framing-altitude survey at P15 caught three corrections (per captures entry 1) so the brief-altitude residual stays at one. First-application evidence continues to hold; the methodology candidate's refinement notes from S52 (Alembic-numbering sub-surface; schema.md-completeness sub-surface) get a third concrete check: import-linter-contract preservation at cross-package value placement.

  6. **MessageIntent enum scope appropriateness (prompt 6).** D144 commits three Phase 2-A values. The enum's scope felt right operationally — REACTIVE_RESPONSE covers every existing call site (manual_entry / audit_conversation / mirror_conversation / dispatch_inbound / plain HTTP send all reactive); BROADCAST_DAILY_BRIEFING covers the S54 daily-briefing implementer's resolver consultation; BROADCAST_THRESHOLD_BRIEFING covers S57's threshold-briefing. No additional values emerged during implementation that suggest the enum needs expansion before S54. Future implementers at S55 (calendar-conversation) and S56 (email-conversation) reactively respond to inbound queries, so they reuse REACTIVE_RESPONSE; no calendar- or email-specific intent values needed. The enum extension is additive when new outbound surfaces surface; the Phase 2-A scope is the right load-bearing-minimum.

  7. **Procedural smoke shape at substrate-only sessions (prompt 7).** S53 is a substrate session with only one user-facing exercise (Stage 4 reactive outbound consultation); Stages 1-3 plus 5-6 exercise the dispatch substrate mechanically without a real user-facing path. The substrate-only smoke shape produced useful verification evidence — the operator can observe registration, dispatch routing, missing-implementer error, dispatch-task lifecycle directly via the smoke-helper script; the in-process semantics validate against the live composition before S54+ implementer registration. The boundary is that the smoke does not exercise BROADCAST_INITIATED audit events (the event class extends at S54), the HTTP trigger endpoint (lands at S54), or any user-facing broadcast output. This is honestly framed in the smoke document: substrate-only at S53; user-facing implementer surfaces verify at S54 (daily-briefing) and S57 (threshold-briefing + ThresholdEvaluator + P15 close). The substrate-only smoke shape feels right at substrate-foundation sessions; the load-bearing user-facing exercise lands at the consuming session.

  8. **Reactive outbound refactor risk-shape (prompt 8).** Commit 6 modifies the existing send_message use case from S45 plus the dispatch_inbound use case plus four router call sites plus six unit-test call sites plus six dispatch_inbound test call sites plus the integration-test MessagingComposition construction. The refactor preserved behaviour cleanly: the Phase 2-A static-config resolver returns WHATSAPP matching the existing default channel kwarg; identity routing at the receive_inbound→reactive_outbound path; no latency-sensitive change (the static-config adapter is in-memory; the consultation adds negligible time). The structured logging emits no error at Phase 2-A; failure handling is unchanged. The only behavioural change is the new FakeChannelResolver consultation-count assertion in test_send_message_delivers_persists_and_audits (the resolver is called once per send). The breadth of the call-site sweep was greater than the brief's "one new line / one new dependency injection" framing implied — captured at the pre-write reconciliation Finding 3 surfacing for accurate scope. No subtle behaviour changes (latency; error handling; logging) introduced.

methodology: Six methodology surfaces exercised at S53. (1) Interface-versus-implementation discipline at standing pre-write reconciliation surface — fifth recurrence test instance after S50/S51/S52/S52-framing. (2) Substrate-inheritance survey at framing-altitude (three corrections at P15 framing per captures entry 1) plus at brief altitude (one finding at S53 brief; one finding at S53 build mode). First-application evidence from S52 holds; the candidate gains a third concrete sub-surface (import-linter-contract preservation at cross-package value placement). (3) Structural-test SSOT binding — the four parametrised contract harness scenarios at tests/contract/broadcast_flow/ verify Protocol satisfaction structurally rather than via per-implementer tests; the synthetic harness implementer is the S53 baseline; S54+ implementers register and the same scenarios cover them. (4) Component-quality versus integration-smoke discipline — held the boundary across the BroadcastDispatch substrate (unit tests + contract harness) and the procedural smoke (substrate-only with one user-facing stage); the synthetic implementer carries the contract harness load; the user-facing implementer surfaces verify at the consuming session per the brief's substrate-only smoke shape. (5) Cross-package value placement at import-linter boundaries — build-mode Finding A reinforces the principle that values referenced by both contexts and platform live at shared_kernel; the MessageIntent + ChannelType both lift to shared_kernel for the same reason; future cross-package value placement decisions inherit the pattern. (6) Brief-altitude reconciliation finding count tracking — S51 five findings, S52 one finding, S53 two findings (one brief + one build mode). The framing-altitude survey discipline is reducing brief-altitude surface as expected; the trend will continue to firm at subsequent sessions.

```
metrics:
  classification: build session (substrate foundation; P15 opens)
  brief_started: 2026-05-27
  session_started: 2026-05-27
  session_closed: 2026-05-28
  merged: 2026-05-28
  close_state: clean (seven-commit substrate landed end-to-end; BroadcastFlow Protocol + BroadcastDispatch substrate + ChannelResolver Protocol + reactive outbound refactor; 1959 unit+contract tests pass; 35/35 import-linter contracts kept; one brief-altitude + one build-mode pre-write reconciliation finding absorbed; subsequent P15 sessions consume the substrate)
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: charter/decisions.md (D142 + D143 + D144 + D145); charter/architecture.md (BroadcastFlow Protocol + BroadcastDispatch substrate + ChannelResolver Protocol + HTTP trigger endpoint sub-sections); charter/packages.md (P15 medium-scope revision + P16 tightened); charter/deferred-decisions.md (calendar tool service + email tool service activations through Path B; D136 Primitive 2 P15 S53 activation note; new P15 framing deferrals section with background sync + MCP transport swap + Path A migration entries); charter/packages/p15-epic.md (new file); log/captures.md (5 new entries from P15 framing); log/sessions.md (this entry); charter/current-package.md (S53 close + S54 next marker); briefs/p15/s53.md (brief preservation per D43); shared_kernel/broadcast_flow.py (new); shared_kernel/channel_type.py (new); shared_kernel/message_intent.py (new); shared_kernel/__init__.py (exports); contexts/messaging/application/ports/broadcast_dispatch.py + broadcast_flow_registry.py + channel_resolver.py (new); contexts/messaging/adapters/dispatch/ (new directory + InProcessBroadcastDispatchAdapter); contexts/messaging/adapters/channel_resolver/ (new directory + StaticConfigChannelResolverAdapter); contexts/messaging/domain/channel_type.py + channel_destination.py (new); contexts/messaging/application/send_message.py + dispatch_inbound.py (refactor for ChannelResolver consultation); padhanam/config/messaging.py (MessagingSettings extension with operator_default_channel + operator_default_address); apps/api/_messaging_wiring.py (MessagingComposition extension + StaticConfigChannelResolverAdapter wiring + InProcessBroadcastDispatchAdapter composite); apps/api/routers/messaging.py (channel_resolver threading at four send_message + one dispatch_inbound call sites); tests/contract/broadcast_flow/ (new harness + 4 scenarios + synthetic implementer registration); tests/contract/tenant_isolation/test_broadcast_dispatch_isolation.py (new 3 scenarios); tests/unit/shared_kernel/test_broadcast_flow.py (new 10 tests); tests/unit/contexts/messaging/adapters/test_in_process_broadcast_dispatch_adapter.py (new 11 tests); tests/unit/contexts/messaging/adapters/test_static_config_channel_resolver_adapter.py (new 5 tests); tests/unit/contexts/messaging/test_channel_domain.py (new 5 tests); tests/unit/padhanam/config/test_messaging_settings.py (new 4 tests); tests/unit/contexts/messaging/application/_fakes.py + test_use_cases.py + test_dispatch_inbound.py (FakeChannelResolver + injection at 12 call sites + 1 consultation assertion); tests/integration/api/test_messaging_routes.py (MessagingComposition extension); docs/smoke/p15_s53_outbound_substrate.md (new procedural smoke)
  corrects:
  corrected_by:
```

## 2026-05-28 — [S54] First end-to-end broadcast: HTTP endpoint + daily-briefing
roles: engineer, technical writer, architect
mode: build (HTTP transport + first BroadcastFlow implementer + idempotency substrate)

S54 delivers the first platform-initiated broadcast end-to-end on top of S53's substrate: the HTTP trigger endpoint (D145 code; architecture committed at S53), the fired_triggers race-safe idempotency substrate (D147), and the daily-briefing BroadcastFlow implementer (D146) composing from three producer contexts. Three pre-write reconciliation findings landed at build-mode altitude before code (Finding 1: no discrete audit event-class set exists; Finding 2: IntakeRecord time-window via pagination not a dedicated filter; Finding 3: TriggerContext.metadata stays as dict for backward compat — sixth interface-vs-implementation recurrence) plus one path-naming reconciliation (the brief's `apps/api/middleware/internal_secret.py` collides with the existing `apps/api/middleware.py` module file; landed at `apps/api/_internal_secret.py`). **P15 first end-to-end broadcast operational**; S55 (calendar-read + calendar-conversation), S56 (email-read + email-conversation), S57 (threshold-briefing + ThresholdEvaluator + P15 close) follow.

- Produced: Seven commits. (1) `docs(charter): D146 + D147 + architecture.md + schema.md + deferred-decisions.md + p15-epic.md + captures.md for S54 framing close` (3eb5478) — D146/D147 at decisions.md tail; architecture.md additions (HTTP trigger endpoint implementation; fired_triggers idempotency substrate; BROADCAST_INITIATED audit event class; daily-briefing context); schema.md fired_triggers table + FiredTrigger value object; deferred-decisions.md P15 S54 section (two-phase commit; retention policy; TriggerContext metadata schema completion; daily-briefing cell_payload); p15-epic.md S54 closing marker; six captures entries; current-package.md S54 in-flight marker; brief at briefs/p15/s54.md. (2) `feat(messaging): Alembic 0025 fired_triggers table for race-safe idempotency` (c2c3254) — Alembic 0025 (UNIQUE on tenant_id+user_id+trigger_type+idempotency_key; CHECK on trigger_type); FiredTrigger value object; FiredTriggersRepository Protocol with insert_or_skip; PostgresFiredTriggersAdapter (INSERT ON CONFLICT DO NOTHING, rowcount-based, bound-tenant defence); FakeFiredTriggersRepository; 12 tests. (3) `feat(messaging): TriggerContext metadata helpers + idempotency key resolver + BROADCAST_INITIATED event class` (905d099) — DailyScheduledMetadata + ManualMetadata convenience constructors above the unchanged dict slot; resolve_idempotency_key (DAILY_SCHEDULED→operator-date; MANUAL→None; future→NotImplementedError); BROADCAST_INITIATED constants + draft helper at the messaging audit_events.py per Finding 1; 15 new tests. (4) `feat(messaging): FireTrigger use case + HTTP trigger endpoint at apps/api/routers/triggers.py + internal-secret auth` (536577e) — FireTrigger seven-step use case; POST /api/v1/internal/triggers/fire router + DTOs; internal-secret auth at apps/api/_internal_secret.py (fail-closed, hmac.compare_digest); trigger path added to middleware public-path set; MessagingSettings gains daily_briefing_window_hours + operator_timezone + internal_secret; FiredTriggersRepositoryAdapter wired into MessagingComposition; 12 tests (4 internal-secret unit + 3 FireTrigger unit + 5 HTTP integration). (5) `feat(daily_briefing): bounded context + DailyBriefingReader consumer port + cross-context wiring adapter` (eb9072f) — contexts/daily_briefing/ (domain: BriefingPeriod + DailyBriefingResponse satisfying CitedResponse + render_for_whatsapp; application/ports: DailyBriefingReader + own DTOs per Finding 4); DailyBriefingReaderAdapter composing intake/audit/portfolio; new layers-daily-briefing import-linter contract + three independence entries; 9 tests. (6) `feat(daily_briefing): LLM composer + BroadcastFlow implementer + WhatsApp render extension` (c0300c6) — DailyBriefingComposer port + LiteLLMDailyBriefingComposerAdapter (StructuredOutputPort, REAL_TIME_REQUIRED, prompt template); BriefingNotifier consumer port + wiring adapter (ChannelResolver + send_message); DailyBriefingImplementer five-step fire; registered against BroadcastFlow registry with trigger_type=DAILY_SCHEDULED; BroadcastFlow contract harness runs against daily-briefing; 13 tests. (7) `docs(p15/s54): first end-to-end broadcast smoke + session log + current-package transition` (this commit) — procedural smoke at docs/smoke/p15_s54_daily_briefing.md (8 stages); this entry; current-package.md S54-close marker.

- Decisions: D146 (daily-briefing composition pattern with DailyBriefingReader consumer port); D147 (fired_triggers table with race-safe idempotency at HTTP trigger endpoint). Both committed at commit 1 (charter commit); no additional D-entries at S54.

- Tests: 2023 unit + contract tests pass at S54 close (+64 from S53 close baseline of 1959); full unit + contract suite exit 0. 36 of 36 import-linter contracts kept (+1 layers-daily-briefing). The AST-walking structural enforcement tests (`tests/_enforcement/` minus host-port-bindings: test_no_getenv, test_no_vendor_in_domain, test_no_raw_neo4j_session, test_no_plaintext_in_state, test_tracer_provider_setup, test_dockerfile_workspace_coverage, test_lint_violations) all green. Pre-existing failures carry forward unchanged — S54 introduces zero regressions: three environmental (contexts/methodology test_lvt_round_trip, contexts/tools test_tool_repository, test_p3_full_slice) plus one structural-enforcement carry-forward surfaced at S54 final verification (`tests/_enforcement/test_host_port_bindings.py` red since S45's commit 7af8e88 added the dev-only `127.0.0.1:8000:8000` loopback webhook binding to compose.yaml without updating the allowlist — confirmed red at S53 close 2071c7b before any S54 code; the operator may clean up the leftover S45-smoke binding or allowlist it in a future hygiene pass; outside S54 scope since S54 touched no compose.yaml). BroadcastFlow contract harness's conformance scenarios now fire against the daily-briefing registration (alongside the synthetic baseline) automatically. New tests by surface: fired_triggers value object + adapter + protocol + isolation (12); idempotency resolver + trigger metadata + BROADCAST_INITIATED audit event (15); internal-secret + FireTrigger + HTTP endpoint integration (12); daily_briefing response/period + composer + implementer + wiring + harness registration (22 incl. parametrised harness re-run).

- Reflection prompts answered:

  1. **First end-to-end broadcast operational closure.** The substrate-plus-implementer composition worked cleanly end-to-end at the unit/contract altitude. One architectural seam surfaced and was resolved at build time: the BroadcastFlow implementer cannot import `contexts.messaging.application.send_message` directly (the contexts-independent-application contract forbids it), so the implementer's outbound send goes through a daily-briefing-owned `BriefingNotifier` consumer port that the wiring adapter implements by consulting ChannelResolver and calling send_message. This made the BROADCAST_INITIATED ordering clean: the audit event fires at the FireTrigger endpoint (after the idempotency insert, before dispatch) per D147, while the outbound Message + its own audit event fire inside the implementer's send. Two distinct audit anchors (the broadcast-initiated trigger event and the outbound-message event) chain into the same tenant chain. The InProcessBroadcastDispatchAdapter from S53 does fire-and-forget dispatch (the implementer's response is discarded by the adapter); the implementer's send happens inside `fire` rather than being driven by the dispatch — confirming the S53 dispatch substrate is a thin router, not an orchestrator.

  2. **fired_triggers idempotency operational verification.** Verified at the unit/integration altitude: the FakeFiredTriggersRepository simulates the four-tuple UNIQUE (fresh→True, duplicate→False, null-key→always-fresh per Postgres NULL semantics) and the HTTP integration test confirms a same-day retry returns ALREADY_FIRED with no second audit event and no second send. The live UNIQUE-constraint behaviour (the actual ON CONFLICT DO NOTHING rowcount) is the smoke's Stage 5 verification. The idempotency-key-at-endpoint design (D147) means the dispatch and implementer never see duplicates — the lowest-overhead skip.

  3. **DailyBriefingReader consumer port composition.** Third instance of the consumer-port-plus-wiring-adapter pattern, and the first composing MULTIPLE producer contexts (intake + audit + portfolio) behind one port. The pattern held cleanly: the wiring adapter delegates each read to its producer (IntakeRepository, AuditEventReader, list_cases) and translates into daily-briefing-owned DTOs. One composition asymmetry surfaced (Finding 2): the audit reader carries a `timestamp_range` filter so the window applies at the query, but IntakeListFilters has no time-window dimension so the intake window trims in-memory at Phase 2-A dogfooding scale. The DTO-at-the-port discipline (Finding 4) kept daily_briefing.application from importing any producer-context domain module — import-graph independence held at the application-to-domain cross-context surface.

  4. **LLM composer empty-day handling.** Verified at the implementer unit test (empty reads → composer still returns prose → briefing still sends, cites active Cases only) and the composer adapter unit test (an empty model narrative falls back to a non-empty count summary so the always-send commitment holds structurally). The prompt template carries the empty-day prose-adjustment instruction explicitly. Live composer behaviour against a real model is the smoke's Stage 7 verification. The always-send-via-prose-adjustment design (D146) keeps skip logic out of the implementer.

  5. **Interface-versus-implementation discipline at S54 (sixth recurrence test instance).** S50 first; S51 second; S52 third; S52 framing fourth; S53 brief fifth; S54 brief sixth. The flagship S54 instance was Finding 3: the brief framed TriggerContext metadata as a discriminated union replacing the dict field, but the interface decision (typed metadata classes exposing the same data via a typed surface) preserves the structural backward-compatible shape while landing the typed-metadata discipline. Two further build-time interface catches: the audit event-class-set framing (Finding 1: the interface is per-context constants + a draft helper, not a new audit-context artefact) and the internal-secret path-naming (the brief's middleware/ subdirectory collides with the middleware.py module — interface lands at _internal_secret.py). The discipline at sixth recurrence is operationally enforced as standing pre-write reconciliation; promotion-readiness for methodology.md continues to firm.

  6. **Substrate-inheritance survey three-category brief-altitude evidence.** S53 close folded import-graph topology as the third category. S54 build-mode pre-write reconciliation exercised all three: substrate-existence (Finding 1 audit event-class set; the DailyBriefingReader/AuditEventReader/list_cases existence checks all returned existing substrate); configuration-conventions (Finding 2 IntakeListFilters has no time-window filter; MessagingSettings extension pattern); import-graph topology (Finding 4 daily_briefing consumer-DTO discipline; the BriefingNotifier port needed to avoid the cross-context application import). Three substantive build-time findings at S54 vs the framing-altitude survey's near-zero residual — slightly above S53's two-finding total because S54's scope (a whole new bounded context plus the cross-context send seam) carried more surface than S53's substrate-only work. The "verify artefact existence at claimed shape" sub-surface (from Finding 1) is the new concrete check the candidate gains.

  7. **Schema.md hygiene check at charter authoring (second instance).** S52 first instance (PendingClarification gap absorbed). At S54 the charter commit's schema.md touch added the fired_triggers table + the FiredTrigger value object; pre-write reconciliation verified the existing messaging-substrate schema sections (messages, pending_clarifications) against codebase reality and found no drift to absorb. The new audit event "class" (BROADCAST_INITIATED) required no schema.md change because the audit chain uses action_verb + resource_type strings on the existing tenant_audit table (Finding 1) — no new column, no CHECK constraint. Clean: the schema.md surface stayed in lockstep with the migration.

  8. **WhatsApp render extension for broadcasts.** Daily-briefing is the first BroadcastFlow render. The render extension followed the D135 domain-decides-content channel-decides-format pattern cleanly — but landed at `contexts/daily_briefing/domain/response.py` (co-located with the response value object) rather than the brief's `contexts/messaging/adapters/`. This matches the established mirror-conversation/audit-conversation precedent (their `render_for_whatsapp` lives in their own context's response module, invoked at the composition boundary). One broadcast-specific render concern surfaced and was accommodated: the briefing-period header line (`Daily briefing · <window>`) at the top, distinct from the conversation renders' citation-footer-only shape. The citation footer (Shape-1) is identical to the conversation precedent.

  9. **Daily-briefing as bet-positioning first instance.** The bet's attentional-infrastructure positioning manifests first here. The composition logic (D146 Option B: recent activity plus current portfolio state) is in place; whether the briefings *feel* attention-management rather than activity-feed is an operator-feedback question that lands at the smoke's live execution (Stages 3 + 7) and accumulating dogfooding evidence. The architectural intent: the "attention framing" emerges from how recent changes surface against the portfolio snapshot, not from heuristic attention-item guessing (D146 Alternatives (b) rejected at first instance). The forward-looking escalation (threshold-briefing) is S57. Operator-feedback trigger: if the briefings read as activity-feed during dogfooding, the composer prompt and/or the read composition revisits before S57.

methodology: Six methodology surfaces exercised at S54. (1) Interface-versus-implementation discipline at standing pre-write reconciliation — sixth recurrence (Finding 3 TriggerContext metadata flagship; Finding 1 audit event-class shape; internal-secret path-naming). (2) Substrate-inheritance survey three-category brief-altitude evidence — three build-time findings; the "verify artefact existence at claimed shape" sub-surface added. (3) Schema.md hygiene check at charter authoring — second instance; no drift to absorb at S54. (4) Structural-test SSOT binding — the BroadcastFlow contract harness's parametrised conformance scenarios bind the daily-briefing implementer structurally (it joins the synthetic baseline; no harness change). (5) Consumer-port-plus-wiring-adapter pattern — third instance verified, first composing multiple producer contexts; the consumer-DTO sub-discipline (Finding 4) preserves application-to-domain cross-context independence. (6) Path-naming reconciliation (S43b promoted pattern) — the brief's `apps/api/middleware/internal_secret.py` collides with the `apps/api/middleware.py` module file; landed at `apps/api/_internal_secret.py` per the leading-underscore convention.

```
metrics:
  classification: build session (first end-to-end broadcast; P15 progresses)
  brief_started: 2026-05-27
  session_started: 2026-05-28
  session_closed: 2026-05-28
  merged: 2026-05-28
  close_state: clean (seven-commit first-end-to-end-broadcast landed end-to-end; HTTP trigger endpoint + fired_triggers idempotency + daily-briefing implementer; 2023 unit+contract tests pass; 36/36 import-linter contracts kept; three build-mode pre-write reconciliation findings + one path-naming reconciliation absorbed; smoke procedural pending live operator execution)
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: charter/decisions.md (D146 + D147); charter/architecture.md (HTTP trigger endpoint implementation + fired_triggers idempotency substrate + BROADCAST_INITIATED audit event class + daily-briefing context sub-sections); charter/schema.md (fired_triggers table + FiredTrigger value object); charter/deferred-decisions.md (P15 S54 deferrals: two-phase commit + retention policy + TriggerContext metadata schema completion + daily-briefing cell_payload); charter/packages/p15-epic.md (S54 closing marker); log/captures.md (6 new entries: 3 framing-close + 3 build-mode); log/sessions.md (this entry); charter/current-package.md (S54 close + S55 next marker); briefs/p15/s54.md (brief preservation per D43); alembic/tenant/versions/2026_05_28_0025_fired_triggers.py (new); contexts/messaging/domain/fired_trigger.py + idempotency.py (new); contexts/messaging/ports/fired_triggers_repository.py (new); contexts/messaging/adapters/outbound/postgres/fired_triggers_postgres_adapter.py + _tables.py (new + extended); contexts/messaging/application/fire_trigger.py + audit_events.py (new + extended); shared_kernel/broadcast_flow.py + __init__.py (TriggerContext metadata helpers); padhanam/config/messaging.py (daily_briefing_window_hours + operator_timezone + internal_secret); apps/api/_internal_secret.py + routers/triggers.py (new); apps/api/middleware.py + main.py + _messaging_wiring.py (trigger wiring); contexts/daily_briefing/ (new bounded context: domain + application + ports + adapters/llm + prompts); apps/api/_daily_briefing_wiring.py (new: DailyBriefingReaderAdapter + BriefingNotifierAdapter + implementer builder); .importlinter (layers-daily-briefing + three independence entries); tests/unit/contexts/messaging/{domain/test_fired_trigger.py,domain/test_idempotency.py,adapters/test_fired_triggers_postgres_adapter.py,test_fired_triggers_repository_protocol.py,test_broadcast_initiated_audit_event.py,test_fire_trigger.py}; tests/unit/shared_kernel/test_trigger_metadata.py; tests/unit/apps/api/{test_internal_secret.py,test_daily_briefing_wiring.py}; tests/unit/contexts/daily_briefing/{test_response.py,test_composer_adapter.py,test_implementer.py}; tests/contract/tenant_isolation/test_fired_triggers_isolation.py; tests/contract/broadcast_flow/test_daily_briefing_broadcast_flow.py; tests/integration/api/{test_triggers_routes.py,test_messaging_routes.py}; docs/smoke/p15_s54_daily_briefing.md (new procedural smoke)
  corrects:
  corrected_by:
```

## 2026-05-28 — [Ciborra audit] Phenomenological second-lens audit (standalone strategic-mode, read-only)
roles: analyst, architect
mode: strategic (assessment session; produces a finding artefact, not a decision artefact; read-only against all code)

A standalone strategic-mode audit applying Claudio Ciborra's Heideggerian seven-concept lens (Gestell, breakdown, Stimmung, care/Sorge, authenticity-versus-das-Man, bricolage, drift) to the as-built Padhanam at HEAD `718c46e`. Complementary to the P12 engineering audit (`charter/p12-audit-findings.md`): where P12 verified D-entries against code through an engineering lens, this reads the code and git for whether they betray the phenomenological self-image the charter projects. Carries no S-series number — it ships no substrate. Read-only constraint held: the only files written are the audit document, this entry, and the captures entries below; `git diff --stat` shows zero changes under `contexts/`, `apps/`, `padhanam/`, `tests/`, or migrations.

- Produced: the audit document at `charter/ciborra-phenomenological-audit.md` (location chosen to sit beside the P12 audit it complements; `docs/audits/` exists but is empty and was rejected for co-location legibility). Seven concept sections plus a header/provenance block, three reconciliation findings (R1–R3), and a synthesis. **Verdicts: aligned ×2 (breakdown, Stimmung), partially aligned ×4 (Gestell, care, authenticity, drift), aligned-in-practice-unnamed-in-vocabulary ×1 (bricolage).** Findings count: three reconciliation findings + at least four substantive departures/tensions the P12 engineering lens did not surface (the single-adapter-per-port Gestell creep; the cultivation→control drift under the interface-versus-implementation discipline; the karma-prior-art das Man transplant; the stale twelve-context anti-drift artefact). HEAD hash recorded in the document header per acceptance criterion 2.

- Decisions: none. Assessment session; the audit assesses and does not act. No D-entry, no principle amendment, no PRFAQ edit. Findings that imply action are recorded as forward-carry items below, not executed.

- Tests: none; no code change. The audit's claims are grounded in `path:line` code references and git references (commit hashes, `git log`/`git blame`/`grep` output) throughout.

- Reflection prompts answered:

  1. **Charter-vs-code fidelity — largest gap.** Gestell showed the largest gap. The principles file commits "vendor swap is configuration or adapter replacement, never domain change" and "vendor lock-in is not architectural" ([principles.md:10](../charter/principles.md)); at HEAD every load-bearing Phase-2-A port (`ChannelResolver`, `ConfidenceCalculator`, `CellDispatch`, `ThresholdResolver`, `BroadcastDispatch`) has exactly one adapter, several degenerate stubs that discard their own inputs — `StaticConfigChannelResolverAdapter` "returns the configured operator-default channel regardless of input" — with the second adapter named only in docstrings. The gap is not a clean charter overstatement: the methodology's own interface-versus-implementation discipline ([methodology.md:381](../charter/methodology.md)) honestly describes commit-the-interface-ship-one-adapter. It is closest to an honest difference of lens that tips into mild overstatement at the procurement-facing register: "pluggable / no lock-in" does rhetorical work the single-adapter reality has not yet earned. Code-honest with itself; oversold at the marketing surface.

  2. **Code-audit versus charter-audit divergence.** The session prompt (drafted in the Claude.ai charter-based register) framed this as a Phase-1-close audit with "twelve bounded contexts." The code contradicts both: the repo is mid-Phase-2-A at S54, and `contexts/` holds nineteen directories, not twelve. I trust the code and git — `ls contexts/` and `git log` are authoritative; the prompt's count matched P12's snapshot at `afabe7a`, which the build has moved seven contexts and dozens of sessions past. The divergence enriched rather than blocked the audit: the Phase-2-A material (the single-adapter ports, the karma transplants, the cultivation→control shift) is precisely what a Phase-1-close snapshot would have hidden. Forward-carry routing adjusted accordingly — items route to the next phase audit, not to a "Phase 2 opening" that has already happened.

  3. **Bricolage in git.** Yes — abundantly, and the methodology names none of it as bricolage (zero occurrences of "bricolag/improvis/materials at hand/recombin" across `charter/`, `docs/archive/`, `log/`). Two cross-referenced commits as evidence for the next-phase promotion candidate: (a) `9bb7c03` (S24), the hash_chain primitive promoted from `contexts/methodology/` to `padhanam/security/` when the agent context became a second consumer — reuse-and-recombine, framed as "second-consumer-promotes"; (b) the S39 mid-build reuse at [log/sessions.md:1799](sessions.md), where the brief planned to extract a new helper but, reading migration 0010 for an unrelated reason, the operator found `padhanam.security.hash_chain` already exposed the mechanism and abandoned the planned build — textbook Lévi-Strauss bricolage, framed as "mid-build pre-write reconciliation / third-consumer-confirms." A third, unnamed instance: the S53 `StaticConfigChannelResolverAdapter` improvised from `MessagingSettings.operator_default_channel` already to hand ([sessions.md:2057](sessions.md)). The finding is sharper than the prompt's binary anticipated: the methodology frames these neither as bricolage nor as "discipline lapses recovered" but as proactive, forward-binding disciplines. Codification of improvisation is the methodology's mechanism — the proprietary insight — and also its blind spot.

  4. **Audit-posture honesty — hardest-to-write finding.** The karma-prior-art transplant (Authenticity section). Four of the most load-bearing Phase-2-A principles ([principles.md:115-154](../charter/principles.md)) each carry "Origin: karma prior-art product specification §11.x, transplanted at P13 framing" (`grep -c "karma prior-art" principles.md` → 4). This is uncomfortable for the bet precisely because the bet's self-image is situated reckoning "hard to copy"; the platform's next-phase spine was, in material part, copied from a prior product — easy to copy because it already had been. The transplant is honestly labelled, which is itself authentic practice, but transplantation-from-prior-art sits closer to das Man than to reckoning. I resisted softening it: it is the single finding most in tension with the proposition, and an audit that found only the D52-reversal authenticity story would have failed the adversarial-posture check.

methodology: The audit surfaced a methodology observation about the methodology itself: Padhanam's signature move — converting every improvisation into a named, forward-binding discipline — is simultaneously the source of the methodology's teachability (the proprietary insight) and the mechanism by which the project's own bricolage and its Phase-2-A drift toward anticipatory control become invisible to the project. Once improvisation is renamed "discipline," it stops looking like improvisation. This is a phenomenological observation for the operator to weigh at promotion; it is not a build-mode methodology edit (D47 reserves methodology.md writes to strategic-mode/audit promotion).

- Forward-carry items (routed to the next phase audit / Phase-2-close strategic-mode work; none executed here):
  1. **Bricolage-vocabulary promotion candidate.** Git evidence assembled for the next phase audit's charter-decision consideration: commits `9bb7c03` (S24), `sessions.md:1799` (S39 mid-build reuse), `sessions.md:2057` (S53 config-to-hand resolver). The candidate is whether to name the codification-of-improvisation pattern explicitly. This audit produces the evidence; promotion is a charter decision the audit does not make (per the prompt's out-of-scope).
  2. **Stale twelve-context anti-drift artefact.** P12 Entry 16's documented count (twelve) is stale at nineteen; the artefact built to prevent rediscovery has itself drifted. Hygiene refresh for the next audit conversation.
  3. **karma-prior-art das Man tension.** Connects to the PRFAQ Option-B pluggability honesty correction the prompt names for Phase-2 opening (now passed); route to the next phase audit's authenticity review.
  4. **Change-failure-rate metric reads 0% while the qualitative failure record is non-empty.** Zero sessions classified `corrective` in the entire log, so CFR computes to 0%, while Failure-modes-observed documents real drift events. A measurement-model honesty item for the Phase-2 audit's metric review.

```
metrics:
  classification: audit
  brief_started: no brief
  session_started: 2026-05-28
  session_closed: 2026-05-28
  merged: 2026-05-28
  close_state: clean (read-only audit; only the audit document, this entry, and captures entries written; git diff --stat confirms zero code/test/migration changes)
  tests_passing: n/a
  principles_intact: n/a
  charter_touchpoints: charter/ciborra-phenomenological-audit.md (new audit artefact); log/sessions.md (this entry); log/captures.md (forward-carry captures entries)
  corrects:
  corrected_by: 2026-05-28 post-Ciborra-audit corrections session (C1 das Man + C2 pluggability findings; stale twelve-context note; ChannelResolver input-discard fix)
```

## 2026-05-28 — [Corrections] Post-Ciborra-audit corrections (standalone build-mode)
roles: analyst, architect, engineer, technical writer
mode: build session, no S-series substrate ID (builds no substrate; corrects two of the Ciborra audit's own findings, fixes the genuine code defect it surfaced, and clears a stale documentation artefact)

A standalone corrections session at the Phase-2-A boundary, acting on the Ciborra phenomenological audit (`charter/ciborra-phenomenological-audit.md`). HEAD audited against: `20c8f37` (clean tree at open, `git status` → nothing to commit). It corrects two audit findings (C1 das Man, C2 pluggability), fixes the one genuine input-discarding stub the audit surfaced, and replaces a stale frozen count with a live-count instruction. Three commits, charter ahead of code per acceptance criterion 9.

- Produced:
  - **Commit 1** (`a76fd0f`, `docs(audit): append corrections section to ciborra audit`): the verbatim `## Corrections` section appended to `charter/ciborra-phenomenological-audit.md`; the findings above it are byte-unchanged (append-only). C1 reclassifies the four karma2 principle transplants as self-reuse / bricolage (karma2 is the operator's own prior iteration, not external prior-art), authentic, with the residual per-principle fit-check as the only open item. C2 affirms the single-adapter-behind-a-Protocol YAGNI engineering and narrows the section-1 finding to the buyer-wording register gap (strategic mode) plus the input-discarding stubs (fixed here).
  - **Commit 2** (`880efee`, `docs(audit): replace frozen context count with live-count instruction`): P12 Entry 16's frozen "twelve" replaced with a live-count instruction (derive via `ls contexts/` at audit time); current count recorded as nineteen as of 2026-05-28, noted to grow as D-authorised contexts ship; the D16 observability-split observation kept as the durable thing to inherit.
  - **Commit 3** (`4fff1ce`, `fix(messaging): mark ChannelResolver per-user/per-intent resolution not-yet-implemented`): the sole bucket-(c) port. The adapter now discards its three required inputs explicitly (`del`) instead of silently, the port + adapter docstrings state the deferral and its activation trigger, and a cross-product test pins the discard as the intended not-yet-implemented contract. 179 messaging unit tests pass; 36/36 import-linter contracts kept.

- Port inventory and classification (acceptance criterion 4). Every Phase-2-A port whose adapter could plausibly discard a declared input was read against its interface. Decision/resolver/dispatch/calculator/composer ports:

  | Port | path:line | Bucket | Reason |
  |---|---|---|---|
  | ChannelResolver | contexts/messaging/application/ports/channel_resolver.py:35 | **(c)** | Three **required** inputs (tenant_id, user_id, message_intent) all discarded silently by the sole adapter; the interface claimed per-user/per-intent resolution the code does not deliver. **Fixed this session.** |
  | ConfidenceCalculator | shared_kernel/confidence_calculator.py:37 | (b) | Adapter reads `response.confidence` (its primary input); `del request` is explicit forward-compat; 0.5 is a disclosed "no-signal" default. The audit's own named honest-stub example. |
  | ThresholdResolver | shared_kernel/confidence_thresholds.py:50 | (b) | `operation_class` is **optional** (`= None`); the contract explicitly says "implementations may ignore" and callers pass None at Phase 2-A. Honest disclosed stub. |
  | CellDispatch | contexts/messaging/application/ports/cell_dispatch.py:27 | (a) | Adapter runs `cell_run()` and logs `context` on failure — both declared inputs consumed. |
  | BroadcastDispatch | contexts/messaging/application/ports/broadcast_dispatch.py:57 | (a) | Adapter routes by `trigger_context.trigger_type`, logs tenant_id/user_id/context, invokes `implementer.fire` — all inputs consumed. |
  | BroadcastFlowRegistry | contexts/messaging/application/ports/broadcast_flow_registry.py:26 | (a) | register/get/registered_types each consume their inputs. |
  | MetaClassifier | contexts/messaging/application/ports/meta_classifier.py:86 | (a) | **Two** real adapters (LLM + rule-based); both classify from the message text. `del tenant_id` is a disclosed tenant-agnostic-classification choice, not the classification input. |
  | MessageDeliveryPort | contexts/messaging/ports/message_delivery_port.py:38 | (a) | **Two** real adapters (local_echo + twilio); both consume channel/from/to/body. |
  | DailyBriefingComposer | contexts/daily_briefing/application/ports/daily_briefing_composer.py:52 | (a) | LiteLLM adapter formats all four inputs (period + three projections) into the prompt; `attention_items=()` is a disclosed forward-compat output field. |
  | BriefingNotifier | contexts/daily_briefing/application/ports/briefing_notifier.py:29 | (a) | Adapter resolves channel and sends; consumes actor + body. |
  | PortfolioGateway | contexts/messaging/application/ports/portfolio_gateway.py:109 | (a) | Adapter queries portfolio per actor; consumes inputs. |

  Repository / reader / lookup / writer ports across the Phase-2-A contexts (portfolio, messaging, intake, daily_briefing, audit_conversation, mirror_conversation, intent_classification_evaluation) are bucket (a) by construction — they persist or query exactly the inputs passed. A `grep` sweep for the deliberate-discard idiom (`del <param>`, "regardless of input", "ignored") across `contexts/` and `apps/` surfaced exactly four discard sites: ConfidenceCalculator (b), ThresholdResolver (b), MetaClassifier `del tenant_id` (a), ChannelResolver (c). No reader/repository port discards a declared filter.

- Decisions: none. Corrections session; it corrects findings and fixes a defect, it does not select between architectural alternatives.

- Tests: 179 messaging unit tests pass; the channel-resolver adapter test file gains `test_per_user_per_intent_resolution_not_yet_implemented` (cross-product of three users × every MessageIntent → one invariant operator-default destination, pinning the discard as the intended contract). 36/36 import-linter contracts kept; no regressions. (Charter commits 1–2 touch no code.)

- Reflection prompts answered:

  1. **Inventory result.** Eleven decision-class ports inventoried plus the reader/repository/writer/lookup set: **one bucket-(c) (ChannelResolver), two bucket-(b) (ConfidenceCalculator, ThresholdResolver), and the remainder bucket-(a).** The audit's "several [input-discarding] stubs share the shape" did **not** hold up on close reading — it conflated three distinct shapes. The audit listed `ChannelResolver`, `ConfidenceCalculator`, `CellDispatch`, `ThresholdResolver`, `BroadcastDispatch` as "same shape — Protocol plus exactly one adapter." But single-adapter ≠ input-discarding: CellDispatch and BroadcastDispatch fully consume their inputs (they dispatch the work); ConfidenceCalculator reads its primary input and discloses a placeholder for the absent signal; ThresholdResolver's discarded argument is optional and documented as ignorable. The genuine bucket-(c) marker is a **required** input the adapter discards while the interface implies a capability — and only ChannelResolver (three required inputs, all discarded, silently) met it. The reconciliation instruction earned its keep: fixing from the remembered "several" would have over-fixed two honest disclosed stubs and failed the anti-over-fix criterion.

  2. **Fix choice.** For ChannelResolver I chose **mark not-yet-implemented**, not honour-the-inputs, because honouring is not viable: both `MessageChannel` and `ChannelType` carry exactly one value (`WHATSAPP`) and Phase 2-A has a single (operator) user, so there is genuinely nothing to resolve `user_id`/`message_intent` against. A real resolver is the deferred `UserScopedChannelResolverAdapter`, gated on the D136 Primitive 1 User aggregate plus a second channel — a whole session's work, correctly deferred per YAGNI (the YAGNI trigger). The marker took the codebase's own blessed form: explicit `del` of the discarded inputs (matching ThresholdResolver and ConfidenceCalculator) plus port and adapter docstrings that name the deferral and its trigger, converting a **silent** discard into a disclosed one (the No-silent-operation principle). One adjacent observation surfaced and was deliberately left unfixed: `send_message` ([contexts/messaging/application/send_message.py:96](send_message.py)) calls `resolve_channel` and discards the returned `ChannelDestination`, delivering on its `channel` kwarg instead — so the resolver is doubly hollow on the reactive path (inputs ignored by the adapter, output ignored by the caller). At Phase 2-A this is benign (identity routing, single channel); but at multi-channel activation `send_message` must consume the resolved destination. This is a distinct defect class (caller discards output, not adapter discards input) and an architectural question about where resolution should live — out of scope for a corrections session, recorded as a forward-carry below.

  3. **Residual strategic item.** Confirmed: the buyer-wording correction — drop "pluggable / no vendor lock-in," use "vendor swap is one adapter, not a rebuild" — is **not** executed here. C2 in the appended Corrections section records it as routed to strategic mode (the next PRFAQ / procurement-facing touch). The code is now honest (the interface no longer silently claims a capability it lacks); the procurement-facing language is not yet corrected, and that correction is a strategic-mode forward-carry, listed below.

methodology: First session in the entire log classified `corrective` — which directly answers the Ciborra audit's forward-carry item 4 (CFR computed to 0% because no session carried the corrective classification while the qualitative failure record was non-empty). The session is genuinely corrective: it remediates a shipped code over-claim (the S53/D144 ChannelResolver interface) plus two audit mis-readings. One methodology observation: the (b)-versus-(c) distinction that the reconciliation turned on — required-and-undocumented-discard (c) versus optional-or-forward-compat-disclosed-discard (b) — is a sharper test than "single adapter behind a Protocol," and is the operational read of the interface-versus-implementation discipline at the honesty register. Forwarded to the next phase audit as a candidate refinement, not promoted here (D47 reserves methodology.md writes to strategic mode).

- Forward-carry items (none executed here):
  1. **Buyer-wording register correction** → strategic mode / next PRFAQ touch. Drop "pluggable / no vendor lock-in"; use "vendor swap is one adapter, not a rebuild." Recorded in C2.
  2. **Per-principle fit-check on the four karma2 transplants** (C1's residual item) → next phase audit. Confirm each transplanted principle fits Padhanam's situation on its own merits rather than on having worked in karma2.
  3. **`send_message` discards the resolver's output** → tied to the multi-channel activation trigger (D136 Primitive 1 User aggregate + second channel). When the `UserScopedChannelResolverAdapter` lands, `send_message` must consume the resolved `ChannelDestination` rather than its `channel` kwarg. Benign at Phase 2-A (single channel); a real routing bug the moment a second channel exists.

```
metrics:
  classification: corrective
  brief_started: 2026-05-28
  session_started: 2026-05-28
  session_closed: 2026-05-28
  merged: 2026-05-28
  close_state: clean (three commits, two charter ahead of one code per AC 9; one bucket-(c) port fixed; 179 messaging unit tests pass; 36/36 import-linter contracts kept; two honest disclosed stubs left byte-unchanged per anti-over-fix AC 6)
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: charter/ciborra-phenomenological-audit.md (Corrections section C1+C2); charter/p12-audit-findings.md (Entry 16 live-count correction); contexts/messaging/application/ports/channel_resolver.py (not-yet-implemented marker); contexts/messaging/adapters/channel_resolver/static_config_channel_resolver_adapter.py (explicit del + docstring); tests/unit/contexts/messaging/adapters/test_static_config_channel_resolver_adapter.py (not-yet-implemented contract test); log/sessions.md (this entry)
  corrects: charter/ciborra-phenomenological-audit.md (sections 1 + 5); charter/p12-audit-findings.md (Entry 16); contexts/messaging ChannelResolver over-claim (shipped S53/D144)
  corrected_by:
```

## 2026-05-28 — [Methodology upgrade] Audit decode step + discipline prune rule + CFR honesty (standalone strategic-mode, charter-only)
roles: analyst, technical writer
mode: strategic (deliverable is a methodology.md revision). Reconciliation correction: the session prompt framed this as a "methodology session" without naming a mode; methodology.md writes are reserved to strategic-mode / audit-boundary promotion per D47 (build-mode sessions do not write to it directly), so this is strategic-mode work — audit-boundary promotion of the Ciborra audit's findings into standing disciplines.

A standalone methodology-upgrade session downstream of the Ciborra phenomenological audit, charter-only. HEAD at open: `59b5f55` (clean tree). It changes only `charter/methodology.md` (verified: `git diff --stat HEAD~3 HEAD` shows one file, +27/-2). It answers the audit's reflexive finding — the discipline-naming habit both teaches the method and hides its drift, and the method had a birth rule for disciplines but no death rule — with two paired mechanisms (a decode step to catch hidden drift, a prune rule to retire disciplines) plus the change-failure-rate honesty fix.

- Produced (three commits, all `charter/methodology.md`):
  - **Commit 1** (`bb0578f`, decode step): a `### Discipline decode step (standing phase-audit pass)` sub-section inserted under Phase lifecycle, with a forward-reference added to the phase-audit-outputs list. Two written-out questions (was it a codified improvisation? does naming it hide an unacknowledged drift?), the standing-not-optional and phenomenological-distinct-from-the-engineering-audit framing, the anti-ritual guard, and the reconcile-before-it-counts guard citing the corrections session's "several was one, every was false."
  - **Commit 2** (`b112a85`, prune rule): a `## Discipline retirement (the prune rule)` section immediately after the structural-promotion birth rule, quoting the birth rule ("first occurrence is implementation; second is suspicion; third is the lift to shared abstraction") and supplying the symmetric death rule — three retirement criteria plus an append-only retirement process.
  - **Commit 3** (`a... no: this commit`, CFR honesty, route (a)): the CFR metric and the "Failure modes observed" section reconciled as one ledger booked through the corrective-session classification; a cross-link added at the Failure-modes intro; the v4 version-log entry recording all three changes.

- CFR honesty route chosen: **(a)** — every Failure-modes entry is booked through a corrective-classified session (corrects/corrected_by populated) and vice versa, so the metric and the narrative record cannot drift apart. Chosen over (b) redefine-to-count-narrative-entries and (c) retire-the-metric because the corrective classification just had its first successful live use (the 2026-05-28 corrections session), so it demonstrably carries signal and CFR is no longer 0% — a metric with its first successful use is not a retirement candidate. Recorded as version-log **v4**, not a new D-entry: the measurement-model refinement sits within D40's existing commitment and the methodology document is the living-hypothesis surface (D113) revised by version-log entry rather than per-revision D-entry.

- Decisions: the CFR honesty route is the session's one decision, recorded as version-log v4 with reasoning rather than as a D-entry (convention reasoning above). No D-entry this session.

- Tests: none; charter-only. `git diff --stat HEAD~3 HEAD` confirms `charter/methodology.md` is the sole file touched (AC 8).

- Reflection prompts answered:

  1. **Cuts-both-ways test.** The mechanism that lets a discipline die is the prune rule's retirement process: a discipline meeting any of the three named criteria — (1) the situation it was built for no longer exists, (2) it is applied by reflex rather than because the situation needs it, (3) the decode step finds it masks a drift better addressed by naming the drift than by keeping the discipline — is recorded retired, append-only with its reasoning, the retired discipline left legible alongside why it was retired. It is at `## Discipline retirement (the prune rule)`, physically adjacent to the birth rule it mirrors. The diff adds both the catch mechanism (decode) and the release mechanism (prune), so the cuts-both-ways criterion holds; a decode-only diff would have tightened the box, which is exactly what the constraint forbids.

  2. **Ritual risk.** The wording that keeps the decode step genuine suspicion: *"a decode pass that clears every discipline without naming a single one as codified improvisation has not been run honestly — the same way an audit that finds nothing usually did not look hard enough."* Is it strong enough? It raises the floor — it makes a clean all-clear pass self-evidently dishonest, which prevents the laziest ritual (tick every box, find nothing). But it is not strong enough on its own to compel depth: a determined ritualist could name one trivial discipline as "codified improvisation" and satisfy the letter. The real defense is the pairing — the reconcile-before-it-counts guard forces any flagged drift to be grounded in code, and the operator's adversarial posture (inherited from the audit's "confirmation that did not look hard enough is a failure") carries the rest. Honest verdict: the clause is necessary and well-aimed but load-bears jointly with the reconcile guard and operator suspicion, not alone.

  3. **Reconciliation-guard test.** The wording: *"A decode finding stated as a quantifier ('several,' 'every,' 'all') without a reconciled count is not yet a finding — it is a hypothesis awaiting the code check."* Would it have stopped the Ciborra audit from writing "several stubs" and "every port single-adapter" as findings? Yes, for this exact failure class. The Ciborra audit stated both as findings on the strength of one cited adapter docstring (`ChannelResolver`) without inventorying every port. Under the guard, "several" and "every" are quantifier claims that cannot be recorded as findings until a reconciled count exists — so the audit would have had to run the per-port inventory first (the inventory the corrections session later ran, which returned one input-discarding stub and two already-multi-adapter ports). The guard would have demoted both claims to hypotheses and the inventory would have corrected them to "one" and "two ports already work" before they were ever written as findings. Caveat: the guard removes the legitimacy of an unreconciled quantifier finding but still relies on the auditor actually performing the reconciliation rather than labelling a guess "reconciled"; it fixes the structural permission, not the auditor's diligence.

  4. **Retroactive test — applying the decode step to the cultivation-to-control drift.** A real walk-through against the interface-versus-implementation discipline (methodology.md Patterns-observed, S49 promotion) and its companion build-at-second-instance discipline (D127). Decode Q1 (was it a codified improvisation?): partially — it was promoted from seven recurrences of a framing-drift *correction*, so it is a codified correction-pattern more than a codified improvisation; Q1 alone would not flag it. Decode Q2 (does naming it hide an unacknowledged drift?): this is the operative question and it is aimed squarely at the Care/Sorge finding. Phase 1's cultivation birth rule extracts at the *third* instance ("third is the lift"); Phase 2-A's build-at-second-instance discipline (D127) moves the threshold to the *second* and the interface-versus-implementation discipline installs ports upfront with one adapter — anticipatory design, not tend-when-forced. An auditor running Q2 *and cross-referencing the new discipline against the cultivation birth rule it quietly modifies* would see the threshold move from three to two and the shift from reactive extraction to anticipatory installation, and would surface "the method drifted from cultivation toward control and named the drift as discipline." So the decode step would *probably* surface it — but only if the auditor brings the cross-reference against the principle the discipline modifies; the wording invites that comparison ("does naming it hide a drift") but does not *compel* it. The cultivation→control drift is visible precisely as a moved threshold, and the decode step does not yet explicitly prompt "does this discipline move a threshold or supersede a principle the method already set, without naming the change as a drift?" Verdict: the decode step needs a small strengthening — add that threshold/supersession sub-check — to make this drift class catchable without depending on the auditor's initiative. Recorded as a forward-carry.

  5. **First prune candidate.** Writing the prune rule surfaced one genuine candidate for *examination* (not retirement): the **interface-versus-implementation / build-at-second-instance discipline pair**, under prune criterion 3 (the decode step finds it masks a drift better named than kept). The Ciborra audit already found these codify the cultivation→control shift without naming it as a drift; that is exactly criterion 3's trigger. The candidate is not "delete the discipline" — the upfront-port pattern is sound YAGNI (the corrections session's C2 affirmed it) — but "name the cultivation→control drift explicitly and decide whether the discipline survives the naming or is reframed." This is a next-phase-audit decode-pass action, not a retirement to perform now (out of scope). The CFR metric is explicitly **not** a candidate: it just had its first successful use. No discipline is retired this session; the mechanism is built and one candidate is forwarded.

methodology: This session is itself a methodology event worth noting — the first use of the new prune rule's candidate-identification half (surfacing the interface-versus-implementation pair as a criterion-3 candidate) happened in the same session that wrote the rule, which is mild evidence the rule has teeth rather than being decorative. Per D47 the methodology.md write is legitimate as strategic-mode audit-boundary promotion, not a build-mode edit.

- Forward-carry items (to the next phase audit; none executed here):
  1. **Decode-step strengthening (from prompt 4).** Add an explicit sub-check: "does this discipline move a threshold or supersede a principle the method already set, without naming the change as a drift?" The cultivation→control drift is catchable only via this cross-reference; the current wording invites but does not compel it.
  2. **First prune candidate (from prompt 5).** The interface-versus-implementation / build-at-second-instance discipline pair is a prune-criterion-3 candidate pending the cultivation→control drift being named. Examine at the next phase audit's decode pass; reframe-or-retain, do not delete reflexively.
  3. **Retroactive CFR reconciliation completeness.** The D52 session is the identifiable correcting session for the silently-deferred package drift; the fabrication-class and single-currency Phase-1 entries are marked pre-mechanism. A future audit may complete the retroactive mapping if the correcting sessions are identifiable.

```
metrics:
  classification: reframe
  brief_started: 2026-05-28
  session_started: 2026-05-28
  session_closed: 2026-05-28
  merged: 2026-05-28
  close_state: clean (three charter commits, methodology.md only per AC 8; decode step + prune rule + CFR honesty route (a) + v4 version log; both catch and release mechanisms present per the cuts-both-ways criterion)
  tests_passing: n/a
  principles_intact: yes
  charter_touchpoints: charter/methodology.md (discipline decode step under Phase lifecycle; discipline-retirement prune rule after the structural-promotion birth rule; change-failure-rate honesty fix at the metric definition + Failure-modes intro cross-link; v4 version-log entry); log/sessions.md (this entry)
  corrects:
  corrected_by:
```

## 2026-05-28 — [Prune-and-reframe] Build-at-second-instance retirement + two-threshold rule (standalone strategic-mode, charter-only, prune rule's first exercise)
roles: analyst, architect, technical writer
mode: strategic (deliverable is a methodology.md revision; methodology writes are reserved to strategic-mode / audit-boundary promotion per D47). Opened in strategic mode deliberately — session B opened as build and had to self-correct; not repeated here.

A standalone strategic-mode methodology session at the Phase-2-A boundary, the **prune rule's first exercise** (the rule was built in session B, commit `b112a85`). HEAD at open: `a2075dd` (clean tree). Charter-only: `git diff --stat HEAD~2 HEAD` shows `charter/methodology.md` as the sole file across both upgrade commits. No vendor specifics touched, so no vendor-documentation reconciliation was required.

- Code-evidence reconciliation (the reframe's entire justification — verified against the tree at HEAD `a2075dd`, confirming session A's inventory still holds):
  - *Early-build justified (second adapter structurally guaranteed, reached).* `MetaClassifier` → two real adapters: `RuleBasedMetaClassifierAdapter` ([contexts/messaging/adapters/rule_based_meta_classifier.py:50](rule_based_meta_classifier.py)), `LlmMetaClassifierAdapter` ([contexts/messaging/adapters/llm_meta_classifier.py:43](llm_meta_classifier.py)). `MessageDeliveryPort` → two real adapters: `LocalEchoMessageDeliveryAdapter` ([contexts/messaging/adapters/outbound/local_echo/local_echo_message_delivery.py:28](local_echo_message_delivery.py)), `TwilioMessageDeliveryAdapter` ([contexts/messaging/adapters/outbound/twilio/twilio_message_delivery.py:93](twilio_message_delivery.py)).
  - *Early-build a drift (second instance merely anticipated).* `ChannelResolver` → one adapter: `StaticConfigChannelResolverAdapter` ([contexts/messaging/adapters/channel_resolver/static_config_channel_resolver_adapter.py:31](static_config_channel_resolver_adapter.py)), built for a second channel that does not exist; became the input-discarding stub corrected in session A. The evidence holds; the reframe was safe to write.

- Produced (two commits, both `charter/methodology.md`):
  - **Commit 1** (`baa3cea`): the append-only retirement record under the prune rule — a new `### Retired disciplines` log, first entry retiring the loose build-at-second-instance form via criterion 3, with the code evidence above and the supersession pointer.
  - **Commit 2** (`<this commit>`): the `## Abstraction-threshold rule (domain and integration)` section superseding the loose form (domain: third, unchanged; integration: second-when-structurally-guaranteed, with the checkable tell); the line-415 interface-versus-implementation reference annotated as superseded so no second live abstraction-threshold discipline remains; the v5 version-log entry naming the Phase-1-to-2 refinement and recording that it dissolves the audit's Care-section finding.

- Reflection prompts answered:

  1. **Did a discipline actually die?** Yes — not a relabelling. What died: the *uniform early-build instinct* — the unconditional permission to commit an integration interface at the second instance, ship one adapter, and defer the rest, regardless of whether the second adapter was real. What survived: early-build under a *new precondition* the loose form did not have — the second adapter must be structurally guaranteed (already exists or committed this phase). The test for a real death is whether the new rule forbids something the old one permitted: it does. The loose form permitted ChannelResolver's early build; the two-threshold rule forbids it (no nameable real second adapter). A relabelling would permit exactly the same set of builds under a new name; this narrows the permitted set, so a discipline genuinely died and a narrower one took its place.

  2. **Checkable tell.** Quoted: *"Before building an integration interface early, name the second adapter and confirm it exists or is committed this phase. If you cannot name a real second adapter, the second instance is merely anticipated; wait. The tell is checkable, not a judgment call: a named, currently-existing-or-this-phase-committed second adapter, or no early build."* Walked against ChannelResolver: at S53 the only candidate second adapter was the `UserScopedChannelResolverAdapter` for a second channel that did not exist and was explicitly deferred (D136 Primitive 1 User aggregate + a second channel, Phase 2-B+) — so the builder could not name a second adapter that exists or is committed *this phase*, only a future-maybe. The tell returns "merely anticipated; wait." It would have stopped ChannelResolver's early build. It is strong enough because it is binary and evidence-bound (a name plus a commitment, or no build), not a reading of intent — the same failure (no real second instance) that produced the stub is exactly what the tell checks for.

  3. **Drift dissolved or relabelled?** Dissolved. The named justification the early-build now rests on: *integration boundary with a structurally-guaranteed second instance.* Before the reframe, MetaClassifier's and MessageDeliveryPort's early builds and ChannelResolver's early build were all permitted by the same unstated instinct — which is why the audit read uniform build-upfront as anticipatory control. After the reframe, the permitted early builds (MetaClassifier, MessageDeliveryPort) carry that named principle as their justification, and the unjustified one (ChannelResolver) is ruled out by the same principle. The drift was "early-build justified by nothing nameable, reading as control"; it is dissolved by giving the justified early-builds a name and forbidding the rest, not by renaming the old permission. The named justification is pointable: the integration-boundary bullet and the tell in the Abstraction-threshold rule.

  4. **Sub-check calibration (forward-carry).** This reframe is a worked instance of the threshold-supersession class that session B's Finding 1 decode sub-check ("does this discipline move a threshold or supersede a principle the method already set, without naming the change as a drift?") is meant to catch automatically. Recorded as the calibration case for that sub-check: the next phase audit, when it adds the sub-check (its mandatory opening move per B's forward-carry), can calibrate against this known case — a threshold that moved from three (domain) to two (integration) unnamed, read as drift, then named and split — rather than inventing a synthetic example. The sub-check itself is *not* added this session (out of scope; it is about the decode step's general future capability, not this specific reframe).

methodology: The prune rule's first exercise produced a genuine narrowing (uniform early-build permission died; conditioned early-build survived), which is first evidence the death rule has teeth rather than being decorative — paired with session B's observation that the rule surfaced its first candidate in the same session it was written. The reframe also demonstrates the decode→prune→reframe loop end to end: the Ciborra decode found the drift (Care section), the prune rule retired the discipline that masked it, and the two-threshold rule named the refinement that dissolves it. Per D47 the methodology.md write is legitimate as strategic-mode audit-boundary promotion.

- Decisions: the supersession (retire the loose build-at-second-instance form; install the two-threshold Abstraction-threshold rule) is the session's decision, recorded as version-log **v5**. No new D-entry — confirmed against the file: the methodology document is the living-hypothesis surface (D113) within D40's existing measurement-and-discipline commitment, revised by version-log entry rather than per-revision D-entry, matching session B's v4 handling.

- Tests: none; charter-only. `git diff --stat HEAD~2 HEAD` confirms `charter/methodology.md` is the sole file touched (AC 9).

- Forward-carry items (to the next phase audit; none executed here):
  1. **Decode sub-check calibration (from prompt 4).** This session is the worked example for session B's Finding 1 threshold-supersession sub-check. The next phase audit adds the sub-check as its opening move and calibrates it against this case.

```
metrics:
  classification: reframe
  brief_started: 2026-05-28
  session_started: 2026-05-28
  session_closed: 2026-05-28
  merged: 2026-05-28
  close_state: clean (two charter commits, methodology.md only per AC 9; loose build-at-second-instance retired via the prune rule's first exercise; two-threshold rule installed; loose form not left live; Phase-1-to-2 refinement named, dissolving the Ciborra Care-section finding)
  tests_passing: n/a
  principles_intact: yes
  charter_touchpoints: charter/methodology.md (Retired disciplines log + build-at-second-instance retirement record under the prune rule; Abstraction-threshold rule section; interface-versus-implementation line-415 supersession annotation; v5 version-log entry); log/sessions.md (this entry)
  corrects:
  corrected_by:
```

## 2026-05-28 — [Nango provisioning] Self-hosted Nango in the dev stack (standalone non-S-numbered infra session)
roles: analyst, engineer, technical writer
mode: build/infra session, no S-series substrate ID (provisions a cross-cutting dev-stack service the calendar build S55 and email build S56 both consume; not a P15 feature story, so non-S-numbered rather than renumbering the P15 sequence). D47's methodology-write restriction does not apply — no methodology.md touch.

A standalone infrastructure session at the Phase-2-A boundary, the precedent being the four standalone 2026-05-28 sessions. HEAD at open: `0512fb8` (clean tree). It stands up Nango self-hosted (free Auth+Proxy tier) as the tool-service substrate the calendar context consumes over HTTP per D14, gating S55 on an operator bring-up-and-verify. Standing up a vendor service from memory is the S4 Langfuse-miss shape, so the load-bearing work was hard reconciliation against the current Nango compose and Docker Hub before any edit. Three commits, no D-entry (reasoning below); `git diff --stat` across them shows `compose.yaml`, `.env.example`, and `docs/runbooks/nango-self-hosted.md` only — no domain code, no calendar context, no Nango HTTP adapter (those are S55).

- Produced:
  - **Commit `e3b7d9e`** (`chore(compose): add nango self-hosted to dev stack`): `nango-server` (`nangohq/nango-server:hosted-0.70.5@sha256:12f35eb4…`, `platform: linux/amd64`) plus bundled `nango-db` (`postgres:16-alpine@sha256:16bc17c6…`), both digests fetched from Docker Hub (not fabricated). Loopback-only host bindings for the dashboard (`SERVER_PORT` 3003) and Connect UI (`CONNECT_UI_PORT` 3009) per the S5-rule dev exception; Elasticsearch omitted (`NANGO_LOGS_ENABLED=false`, logs to stdout); Redis omitted (the free Auth+Proxy server config references no Redis env); a path-independent node TCP-connect healthcheck on `SERVER_PORT`. `docker compose config -q` exits 0.
  - **Commit `0c52272`** (`chore(env): document nango env vars and encryption key`): eleven `NANGO_*`/`SERVER_PORT`/`CONNECT_UI_PORT` keys in `.env.example`, keys only — `NANGO_ENCRYPTION_KEY` and `NANGO_DASHBOARD_PASSWORD` are `replace-me`; `openssl rand -base64 32` generation documented with the rotation-unsupported note; secrets gitignored via the pre-existing `.env`/`.env.*` rules (no `.gitignore` change needed).
  - **Commit `c474e01`** (`docs(runbook): nango bring-up and verify procedure`): `docs/runbooks/nango-self-hosted.md`, operator-executed: env/encryption-key setup, bring-up, health check, dashboard + Connect UI access, Google Calendar OAuth config including the `<NANGO_SERVER_URL>/oauth/callback` redirect to register in Google Cloud Console, test-connection establishment, and a Proxy `events.list` 200 that is the gate starting S55; port-gotcha and Apple-Silicon troubleshooting included.

- Decisions: none recorded as a D-entry (see reflection 4). The free-tier Auth+Proxy constraint S55 inherits is already in the charter (`charter/deferred-decisions.md:459` plus the 2026-05-28 calendar-retrieval-design captures entry), so no charter edit was required to record it — which also kept the change set within the infra-only file scope.

- Tests: `docker compose config -q` exits 0 (AC5); the rendered config shows `nango-server` digest-pinned with `SERVER_PORT: "3003"` and a healthy `nango-db` dependency. No unit tests (infra). Live verification — bring-up, the OAuth connection, and the Proxy call — is operator-executed via the runbook per the S53/S54 procedural-then-executed precedent; the build environment cannot reach docker, the public internet for OAuth, or Google.

- Reflection prompts answered:

  1. **Reconciliation drift (the S4 check).** Re-verification against the upstream `NangoHQ/nango` master compose and Docker Hub genuinely differed from the prompt's baseline in five specific ways, so the check was real rather than a rubber-stamp. (a) **Image version drifted**: the baseline named `hosted-0.69.48`; Docker Hub's current build is `hosted-0.70.5`, published 2026-05-28 (the same day), digest `12f35eb4…` — a minor-release drift in the window since draft. (b) **Bundled Postgres is pg16, not unspecified**: the upstream compose pins `postgres:16.0-alpine`, so I used `postgres:16-alpine` (latest 16.x patch, digest `16bc17c6…`) rather than the project's pg17 — symmetric to how `langfuse-db` runs the Postgres version Langfuse documents. (c) **The Connect-UI port env var name in the baseline was wrong**: the baseline said `NANGO_CONNECT_UI_PORT`; the upstream compose uses `CONNECT_UI_PORT` (default 3009). (d) **`NANGO_PUBLIC_SERVER_URL` upstream default is `http://localhost:3000`**, not 3003; I set it to 3003 to match `SERVER_PORT`. (e) **A `nango-redis` service exists upstream** that the baseline's service set omitted — and on inspection `nango-server`'s env block references no Redis at all, so I omitted Redis (it serves Enterprise sync/orchestration, out of free-tier scope) rather than copy it in. Conversely, the baseline's `TELEMETRY=false` and `NANGO_PUBLIC_CONNECT_URL` are absent from the current upstream compose; I kept `TELEMETRY=false` belt-and-suspenders for the project's telemetry-off posture and dropped `NANGO_PUBLIC_CONNECT_URL`. The one structural fact the baseline got exactly right was the port gotcha (#5305), confirmed against the live GitHub issue.

  2. **Port resolution.** The published image bakes `PORT=8080` and resolves `SERVER_PORT → PORT → 3003`, so leaving `SERVER_PORT` unset (as the upstream compose's bare `- SERVER_PORT` passthrough does) makes the server listen on 8080 while only the published port is exposed — unreachable, which is issue #5305 exactly. Resolved by setting `SERVER_PORT: ${SERVER_PORT:-3003}` in the container env so the listen port is forced to match, and mapping `127.0.0.1:${SERVER_PORT:-3003}:${SERVER_PORT:-3003}` on both sides. The compose healthcheck is a node `net.connect` to `SERVER_PORT` — path-independent and using the app's own runtime (guaranteed on PATH, unlike `wget`), and it tests precisely the failure #5305 produces (server not listening on the published port). The runbook lets the operator confirm reachability two ways: `docker compose ps nango-server` showing `healthy`, then `curl -i http://localhost:3003/health` expecting `200`; the troubleshooting section names connection-refused-on-3003 as the port-gotcha signature and tells the operator to confirm `SERVER_PORT: "3003"` in `docker compose config`.

  3. **Free-tier constraint.** Confirmed: the calendar plan is Auth + Proxy only, no Syncs or Functions. The P15 epic commits the calendar context to "consuming Nango self-hosted via HTTP adapter" with "pull-on-demand sync verified end-to-end" (`charter/packages/p15-epic.md:9,65`), and the 2026-05-28 calendar-retrieval-design captures entry is explicit that Padhanam orchestrates the sync-token incremental loop itself through Proxy and does not use Nango's paid Syncs. Pull-on-demand = on-demand Proxy calls, which free self-hosting supports. No blocker. If a future surface genuinely needs background Syncs or Functions (e.g. webhook-driven push, which the calendar design already defers), the options are Enterprise self-hosted, Nango Cloud, or staying on pull-on-demand Proxy — a tier decision to surface in strategic mode, not a config tweak here.

  4. **D-entry decision.** Not warranted; covered by D14 plus the existing deferred-decisions entries. D14 commits the separate-service / tools-as-configuration pattern, and `charter/deferred-decisions.md:459` already records the Path B self-hosted-Nango choice and *explicitly schedules* the protocol/auth/scope D-entry for S55 "with the live integration verification." This session's decisions — the image pin, the bundled pg16 dev DB (production swaps external per local-first), the #5305 port resolution, the loopback binding, the ES/Redis omission — are implementation of that already-committed pattern, not selection between architectural alternatives, which is the D-entry bar per the charter. Writing a D-entry now would also fragment or pre-empt the S55 entry the charter has already reserved. The free-tier scope that does bound future work is recorded already (deferred-decisions + the calendar captures entry), so nothing about the constraint needed a new charter write.

methodology: The reconciliation discipline (the standing "verify vendor specifics before code lands" pre-write surface) earned its keep concretely this session — five real differences from the from-memory baseline, including a same-day image-version bump and a wrong env-var name, exactly the class of topology bug the discipline exists to catch. Worth noting as a data point that the discipline's value shows up even when the baseline is recent and detailed: "drafted from memory/recent notes" still drifted within days.

- Forward-carry items (none executed here):
  1. **Operator bring-up-and-verify gate (starts S55).** Run `docs/runbooks/nango-self-hosted.md` end to end; a green stage 6 — an active Google Calendar connection plus a `200` from the Proxy `events.list` call — is the gate. Record the integration id and connection id; the S55 brief references them.
  2. **Free-tier Auth+Proxy constraint S55 inherits** (already charter-recorded): the calendar HTTP adapter is Proxy pull-on-demand; no Syncs/Functions.
  3. **The S55 protocol/auth/scope D-entry** (`charter/deferred-decisions.md:459`) lands at S55 with the live integration verification — not this session.

```
metrics:
  classification: infra
  brief_started: 2026-05-28
  session_started: 2026-05-28
  session_closed: 2026-05-28
  merged: 2026-05-28
  close_state: clean (three commits — compose, env, runbook; no D-entry per D14 + deferred-decisions coverage; docker compose config -q exits 0; live verification operator-gated via the runbook)
  tests_passing: n/a (compose config lint passes; no unit tests in an infra session)
  principles_intact: yes
  charter_touchpoints: compose.yaml (nango-db + nango-server services + nango_db_data volume); .env.example (NANGO_* keys + encryption-key generation note); docs/runbooks/nango-self-hosted.md (operator bring-up-and-verify runbook); log/sessions.md (this entry). No D-entry (D14 separate-service pattern + charter/deferred-decisions.md:459 cover the deployment shape; the protocol/auth/scope D-entry is reserved for S55)
  corrects:
  corrected_by:
```

### Verification addendum — green gate confirmed (operator-executed)

Stage-6 verification passed on the operator's machine: Auth plus Proxy working end-to-end against real Google Calendar through self-hosted Nango. Nango Proxy pulled live calendar data (calendar operator@example.com, Europe/London; one upcoming event returned). The read-only scope assumption the calendar design rests on is now verified, not pending: the granted scope is calendar.readonly.

Verified handles (the S55 calendar adapter references these):
- environment: dev (Nango env id 2)
- integration unique key (Provider-Config-Key): google-calendar
- connection id: d46195b2-ad85-4d1c-a876-b978b9347ccd
- proxy base path: /proxy/calendar/v3/... maps to https://www.googleapis.com/calendar/v3/...
- granted scope: calendar.readonly

Operational cautions (preserved because they cause baffling failures later if forgotten):
- The connection persists in the nango_db_data volume across docker compose down. Stopping the stack does not lose the connection; only removing the volume does.
- Do not change NANGO_ENCRYPTION_KEY after a connection is stored. The stored token is encrypted with it; changing the key makes the token undecryptable and forces a re-grant.

Status: Nango provisioning complete and verified. S55 (calendar build) is unblocked.

## 2026-05-28 — [S55a] Calendar data substrate (first of the S55 split)
roles: engineer, architect, analyst, technical writer
mode: build (new bounded context + one shared-kernel discriminator + D-entry + tests)

S55a, the first of the two-way split of S55, builds the calendar data substrate: pull calendar events through self-hosted Nango's Proxy (the five verified handles from the provisioning session), store each as a Meeting artefact keyed on the stable Google event id (a mutable search cache — deltas upsert modified events and tombstone cancelled ones), keep it fresh with self-driven incremental sync tokens, and index it into the inherited hybrid-retrieval substrate. HEAD at open `a412da5` (clean tree). The conversation surface, the MetaClassifier extension, the gold sets, response composition, and the citation-time audit-snapshot wiring are S55b. **The calendar data substrate is operational end-to-end at the unit/contract altitude behind the calendar port, no Google/Nango specifics in domain code; the live pull-store round-trip is operator-pending per the procedural smoke.** S56 (email) follows symmetric to calendar; S57 closes P15.

- Vendor reconciliation (the S4 safeguard): one substantive finding. Google `events.list` makes `syncToken` mutually exclusive with `q`, `timeMin`, `timeMax`, `orderBy`, and `updatedMin` (400 if mixed). The 2026-05-28 calendar-retrieval-design capture framed pull-time scoping (time bounds + free-text `q`) and incremental sync (syncToken) as if both apply continuously; they cannot. Built shape: the initial full sync carries `timeMin`/`timeMax` (RFC3339 with offset) + `singleEvents=true` and returns `nextSyncToken`; incremental syncs pass `syncToken` only (plus pagination + a consistent `singleEvents`); 410 GONE → clear token + full resync. The sync path never sends `q` (search runs locally over the substrate, so dropping it costs nothing and avoids the 400). Consequence for the window knob: `timeMin` cannot be slid forward on an incremental pull — sliding the window means a fresh full sync that re-anchors it and discards the old token. Confirmed as assumed: cancelled events carry `status:"cancelled"` (always present in incremental results); `timeMin`/`timeMax` need a mandatory offset; `singleEvents=true` expands recurring events into individually-keyed instances (the clean model for an event-id-keyed store). The five verified handles confirmed against the provisioning addendum; the adapter's request shape matches the runbook's verified Proxy call (three headers: Authorization Bearer, Provider-Config-Key, Connection-Id).

- Substrate-inheritance survey result: reuse (capability inheritance without pipeline reuse). Meeting **inherits** the embedding capability (ingestion's `ChunkEmbedderPort`) and the graph store (ingestion's `GraphRepositoryPort`), reached through calendar's own thin consumer ports (`MeetingEmbeddingPort`, `MeetingGraphIndexPort`) bridged at the apps/ composition root to ingestion's adapters per the D146 daily-briefing precedent — calendar never imports ingestion internals (D16/D17/D28). Meeting **does not inherit** `SourceRepositoryPort` + the parse→embed→extract state machine (Meeting has no raw bytes, is keyed on the external event id, upserts/tombstones on deltas — the Source pipeline would distort it), `parser_port` (no parsing; the trivial Meeting→text synthesis replaces it), or `entity_extractor_port` (Meeting's entities are structured — organizer/attendees→Person, location→Place — mapped directly; LLM extraction over the description is a deferred enhancement). Calendar **builds** the meetings table keyed on `google_event_id` with a `vector(768)` column, the synthesis, the structured graph mapping, and pgvector search over its own table — table-ownership plus a query in the inherited embedding space, not a second substrate.

- Produced: Eight commits. (1) `docs(decisions): D148 calendar protocol/auth/scope + substrate-inheritance survey result` (c19c6e1) — D148 summary + full entry at decisions.md; deferred-decisions "Calendar tool service" status closed by D148. (2) `feat(calendar): bounded context skeleton + Connection model (opaque Nango ref)` (4355f13) — contexts/calendar/ (domain/ports/application/adapters); Connection value object (opaque provider_connection_ref); layers-calendar contract + three independence additions + domain-purity; 6 tests. (3) `feat(calendar): nango proxy adapter wired to verified handles (single adapter)` (c612c12) — CalendarEventSourcePort; NangoProxyCalendarAdapter (full/incremental separate methods; 410/auth/5xx mapping); CalendarEvent/EventAttendee/CalendarEventPage domain; errors; CalendarSettings + .env.example consumer keys; 13 tests via httpx MockTransport. (4) `feat(calendar): Meeting artefact + tenant store keyed on event id (upsert/tombstone)` (22accae) — Meeting domain + CalendarEvent→Meeting mapping + synthesis/content-hash; MeetingRepository/MeetingReader/ConnectionRepository ports; PostgresMeetingStore (envelope-encrypted content; upsert/tombstone/set_embedding) + PostgresConnectionRepository; Alembic 0026 (connections + meetings tables; vector(768)); schema.md in the same commit; 24 tests. (5) `feat(calendar): pull-store-sync pipeline ... as trigger-agnostic function` (4c6f8b1) — CalendarSyncTrigger value; NoSuchConnectionError; sync_calendar function (full/incremental/410-resync, content-change detection, id reuse); 6 pipeline tests with in-memory fakes. (6) `feat(calendar): index Meeting into retrieval substrate per survey` (6bbbad3) — MeetingEmbeddingPort + MeetingGraphIndexPort consumer ports; Meeting→graph mapping; index_meeting; sync_calendar indexing step (optional ports); 11 indexing tests. (7) `feat(shared-kernel): Meeting discriminator on ArtefactCitation` (616db9a) — KNOWN_ARTEFACT_TYPES closed union (case/data_point/meeting); membership validation; 2 discriminator tests. (8) `docs(p15/s55a): calendar substrate smoke + tenant-isolation contract + session log + close` (this commit) — procedural smoke at docs/smoke/p15_s55a_calendar_substrate.md; calendar tenant-isolation contract test; this entry; current-package + p15-epic close markers.

- Decisions: D148 (calendar integration contract — Nango Proxy pull-on-demand via the five verified handles, `calendar.readonly`, separate-service per D14, Meeting as event-id-keyed mutable cache with the two-store evidence split, the honest replaceability claim, and the recorded substrate-inheritance survey result, vendor-reconciliation finding, tombstone-retention default, and latency floor). Committed at commit 1. Tombstone-retention default decided: retain the row keyed on the event id marked CANCELLED, purge content (the five enc_* columns) + content_hash + embedding, set cancelled_at — cancelled events leave search while the row is retained so a re-appearing event id is recognised; the immutable audit snapshot is the evidence record (consistent with originals-never-erased, since the original is the snapshot, not the cache).

- Tests: 2052 unit + contract tests pass at S55a close (0 failures; +40 calendar/discriminator tests over the S54 baseline of 2023); 12 skipped. 37 of 37 import-linter contracts kept (+1 layers-calendar). AST enforcement green (no-getenv, no-vendor-in-domain, no-plaintext-in-state, no-raw-neo4j-session). Enforcement layer added: the `layers-calendar` hexagonal contract plus calendar's entries in the three cross-context independence contracts and domain-purity guarantee the calendar context's layering and cross-context independence (no Google/Nango or ingestion imports leak into calendar domain/application); the calendar tenant-isolation contract test (`tests/contract/tenant_isolation/test_calendar_isolation.py`) guarantees the meeting store and connection repository reject cross-tenant TenantContexts at the bound-tenant guard (D24). New tests by surface: Connection (6); Nango adapter request-shape + mapping + error mapping via httpx MockTransport (13); Meeting domain + mapping + synthesis (5); store codec encrypt/decrypt round-trip + cross-tenant AAD rejection + row mapping + bound-tenant guard (7); pull-store-sync pipeline full/incremental/410/tombstone/change-detection (6); graph mapping + index_meeting + pipeline indexing step (11); ArtefactCitation meeting discriminator (2); calendar tenant-isolation contract (3).

- Reflection prompts answered:

  1. **Survey honesty (reuse vs parallel).** The survey landed on reuse, and the line is principled enough to survive the next phase audit's scrutiny: inherit *capabilities/substrates* (the embedding model + dimensions via ChunkEmbedderPort; the Neo4j graph store via GraphRepositoryPort), own *storage shaped to the artefact's nature* (the event-id-keyed, upsert/tombstone, mutable-cache meetings table). The discipline's actual test — do not re-implement embedding or graph indexing — is met: calendar re-implements neither. What calendar builds (a table with a vector column, a SQL similarity query, a synthesis function, a structured field→entity mapping) is what every context does; pgvector is a column type and Neo4j is a shared store, not a per-context substrate. The one place this could be read as parallel is the pgvector search over the meetings table rather than the chunks table — but forcing Meeting into the chunks table would require the full Source pipeline (raw bytes, parse/embed/extract state machine, claim workers) and would distort the event-id-keyed mutable-cache semantics, which is itself the parallel-substrate-in-reverse the survey rejects (alternative a in D148). The honest reading is reuse with calendar-owned storage, not a parallel substrate wearing a justification.

  2. **Two-threshold rule, second exercise — held without prompting.** The Nango adapter stayed a single adapter behind a hexagonal port (`CalendarEventSourcePort`), with no stubbed second provider and no separate vendor-abstraction Protocol; the port exists only because the application cannot import adapters (hexagonal layering), and replaceability is secured by the Connection model + self-hosting, not by a premature second adapter. The ingest stayed a function (`sync_calendar`) taking its trigger context (`CalendarSyncTrigger`) as a plain parameter with one poll caller; push is deferred and merely anticipated, so the rule's tell returned *wait* — no trigger Protocol with a poll adapter and a stubbed webhook adapter (which would repeat the ChannelResolver mistake one phase after the v5 prune retired the loose form). One nuance worth recording for the rule's own clarity: a hexagonal *port* with one adapter is not the thing the rule forbids — the rule forbids a *second adapter* or a *vendor-abstraction Protocol* justified only by an anticipated second instance. The port-for-layering vs abstraction-for-anticipated-vendor distinction is the operative line, and it held cleanly here. This is the v5 rule's second real exercise (after the build-at-second-instance reframe) and it held without the brief having to force it.

  3. **Vendor reconciliation.** The specific difference: the calendar-retrieval-design capture (and a from-memory reading) treats `q`/`timeMin`/`timeMax` and `syncToken` as co-applicable across the sync lifecycle. The current Google docs are explicit that `syncToken` is mutually exclusive with `q`, `timeMin`, `timeMax`, `orderBy`, and `updatedMin` (400 if combined). This reshapes the "how the window slides" knob the brief deferred: the window is fixed at full-sync time and re-anchored only by a fresh full sync, never by a moved `timeMin` on an incremental pull. The adapter encodes the constraint structurally as two methods (full vs incremental) rather than one method with a flag, so a caller cannot accidentally produce the 400. Had this been built from memory, the incremental path would have re-sent `timeMin` and silently 400'd at the first delta pull — exactly the S4 Langfuse-class topology bug the reconciliation exists to catch.

  4. **Latency floor.** The pull function's external round-trip is local → Nango (localhost, sub-millisecond) → Google `events.list` (public internet) → back; the Nango hop is negligible, so the load-bearing floor is the Google `events.list` round-trip, documented at ~150–400ms typical (no operator-measured number yet — the build environment cannot reach Nango/Google; the procedural smoke's Stage 1 instructs the operator to wrap the verified pull in `time.perf_counter()` and record the measured number). S55b's refresh-before-answer tiering budgets time-sensitive calendar turns against this floor relative to the D122 latency-tier routing primitive: a turn that must reflect the latest calendar state pays one delta round-trip (cheap in payload, but the floor is round-trip-bound, not payload-bound), so the tiering is a freshness-versus-latency trade against the measured number, not a guess. Forward-carry: S55b records the operator-measured floor and sets the refresh-before-answer threshold against it.

  5. **Build-time scope refinement vs the brief's AC-13 touch set.** The brief's AC 13 idealizes the touch set as "calendar context + the one shared-kernel discriminator + the D-entry + tests." Build surfaced four necessary additions, each defensible per the framing-names-options/build-commits-with-refinements discipline: `padhanam/config/calendar.py` + `.env.example` (the adapter must read the Nango base URL + secret through config, never os.getenv — D19); `.importlinter` (every new bounded context registers its layering + independence + purity contracts); `charter/schema.md` (the migration's same-commit schema touch per the charter discipline); and `charter/deferred-decisions.md` (the D-entry's status close). The apps/ wiring bridge for the inherited indexing ports and the operator sync entry point were deferred to S55b — the consumer that drives sync from a user turn and wires the cross-context bridge at the composition root — which keeps S55a's code touch to the calendar context + the one discriminator and matches the brief's own scoping (the conversation surface is S55b). The cost: the survey's reuse claim is proven at the seam-and-fake altitude in S55a (consumer ports + index_meeting + the pipeline step, all tested with fakes) and wired to live ingestion adapters at S55b.

methodology: Four methodology surfaces exercised at S55a. (1) The two-threshold Abstraction-threshold rule (v5) — its second real exercise; held without prompting at both the Nango-adapter (single adapter behind a layering port) and the ingest (function with a trigger parameter, not a trigger Protocol) decisions; the port-for-layering vs abstraction-for-anticipated-vendor distinction is the operative clarification. (2) Pre-write reconciliation against vendor docs (the S4 safeguard) — caught the syncToken/timeMin-q exclusivity, which a from-memory build would have shipped as a 400 on the first delta pull; encoded structurally as two adapter methods. (3) The substrate-inheritance survey — second build-altitude application (after S54's three-category brief-altitude evidence); landed on reuse with a principled inherit-capability/own-storage line. (4) The path-naming / build-time-refinement discipline — the AC-13 idealized touch set vs the four necessary additions, recorded as refinements rather than fought into uniformity. No new methodology candidate proposed for promotion: each surface is existing methodology applied correctly, which is below the captures candidate-promotion threshold (structural novelty or 3x recurrence).

```
metrics:
  classification: build session (calendar data substrate; first of the S55 split; P15 progresses)
  brief_started: 2026-05-28
  session_started: 2026-05-28
  session_closed: 2026-05-28
  merged: 2026-05-28
  close_state: clean (eight-commit calendar data substrate landed end-to-end behind the calendar port; D148; Nango Proxy single adapter; Meeting event-id-keyed mutable-cache store with P3 encryption; trigger-agnostic sync pipeline with 410 resync; inherited embedding+graph indexing per the survey; Meeting discriminator on ArtefactCitation; 2052 unit+contract tests pass, 0 failures; 37/37 import-linter contracts kept; one vendor-reconciliation finding absorbed; smoke procedural pending live operator execution)
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: charter/decisions.md (D148 summary + full entry); charter/deferred-decisions.md ("Calendar tool service" status closed by D148); charter/schema.md (connections + meetings tables; Meeting/Connection domain shapes); charter/current-package.md (S55a close + S55b next marker); charter/packages/p15-epic.md (S55a closing marker on the S55 line); log/sessions.md (this entry); contexts/calendar/ (new bounded context: domain {connection, calendar_event, meeting, meeting_graph, sync_trigger, errors}; ports {calendar_event_source_port, meeting_repository, connection_repository, meeting_index_ports}; application {sync_calendar, index_meeting}; adapters/outbound {nango/nango_proxy_calendar_adapter, postgres/_tables + meeting_store + connection_repository}); shared_kernel/conversation_flow.py (KNOWN_ARTEFACT_TYPES + meeting discriminator); padhanam/config/calendar.py + __init__.py (CalendarSettings); .env.example (NANGO_BASE_URL + NANGO_SECRET_KEY consumer keys); .importlinter (layers-calendar + three independence + domain-purity additions); alembic/tenant/versions/2026_05_28_0026_calendar_substrate.py (new); tests/unit/contexts/calendar/{test_connection,test_nango_proxy_calendar_adapter,test_meeting,test_meeting_store,test_sync_calendar,test_indexing}.py; tests/unit/shared_kernel/test_conversation_flow.py (2 discriminator tests); tests/contract/tenant_isolation/test_calendar_isolation.py (new); docs/smoke/p15_s55a_calendar_substrate.md (new procedural smoke). NOT touched (deferred to S55b): apps/ wiring bridge for the inherited indexing ports; the operator sync entry point.
  corrects:
  corrected_by:
```

## 2026-06-02 — [S55a-fix] Calendar sync: defer incremental to scoped full-pull
roles: analyst, architect, engineer, technical writer
mode: build session (corrective substrate session on the closed S55a calendar data substrate, landing before S55b). HEAD at open: `889ab29` (S55a close) plus this session's pre-existing `86421e5` (Nango proxy env passthrough committed earlier in the conversation, ahead of the smoke). D149 supersedes D148's receive-side sync clause only; the methodology.md write-restriction (D47) does not apply — no methodology.md touch.

A corrective session opened by a finding the S55a Stage-1 smoke surfaced live: the operator ran the smoke against the running self-hosted Nango and their real Google Calendar, and the pull returned 3 events at 376 ms but **`next_sync_token present: False`** — against the smoke's expectation of `True`. Rather than power through to Stages 2–3, the divergence was treated as a substrate finding and confirmed empirically with an A/B against the same Proxy before any conclusion: the bounded request the adapter builds (`timeMin`+`timeMax`+`orderBy=startTime`) returned 3 events and no token; an unbounded request returned 35 events and a token. The finding falsifies D148's assumption that the bounded initial sync returns `nextSyncToken`, which means the self-driven-token + 410-resync design D148 built can never bootstrap incremental sync — `connections.sync_token` never populates, and incremental sync is unreachable in practice. The operator framed the resolution (option 4): defer incremental to a scoped full pull per refresh, keeping the incremental machinery dormant behind the port with a named activation trigger, rather than ripping it out or forcing an unbounded full sync that would break D148's scoped-pull intent.

- Pre-write reconciliation (the S4 Langfuse safeguard, against current Google Calendar API docs + live Nango):
  - **Token-suppression param set.** The `events.list` reference confirms `syncToken` is mutually exclusive (400) with `timeMin`, `timeMax`, `orderBy`, `q`, `updatedMin`, `iCalUID`, and the extended-property params. The *receive-side* suppression (a bounded/ordered request returning no `nextSyncToken`) is not stated explicitly in the docs but is decisively established by the live A/B; the decision rests on the measured contract, framed honestly in D149 as "measured."
  - **Deletion mechanism.** `showDeleted=true` is documented to return cancelled events with `status="cancelled"`, is not prohibited with `timeMin`/`timeMax`, and (with `singleEvents=true`) surfaces cancelled instances. Live request returned HTTP 200. → **`showDeleted=true` selected**; the full pull tombstones via the existing `status=cancelled` store path with no new deletion code. Set-diff over the window was the documented fallback, not needed.
  - **Pagination.** `sync_calendar._drain_full` already collects every page into one list before the store/tombstone loop runs, so a tombstone pass never sees a partial window; with `showDeleted` there is no set-diff, so a partial pull cannot false-delete in any case. Pinned by a new multi-page test.
  - **Nango auth.** Adapter sends `Authorization: Bearer` ([nango_proxy_calendar_adapter.py:121](../contexts/calendar/adapters/outbound/nango/nango_proxy_calendar_adapter.py)); live Stage-1 confirms it; Nango 0.70.5 rejects HTTP Basic with a misleading `not a UUID v4` error. No Basic path remains.
  - **Brief path drift.** The brief referenced `charter/captures.md`; the file lives at `log/captures.md`. Used the real path.

- Produced (five commits; charter ahead of code):
  - **Commit 1 (`63faa53`)** `docs(charter): D149 ...` — D149 (index summary line + full entry) superseding D148's receive-side sync clause only; the three-point `log/captures.md` 2026-06-02 [S55a-fix] entry (nextSyncToken suppression; MockTransport blind-spot; Nango Bearer gotcha); the S55a-fix close marker in `charter/current-package.md` (above the unchanged S55a marker); the S55 line update in `charter/packages/p15-epic.md`.
  - **Commit 2 (`86421e5`, pre-existing this session)** `build(p15): wire Nango proxy env into padhanam-api + advance image pin` — committed earlier in the conversation ahead of the smoke so the smoke evidence points at a committed digest. Listed here for the commit record; not re-committed.
  - **Commit 3 (`259763d`)** `fix(p15/s55a): defer incremental to scoped full-pull; full-pull deletion detection` — `sync_calendar` active path is now a scoped full pull on every refresh (no token read/written, absent token expected), carrying `show_deleted=True`; `CalendarSyncResult` drops the now-constant `next_sync_token`/`did_full_resync_after_410` fields; port + adapter gain the `show_deleted` parameter and `showDeleted` query param; `list_events_incremental` + the 410 mapping + `connections.sync_token` remain dormant. sync_calendar unit tests rewritten for the full-pull path (no-op re-pull, content-change upsert, cancelled-event tombstone via full pull, absent-token-no-raise, multi-page drain-before-tombstone).
  - **Commit 4 (`80f52fc`)** `test(p15/s55a): pin adapter Bearer auth; assert showDeleted; mark dormant 410/incremental` — dedicated `test_proxy_auth_is_bearer_not_basic`; `showDeleted=true` asserted on the full sync; incremental + 410 tests annotated as the dormant path; module docstring records the MockTransport emit-side limit.
  - **Commit 5 (`990eec8`)** `fix(p15): sync host-port-binding allowlist with compose (nango-server + padhanam-api carryover)` — see the enforcement-carryover finding below; restores `tests/_enforcement/test_host_port_bindings.py` to green.
  - **Commit 6 (`<this commit>`)** `docs(p15/s55a): revise calendar smoke + captures enforcement-drift note + session log + close` — the revised smoke (Stage 1 scoped full pull asserting *no* token + re-confirmed latency; Stage 2 refresh idempotency + cancellation tombstone; bogus-token→410 demoted to the dormant-path unit test; Stages 3–4 carried forward), the captures enforcement-drift note, plus this entry.

- Enforcement-carryover finding (surfaced during the AC12 close run, fixed this session). `make lint`'s host-port-binding enforcement (`tests/_enforcement/test_host_port_bindings.py`) was **already red at S55a close (`889ab29`)** — and had been since `7af8e88`. Two loopback dev bindings were added to `compose.yaml` without the same-commit allowlist sync the test's own docstring mandates: `padhanam-api` `127.0.0.1:8000:8000` (`7af8e88`, webhook smoke) and `nango-server`'s `3003`/`3009` (the Nango provisioning session `e3b7d9e`). The failure was **masked by stale `.pyc` bytecode** carrying the repo's former path (`/Users/sabu/Zephyr`, pre-rename) — clearing `__pycache__` was required to see the true `extras` set; the first run conflated the two services. Prior sessions' "AST enforcement green" claims did not hold for this contract. Both bindings are legitimate loopback-only `per S5` dev exceptions (same class as `postgres-control-plane`), so the correct action is allowlisting both with inline reasons (done), not removing them. Recorded in `log/captures.md` (2026-06-02 [S55a-fix] enforcement-drift) as a project-tooling finding: stale bytecode can mask enforcement-layer drift, and `make lint` should be run from a clean `__pycache__` (or with `-p no:cacheprovider` / a clean-bytecode step) at close. This is not a D-entry (vendor/tooling hygiene, not an architectural decision); the recurrence test is whether a third allowlist-sync miss surfaces.

- Decisions: D149 (`charter/decisions.md`). No methodology.md write (build mode, D47). The supersession is scoped to D148's receive-side sync clause; D148's auth/scope/Meeting model/encryption/substrate-inheritance stand.

- Tests: calendar unit tests pass (the new Bearer pin and multi-page drain test added, net of the removed active-path incremental/410 sync_calendar tests). Close run: **2062 passed, 12 skipped, 0 failed** across `tests/unit` + `tests/contract` + `tests/_enforcement`; import-linter **37/37 contracts kept**; AST enforcement green after the host-port-binding allowlist carryover fix (see finding below). The three known docker-dependent environmental failures (`test_lvt_round_trip`, `test_tool_repository`, `test_p3_full_slice`) are not in the unit+contract+enforcement scope and carry forward unchanged; S55a-fix introduces zero regressions.

- Reflection prompts answered:

  1. **Two-threshold recurrence.** S55a built the incremental sync machinery ahead of need, against the v5 two-threshold rule D148 had invoked twice on itself (one Nango adapter, not a stubbed second; a trigger function, not a trigger Protocol). The incremental path was the one place the discipline did not bite — and the reason is instructive: incremental sync was not an *anticipated second adapter* the rule screens for, it was an assumed *capability of the one adapter*, justified by a docstring reconciliation that checked the send side and missed the receive side. The two-threshold rule guards against premature second *implementations*; it does not guard against a single implementation built on an unverified vendor-contract assumption. That is a distinct gap, and the lesson it promotes is a **live-contract reconciliation gate**: framing-altitude vendor-API assumptions that are load-bearing (here, "the full sync returns a token that bootstraps everything downstream") need confirmation against the live vendor, not only against the docs at adapter-build time — because the docs stated the send-side rule and left the receive-side behaviour to be discovered empirically. This is the same shape as S4's Langfuse vendor-version drift; it is now at a second distinct instance (S4 trace-UI, S55a sync-token), and the promotion question (does the live-contract gate become a `charter/methodology.md` entry, distinct from pre-write reconciliation which is a *code*-reconciliation gate) carries to the next phase audit. Notably the discipline worked at the *response* level: the smoke was the live gate, and it fired exactly as designed — the finding is evidence the procedural-smoke-as-success-criterion discipline earns its place, not evidence it failed.

  2. **MockTransport blind-spot.** The 13 `httpx.MockTransport` unit tests could not catch the miss, and the reason is structural rather than an authoring oversight. A MockTransport test installs a fake HTTP layer that returns a response body the test author wrote; `_PAGE_BODY` carries `"nextSyncToken": "SYNC_NEXT"`, so `test_full_sync_request_shape_and_parsing` asserts the adapter *parses* a token correctly. It can never assert that real Google *emits* a token for the request shape the adapter builds, because the mock **is** the author's model of the vendor — the very assumption under test is baked into the fixture. This is why unit-test green coexisted with runtime token-absence. The generalisable lesson: MockTransport (and request/response fakes broadly) verify the *parse and request-construction* side of a vendor boundary with high fidelity and zero network, but they are blind to the *emit* side — what the vendor actually returns for a given request. Emit-side contract belongs to a live smoke, which is exactly where it surfaced. First instance of this specific "mock encodes the assumption under test" shape named on the calendar context; recorded in captures as a methodology candidate. The mitigation already shipped (the smoke is the gate); the discipline addition is to *name*, when writing a vendor adapter, which assertions are parse-side (unit-testable) and which are emit-side (smoke-only), so the smoke checklist explicitly carries the emit-side claims rather than leaving them implicit.

  3. **Deletion-mechanism reconciliation.** The Google docs confirm `showDeleted=true` returns cancelled events with `status="cancelled"` and do not prohibit combining it with `timeMin`/`timeMax`/`singleEvents=true`; the one documented caveat (cancelled recurring instances appearing only when `showDeleted` and `singleEvents` are *both* false) does not apply since the adapter uses `singleEvents=true`. Live request returned 200 (the operator's calendar had no in-window cancellations to display, but the request shape is valid and error-free). Shipped: `showDeleted=true` on the full pull, reusing the existing `status=cancelled` tombstone path — no new deletion code, and inherently safe against partial-pull false-deletes (tombstones are explicit cancelled events, not a set-diff).

  4. **Latency re-confirmation.** The S55a-fix authoring baseline is **376 ms** for a 3-event scoped pull (the live Stage-1 run, without `showDeleted`); `showDeleted=true` adds only cancelled events to the payload, negligible at this volume, so the floor is expected to hold at ~376 ms. The operator re-runs Stage 1 post-fix to confirm the number; that re-measured floor is what S55b's refresh-before-answer tiering budgets against (D122). S55b opens only after the operator re-runs stages 1–3 green on the corrected full-pull substrate.

methodology: The session is a clean instance of the response-level discipline working as designed — a procedural smoke surfaced an emit-side vendor-contract miss that no unit test could have caught, and the finding was confirmed empirically (A/B against the live Proxy) before any code moved. It also distinguishes two reconciliation gates that the methodology has so far conflated: pre-write reconciliation (code-vs-prompt, catches as-built drift) and a live-contract reconciliation gate (live-vendor-vs-assumption, catches emit-side contract drift) — the latter is the S4 Langfuse shape at its second distinct instance, forwarded to the next phase audit as a promotion candidate distinct from pre-write reconciliation.

- Close verdict: D149 lands the option-4 resolution cleanly — the active calendar sync path is a scoped full pull on every refresh that depends on no sync token, tombstones cancellations via `showDeleted=true`, and writes nothing to `connections.sync_token`; the incremental syncToken/410 machinery is present, unit-covered, and dormant with a named reactivation trigger. The charter commit precedes every code commit. The revised smoke matches the full-pull mechanism and its live stages remain pending operator re-execution per the procedural-then-executed precedent. S55b opens only after the operator re-runs stages 1–3 green.

```
metrics:
  classification: fix (corrective substrate session)
  brief_started: 2026-06-02
  session_started: 2026-06-02
  session_closed: 2026-06-02
  merged: 2026-06-02
  close_state: clean (six commits — charter ahead of code; D149 supersedes D148's receive-side sync clause only; active path is scoped full-pull with no sync-token dependency; incremental/410/sync_token dormant with named trigger; Bearer auth pinned; host-port-binding enforcement carryover fixed to green; smoke revised; live stages pending operator re-run)
  tests_passing: 2062 passed, 12 skipped, 0 failed (tests/unit + tests/contract + tests/_enforcement); import-linter 37/37 kept; three known docker-dependent environmental failures carry forward, not in scope, not introduced
  principles_intact: yes
  charter_touchpoints: charter/decisions.md (D149 summary + full entry; D148 receive-side clause superseded); log/captures.md (2026-06-02 [S55a-fix] three-point vendor-contract entry + enforcement-drift entry); charter/current-package.md (S55a-fix close marker above the unchanged S55a marker); charter/packages/p15-epic.md (S55 line D149 note); contexts/calendar/application/sync_calendar.py (scoped full-pull active path; CalendarSyncResult shape); contexts/calendar/ports/calendar_event_source_port.py (show_deleted param; dormant incremental docstring); contexts/calendar/adapters/outbound/nango/nango_proxy_calendar_adapter.py (showDeleted param; D149 + Bearer docstring); tests/unit/contexts/calendar/test_sync_calendar.py (full-pull tests); tests/unit/contexts/calendar/test_nango_proxy_calendar_adapter.py (Bearer pin + showDeleted assertion + dormant-path annotation); tests/_enforcement/test_host_port_bindings.py (allowlist sync for padhanam-api + nango-server loopback bindings, carryover); docs/smoke/p15_s55a_calendar_substrate.md (revised for full-pull); compose.yaml (Nango proxy env passthrough, committed 86421e5 earlier this session); log/sessions.md (this entry)
  corrects: S55a (the calendar data substrate sync mechanism; D148 receive-side sync clause); host-port-binding enforcement allowlist drift (carryover from 7af8e88 + e3b7d9e)
  corrected_by:
```

## 2026-06-02 — [S55b-1] Calendar conversation surface (first of the S55b split)
roles: architect, engineer, technical writer
mode: build session (first of the two-way S55b split). HEAD at open: `86421e5` (S55a-fix close tip). Builds the calendar conversation surface as an independently-verifiable unit; dispatch integration (MetaClassifier four-way, routing gold set, citation-time audit-snapshot) is S55b-2. Opens with a clean-bytecode enforcement hardening so this close runs enforcement from non-stale bytecode for the first time post-S55a-fix.

The session adds `contexts/calendar_conversation/` — the third ConversationFlow implementer, mirroring audit-conversation — plus refresh-before-answer (D150 Option A), the apps indexing-and-sync wiring bridge deferred from S55a, the calendar-conversation gold set, and CitedResponse + resolution-ambiguity contract conformance. The cell classifies a calendar intent (five classes), refreshes the calendar through `sync_calendar` at turn-open within a tier budget, queries the Meeting store, and returns a `CalendarConversationResponse` citing Meetings with the `meeting` discriminator; on refresh timeout/failure it serves the cached store with a staleness note (D146 always-produce-output mirror). Note: per D47 build sessions do not normally write `charter/methodology.md`; the prompt explicitly directed the clean-bytecode Patterns-observed promotion this session, recorded as a build-mode promotion within D40/D113's living-hypothesis surface (v6) — flagged here for the operator's eye.

- Pre-write reconciliation (recorded per the discipline):
  - **Clean-bytecode mechanism.** No in-repo CI workflow exists; the gate is the Makefile `lint`/`test` targets. `pytest -p no:cacheprovider` clears only pytest's cache, and `python -B` only suppresses *writing* bytecode — neither removes a stale `.pyc`. Shipped: a `clean-pyc` target (remove all `__pycache__` + `*.pyc`) as a prerequisite of `lint` and `test`, plus `-p no:cacheprovider`. Verified a planted garbage `.pyc` is removed before the run and lint stays green.
  - **apps wiring signatures.** Confirmed against current source: ingestion's `ChunkEmbedderPort` exposes `embed_query(query, tenant_context, task) -> Sequence[float]` (the single-text path the Meeting-text synthesis needs) and `GraphRepositoryPort` exposes `merge_entities(Sequence[Entity], …)` / `merge_relationships(Sequence[Relationship], …)`; `Entity(tenant_id, jurisdiction, name, entity_type, …)` and `Relationship(…, source: EntityRef, target: EntityRef, relationship_type, source_chunk_id: UUID, …)`. Concrete adapters: `LiteLLMChunkEmbedder()`, `Neo4jGraphRepository.from_settings(Neo4jSettings())`, both reached lazily in the builder. The S55a substrate-inheritance survey holds.
  - **Refresh tier budget.** D122's REAL_TIME_REQUIRED tier is the cell's tier (under one second budget intent); the refresh timeout slots at **2000 ms**, clearing the measured 513 ms cold floor ~4× while staying well under the multi-second LLM cascade. Recorded as the D150 default.
  - **ConversationFlow + CitedResponse contracts.** The cell mirrors `AuditConversationCell` (constructor shape; D134 three-case confidence; D139 resolution-ambiguity through PendingClarification with positional selection); the response mirrors `AuditConversationResponse` (three citation tuples). Conformance needs `_PAYLOAD_KEY_PER_IMPLEMENTER['calendar_conversation_cell'] = 'calendar_response'` and an `_AMBIGUOUS_RESOLUTION_FACTORIES` entry. Build-time finding: `KNOWN_TARGET_CELLS` (a closed union at `contexts/messaging/domain/pending_clarification.py`) needed `calendar_conversation` added — the same closed-union extension shape as the ArtefactCitation discriminator.
  - **Brief path drift.** The captures file is `log/captures.md`, not `charter/captures.md`.

- Produced (eight commits; charter ahead of code; built in dependency order — 0,1,2,3,6,5,4,7 — which differs from the prompt's numbering for commits 4/5/6, noted for the record):
  - **Commit 0 (`e976917`)** `chore(enforcement): run lint/test from clean bytecode` — the `clean-pyc` prerequisite; AC1 verified against a planted stale `.pyc`.
  - **Commit 1 (`dc5e4c7`)** `docs(charter): D150 ...` — D150 (index + full entry); the clean-bytecode methodology promotion (methodology.md v6); the S55b-1 captures annotation (synced-source verification + deferred baked-image/cancellation confirmations); current-package S55b-1 marker; p15-epic S55b-split note; architecture calendar_conversation + refresh sub-sections with the citation-snapshot forward note to S55b-2.
  - **Commit 2 (`d72b2a2`)** `feat(calendar_conversation): cell + intent classification + response + resolution-ambiguity` — the new bounded context (domain intent + parse; `shared_kernel/intent_classification_calendar.py` prompt+schema; cell; query builder; `CalendarConversationResponse`); `KNOWN_TARGET_CELLS` + `layers-calendar-conversation` contract; 24 unit tests.
  - **Commit 3 (`ca6dea1`)** `feat(calendar_conversation): refresh-before-answer (D150 Option A)` — `CalendarRefreshPort` + `CalendarRefreshError`; cell refreshes at turn-open within the budget and falls back to the cached store with a staleness note on timeout/failure; three unit tests.
  - **Commit 6 (`5752e6d`)** `test(contract): CitedResponse + resolution-ambiguity conformance for calendar_conversation` — registration module + both conformance-map extensions; 29 conversation_flow conformance tests green.
  - **Commit 5 (`f884c29`)** `feat(intent_classification_evaluation): calendar-conversation gold set + INTENT_CLASSES extension` — INTENT_CLASSES/INTENT_SURFACES + runner `_SURFACE_PRIMITIVES` calendar entry (fifth D137 surface); 20-entry gold set; loader test.
  - **Commit 4 (`7f0b81a`)** `feat(apps): calendar indexing + sync wiring bridge` — `MeetingEmbedderBridge` + `MeetingGraphIndexBridge` + `CalendarRefreshAdapter` + `build_calendar_refresh_adapter` at the apps composition root; four wiring tests with doubles (both bridges + refresh-driven pull-store-index + failure mapping).
  - **Commit 7 (`<this commit>`)** `docs(p15/s55b-1): calendar conversation smoke + session log + close` — the procedural close smoke (baked-image rebuild + Stage 1 re-confirm; cell end-to-end; refresh fallback), this entry, and the current-package S55b-1 close marker.

- Decisions: D150 (`charter/decisions.md`). Methodology.md v6 (clean-bytecode promotion; see the D47 note above).

- Tests: close run **2097 passed, 12 skipped, 0 failed** across `tests/unit` + `tests/contract` + `tests/_enforcement` from cleared `__pycache__` (+35 over the S55a-fix 2062 baseline); import-linter **38/38 contracts kept** (+1 `layers-calendar-conversation`); AST enforcement green. The four `tests/integration/apps/cli/test_calendar_wiring.py` doubles-based wiring tests pass. The three known docker-dependent environmental failures carry forward, not in scope, not introduced.

- Reflection prompts answered:

  1. **Clean-bytecode close integrity (required).** This is the first close after the commit-0 hardening, and the close run was deliberately executed from cleared `__pycache__` (the `clean-pyc` prerequisite plus an explicit `find … -name __pycache__ -delete` ahead of the final `lint`/`pytest`). What the hardening changed about the green signal's trustworthiness is concrete: before it, "37/37 kept, enforcement green" could be a statement about *stale compiled artefacts* whose `co_filename` and (in edge cases) cached code objects predate the current source — exactly the S55a-fix situation where a contract had been red since the repo rename while closes reported green. After it, `make lint` and `make test` cannot report green off stale bytecode because the bytecode is gone before the interpreter starts; a green from these targets is now a statement about the source on disk, not about whatever `.pyc` happened to survive. The signal is trustworthy in a way it structurally was not, and the 38-contract / 2097-test green this session is the first close that can claim that. The residual gap: developers who run `uv run pytest` directly (bypassing the `make` targets) still read stale bytecode — the hardening binds the *gate*, not every invocation; CI on a fresh checkout is the belt to that suspenders, and there is no in-repo CI yet (a forward item).

  2. **Standalone-surface boundary (required).** The cell-without-MetaClassifier boundary held cleanly, and the test surface is the evidence: the cell is constructed and driven directly in every unit test and in the conformance harness, with no MetaClassifier, no dispatch, and no routing gold set in sight — the cell's public surface is `open`/`turn`/`close` plus its constructor ports, and nothing in commits 2–4 reaches toward the dispatch layer. The one place coupling could have leaked is the citation surface: the cell cites the live Meeting row via `ArtefactCitation(meeting)`, and the *immutable citation-time audit-snapshot* (the two-store split, D148) is genuinely a dispatch-adjacent concern — but it stayed out (the response carries the live-row pointer; the snapshot wiring is explicitly S55b-2), so no premature coupling. The refresh port is the other seam that could have pulled dispatch detail in; it did not, because the port is defined in the cell's own ports package and the apps adapter behind it is wired at the composition root, invisible to the cell. The split was clean because the surface (a cell that answers one calendar turn) and the dispatch (which cell handles an inbound message) are genuinely separable concerns, and the audit-conversation precedent already proved a cell can be built, tested, and conformance-checked before its dispatch lands.

  3. **Refresh fallback behaviour.** The fallback is unit-proven (timeout → `asyncio.TimeoutError` → cached + "timed out" note; failure → `CalendarRefreshError` → cached + "unavailable" note; success → no note), and the staleness note is threaded only onto *answers drawn from the store* (`_compose`), not onto clarifications — so a stale answer is always marked and a clarification is never spuriously caveated. The live rendering (the `render_for_whatsapp` `⚠` line ahead of the citation line) and the perceived latency (does always-refresh feel fresh-by-default or laggy) are pending the Stage 3/4 live smoke; the 376 ms floor against the multi-second LLM cascade predicts imperceptible, but that is a prediction to confirm live, not a measured close result.

  4. **Gold-set legitimate-ambiguity recurrence (S48b/S51 recurrence test).** Yes — and it was authored honestly, not auto-corrected. The four `unclear_calendar` entries include two write requests ("Schedule a meeting with Ada tomorrow at 3pm", "Cancel my 2pm today"). On the *read-only* calendar surface (D148: Google is the sole writer) these correctly map to `unclear_calendar` — but they carry defensible cross-surface ambiguity: a future calendar-*write* surface would classify them as a distinct write intent, not as unclear. The gold set commentary names this explicitly rather than pretending the mapping is unconditional. This is the third instance of the legitimate-ambiguity pattern (S48b manual-entry, S51 audit, S55b-1 calendar); it continues to be a property of honest gold-set authoring, not a defect to correct.

  5. **Residue closure.** The S55a-fix live verification used `make sync-code` (working-tree source into the running container), so the committed image digest predates the calendar fix; the baked-image confirmation folds into this session's close smoke (Stage 1 against the rebuilt image). It is **not yet executed** in this session (the smoke is procedural, pending operator run). The live cancellation-tombstone sub-step remains **operator-gated** (the build agent cannot mutate the `calendar.readonly` calendar); it is unit-proven and folds into Stage 2 if the operator has cancelled an in-window event.

methodology: A standalone-surface split (cell now, dispatch next) let the calendar conversation surface ship fully conformance-checked and gold-set-evaluable before any routing exists — the audit-conversation precedent generalised cleanly, suggesting "build the cell, prove it against the ConversationFlow + CitedResponse + resolution-ambiguity harness, then wire dispatch separately" is a reusable shape for new conversation surfaces. The clean-bytecode hardening also demonstrates the captures→methodology promotion path firing one instance ahead of the recurrence threshold by operator choice, because the cost of the masked-enforcement failure (a contract red across multiple closes) justified hardening now rather than waiting for a second instance.

- Close verdict: the calendar conversation surface is built, refresh-guarded (D150), gold-set-evaluable, and conformance-clean (CitedResponse + resolution-ambiguity), with the apps bridge wiring the real source/embedder/graph behind the refresh port and a doubles-based integration test proving the refresh-driven pull-store-index. Enforcement runs from clean bytecode for the first time post-S55a-fix; 2097 unit+contract+enforcement tests pass, 38 import-linter contracts kept. The procedural close smoke (baked-image re-confirm + cell end-to-end + refresh fallback) is pending operator execution; **S55b-2 opens only after the operator runs stages 1 and 3 green**.

```
metrics:
  classification: feat (new conversation surface; first of the S55b split)
  brief_started: 2026-06-02
  session_started: 2026-06-02
  session_closed: 2026-06-02
  merged: 2026-06-02
  close_state: clean (eight commits; charter ahead of code; new contexts/calendar_conversation/ with cell + intent + response + query builder + resolution-ambiguity + refresh-before-answer; apps bridge wiring source/embedder/graph behind the refresh port; gold set + INTENT extension; CitedResponse + resolution-ambiguity conformance; clean-bytecode enforcement hardening; procedural smoke pending operator run)
  tests_passing: 2097 passed, 12 skipped, 0 failed (tests/unit + tests/contract + tests/_enforcement, clean bytecode); +4 doubles-based calendar wiring integration tests; import-linter 38/38 kept; three known docker-dependent environmental failures carry forward, not in scope, not introduced
  principles_intact: yes
  charter_touchpoints: charter/decisions.md (D150 summary + full entry); charter/methodology.md (clean-bytecode Patterns-observed entry + v6 version-log); log/captures.md (2026-06-02 [S55b-1] synced-source/residue annotation); charter/current-package.md (S55b-1 in-flight + close markers); charter/packages/p15-epic.md (S55b-split note); charter/architecture.md (calendar_conversation + refresh-before-answer sub-sections); Makefile (clean-pyc); .importlinter (layers-calendar-conversation); shared_kernel/intent_classification_calendar.py; contexts/calendar_conversation/** (domain intent, cell, query builder, response, ports incl. calendar_refresh); contexts/messaging/domain/pending_clarification.py (KNOWN_TARGET_CELLS); contexts/intent_classification_evaluation/** (gold_set INTENT extension, runner surface primitive); apps/cli/_calendar.py (bridges + refresh adapter + builder); tests/unit/contexts/calendar_conversation/**; tests/contract/conversation_flow/** (registration + conformance maps); tests/fixtures/intent_classification/calendar_conversation_gold_set.yaml; tests/integration/apps/cli/test_calendar_wiring.py; docs/smoke/p15_s55b1_calendar_conversation.md; log/sessions.md (this entry)
  corrects:
  corrected_by:
```

## 2026-06-02 — [S55b-2] Calendar dispatch integration + citation evidence (S55b close)
roles: architect, engineer, technical writer
mode: build session (second and final unit of the S55b split; closes S55b). HEAD at open: `5a57515` (S55b-1 close tip). Integrates the S55b-1 calendar conversation surface into the meta-classifier dispatch (four-way routing) and makes its citations procurement-grade (the citation-time audit-snapshot evidence, D148 option b), plus the S55b-1 charter carryovers.

The session extends the meta-classifier (D140) to its fourth surface and wires the live dispatch path to the calendar cell, authors the four-way routing gold set, and lands the first mutable-source citation evidence: when `calendar_conversation` cites a Meeting, an immutable snapshot is frozen into a `meeting_citation` audit event whose sensitive content is envelope-encrypted (D21) so no plaintext reaches the plaintext-at-rest audit `after_state`. The standing operator flag on the snapshot (does it need a shared evidence hook, and does it leak plaintext) was resolved in reconciliation and carried into the design.

- Pre-write reconciliation (the load-bearing findings):
  - **D47 reading.** D47 governs the *authority and provenance* of a methodology promotion (a strategic-mode deliverable), not the file-write mechanics — every charter file commits through the build UI, so a strict no-build-write reading is self-undermining. The S55b-1 build prompt mislabelled the clean-bytecode promotion's authority; the remedy is the v6 provenance annotation (strategic-mode ratified 2026-06-02), not a revert (a revert discards a true lesson). Applied at commit 1; no D47 conflict that warranted stopping.
  - **MetaClassifier extension point.** The routing SSOT is `shared_kernel/meta_classification.py` (preamble + `cell_identifier` schema enum, shared by the production adapter and the D137 runner) plus the `CellIdentifier` enum and the `dispatch_inbound` lexicon/route map. Extended all four coherently; the `layers` contracts needed no change.
  - **D148 option-b citation path — the operator's stop-and-surface point.** Reconciliation confirmed there is **no existing citation-emission audit event** and no shared citation-evidence mechanism: every prior cited artefact (Case, DataPoint, audit events) is append-only or revision-with-lineage, so its own audit trail is the evidence. Meeting is genuinely the **first mutable cited source**. Conclusion (surfaced to and ratified by the operator before commit 4): introduce the snapshot **calendar-local**, not as a shared hook — a shared abstraction is justified only by a *second* mutable cited source, and Email (S56) is append-only, so a shared hook now would be the anticipatory-control the two-threshold rule forbids; deferred to the second mutable-source instance (recorded in `charter/architecture.md` prose as a findable trigger).
  - **Encryption posture — the security-load-bearing finding (operator's commit-4 flag).** Audit `after_state` is **plaintext JSONB at rest** (no audit-layer encryption), and the meetings table envelope-encrypts exactly title/description/location/attendees/organizer (D21). And `test_no_plaintext_in_state` is **not** what its name suggests for this case — it is a tenancy-adapter AST check for the `TenantConnectionConfig` credential type held as instance state; it does **not** and cannot cover audit payloads, and the draft-then-recompute pattern does **not** encrypt after_state. So the snapshot must encrypt the sensitive fields itself: it reuses `crypto.encrypt_field` (the D21 mechanism) with a citation-scoped AAD, stores only ciphertext + non-sensitive metadata (event id, status, content_hash, timestamps, attendee count) in after_state, and the no-plaintext guarantee is enforced by dedicated unit tests on the emitted event (not by `test_no_plaintext_in_state`).
  - **Read-only routing boundary.** `calendar.readonly` (D148) means a calendar-mutation phrasing has no write path; it routes to `calendar_conversation` and the cell returns `unclear_calendar`. Recorded in the four-way routing architecture sub-section; calendar-write deferred until a `calendar.write` scope exists.

- Produced (six commits; charter ahead of code):
  - **Commit 1 (`01a94e7`)** `docs(charter): D47 provenance correction + ... captures + D150 floor + ...` — v6 provenance annotation; the prompt-drafting + closed-union captures notes; the D150 floor updated to the baked-image figures (~250 ms steady / 524 ms cold, 5a57515); current-package S55b-2 marker; p15-epic S55b-1-closed; architecture four-way routing + citation-snapshot sub-sections (with the deferral trigger in prose).
  - **Commit 2 (`36f4b50`)** `feat(messaging): MetaClassifier four-way routing to calendar_conversation` — `CellIdentifier.CALENDAR_CONVERSATION`; the calendar route in the meta preamble + schema enum + dispatch lexicon/prompt body + rule-based fallback; `_run_calendar_conversation_cell` wired into `_build_cell_runners` (builds the Meeting store + D150 refresh adapter per request, reusing the messaging composition's shared ports — no `MessagingComposition` field change). Unit tests: rule-based + LLM calendar routing.
  - **Commit 3 (`bcbbb60`)** `feat(intent_classification_evaluation): meta-classifier four-way routing gold set` — `calendar_conversation` added to INTENT_CLASSES dispatch targets; a 21-entry four-way gold set (calendar scoped to queries; 4 ambiguous best-guess entries); loader test.
  - **Commit 4 (`663faac`)** `feat(calendar_conversation): citation-time audit-snapshot evidence (D148 option b)` — `meeting_citation` audit event freezing per-Meeting snapshots; sensitive content envelope-encrypted (D21) so after_state carries no plaintext; five unit tests (no-plaintext, decryptable, frozen-under-mutation, event shape, cell emits).
  - **Commit 5 (`277cfb9`)** `test(contract): four-way routing + citation-snapshot evidence conformance` — four-way routing SSOT conformance (the four cells coherent across enum/schema/lexicon/INTENT_CLASSES); citation-snapshot conformance proving evidence immutability under a simulated refresh.
  - **Commit 6 (`<this commit>`)** `docs(p15/s55b-2): dispatch-through smoke + session log + S55b close` — the procedural dispatch smoke, this entry, the current-package S55b close marker, and the p15-epic S55b-complete update.

- Decisions: none (no new D-entry — four-way routing implements D140's committed extension; the citation snapshot implements D148 option b; a D151 would only restate them — operator concurred).

- Tests: close run **2110 passed, 12 skipped, 0 failed** (tests/unit + tests/contract + tests/_enforcement, clean bytecode; +13 over the S55b-1 2097 baseline); import-linter **38/38 kept**; AST enforcement green. Three known docker-dependent environmental failures carry forward, not in scope, not introduced.

- Reflection prompts answered:

  1. **Citation evidence under mutation (required).** Meeting is the first mutable search cache the platform cites, and the two-store split (live mutable row vs immutable audit snapshot) is what makes a citation defensible when the cited thing can change with no platform action. The design freezes the evidence at emission into an append-only hash-chained audit event, so D148 option b holds by construction: the snapshot is a value captured at citation time, not a reference to the live row, and the audit chain's immutability carries it. The contract conformance proves it under a simulated refresh (the frozen event decrypts to the original title while a later turn reflects the mutation), and the live smoke Stage 3 is the real-refresh confirmation. The non-obvious design tension the reconciliation surfaced: the snapshot must *also* honour D21, because audit after_state is plaintext at rest — so the evidence is encrypted with the same envelope mechanism the live store uses, and the content_hash (plaintext) is the integrity anchor that binds the encrypted blob to exactly what was synthesised and shown. The evidence is therefore both immutable (audit chain) and confidential (D21 envelope) — the two properties a procurement reader needs.

  2. **Four-way routing headroom.** Adding the fourth route did not degrade the prior three: the no-regression spot-check (manual/audit/mirror) is the smoke Stage 2, and the rule-based fallback orders the calendar branch before the generic mirror read-verbs so "show my meetings" routes to calendar without stealing "show me the Q3 review" from mirror (the calendar branch keys on calendar/meeting/schedule nouns, not on "show"). The dispatch-ambiguity classes worth noting for the S56 five-way: short relative phrasings ("what's next", "update on the board") are genuinely ambiguous between calendar and mirror/audit and are authored into the gold set as best-guess-with-low-confidence-routes-to-clarification entries — the five-way will widen that ambiguous band (email "what came in" vs calendar "what's on" vs mirror "what's the status"), so S56 should expect the dispatch_clarification rate to rise and budget a few more ambiguous gold entries rather than treat every miss as a misclassification.

  3. **D47 closure.** The provenance correction restored a clean authorship trail: v6 now records the clean-bytecode promotion as strategic-mode-ratified (2026-06-02) with the build-prompt mislabel named explicitly, and the prompt-drafting captures note codifies the rule (a build prompt may route a build-mode observation to the living-hypothesis surface but must never direct a promotion). This session's prompt directed **no** methodology promotion — it directed a provenance *correction* to an existing entry, which is itself a strategic-ratified charter edit the operator authored, not a new promotion. The authorship trail is clean.

  4. **Cancellation-tombstone closure.** Pending the live smoke Stage 4 (recorded with the smoke results). The tombstone-via-full-pull path is unit-proven; the live confirmation needs an operator-cancelled in-window event.

methodology: The operator's two-part flag on the citation snapshot (shared-vs-local hook; plaintext leak) is a worked instance of why a mutable-source citation is a genuinely new architectural case, not a copy of the append-only citation pattern — and both halves resolved by reconciliation against existing decisions (two-threshold rule → local; D21 + plaintext-at-rest audit → encrypt the snapshot) rather than new decisions, which is the charter doing its job. The `test_no_plaintext_in_state` name-vs-scope mismatch is a small lesson: an enforcement test's *name* is not its *coverage*, and a security property assumed to be covered by a named test should be confirmed against what the test actually walks (here it walks tenancy-adapter instance state, not audit payloads) — the no-plaintext guarantee for meeting_citation is carried by dedicated unit tests on the emitted event instead.

- Close verdict: S55b closes. The calendar conversation surface is reachable through the live four-way MetaClassifier dispatch; the four-way routing gold set scores on the dispatch_classifier surface; calendar citations freeze an immutable, D21-encrypted, plaintext-free snapshot into the audit chain (D148 option b, calendar-local with the shared hook deferred); four-way routing and citation-snapshot conformance pass; 2110 unit+contract+enforcement tests green from clean bytecode, 38 import-linter contracts kept. The procedural dispatch smoke (dispatch-through, four-way no-regression, citation-snapshot under real refresh, cancellation tombstone) is pending operator execution; **P15 continues to S56 (email substrate + email conversation; MetaClassifier five-way) after the operator runs stages 1 and 3 green.**

```
metrics:
  classification: feat (calendar dispatch integration + citation evidence; S55b close)
  brief_started: 2026-06-02
  session_started: 2026-06-02
  session_closed: 2026-06-02
  merged: 2026-06-02
  close_state: clean (six commits; charter ahead of code; MetaClassifier four-way routing + live dispatch wiring; four-way routing gold set; citation-time audit-snapshot evidence with D21 envelope encryption and no-plaintext unit enforcement; four-way + citation-snapshot conformance; S55b-1 charter carryovers incl. the D47 provenance correction; procedural dispatch smoke pending operator run)
  tests_passing: 2110 passed, 12 skipped, 0 failed (tests/unit + tests/contract + tests/_enforcement, clean bytecode); import-linter 38/38 kept; three known docker-dependent environmental failures carry forward, not in scope, not introduced
  principles_intact: yes
  charter_touchpoints: charter/methodology.md (v6 provenance correction); charter/decisions.md (D150 floor update); log/captures.md (2026-06-02 [S55b-2] prompt-drafting + closed-union notes); charter/current-package.md (S55b-2 + S55b close markers); charter/packages/p15-epic.md (S55b complete); charter/architecture.md (four-way routing + citation-snapshot sub-sections); contexts/messaging/domain/cell_identifier.py (CALENDAR_CONVERSATION); shared_kernel/meta_classification.py (four-way preamble + enum); contexts/messaging/application/dispatch_inbound.py (lexicon + prompt body); contexts/messaging/adapters/rule_based_meta_classifier.py (calendar branch); apps/api/routers/messaging.py (_run_calendar_conversation_cell + runner registration); contexts/intent_classification_evaluation/domain/gold_set.py (calendar_conversation dispatch target); tests/fixtures/intent_classification/meta_classifier_four_way_gold_set.yaml; contexts/calendar_conversation/application/audit_events.py (citation snapshot, envelope-encrypted); contexts/calendar_conversation/application/cell.py (_compose_and_emit); tests/unit/contexts/calendar_conversation/test_citation_evidence.py; tests/unit/contexts/messaging/adapters/test_meta_classifier_adapters.py (calendar routing); tests/unit/contexts/intent_classification_evaluation/test_yaml_gold_set_reader.py (four-way loader); tests/contract/intent_classification/test_four_way_routing_conformance.py; tests/contract/conversation_flow/test_citation_snapshot_conformance.py; docs/smoke/p15_s55b2_dispatch.md; log/sessions.md (this entry)
  corrects: S55b-1 methodology-promotion provenance (the D47 mislabel; v6 annotation)
  corrected_by:
```

## 2026-06-02 — [S56a] Email substrate + measurement
roles: architect, engineer, technical writer
mode: build session (first of the S56 split, mirroring S55a). HEAD at open: `88b61f4` (S55b-2 close tip). Builds the email data substrate per D151 against the S56 reconnaissance; the load-bearing deliverable — the measured message volume + full-pull floor — is operator-gated on a google-mail Nango provisioning step that has not yet happened, so the two deferred decisions (sync mechanism, refresh strategy) wait on the operator-run smoke.

The substrate mirrors calendar (D148/D149) with the email-specific divergences: a two-call N+1 Gmail adapter, an Email artefact + encrypted store + an email-local `email_chunks` store for long bodies, full-pull-only sync with set-diff deletion, and body chunking inheriting ingestion's embedder port but with an email-local chunker. Email cites directly (content immutable; no citation snapshot).

- Pre-write reconciliation (the build needed exact shapes the strategic recon left open; two non-blocking corrections surfaced and sharpened the design):
  - **historyId anchor source (correction).** The prompt assumed `history_id` is on the `messages.list` response. It is not — `messages.list` returns `{id, threadId}` stubs only; the mailbox historyId comes from `users.getProfile`. The adapter's `get_mailbox_history_id` reads `getProfile`; `sync_email` stores it as the dormant anchor. Surfaced and adapted (no design change).
  - **"Ingestion's chunker" is not a standalone tool (correction).** Chunking is embedded in ingestion's format-specific parsers (`plain_text_parser`/`markdown_parser`), each coupled to ingestion's `Source` domain object. So email inherits only the **embedder** (`ChunkEmbedderPort.embed(chunks)`, a clean injected port — the substrate-inheritance discipline) and builds a small **email-local chunker** (`chunk_email`), rather than constructing a synthetic `Source` to borrow a parser. This is the honest answer to reflection prompt 2: the injected-tools boundary held because the embedder is a port and the chunker is local.
  - **Nango google-mail (confirmed).** The live Nango knows `google-mail` (proxy base `https://gmail.googleapis.com`, OAUTH2); the same Bearer + proxy adapter pattern transfers; adapter path `/proxy/gmail/v1/users/me/...`. `EmailSettings` reads the shared `NANGO_*` env.
  - **Table-name collision (confirmed + handled).** Calendar's per-tenant tables are `connections`/`meetings`; email uses `email_connections`/`emails`/`email_chunks` to avoid a same-database collision.
  - **Auth Bearer; `received_at` (internalDate) stable per message → set-diff scope sound.**

- Produced (six commits; charter ahead of code):
  - **Commit 1 (`e4faf7d`)** `docs(charter): D151 ...` — D151 (index + full entry) with the reconnaissance divergences + the two corrections; captures [S56a] Gmail vendor findings; `KNOWN_ARTEFACT_TYPES += "email"` (artefact-level only; cell/routing unions are S56b); current-package/p15-epic open the S56 split; architecture email substrate sub-section.
  - **Commit 2 (`482ce59`)** `feat(email): Connection + NangoProxyEmailAdapter (two-call list+get, batched)` — Connection, EmailMessage/EmailMessageIdPage DTOs, EmailMessageSourcePort, the two-call batched adapter (list stubs; bounded-concurrency `get(format=full)` with MIME-tree body parsing; getProfile anchor; 404-skip), `layers-email` contract (39 kept), 7 MockTransport tests incl. the Bearer pin.
  - **Commit 3 (`ad3ba70`)** `feat(email): Email artefact + encrypted store + email_chunks (Alembic)` — Email + EmailChunk domain, the ports, PostgresEmailStore (upsert/tombstone/set-diff-scope reader) + PostgresEmailChunkStore (encrypted chunk text + per-chunk vector) + PostgresConnectionRepository (history_id anchor); migration 0027 (email_ prefix; HNSW), applied to both tenant DBs; schema.md same commit; 10 unit tests (incl. no-plaintext-at-rest round-trip) + 5 tenant-isolation scenarios (D24).
  - **Commit 4 (`11e1f40`)** `feat(email): sync_email full-pull pipeline + set-diff deletion` — full-pull-only; set-diff tombstone over the window-scoped live set; dormant history anchor; 4 unit tests.
  - **Commit 5 (`5c92b10`)** `feat(email): body chunking + indexing seams` — the email-local chunker, email_to_graph (Person + CORRESPONDED_WITH), the index ports, index_email, sync_email indexing hook; apps bridges (EmailChunkEmbedderBridge over the batch embed; EmailGraphIndexBridge) + build_email_sync_components + EmailSettings; unit + bridge-integration tests with doubles.
  - **Commit 6 (`<this commit>`)** `docs(p15/s56a): email substrate smoke (with measurement) + session log + close` — the procedural smoke (provisioning gate + measurement + set-diff + index), this entry, the current-package S56a close marker.

- Decisions: D151 (`charter/decisions.md`). No methodology.md write (build mode, D47).

- Tests: close run **2125 passed, 12 skipped, 0 failed** (tests/unit + tests/contract, clean bytecode; +15 over the S55b-2 2110 baseline); import-linter **39/39 kept** (+1 `layers-email`); AST enforcement green (9). The email-wiring doubles integration tests pass. Three known docker-dependent environmental failures carry forward, not in scope, not introduced.

- Reflection prompts answered:

  1. **The measurement (required) — PENDING, and honestly so.** The load-bearing deliverable of this session is the measured message count and full-pull floor, and **it could not be taken**: only google-calendar is connected in Nango; there is no google-mail connection, and the agent is `gmail.readonly`-scoped at most — it cannot OAuth-provision a mailbox. So the measurement is operator-gated behind the google-mail Nango provisioning step (the same gate calendar had at the Nango provisioning session). Consequently the two decisions that depend on it — the email sync-mechanism graduation (incremental, gated on volume) and the refresh-before-answer strategy (D150 Option A vs background-sync-then-serve, gated on the floor) — **remain open and must be decided from the operator-run smoke before S56b**. What reconnaissance predicts, to be confirmed live: at ~1,000–6,000 messages / 30 days, the two-call N+1 (list + thousands of batched gets + chunk + embed over Ollama) likely puts the full-pull floor in the multi-second-to-minutes range, which would (a) justify incremental sooner than calendar and (b) kill always-refresh-in-turn in favour of background-sync-then-serve. The substrate is built full-pull-only with the dormant history anchor exactly so that decision lands against the real number, not the estimate.

  2. **Chunk-store divergence — boundary held.** Body chunking is the largest divergence from calendar, and the injected-tools boundary stayed clean because the reconciliation drew the line correctly: the **embedder is inherited as a port** (`EmailChunkEmbeddingPort` → the apps bridge → ingestion's batch `ChunkEmbedderPort.embed`), while the **chunker is email-local** (`chunk_email`), because ingestion's chunking is not a standalone tool — it is welded into the format parsers and their `Source` coupling. Email did not pull itself into ingestion's pipeline: it owns its `email_chunks` store, calls the embedder through a thin consumer port, and never imports ingestion internals (the `layers-email` contract + the cross-context contracts hold). The temptation that would have breached the boundary — constructing a synthetic ingestion `Source` to reuse `plain_text_parser` — was declined; a 30-line local chunker is the honest cost of the clean boundary.

  3. **Set-diff deletion.** Unit-proven (`test_set_diff_tombstones_removed_message`: a stored live in-window message absent from the pull tombstones). The live confirmation (trash a Gmail message, re-pull) is operator-gated behind both the google-mail provisioning and a `gmail.readonly` agent that cannot trash; folded into the smoke Stage 3. One window-scope edge case noted for the live run: the set-diff scope is `received_at >= now - window_days`, so a message whose `received_at` is near the window boundary could oscillate in/out across pulls at the edge — benign (it re-upserts when back in window) but worth watching at the smoke.

  4. **S55b-2 baked confirmation.** Carried into this session per plan; the baked-image rebuild (retiring the S55b-2 synced-source residue and baking S56a) is part of the smoke's provisioning gate. Recorded in the smoke; executed at the operator's smoke run (the rebuild is deterministic but bundled with the provisioning gate the measurement needs).

methodology: Two pre-write reconciliation corrections (historyId anchor source; chunker-is-not-a-tool) both came from reconciling the strategic reconnaissance against exact code/vendor shapes at build time — the reconnaissance got the *shape* right (two-call, set-diff, incremental gotcha) but two *mechanics* needed the build-time check, which is the division of labour the reconnaissance-then-build split is meant to produce: strategy surfaces the gotchas, the build pins the signatures. The measurement being operator-gated is the honest limit of an agent-run substrate session against a vendor that requires interactive OAuth — the same shape as calendar, handled the same way (build the substrate, gate the live number on operator provisioning).

- Close verdict: the email data substrate is built end to end — two-call adapter, encrypted store + email-local chunk store, full-pull-only sync with set-diff deletion, body chunking via the inherited embedder + email-local chunker, the dormant history anchor — per D151, mirroring calendar with the reconnaissance divergences. 2125 unit+contract tests green from clean bytecode, 39 import-linter contracts kept (+layers-email), migration 0027 applied to both tenant DBs. **The load-bearing measurement is pending the operator's google-mail Nango provisioning + smoke run; the email sync-mechanism and refresh-strategy decisions are made from it before S56b opens.**

```
metrics:
  classification: feat (email data substrate; first of the S56 split)
  brief_started: 2026-06-02
  session_started: 2026-06-02
  session_closed: 2026-06-02
  merged: 2026-06-02
  close_state: clean (six commits; charter ahead of code; contexts/email/ two-call adapter + encrypted store + email_chunks + full-pull set-diff sync + body chunking via inherited embedder + email-local chunker; migration 0027 on both tenant DBs; KNOWN_ARTEFACT_TYPES += email; procedural smoke pending operator google-mail provisioning; the load-bearing measurement is operator-gated)
  tests_passing: 2125 passed, 12 skipped, 0 failed (tests/unit + tests/contract, clean bytecode); +email-wiring doubles integration tests; import-linter 39/39 kept (+layers-email); AST enforcement green; three known docker-dependent environmental failures carry forward, not in scope, not introduced
  principles_intact: yes
  charter_touchpoints: charter/decisions.md (D151 summary + full entry); log/captures.md (2026-06-02 [S56a] Gmail vendor findings + two corrections); shared_kernel/conversation_flow.py (KNOWN_ARTEFACT_TYPES += email); charter/current-package.md (S56a in-flight + close markers); charter/packages/p15-epic.md (S56 split); charter/architecture.md (email substrate sub-section); charter/schema.md (email_connections/emails/email_chunks); .importlinter (layers-email); contexts/email/** (domain: connection/email/email_message/email_chunk/email_graph/email_chunking/errors/sync_trigger; ports: connection_repository/email_message_source_port/email_repository/email_index_ports; application: sync_email/index_email; adapters/outbound: nango/nango_proxy_email_adapter, postgres/_tables+email_store+connection_repository); alembic/tenant/versions/2026_06_03_0027_email_substrate.py; padhanam/config/email.py + __init__.py (EmailSettings); apps/cli/_email.py (bridges + build_email_sync_components); tests/unit/contexts/email/**; tests/contract/tenant_isolation/test_email_isolation.py; tests/integration/apps/cli/test_email_wiring.py; docs/smoke/p15_s56a_email_substrate.md; log/sessions.md (this entry)
  corrects:
  corrected_by:
```

## 2026-06-03 — [S56b] Email conversation surface + five-way dispatch (S56 close)
roles: architect, engineer, technical writer
mode: build session (second of the S56 split, mirroring S55b). HEAD at open: `80e6740` (S56a wiring-proof close tip). Builds the email conversation surface and integrates it as the fifth MetaClassifier route, mirroring the calendar conversation surface (D138/D139/D140/D150) with the email-specific divergences from D151/D152.

The surface mirrors calendar's cell shape: a five-intent classifier (find_by_date_range, find_from_sender, find_by_subject, find_recent, unclear_email), refresh-before-answer through a port (D152 Option A), CitedResponse + resolution-ambiguity (find_by_subject is the ambiguity carrier), and five-way dispatch routing. The load-bearing divergence from calendar is **cite-directly, no snapshot** (D151: email content is immutable, so the citation references the live row rather than freezing an encrypted after_state snapshot — the inverse of D148 option b).

- Two operator holds, both honoured:
  - **The Option-A wiring is the load-bearing boundary.** The cell reaches the substrate *only* through `EmailRefreshPort`; it imports no `sync_email`. Background-sync-vs-incremental stays a pure wiring swap behind the port (D152). Verified live in the smoke (both refresh branches fire through the port) and structurally (the cell module imports the port, not the application use case).
  - **The smoke is a wiring-proof again.** Five-way routing live + refresh fires on both branches + cite-directly renders — proven. Volume-path proof and binding sync/refresh latency measurement stay open by design, gated on a representative inbox at dogfooding (the `tenant_a` mailbox is n=1 and unrepresentative; no sync/refresh architecture is decided on it).

- Produced (seven commits; charter ahead of code):
  - **Commit 1 (`950e5f8`)** `docs(charter): D152 ...` — D152 (email refresh Option A; background-sync/incremental deferred, measurement-gated; the cell-never-reaches-past-the-port clause) + the closed-union extensions (cell/routing) + current-package/p15-epic/architecture.
  - **Commit 2 (`fc6354c`)** `feat(email_conversation): cell + intent + query + response + cite-directly + resolution-ambiguity` — the EmailIntent union + parser, the query builder (window/sender/subject-resolution/recent), EmailConversationResponse (CitedResponse, "email" citation discriminator), the cell (mirroring calendar; cite-directly, no snapshot), `layers-email-conversation` (40 kept); 14 cell/intent/query/response unit tests.
  - **Commit 3 (`e1b2972`)** `feat(email_conversation): refresh port wired to Option A full-pull-in-turn` — EmailRefreshPort + EmailRefreshError, the `_maybe_refresh` timeout/fallback, the apps EmailRefreshAdapter over `sync_email`; 3 refresh fallback/timeout/success unit tests.
  - **Commit 4 (`7ea7795`)** `feat(messaging): MetaClassifier five-way routing to email_conversation` — CellIdentifier.EMAIL_CONVERSATION, the schema enum + routing-prompt body (option 5, read-only + disambiguation guidance), the dispatch lexicon, the rule-based email branch (before the generic mirror read-verbs), the eval-runner surface primitive, and `_run_email_conversation_cell` + `_resolve_email_connection_id` + runner registration in the API; the four-way→five-way routing conformance widening; rule-based + LLM email-routing unit tests.
  - **Commit 5 (`c1654f0`)** `test(p15/s56b): email-conversation + five-way routing gold sets` — the email-conversation intent gold set (20 entries) + the five-way routing gold set (30 entries, with two new ambiguous email/calendar-band entries); loader tests.
  - **Commit 6 (`69f88a1`)** `test(p15/s56b): register email_conversation in ConversationFlow conformance` — the email registration module (auto-discovered), CitedResponse-conformance payload-key entry, resolution-ambiguity duplicate-subject fixture; all conformance scenarios green with five implementers.
  - **Commit 7 (`<this commit>`)** `docs(p15/s56b): email dispatch smoke (executed) + session log + S56b/S56 close` — the procedural dispatch smoke (executed live), this entry, the current-package S56b/S56 close markers, p15-epic S56-complete.

- Decisions: D152 (`charter/decisions.md`, Commit 1). No methodology.md write (build mode, D47).

- Tests: close run **2155 passed, 12 skipped, 0 failed** (tests/unit + tests/contract, clean bytecode; +30 over the S56a 2125 baseline); import-linter **40/40 kept** (+1 `layers-email-conversation`); AST enforcement green (9). Three known docker-dependent environmental failures carry forward, not in scope, not introduced.

- Smoke (executed live 2026-06-03, `docs/smoke/p15_s56b_email_dispatch.md`):
  - **Stage 1 (five-way routing) — PASS.** The live LLM meta-classifier routed both email phrasings to `email_conversation` at 0.90.
  - **Stage 2 (no-regression) — PASS.** manual_entry / audit / mirror / calendar all routed correctly at 0.90 alongside the fifth route.
  - **Stage 3a (refresh fallback) — PASS.** The email cell, built with the live structured-output port + the real `tenant_a` store + the real D152 refresh adapter, fell back to the cached store on a default-timeout refresh, cited 1 email directly, and carried the staleness note.
  - **Stage 3b (refresh success) — PASS.** With a 120s timeout the refresh completed in-turn (no staleness note). Both Option-A branches proven live; the cell reached the substrate solely through the port.
  - **Deferred by design:** volume-path + binding sync/refresh measurement, gated on a representative inbox (operator guardrail; n=1).

- Reflection: The cell mirrored calendar cleanly because the two-threshold abstractions (CitedResponse, resolution-ambiguity, the ConversationFlow harness, the per-surface eval primitive, the five-way routing conformance) were already structurally guaranteed by the calendar instance — the second instance landed by extension, not by new abstraction, which is the discipline working as intended. The one real divergence — cite-directly vs the D148 snapshot — fell out of the data property (email content immutable) and required no new machinery, only the *absence* of the snapshot hook the calendar cell carries. The refresh-port boundary held under the live smoke: the same timeout-fallback the calendar cell uses produced the graceful cached-store answer when the live Gmail pull was slow, which is exactly the degradation the operator's "never reach past the port" hold protects.

methodology: The fifth surface added by pure extension across five routing/conformance seams (CellIdentifier, schema enum, dispatch lexicon, rule-based branch, eval primitive, routing conformance, CitedResponse map, resolution-ambiguity factory, ConversationFlow registration) with no new abstraction is the strongest evidence yet that the P14 conversational substrate's two-threshold abstractions were drawn at the right altitude — adding a surface is now a checklist, not a design. Not a promotion candidate (existing methodology applied correctly; no structural novelty, no 3x recurrence).

- Close verdict: the email conversation surface is built and integrated as the fifth MetaClassifier route, mirroring calendar with cite-directly as the sole load-bearing divergence (D151), refresh-before-answer behind a port (D152 Option A). 2155 unit+contract tests green from clean bytecode, 40 import-linter contracts kept (+layers-email-conversation), all ConversationFlow conformance scenarios green with five implementers. The live wiring-proof smoke is green (five-way routing, both refresh branches, cite-directly). **S56b closes; S56 closes; P15 continues.** Volume-path + binding sync/refresh measurement remain deferred to a representative inbox at dogfooding — where D152's background-sync-vs-incremental decision (a wiring swap behind the port) gets its number.

```
metrics:
  classification: feat (email conversation surface + five-way dispatch; S56 close)
  brief_started: 2026-06-03
  session_started: 2026-06-03
  session_closed: 2026-06-03
  merged: 2026-06-03
  close_state: clean (seven commits; charter ahead of code; email_conversation cell + intent + query + cite-directly response + resolution-ambiguity; refresh port wired to D152 Option A with both branches proven live; MetaClassifier five-way routing + live dispatch wiring; email + five-way routing gold sets; ConversationFlow conformance with five implementers; live wiring-proof smoke green; volume + binding measurement deferred to a representative inbox)
  tests_passing: 2155 passed, 12 skipped, 0 failed (tests/unit + tests/contract, clean bytecode); import-linter 40/40 kept (+layers-email-conversation); AST enforcement green; three known docker-dependent environmental failures carry forward, not in scope, not introduced
  principles_intact: yes
  charter_touchpoints: charter/decisions.md (D152 summary + full entry); charter/current-package.md (S56b + S56 close markers); charter/packages/p15-epic.md (S56 complete); charter/architecture.md (email conversation surface + five-way routing sub-sections); contexts/messaging/domain/cell_identifier.py (EMAIL_CONVERSATION); contexts/messaging/domain/pending_clarification.py (KNOWN_TARGET_CELLS += email_conversation); shared_kernel/meta_classification.py (five-way preamble + enum); shared_kernel/intent_classification_email.py (extraction prompt + schema); contexts/messaging/application/dispatch_inbound.py (lexicon + prompt body option 5); contexts/messaging/adapters/rule_based_meta_classifier.py (email branch); apps/api/routers/messaging.py (_run_email_conversation_cell + _resolve_email_connection_id + runner registration); apps/cli/_email.py (EmailRefreshAdapter + build_email_refresh_adapter); contexts/email_conversation/** (domain/intent; application/cell+response+query_builder+ports/email_refresh); contexts/intent_classification_evaluation/application/run_intent_classification_evaluation.py (_SURFACE_PRIMITIVES email); contexts/intent_classification_evaluation/domain/gold_set.py (email intent classes + dispatch target); shared_kernel/conversation_flow.py (KNOWN_ARTEFACT_TYPES email — S56a); .importlinter (layers-email-conversation); tests/fixtures/intent_classification/email_conversation_gold_set.yaml + meta_classifier_five_way_gold_set.yaml; tests/unit/contexts/email_conversation/**; tests/unit/contexts/messaging/adapters/test_meta_classifier_adapters.py (email routing); tests/unit/contexts/intent_classification_evaluation/test_yaml_gold_set_reader.py (email + five-way loaders); tests/contract/intent_classification/test_four_way_routing_conformance.py (widened to five-way); tests/contract/conversation_flow/test_email_conversation_conversation_flow.py + test_cited_response_conformance.py + test_resolution_ambiguity_conformance.py; docs/smoke/p15_s56b_email_dispatch.md; log/sessions.md (this entry)
  corrects:
  corrected_by:
```

## 2026-06-03 — [S57] Threshold-briefing + P15 close
roles: architect, engineer, technical writer
mode: build session (the P15-closing session). HEAD at open: `1c2c9a9` (S56b close tip). Builds the proactive threshold-briefing arc on the S53/S54 broadcast machinery — ThresholdEvaluator + threshold-briefing as the two-stage chain — and closes P15. This is outbound broadcast, reusing BroadcastFlow + BroadcastDispatch, not the conversation/dispatch path.

- **Pre-write reconciliation caught the load-bearing wrong assumption before the charter commit.** The epic (line 35) and the S57 brief's D153 both asserted the evaluator "polls the audit chain for state-change matches." The reconciliation gate falsified it: `sync_calendar` writes only to the meetings store (upsert/tombstone) and emits **no** audit events, so calendar cancellations/moves are not in the chain (the only calendar audit events are the conversation cell's `meeting_citation` snapshots). And more fundamentally, **conflict/double-booking was never a state-*change* event** — it is a standing condition over the current set, a query. Surfaced and the operator chose **Design 1: evaluate over the calendar state store**, a single coherent model (both rules are state queries) over a hybrid that makes cancel/move into events but special-cases conflict — and one that avoids pulling an AuditPort into the calendar substrate at close. The brief's "the cancellation tombstone is in tenant_a's audit chain" was a conflation of the `meeting_citation` snapshot (a citation of the since-cancelled meeting) with a state-change record (which does not exist). This is the reconciliation-then-build front-loading working exactly as intended.

- Produced (seven commits + one test-fix follow-up; charter ahead of code):
  - **Commit 1 (`c7ce128`)** `docs(charter): D153 ... + D154 P15 close ...` — D153 (reworded to state-store evaluation, the narrow cancel+conflict set, THRESHOLD_CROSSED re-keyed on derived-state identity, the restraint discipline) + D154 (P15 close interpretation); epic line-35 correction; current-package/p15-epic close markers; architecture sub-section; deferred-decisions (audit-chain CDC); captures (reverse-Kano candidate).
  - **Commit 2 (`ead983c`)** `feat(broadcast): ThresholdEvaluator implementer + configured-rules schema` — `contexts/threshold_briefing/`: the rule schema (two must-haves active, deferred shapes supported), pure evaluation functions (cancel + conflict), RuleMatch, the evaluator (refresh→read state→evaluate→emit), the three consumer ports, `BroadcastTriggerType += SCHEDULED_EVALUATION`, `layers-threshold-briefing` (41 contracts); 13 unit tests.
  - **Commit 3 (`a6ac323`)** `feat(broadcast): threshold-briefing implementer` — the crossing read-model, the briefing response + render, composer + notifier ports, the implementer (compose, fall back to summary on LLM failure, render, send), the LiteLLM composer adapter; 6 unit tests.
  - **Commit 4 (`83acd08`)** `feat(broadcast): refresh-then-evaluate scheduled trigger + idempotency + wiring` — idempotency for SCHEDULED_EVALUATION (None) + THRESHOLD_CROSSED (crossing identity); BROADCAST_INITIATED after_state carries the crossing metadata (traceability); the four apps wiring adapters (state reader, lazy refresh, emitter-via-FireTrigger, notifier); main.py registers both implementers (the three-implementer set complete); tests.
  - **Commit 5 (`cd3ef47`)** `test(contract): BroadcastFlow three-implementer conformance + threshold rule eval` — registers both implementers with the harness; the rule-eval conformance (two-stage hand-off, restraint no-cross); three-implementer completeness (close criterion 1).
  - **Commit follow-up (`f2365c7`)** `fix(test): update BroadcastTriggerType count for SCHEDULED_EVALUATION` — the shared_kernel value-set test asserted the old count of 5 (a verification miss in Commit 2: the enum addition was checked against the threshold subset but not this test).
  - **Commit 6-fix (`8fa8203`)** `fix(broadcast): stabilise cancellation crossing identity + lower-bound window` — the two live-smoke findings (below), fixed in the threshold context, with D153 + architecture corrected and the deeper calendar fixes deferred.
  - **Commit 7 (`<this commit>`)** `docs(p15/s57): threshold-briefing smoke + P15 close + session log` — the executed smoke, this entry, the P15 close verdict.

- Decisions: D153, D154 (`charter/decisions.md`). No methodology.md write (build mode, D47).

- Tests: close run **2190 passed, 12 skipped, 0 failed** (tests/unit + tests/contract, clean bytecode; +35 over the S56b 2155 baseline) plus the post-fix threshold/idempotency/conformance subset green; import-linter **41/41 kept** (+`layers-threshold-briefing`); AST enforcement green (9). Three known docker-dependent environmental failures carry forward, not in scope, not introduced.

- Smoke (executed live 2026-06-03, `docs/smoke/p15_s57_threshold_briefing.md`): a wiring-proof of the two-stage chain over live tenant_a state. Stage A (refresh-then-evaluate) — PASS: the evaluator refreshed calendar, read the state store, emitted one stable-identity cancellation crossing. Stage B (two-stage hand-off) — PASS: metadata round-tripped, the briefing fired, the LLM composer errored and the **fallback-to-summary** path fired and rendered (the resilience guarantee proven; happy-path compose not separately confirmed this run). Stage C (no-cross / restraint) — PASS: the conflict rule over the live confirmed set (no overlaps) emitted zero. The proactive end-to-end fire (operator cancellation → WhatsApp) and idempotency re-fire stay operator-gated.

- Reflection prompts answered:

  1. **Restraint in practice (required).** The no-cross case held live (Stage C: conflict rule over the real confirmed set → zero crossings) and structurally (`test_evaluate_no_cross`, `test_evaluator_emits_nothing_on_no_cross`). The reverse-category discipline is observable in how narrow the active set is — two rules, with the schema explicitly enumerating five deferred shapes it declines to enable (`PHASE_2A_ACTIVE_RULE_TYPES` is a strict subset of `ThresholdRuleType`), and `evaluate` structurally skips any rule outside the active set so restraint is enforced, not assumed. The live run sharpened the point: the `cancelled_at`-churn finding was a *reverse-Kano-in-the-mechanism* bug — a naive idempotency key would have re-briefed the same cancellation every scan, the precise noise that collapses a proactive surface's value. Restraint is not only which rules fire but that each crossing fires *once*. **The captures observation is worth promoting to methodology at the P15 audit** — "restraint against the reverse category on any platform-initiated surface" generalises beyond this one surface, and the live finding adds a second face (restraint applies to firing *frequency* and *content*, not only rule selection); held as a candidate per the 3x-or-structural-novelty bar.
  2. **Proactivity.** Confirmed live: the scheduled trigger fired the evaluation and the briefing **without a conversation turn** (Stage A/B driven from a `SCHEDULED_EVALUATION` trigger, no inbound). This is the property that distinguishes refresh-then-evaluate from the conversation cell's in-turn freshness (D150) — the briefing surfaces a change the user never asked about.
  3. **ThresholdEvaluator as a BroadcastFlow implementer — it fit, with a noted strain.** The evaluator satisfies BroadcastFlow cleanly (it has `fire`, returns a BroadcastResponse, registers under a trigger type), but the fit is slightly forced: its `fire` does not send a user-facing message — it emits a downstream trigger. The two-stage chain (SCHEDULED_EVALUATION → evaluator → THRESHOLD_CROSSED → briefing) reuses BroadcastDispatch for both a non-user-facing scan and a user-facing send, which is economical but means "BroadcastFlow" now covers two semantically different things (compose-and-send vs. evaluate-and-emit). Worth flagging for the P15 audit: if a third evaluator-shaped implementer appears, a distinct `Evaluator`/`Scan` abstraction alongside BroadcastFlow may be the second-instance threshold.
  4. **P15 close honesty.** The nine criteria are met as written (Stage 4 walk). The email debts are mapped to the dogfooding gate **in the close verdict itself** (D154, criterion 7 recorded wiring-proven-not-volume-proven, criterion 9 as dogfooding-*ready*), not obscured — the close states the gap and where it resolves.

methodology: The session's load-bearing lesson is that the **live smoke caught a class of bug the stubbed tests structurally could not** — the `cancelled_at` churn is a *runtime substrate behaviour* (the tombstone resets the timestamp on every sync), invisible to tests that stub the store, and it would have shipped a re-brief-every-scan defect. This is the S4 Langfuse lesson recurring in a new form (CLI/unit green while the real-substrate behaviour differs) and reinforces the browser/live-verification discipline for any acceptance criterion that depends on real substrate state, not only UI. The reconciliation gate caught the *static* wrong assumption (audit-chain polling) before the charter commit; the live smoke caught the *dynamic* one (cancelled_at churn) before close — two different gates, two different classes of error, both load-bearing.

- Close verdict: the proactive threshold-briefing arc is built and integrated as the two-stage chain on the broadcast machinery, evaluating over the calendar state store per D153 (the reconciled model), with the narrow cancel+conflict must-have set under the restraint-against-reverse-Kano discipline. 2190 unit+contract tests green from clean bytecode, 41 import-linter contracts kept (+`layers-threshold-briefing`), AST green; the three-implementer BroadcastFlow set is complete. The live wiring-proof is green (refresh-then-evaluate, two-stage hand-off with proven fallback, restraint), and surfaced + fixed two real substrate-behaviour findings. **P15 closes at S57 (D154)**: all nine close criteria met, the email volume debts + the calendar meeting-moved threshold mapped to the Phase 2-A dogfooding gate. **The next strategic-mode steps are the P15 audit (drift-finding; the reverse-Kano + BroadcastFlow-strain candidates triaged there) and the Phase 2-A dogfooding gate (one week of real use + senior-leader-ICP validation).** P16 follows the gate.

```
metrics:
  classification: feat (threshold-briefing + P15 close; the P15-closing session)
  brief_started: 2026-06-03
  session_started: 2026-06-03
  session_closed: 2026-06-03
  merged: 2026-06-03
  close_state: clean (eight commits incl. two fixes; charter ahead of code; contexts/threshold_briefing/ two-stage chain — ThresholdEvaluator over the calendar state store + threshold-briefing implementer; refresh-then-evaluate scheduled trigger; SCHEDULED_EVALUATION + THRESHOLD_CROSSED idempotency; the three-implementer BroadcastFlow set complete; live wiring-proof green; two live-smoke substrate findings fixed in-context; P15 CLOSED with email debts + meeting-moved mapped to the dogfooding gate)
  tests_passing: 2190 passed, 12 skipped, 0 failed (tests/unit + tests/contract, clean bytecode); import-linter 41/41 kept (+layers-threshold-briefing); AST enforcement green; three known docker-dependent environmental failures carry forward, not in scope, not introduced
  principles_intact: yes
  charter_touchpoints: charter/decisions.md (D153 + D154 summary + full entries; D153 idempotency clause corrected post-smoke); charter/architecture.md (threshold-briefing + ThresholdEvaluator sub-section; two stale audit-chain phrasings corrected; BROADCAST_INITIATED metadata; identity correction); charter/current-package.md (P15 CLOSED / S57 close marker); charter/packages/p15-epic.md (line-35 correction; P15 close verdict); charter/deferred-decisions.md (audit-chain CDC; stable cancelled_at + cancellation-title enrichment); log/captures.md (reverse-Kano promotion candidate; live-smoke cancelled_at-churn finding); shared_kernel/broadcast_flow.py (SCHEDULED_EVALUATION); contexts/threshold_briefing/** (domain: threshold_rule/meeting_state/rule_match/evaluation/crossing/response; application: threshold_evaluator/threshold_briefing_implementer/rule_config + ports active_rule_refresh/calendar_state_reader/threshold_crossed_emitter/threshold_briefing_composer/threshold_notifier; adapters/llm composer + prompt); contexts/messaging/domain/idempotency.py (SCHEDULED_EVALUATION + THRESHOLD_CROSSED keys); contexts/messaging/application/audit_events.py + fire_trigger.py (metadata capture); apps/api/_threshold_briefing_wiring.py; apps/api/main.py (registration); .importlinter (layers-threshold-briefing); tests/unit/contexts/threshold_briefing/**; tests/unit/contexts/messaging/domain/test_idempotency.py; tests/unit/contexts/messaging/test_broadcast_initiated_audit_event.py; tests/unit/shared_kernel/test_broadcast_flow.py; tests/contract/broadcast_flow/test_threshold_*; docs/smoke/p15_s57_threshold_briefing.md; log/sessions.md (this entry)
  corrects:
  corrected_by: P15 audit (docs/audit/p15-audit-evidence.md) + the [P15-audit-residue] corrective session 2026-06-03 (idempotency fallback + matched_audit_event_id/cancelled_at residue)
```

## 2026-06-03 — [P15-audit-residue] Corrective: THRESHOLD_CROSSED idempotency + audit residue sweep
roles: engineer, technical writer
mode: corrective build session (no new S-number, matching the S55a-fix precedent). HEAD at open: `781ffa1` (the P15-audit charter hygiene-sweep tip). Remediates the code residue the P15 audit surfaced (`docs/audit/p15-audit-evidence.md`) plus the latent `idempotency.py` fallback bug; the charter-only half (D155 + the hygiene sweep) landed first at `1d64d28`/`781ffa1`. Classification **corrective**: it fixes shipped stale residue and a latent re-introduction of a fixed bug, booked honestly into the CFR ledger.

- **Pre-write reconciliation — the fallback decision was delete-vs-fix and was reconciled against current code, not guessed.** Traced the THRESHOLD_CROSSED metadata path from emitter through FireTrigger to `resolve_idempotency_key`. Two producers reach the resolver: (1) the in-process emitter `apps/api/_threshold_briefing_wiring.py:182-187` always sets `crossing_identity` via `RuleMatch.to_trigger_metadata`; (2) **the HTTP trigger endpoint `apps/api/routers/triggers.py:62,152-157`** accepts `FireTriggerRequest.trigger_type` over the *full* `BroadcastTriggerType` enum (THRESHOLD_CROSSED included) with a caller-supplied open `metadata` dict that need not carry `crossing_identity`. Because producer (2) makes the fallback genuinely reachable, deleting it would crash a structurally-permitted request → **reconstruct, not delete**: the fallback now mirrors `RuleMatch.crossing_identity()` (cancellation `rule_id:google_event_id`; conflict `rule_id` + sorted pair), never embedding `cancelled_at`. (Grep evidence: `resolve_idempotency_key` callers = `fire_trigger.py:98` + the `triggers.py` import; the only THRESHOLD_CROSSED TriggerContext constructors are the wiring emitter and the HTTP router.)

- Produced (charter ahead of code, four commits):
  - **Commit 1 (`829582e`)** `docs(charter): captures — D153 residue-propagation + latent-fallback lesson` — the [P15-audit-residue] capture: the correction-sweep-checklist candidate and the dead-or-defensive-path latent-bug observation, with the reconciliation finding recorded.
  - **Commit 2 (`be829bc`)** `fix(p15/audit): stabilise THRESHOLD_CROSSED idempotency off cancelled_at` — `idempotency.py` fallback reconstructs the stable identity (no `cancelled_at`); docstrings corrected; `test_idempotency.py` gains an SSOT-conformance test binding the resolved key to `RuleMatch.crossing_identity()` (incl. a churned-`cancelled_at`-re-keys-the-same assertion) and a reconstruction test, and the two fixtures that baked in the `cancelled_at`-bearing shape are corrected.
  - **Commit 3 (`99c8920`)** `docs(p15/audit): sweep matched_audit_event_id + email-future docstring residue` — `broadcast_flow.py` + `test_broadcast_flow.py` (off `matched_audit_event_id`), `conversation_flow.py` (four-type union names `email`), `sync_calendar.py`/`sync_email.py` (the D155 no-audit-events note).
  - **Commit 4 (`<this commit>`)** `docs(p15/audit): session log + close` — this entry.

- Tests: `make lint` from cleared bytecode → import-linter **41/41 kept**, AST enforcement green (9); `tests/unit tests/contract` from cleared bytecode → **exit 0** (the py3.14/pytest non-TTY quirk suppresses the final tally line, but exit 0 confirms zero failures; the `test_idempotency.py` subset shows 13 green incl. the new SSOT-conformance + reconstruction tests). The three known docker-dependent live-infra failures in the full tier carry forward unchanged, not in scope, not introduced.

- Reflection prompts answered:

  1. **The correction-sweep lesson.** D153 reached the code SSOT (`rule_match.py`) and the rewritten prose but not five secondary surfaces, surfacing only at a dedicated audit. This is **distinct from pre-write reconciliation**: reconciliation guards the *new* code against a wrong assumption before writing; a correction sweep guards the *old* shape's *removal* after a correcting decision lands. Its checkable tell is concrete — before closing any session whose deliverable is a correction, grep the old shape's identifier (here `matched_audit_event_id`, `cancelled_at`-in-identity) across `contexts apps shared_kernel tests charter`. Promotion is **held, not taken**: D149 was the first reconciliation correction, D153 the second; the candidate meets the bar at a third instance leaving similar residue, or on structural-novelty grounds at the Phase 2-A close audit (strategic mode owns the promotion per D47). Recorded as a captures candidate, not promoted here.

  2. **The latent-fallback finding.** The fallback was **defensive, not dead** (the HTTP path reaches it), which makes the bug worse than a dead branch: it is a live-reachable path the green suite *actively asserted in its buggy form* (`test_threshold_crossed_falls_back_to_composite_when_identity_absent` checked the `cancelled_at`-bearing key). So the suite did not merely miss the bug — it locked it in. This is the **same face** as the stale-bytecode enforcement masking and the S57 tombstone churn: each is a green that does not exercise the real path (stale `.pyc` → contract never actually run; stubbed store → runtime churn never seen; a fixture asserting the wrong shape → the corrected shape never demanded). All three are instances of the **live-substrate / real-path verification gate** candidate. The distinct sub-lesson here: a unit test can be *worse* than absent when its fixture encodes the bug as the expected value — so the corrective test now binds to the cross-context SSOT (`RuleMatch.crossing_identity()`) rather than to a hand-written expected string, which a future shape change would force to update in one place.

  3. **Corrective classification.** This is the **second `corrective` session** after the ChannelResolver corrective (the two-threshold-rule retraction). The CFR ledger now carries two corrective sessions; both originate in front-loaded-abstraction or correction-propagation drift caught by a later gate (reconciliation, audit) rather than by the green suite — a pattern worth the Phase 2-A close audit's attention when it reviews the corrective rate against the build rate.

methodology: The corrective work confirms a layered-gates picture the P15 arc has been drawing: the **reconciliation gate** catches static wrong assumptions before the write (S57: audit-chain polling), the **live smoke** catches runtime-substrate assumptions before close (S57: `cancelled_at` churn), and the **audit pass** catches correction-propagation residue after close (this session: five stale surfaces + the latent fallback). Each gate catches a class the prior one structurally cannot. The new candidate — a correction-sweep checklist — is the natural fourth, folding the audit's residue-finding back into the closing session that makes the correction, so the audit becomes a backstop rather than the primary catch. Held for strategic-mode promotion per D47.

- Close verdict: the THRESHOLD_CROSSED idempotency key carries no `cancelled_at` and no `matched_audit_event_id` on any path (the fallback reconstructs the stable identity, reachable via the HTTP endpoint), bound to the `RuleMatch` SSOT by a new unit test; the `matched_audit_event_id` / email-future docstring + fixture residue is swept from `shared_kernel` and the broadcast/conversation tests; the two sync entry points carry the D155 note. `make lint` 41/41 + AST green and unit+contract exit 0 from cleared bytecode. The two methodology candidates (correction-sweep checklist; live-substrate-verification gate) and the dogfooding-gate watch items remain for strategic mode / the Phase 2-A close audit.

```
metrics:
  classification: corrective
  brief_started: 2026-06-03
  session_started: 2026-06-03
  session_closed: 2026-06-03
  merged: 2026-06-03
  close_state: clean (four commits; charter/captures ahead of code; idempotency fallback reconstructs the stable identity off cancelled_at, bound to the RuleMatch SSOT by a new test; matched_audit_event_id + email-future docstring/fixture residue swept; D155 sync notes added; reconciliation outcome reconstruct-not-delete recorded with grep evidence)
  tests_passing: tests/unit + tests/contract exit 0 from cleared bytecode (final tally line suppressed by the py3.14/pytest non-TTY quirk; test_idempotency.py subset 13 green incl. new SSOT-conformance + reconstruction tests); import-linter 41/41 kept; AST enforcement green (9); three known docker-dependent live-infra failures carry forward, not in scope, not introduced
  principles_intact: yes (D155 now scopes the audit-trail principle the sync notes cite)
  charter_touchpoints: log/captures.md ([P15-audit-residue] entry); contexts/messaging/domain/idempotency.py (fallback reconstruct + docstrings); tests/unit/contexts/messaging/domain/test_idempotency.py (SSOT-conformance + reconstruction tests; stale fixtures corrected); shared_kernel/broadcast_flow.py (TriggerContext docstring off matched_audit_event_id); tests/unit/shared_kernel/test_broadcast_flow.py (fixture keys); shared_kernel/conversation_flow.py (four-type union docstrings name email); contexts/calendar/application/sync_calendar.py + contexts/email/application/sync_email.py (D155 no-audit-events note); log/sessions.md (this entry)
  corrects: the D153 audit-chain→state-store correction's propagation residue (the matched_audit_event_id/cancelled_at shape left in shared_kernel/broadcast_flow.py, shared_kernel/conversation_flow.py docstrings, and the test fixtures after S54/S55a/S56a/S57) + the latent idempotency.py fallback that re-introduced the S57 cancelled_at-churn bug
  corrected_by:
```

---

