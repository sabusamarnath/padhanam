# Phase 1 Packages

Historical static reference per D44. The canonical living strategic-tree artefact is [charter/roadmap.md](roadmap.md), versioned with reasoning categories per change. This file is retained for direct package-list reference and is updated at phase boundaries; mid-phase content can drift from as-built reality (the P2 line drifted from S4 to 2026-05-06; corrected at the carryover-cleanup strategic session per D52, with the drift recorded as the first entry in `charter/methodology.md`'s Failure modes section). RICE scores and version log live in `roadmap.md`.

The work breakdown for Phase 1. Order reflects dependency and learning value.

- **P1: Scaffold.** Repo, charter and log structure at repo root, README, .gitignore, .env.example, Makefile, incremental Compose stack (services land per package per D11), mkcert HTTPS fronting the stack.
- **P2: First LLM call.** Langfuse 3 in Compose behind Caddy, security baseline (`padhanam/config/`, `padhanam/security/`, audit-context scaffold, supply-chain hardening), Ollama and LiteLLM in Compose with OTel-native traces, FastAPI skeleton with bounded contexts and uv workspaces, auth middleware with signed-token dev backend per D23, the platform rebrand at S8. Identity foundation (Keycloak realm, OIDC, SAML SP, SCIM 2.0, federated session management) deferred to Phase 2 per D52, in supersession of D3.
- **P3: Tenancy primitives at enterprise grade.** Tenant registry on a dedicated control-plane Postgres instance, per-tenant database connections to per-tenant Postgres instances, two-phase Alembic migration runner (control-plane and per-tenant tracks), real audit context adapter with hash-chained append-only storage, credential encryption from inception via envelope encryption.
- **P4: LLM gateway.** LiteLLM-backed clients, trace capture middleware, OpenTelemetry GenAI conventions, self-hosted Langfuse wired up. Cost capture from traces lands here per D41 (pricing table in `padhanam/config/inference.py`, OTel attribute extension joining token counts to USD, per-tenant cost-attribution column added to the tenant registry as an early Alembic migration).
- **P5: Evaluation harness.** Canonical interaction set storage, replay engine, deterministic and LLM-as-judge scoring, regression reporting. Cost-per-successful-task metric implementation per D8 and D41.
- **P6: Source ingestion.** Upload, two-track pipeline (vector to pgvector, entity extraction to Neo4j), retrieval interfaces.
- **P7: Agent CRUD.** Name, system prompt, source IDs, tool allowlist, retrieval strategy, model selection.
- **P8: Agent runtime.** AgentLoopExecutor behind AgentExecutor interface. SSE-streamed responses. Full instrumentation. Workflow context architecture committed for Phase 2 implementation per D83.
- **P9: Run history (backend-only).** Run history data substrate, query APIs, citation linking back to source chunks and graph entities at the data layer. Replay UI deferred to Phase 2 per D92.
- **P10: Audit log viewer (backend-only).** Audit log query infrastructure and APIs. Tenant-owner UI deferred to Phase 2 per D92.
- **P11: Optimization dashboard (backend-only).** Recommendation engine, trace inspection APIs, evaluation results and active test report APIs. Recommendation-shaped, with cost-aware recommendations as a first-class surface per D41. Dashboard UI deferred to Phase 2 per D92. **Closed 2026-05-16** after six build sessions (S39 + S39b + S40 + S40b + S41 + S42) plus pre-P12 hygiene; D108 through D112 landed; the four-context substrate scaffold complete (`contexts/retrieval_evaluation/`, `contexts/run_history/` for the run-history reads, `contexts/audit/` for the audit reads, `contexts/optimization/`) with HTTP transport surface for all four; the bet's success criterion 4 holds at substrate level.
- **P12: Phase 1 close audit (audit-mode session).** Eleven-commit strategic-mode synthesis session at 2026-05-16. Three audit tracks (top-down D-entry verification, bottom-up codebase tour, fourteen audit-inputs entries dispositioned plus three audit-surfaced entries). Outputs: `charter/p12-audit-findings.md` consolidated deliverable; D113 supersession of D39's pending-authorship framing; principles.md line 45 refresh; three deferred-decisions refreshes; methodology document v2 substantive update with Session shapes sub-section and six new Patterns-observed entries; `charter/p12-phase-2-inputs.md` strategic input set for Phase 2 packaging. Audit verdict: Phase 1 closes with the bet's load-bearing claims holding; Phase 2 inherits a complete substrate to build against per D93's methodology-as-product positioning. The originally-planned P12 active testing scheduler scope reframes as Phase 2 candidate per the audit's deferral disposition: active testing depends on a recommendation-engine consumer that surfaces evidence demanding it, which Phase 2 UX consumer surfaces will surface. The audit-shape session replaces the originally-planned active-testing build session at Phase 1 close because the bet's substrate-completion claim closes structurally with P11 plus audit, and active testing's load-bearing surface is consumer-facing recommendation surface that Phase 2 ships.

Phase 1 ends after P12 with a phase audit per `charter/methodology.md`. Phase 2 direction is methodology-as-product per D93, with focus purely on UX/UI on top of Phase 1's complete substrate. Specific Phase 2 package shape decided at the Phase 1 close audit.

# Phase 2 Packages

Phase 2 ships methodology-as-product UX on top of Phase 1's complete substrate per D93 and per the Phase 2 design 7-Step arc at `charter/phase-2-design-7step.md`. Originally eight packages across two stages per Step 6 Pass 2 LVT placement; extended to twelve at roadmap v8 (the Phase 2-A dogfood gate revealed its threshold sits above the thin slice — a gate-driven correction). Phase 2-A delivers the operator-validated find-rhythm-and-settle-in instance in two waves (P13-P20): Wave 1 (P13-P16, the thin slice) and Wave 2 (P17-P20, the plan-side build that reaches the gate). Phase 2-B scales to broader senior-leader-ICP (P21-P24). **The v8 old→new mapping (`charter/roadmap.md` version log) is the single source of truth for the B-stream renumber; historical references to the B-stream's prior P17-P20 numbers in D-entries and design records are left verbatim and read against that mapping.** User segment commitment at `charter/phase-2-user-segment.md`; six architectural primitives at `charter/architecture.md` Phase 2 architectural primitives section; sixteen carry-forward question dispositions plus integrated storyline at `charter/phase-2-design-7step.md` Step 6 section. Concretised at D156: the Phase 2 UX surface is a whole-life causal daily driver for the first-instance senior-leader ICP (a prioritised today surface plus a causal decision map, two views of one graph), active rather than passive, spanning professional and personal because the causal chains do. The bet and the ICP are unchanged.

## Phase 2-A: Operator-validated find-rhythm-and-settle-in instance

- **P13: Foundational substrate (Wave 1).** 1.3 State persistence; 1.1 manual entry cell. Latency-tier routing extension at LiteLLM port per D122; Twilio Sandbox setup plus messaging adapter scaffold per D119; Revisable Protocol defined per D114; ConversationFlow Protocol defined per D115; no-silent-operation principle commits at `charter/principles.md` per D121; tiered-by-salience plus two-vector decay plus three-tier consent-and-awareness D-entries plus architecture.md additions per D116, D117, D118. Phase 2 PRD entry at `charter/p12-phase-2-inputs.md` substrate-completion handoff.

- **P14: ConversationFlow implementers — audit-conversation + mirror-conversation (Wave 2; CLOSED at S52).** Phase 2-A read-loop substrate per P14 framing 2026-05-26 (narrow-scope revision of the original Wave 2 line; the prior calendar-read, email-read, goal-state-tracking, status-veracity, intelligence-layer-guardrails, and methodology-library work-streams defer to P15+ per the activation triggers at `charter/deferred-decisions.md`). Audit-conversation per 5.1 at `contexts/audit_conversation/` committed at S51 composing the existing `AuditEventReader` port (S36) with D137 intent classification and D131/D135/D138 response composition; mirror-conversation per 4.1 at `contexts/mirror_conversation/` committed at S52 composing portfolio read-side ports with D137 intent classification, drill-down stateless-per-turn against conversation history with ConversationFlow cell-payload persistence per D141, and D131/D135/D138 response composition. Two build sessions: S51 (CitedResponse Protocol + ArtefactCitation at shared_kernel + audit-conversation implementer + structural-test contract harness extensions; the S51 brief's separate AuditQueryPort plus Postgres adapter plus Alembic migration was dropped at framing per pre-write reconciliation Finding 1 because the S36 AuditEventReader covers the seven filter dimensions plus cursor pagination plus chain-integrity verification) and S52 (meta-classifier dispatch substrate per D140 + mirror-conversation context end-to-end + cell-payload persistence per D141 + WhatsApp render extension + contract-harness extension + meta-classifier and mirror-conversation gold sets as third and fourth instances of the parameterised D137 substrate + P14 close marker). Calendar-read, email-read, daily-briefing, threshold-briefing, outbound initiation defer to P15+. No D136 multi-channel UX primitive activates at P14 beyond the target_cell extension of Primitive 3 at D140. P14 closes the bet's read-loop substrate; P13 closed the write-loop substrate; P15 lands outbound initiation plus intake-side integrations.

- **P15: Outbound initiation plus intake-side integrations (medium scope).** Outbound initiation substrate: BroadcastFlow Protocol at shared_kernel (D142); BroadcastDispatch substrate at messaging context (D143); ChannelResolver Protocol with static-config adapter (D144); HTTP trigger endpoint substrate (D145). Daily-briefing and threshold-briefing BroadcastFlow implementers. ThresholdEvaluator (non-user-facing BroadcastFlow implementer for periodic state-change matching). Calendar-read intake substrate at `contexts/calendar/` plus calendar-conversation ConversationFlow implementer at `contexts/calendar_conversation/`. Email-read intake substrate at `contexts/email/` plus email-conversation ConversationFlow implementer at `contexts/email_conversation/`. MetaClassifier extends from three-way to five-way routing. ArtefactCitation discriminator extends from Case/DataPoint to include Meeting and Email. External tool services consumed via Nango self-hosted (operator-controlled infrastructure; D14 separate-service pattern preserved). Five sessions estimated: S53 (substrate foundation); S54 (HTTP endpoint plus daily-briefing); S55 (calendar substrate plus calendar-conversation); S56 (email substrate plus email-conversation); S57 (threshold-briefing plus ThresholdEvaluator plus P15 close).

- **P16: Wider Phase 2-A surfaces (Wave 4).** 1.1 Slack messaging trio (write, observe-status, observe-incoming); 2.1 methodology library activation (matching, recommendation, adaptation flows); 3.1 Surfacing mechanics; 1.5 user-authored items; 3.2 drop-decision support. ConversationFlow Protocol exercised across newly-added surfaces. Reframed at D156 as the first daily-driver surface: the surfacing mechanics, user-authored items, and drop-decision support render the first instance of the prioritised today surface. (Originally carried the Phase 2-A close gate; v8 moved the gate to the end of Wave 2, because the dogfood revealed the thin slice is below the usable threshold.)

### Phase 2-A Wave 2: the plan-side build (added at roadmap v8)

Padhanam as the assessment layer over the work tools the user already keeps (the assess-not-replace principle; D166). Goals are seeded by script (the German/get-a-job precedent), so a goal-capture UI is not on the critical path (deferred to Phase 2-B, shaped by use). RICE pass, build sequence, and the dogfoodable-after-P19 threshold at `charter/roadmap.md` Initiative 2 Wave 2 subsection. The Phase 2-A close gate (dogfooding plus the senior-leader-ICP read) sits at the end of this wave. **Precondition: S64 test-integrity** (the clock-seam and mock-blindness fixes), outside the RICE pass, runs first or alongside.

- **P17: Task ingestion — Google Tasks (Wave 2, the spine's entry point).** The TASKS_READ intake source and the Google Tasks adapter behind the existing intake-source and Nango seam, read-only, per the D148/D155 external-cache model; reuses the connected Google provider (D148/D151/D161) as an added OAuth scope rather than a new connector. Depends on nothing new. The task-tool ingestion deferred-decisions entry (Google Tasks first) activates here.

- **P18: Unit-of-work correlation (Wave 2, the substrate).** Assembles a unit of work from its facets — the ingested task, the existing calendar block, the email-origin where present — and links them with the D166 Padhanam-native edge, inferred by title and time proximity, high-precision and confirm-rather-than-assume, never written back. The substrate everything downstream rides on. Depends on P17.

- **P19: Goal-aligned assessment — the moat (Wave 2, the delighter).** The two reads only the goal facet makes (D166): orphan work that points at no named goal, and the neglected goal that nothing in the plan advances. The D166 Kano delighter — the reason Padhanam is not a rebuilt Motion. **The dogfoodable threshold sits after P19:** goals seeded, tasks ingested, units correlated, assessment live → the operator runs a real day through it. Depends on P18 and the seeded goals.

- **P20: Missing-facet suggestions (Wave 2, the parity layer).** The recommendation engine: a substantial task with no time gets a block suggestion, an event with no task gets a satellite-work suggestion, an email request becomes a candidate task — carrying the credulity discipline (selective and confident, or silent; the S62 smoke's flip-on-one-observation risk). Parity value (the time-blockers ship this); lands just after the threshold rather than gating it. Depends on P18.

## Phase 2-B: Scale to broader senior-leader-ICP (renumbered P21-P24 at roadmap v8)

**Phase 2-A's close gate opened on the engine, not the experience** (gate audit, 2026-06-09; roadmap v9, the canonical record). The moat is trustworthy on real data; the experienced signal is thin at current goal density — a seeding/coverage problem, not an engine problem. Phase 2-B therefore opens with a strict **thicken → live → clothe** sequence:

- **E1 — thicken (by seed).** Script-seed professional and personal goals to raise SERVES-edge density; an **opening sequence ahead of P21, not a numbered package**, with **S74 its first story**. The already-built Google Tasks pull (S65) is activated; Trello stays the decoupled later adapter. A seeded-goal-coverage density trigger (professional broken out, not a global orphan rate) gates entry to E2.
- **E2 — the lived-use week.** A **gate-milestone**, not a build epic: the lived-use validation condition 1 demands, on the thickened signal, before any clothing, against a pre-committed falsifiable verdict. Its week-gate trigger is gate-audit output 3, **ratified at roadmap v10 after S74** measures achievable professional coverage.
- **E3 — clothe.** The experience layer (the daily-driver surfaces D156 places in the B-stream below); internal ordering and its priority against the B-stream packages defer to a **post-week RICE pass** (pre-ordering now re-commits the structure the week exists to teach).

**The P21–P24 B-stream below is provisionally preserved, not endorsed.** Those methodology-platform packages predate the attentional-assistant pivot; whether they survived it resolves at the post-week RICE pass, when E3's experience-layer packages are prioritised against them. Any re-home to Phase 3 or retirement is a deliberate later decision with its own log entry — never folded silently here. The B-stream is **not renumbered again** (the v8 old→new mapping stands verbatim). The connected-calendar source dependency (work calendar needed for confirmed professional coverage) is named at roadmap v9, surfaced by S74 Step 0.

Per D156, Phase 2-B absorbs the daily-driver epics into the B-stream: cadence-with-staleness (the
must-have active-surfacing primitive on the threshold engine per D153), scheduled-with-lead-time
(due dates and lead-time pull-forward), the causal-decision-graph context (Factor and CausalLink
over the Neo4j graph per D5/D63, qualitative propagation first), the Case-substrate exercise
across the ICP's tracked types (the CDA-beyond-portfolio deferral), and the Day concept (persisted
daily ordering and done marks). Specific package assignment and RICE ordering settle at Phase 2-B
sequencing; cadence-with-staleness and the causal-graph context lead. The four B-stream packages
below renumber P17-P20 → **P21-P24** at roadmap v8 (the v8 old→new mapping is the single source of
truth; their internal B-stream content is unchanged). **Roadmap v8 also routes the plan-side
build's deferred scope here, shaped by the dogfood:** the five D166 constraint dimensions
(dependency, deadline, capacity, recurrence, waiting-on-others); the goal-capture UI (deferred by
the seed-first reframe); Trello ingestion (the second task adapter, the hardest assess-not-replace
proof); personal-chat ingestion (connector-blocked for iMessage / personal WhatsApp); and
write-back (a fresh decision against the read-only posture, if ever wanted). None scored now; the
dogfood verdict prioritises them.

- **P21 (was P17): B9 elevated plus foundational substrates (Wave 1).** B9 methodology authoring extensions running in parallel (role-extensions per methodology-extension pattern plus skills-per-role surface per D120, per D123 elevation); B10 measurement substrate operationalisation (aggregation pipeline); B1 partial (highest-priority substrate expansions per dogfooding evidence); B2 partial (multi-device sync implementation kicks off); Twilio Production verified-business plus template-approval transition per D119.

- **P22 (was P18): Operational delivery plus agent-runtime exercise (Wave 2).** B1 remainder; B2 remainder (conflict-resolution, audit/surfacing extensions for multi-device); B3 partial (information-based freshness per D118 information vector, identity-fork mechanism per the deferred-decisions entry); B4 partial (voice channel paired with B1 voice substrate, preference expansions, per-message review surface); B6 partial (per-action-class cadence configuration, cross-channel digest delivery, reclassification mechanism); B7 partial (per-user customised thresholds); B8 partial (per-device authoring optimisation); plus agent-runtime exercise of McKinsey 7-Step end-to-end per Step 6 Pass 1 Q15 disposition (closes the bet's higher-bar methodology-embedding claim within Phase 2 alongside criterion-4 demonstration).

- **P23 (was P19): Accumulated-history wave (Wave 3).** B3 remainder (2.2 Methodology-to-item binding, 2.4 User-authored methodology surface, 6.4 Methodology-fit lifecycle activations); B4 remainder (pattern-based triggers, settle-in adaptation); B5 (reflection layer extensions; pattern surfacing per 4.4; value-versus-time accounting per 4.3; audit narrative density); B6 remainder (learned migration thresholds, sub-class granularity automation, combined-signal detection, per-class consent refinement per 3.5); B7 remainder (pattern-based status suggestions, status-narrative learning); B8 remainder (learned parsing improvements); B10 remainder (threshold monitoring and alerting, operator-facing measurement-review surface).

- **P24 (was P20): Phase 2-B late or Phase 3 boundary (Wave 4).** B1 remainder (work-app cells beyond operator stack); B7 conditional (watching, delegated additions); B9 second wave (skills-per-role surface refinement if not landed earlier); B5 conditional (normalised value units if Phase 2-A per-methodology friction). Phase 2-B Wave 4 versus Phase 3 boundary settles per the deferred-decisions entry when approaching this wave.

## Phase 3 candidates (tagged at Phase 2 close)

Vertical-wedge work in financial services, legal, healthcare per senior-leader-ICP refinement at `charter/phase-2-user-segment.md`. Meta WhatsApp Cloud API direct as second messaging path per D119. Foundation-model defence work per the Nerve acqui-hire structural signal in `charter/competitors.md`. End-of-Phase-3 competitive landscape review. Sub-problems 5.2 Source attribution, 5.3 Cost transparency, 5.5 Trust history (not in Step 5's Phase 2-B map; likely Phase 3). Sub-problems 4.5, 6.1, 6.2, 6.5 (Tier 4 deferred without specific Phase 2-B trigger per Step 5 activation map).
