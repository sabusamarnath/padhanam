# Current Package

Active package details. Updated when a new package starts. Archived to `docs/archive/packages/` at package close.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S102 — per-goal LLM-draft + user-proof, with provenance (build mode) — IN FLIGHT

**The first story under the v13 epic: the CDD's meaning becomes authored.** S101's dogfood showed a generic, auto-derived, read-only CDD loses meaning; D200 pivoted to authored-per-goal. **S102 lands the authoring layer (draft + proof + provenance only).** Artefacts at close: (1) a Neo4j migration `0005_authored_cdd.cypher` adding `:Intermediary` + `:External` node types, the `:Lever` extension (a stable `lever_id`, nullable `commitment_id`, `provenance_origin`/`proof_state`/`name`), and the authored `FEEDS`/`INFLUENCES` edges — idempotent, tenant-scoped, behind the `TenantScopedNeo4jSession` wrapper; (2) a per-goal **CDD draft** use case calling `StructuredOutputPort.generate_structured` against the Ollama dev model for each of the 8 live goals, persisting each element `provenance_origin=llm_drafted`, `proof_state=pending`; (3) a **proof** read + accept/reject/correct write paths (correct flips to `user_authored`, reject removes — user-initiated delete allowed); (4) a **proof surface** on `/app` beside the S101 List/Map toggle; (5) `schema.md` authored-layer section (charter-first); (6) green suite incl. tenant-isolation on the new node types + a schema-shape enforcement test tying the migration constraints to the live shape.

**Step 0 reconciliations (live tree):** latest Neo4j migration `0004` → new `0005`; node/edge inventory confirmed (`:Outcome`/`:Lever`/`:Unit`/`:Facet`; `LEVER_FOR`/`SERVES`/`SAME_WORK`); `lever_unique_per_tenant` on `(tenant_id, commitment_id)` confirmed (reconciled: nullable `commitment_id` exempts drafted levers; new `lever_id_unique_per_tenant` carries authored identity — Neo4j exempts nodes missing a constraint property, so both coexist, **brief-altitude, no D201**); `StructuredOutputPort.generate_structured(StructuredOutputRequest{prompt, schema, latency_tier, temperature?, model_hint?}) -> StructuredOutputResponse[dict]` confirmed (the **checkin** context is the exact consumer precedent: domain holds the schema dict + pure mapping, adapter wires the port); wrapper/`neo4j-confined`/`test_no_raw_neo4j_session.py` all bind. **Postgres untouched** (authored CDD is graph-only); Step-0 correction: tenant Alembic revisions are **date-prefixed (`2026…`)**, not `0038` as the brief expected — immaterial here, recorded.

**Scope cut:** author + proof + provenance only. **Out:** the matcher rewrite from goal-linking to element-evidence (S103); the ongoing edit loop + correction-as-learning-signal (S103, v13 increment 2); the co-evolving suggestion loop (increment 3); cross-goal edges/traversal/ripple (D162 post-week); the Flow/process view + portals + `step_state`-to-status (D198 post-week, alongside the authored sequence-step levers). The matcher (`correlate_goal_facets`, the `SERVES` write) is **untouched** this session; no correction is captured as learning signal yet.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103a — authoring completion: surface the outcome, add elements, reclassify across types (build mode) — IN FLIGHT

**Roadmap v13 increment 2's authoring half: the model becomes fully authorable.** S102's proof pass surfaced three gaps that block a usable authored model, all closed here over the S102 layer with **no matcher change**: (1) the drafted expected outcome was persisted but not proofable; (2) no way to **add** an element (the draft produced zero externals across all 8 goals, so externals could not enter at all); (3) no way to **reclassify** a mis-typed element. S103b's matcher rewrite needs this finished authorable model as its target — the same dependency that set the S102 line.

**Goal as artefacts:** (1) the drafted expected outcome renders as a proofable terminal element (accept/edit/reject) — schemaless `authored_outcome_proof_state` + `authored_outcome_origin` props on `:Outcome`, read coalescing the 8 S102 drafts to `pending`/`llm_drafted`; (2) an **add** path creating a user-authored element of any of the four types (`user_authored`/`accepted`, default edge to the outcome), the path by which externals enter; (3) a **reclassify** path per **D201** — preserve node + stable id (moving the value between `lever_id`/`element_id`), flip origin to `user_authored`, flag (never drop) now-invalid incident edges (`needs_review`); (4) add + reclassify + outcome-proof affordances on the CDD lens; (5) tests (add origin/proof, reclassify identity+flag-not-drop, outcome, tenant isolation on the new writes, an edge-grammar guard).

**Step 0 reconciliations (live tree, all confirmed):** migration latest **0005** — **no 0006 needed** (the outcome proof/origin props and the edge `needs_review` flag are schemaless, no constraint); element **type is a node label** (`:Lever`/`:Intermediary`/`:External`), so reclassify is a **label swap with an id-property move** — the `_AUTHORED_NODE` whitelist keys `:Lever` by `lever_id` and `:Intermediary`/`:External` by `element_id`, so crossing the lever boundary moves the id value, not just the label (the wrinkle the brief flagged "correct if wrong"); the drafted outcome **is persisted** as `o.authored_expected_outcome` but with no proof/origin — Commit 1 adds them; the S102 accept/reject/correct paths + `DAILY_DRIVER_CDD_READ`/`WRITE` (D126) confirmed, add/reclassify extend them, **no new permission**; CDD lens on `apps/api/static/daily_driver.html` beside List/Map (`data-mode="cdd"`); deploy idiom `make build-api` (digest pin), live pass operator-gated (S101 idiom). **Postgres untouched** (graph-only). **schema.md changed** (outcome proof/origin props, edge `needs_review`, the S103a authoring-completion note); **D201 minted** (the reclassify correction semantic — framing, per the brief).

**Scope cut:** surface the outcome, add, reclassify only. **Out:** the matcher rewrite to element-evidence (S103b); the relink/unlink of ingested signal + correction-as-learning-signal (S103b); the emergent suggestion loop incl. externals-emergent (S104); strengthening the externals draft prompt vs routing to emergent (decided by reflection-1's tally, lands in S103b/S104); the full edge-edit/re-point loop (the flagged edge is surfaced, not yet resolvable in-place — S103b). The matcher (`correlate_goal_facets`, the `SERVES` write) is **untouched**; grep-confirmed in the diff.

**S103a-fix (surface-only patch).** The operator-gated S103a browser pass found two surface gaps over paths that already work: (1) the S102 correct-and-edit affordance was present but unrecognisable (a `window.prompt`-based ghost button), so the only perceived correction was reject-then-re-add, which breaks element identity + incident edges before the matcher binds; (2) "draft missing CDDs" reported a silent zero when every goal already had a CDD, reading as failure not the correct result. Fix: a clear **in-place** edit control (inline input + Save/Cancel, on each element and the outcome) wired to the existing `/correct` path, and a clear "all goals already have a CDD" empty state reading the existing `skipped_existing` zero-count. **Surface-only** — no schema, no migration, no new endpoint or permission, no matcher; **no D-entry** (exposing an existing path is not a new decision). **Step 0 correction:** the brief framed the edit control as possibly absent; it was present-but-unrecognisable, so the fix made it clear rather than adding a path.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103b — the element-evidence matcher: signal binds to authored elements, not goals (D202) (build mode) — IN FLIGHT

**Roadmap v13 increment 2's learning-signal half (binding), executing D202.** The authoring half (S103a + fix) is closed; this reroutes ingested work onto the authored elements it produced. **Spine:** a unit binds to the nearest authored element *within* its goal via lexical+alias recall applied to per-element targets (the per-element extension of the goal-level recall); multi-attach (a unit may evidence >1 element); goal-level evidence **derived on read** from element evidence, the goal-level `SERVES` write **retired** (D155, no drift); a unit matching no element **parks unbound** as the emergent loop's queue (S104), never dropped.

**Framing fork settled in conversation (Step 0 surfaced it; the operator's call).** Two of the brief's spine assumptions did not hold against the live system, and one scope call was made: **(1) No direction this session.** Direction fields (email from/to, calendar organiser/attendee) are ingested at source but not projected into `WorkFacet`, and — the real blocker — no user-identity reference is stored to orient inbound-vs-user-initiated (`Connection` carries no account email, `MeetingAttendee` no self-flag). Direction's only consumer is the emergent loop and with one authored external it separates almost nothing, so direction + the lever/external orientation + its configured-account-identity (**D203, reserved**) all move to **S104** — a signal lands where it informs a decision (the observability rule). **(2) No embedding tier.** The live matcher (`infer_goal_edges`) is lexical+alias only — there is no embedding tier here — and the email-embedding corpus is empty (S100); so S103b extends lexical+alias to per-element targets honestly and names the embedding tier as later work. **(3) The split.** S103b lands the rewrite + re-match + derive-on-read + unbound bucket + a **read-only** element-evidence/unbound display; relink/unlink + correction capture become **S103c** (with the `contexts/audit/` event path); direction + the emergent loop are **S104**.

**Step 0 reconciliations (live tree):** matcher = `correlate_goal_facets` → `infer_goal_edges` (lexical lever-name + alias, **no embeddings**); the SERVES write is `replace_goal_edges` and the three readers (`list_units_by_goal`, `list_goal_assessment`, `list_facet_suggestions`) consume `list_goal_edges` → `GoalEdge`; `group_units_by_goal`/`goal_status` take `edges` as a **parameter** (pure projections), so derive-at-`list_goal_edges` leaves them byte-unchanged; element ids per S102 (`lever_id`/`element_id`/outcome node); live corpus = **669 SERVES / 1359 units / 31 authored levers + 29 intermediaries + 1 external**; append-only store = `contexts/audit/` (daily_driver emits none today → S103c). Next migration **0006**. D202 + D200 dual-nature addendum minted charter-first; D203 reserved for S104.

**Scope cut:** rewrite + re-match + derive + unbound bucket + read-only display only. **Out:** relink/unlink + correction capture (S103c); consuming corrections to adjust matching (the learning half, next); direction + config-identity + the emergent loop (S104); the embedding recall tier (named, S100 empty corpus); draft-strengthening for external category slots (D200 addendum, deferred). The authored layer (`FEEDS`/`INFLUENCES`, the proof paths) is untouched.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103c — editability and the learning signal: relink, unlink, capture, re-runnable re-match (D203) (build mode) — IN FLIGHT

**Increment 2's editability half, completing S103b.** S103b bound signal to elements and parked the rest; S103c lets the operator **correct** those bindings and **captures each correction as the learning signal**, and makes the re-match **re-runnable** so authoring a missing goal recovers coverage on existing work. **Two rules make it safe (D203):** (1) **correction precedence** — any relink/unlink makes the unit user-owned, and the re-match never overwrites a user-owned unit (the user outranks the matcher); (2) **idempotent re-match** — re-running adds no duplicate edges and changes no already-correct matcher binding, it only fills newly-authored elements with previously-unbound work. **Unit-level ownership grain** (coarse on purpose; per-element is the named refinement, deferred unless reflection-3 shows it bites).

**Goal as artefacts:** (1) relink/unlink write paths over `EVIDENCES` (single-edge mutation; relink touches one binding of a multi-attach unit), behind `CDD_WRITE`, marking the unit `user_owned` and the edge `user-corrected`; (2) an append-only **correction audit event** per relink/unlink (`AuditPort.emit`, `cdd.relink`/`cdd.unlink`, before/after the binding pair), captured not consumed; (3) a re-runnable, idempotent, correction-respecting re-match (`correlate_goal_facets` excludes user-owned units; `replace_element_evidence` deletes only non-user-owned `EVIDENCES`); (4) the S103b read-only evidence display made interactive (relink/unlink affordances + a re-match trigger); (5) tests + a live in-container guard.

**Step 0 reconciliations (live tree):** EVIDENCES from `0006` mutated via new single-edge Cypher; **correction store = the audit context** (`AuditPort.emit`) — the platform audit-trail principle + the optimisation precedent, the API already builds `audit_adapter`, import-linter's cross-context independence is domain-only so the application-layer `contexts.audit.domain` import is clean, and the faceted reader serves the consume read (the **Step-0 audit-versus-dedicated finding, settled for audit**); user-ownership = `:Unit.user_owned` (schemaless, survives `correlate_units`' MERGE); the re-match endpoint can run `correlate_goal_facets` from app.state (unit_graph/facet_source/goal_graph/commitment_repository/email_job_search_source all wired; quality/suppression ops-only → None); `CDD_WRITE` for relink/unlink, `DAILY_DRIVER_UNITS_CORRELATE` for the re-match; **no migration** (0007 not needed). D203 minted (D202's tentative "D203 reserved for direction" → direction takes the next free number at S104).

**Scope cut:** relink + unlink + capture + re-runnable re-match + interactive display only. **Out:** consuming the captured corrections to adjust matching (the learning half — next session or folded into S104); direction + config-identity + the emergent loop (S104, consumes the unbound queue + these corrections); the embedding recall tier (deferred, re-assess after coverage improves); authoring the missing recurring goals (operator work on the shipped surfaces); per-element ownership (refinement, only if reflection-3 shows unit-level too coarse).

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103c-fix-2 — view coherence: one corrected truth across List, Map, CDD (D202/D203) (build mode, surface + read-side) — IN FLIGHT

**Make List and Map first-class against the element-evidence model.** The §4 one-graph standard: three views = three projections of one corrected truth, not one live view (CDD) and two read-only ones (List, Map). **Step 0 correction (the brief's load-bearing question):** the brief framed List and Map as reading "the older goal-level picture / a stale model" — **they do not**. Since S103b they already derive from element evidence: `/units-by-goal` → `list_units_by_goal` → `unit_graph.list_goal_edges()`, and the bridge's `list_goal_edges` derives from `list_element_evidence` + `derive_goal_edges`. The remaining `SERVES` references are stale docstrings, not a live read. So the **read-coherence gap was already closed at S103b**; the re-point is a no-op (not a separate-model reconciliation, so no scoping fork, no D-entry). The genuine gap is **write-reach**: List and Map are read-only — they lack the unlink/relink the CDD view got at S103c.

**Goal as artefacts:** (1) `outcome_id` on the binding read so List/Map can resolve a unit's element evidence per goal (the read-side enablement for the writes) + the stale `SERVES` docstrings refreshed to element-evidence-derived; (2) **unlink** from List/Map rows (reuses the S103c path — orphans the unit to the unbound bucket, marks user-owned, emits a correction record); (3) **element-level relink** from List/Map via a goal-then-element picker (cross-goal allowed, reuses the S103c relink path); (4) a **consistency guard** so the three views cannot silently diverge again, + correction-parity tests + served-HTML guard. Correction semantics identical across views (D203): the view is just the entry point.

**Scope cut:** surface + read-side only — no schema change, no new write path beyond exposing the S103c unlink/relink from more entry points; matcher logic + the consume side untouched. **Out:** excluding a unit from every view (orphaning to unbound is acceptable); a goal-level relink default (deferred unless reflection-2 shows the element picker is the wrong grain); the embedding tier, the consume side, direction, the emergent loop (later). **No D-entry** (reuses D202's element-level model + D203's correction semantics).

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103c-fix-3 — make the bug and the why honest (D202/D203) (build mode, surface + read-side) — IN FLIGHT

**A bug fix + read-side honesty; no matcher-logic change.** The operator's live correction pass surfaced two fixes and one classify-only. **Step 0 findings:** (1) **Map unlink fault root-caused — the outcome-kind 422.** `ElementKind` is `{lever, intermediary, external}` with no `outcome`; the weak alias bindings bind to the **outcome** element (`element_kind="outcome"`, S103b), and `renderGoalCorrections` (List/Map) sends every binding's kind to `/cdd/evidence/unlink|relink`, so `ElementKind("outcome")` raises → 422. The graph layer supports outcome (`_AUTHORED_ENDPOINT`); only the router's `ElementKind` validation rejects it. The S103c-fix-2 served-HTML guard passed because it checked the affordance *renders*, not that it *works*. (2) **The "goal name" why is a placeholder, not a token** — for alias bindings the real token is the unit∩goal-name overlap; classify by whether the recompute reproduces one. (3) **"Email activity: 2 applications" under Wide World Marathon is a symptom of spurious binding, not a label change** (the kind label is correct; job-search emails are spuriously bound to the marathon goal) → deferred and named, not a one-line fix.

**Goal as artefacts:** (1) unlink/relink accept the 4-value endpoint kind (incl. `outcome`) so unlink works from Map (and relink to/from the outcome) — the graph already handles it; (2) an **end-to-end functional guard** that unlink+relink actually complete (the edge removed/moved), not merely that the control renders — the live-surface law extended from presence to function; (3) the **honest why** — the real overlapping token where the recompute finds one, "no clear basis" where it cannot, replacing the "goal name" placeholder; the **no-basis count reported** as the spurious-binding signal. **No matcher binding change** (the looseness is a strategic call held outside this session, informed by the no-basis count).

**Scope cut:** surface + read-side + the Map-wiring (kind-validation) fix only. **Out:** fixing the matcher looseness / spurious weak bindings (strategic, informed by the no-basis count); the activity mislabel (a symptom of spurious binding, deferred); the embedding tier, consume side, S104; current-package windowing (flagged at S103c-fix-2). **No D-entry**, no schema.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103c-fix-4 — one correction interaction across all three views (D202/D203) (build mode, surface-only) — IN FLIGHT

**Single-source the correction interaction so divergence stops recurring.** The in-place re-render + bulk select shipped for the CDD view at S103c-fix, but the List/Map corrections added at S103c-fix-2 were a *separate* rendering that copied the display (why, strength) not the interaction — so they still collapsed on unlink and had no bulk. The fix is the UI analogue of the read-consistency guard: one component for how every view lets you correct. **Step 0 shape decision:** the two renderings group differently (CDD by element, List/Map by the goal's bound units) and relink differently (CDD same-goal element select, List/Map cross-goal goal-then-element picker), so the shape is a **shared interaction behind two thin renderers** — one `renderCorrectionList` owns triage + bulk + in-place re-render + the why/strength/unlink row; the one context-specific piece, the **relink control**, is passed in. The unlink uses `b.element_kind` uniformly (correct for both since a binding's kind is its element's kind).

**Goal as artefacts:** (1) `renderCorrectionList` (+ `unlinkBinding`) the single correction source; CDD and List/Map both call it behind thin renderers; (2) List/Map gain in-place re-render (no collapse) + bulk select + triage, falling out of pointing them at the shared source; (3) a parity/single-source guard so a future view re-implementing its own correction handling is caught. **Surface-only**, reuses `/cdd/evidence/unlink` + `/relink`; no write-path or matcher change, no schema, no D-entry. The consolidated row standardises on element_kind + strength + why (tier dropped as redundant with strength).

**Scope cut:** the correction-interaction consolidation + List/Map parity only. **Out:** the matcher-looseness strategic call (pending the cross-goal weak/medium unlink rate by tier); the embedding tier, consume side, S104; the status mislabel (resolves with the matcher call).

**S103c-fix (surface-mostly legibility patch).** The S103c correction pass surfaced three gaps that made it a guess rather than a judgment: no **why** (the operator can't see what the matcher matched on, so the captured teaching signal is thin), no **match strength** (no way to triage which bindings to check), and a painful **unlink** (it collapsed the list, no bulk path — throttling the very pass that makes the learning signal). All three close on the read side + surface. **Step 0 finding:** the EVIDENCES edge records `tier` + `basis` but **not the matched term**, so the why and the strength are **recomputed on read** from the deterministic lexical/alias match against the current element vocabulary — no storage change, no migration. **Honest framing:** the matcher is lexical+alias only, so what is shown is **match strength (string-match), not correctness** — tier orders it (exact > keyword), and within keyword a distinctive term outranks an incidental shared one (discriminativeness); a high-strength match on an incidental token is the reflection-1 trap the why exposes. Genuine semantic confidence is an embedding-tier property, deferred. **Surface-mostly** — recompute-on-read + front-end; no migration, no new architectural decision, **no D-entry** (exposing existing match properties and an honest read-side proxy is not a decision); matcher logic + the consume side untouched.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103d — align the matcher's match rule to the read-side basis rule (D204) (build mode, matcher logic) — IN FLIGHT

**The first touch on matcher binding logic in the S103c sub-series.** The honest-why (S103c-fix-3) made the matcher narrate some of its own binds as baseless; this stops it creating them. `_keyword_match` fired on a shared significant token *or* a bare substring; the read-side `binding_rationale` requires a shared token, returning `NO_CLEAR_BASIS` for substring-only. D204 drops the substring branch and **single-sources** both to one `significant_tokens`/overlap helper, with a tie-test pinning *matcher binds ⟺ read-side has basis*. **Principled-subtractive**: removes flagged-wrong behaviour, adds none. **Step 0 findings (3 brief-premise corrections, all toward the brief's own single-source preference):** (1) **Stamping stale** — the brief's "number off S100" reflects the lagged strategic snapshot; live max is S103c-fix-4, so stamped **S103d** / **D204** (S104 reserved for direction). (2) **`_keyword_match` is one shared function** used by *both* the keyword and alias tiers (not an "alias-only path") — so dropping substring aligns both tiers, the true single-source; the keyword tier had **zero** substring-only binds live, so no keyword behaviour changes in practice. (3) **Baseline drifted** to **10 of 745** (all alias, all substring-only) from the brief's 12 of 839, same pattern; single-source is **feasible** (matcher + read-side in one module, no import boundary), so the stronger "one shared symbol" form is taken, not just align-and-tie-test.

**Goal as artefacts:** (1) a shared `significant_tokens`/shared-token-overlap helper in `goal_assessment.py`; `_keyword_match` becomes shared-token-only (substring dropped), `binding_rationale` calls the same rule; (2) a **tie-test** asserting matcher-binds-iff-read-side-has-basis (the anti-drift invariant guard) + a **precision** test (substring-only, no shared token, does *not* bind) + a **recall** test (shared significant token still binds); (3) a **live re-measure** proving the 10 baseless binds gone, the bound count down by exactly that set, zero alias `NO_CLEAR_BASIS` remaining — counts/ids only, no content (D21). **No schema, no migration** (re-runnable via `make correlate-units`).

**Matcher-quality sequence (settled):** coverage authoring first (operator work on the shipped surfaces) → **alias/keyword bind-rule tightening now (S103d)** → the correction-consumption pass next → embedding recall tier deferred (D197 two-threshold). **Out of S103d:** recall recovery via embeddings, coverage authoring, the consume side, the activity mislabel (resolves with the broader matcher call), S104 direction.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103e — re-scope the dogfood to get-a-job by archiving the other goals (D205) (build mode, clean-data) — IN FLIGHT

**The clean-data move before the get-a-job framework build.** Tuning the matcher across eight goals on a two-thirds-unbound corpus produced abstract questions, not inspectable answers; a clean single-goal corpus makes the matcher's get-a-job binds readable by eye and turns the matcher-quality call into inspect-and-correct on real data (D200's authored-and-corrected model). **Archive, never erase** (invariant 4 + originals-never-erased): every authored goal except get-a-job is marked archived on the personal tenant via a schemaless `:Outcome.archived_at`, reversibly, with its CDD elements, binds, and audit history intact.

**Goal as artefacts:** (1) `:Outcome.archived_at` archive/unarchive write path on the goal graph (session + repo + `OutcomeGraphPort`), schemaless (no migration); (2) the single `list_goals()` read seam scoped to active goals (`_LIST_OUTCOMES WHERE o.archived_at IS NULL`) — scopes the assess surface (List/Map/CDD) **and** the matcher (`correlate_goal_facets` reads `list_goals`); (3) the coverage projection (`assess_goals`/`group_units_by_goal`) counts a unit linked only against a goal in the active set, so a unit bound only to a now-archived goal reads **unlinked**, not hidden (D171 honesty); (4) `schema.md` `:Outcome.archived_at` note; (5) an ops archive script + a live re-measure (counts/shape only, D21); (6) tests (archived goal does not render, matcher writes no bind against it, get-a-job still renders + binds, re-activation returns a goal whole).

**Step 0 reconciliations (live tree):** goals carried **no** archive/status field (status is a computed-on-read verdict; the Revisable Protocol/`archived_at` pattern covered tools/roles/templates in Postgres, not the `:Outcome` graph node) — so a **schemaless** status add (no Alembic, no Cypher constraint, the S102/S103a precedent), a small graph change not a DB migration; the read seam is a **single chokepoint** (assess + matcher both read `list_goals`); the coverage projection counted "linked" on any edge without intersecting the active goal set — a latent inconsistency corrected here. **Moving house exists in neither form** — not authored, and the "House purchase" class is only a deferred goal-discovery *nomination* (D197/D184), not a built latent record — so it is authored at its own framework step later. Stamped S103e/D205 (S104 reserved for direction).

**The three-slice framework build order (after this clean-data move):** **slice one** — the goal-level portfolio-and-state CDD for get-a-job (operator authoring against the S102/S103 surface + any small surface support); **slice two** — process depth (per-gate CDDs, step-as-portal; the deferred D162/D198 build); **slice three** — the state variables and the metrics loop (win-probability, conversion-rate recommendations).

**Scope cut:** archive + scope-the-reads + coverage-consistency + live re-measure only. **Out:** authoring the get-a-job CDD (slice one, next); process depth (slice two); state variables + metrics loop (slice three); creating moving house + its framework (its own step); deleting any goal (never — invariant 4 + originals-never-erased).

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103f — author the get-a-job goal-level CDD from the framework, slice one (D206) (build mode) — IN FLIGHT

**Slice one of the three-slice get-a-job framework build: the goal-level portfolio-and-state CDD.** S103e left a clean single-goal corpus; this turns get-a-job from a generic tagging-CDD into the framework-authored causal model the matcher binds to and the operator corrects (the S101 pivot, D200). The job-search decision framework is committed as the authoring source (`charter/frameworks/job-search-decision-framework.md`) and seeded as the goal-level CDD: one multi-objective outcome (Role secured), 7 portfolio levers, 5 measured intermediaries, 5 uncontrolled externals — provenance `llm_drafted`/`pending`. Applications and opportunities are **Flow items per D198**, never levers. The matcher re-correlates to element-level evidence (D202) on the shared-token floor (D204).

**Step 0 reconciliations (live tree):** the pre-flight check-in composer eligibility already scopes to active goals — it reads `list_goals` (the S103e seam), not a separate `:Outcome{mode:'homeostatic'}` traversal, so no code change, a guard test added. get-a-job's current CDD is a **mix, not all-generic**: 4 `llm_drafted` + **3 `user_authored`** elements (the operator's prior proof corrections); invariant 4 bars wiping the user-authored three, so the seed **deletes only the `llm_drafted` drafts** (incl. the D198-violating "Apply to jobs") and **preserves the user-authored three** (the operator reconciles in proof). All four element types are authorable + renderable (S102/S103). Stamped S103f/D206 (S104 reserved for direction).

**The three-slice framework build order:** **slice one (this) — goal-level portfolio-and-state CDD**; slice two — process depth (per-gate CDDs, step-as-portal, the deferred D162/D198 build); slice three — the state variables + metrics loop (per-process win-probability, conversion-rate recommendations).

**Scope cut:** author goal-level + re-correlate + leave the surface ready for the operator's proof. **Out:** the operator's proof-and-correct pass (operator work, after this); per-gate process CDDs + portals (slice two); per-process win-probability Bayesian state + the metrics loop (slice three); moving house (its own step); the 6 pre-existing red live-stack e2e tests (carry-forward triage).

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103g — the process flow + gate portals + two gate CDDs, slice two-A (D207) (build mode) — IN FLIGHT

**The heavy one: the first build of the deferred step-as-portal depth (D162/D198).** Slice one authored the goal-level (portfolio) CDD; its diagnostic showed gate-altitude labels flood (Tailoring 116, win-probability 114) because the gate structure the framework hangs its CDDs on did not exist. This builds it: the framework's process layer as first-class `:Gate` nodes (a new flow referencing the D163 steps, not replacing them), each a **portal** into a local CDD with Pratt wiring; **Apply + Screening** authored and wired end to end; the matcher binds work to gate elements with token-level gate-local precedence so application/screening work re-homes to its gate.

**Goal as artefacts:** (1) `:Gate` nodes in sequence (migration 0007, gate uniqueness), built on the D163 steps; (2) a gate = a portal into its local CDD (levers/intermediary/externals/local-outcome); (3) Apply + Screening carry authored Pratt-wired CDDs (lever→intermediary FEEDS, external→intermediary INFLUENCES, intermediary→gate FEEDS); (4) gate-altitude `Tailoring effort` relocates into the Apply gate (node + provenance + proof carried, goal-level edge migrated), portfolio levers stay, the 2 `user_authored` stage intermediaries preserved in place; (5) the matcher binds gate elements, gate-local winning the shared token; (6) the surface renders the flow as gate sections, each expandable into its local CDD; (7) D207 + schema.md (with the 0007 migration) + session log.

**Step 0 reconciliations (live tree):** the D163 steps are **3 coarse lever-steps** (Refresh CV / Apply / Interview prep), not the brief's "Apply, HR call, interview, offer" — gates **reference** steps, not replace them; intra-CDD Pratt edges **already exist** (19 live, slice one) — the brief's "flat, not Pratt" was a *render* limitation, not a model gap (the view-layer that draws edges is a separate build); `:Gate` follows the 0005 constraint pattern → migration **0007**; the merged `Tailored resume` left `Tailoring effort` `llm_drafted` (delete-of-duplicate, no fold) so the relocation carries `llm_drafted`/`pending`; user_authored count is **2** (`2nd stage interviews`, `Initial Interview invitations`). Stamped S103g/D207 (S104 reserved for direction).

**The three sub-slices of slice two:** **two-A (this) — the flow spine + portal mechanism + 2 gates (Apply, Screening) wired end to end**; two-B — process instances (opportunities as Flow items moving through gates; until then gate CDDs aggregate across opportunities); two-C — the remaining gates (Pre-search, Target, Interviews ×6, References, Negotiate, Accept) as a repeatable pattern, and the connection of the 2 stage intermediaries to the interview gates.

**Scope cut:** flow + portal + 2 gates + matcher precedence + gate-section render only. **Out:** process instances (two-B); the remaining gates + the stage-intermediary→interview-gate connection (two-C); a true graph render vs cards (a named view-layer build); the portfolio control layer + state-variable engine + metrics loop (slice three); the 6 pre-existing red live-stack e2e tests (standing triage).

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103h — process instances as Flow items, 3 opportunities (D208) (build mode) — IN FLIGHT

**The structural flood fix.** Two-A relocated the floods to the gates but did not resolve them — a gate with no instances aggregates every opportunity's work on one lever. This builds the process-instance layer: an opportunity is a first-class `:Opportunity` Flow item (D198) belonging to get-a-job, positioned at its furthest-evidenced gate, grouping its units (`BELONGS_TO`). A clustered unit's gate bind reads **per opportunity** so the gate pile distributes across opportunities + an honest unclustered residual (D171). **3 operator-confirmed opportunities** (Acme, Globex Legal, Initech AI — spanning interview/application/mid-process to test the position inference) instantiated from the densest live clusters; auto-clustering the whole corpus is the named next increment.

**Goal as artefacts:** (1) `:Opportunity` node + `BELONGS_TO` (unit→opportunity) + `current_gate_id` (furthest-evidenced gate), migration 0008; (2) 3 instantiated, units attached by company signature (Acme guarded as acme.example + Screenloop-naming-Acme), gate positions set; (3) opportunity-scoped binding (the read attributes a clustered unit's gate binds to its opportunity); (4) the surface shows opportunities under gates + per-opportunity counts + position; (5) re-measure of the Apply pile split + residual; (6) D208 + schema.md + session log.

**Step 0 reconciliations (live tree):** Targeting still **0** (proof pass not run — gates/elements all `pending`), so the named **precedence-by-strength** fallback applies — but the collision is a **symmetric incidental token** (`warm`, equal keyword tier on Targeting and the Apply Referral elements), so "≥ strength" wouldn't fix it; the rule narrows to **strictly-stronger** (gate suppresses a goal match only when *strictly* stronger; equal-tier both survive), which repopulates Targeting. Honest cost: the two-A win-probability drain (114→7) partially **returns** via multi-attach — the opportunity layer is the real declutter, not the precedence. Next migration **0008**. google-ads Pixel mis-link deferred to the matcher-precision pass. Stamped S103h/D208.

**Scope cut:** the instance + its position + per-opportunity read + the surface + the precedence narrowing only. **Out:** auto-clustering the whole corpus (next increment); the remaining gates + the stage-intermediary connection (two-C); per-process win-probability + portfolio start/kill/sync + the metrics loop (slice three); the graph render (queued after two-B); the gate-CDD proof pass (owed, operator's, best right after two-B); the matcher-precision pass (the warm/google incidental-token mis-links); the 6 pre-existing red live-stack e2e tests.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103i — precision suppression: source-class taxonomy + genuine-match bar (D209) (build mode) — IN FLIGHT

**The matcher-precision pass, earned at three instances** (the google Pixel ads, the Targeting "warm" collision, the LinkedIn pile). The two-B residual diagnostic found the 462 unclustered units are ~half **wrong-goal work** (Health doses, a child's "warm"-ups, a house thread — re-homed onto get-a-job by the S103e re-scope on incidental tokens), ~quarter **alert** (board listings), ~quarter **pipeline** (one-touch acks), with a sliver of real opportunity. This stops the matcher force-fitting non-opportunity and wrong-goal work onto get-a-job — extending coverage-honesty (D171/D193) to the **binding decision**, without revising the archive (D205).

**Two mechanisms, one pass, four dispositions:** (A) a **source-class taxonomy** (direct/recruiter/board/ATS/ad-newsletter/internal-facet, facet+domain derived in the use case per D184/D16) — ads suppress, board → Labor-market external count, one-touch acks → Pipeline-depth count, multi-touch → opportunity candidate, internal-facet → defer to B; (B) a **genuine-match bar** — a single generic token is not a match, tied to the honest-why's `element_token_counts` discriminativeness (D204), so a read-side-weak single-token bind is not persisted. Park: no genuine match + no routable real-job source → **unbound**.

**Step 0 reconciliations (live tree):** the bar ties to the **existing** honest-why discriminativeness (no new corpus-IDF needed — "warm" is in 3 element labels → already WEAK); source-class needs the email **sender domain + thread size** (not in the title-only `WorkFacet`) → a focused `EmailSourceMetadata` port reads the email store (D184 pattern); both filter in the use case (the `suppression_policy`-hook precedent); routed counts persist as `routed_count` on the destination elements (Labor-market external + Pipeline-depth intermediary, slice-one seeded). Stamped S103i/D209.

**Scope cut:** the two mechanisms + the routing + park + re-correlate + split re-measure only. **Out:** which-absent-goal detection + archived-goal-signature routing (homing waits for the goals to exist); creating moving-house (the House thread stays unbound until then); content-extraction clustering of the real-opportunity remainder (the next slice, sized by what survives); two-C; slice three; the graph render; the gate-CDD proof pass (operator-owed); the 6 pre-existing red live-stack e2e tests.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103j — the Map renders the real graph; proof on it (D210) (build mode, view-layer) — IN FLIGHT

**The render that completes the D199 Map and unblocks the owed proof pass.** The Map has been a depth-two sibling-feeder stopgap since Phase 2-B opened; two-A built the gate portals + Pratt wiring, two-B the opportunities, S103i cleaned the pile — and none of it was drawn. This draws the live get-a-job graph to design-language §4: the goal-level control CDD, the flow spine with Apply + Screening as portals into their local CDDs (remaining gates dashed for two-C), the three opportunities at their gates, and a recommendation-shaped disposition summary. **Node-tap opens the existing proof drawer**, so the owed D200 proof pass runs on the graph — render then proof.

**Step 0 reconciliations (live tree):** the CDD endpoint already returns elements+edges (two-A), gates (two-A), opportunities (two-B) — the graph data is on the wire; the one gap is the precision **disposition counts** (S103i logged, not persisted) → a schemaless `disposition` block on the `:Outcome` written at correlate + exposed on the CDD read (the "add to the existing read" path, **no migration**). The Map seam is `renderAssessMap` (the D199 stopgap); the `.panel` right drawer (§3) + `openNodePanel` + the `cddElementRow` proof controls exist to reuse. Stamped S103j/D210 (supersedes D199's Map-render clause).

**Scope cut:** render the current state + node-tap-to-proof only. **Out:** Flow-edit-ripples-to-Map + lever-deep-links-to-Flow-step (later interaction slices); the prioritisation drag (§5); content-extraction clustering (next model slice); two-C (the dashed gates hold its slots); slice three; the week/dashboard renders of the same graph; the gate-CDD proof pass itself (operator-owed, now runnable on this render); the 6 pre-existing red live-stack e2e tests.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103k — the outcome-layer correction: split the bundled outcome into measurable outcomes (D211) (build mode, small + structural) — IN FLIGHT

**The Pratt fix the proof pass caught before it formally began.** Slice one bundled the measurable results into a single "Role secured" outcome, which left the intermediaries (pipeline depth, win-probability, leverage) reading as endpoints. The deeper truth the live read surfaced: the outcome layer **did not exist in the model** — `ElementKind` is levers/intermediaries/externals only, and the one outcome *was* the goal node, so the seven goal-level intermediary→outcome edges pointed at the goal itself. This **introduces** the layer: a new `:MeasurableOutcome` kind (migration 0009) distinct from the `:Outcome` goal node, five outcomes (Offer received, Offer quality, Optionality, Reputation, Relationships), intermediaries re-pointed to feed them, the outcomes feed the goal. The chain now reads levers → intermediaries → outcomes → goal.

**Step 0 reconciliations (live tree):** Step 0.2's premise (the platform already distinguishes goal from an `outcome` element kind) was **wrong** — CC's read corrected it; the fix is a model extension, not a re-type. "Role secured" is `llm_drafted` (no invariant-4 concern); the stance is repurposed to the goal statement. Live found **7** goal-level intermediary→goal edges (5 framework `llm_drafted` + 2 `user_authored`: "Initial Interview invitations", "2nd stage interviews") and moat **166** at the goal. All 7 re-point onto the outcome layer (the 2 user-authored keep label/type, edge moved to Offer received, flagged as proof candidates — invariant 4 protects content, not the downstream edge). Three consequences of the new bindable type folded in: matcher/genuine-match-bar govern outcome binds (kind-agnostic, no fresh flood), the render gains an outcome column, reclassify allows movement to/from outcome both ways. Stamped S103k/D211. Next migration **0010**.

**Scope cut:** the outcome layer + its wiring + the three new-type consequences only. **Out:** the verification surface (openable source, discriminative basis, editable match surface) + the drill path (goal entry, breadcrumb) — **the next session**, built against this corrected chain, with the genuine-match-bar stopword fix folded in; the operator's full proof pass (runs after the verification surface, against the corrected and now-verifiable chain); content-extraction clustering; two-C; slice three; the 6 pre-existing red live-stack e2e tests.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103l — the verification drawer: basis, source, match surface (D212) (build mode) — IN FLIGHT

**The drawer that makes a bind verifiable, which is what makes the proof pass real.** The S103j export proved the gap: a correct Acme bind justified by the stopword "you", and no item openable to read. This gives the proof drawer the three legs proof needs — the discriminative basis (the most discriminative shared term, or "no clear basis"), the openable read-only source (sender/date/body shown by default), and the editable match surface (the words an element links on, with per-word pull counts, generic flagged) — and refines the matcher so a generic token is never offered as a reason. Basis and bar single-sourced (D204): the why never offers a token the bar would not bind on.

**Step 0 reconciliations (live tree):** no contradicted premise (unlike S103k). The drawer (`openProofNode`→`renderBindings`) + node-tap (S103j) + the §3 proof controls exist to extend; the read-side basis (`binding_rationale`) + the bar (`is_genuine_bind`) both compute from `significant_tokens` (D204 single source) — the basis bug is it ranks by element-count only, not corpus frequency, so "you" wins; the email content (subject/sender/date/body) is ingested + decryptable via the email store, read through an apps adapter (the `EmailSourceMetadata`/D209 precedent, read-only, §9). The fix: one shared discriminative-token predicate (element-distinctive AND not corpus-generic) drives both the bar and the basis; true grammatical stopwords ("you", "we", ...) move into `_STOPWORDS`, the rest ("stage/interview/warm/...") fall to the corpus-IDF where actually common. Stamped S103l/D212. Next migration 0010 (none expected this session).

**Scope cut:** the drawer (3 legs) + the matcher refinement that powers leg 1 only; leg 3 (match surface) is deferrable to a fast-follow if the session runs long (legs 1+2 are what the proof pass strictly needs). **Out:** the drill path (goal-overview entry, breadcrumb) — the next session, drawer reachable via Map node-tap meanwhile; the operator's full proof pass (runs after this, on the now-verifiable chain — the Reputation thin edge + the 2 re-pointed user intermediaries first); content-extraction clustering; two-C; slice three; the 6 pre-existing red live-stack e2e tests.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103m — the opportunity lens: filter the set down to one process (D213) (build mode, view-layer) — IN FLIGHT

**The lens that makes the proof pass per-opportunity and answers "filter a thread amongst the set."** Two-B instantiated Acme, Globex Legal, Initech AI with units attached + a gate position each; this turns that into a navigable lens. A selector (All / the three / unclustered) scopes get-a-job to one process: its correspondence thread (units in time order), its current gate on the flow, and its element binds — each proofable through the S103l verification drawer. So proofing Acme means filter to Acme, read its thread, read each bind's basis + source, keep or unlink. The unclustered remainder shows its count honestly (D171); clustering extends the lens next.

**Step 0 reconciliations (live tree):** no contradicted premise. The opportunity model is queryable without a model change — `list_opportunities` (id, name, current_gate_id, unit_count), `ElementEvidenceRecord.opportunity_id` on every bind (S103h/D208), units' BELONGS_TO. The lens data already rides two payloads the surface fetches: the CDD read (`cdd.opportunities` with `current_gate_id` — the flow spine already places them) and the bindings read. Leanest path: plumb `opportunity_id` + the unit's `occurred_at` onto each binding, then the client builds the thread (distinct units, time-ordered) + the scoped binds + the unclustered entry (`opportunity_id == null`) from data already on the wire — no new endpoint, no model change. Stamped S103m/D213. No migration.

**Scope cut:** the selector + the scoped thread/flow/binds over the three instantiated opportunities + the honest unclustered count only. **Out:** content-extraction clustering to populate beyond three (the next session; the unclustered count sizes it); per-opportunity process configuration (after two-C's gate library + the assessment); the "how am I doing" assessment view (specced as a mock, built after clustering); leg 3 (the match surface) + the drill-path breadcrumb (fold in later); the 6 pre-existing red live-stack e2e tests.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103n — close-lost: a closed state with an outcome reason on opportunities (D214) (build mode, small model extension) — IN FLIGHT

**The state the assessment and the lens both need to tell the truth.** All three instantiated opportunities are closed or dead, but the model has no closed state, so the lens shows them as live processes at gates — a false picture. This adds the state: `:Opportunity` gains status (live/closed), a required `closed_reason` (won, declined, withdrawn-or-killed, rejected, went-cold), and `closed_at`. Closing is archive-not-erase (D114, invariant 4) — correspondence + binds intact, reopenable; the live set excludes closed; the furthest gate already tracked + the reason give the real-outcome-versus-non-start signal. Closed-won/closed-lost are the first ground-truth labels the win-probability engine will calibrate against. Must precede clustering, which would otherwise create live-looking dead processes at scale.

**Step 0 reconciliations (live tree):** the migration premise, **inverted from S103k** — the brief assumed a migration; none is structurally needed. `status`/`closed_reason`/`closed_at` are schemaless property adds on the existing `:Opportunity` (no new label, no property-existence constraint in Community Edition) — the `:Outcome.archived_at` (S103e) / disposition-block (S103j) precedent; the read coalesces a missing status to `live`, so old opportunities need no backfill. `current_gate_id` already tracks furthest gate (no new field). `schema.md` documents the fields; a no-op migration file would be shape without substance. Stamped S103n/D214. No migration.

**Scope cut:** the closed state + close/reopen (archive-not-erase) + the lens reflecting it only. **Out:** the assessment's live-versus-closed funnel + verdict (built with the assessment; this makes the state it reads); content-extraction clustering (the session after; it classifies each new opportunity live/closed, possible only because this state exists); per-opportunity process configuration (the heavier extension, later); the 6 pre-existing red live-stack e2e tests.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103o — content-extraction clustering, live/closed classification (D215) (build mode) — IN FLIGHT

**Extends the lens past three; classifies each live or closed.** Extract company + role from the content of the 229 unclustered bound units (not domain — ATS hides the company behind `ashbyhq`/`linkedin`); group multi-touch threads into candidate opportunities; classify each live or closed using S103n's state and the thread signals; instantiate `system_suggested` for the operator to proof (confirm/correct/reject). One-touch + board units stay the S103i pipeline/market counts. With the three already closed, this surfaces the real, mostly-closed history — the honest record and the first win-probability calibration data, not a hidden live pipeline.

**Step 0 reconciliations (live tree):** no model change (no longer asserting migration-or-not after being wrong both ways) — instantiation uses two-B's `merge_opportunity` (provenance `system_suggested`) + S103n's `close_opportunity` (status/reason) + `attach_unit_to_opportunity`, all present; the lens already renders + marks them (S103m/S103n), so extracted opportunities surface for proof for free; the proof actions (confirm = provenance→user_authored; reject = delete the suggested cluster, binds survive) are small query additions. The inference port is `LiteLLMAdapter(settings=InferenceSettings())`, buildable in an ops script (the correlate/instantiate precedent); extraction runs behind the structured-output seam (D16) on read-only content (§9). Routed pipeline/market units aren't in the bound-unclustered set (S103i dropped them from binds), so the counts are unchanged by construction. Stamped S103o/D215. No migration.

**Scope cut:** extraction + clustering + live/closed classification + `system_suggested` instantiation + the confirm/reject proof actions only. **Out:** the operator's proof pass (operator work in the lens, after); the "how am I doing" assessment view (the next build, now able to read a true live count + real opportunity set); per-opportunity process configuration (the heavier extension); correct-company rename + core-action discoverability (the drill-path session); the 6 pre-existing red live-stack e2e tests.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103p — the "how am I doing" assessment (D216) (build mode, view-layer) — IN FLIGHT

**The payoff surface the opportunity layer was built to feed.** A recommendation-shaped assessment of get-a-job: an empty-pipeline verdict anchored on the label-independent funnel (0 confirmed live, the applied→engaged collapse, ~1 interview, 0 offers), the move it implies (the framework's response-rate levers — sharper targeting, warm intros), and the close-reason split shown as the proof-dependent *sharpener* (response-vs-conversion), never flipping the headline. Reads live (option 2), so it shows the honest unproofed state and motivates the proof pass. Brief preserved at `briefs/p2b/s103p.md`.

**Step 0 reconciliations (live tree):** no model change — a pure projection over the `GoalCddView` (opportunities w/ status/closed_reason/current_gate_id/provenance, gates w/ gate_order, the S103i/j disposition block: moat 166, pipeline 102, market 96, parked 247). Live shape: 17 opportunities — 0 confirmed-live, 6 system-suggested live, 11 closed; interviewed = 1 (Acme at Screening, gate_order > Apply); offers = 0. No clean "applied" denominator (one-touch acks are unit counts, not distinct applications) → funnel shows counts + one-touch volume separately (D171). Stamped S103p/D216. No migration.

**Scope cut:** the pure verdict + the read endpoint + the render only. **Out:** the per-process win-probability engine (slice three, reads the proofed closed reasons as calibration); the operator's proof pass (the assessment reads whatever state it's in + flags the unproofed); per-opportunity process configuration; correct-company rename + core-action discoverability (the drill-path session); the 6 pre-existing red live-stack e2e tests.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103q — Pipeline stats: the three-way split, the depth ladder, the engaged Kanban (D217) (build mode, view + small rules) — IN FLIGHT

**The operating surface beneath the verdict.** A Pipeline stats tab beside Doing (untouched): applied split three ways (rejected / no-response / engaged), the engaged depth ladder by furthest gate, and an engaged Kanban of opportunity cards with history + status + a deterministic rules-based next best action; drag-to-re-stage writes `current_gate_id` and propagates to the ladder + the Doing interview count (the "1 should be 2" fix). The operator's own funnel model — mostly relabelling existing data; the one new piece is the per-card next-best-action rules layer (the small cousin of the win-probability engine).

**Step 0 reconciliations (live tree):** no model change — a read-only projection over the disposition (S103i: pipeline 102 one-touch) + opportunity set (17: 9 rejected, 8 engaged) + gates (Apply, Screening) + thread (S103m occurred_at for last-activity) — plus one small write (`set_opportunity_gate`, SET `current_gate_id`) for the re-stage + the deterministic rules function. The three-way split is **mixed grains** (rejected/engaged are processes, no-response is the 102 one-touch units), so **no clean "applied" denominator** — parts shown separately with the grain surfaced (D171). Columns/rungs = built gates (Apply, Screening) + Unplaced; finer interview rounds → two-C. A manual re-stage sticks until the next operator-gated re-cluster. Stamped S103q/D217. No migration.

**Scope cut:** the tab + the three-way split (drillable) + the depth ladder (drillable) + the engaged Kanban (drag-to-re-stage) + the rules-based next-best-action only. **Out:** the period filter (next session, ships all-time); finer interview-round stages (two-C gate library); the win-probability engine (slice three, replaces the rules without changing the surface); per-opportunity process config; the no-response→engaged promotion (clustering territory, deferred — re-status covers the opportunity buckets via close/reopen); the 6 pre-existing red live-stack e2e tests.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103r — split the board: live active board, closed record by outcome (D218) (build mode, view restructure) — IN FLIGHT

**The board adopts the sales-pipeline shape.** The S103q engaged Kanban splits into an **active board** (live processes only, in stage columns, with next best actions) and a **closed record** (terminal processes grouped by outcome — won/rejected/went-cold/declined/withdrawn — each with stage-at-close as the within-outcome depth-of-death breakdown). A rejection is terminal + stage-independent, so a closed process leaves the active board the way closed-lost leaves a sales pipeline — dissolving the S103q unplaced-rejected nonsense. Stage-at-close reuses `set_opportunity_gate` (no new field). With everything currently closed the active board is empty — the honest "nothing is live, originate" read.

**Step 0 reconciliations (live tree):** no model change — a view restructure of `buildPipeline` (the DTO cards already carry status/closed_reason/stage/next_action; the read returns the gate ladder), plus reuse of the existing `set_opportunity_gate` write for stage-at-close. The depth ladder already reads the gate (furthest/stage-at-close), so it stays correct once closed cards carry their stage-at-close. The three-way split + the depth ladder stay. Stamped S103r/D218. No migration. Supersedes the S103q single-Kanban placement.

**Scope cut:** the active/closed split + the outcome-led closed record + the stage-at-close setter only. **Out:** the period filter (next session); finer interview-round stages (two-C); the win-probability engine (slice three); per-opportunity process config; the 6 pre-existing red live-stack e2e tests.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103s — the process detail view: open it, work it, close it (D219) (build mode, view assembly) — IN FLIGHT

**The process becomes a first-class object you open, work, and close.** A detail view opened from any card (active board, closed record, lens, Map opp chip) assembles the built pieces into one panel: header + stage picker (S103q gate write), the correspondence thread with openable read-only sources (S103m/S103l), the binds each verifiable through the drawer (S103l), the next best action (S103q), and a close row (S103n). Every active card also gets a direct close control. The process-level answer to the drill gap and the close gap — open, verify, act, close in one motion.

**Step 0 reconciliations (live tree):** pure assembly, no backend — the thread (S103m), the openable source + verification drawer (S103l), the stage picker + NBA (S103q), and the close (S103n) all exist and are reused; the detail view is hosted in the existing §3 panel and reads the pipeline-stats DTO (card + gates) + the global bindings. No model change, no migration. Stamped S103s/D219.

**Scope cut:** the detail view (assembly) + open-from-any-card + close-on-active-cards only. **Out:** the aging rule (recent applications live until a configurable silence threshold, then confirm-close) + the period filter — the next session, built together as time windows with confirm-not-auto; finer interview-round stages (two-C); the win-probability engine (slice three); the 6 pre-existing red live-stack e2e tests.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103t — new-lead origination: the fit rubric, the Lead gate, the origination column (D221) (build mode) — IN FLIGHT

**The origination layer lands — the empty board gets a left edge.** The S103s proof left the active board near-empty (1 live, 12 rejected, 4 closed-other, 102 one-touch no-response, 0 offers): a targeting-and-channel failure (framework §2/§5), not a volume one. This builds the first upstream lever. **First origination / portfolio-layer session** ("the layer above the flow" the framework names) — placed under the current epic header for continuity; whether it opens a distinct epic is a `roadmap.md` call for a later strategic session, flagged not acted. Goal as artefacts: (1) a **Lead gate** in the get-a-job ladder below Apply (`gate_order` 2; ladder reads Lead, Apply, Screening); (2) the **fit rubric** authored as a proofable `llm_drafted`/`pending` CDD element — a goal-level lever feeding Pipeline depth — carrying the three tiers (bullseye/strong/opportunistic) and the fit-times-warm rule; (3) three schemaless `:Opportunity` props: **`fit_tier`**, **`warm_access_available`**, **`origination_source`**; (4) a **create-lead** path (a `user_authored` opportunity at the Lead gate, zero touches, no thread); (5) an **origination Lead column** at the board's left, leads sorted fit-tier primary + warm-access secondary, each with an **apply** action advancing Lead→Apply. The operator sets the tier (assess-not-replace, D200); the platform does not auto-score.

**Step 0 reconciliations (live tree):** three prompt assumptions corrected. (1) **`source` was already taken** — `:Opportunity.source` is the D215 clustering signature (`merge_opportunity(..., source=...)`), so the origination channel is named **`origination_source`**, not `source`. (2) **No goal-level "response-rate" intermediary exists** — the five goal-level intermediaries are Pipeline depth / win-probability / leverage / information / offer-sync; screen-through probability is Apply-gate-local. The rubric lever feeds **Pipeline depth** (the portfolio origination-effect, §2), and the application-to-screen response rate is the gate-local measure the deferred instrumentation calibrates against (§5). (3) **The rubric is seeded via the S103f seed path** (`merge_authored_element`/`merge_authored_edge` → `llm_drafted`/`pending`), not the S103a `add_cdd_element` path (which stamps `user_authored`/`accepted`) — it must land drafted to be proofable. Otherwise: the Lead gate reuses `merge_gate` (idempotent MERGE; 0007 `gate_unique_per_tenant` already covers it); the three props are schemaless adds (the D214 `status`/`closed_reason` precedent, read coalesces), so **no migration**; create-lead reuses `merge_opportunity` (a new use case + endpoint); apply-advance reuses `set_opportunity_gate` (the S103q `/stage` write); leads (live, at Lead gate, zero touches) are partitioned out of the engaged/applied funnel, ladder, and cards in `build_pipeline_stats` and surfaced in a dedicated leads bucket; a threadless `user_authored` lead is consistent with D215 (its two-touch rule governs system-suggestion, not user authoring); no new permission (`DAILY_DRIVER_CDD_WRITE`, D126); Postgres untouched; the decisions.md TOC is regenerated by `ops/decisions_toc.py` (no hand-maintained summary index). Stamped S103t/D221 (D220 live max; S104 reserved for direction).

**Scope cut:** the fit rubric + the Lead gate + the three props + create-lead + apply-advance + the Lead column only. **Out:** retrospective tiering of the existing 17 opportunities (optional later diagnostic); the prospective target-instrumentation layer that calibrates the rubric against response (a future session, gated on the rubric proving it discriminates, calibrating per §4 on revealed effort — response speed, routing seniority — not application volume); auto-scoring `fit_tier` (deferred assist; the operator is ground truth, D200); finer interview-round gates (two-C gate library); the aging rule + period filter (the previously-queued time-windows session, still to run); the win-probability engine (slice three); the went-cold-vs-no-response taxonomy question (the operator's open S103s steer); the 6 pre-existing red live-stack e2e tests.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103u — the contact graph: warm access becomes a derived, email-seeded, operator-proofed state (D222) (build mode) — IN FLIGHT

**Warm access stops being a self-reported tag and becomes evidence.** S103t named the highest-value funnel work (turn a cold bullseye warm) and handed the operator nothing to do it with. This backs it with a **`:Contact`** graph. Goal as artefacts: (1) a tenant-scoped **`:Contact`** node (migration 0010, the 0009 precedent) carrying `name`/`company`/`degree`/`strength`/`reachability`/`capture_source`/`provenance_origin`; (2) a contact **links to a company** by normalized string match (the S103o signature precedent — no `:Company` node; `:Opportunity` itself carries company as free text in `name`); (3) candidate contacts **seeded from the moat senders** (the confirmed job-search emails, read-only) as `system_suggested`; (4) a **proof surface** — confirm→`user_authored`, enrich degree/strength/reachability, reject, and **add-contact** (setting `capture_source` email/linkedin/manual, the manual capture route for LinkedIn until S103v's bulk file adapter); (5) **`warm_access_available` derives** from a usable contact linking to the lead's company, the S103t manual tag becomes the **override** (effective = override ?? derived, the D217 precedent), read-side + recomputed (D155); (6) the **warming next-best-action reads the real contact** (S103t shipped none on leads); (7) a lead renders its **linked contacts inline** (list, not a graph — the visual map is S103v).

**Step 0 reconciliations (S103t as-built + pre-S103t model):** S103t reconciled as-built (this prompt predates its commits): Lead gate `name="Lead"`/`gate_order=2`; a lead's company is `:Opportunity.name` before `" — "` (free text, so the join is a normalized string match, not a company node); `warm_access_available` is a `warm`/`cold` string repurposed as the override; S103t shipped **no** warming NBA on leads (they're partitioned out of the card NBA), so S103u adds the contact-specific one. Pre-S103t: `:Contact` is a new label with a `(tenant_id, contact_id)` uniqueness migration **0010** (the 0009 `:MeasurableOutcome` precedent); seeding reuses the D215 system-suggested→proof writes (`merge_contact` mirrors `merge_opportunity`; confirm→`user_authored`, reject deletes); the senders come from `PostgresEmailStore.list_emails().from_address` ∩ `list_job_search_classifications()` (the moat), **read-only** (D21/D148/D151), filtered by the `precision.py` ATS/BOARD/FREE domain lists + a no-reply denylist; company derived from the sender's real domain (title-cased SLD), null for free-domain senders; the derive-warm read is a projection join on the lead company, recomputed never stored (D155). No Postgres write. Stamped S103u/D222 (D221 live max; S104 reserved for direction).

**Scope cut:** the `:Contact` model + email seeding + proof + derived-warm + contact-NBA + inline list only. **Out (→ S103v):** the LinkedIn self-export **file adapter** for bulk import (S103u ships only the manual `linkedin` capture_source tag on add-contact); the **full visual network map**; **warming-step tracking** (intro requested / follow-up sent per contact) and **warmed-to-reply conversion** instrumentation (calibrates per §4 on revealed effort). **Also out:** auto-deriving strength from email volume (a later hint, never a value — the operator authors strength, D200); network path-finding beyond the operator's own contacts (connector-blocked; the platform holds Gmail/Calendar/Drive, not a network graph); the aging rule + period filter (the queued time-windows session).

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103v — the LinkedIn arm, warming-step tracking, the contact network map (D223/D224/D225, BUNDLED) (build mode) — IN FLIGHT

**A deliberately bundled session — three capabilities, one session, an override of the one-per-session discipline that held S103a–S103u** (recorded per the operator's choice; reflection 4 reports whether it cohered). The three build in sequence and are the S103u-named deferrals: the **LinkedIn arm** (D223), **warming-step tracking** (D224), the **contact network map** (D225) — the map renders what the other two populate. One capability was **pulled on evidence**: warmed-to-reply conversion is deferred (zero warming steps + zero replies inform no decision — the metric-needs-a-decision rule). Goal as artefacts: (1) a **LinkedIn source port** whose sole built adapter parses the member's self-export archive (the DMA API a documented deferred adapter — EEA/Switzerland-only, the UK operator ineligible); (2) **connections + message senders** seed `system_suggested` `:Contact` via `merge_contact` (`capture_source`=`linkedin`, `degree`=`first`, company from the export column), deduped against the 27 email contacts on the normalized signature; (3) **warming-step tracking** — log intro-requested/follow-up-sent/referral-asked against a contact/lead as append-only audit events (`warming.step` verb), read back per subject; (4) a **contact network map** — contacts by company, the leads at those companies, derived-warm state + logged warming steps, honest for unproofed contacts.

**Step 0 reconciliations (S103u as-built + vendor + audit + map):** the four S103u facts bind — `:Contact` keyed by **`contact_id`** (a person, not a CDD element), the channel field is **`capture_source`** (never `source`), the seed reuses **`merge_contact`/`confirm_contact`/`delete_contact`** feeding the S103u proof loop, dedup on the **normalized (name, company) signature** (email contacts keyed `uuid5(email)`, LinkedIn `uuid5(name:company)`, skip-on-match preserves proof work + a name-only fallback for company-less message senders). **Vendor:** reconciled against LinkedIn's documented current export (a live archive couldn't be fetched here) — `Connections.csv` has a **3-4-line Notes preamble before the header** (First/Last Name, Email (often blank), Company, Position, Connected On; a `URL` column drifts), so the parser is **header-driven, preamble-skipping**; `messages.csv` keys on **`FROM`**. **Audit (D224):** the faceted reader **supports `resource_type`+`resource_id`+`action_verbs` filtering** (confirmed), so per-subject read-back works with **no fallback** — reuses `AuditPort.emit` + the S103c `audit_events.py` drafting precedent, `app.state.audit_event_reader` + `daily_driver_audit_port`. **Map (D225):** the `#assess-toggle` List/Map mode-toggle pattern (re-render from cached data, no refetch) hosts it beside the contacts list. **Intake:** the ops-file idiom (`make seed-contacts-linkedin FILE=<path>`, the `seed-contacts` precedent) — no new upload stack. **No new node type, no migration** (0010 stays latest; warming steps are Postgres-audit-backed). Read-only on the archive; no vendor SDK in domain. Stamped S103v/D223-D225 (D222 live max; S104 reserved for direction).

**Scope cut:** the LinkedIn file arm + the message-sender seed + warming-step tracking + the network map only. **Out (→ S103w):** **warmed-to-reply conversion** (pulled — needs warming steps + replies to inform a decision; calibrates per §4 on revealed effort); the **DMA Member Portability API** adapter (deferred behind the port, gated on EEA/Switzerland eligibility the UK operator lacks); **automated/scheduled re-import** (the archive is a manual periodic export; live sync is not on the file route); auto-deriving strength from message volume (a later hint, never a value — D200); the aging rule + period filter (the queued time-windows session).



## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103w — the qualification + history layer: stage ladder, contact roles, MEDDPICC-native qualification, activity history + stage-relative freshness (D226-D229, BUNDLED) (build mode) — IN FLIGHT

**A second bundled session (the S103v heuristic: bundle when coupled) — four coupled capabilities that make each process legible.** The qualification model activates against the corrected stages, reads the contact roles for its people fields, and computes freshness from the activity history. Goal as artefacts: (1) a **five-stage ladder** — Lead, Application, Screening, Interviewing, Offer — with **In role** as terminal `won`; (2) **`:Contact.process_role`** (hiring manager / recruiter / HR / champion / interviewer / decision-maker), feeding the qualification's people fields; (3) a **stage-aware qualification model** — eight job-search-native fields (why-open, success-measures, selection-criteria, interview-process, champion, decision-maker, competing-candidates, vetting-checks), each value+`last_touched`, seed-and-proof, **soft-activated** by stage (all visible, active highlighted, rest dimmed); (4) a **per-opportunity activity history** (append-only audit events generalising warming steps); (5) **stage-relative freshness** — a stale field is a risk badge only when active at the current stage.

**Step 0 reconciliations (S103t/u/v as-built):** **`won` already exists** in `CLOSE_REASONS` + the close picker + assessment — so In role is a **relabel of `won`**, `close_opportunity` unchanged. Gates Lead(2)/Apply(3)/Screening(4) via `merge_gate` → add **Interviewing(5)/Offer(6)** + **rename Apply→Application** (code ripple: the apply-advance gate lookup, `next_best_action`'s deep-stage check, the seed — the Lead-column "Apply ▸" button label stays). The "staging state" is the render-only "Needs staging" (Unplaced) column, kept. The **8 qualification fields + timestamps are dynamic-key schemaless props** (`SET o[$key]`, `q_<field>`/`q_<field>_ts`, read as a `q_`-map) — no migration, no `merge_opportunity` widening (a dedicated `set_qualification_field` write); the **activation map is a domain-level code default** (operator-tunable/stored deferred, brief-altitude). **`process_role`** is a schemaless `:Contact` prop. Activity history reuses the **D224 audit route** (retain `warming.step`, add `opportunity.activity`, read the **union** per opportunity — no orphaning); freshness reuses **`staleness.py`** (D187) gated by the activation map. No new node type, **no migration** (0010 stays latest). Stamped S103w/D226-D229 (D225 live max; S104 reserved for direction).

**Scope cut:** the five-stage ladder + In-role relabel + contact roles + the 8-field qualification (soft activation, seed+proof) + activity history + stage-relative freshness only. **Out:** warmed-to-reply conversion (needs warming steps + replies — the metric-needs-a-decision rule); the tier-to-response calibration (needs a lead cohort); exploding Interviewing into per-round gates (the gate library, only if the single column proves coarse); **hard activation** (deferred pending reflection 2); operator-tunable/stored activation map (brief-altitude); the DMA LinkedIn API adapter (UK eligibility); the aging rule + period filter (the queued time-windows session).

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103x — the address-book feeder: Google Contacts behind the source port; capture_source becomes set-valued (D230) (build mode) — IN FLIGHT

**The third contact feeder — the address book, the highest-signal warm ties.** Email (S103u) and LinkedIn (S103v) fed the channel-agnostic `:Contact` graph (D222); the address book is the missing third, people the operator deliberately saved. Goal as artefacts: (1) a **Google Contacts adapter** behind a general `ContactSource` port (People API, read-only), (2) it **seeds `system_suggested` `:Contact`** via `merge_contact` with `capture_source` including **`address_book`**, company from the `organizations` field, deduped on the normalized signature; (3) **`capture_source` becomes set-valued** — a multi-channel contact carries every channel (email + linkedin + address_book), scalars backfilled to single-element sets; (4) an **organisation filter** — only company-carrying contacts seed (a company-less contact can't link to a lead and would flood the proof queue, the reverse-Kano noise); (5) seeded contacts flow through the existing proof loop unchanged.

**Step 0 reconciliations (vendor + consent + as-built):** **People API reconciled** against current Google docs — scope `contacts.readonly`, endpoint `people.connections.list` on `people/me`, field mask `names,emailAddresses,organizations` (org carries company directly, no extraction). **The load-bearing consent finding: the Google connector holds calendar/gmail/tasks/openid but NO contacts scope** (Nango-dashboard-configured, not code) → the live seed is **operator-gated on a re-consent** (add `contacts.readonly` to the Nango Google integration + re-authorise); the adapter + seed ship built + unit-tested, the live run follows the re-consent. The **source port as-built is LinkedIn-specific** (`LinkedInSource`) → a general **`ContactSource`** port + `SourcedContact` added, Google implements it (LinkedIn's unification deferred). The adapter mirrors the **Nango proxy pattern** (httpx, Bearer + Provider-Config-Key + Connection-Id), no vendor SDK in domain. **`capture_source` scalar→set** = a Cypher union + tolerant reads (normalize scalar/list/null via Neo4j-5 `valueType()`) + an idempotent backfill — **no migration**; dedup reuses the S103o signature, same `contact_id` + `address_book` unioned on a match. Read-only on Google. Stamped S103x/D230 (D229 live max; S104 reserved for direction).

**Scope cut:** the set-valued `capture_source` + the Google People API adapter + the org-filtered deduped seed only. **Out:** the vCard/CSV file fallback (deferred — non-Google contacts, if the connector route proves too heavy per reflection 3); deriving strength from channel-count (a later hint, never a silent value — operator ground truth, D200); the DMA LinkedIn API adapter (UK-eligibility-gated); cumulative activation, job-description extraction, CV assessment (separate arcs, not data-flow-coupled here); the aging rule + period filter (the queued time-windows session).
