# Current Package

Active package details. Updated when a new package starts. Archived to `docs/archive/packages/` at package close.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S102 — per-goal LLM-draft + user-proof, with provenance (build mode) — IN FLIGHT

**The first story under the v13 epic: the CDD's meaning becomes authored.** S101's dogfood showed a generic, auto-derived, read-only CDD loses meaning; D200 pivoted to authored-per-goal. **S102 lands the authoring layer (draft + proof + provenance only).** Artefacts at close: (1) a Neo4j migration `0005_authored_cdd.cypher` adding `:Intermediary` + `:External` node types, the `:Lever` extension (a stable `lever_id`, nullable `commitment_id`, `provenance_origin`/`proof_state`/`name`), and the authored `FEEDS`/`INFLUENCES` edges — idempotent, tenant-scoped, behind the `TenantScopedNeo4jSession` wrapper; (2) a per-goal **CDD draft** use case calling `StructuredOutputPort.generate_structured` against the Ollama dev model for each of the 8 live goals, persisting each element `provenance_origin=llm_drafted`, `proof_state=pending`; (3) a **proof** read + accept/reject/correct write paths (correct flips to `user_authored`, reject removes — user-initiated delete allowed); (4) a **proof surface** on `/app` beside the S101 List/Map toggle; (5) `schema.md` authored-layer section (charter-first); (6) green suite incl. tenant-isolation on the new node types + a schema-shape enforcement test tying the migration constraints to the live shape.

**Step 0 reconciliations (live tree):** latest Neo4j migration `0004` → new `0005`; node/edge inventory confirmed (`:Outcome`/`:Lever`/`:Unit`/`:Facet`; `LEVER_FOR`/`SERVES`/`SAME_WORK`); `lever_unique_per_tenant` on `(tenant_id, commitment_id)` confirmed (reconciled: nullable `commitment_id` exempts drafted levers; new `lever_id_unique_per_tenant` carries authored identity — Neo4j exempts nodes missing a constraint property, so both coexist, **brief-altitude, no D201**); `StructuredOutputPort.generate_structured(StructuredOutputRequest{prompt, schema, latency_tier, temperature?, model_hint?}) -> StructuredOutputResponse[dict]` confirmed (the **checkin** context is the exact consumer precedent: domain holds the schema dict + pure mapping, adapter wires the port); wrapper/`neo4j-confined`/`test_no_raw_neo4j_session.py` all bind. **Postgres untouched** (authored CDD is graph-only); Step-0 correction: tenant Alembic revisions are **date-prefixed (`2026…`)**, not `0038` as the brief expected — immaterial here, recorded.

**Scope cut:** author + proof + provenance only. **Out:** the matcher rewrite from goal-linking to element-evidence (S103); the ongoing edit loop + correction-as-learning-signal (S103, v13 increment 2); the co-evolving suggestion loop (increment 3); cross-goal edges/traversal/ripple (D162 post-week); the Flow/process view + portals + `step_state`-to-status (D198 post-week, alongside the authored sequence-step levers). The matcher (`correlate_goal_facets`, the `SERVES` write) is **untouched** this session; no correction is captured as learning signal yet.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103a — authoring completion: surface the outcome, add elements, reclassify across types (build mode) — IN FLIGHT

**Roadmap v13 increment 2's authoring half: the model becomes fully authorable.** S102's proof pass surfaced three gaps that block a usable authored model, all closed here over the S102 layer with **no matcher change**: (1) the drafted expected outcome was persisted but not proofable; (2) no way to **add** an element (the draft produced zero externals across all 8 goals, so externals could not enter at all); (3) no way to **reclassify** a mis-typed element. S103b's matcher rewrite needs this finished authorable model as its target — the same dependency that set the S102 line.

**Goal as artefacts:** (1) the drafted expected outcome renders as a proofable terminal element (accept/edit/reject) — schemaless `authored_outcome_proof_state` + `authored_outcome_origin` props on `:Outcome`, read coalescing the 8 S102 drafts to `pending`/`llm_drafted`; (2) an **add** path creating a user-authored element of any of the four types (`user_authored`/`accepted`, default edge to the outcome), the path by which externals enter; (3) a **reclassify** path per **D201** — preserve node + stable id (moving the value between `lever_id`/`element_id`), flip origin to `user_authored`, flag (never drop) now-invalid incident edges (`needs_review`); (4) add + reclassify + outcome-proof affordances on the CDD lens; (5) tests (add origin/proof, reclassify identity+flag-not-drop, outcome, tenant isolation on the new writes, an edge-grammar guard).

**Step 0 reconciliations (live tree, all confirmed):** migration latest **0005** — **no 0006 needed** (the outcome proof/origin props and the edge `needs_review` flag are schemaless, no constraint); element **type is a node label** (`:Lever`/`:Intermediary`/`:External`), so reclassify is a **label swap with an id-property move** — the `_AUTHORED_NODE` whitelist keys `:Lever` by `lever_id` and `:Intermediary`/`:External` by `element_id`, so crossing the lever boundary moves the id value, not just the label (the wrinkle the brief flagged "correct if wrong"); the drafted outcome **is persisted** as `o.authored_expected_outcome` but with no proof/origin — Commit 1 adds them; the S102 accept/reject/correct paths + `DAILY_DRIVER_CDD_READ`/`WRITE` (D126) confirmed, add/reclassify extend them, **no new permission**; CDD lens on `apps/api/static/daily_driver.html` beside List/Map (`data-mode="cdd"`); deploy idiom `make build-api` (digest pin), live pass operator-gated (S101 idiom). **Postgres untouched** (graph-only). **schema.md changed** (outcome proof/origin props, edge `needs_review`, the S103a authoring-completion note); **D201 minted** (the reclassify correction semantic — framing, per the brief).

**Scope cut:** surface the outcome, add, reclassify only. **Out:** the matcher rewrite to element-evidence (S103b); the relink/unlink of ingested signal + correction-as-learning-signal (S103b); the emergent suggestion loop incl. externals-emergent (S104); strengthening the externals draft prompt vs routing to emergent (decided by reflection-1's tally, lands in S103b/S104); the full edge-edit/re-point loop (the flagged edge is surfaced, not yet resolvable in-place — S103b). The matcher (`correlate_goal_facets`, the `SERVES` write) is **untouched**; grep-confirmed in the diff.

**S103a-fix (surface-only patch).** The operator-gated S103a browser pass found two surface gaps over paths that already work: (1) the S102 correct-and-edit affordance was present but unrecognisable (a `window.prompt`-based ghost button), so the only perceived correction was reject-then-re-add, which breaks element identity + incident edges before the matcher binds; (2) "draft missing CDDs" reported a silent zero when every goal already had a CDD, reading as failure not the correct result. Fix: a clear **in-place** edit control (inline input + Save/Cancel, on each element and the outcome) wired to the existing `/correct` path, and a clear "all goals already have a CDD" empty state reading the existing `skipped_existing` zero-count. **Surface-only** — no schema, no migration, no new endpoint or permission, no matcher; **no D-entry** (exposing an existing path is not a new decision). **Step 0 correction:** the brief framed the edit control as possibly absent; it was present-but-unrecognisable, so the fix made it clear rather than adding a path.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103b — the element-evidence matcher: signal binds to authored elements, not goals (D202) (build mode) — IN FLIGHT

**Roadmap v13 increment 2's learning-signal half (binding), executing D202.** The authoring half (S103a + fix) is closed; this reroutes ingested work onto the authored elements it produced. **Spine:** a unit binds to the nearest authored element *within* its goal via lexical+alias recall applied to per-element targets (the per-element extension of the goal-level recall); multi-attach (a unit may evidence >1 element); goal-level evidence **derived on read** from element evidence, the goal-level `SERVES` write **retired** (D155, no drift); a unit matching no element **parks unbound** as the emergent loop's queue (S104), never dropped.

**Framing fork settled in conversation (Step 0 surfaced it; the operator's call).** Two of the brief's spine assumptions did not hold against the live system, and one scope call was made: **(1) No direction this session.** Direction fields (email from/to, calendar organiser/attendee) are ingested at source but not projected into `WorkFacet`, and — the real blocker — no user-identity reference is stored to orient inbound-vs-user-initiated (`Connection` carries no account email, `MeetingAttendee` no self-flag). Direction's only consumer is the emergent loop and with one authored external it separates almost nothing, so direction + the lever/external orientation + its configured-account-identity (**D203, reserved**) all move to **S104** — a signal lands where it informs a decision (the observability rule). **(2) No embedding tier.** The live matcher (`infer_goal_edges`) is lexical+alias only — there is no embedding tier here — and the email-embedding corpus is empty (S100); so S103b extends lexical+alias to per-element targets honestly and names the embedding tier as later work. **(3) The split.** S103b lands the rewrite + re-match + derive-on-read + unbound bucket + a **read-only** element-evidence/unbound display; relink/unlink + correction capture become **S103c** (with the `contexts/audit/` event path); direction + the emergent loop are **S104**.

**Step 0 reconciliations (live tree):** matcher = `correlate_goal_facets` → `infer_goal_edges` (lexical lever-name + alias, **no embeddings**); the SERVES write is `replace_goal_edges` and the three readers (`list_units_by_goal`, `list_goal_assessment`, `list_facet_suggestions`) consume `list_goal_edges` → `GoalEdge`; `group_units_by_goal`/`goal_status` take `edges` as a **parameter** (pure projections), so derive-at-`list_goal_edges` leaves them byte-unchanged; element ids per S102 (`lever_id`/`element_id`/outcome node); live corpus = **669 SERVES / 1359 units / 31 authored levers + 29 intermediaries + 1 external**; append-only store = `contexts/audit/` (daily_driver emits none today → S103c). Next migration **0006**. D202 + D200 dual-nature addendum minted charter-first; D203 reserved for S104.

**Scope cut:** rewrite + re-match + derive + unbound bucket + read-only display only. **Out:** relink/unlink + correction capture (S103c); consuming corrections to adjust matching (the learning half, next); direction + config-identity + the emergent loop (S104); the embedding recall tier (named, S100 empty corpus); draft-strengthening for external category slots (D200 addendum, deferred). The authored layer (`FEEDS`/`INFLUENCES`, the proof paths) is untouched.

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103c — editability and the learning signal: relink, unlink, capture, re-runnable re-match (D203) (build mode) — IN FLIGHT

**Increment 2's editability half, completing S103b.** S103b bound signal to elements and parked the rest; S103c lets the operator **correct** those bindings and **captures each correction as the learning signal**, and makes the re-match **re-runnable** so authoring a missing goal recovers coverage on existing work. **Two rules make it safe (D203):** (1) **correction precedence** — any relink/unlink makes the unit user-owned, and the re-match never overwrites a user-owned unit (the user outranks the matcher); (2) **idempotent re-match** — re-running adds no duplicate edges and changes no already-correct matcher binding, it only fills newly-authored elements with previously-unbound work. **Unit-level ownership grain** (coarse on purpose; per-element is the named refinement, deferred unless reflection-3 shows it bites).

**Goal as artefacts:** (1) relink/unlink write paths over `EVIDENCES` (single-edge mutation; relink touches one binding of a multi-attach unit), behind `CDD_WRITE`, marking the unit `user_owned` and the edge `user-corrected`; (2) an append-only **correction audit event** per relink/unlink (`AuditPort.emit`, `cdd.relink`/`cdd.unlink`, before/after the binding pair), captured not consumed; (3) a re-runnable, idempotent, correction-respecting re-match (`correlate_goal_facets` excludes user-owned units; `replace_element_evidence` deletes only non-user-owned `EVIDENCES`); (4) the S103b read-only evidence display made interactive (relink/unlink affordances + a re-match trigger); (5) tests + a live in-container guard.

**Step 0 reconciliations (live tree):** EVIDENCES from `0006` mutated via new single-edge Cypher; **correction store = the audit context** (`AuditPort.emit`) — the platform audit-trail principle + the optimisation precedent, the API already builds `audit_adapter`, import-linter's cross-context independence is domain-only so the application-layer `contexts.audit.domain` import is clean, and the faceted reader serves the consume read (the **Step-0 audit-versus-dedicated finding, settled for audit**); user-ownership = `:Unit.user_owned` (schemaless, survives `correlate_units`' MERGE); the re-match endpoint can run `correlate_goal_facets` from app.state (unit_graph/facet_source/goal_graph/commitment_repository/email_job_search_source all wired; quality/suppression ops-only → None); `CDD_WRITE` for relink/unlink, `DAILY_DRIVER_UNITS_CORRELATE` for the re-match; **no migration** (0007 not needed). D203 minted (D202's tentative "D203 reserved for direction" → direction takes the next free number at S104).

**Scope cut:** relink + unlink + capture + re-runnable re-match + interactive display only. **Out:** consuming the captured corrections to adjust matching (the learning half — next session or folded into S104); direction + config-identity + the emergent loop (S104, consumes the unbound queue + these corrections); the embedding recall tier (deferred, re-assess after coverage improves); authoring the missing recurring goals (operator work on the shipped surfaces); per-element ownership (refinement, only if reflection-3 shows unit-level too coarse).

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103c-fix-2 — view coherence: one corrected truth across List, Map, CDD (D202/D203) (build mode, surface + read-side) — IN FLIGHT

**Make List and Map first-class against the element-evidence model.** The §4 one-graph standard: three views = three projections of one corrected truth, not one live view (CDD) and two read-only ones (List, Map). **Step 0 correction (the brief's load-bearing question):** the brief framed List and Map as reading "the older goal-level picture / a stale model" — **they do not**. Since S103b they already derive from element evidence: `/units-by-goal` → `list_units_by_goal` → `unit_graph.list_goal_edges()`, and the bridge's `list_goal_edges` derives from `list_element_evidence` + `derive_goal_edges`. The remaining `SERVES` references are stale docstrings, not a live read. So the **read-coherence gap was already closed at S103b**; the re-point is a no-op (not a separate-model reconciliation, so no scoping fork, no D-entry). The genuine gap is **write-reach**: List and Map are read-only — they lack the unlink/relink the CDD view got at S103c.

**Goal as artefacts:** (1) `outcome_id` on the binding read so List/Map can resolve a unit's element evidence per goal (the read-side enablement for the writes) + the stale `SERVES` docstrings refreshed to element-evidence-derived; (2) **unlink** from List/Map rows (reuses the S103c path — orphans the unit to the unbound bucket, marks user-owned, emits a correction record); (3) **element-level relink** from List/Map via a goal-then-element picker (cross-goal allowed, reuses the S103c relink path); (4) a **consistency guard** so the three views cannot silently diverge again, + correction-parity tests + served-HTML guard. Correction semantics identical across views (D203): the view is just the entry point.

**Scope cut:** surface + read-side only — no schema change, no new write path beyond exposing the S103c unlink/relink from more entry points; matcher logic + the consume side untouched. **Out:** excluding a unit from every view (orphaning to unbound is acceptable); a goal-level relink default (deferred unless reflection-2 shows the element picker is the wrong grain); the embedding tier, the consume side, direction, the emergent loop (later). **No D-entry** (reuses D202's element-level model + D203's correction semantics).

## Phase 2-B, the CDD authoring-and-correction layer (D200, roadmap v13); S103c-fix-3 — make the bug and the why honest (D202/D203) (build mode, surface + read-side) — IN FLIGHT

**A bug fix + read-side honesty; no matcher-logic change.** The operator's live correction pass surfaced two fixes and one classify-only. **Step 0 findings:** (1) **Map unlink fault root-caused — the outcome-kind 422.** `ElementKind` is `{lever, intermediary, external}` with no `outcome`; the weak alias bindings bind to the **outcome** element (`element_kind="outcome"`, S103b), and `renderGoalCorrections` (List/Map) sends every binding's kind to `/cdd/evidence/unlink|relink`, so `ElementKind("outcome")` raises → 422. The graph layer supports outcome (`_AUTHORED_ENDPOINT`); only the router's `ElementKind` validation rejects it. The S103c-fix-2 served-HTML guard passed because it checked the affordance *renders*, not that it *works*. (2) **The "goal name" why is a placeholder, not a token** — for alias bindings the real token is the unit∩goal-name overlap; classify by whether the recompute reproduces one. (3) **"Email activity: 2 applications" under Wide World Marathon is a symptom of spurious binding, not a label change** (the kind label is correct; job-search emails are spuriously bound to the marathon goal) → deferred and named, not a one-line fix.

**Goal as artefacts:** (1) unlink/relink accept the 4-value endpoint kind (incl. `outcome`) so unlink works from Map (and relink to/from the outcome) — the graph already handles it; (2) an **end-to-end functional guard** that unlink+relink actually complete (the edge removed/moved), not merely that the control renders — the live-surface law extended from presence to function; (3) the **honest why** — the real overlapping token where the recompute finds one, "no clear basis" where it cannot, replacing the "goal name" placeholder; the **no-basis count reported** as the spurious-binding signal. **No matcher binding change** (the looseness is a strategic call held outside this session, informed by the no-basis count).

**Scope cut:** surface + read-side + the Map-wiring (kind-validation) fix only. **Out:** fixing the matcher looseness / spurious weak bindings (strategic, informed by the no-basis count); the activity mislabel (a symptom of spurious binding, deferred); the embedding tier, consume side, S104; current-package windowing (flagged at S103c-fix-2). **No D-entry**, no schema.

**S103c-fix (surface-mostly legibility patch).** The S103c correction pass surfaced three gaps that made it a guess rather than a judgment: no **why** (the operator can't see what the matcher matched on, so the captured teaching signal is thin), no **match strength** (no way to triage which bindings to check), and a painful **unlink** (it collapsed the list, no bulk path — throttling the very pass that makes the learning signal). All three close on the read side + surface. **Step 0 finding:** the EVIDENCES edge records `tier` + `basis` but **not the matched term**, so the why and the strength are **recomputed on read** from the deterministic lexical/alias match against the current element vocabulary — no storage change, no migration. **Honest framing:** the matcher is lexical+alias only, so what is shown is **match strength (string-match), not correctness** — tier orders it (exact > keyword), and within keyword a distinctive term outranks an incidental shared one (discriminativeness); a high-strength match on an incidental token is the reflection-1 trap the why exposes. Genuine semantic confidence is an embedding-tier property, deferred. **Surface-mostly** — recompute-on-read + front-end; no migration, no new architectural decision, **no D-entry** (exposing existing match properties and an honest read-side proxy is not a decision); matcher logic + the consume side untouched.

