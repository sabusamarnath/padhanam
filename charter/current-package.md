# Current Package

Active package details. Updated when a new package starts. Archived to `docs/archive/packages/` at package close.

## P11 framed; S39 next

P11 framed at the strategic-mode conversation between S38b close and S39 open on 2026-05-15. D108 commits the optimization-engine substrate as new bounded context at `contexts/optimization/` consuming four producer contexts (evaluation, retrieval_evaluation per D105, run_history, observability) through consumer-defined ports plus wiring adapters, with the `RunHistoryReader` from `contexts/run_history/ports/reader.py` reused per the S33 vintage and three new reader ports defined at S41. The recommendation aggregate carries five fields (category, subject, text, evidence_citations, status); numeric confidence scores stay out on D9 grounds. Four recommendation categories ship at P11 close: retrieval_strategy, model_choice, prompt_revision, cost_optimization. The allowlist closure folds into S39's front half (role-allowlist seed migrations gain the retrieval tool reference); per-invocation retrieval-constraint threading stays Phase 2. HTTP transports at S42 cover three router trees including the P5-deferred evaluation-HTTP carryover absorption. Active testing scheduler integration is P12 territory.

Four sessions firm (S39 allowlist plus `contexts/retrieval_evaluation/` substrate; S40 retrieval evaluation runner and metric computation; S41 `contexts/optimization/` substrate; S42 HTTP transports plus P11 close demo), S43 reserved for optional carryover hygiene. Epic note at [charter/packages/p11-epic.md](packages/p11-epic.md).

**Next Claude Code session.** S39 build session lands the allowlist closure as commit 1 (Alembic migration plus smoke verification adding the retrieval tool reference to seeded role allowlists per the existing migrations) followed by the `contexts/retrieval_evaluation/` substrate as subsequent commits. The S39 prompt drafts at the next Claude.ai conversation handoff.

D107 landed at S38b on 2026-05-15 committing the session-log per-package archival convention with the P0-through-P10 bootstrap; `log/sessions.md` cut from 1879 lines to 56 lines (header plus S38b entry). Forward from S42 close, package archives commit at package-close per D107.

**S39 in flight.** Substrate work for the retrieval evaluation context underway per D108's first commitment and D105's substrate commitments. D109 commits the gold-set domain shape and hash-chain reuse pattern at S39 charter commit. Allowlist closure as commit 2 (forward-only Alembic migration 0012 on the control plane UPDATEs eight seeded role-revision rows — LVTGuide plus seven McKinsey 7-Step roles — to add the retrieval tool reference and recomputes role-revision chain-self-contained hashes per the migration 0010 helper pattern). Audit-context refactor at commit 2.5 extracts `compute_chained_payload_hash` as a thin utility primitive inside `contexts/audit/domain/events.py`; `compute_event_hash` continues to own audit-event input-shaping and calls the helper internally; bit-identity preserved on tenant_a's existing audit chain. Retrieval evaluation context substrate as subsequent commits per D109's six commitments: domain aggregate root plus revision and entry value objects with `compute_revision_hash` wrapper consuming the extracted helper, application use cases for authoring, `GoldSetRepository` write-side port plus `GoldSetReader` consumer-defined read port, Postgres adapter, Alembic migration creating three new per-tenant tables, wiring at both composition roots, CLI surface for discovery-mode gold-set authoring, tenant-isolation contract scenarios, live-stack smoke against tenant_a including hash-chain integrity verification. D109's structural precedent for revision-with-hash-chain is `contexts/methodology/` (not `contexts/evaluation/`'s scoring-sheet, which is read-only at S16); the gold-set is the first Phase 1 context to ship the full create/append/finalize/list/get lifecycle with revision-granularity hash-chain at application-layer granularity. D105's "sibling-contexts-sharing-patterns" framing flagged for the P12 audit per the methodology line in the S39 session log entry; D105 not revised at S39.

**S39b in flight.** Verification-and-hygiene bridge between S39 substrate close and S40 runner work. tenant_a corpus re-ingest from substitute LVT-shaped markdown content (per S39 smoke carryover; S25's original synthetic sources never landed in the repo and are not recoverable), LVTGuide re-seed plus retrieval-allowlist application via new control-plane migration 0013_lvtguide_reseed lifting the LVT system prompt verbatim from briefs/p7/s25.md (per S30b fixture-leak carryover surfaced again at S39 smoke), real-corpus discovery-mode gold-set authoring with the same three queries as S39's synthetic gold-set to produce a structural-comparison artefact for the P12 audit (closes S39 AC 10 verification gap), migration name-length convention captured in log/captures.md as a project-tooling constraint. No new D-entries; this is execution work closing S39's verification gaps.

**S40 closed** at 2026-05-15 with the retrieval-evaluation runner substrate in place end-to-end against tenant_a. Eleven commits closed S40: charter touch-points landing D110 (9e062b7) plus the two new `charter/deferred-decisions.md` entries (parallel_rrf implementation, gold-set aggregate-level audit emission); domain layer with `EvaluationRun` aggregate root, `EvaluationResult` and `EvaluationAggregate` value objects, and metric primitives (a76919b); application use cases (`run_retrieval_evaluation`, `get_evaluation_run`, `list_evaluation_runs`) plus three consumer-defined ports plus 19 new unit tests covering strategy_keys projection, cursor codec round-trip, runner orchestration happy/failure paths, audit-event emission per write, cross-tenant isolation, and pagination (8a86265); Postgres adapters with bound-tenant defence-in-depth and the uuid<uuid cast fix from S33 (b4c44f3); Alembic migration 0014 with status CHECK + terminal-state pairing CHECK + three indices (d9cb409); wiring adapters #13 (`EvaluationRunRepositoryAdapter`) and #14 (`EvaluationRunReaderAdapter`) at the CLI composition root plus `build_evaluation_run_repository`/`build_evaluation_run_reader` factories at the API composition root (e7c02b0); CLI subcommands `evaluation-run start/get/list` with inline `_CliCompositeRetrievalClient` and `_CliRetrievalRunnerPort` wiring helpers, registered under the new `padhanam evaluation-run` typer sub-app (b20262a); twelve new tenant_isolation contract scenarios (8f179fd) extending the harness from 114 to 126; runtime fix at the audit-adapter construction site surfaced at smoke time (21acd36); live-stack smoke against tenant_a's S39b real-corpus gold-set producing run `ef58678a-...` with 6 per-query results plus 2 per-strategy aggregates plus 10 audit events with chain integrity intact (422d9c9). 1105 unit tests pass (+58 from S39b's 1047); 26 import-linter contracts kept. **S40-close verdict on the S39b gold-set's S41 usability: an S40b clean-gold-set bridge session is required before S41 ships.** The S39b gold-set's `expected_chunk_ids` were rank-1 selected from vector retrieval at authoring time and the runner uses the same vector retrieval at evaluation time, so MRR=1.0 is the artefact of two identical functions on the same corpus, not retrieval-quality measurement. Bridge-session-shape methodology candidate confirms at second instance (S39 → S39b verification-and-hygiene; S40 → S40b methodologically-clean-artefact authoring); one more instance at P11 close or P12 would promote.

**S40b closed** at 2026-05-15 with the clean gold-set authored end-to-end against tenant_a and the S41-evidence verdict landed. Five commits closed S40b: charter touch-points at `charter/current-package.md` plus the `.gitignore` rule for `docs/archive/snapshots/` closing the AC 17 cleanliness gap from S40 (ad9d729); corpus content at `tests/fixtures/corpus/p11_s40b/pacelane_recovery_first_case.md` describing a fictional fitness-wearable startup applying LVT — vocabulary divergence from the LVTGuide system prompt is on case-study-specific terms (Pacelane, recovery-first, HRV, Spearman correlation, subscription retention) per the operator's brief-strengthening insight that Query 2 vocabulary needs reframing alongside corpus content (db9cd2d); live-stack smoke against tenant_a (221ef1c) walking corpus refresh, the S39b rename ("P11 retrieval baseline (real corpus)" → "P11 retrieval baseline (rank-selected, S39b)" with hash byte-identical post-rename), clean gold-set authoring with content-fit selection (gold-set `3b001430-...`, finalized revision hash `8fec2553d9...`, three queries with explicit per-entry selection-vs-rank divergence including the borderline "What Pacelane learned" section operator-decided to include), verification re-run producing `c168c2ba-...` with vector_only recall@1 0.555 → 0.400 (−28% relative) / recall@3 1.0 → 0.800 (−20%) / recall@5 1.0 → 0.867 / MRR unchanged at 1.0; this commit landing the session log entry plus this close marker. **S40b-close verdict: contamination demonstrably broke at the recall@k surface; the S40b run can be cited as procurement-grade S41 evidence with the caveat that recall@k and precision@k are the load-bearing metric surfaces and MRR is structurally non-discriminating in this evaluation setup.** Four methodology candidates forwarded to P12 audit: pre-write reconciliation surfacing brief-vs-required-structure gaps (third P11 instance), metric-threshold expectations needing structural-understanding grounding rather than gut-intuition framing (MRR>0.9 threshold operator-owned as wrong implicit assumption), graph-extract pipeline reliability as future hygiene session candidate, and the bridge-session-shape pattern's second instance close. S41 framing brief should privilege recall@k differentials over MRR in evidence-citation specs explicitly.

**S41 closed** at 2026-05-15 with the optimization layer substrate in place end-to-end against tenant_a and the four-context P11 substrate scaffold complete. Twelve commits closed S41: charter touch-points landing D111 (ecbf4c5) plus the vendor-flexibility principle at `charter/principles.md` and the three optimization-table schema additions at `charter/schema.md`; MetricCalculator extraction refactor (fdf2635) with bit-identity preserved across 80 retrieval_evaluation tests; optimization domain layer (fa12fa9) with OptimizationRun + Recommendation + RecommendationStatusTransition + discriminated EvidenceCitation union + RecommendationRule Protocol + 22 domain tests; four default rule implementations + EvidenceContext (11f824c) at the application layer (placement deviation from the brief's domain placement per the structural-honesty layering call; captured as principle-versus-framing drift methodology candidate); application use cases + four ports + audit_events + cursors + citation_serialization (907f365) with 26 new application tests (63 optimization tests total); four Postgres adapters + _tables (9e3da11); Alembic migration 0015 (bcfc393); wiring at CLI + API composition roots as #15-#18 plus import-linter contract extensions including the new layers-optimization hexagonal contract (4545db9); CLI subcommands at `padhanam optimization` and `padhanam optimization-run` (f7e8167); eleven tenant_isolation contract scenarios (e53bf22) extending the harness from 126 to 137 post-migration; live-stack smoke against tenant_a (aedbfa6) producing optimization run `3b538acb-...` with 2 retrieval_strategy recommendations citing S40b's c168c2ba and S39b's ef58678a both carrying CaveatAnnotation on graph_only's all-zero aggregates, cost_optimization substrate-honest zero emission against local dev costs, model_choice and prompt_revision skipped with structured substrate-gap reasons, lifecycle exercise (acknowledge → apply, reject), 7 audit events with full evidence_citations embedded; this commit landing the session log entry plus this close marker. 1166 unit tests pass (+61 from S40b's 1105); 27 import-linter contracts kept (+1 from layers-optimization). **S41-close verdict: procurement-grade defensibility holds end-to-end.** A procurement reader can trace prose → rule → citation → producer-context records (evaluation_run + gold_set) without ambiguity for every recommendation; the all-zero CaveatAnnotation flags graph_only's infrastructure substrate state transparently; the audit chain absorbs both the engine invocation and the recommendation lifecycle with five chain-anchoring points per citation. **The four-context P11 substrate scaffold is complete** (`contexts/retrieval_evaluation/` per D105/D109/D110 + `contexts/run_history/` per D94/D97 + `contexts/audit/` per D102 + `contexts/optimization/` per D108/D111). Methodology candidates forwarded to P12 audit: pre-write reconciliation at fourth P11 instance (now beyond promotion threshold); principle-versus-framing drift as distinct methodology candidate (two S41 instances; captured at `log/captures.md`); scope-check-at-substrate-application-boundary as candidate default for substrate sessions (captured at `log/captures.md`); container-image-lag pattern at smoke time at third P11 instance (promotion candidate for documented dev-workflow fast-path); vendor-flexibility principle operationalisation verdict (MetricCalculator + RecommendationRule structurally honest at consumption-pattern granularity); four-context scaffold complete (the bet's criterion-4 procurement-grade demonstration artefact is in place; HTTP transports at S42 build against this substrate without rework). **S42 next**: HTTP transports across the three router trees (optimization reads, retrieval_evaluation reads, evaluation reads) per D108's seventh commitment, completing the Phase 2 UX consumer surface for the full P5-P11 substrate.

**Pre-P12 hygiene session closed** at 2026-05-16 with the seven scope items landed across six commits. Charter touch-points (commit 41f33d7) updated `charter/current-package.md` with the in-flight marker and created `charter/p12-audit-inputs.md` consolidating fourteen methodology candidates and architectural-honesty observations forwarded from S39 / S39b / S40 / S40b / S41 / S42 plus the hygiene-session investigation outcomes. README expansion (f9c41f1) landed the four operator-confirmed dispositions: methodology framing as living hypothesis (replacing the stale "pending operator authorship per D39" framing); schema.md and deferred-decisions.md added to the charter reading order at positions 5 and 6; Apache 2.0 repositioned as load-bearing strategic commitment under a new "The strategic commitment" section; two new sections covering codebase visibility (six producer contexts plus two applications under contexts/ and apps/) and operational orientation (install, start the stack, apply migrations, seed dev tenants, exercise the CLI surface via docker compose exec, exercise the HTTP surface against https://localhost/openapi.json). Cleanup commit (db285f8) refreshed the compose.yaml padhanam-api digest pin via `make build-api` (the post-S41 target's S42-fix held: sha256:fa829a07277c... → sha256:aa21431690ab...) and removed the stray apps/api/{routers}/ literal-braces empty directory. Graph-extract structural finding (44f3897) landed at `charter/deferred-decisions.md` per Finding 1 disposition: EXTRACTING state has no reclaim-after-timeout transition, extraction LLM call has no worker-boundary timeout; three structural pieces missing (reclaim policy, worker-side asyncio timeout, optimistic-concurrency-control on reclaim) plus operational mitigation at Phase 1 (manual re-trigger via CLI). S37 validator loosening (4dc4c8c) at `contexts/audit/domain/audit_event_record.py` accepts empty correlation_id as legitimate "no inbound HTTP context" signal for engine-internal events from S40 (retrieval_evaluation runner) and S41 (optimization engine); two new unit tests verify the accept path and the still-raises-on-None path; the alternative-(b) backfill migration was rejected because the empty-string state is structurally meaningful. This commit (commit 6) landing the session log entry plus this close marker. 1213 unit tests pass (+1 from S42's 1212 — one parametrize case removed, two new explicit tests added); 148 + 18 contract scenarios + 33 integration api tests unchanged; 27 import-linter contracts kept. **Investigation outcomes captured at session log entry below.** Finding 1 (graph-extract): structural; documented at deferred-decisions per the disposition. Finding 2 (CLI/API wiring parity): defensible asymmetry; no backfill needed because CLI side has explicit cross-context adapter tests at `tests/integration/apps/cli/test_cross_context_adapters.py` and `test_cross_context_tool_adapters.py` plus dedicated reader-adapter tests; API side exercises wiring through route integration tests at `tests/integration/api/test_*_routes.py` (eight test files post-S42) via FastAPI dependency injection. The two surfaces test wiring at different altitudes for procurement-grade defensibility. **P11 session-level work closed.** Next strategic-mode block is the P12 phase audit acting on `charter/p12-audit-inputs.md` entries plus any audit-surfaced additions.

**P12 audit closed** at 2026-05-16. Eleven commits closed P12: charter skeleton landing the findings document and the in-flight marker (commit 1); top-down findings populating section 1 of `charter/p12-audit-findings.md` (commit 2); bottom-up findings populating section 2 (commit 3); fourteen audit-inputs entries dispositioned plus three audit-surfaced entries populating sections 3 and 4 plus the cross-reference index (commit 4); D113 supersession of D39's pending-authorship framing landing at `charter/decisions.md` (commit 5); principles.md line 45 refresh per D113 (commit 6); three deferred-decisions refreshes — gold-set audit emission, graph-extract reliability, plus new optimization-engine cost-threshold tuning entry (commit 7); methodology document v2 substantive update integrating Phase 1's accumulated discipline-pattern data points with the new Session shapes sub-section plus six new Patterns-observed entries (commit 8); `charter/p12-phase-2-inputs.md` consolidating the audit's strategic input set for Phase 2 packaging conversation (commit 9); this commit landing the close marker and packages.md update (commit 10); session log entry (commit 11).

**Audit verdict: Phase 1 closes with the bet's load-bearing claims holding.** A single tenant runs locally with the full stack; one agent can be configured, run, audited, and optimised through the platform's own tooling; the evaluation harness produces meaningful quality signals; the trace capture layer surfaces optimization recommendations procurement readers can verify end-to-end with full evidence-citation traceability; the operator can explain every architectural decision via 112 D-entries each carrying Choice + Reasoning + Alternatives Considered + Kano; the methodology document at v2 captures the architect-implementer pattern with enough specificity that another senior product leader could read it and adopt the discipline. Phase 2 inherits a complete substrate to build against per D93's methodology-as-product positioning. **P11 plus P12 close Phase 1.** The next strategic-mode block is Phase 2 packaging.

**Post-P12 charter-discipline hygiene closed** at 2026-05-16. Seven commits closed the ten-item charter-discipline punch list between P12 audit close and Phase 2 packaging open. Three new discipline conventions landed at `charter/methodology.md` (per-phase decisions archival pattern; deferred-entry closure-by-D-entry convention; principles-D-entry reference convention). Methodology document maintenance pass confirmed the document earns its place as the narrative companion to principles+decisions; the three named compression candidates preserved verbatim. Twenty-two principles in `charter/principles.md` gained parenthetical D-entry references; five required multi-D references (D4+D16, D1+D32, D4+D15, D14+D76, D16+D17+D28). Three stale-framing references at `charter/deferred-decisions.md` refreshed to D113's living-hypothesis framing; the file gained a closure-convention preamble sentence; "HTTP API for ingestion management" gained "Status: closed by D104, 2026-05-14" marker. Pre-write reconciliation finding: no dedicated entry exists for the parallel "HTTP API for evaluation management" carryover; D112 absorbed the work at S42; no Status marker landed because no entry exists. `charter/deferred-decisions.md` reorganised into eight themed sub-sections with table of contents (Architectural primitives; Phase 2 substrate completion; Production-deployment readiness; Workflow context extensions; Methodology and governance; Compliance and security; Tool registry and authoring; Phase 1 close audit findings); entry content preserved verbatim. D106 reservation note inserted between D105 and D107 summaries in active `charter/decisions.md`; D1 through D113 archived to `docs/archive/decisions/phase-1.md` with full content; active file drops from 1589 to 136 lines. **Phase 1 decisions archived; charter surface ready for Phase 2 framing.** The next strategic-mode block is Phase 2 packaging, reading the cleaned charter surface plus `charter/p12-phase-2-inputs.md` Phase 2 entry handoff section.

**Pre-Phase-2 architecture synthesis and methodology refinement closed** at 2026-05-16. Ten commits closed the session between post-P12 charter-discipline hygiene close and Phase 2 packaging open. Commit 1 appended an "Updates since P12 close" section to `charter/p12-phase-2-inputs.md` recording the seven post-P12-hygiene commits plus the refreshed Phase 2 entry handoff artefact list plus the substrate hygiene workitems status. Commits 2-5 authored `charter/architecture.md` v1 with seven sections (Overview, Architectural patterns, Tenancy and jurisdiction, Vendor and dependency posture, Domain primitives, The four-context substrate, Cross-document map) and five Mermaid diagrams (bounded contexts overview; hexagonal architecture within a context; database-per-tenant topology; vendor port-adapter pattern; four-context evaluation-to-recommendation pipeline); the document is the architectural synthesis surface for procurement-grade-touch, phase-audit, and onboarding reading. Commits 6-8 refined `charter/methodology.md`: Foundation super-section introduced grouping What Padhanam is + What's being investigated + new Hypothesis and iteration sub-section; Work organisation super-section restructured into six sub-sections (Strategic tree, Phase lifecycle, Package lifecycle, Session lifecycle, Session shapes, Frameworks); Measurement model section gained hypothesis-framing prose at top. Commit 9 added the three-document pointer at the top of `charter/principles.md` and updated the Charter structure sub-section in methodology.md to include architecture.md as a Strategic constant plus the v3 version log entry. Commit 10 (this commit) lands the close marker plus the session log entry. **Charter surface ready for Phase 2 framing.** The next strategic-mode block is Phase 2 packaging, reading the four-document charter (bet, principles, architecture, methodology) plus the consolidated `charter/p12-phase-2-inputs.md` handoff section.

**Phase 2 design 7-Step arc Step 1 commit closed** at 2026-05-16. Two commits closed the session. The Step 1 deliverables from the Claude.ai Step 1 conversation land charter-grade at `charter/phase-2-design-7step.md`: arc-introduction header committing charter-grade placement per the conversation's Decision 1 (Step 3's prioritised bets become Phase 2 LVT structure at the v6 roadmap entry per D44) and blank-sheet discipline within the bet's success criteria per Decision 2 (bet's core claims hold; Phase 2-specific commitments — D93 framing, three-wave sequencing, mass-market-UX commitment in `log/captures.md` — open to re-derivation); Step 1's problem statement (busy professionals working twelve-plus hour days carrying a portfolio of work and personal goals with calibrated variable pacing; the calibration layer breaks down under load); two context paragraphs (current substitutes that solve slices but require the user to integrate; CoS analogue and the larger CoS-unsupported population gap); five-paragraph dogfooding-evidence record (Posture 1.5 framing lead-in; what the McKinsey 7-Step ProblemFramer role's function-focused system_prompt informed; where four of five conversation sub-prompts extend beyond the template's authored scope; the methodology-extension workitem this surfaces for Phase 2; the bet's methodology-embedding claim now carrying structural-level evidence with behavioural dogfooding still untested); initial-disaggregation paragraph (seven candidates: judgment layer, mirror function, whisperer function, portfolio view persistence, user-authored methodology surface, personal-domain integration, substrate integration) plus Step 2 disaggregator handoff note; two open questions (Step 2's substrate-integration cut; Step 3's dogfooding-vs-broader-population prioritisation framing); Step 1 close paragraph. Step 1 conversation prompt preserved at `briefs/phase-2/design-7step-step-1.md` and this commit-session prompt at `briefs/phase-2/design-7step-step-1-commit.md` per D43, restoring the briefs/ preservation convention after a three-block lapse since P9 open (pre-Phase-2 architecture synthesis, post-P12 charter-discipline hygiene, P12 audit did not preserve briefs). No new D-entries at this session per AC 7; the eight D-entries the deliverables cite (D14, D77, D78, D81, D82, D85, D87, D93) are existing summary lines in active `charter/decisions.md`. **The next strategic-mode block is Phase 2 design 7-Step arc Step 2 (Disaggregation).** Step 2 opens at Claude.ai; the McKinsey 7-Step Disaggregator role's discipline produces a MECE issue tree from the seven initial-disaggregation candidates, addresses the Step 2 open question on substrate-integration cut, and lands its artefacts as a Step 2 section in `charter/phase-2-design-7step.md` via a subsequent commit session. Step 3 prioritises and produces the bets that land as Phase 2 LVT structure at the v6 roadmap entry per D44 plus the Phase 2 PRD per D43.

**Phase 2 design 7-Step arc Step 2 commit closed** at 2026-05-16. Two commits closed the session. The Step 2 section at `charter/phase-2-design-7step.md` lands the disaggregated issue tree at six branches × five sub-branches (Branch 1 portfolio existence as a unified picture; Branch 2 pace calibration for each item; Branch 3 action at the right moment; Branch 4 feedback on whether calibration is working; Branch 5 trust substrate for offloading; Branch 6 signal fidelity and methodology-fit), two cross-cutting disciplines (detection at every platform-user interface; the four-stage temporal lifecycle of find-rhythm, settle-in, watch, adapt), MECE self-challenge (closest cross-branch pairs identified as 4.5-and-6.5 and 1.5-and-2.4, both held as distinct work units; items deliberately excluded named explicitly), the Step 2 dogfooding-evidence record (what the McKinsey 7-Step Disaggregator role authored at S26b informed; where two extensions — temporal-lifecycle discipline and orthogonal-dimension handling — surfaced outside the role's system_prompt; the role's extensibility-without-breaking as procurement-grade evidence signal), and four open questions carried forward to Step 3 (dogfooding-only versus broader-population framing as prioritisation input; substrate-type × integration-function matrix at sub-branch 1.1; dependency ordering across branches with Branch 5 as partially-independent foundation; lifecycle-stage prioritisation across all branches versus full-lifecycle for fewer branches). Two structural insights surfaced during the Step 2 conversation: Branch 6 (signal fidelity and methodology-fit) was added because the initial five-branch shape did not accommodate the platform-to-user signal-verification work distinct from Branch 5's user-to-platform trust; the four-stage temporal lifecycle elevated from a methodology-fit sub-concern to cross-cutting discipline applying at every branch. The Step 2 retrospective brief at `briefs/phase-2/design-7step-step-2.md` (flagged in the file as synthetic retrospective construction because Step 2 transitioned from Step 1 close within the same Claude.ai conversation; no separate opening prompt existed) and this commit-session prompt at `briefs/phase-2/design-7step-step-2-commit.md` preserve the briefs/ discipline per D43; the synthetic-retrospective shape is honestly framed as a recoverable artefact of the same-conversation transition, not the target convention. No new D-entries at this session per AC 7; D82 (intelligence-layer commitment cited at sub-branch 3.5) and D85 (McKinsey 7-Step methodology authoring placement cited in the dogfooding-evidence record) are existing summary lines in active `charter/decisions.md`. **The next strategic-mode block is Phase 2 design 7-Step arc Step 3 (Prioritisation).** Step 3 opens at Claude.ai with the full thirty-sub-problem set as the impact-tractability scoring surface plus the four Step 2 open questions as inputs; Step 3 outputs become the prioritised bets that land as Phase 2 LVT structure at the v6 roadmap entry per D44 plus the Phase 2 PRD per D43. The Step 3 pre-conversation brief should land at `briefs/phase-2/design-7step-step-3.md` before the Step 3 Claude.ai conversation opens, restoring full briefs/ discipline (synthetic-retrospective shape at Step 2 is the recurrence test for the methodology line landing at Step 1 commit).

**Phase 2 design 7-Step arc Step 3 commit closed** at 2026-05-16. Two commits closed the session. The Step 3 section at `charter/phase-2-design-7step.md` lands the prioritised list (thirty sub-problems scored on impact-tractability per the McKinsey 2x2 matrix; tier clustering at scores 10, 9, 8, 7, 6, 5), the top quartile flagged at both strict (5-item: 1.3 State persistence, 1.1 Substrate connection, 2.1 Methodology library, 3.1 Surfacing mechanics, 5.1 Audit visibility) and inclusive (11-item, adding 1.5 User-authored items, 3.2 Drop-decision support, 4.1 Mirror surface, 4.2 Goal-state tracking, 5.4 Intelligence-layer guardrails, 6.3 Status veracity) cuts, self-challenge (dependency awareness held; operator-as-first-instance population framing held; rules-versus-learned tractability framing held), the Step 3 dogfooding-evidence record at third-instance evidence of the methodology-template-extensibility-without-breaking pattern across the ProblemFramer (Step 1), Disaggregator (Step 2), and Prioritiser (Step 3) roles, and five open questions carrying forward to Step 4 (workplan granularity per-sub-problem versus per-priority-cluster; substrate-type × integration-function matrix sequencing at 1.1 across twenty-eight cells; dependency-versus-priority sequencing within the workplan; lifecycle-stage prioritisation strategy across the four-stage discipline; messaging-first delivery design constraint and meta-signal observability for trust-history 5.5). Three pre-conversation decisions framed scoring: operator-as-first-instance population scope, pure impact-tractability per McKinsey template (RICE held for Phase 2 LVT placement), distribute cross-cutting disciplines into per-sub-problem rationale rather than score them as separate items. Two operator pushbacks during scoring sharpened the prioritised list (3.4 Delegation reframed to AI-plus-human scope lifting score from 4 to 7; 5.5 Trust history meta-signal value recognition lifting score from 5 to 6). Two scope expansions in the substrate-type matrix at sub-problem 1.1 (documents and messaging joined calendar, email, notes, manual entry, existing trackers as substrate types; messaging additionally functions as primary delivery interface for sub-problem 3.1's surfacing mechanics carrying forward to Step 4 as design constraint). The Step 3 pre-conversation brief at `briefs/phase-2/design-7step-step-3.md` (authored pre-conversation per the synthetic-retrospective-brief-pattern recurrence test from Step 2's methodology line; pattern stays at single instance) and this commit-session prompt at `briefs/phase-2/design-7step-step-3-commit.md` preserve the briefs/ discipline per D43. No new D-entries at this session per AC 7; D85 (McKinsey 7-Step methodology authoring placement cited in the dogfooding-evidence record) is an existing summary line in active `charter/decisions.md`. The methodology-template-extensibility-without-breaking pattern at three-instance evidence base lifts the Phase 2 methodology-extension workitem from two-instance pattern-based candidate to three-instance evidence base. **The next strategic-mode block is Phase 2 design 7-Step arc Step 4 (Plan).** Step 4 opens at Claude.ai with the top quartile as the workplan surface plus the five open questions as planning inputs; Step 4 outputs the workplan that lands as a Step 4 section in `charter/phase-2-design-7step.md` via a subsequent commit session. The Step 4 pre-conversation brief should land at `briefs/phase-2/design-7step-step-4.md` before the Step 4 Claude.ai conversation opens, continuing the briefs/ discipline restoration test from Step 3.

**Phase 2 design 7-Step arc Step 4 commit closed** at 2026-05-17. Three commits closed the session (the first three-commit session in the arc; prior Steps closed with two commits each). The Step 4 section at `charter/phase-2-design-7step.md` carries eleven workplan entries spanning the inclusive top quartile from Step 3, with the McKinsey Planner role's six-field structure (hypothesis, analyses to be run, data needed, owner, deadline, deliverable) applied per sub-problem across all six branches (1.3 State persistence, 1.1 Substrate connection, 1.5 User-authored items; 2.1 Methodology library; 3.1 Surfacing mechanics, 3.2 Drop-decision support; 4.1 Mirror surface, 4.2 Goal-state tracking; 5.1 Audit visibility, 5.4 Intelligence-layer guardrails; 6.3 Status veracity). Five pre-conversation operator decisions framed the workplan: inclusive eleven-item cut over strict five-item, per-sub-problem granularity, score-first within dependency constraints, find-rhythm-plus-settle-in coverage across all priority items first, owner by role-function distribution. Substantive mid-conversation scope additions reshaped multiple entries: multi-device sync architectural commitment at 1.3 (Phase 2-A single-device with Phase 2-B multi-device); work apps as eighth substrate type at 1.1 (matrix lifted from twenty-eight to thirty-two cells); methodology library expansion at 2.1 across audit-trail-lineage for methodology adaptation, minimum-viable matching at find-rhythm-plus-settle-in stages, and effect-first comprehension surface with lightweight-versus-deep-application bifurcation; three-tier consent-and-awareness framework at 5.4 reframed as commercial positioning differentiator beyond safety hygiene with Tier 1 real-time review, Tier 2 user-controlled digest cadence, and Tier 3 silent operation explicitly absent. Two design constraints integrated mid-conversation from competitor research input: senior-leader ICP refinement (sharpening the Step 1 broader busy-professional framing to senior leaders at established firms augmenting human EA/CoS plus founders at early-stage tier, with vertical-wedge candidates in financial services, legal, healthcare for Phase 2-B or Phase 3) and voice as ninth substrate type at 1.1 plus secondary delivery channel at 3.1 (Phase 2-A messaging-first stays primary; Phase 2-B adds voice based on dogfooding evidence). The Step 4 dogfooding-evidence record at fourth-instance structural evidence of the methodology-template-extensibility-without-breaking pattern across all four sequential roles (ProblemFramer, Disaggregator, Prioritiser, Planner) spanning the McKinsey 7-Step's analytical arc; the Phase 2 methodology-extension workitem moves from three-instance candidate to four-instance observed-pattern with clear shape (short-term role-system-prompt expansion encoding the discipline extensions; long-term skills-per-role surface per the Phase 2 deferred commitment). Five open questions carry forward to Step 5 (senior-leader ICP commitment landing surface; measurement substrate for "won't know until real users"; architectural pattern surfacing including revision-with-lineage, conversation-flow, and three-tier consent-and-awareness patterns; Phase 2-A versus Phase 2-B sequencing; Phase 2-B workitem cluster). Competitor catalog landed as charter reference at `charter/competitors.md` carrying twenty-two named entries across hyperscaler horizontals, autonomous assistants, AI Chief of Staff, and adjacent-and-other categories, with end-of-Phase-3 competitive landscape review explicitly framed as the binding strategy artefact (the catalog is reference material informing Phase 2 design decisions, not binding strategy). The Step 4 pre-conversation brief at `briefs/phase-2/design-7step-step-4.md` (authored pre-conversation per the Step 3 discipline-correction; the brief landed before substantive Step 4 work began but within the same Claude.ai conversation as Step 3, straddling pre-conversation and synthetic-retrospective shapes per the methodology line in the session log entry) and this commit-session prompt at `briefs/phase-2/design-7step-step-4-commit.md` preserve the briefs/ discipline per D43. No new D-entries at this session per AC 9; D85 (McKinsey 7-Step methodology authoring placement) and D82 (intelligence-layer commitment) cited in the dogfooding-evidence record and three-tier framework respectively are existing summary lines in active `charter/decisions.md`. **The next strategic-mode block is Phase 2 design 7-Step arc Step 5 (Analyse).** Step 5 opens at Claude.ai with the eleven workplan entries plus the five open questions as analytical inputs; the McKinsey Analyst role's discipline gathers evidence and produces findings that downstream Steps 6 (Synthesise) and 7 (Tell the story) operationalise. The Step 5 pre-conversation brief authors at `briefs/phase-2/design-7step-step-5.md` before the Claude.ai conversation opens, continuing the briefs/ discipline.

**Step 5 (Analyse) closed at strategic mode on 2026-05-18.** Eleven sub-problem analyses produced at design-architectural altitude per Posture 1.5 across the inclusive top quartile from Step 3. Pass 2 cross-cutting work surfaced five architectural patterns (revision-with-lineage; conversation flow; three-tier consent-and-awareness framework; tiered-by-salience candidate; two-vector decay model candidate), sequenced Phase 2-A into four waves, clustered fifty-two Phase 2-B candidates across ten clusters with Tier 4 activation map. Pass 3 close delivered five-instance methodology-template-extensibility-without-breaking evidence and sixteen carry-forward questions to Step 6. Senior-leader ICP commitment landed at `charter/phase-2-user-segment.md` per Step 5 Decision 1.

**Phase 2 design 7-Step arc Step 6 (Synthesise) Pass 1 plus Pass 2 closed** at 2026-05-19. Sixteen carry-forward question dispositions plus eight-package Phase 2 LVT structure (P13-P20 across Phase 2-A Waves 1-4 and Phase 2-B Waves 1-4 with Cluster B9 elevated at Phase 2-B Wave 1 per Q16) landed at `briefs/phase-2/design-7step-step-6-interim.md` as substrate the Pass 3 conversation reads and appends to. Pass 3 (integrated storyline construction per pyramid principle override on the Synthesiser role) opens in a fresh Claude.ai conversation per operator's pause-between-passes decision. Pass 3 brief at `briefs/phase-2/design-7step-step-6-pass-3.md`. The full Step 6 commit landing the Step 6 section at `charter/phase-2-design-7step.md` plus charter additions (D-entries, principles.md line, architecture.md additions, deferred-decisions.md entries, `charter/packages.md` updates) drafts after Pass 3 closes.

**Post-P12 methodology comparison strategic block closed** at 2026-05-20. The block opened from a brief to add a methodology comparison act to `charter/deck.html` and revised its deliverable on operator decision mid-session. `charter/deferred-decisions.md` gained three entries at the end of the "Methodology and governance enhancements" sub-section: maintenance and continuous-operation discipline as a load-bearing methodology workitem (forward commitment; initial articulation at Phase 1 close drawing on D78 personal-use evidence); the multi-implementer extension hypothesis covering single team and team-of-teams per McChrystal (forward hypothesis; no specific phase-boundary commitment, activating when evidence at non-solo altitudes accumulates per the hypothesise-test-iterate discipline); and the methodology comparison act for the case-study deck (deferred to Phase 2 close). The comparison content — an operator-authored preamble and close plus a seven-methodology, eight-dimension comparison table — landed as a draft at the new `charter/methodology-comparison.md` rather than as a deck act, because the table makes procurement-facing factual claims about six named third-party methodologies that need human verification; verification and deck integration are deferred together to Phase 2 close per the operator decision. `charter/deck.html` was not modified. No new D-entries. The next strategic-mode block is Phase 2 design 7-Step arc Step 6 Pass 3, continuing separately.

**Phase 2 design 7-Step arc Step 6 (Synthesise) Pass 3 closed** at 2026-05-20. Integrated storyline (top-line answer plus five supporting arguments plus evidence trail) plus sixth-instance dogfooding-evidence record (six Step 6 reflection prompts answered; three methodology lines) plus Step 7 carry-forward landed at `briefs/phase-2/design-7step-step-6-interim.md`. Step 6 substantive work closes; Pass 3 commit prompt at `briefs/phase-2/design-7step-step-6-pass-3-commit.md`. The full Step 6 commit landing the Step 6 section at `charter/phase-2-design-7step.md` plus charter additions (D-entries from Pass 1 dispositions including six architectural primitives plus latency-tier inference routing plus Q6 messaging-channel plus Q11 methodology-extension plus Q13 no-silent-operation plus Q14 latency-tier plus Q15 agent-runtime plus Q16 B9 elevation; principles.md no-silent-operation addition; architecture.md primitive additions; deferred-decisions.md entries for Q8, Q9, Q10, Q12; `charter/packages.md` Phase 2 P13-P20 structure) drafts next.

**Phase 2 design 7-Step arc Step 6 (Synthesise) closed** at 2026-05-20. Full canonical Step 6 section landed at `charter/phase-2-design-7step.md`; ten D-entries (D114-D123) landed at `charter/decisions.md`; no-silent-operation principle elevated to charter-grade at `charter/principles.md` User safety section; six architectural primitives (revision-with-lineage, conversation flow, three-tier consent-and-awareness, tiered-by-salience, two-vector decay model, latency-tier inference routing) landed at `charter/architecture.md` new Phase 2 architectural primitives section plus latency-tier sub-paragraph at LiteLLM subsection; four deferred-decisions entries (Phase 2-B Wave 4 boundary, Tier 4 activation triggers, identity-fork threshold, twelve event classes) landed at `charter/deferred-decisions.md` new Phase 2 design 7-Step deferrals section; Phase 2 P13-P20 package structure landed at `charter/packages.md`; Step 6 brief preserved at `briefs/phase-2/design-7step-step-6.md`; interim record archived via header note at `briefs/phase-2/design-7step-step-6-interim.md` with content preserved as historical authoring substrate. Step 7 (Communicate) opens as the next strategic-mode block at a fresh Claude.ai conversation per arc precedent; Step 7 brief authors pre-conversation at `briefs/phase-2/design-7step-step-7.md`.

**Phase 2 design 7-Step arc Step 7 (Communicate) closed; design 7-Step arc closes** at 2026-05-20. Step 7 canonical section landed at `charter/phase-2-design-7step.md` carrying executive summary plus structured prose body across five Pass 3 supporting arguments plus appendix-shaped evidence trail plus package-timeline mermaid diagram plus design 7-Step arc close prose plus seventh-instance dogfooding-evidence record plus Step 7 carry-forward to Phase 2 build. Three Phase 2 audit input entries landed at `charter/phase-2-audit-inputs.md` (deliberate-variation as charter discipline; communication-content discipline as fourth methodology stream; seven-category methodology-extension set as P17 substrate). Design 7-Step arc closes; arc duration spanned multiple weeks across multiple Claude.ai conversations with one deliberate methodology variation at Step 7 substantive work. Phase 2-A P13 framing opens as the next strategic-mode block.

**Phase 2-A P13 framing brief landed** at 2026-05-20. Brief preserved at `briefs/p13/framing.md` carrying seven pre-conversation decisions (substrate-completion carryover; hygiene workitems; session granularity at four sessions; roadmap v6 entry; Phase 2 PRD shape; spec-as-architectural-input commitment; forward-compat substrate-depth classification with build-now plus defer-with-trigger plus flag-for-future-testing categories); three substantive work-streams (epic note authoring; first-session prompt drafting; roadmap plus PRD plus spec-extracted charter additions); six reflection prompts at session close. Operator-supplied Private Assistant Platform spec preserved at `docs/notes/spec-private-assistant-platform.md`. Phase 2-A P13 framing substantive conversation opens at Claude.ai in a fresh thread per the briefs/ discipline pattern extended by analogy from arc-step framing (six-instance evidenced) to package framing (first instance).

**Phase 2-A P13 framing closed** at 2026-05-21. Six drafted artefacts plus two close deliverables from the P13 framing substantive conversation landed across a nine-commit landing session: P13 epic note at `charter/packages/p13-epic.md`; spec-extracted Phase 2 principles plus the karma domain vocabulary at `charter/principles.md` and `charter/architecture.md`; the PA communication discipline principle at `charter/principles.md`; two defer-with-trigger plus six flag-for-future-testing entries in a new `charter/deferred-decisions.md` section; roadmap v6 entry at `charter/roadmap.md`; Phase 2 PRD section at `charter/prfaq.md`; first-session prompt at `briefs/p13/s43.md`; landing-commit prompt preserved at `briefs/p13/framing-landing-commit.md`; karma prior-art spec relocated to `docs/notes/prior-art-karma/spec.md` (partial Block 2 Part B closure per Decision 6 Sub-decision A). The substantive conversation resolved seven pre-conversation decisions (five revised or refined, two adopted) and surfaced the three-mode product picture (attentional plus workflow plus observation-and-suggestion) plus the meta-layer positioning. Pre-write reconciliation at the landing session corrected a D-number scramble (the three architecture primitives now cite D116 three-tier consent / D117 tiered-by-salience / D118 two-vector decay per `charter/decisions.md`) and reshaped roadmap v6 to the existing version-log bullet convention. Pre-P13 hygiene session next (schema.md formalisation plus doc-content rebrand per Decision 2 triage); P13 Wave 1 first build session (S43) follows per `briefs/p13/s43.md`.

**Pre-P13 hygiene closed** at 2026-05-21. Three substantive commits landed two carryover hygiene items plus one new Phase 2-A close hygiene list surface. `charter/schema.md` gained a "Cross-cutting binding shapes" section formalising TenantContext, ErrorResponse, and the canonical cursor codec per P12 audit Finding B3 plus B5; the field tables reflect verified current code state, absorbing four points of drift from the audit-finding snapshots. The old-platform-name find-replace ran the verification grep — widened to cover both former platform names per the old-platform-name-scrubbing principle — against current narrative state; non-allowlist matches in `charter/methodology.md`, `charter/packages.md`, and `charter/packages/p13-epic.md` rephrased per the rebrand-playbook verification discipline, with the dated `docs/notes/status-2026-05-06.md` snapshot exempt as structurally historical content. `charter/phase-2-audit-inputs.md` gained a "Phase 2-A close hygiene list" section with the roadmap stale-headers entry as first item. S43 first build session opens next per `briefs/p13/s43.md`.

**P13 in flight; S43 in flight.** S43 — the `contexts/portfolio/` foundational domain layer — opened on 2026-05-21. Charter updates landed at commit 1: D124 (portfolio context substrate — Case/DataPoint/Assertion domain entities plus three per-tenant tables) and D125 (Revisable Protocol shape — a generic `Protocol` in `shared_kernel/`). `charter/schema.md` gained a "Portfolio context substrate" section plus Revisable Protocol and AssertionChange sub-sections under "Cross-cutting binding shapes". Substrate commits 2-10 follow per `briefs/p13/s43.md`.

**S43 closed at 7/10 commits per substrate-and-transport split** at 2026-05-21. Seven commits landed the portfolio context substrate end-to-end through the CLI surface: D124/D125 plus schema.md plus epic plus current-package plus brief (d0ada39); Revisable Protocol plus ActorReference at `shared_kernel/` (0484657); Case/DataPoint/Assertion entities plus skeleton plus import-linter at 28/28 (70b94e1); Alembic 0016 migration applied to both tenant synthetic DBs (121ecc1); ports plus Postgres repository/reader adapters with 13 contract tests (801264e); 5 application use cases plus cursor codec plus audit events with 11 unit tests (ff87b96); padhanam portfolio CLI subcommand with 4 CLI tests (3ddff81). 62 new tests passing; 28 of 28 import-linter contracts kept; AST enforcement green. Three forced ordering/shape adjustments from the brief absorbed and flagged (commits 2/3 swapped on dependency graph; jurisdiction restored per D12; ports landed with adapter commit). HTTP transport surface plus live-stack smoke deferred to S43b at operator decision at substrate-completion boundary; the planned-bridge-session sub-variant is first-instance methodology candidate. S43b lands HTTP transport plus live smoke next per `briefs/p13/s43b.md`.

**S43b in flight.** Portfolio context HTTP transport surface plus live-stack smoke; planned bridge session (substrate-and-transport split) per `briefs/p13/s43b.md`.

**S43b closed; S44 next** at 2026-05-21. The portfolio context HTTP transport surface plus live-stack smoke landed across five commits: retroactive S43 entry plus current-package markers plus brief preservation (aba4053); HTTP transport surface — two GET routes, DTOs, query parser, error handlers, API composition-root wiring (f2a8ec8); HTTP contract tests plus tenant-isolation scenarios plus the integration lifecycle test (00c0f5f); image rebuild plus migration verification plus the live-stack smoke against tenant_a (11c059f); this session log entry plus close marker. 16 new tests at S43b; 28 of 28 import-linter contracts kept; AST enforcement green. The live smoke exercised the full Case-DataPoint-Assertion chain end-to-end (CLI write, HTTP read, audit trail) on a freshly-rebuilt image with 0016 freshly applied to both tenant containers — the S43 deployment-honesty deferral resolved. The planned substrate-and-transport split (S43 substrate, S43b transport) is the first instance of the planned-bridge-session sub-variant. The portfolio context is complete end-to-end through the read surface; write-side HTTP, the ActorContext extension replacing the ActorReference placeholder, the authorisation decorator, the intake record, and the three-tier consent-and-awareness architecture-primitive commit defer to S44. S44 next.

**Pre-S44 hygiene closed** at 2026-05-21. Two commits closed the session: the methodology promotion plus the session log entry. The captures-backfill commit the brief planned collapsed to empty — pre-write reconciliation found all three S43/S43b methodology candidates (planned-bridge-session sub-variant, brief path-drift, substrate-completion-versus-deployment honesty) already at `log/captures.md` from the S43b close commit, so the backfill folded away per the brief's conditional logic. The substantive commit promoted the brief path-drift sub-pattern to a *Path-naming sub-pattern* paragraph under the pre-write reconciliation Patterns-observed entry at `charter/methodology.md`, and re-triaged the captures path-drift entry as resolved. Promotion landed at two verified P13 instances, not the three the S43b close framing carried: S43 (brief named a flat adapter path against the actual `adapters/outbound/postgres/` subdirectory convention) and S43b (brief named a `tests/contract/http/portfolio/` subdirectory against the flat `tests/contract/http/` convention). S40 reclassified at instance verification — its session-log methodology lines record strategy-enumeration drift (D66 catalogues three retrieval strategies, the adapter executes two), not path naming. Two-instance promotion holds per the corrective-discipline-on-first-instance precedent. The planned-bridge-session sub-variant and substrate-completion-versus-deployment honesty candidates stay at `log/captures.md` as pending recurrence with explicit second-instance thresholds named. The pre-write reconciliation that drove this session caught a brief-vs-codebase drift in the hygiene brief's own framing — the S43 path-drift direction was inverted across the brief's templates — which is the cleanest possible validation of the discipline the methodology entry promotes. S44 next.

**P13 in flight; S44a in flight.** S44a — the ActorContext identity-and-permissions substrate — opened on 2026-05-21. Pre-write reconciliation across six surfaces reshaped the framing-time D126 bundle before any code: the central finding is that ActorReference cannot be superseded by ActorContext at the persisted `authored_by` field — a single `authored_by_user_id` text column cannot round-trip a request-scoped authorisation envelope, and freezing an `authorisation_set` onto a permanent record would misrepresent identity as capability. D126 therefore retains ActorReference unchanged as the persisted authoring-identity value object, introduces ActorContext as new for request-scoped capability, and supersedes D124's forward commitment (the first instance of a D-entry superseding an earlier D-entry's forward commitment). Charter updates landed at commit 1: D126 (ActorContext value object, authorisation decorator at the use-case boundary, ActorReference retention); the p13-epic Session 2 line split into S44a/S44b; `charter/schema.md` gained an ActorContext sub-section under Cross-cutting binding shapes. Substrate commits 2-8 follow per `briefs/p13/s44a.md`; the brief's nine-commit plan dropped to eight when the ActorReference-deletion commit fell away.

**S44a closed; S44b next** at 2026-05-22. The Phase 2-A identity-and-permissions substrate landed end-to-end across eight commits: D126 plus the p13-epic / schema.md / current-package touch-points (4254c43); the ActorContext frozen dataclass at `shared_kernel/actor_context.py` (b98c226); the `requires_authorisation` decorator, `AuthorisationDenied`, and the Phase 2-A role-to-authorisation policy at `shared_kernel/authorisation.py` plus the 403 translation at `apps/api/_auth_errors.py` (fa5c9cf); the `get_actor_context` dependency at `apps/api/middleware.py` (e1060de); the five portfolio use cases consuming ActorContext and applying the decorator, plus the CLI ActorContext synthesis (2b8539c); the portfolio router wiring `get_actor_context` (8222813); the live-stack smoke plus the compose.yaml digest-pin advance (908c88a); this session log entry plus close marker. Roughly 41 new tests; the full unit-plus-contract-plus-integration run is 1685 passed, with two pre-existing environmental failures in `contexts/methodology` and `contexts/tools` unrelated to S44a (both fail in isolation against the live control-plane DB state; S44a touched zero files in either context's dependency closure). 28 of 28 import-linter contracts kept; AST enforcement green. The central pre-write-reconciliation finding reshaped D126: ActorReference is retained as the persisted authoring-identity value object and ActorContext is introduced as new for request-scoped capability, superseding D124's forward commitment. The six-stage live-stack smoke verified the full ActorContext propagation path against tenant_a on a freshly-rebuilt image. S44b lands the write-path substrate and transport (the `contexts/intake/` bounded context, write-side portfolio HTTP, intake HTTP) per the planned-bridge sub-variant.

**S44b in flight.** S44b — the intake context substrate plus write-side HTTP plus intake-canonical orchestration — opened on 2026-05-22. Pre-write reconciliation across eight surfaces (the seven from the brief plus the operator-added cross-context-contract verification surface) reshaped the build before any code: the central finding is that the brief's orchestration design (orchestrations in `contexts/intake/application/` directly importing `contexts/portfolio/application/` use cases) violates the import-linter "Cross-context: application layers are independent" contract (D16/D17/D28) — resolved by the consumer-port pattern (a `PortfolioWriter` port the `apps/` composition layer implements). Two further structural findings: `create_data_point` was an uncovered write surface (resolved with a third orchestration `record_intake_and_create_data_point` plus a POST `/api/v1/data_points` route), and the brief's single-transaction claim was not achievable (resolved with two-transaction intake-first ordering, the orphaned-intake-on-failure being the honest record-of-attempt). Charter updates landed at commit 1: D127 (intake substrate plus the three orchestrations on a consumer port) and D128 (intake-canonical commitment scoped to the platform's write surfaces); the p13-epic Session 2b line; `charter/schema.md` gained an Intake context substrate section. Twelve commits per `briefs/p13/s44b.md`; the framing eleven-commit plan grew to twelve when the create_data_point gap surfaced.

**S44b closed; S45 next** at 2026-05-22. The intake context substrate plus write-side HTTP plus intake-canonical orchestration landed end-to-end across twelve commits: D127/D128 plus the charter touch-points (eda60de); the `contexts/intake/` domain layer (bd0f9b1); the repository port, Postgres adapter, and Alembic 0017 (0993e20); the three standalone use cases plus three intake permissions (a003749); Alembic 0018 plus `intake_id` on the portfolio substrate (429ec7f); the `PortfolioWriter` consumer port plus the create-case and revise orchestrations (ff16b2d); the `record_intake_and_create_data_point` orchestration (a9a89db); the write-side portfolio HTTP routes (affa966); the intake HTTP routes (181f86e); the CLI write path via the orchestrations (1d0ccf4); the live-stack smoke (190c346); this session log entry plus close marker. Roughly 90 new tests; the full unit-plus-contract-plus-integration run is 1746 passed, with the same two pre-existing environmental failures in `contexts/methodology` and `contexts/tools` carried from S44a (both fail in isolation against the live control-plane DB state; S44b touched zero files in either context). 29 of 29 import-linter contracts kept (28 plus the new `layers-intake`); AST enforcement green. The central pre-write-reconciliation finding reshaped the orchestration design: the brief's direct cross-context import was inverted against the import-linter contract, resolved by the consumer-defined `PortfolioWriter` port. The eight-stage live-stack smoke verified the intake-canonical propagation path against tenant_a on a freshly-rebuilt image with migrations 0017/0018 deployed to both tenant planes. The portfolio context is complete end-to-end through both the read and write surfaces; the intake substrate is in place. S45 — the messaging substrate and conversational protocol per the p13-epic Session 3 line — is next.

**Pre-S45 principles assessment closed; S45 next** at 2026-05-22. A strategic-mode assessment session ran the first code-altitude software-engineering-principles audit — KISS, DRY, the five SOLID letters individually, YAGNI, and Tell Don't Ask — against the Phase 2-A codebase at S44b close. Three commits: the findings document at the new `audits/p13-principles-baseline.md` plus the brief preserved at `briefs/p13/pre-s45-principles-assessment.md` (e66059c); three methodology-observation entries at `log/captures.md` (0c7cc07); this session log entry plus this close marker. The findings document carries one load-bearing finding (the Revisable Protocol's contract-test harness is asserted by three charter surfaces but does not exist — already a tracked phase-2-audit-inputs entry), nine material findings (the cross-context structural-duplication cluster — cursor codec across seven contexts, pagination value objects, audit-event drafts, per-tenant wiring adapters; three over-800-line `apps/` composition files including the un-flagged `apps/cli/_cross_context.py` at 1704 lines; the message-sniffing tenant-rewrap repeated ten times), and twelve marginal findings, plus four cross-principle patterns. No code, test, enforcement-layer, or charter changes beyond captures.md and the session log per the assessment-shape discipline. Disposition of findings into hygiene items, methodology promotions, deferred-decisions entries, or charter additions belongs to the subsequent disposition conversation, which opens with the severity-classified findings list and the disposition forward-pointers from the session-log reflection prompt 6. S45 — the messaging substrate and conversational protocol per the p13-epic Session 3 line — is next.

**Pre-S45 Revisable harness in flight.** A hygiene session closing the load-bearing finding LSP1 from the pre-S45 principles assessment: the Revisable Protocol's contract-test harness, asserted by D114, D125, and `architecture.md` line 274 but never landed. The session adds `tests/contract/revisable/` — a parametrised conformance harness verifying signature shape, return type, and append-only / ordering / genesis semantics (what `@runtime_checkable` does not cover) over each registered implementer, with `contexts/portfolio` DataPoint registered as the first. No D-entry additions — the session closes existing charter commitments rather than making new ones. No methodology-document additions — principle-assessment methodology promotions defer to Phase 2-A close per the disposition. Charter touch-points at commit 1: this in-flight marker; `charter/phase-2-audit-inputs.md` (the Revisable harness entry resolved, plus `apps/cli/_cross_context.py` added to the close-hygiene list per assessment finding SRP3); `briefs/p13/pre-s45-revisable-harness.md` preserved.

**Pre-S45 Revisable harness closed; S45 next** at 2026-05-22. The Revisable Protocol contract-test harness landed across five commits: the charter touchpoint (95c2b2e); the harness scaffold — `tests/contract/revisable/__init__.py` and `conftest.py` with the implementer-registration mechanism (66f6a09); the five parametrised contract scenarios at `test_revisable_contract.py` (da3bf5d); `test_data_point_revisable.py` registering `contexts/portfolio` DataPoint as the first implementer (cb32e53); this session log entry plus this close marker. 6 new tests — the five conformance scenarios (signature, return type, append-only, ordering, genesis) over `portfolio.DataPoint` plus a `@runtime_checkable` sanity test; `tests/unit` and `tests/contract` green (pytest exit 0); the harness is test-only so the integration and e2e tiers are structurally unaffected and the two pre-existing environmental failures carried from S44a/S44b are unchanged. 29 of 29 import-linter contracts kept; AST enforcement unaffected. No new D-entries — the session closed existing charter commitments (D114, D125, `charter/architecture.md` line 274) by landing the artefact they assert. The load-bearing finding LSP1 from `audits/p13-principles-baseline.md` is resolved in code; the harness's registration mechanism is ready for P14's methodology-application revision implementer with no harness change. The remaining material findings from the principles assessment stay queued for Phase 2-A close hygiene per the disposition. S45 — the messaging substrate and conversational protocol per the p13-epic Session 3 line — is next.

**P13 in flight; S45 in flight.** S45 — the `contexts/messaging/` messaging substrate (WhatsApp via Twilio Sandbox), the ConversationFlow Protocol shape, the structured-output discipline, and provenance-aware response composition — opened on 2026-05-22. The updated brief at `briefs/p13/s45.md` supersedes the original 284-line draft, absorbing the eight pre-write-reconciliation findings: SMS corrected to WhatsApp throughout per D119's existing channel commitment; the D-number sequence corrected to D129/D130/D131 because D119 was already taken; the inbound orchestration placed at the intake context per D127 alternative (d); the import-linter contract count corrected to 32 (adds `layers-messaging`, `twilio-confined`, `twilio-confined-apps`); CLI messaging commands deferred; the DTO/query-parser path corrected to `apps/api/routers/`. Charter updates landed at commit 1: D129 (messaging substrate), D130 (structured-output discipline), D131 (provenance-aware response composition); the `charter/architecture.md` provenance-aware-response-composition prose; the p13-epic Session 3 line; `charter/schema.md` messaging-substrate and shared-kernel additions; two `charter/deferred-decisions.md` entries (structured-output-to-agent-tool convergence; multi-channel UX readiness); a `log/captures.md` forward-strategic-block entry for the UX convergence session. Thirteen commits per `briefs/p13/s45.md`.

**S45 closed; S46 next** at 2026-05-22. The messaging substrate, the ConversationFlow Protocol shape, the structured-output discipline, and provenance-aware response composition landed end-to-end across fourteen commits (the brief's thirteen plus a session-close test correction): D129/D130/D131 plus the charter touch-points (db5274d); the `contexts/messaging/` domain layer (b5e543f); the ports, Postgres adapter, and Alembic 0019 (b5f9721); the Twilio and LocalEcho delivery adapters plus the twilio-confined contracts (5c05874); the four use cases, four permissions, and MessagingSettings (07a3a19); Alembic 0020 extending intake_source with WHATSAPP_INBOUND (05f8259); the `record_intake_and_record_inbound_message` orchestration plus the MessageWriter consumer port (6cba23e); the shared_kernel ConversationFlow and structured_output primitives (7cf4295); the inference adapter's StructuredOutputPort extension (8161ce5); the two contract harnesses (ccbaabc); the messaging HTTP routes, webhook receiver, and composition wiring (a3c2292); the live-stack smoke (c068c87); the operator-permission-set test correction (5cb4df9); this session log entry plus this close marker. Roughly 104 new tests; `tests/unit` plus `tests/contract` green (1488 passed, 183 docker / empty-registry skips, pytest exit 0). 32 of 32 import-linter contracts kept (29 plus `layers-messaging`, `twilio-confined`, `twilio-confined-apps`); AST enforcement green. The `contexts/messaging/` bounded context is complete end-to-end — domain, two ports, three outbound adapters (Twilio, LocalEcho, Postgres), four use cases, HTTP transport including the Twilio WhatsApp webhook receiver with X-Twilio-Signature verification. D119's WhatsApp-via-Twilio channel commitment is the substrate D129 operates on; the inbound webhook is intake-canonical per D128 (second-instance evidence). ConversationFlow and structured-output landed as shared_kernel primitives with contract harnesses; the inference adapter is the first StructuredOutputPort implementer; no ConversationFlow implementer registers at S45. The central pre-write-reconciliation correction — SMS reframed to WhatsApp per D119, D-numbers reassigned because D119 was already taken — was absorbed into the updated brief; messaging became the first context since retrieval_evaluation to carry its own `pyproject.toml`, scoping the new `twilio` vendor dependency. The live-stack smoke is a procedural walkthrough: docker was not reachable from the build environment and the Twilio Sandbox stages need operator credentials. S46 — the P13 Wave 1 end-to-end exercise (manual entry cell, latency-tier routing, four-layer model ontology shape) per the p13-epic Session 4 line — is next.

**S45 smoke complete; S46 next** at 2026-05-22. The procedural smoke from the S45 close ran end-to-end as a live-stack operational continuation — all ten stages (0-9) green against tenant_a on the rebuilt `padhanam-api` image (`sha256:77ad47bf`), including a real Twilio WhatsApp Sandbox round trip in both directions. Outbound: LocalEcho (`external_id=local-echo-…`, the local-first path, no Twilio call) and Twilio (`external_id=SM81b3ed…`, delivered to the operator's WhatsApp). Inbound: a real Twilio webhook produced an IntakeRecord (`WHATSAPP_INBOUND`) then a Message (`INBOUND`, `intake_id`-linked) in a 97 ms cascade — D128's intake-canonical commitment operationally demonstrated at second instance. Invalid-signature rejected 403 with zero side effects; cross-tenant probes returned 404; the audit chain held (134 events, one genesis, zero broken links); pagination and direction/channel filters returned correct shapes. Five commits across the smoke arc: the Twilio env wiring (12f9716), the image re-pin (0f50c80), the loopback `127.0.0.1:8000:8000` webhook binding — a dev-only S5 exception (7af8e88), the `WEBHOOK_*` passthroughs (6079b9d), and the executed-evidence smoke document plus session-log entry plus this marker. The smoke document at `docs/smoke/p13_s45_messaging_substrate.md` now carries executed evidence for all ten stages. One methodology-recurrence note: the original smoke doc's stage 0 named `ngrok http 8000` without verifying port 8000 is host-bound (it is not, per S5) — brief-vs-codebase-actuality drift at the smoke-document altitude, deferred to Phase 2-A close methodology assessment. S46 — the P13 Wave 1 end-to-end exercise per the p13-epic Session 4 line — is next.

**P13 in flight; S46 in flight.** S46 — the manual entry cell (first ConversationFlow implementer), latency-tier routing (D122), and the four-layer model ontology (D132) — opened on 2026-05-22. Pre-write reconciliation across the nine brief surfaces resolved four structural findings before code, all operator-approved: (A) Path A — `latency_tier` is a *defaulted* `InferencePort`/`StructuredOutputPort` parameter (`REAL_TIME_REQUIRED` default), because the brief's Path B (required parameter) contradicts D122's committed "Phase 1 call sites preserve current behaviour with opt-in retrofit"; (B) the cell moves from the brief's `contexts/messaging/domain/` to `contexts/messaging/application/manual_entry_cell.py` — a cell holding ports is not pure-domain per the `layers-messaging` hexagonal contract (the S41-commit-4 drift class); (C) `ModelIdentifier` composes at the LiteLLM adapter boundary, not at the public call signature — call sites carry no Provider/Account knowledge and the Configuration layer overlaps existing request fields; the composition lands at a sibling `model_ontology.py` so the 784-line `adapter.py` stays under the 800 ceiling; (D) a single `PortfolioGateway` consumer port carries both the resolution reads and the three orchestration writes (Surface 8's one-port disposition; `resolve_target` needs portfolio read access the brief's write-only port framing omitted). Charter updates landed at commit 1: D132 (four-layer model ontology); D131's Expected-exercise line updated for the S46 first-instance exercise; `charter/architecture.md` four-layer-ontology subsection; `charter/schema.md` latency-tier / ModelIdentifier / intent-hierarchy / cell-registration additions; `charter/captures.md` D131 first-instance entry; the p13-epic Session 4 line. Twelve commits per `briefs/p13/s46.md`.

**S46 closed; P13 Wave 1 complete; UX convergence session next** at 2026-05-22. The manual entry cell, latency-tier routing (D122), and the four-layer model ontology (D132) landed end-to-end across twelve commits: the charter updates (14e7d0e); the intent value objects (9dd832f); the target identifier resolution helper (5b162d0); the four-layer ontology and LatencyTier at `shared_kernel/inference.py` (1f7033b); the InferenceSettings `latency_tier_config` extension (3f40c32); the tier-aware LiteLLM adapter plus the `model_ontology` sibling plus the Path-A call-site retrofit (f257e14); the `PortfolioGateway` consumer port plus the apps/-layer adapter (7351911); the manual entry cell plus `cell_response` (a49a34a); the ConversationFlow harness registration (64bf0d8); the composition-root wiring plus end-to-end integration tests (fad90d7); the procedural live-stack smoke (0977ca9); this session-log entry plus this close marker. Roughly 55 new tests; `tests/unit`, `tests/contract`, and `tests/integration/api` green; 32 of 32 import-linter contracts kept; AST enforcement green. Pre-write reconciliation resolved four structural findings before code — Path A (defaulted tier) honouring D122; the cell at the application layer not the domain layer; ModelIdentifier composed at the adapter boundary; a single `PortfolioGateway` consumer port. The manual entry cell is the first ConversationFlow implementer (D115); the harness's five conformance scenarios run against it. D131's first-instance exercise lands at the cell's cited response; D132's four-layer ontology composes at the LiteLLM adapter boundary with per-call audit capture. The live-stack smoke at `docs/smoke/p13_s46_manual_entry_end_to_end.md` is procedural — the build environment cannot reach docker or the Twilio Sandbox; the operator executes it live. **P13 Wave 1 closes at S46.** The next strategic-mode block is the UX convergence session (per the S45 `log/captures.md` entry — multi-channel UX, provenance-aware response composition, confidence-aware response composition), sitting between this close and P14 epic framing.

**S46 smoke complete; UX convergence session next** at 2026-05-22. The procedural smoke from the S46 close ran end-to-end as a live-stack operational continuation against tenant_a on the rebuilt `padhanam-api` image (`sha256:523e9419…`, pin advanced from S45's `sha256:77ad47bf…`). Two CreateCaseIntent cascades clean (stage 1 Case `fa6401b0` "Q3 portfolio review"; stage 8e-i Case `4fac3ae0` "Q3 budget review" against the now-similar-named existing case, no conflation). Seven AddDataPointIntent attempts across four distinct phrasings (the smoke document's template plus three structural rephrasings — verb-first-full-title, target-front-loaded, no-verb-noun-phrase — each sent twice) all classified to UnclearIntent; the cell behaved contract-correctly throughout (clarification, no write, no citation line); the failure is upstream at the model's classification accuracy. D122/D132 trace attributes verified at the stage-1 LLM-call observation (all four `gen_ai.model.*` dimensions plus `latency_tier=real_time_required` in the configuration layer). D131 citation contract held in all 9 outbound replies (2 cited per Shape 1; 7 clarifications carrying no citation line). Final tenant_a state: intakes 17 (+11), cases 6 (+2), data_points 3 (unchanged), messages 21 (+18), tenant_audit 165 (+31); audit chain holds (165 events, 1 genesis, 0 broken links, 0 duplicate hashes). Three commits across the smoke arc: the compose digest pin advance (`chore(compose)`); two captures entries at `log/captures.md` (`docs(charter)`) for the two structural-honesty findings — webhook synchronous-cell-run-vs-Twilio-timeout (with cold-vs-warm latency dynamic; cold 18-23s exceeds Twilio's ~15s timeout, warm 8-9s within; webhook completes server-side regardless; root finding on contract grounds independent of retry-duplication) and intent-extraction reliability at REAL_TIME_REQUIRED with `qwen2.5:7b` (intent-class blindness on AddDataPoint; three architectural responses for the convergence session — raise model tier, constrain classification surface, render model uncertainty more honestly; instrumentation-gap paragraph on prompt/output capture); and the executed-evidence smoke document at `docs/smoke/p13_s46_manual_entry_end_to_end.md` plus the session-log entry plus this marker (`docs(p13/s46-smoke)`). Both findings feed convergence-session inputs as first-class architectural observations rather than mid-stream optimisation. **P13 Wave 1 fully closed in repo state.** The next strategic-mode block is the UX convergence session, sitting between this smoke close and P14 epic framing.



**S42 closed** at 2026-05-15 with the HTTP transport surface for the retrieval_evaluation and optimization producer-context substrates in place end-to-end against tenant_a. Nine commits closed S42: charter touch-points landing D112 at `charter/decisions.md` tail (f080b76) with five coupled commitments (scope: retrieval_evaluation + optimization only since run_history per D98/S34 and audit per D103/S37 already shipped; flat-module router convention matching the five existing surfaces per Finding 5 deviation caught at session open; JWT-resolved tenancy per S15/S37; cursor pagination matching the reader-layer shape; OpenAPI as procurement-grade Phase 2 UX-consumer documentation deliverable) plus the S42 in-flight marker on current-package.md; Pydantic v2 DTOs at the flat convention with discriminated-union evidence_citations plus two query parsers plus extended `_errors.py` with two new error-handler groups (dc1d2e4) and 46 new unit tests; retrieval_evaluation routes at three FastAPI routers (gold_set, discovery, evaluation_run) covering the full surface including the two-step gold-set discovery decomposition and the synchronous evaluation-run kickoff per Finding 3 (56c83a7) and 18 integration tests; optimization routes at two FastAPI routers (optimization_run, recommendation) covering engine kickoff plus the read surface plus the three-route lifecycle (acknowledge/apply/reject with the 409 transition-not-permitted path carrying structured details) (c6e0572) and 15 integration tests; wiring at apps/api/main.py extending AppCompositions with ten new optional seams plus two new builders (build_retrieval_client, build_retrieval_runner_port) at `apps/api/_agent_runtime_wiring.py` plus the new HttpRetrievalRunnerPort class (d298c47); eleven tenant_isolation HTTP contract scenarios at tests/contract/tenant_isolation/test_retrieval_evaluation_http_isolation.py and test_optimization_http_isolation.py (75dd012) extending the harness from 137 to 148; eighteen pagination and error-response contract scenarios at the new tests/contract/http/ directory (70f64a1); live-stack smoke at docs/smoke/p11_s42_http_transport_surface.md (ac5a6bf) walking nine stages end-to-end against the production-shape padhanam-api container including the Makefile build-api target's in-session fix (docker build directly instead of docker compose build); this commit landing the session log entry plus this close marker. 1212 unit tests pass (+46 from S41's 1166); 148 tenant_isolation contract scenarios (+11); 18 new pagination+error contract scenarios; 33 new integration api tests; 27 import-linter contracts kept. **S42-close verdict: procurement-grade UX-consumer surface ready for Phase 2.** A Phase 2 frontend developer reads `/openapi.json` (17/17 S42 operationIds present) and integrates against the platform without needing to read source; the two-step gold-set discovery decomposition preserves human-in-the-loop content-fit discipline at the HTTP layer per S40b precedent; the recommendation lifecycle through HTTP lands equivalently to the S41 CLI exercise; tenant isolation holds with no information leakage. **The four-context P11 substrate scaffold's consumer surface is complete** (run_history at S34, audit at S37, retrieval_evaluation and optimization at S42). Methodology candidates forwarded to P12 audit: smoke-time verification of dev-workflow tooling at the same session that ships it (the post-S41 make build-api target's first production use surfaced the docker-compose-build-rejects-digest-tag bug; landed as captures entry; new candidate); audit chain rows from S40/S41 carry empty correlation_id which the S37 reader's validator rejects (pre-existing data shape inconsistency; landed as captures entry with recommended one-line validator loosening at pre-P12 hygiene); OpenAPI-as-procurement-grade-deliverable verdict (the spec mirrors FastAPI route definitions which themselves drive the runtime; no spec-vs-implementation divergence surface); flat-module router convention reinforcement (the Finding 5 brief-vs-codebase deviation reinforces principle-versus-framing drift methodology candidate's second-instance count in P11; aligns with the S41 commit-4 finding). **P11 substrate sessions complete; pre-P12 hygiene next** (carryover entries for audit-row correlation_id, README expansion, the stray `apps/api/{routers}/` empty directory; methodology candidate consolidation; phase-audit prep).

## P10 — final history (preserved below for traceability)

**P10 framed** at strategic-mode conversation on 2026-05-14.
D102 commits the audit log read substrate as extension of the
existing `contexts/audit/` bounded context: consumer-defined
`AuditEventReader` port on the read side, two-destination model
(per-tenant `tenant_audit` and control-plane `tenant_audit`)
through a destination parameter on port methods, chain
integrity verified on read at page granularity reusing
`compute_event_hash` and `GENESIS_HASH` primitives from
`contexts/audit/domain/events.py` with new page-granularity
verifier logic on top (the existing `verify_chain` walker is
from-genesis-only and is NOT reused), HTTP transport exposing
both destinations on separately authorized routes (`/audit`
under principal-derived tenant context, `/platform/audit`
under a new platform-operator claim extending D23), HTTP API
for ingestion management absorbed at P10 from the P6
carryover. P10 epic at
[charter/packages/p10-epic.md](packages/p10-epic.md). Three-
to-four-session forecast (S36 read port + adapter + chain
integrity; S37 HTTP transport + platform-operator claim; S38
ingestion management HTTP API + P10 close demo; S39 carryover
hygiene if needed).

**Corrective note on misrouted P10 framing inputs.** The S35
close paragraph below carries the line "P10 is the evaluation
territory per the roadmap" and names two P10 framing inputs
(retrieval-not-exercised model-behaviour gap; trace-id
propagation gap). The P10 strategic-mode framing conversation
on 2026-05-14 surfaced that this characterisation contradicts
the canonical roadmap and D92, which scope P10 as the audit
log viewer (backend-only). The two named framing inputs are
correctly routed to the queued retrieval-evaluation design
session pre-P11, not to P10. The corrective is captured
append-only per the existing discipline; the original S35
text stands as written below and this note attaches at P10
open.

**S36 closed** at 2026-05-14 with the audit context's read
side now in place end-to-end. Eight commits closed S36:
charter touch-points landing D102 (audit log read substrate
framing); four domain value-object modules at
`contexts/audit/domain/` (`audit_event_record.py`,
`query_filters.py`, `chain_integrity.py`, `destination.py`);
`AuditEventReader` Protocol port at the new
`contexts/audit/ports/reader.py` (operator-selected option A
at the pre-write reconciliation finding 1 user-question:
keep write-side `AuditPort` at `domain/ports.py`, place new
reader at `ports/reader.py`; intra-context asymmetry
recorded as carryover); cursor codec at
`contexts/audit/application/cursor.py` mirroring run_history;
`.importlinter` layers-audit contract extended with
`contexts.audit.ports`; `PostgresAuditEventReader` adapter at
`contexts/audit/adapters/outbound/postgres/reader.py`
implementing the port against both destinations through
destination-parameter routing per D102 alternative (b);
chain integrity verifier reuses `compute_event_hash` and
`GENESIS_HASH` primitives (the existing `verify_chain`
walker is from-genesis-only and is NOT reused per D102
alternative (h)); wiring at both composition roots
(`AuditEventReaderAdapter` as the tenth consumer-port-plus-
wiring-adapter class on `apps/cli/_cross_context.py`;
`build_audit_event_reader` factory at
`apps/api/_agent_runtime_wiring.py`); seven tenant-isolation
contract scenarios at
`tests/contract/tenant_isolation/test_audit_reader_isolation.py`;
live-stack smoke at `docs/smoke/p10_s36_audit_reader.md`
walks five scenarios + two routing-guard pairs end-to-end
against tenant_a's chain (20 audit rows) and a seeded
control-plane probe event. Net new tests at S36 close: 92
audit unit tests (was 50 pre-S36; +42 new), 7 contract
harness scenarios, 6 wiring tests = 55 net new test cases.
25/25 import-linter contracts kept; AST enforcement passes.
Two new findings landed in `briefs/p10/s36.md` Appendix D
at write time: finding 8 (Pydantic v2 versus frozen
dataclass — frozen dataclass picked for sibling-symmetry)
and finding 9 (single `timestamp_range` versus the brief's
`started_at_range`/`ended_at_range` shape — the audit
schema has one timestamp column).

**S37 closed** at 2026-05-14 with the audit HTTP transport
and the platform-operator principal type now in place
end-to-end. Eight commits closed S37: D103 + charter
touch-points (commit 1) with alternative (b) revised at
pre-write reconciliation finding 1 user-question resolution
to acknowledge the existing `Principal.roles` + `OPERATOR_ROLE`
as production code rather than hypothetical Phase 2 future,
framing `principal_type` as structurally complementary
(discriminator anchors `tenant_id` conditional validity at
decode time, which role-list cannot do cleanly because
`verify_credential` requires `tenant_id` unconditionally
today); D23 backend extension at `padhanam/security/auth.py`
plus dependency surface at `apps/api/middleware.py` and
`apps/api/routers/inference.py` (commit 2); Pydantic DTOs +
query parser at `apps/api/routers/_audit_dto.py` and
`_audit_query.py` mirroring run-history precedent per finding
2 (commit 3); audit error response extension at
`apps/api/_errors.py` with five new error-code paths and the
parallel `register_audit_error_handlers` per finding 4 plus
the refactor of commit-2 dependencies to raise typed
`PrincipalTypeMismatchError` (commit 4); HTTP routes at
`apps/api/routers/audit.py` with two FastAPI routers under
`/audit/*` and `/platform/audit/*` plus composition wiring at
`apps/api/main.py` including `PostgresAuditAdapter`'s new
public `control_plane_sessionmaker` property (commit 5); six
tenant-isolation contract scenarios at
`tests/contract/tenant_isolation/test_audit_http_isolation.py`
(commit 6) extending the harness from 27 to 33 scenarios;
live-stack smoke at `docs/smoke/p10_s37_audit_http.md` plus
script at `scripts/smoke_p10_s37.py` walking all ten
verification paths end-to-end against tenant_a's 23-row chain
and a seeded control-plane probe event, with four security
events captured in `logs/security.jsonl` (commit 7); this
entry (commit 8). 1251 passed + 17 skipped + 31 deselected
(live_llm) at S37 close — net +51 from S36's 1200. 25/25
import-linter contracts kept; AST enforcement passes.

**S38 next**: HTTP API for ingestion management at
`apps/api/routers/ingestion.py` plus the P10 close
end-to-end demonstration absorbing both the audit HTTP
transport and the ingestion management surface. P6
carryover scope (list sources, get source by id, get source
ingestion status) lands here per D102. S38 anchors directly
from the P10 epic note at `charter/packages/p10-epic.md`
unless structural drift surfaces.

## P9 closed (preserved below)

**S35b closed** at 2026-05-14 with harness integrity restored across all six pre-existing carryover failures. Five commits closed S35b: commit 1 transitioned `charter/current-package.md` and preserved the brief; commit 2 fixed TRUNCATE-without-CASCADE on chunks-to-`run_chunk_citations` (two fixture sites — `test_concurrent_workers.py`, `test_create_from_methodology_flow.py` — extended their TRUNCATE table-list to include `run_chunk_citations` per D95's FK chain; production ORM-deletion semantics with `ON DELETE SET NULL` are unaffected because TRUNCATE bypasses FK-action triggers); commit 3 migrated `test_ingestion_isolation.py`'s S22/D65 retrieval-surface block from the failing padhanam-api psql shell-out to the in-tenant-container psql pattern already used at `test_concurrent_workers.py` and `test_create_from_methodology_flow.py`, with each tenant seeded a distinct marker chunk (placeholder unit-vector embedding, sources.state='indexed') so the test asserts own-marker-present AND other-marker-absent — pass-because-isolated; commit 4 folded in the streaming-isolation test-scaffolding fix (added `_NullRunHistoryWriter` to `_build_app` to satisfy S31's `run_history_writer` field on `AgentRuntimeComposition`); commit 5 (this commit) appends the session log entry and transitions `current-package.md` back to "P9 closed; P10 framing next." Pre-write reconciliation fired its fourteenth instance at session-open: one structural-drift finding (surface 2: methodology fixture pattern doesn't transfer to per-tenant DBs because per-D5 they have no host-port bindings; operator-resolved via user-question to in-tenant-container psql) plus four mechanical absorptions, plus a mid-commit-3 structural finding (deliberate-violation cycle exposed that D32's per-DB topology is the *primary* isolation mechanism, with the SQL predicate as defense-in-depth — meaningful methodology signal for the Phase 1 close audit). 1106 passed + 17 skipped + 31 deselected at S35b close (+6 from S35a's 1100). Zero failures, no regressions. 25/25 import-linter contracts kept; AST enforcement passes. No new D-entries at S35b; D24 already commits the principle the fixes restore. The S35a + S35b carryover-hygiene tail folds into the P9 archive at P10 strategic-mode framing per operator direction.

**S35a closed** at 2026-05-14 with the four P9 carryover hygiene workstreams landed. Six commits: D99 (two-tier integration test strategy: file-level `pytestmark = pytest.mark.live_llm` plus `addopts = "-q -m 'not live_llm'"` plus `make test-live-llm` Makefile target; 10 integration test files / 31 tests at the live tier); D100 (CLI settings flow through composition root via new `apps/cli/_composition.py` mirroring `apps/api`'s `AppCompositions` pattern; five `ControlPlaneSettings()` construction sites refactored; all 17 methodology + tool CLI integration tests now pass); D101 (tenant registry actor provenance via control-plane Alembic `0011_tenant_actor_provenance` adding `created_by_user_id text NOT NULL` with server-default backfill, adapter signature change, seed-script subject migration to `migration:ops/seed_tenants`, four-site wipe-fixture guard); D27 join-key first end-to-end exercise (trace_id propagation from active OTel span to runs row; new `_current_otel_trace_id_hex()` helper; smoke at `docs/smoke/p9_s35a_trace_id_propagation.md` verified row 486af46b carries `trace_id=b7e677a03e28afd51c5f691055545022`). One structural reconciliation finding at session-open (surface 9: tenant_registry's missing column; operator chose option (a) add via migration); one reconciliation-at-execution-time correction (alembic_version varchar(32) limit forced revision-id rename from 40 chars to 28 chars). 1100 passed + 17 skipped + 31 deselected (live_llm) at S35a close. Six pre-existing carryover failures unchanged (four already-named; two newly-named TRUNCATE-without-CASCADE failures from chunks ↔ run_chunk_citations FK surfaced now that the demo runs populated chunks). P9 retrospective and archive stand from S35; P10 framing is the next strategic-mode conversation.

### Canonical P9 close framing (from S35)


P8 closed on 2026-05-13. Archive at [docs/archive/packages/p8.md](../docs/archive/packages/p8.md); measured outcomes appended to [log/packages.md](../log/packages.md). Seven sessions (S26a-1, S26a-2, S26b, S27b, S28b, S29b, S30b) shipped the agent runtime substrate per D86 (role-first model), D87 (override-mode space), D88 (agent runtime architecture), D89 (tool registry), and D90 (streaming runtime), plus D91 from the parallel brand-transplant strategic block. P8's contribution is the agent runtime substrate that makes the platform demonstrable in product form: roles as first-class primary aggregates composing into methodologies via `role_refs`; a streaming runtime exposing eleven domain-layer events through transport-neutral ports; an SSE transport at `apps/api/routers/agent.py` consumed end-to-end by the `padhanam agent run` CLI at S30b; production wiring of the runtime composition at `apps/api/_agent_runtime_wiring.py` including a per-tenant retrieval router. Two end-to-end demonstrations close P8 in product form: Flowstate-McKinsey ProblemFramer on tenant alpha producing a SMART problem statement (narrow artifact, 76s); Forgepath-LVT LVTGuide on tenant beta producing a full Lean Value Tree (broad artifact, 271s). Same substrate, two artifact scales — the bet's intelligence-layer commitment per D82 exercised in product form. Pattern reinforcements that solidified Phase 1 architectural norms during P8: the consumer-port-plus-wiring-adapter pattern reached four reinforcements across five sessions in a row with three-altitude generality (cross-context, intra-context wiring, transport) and a fourth observation (same-altitude cross-composition-root re-use at S30b); pre-write reconciliation as architectural discovery reached six-plus reinforcements with the new sub-observation that pre-session operator setup is itself a pre-write reconciliation moment. Both promotion candidates for the Phase 1 close audit window.

**Phase 1 / Phase 2 boundary committed.** D92 confirms Phase 1 scope at P1 through P12 with P9-P12 reframed as backend-only substrate. D93 commits Phase 2 direction to methodology-as-product positioning with focus purely on UX/UI. The Phase 1 close audit lands at P12 close per `charter/methodology.md`'s audit posture; mid-phase audit work that would have captured methodology promotions and PRFAQ revoice defers to that audit. The new carryover entries below name the substrate-readiness items Phase 1 absorbs so Phase 2 UX work runs cleanly.

**P9 framed.** D94 commits the run-history substrate as Shape C: per-tenant Postgres `runs` table plus `run_chunk_citations` and `run_entity_citations` with rendering-grade snapshot columns alongside the technical references, single-transaction write at invocation completion, new bounded context at `contexts/run_history/` separate from `contexts/agent/`, single consumer-defined query port shaped to Phase 2 UX consumption at P9 with P11's aggregation-shaped port deferred to P11 framing. P9 epic at [charter/packages/p9-epic.md](packages/p9-epic.md). Four-to-five session forecast (S31 substrate-and-write-path, S32 citation-linking-and-completion-seam, S33 UX-query-port, S34 HTTP-ingestion-API-carryover, S35 P9 close if needed); session boundaries settle session-by-session per the established discipline. **S31 closed** at 2026-05-13 with the bounded-context substrate now in place: `contexts/run_history/` with the hexagonal-layer convention, three per-tenant tables (`runs`, `run_chunk_citations`, `run_entity_citations`) via Alembic revision `0011_create_run_history`, `RunHistoryWriter` consumer port at the agent context, `record_run` use case and `PostgresRunHistoryAdapter`, `RunHistoryWriterAdapter` wiring at both `apps/cli/` and `apps/api/` composition roots, `invoke_agent` extended with the run-record accumulator and post-terminal-yield write seam (D95 shape B), tenant-isolation contract harness extended with nine new scenarios. Live-stack smoke verified one runs row on `tenant_a` with audit hashes byte-for-byte matching the audit chain. Citation tables exist but no citation rows get written; citation linking with snapshot population and the single-transaction completion seam lands at **S32 next**. The two p9-epic open questions named at S31 charter update (citation snapshot semantics — chunk excerpt verbatim versus summary; whether `run_entity_citations` snapshots `source_chunk_ids` alongside the display label) carry forward to S32 framing. Reconciliation-time observations queued for the operator at session close: an automated rebuild-and-recreate target (the brief's missing `make build-api`) would absorb the manual `docker build` + `compose.yaml` digest pin + `force-recreate` cycle; the `.importlinter` location at the repo root (not a `[tool.importlinter]` block in `pyproject.toml`) is a brief-shape correction worth noting for future strategic-mode authoring.

**S32 closed** at 2026-05-13 with the citation surface now in place end-to-end from retrieval through to per-tenant Postgres rows. Eleven commits closed S32: charter updates landing D96 (`charter/decisions.md`, `charter/schema.md`, `charter/packages/p9-epic.md`, `charter/current-package.md`, brief preserved at `briefs/p9/s32.md` with Appendix D capturing pre-write reconciliation outcomes); citation candidate value objects at `contexts/agent/domain/citation_candidates.py` with discriminated union; `ToolCallCompleted` extension carrying `citation_candidates: tuple[CitationCandidate, ...] = ()`; `AgentRetrievalClient` port growing a `RetrievalResult` envelope so the wiring adapter produces both LLM-facing `RetrievedChunk` projections and citation candidates single-pass; ingestion-side `ChunkResult` extended with `chunk_index` and `source_snapshot` plus the pgvector SQL projecting the additional columns; `AgentLoopExecutor` populates `citation_candidates` on `ToolCallCompleted` events; `invoke_agent` accumulates with within-run deduplication first-seen-wins per `(chunk_id, run_id)` and `(entity_tenant_id, entity_name, entity_type, run_id)`; Alembic 0012_revise_citation_snapshots drops `source_citation text` for `source_snapshot jsonb` and drops `entity_display_label text` for `source_chunk_ids text[]` (applied directly to both tenants since the tenant-registry-driven `make migrate` path was non-operational due to a wiped registry); run-history-context `ChunkCitationRecord` and `EntityCitationRecord` mirror the agent candidates one-for-one per D54; `PostgresRunHistoryAdapter` writes all three tables within `async with session.begin()` with partial-failure rollback; tenant-isolation contract harness extended to 13 scenarios. Live-stack smoke verified one runs row + two chunk citation rows + one entity citation row on `tenant_a` with `source_snapshot` JSONB populated (Phase 1 keys `file_name`, `file_type`) and `source_chunk_ids` text[] carrying provenance. Pre-existing failures carried over from S30b/S31: methodology/tool CLI command tests (config shape drift); ingestion e2e tests (the same tenant-registry-wipe class). The HTTP API for ingestion management absorbed from the P6 deferred carryover is **S34 next** (or earlier if folded).

**S33 closed** at 2026-05-14 with the UX-shaped read surface now in place end-to-end. Seven commits closed S33: D97 at `charter/decisions.md` (RunHistoryReader port at run-history producer context; RunRecord-as-aggregate read DTO; four-filter RunListFilters vocabulary; cursor pagination on `(started_at, id)` with base64-of-JSON opaque cursor) plus charter touch-points; `RunHistoryReader` Protocol port at `contexts/run_history/ports/reader.py` with `get_run` returning `RunRecord | None` and `list_runs_with_filters` returning `RunListPage`; `RunListFilters` / `RunListCursor` / `MalformedCursorError` value objects at `contexts/run_history/domain/query_filters.py`; cursor codec at `contexts/run_history/application/cursor.py`; `PostgresRunHistoryReader` at `contexts/run_history/adapters/outbound/postgres/reader.py` implementing the port via three queries (avoids LEFT-JOIN cartesian product); `RunHistoryReaderAdapter` wiring at `apps/cli/_cross_context.py` (ninth wiring class on that file) and `build_run_history_reader` factory at `apps/api/_agent_runtime_wiring.py`; tenant-isolation contract harness extended to 16 scenarios; live-stack smoke against tenant_a exercises all four read scenarios end-to-end (get_run with citations populated, list with no filters, termination_reasons filter, cursor pagination across two pages). 831 unit tests pass at session close (+57 from S32's 774); 25/25 import-linter contracts kept. One real-Postgres finding surfaced at smoke time (tuple comparison `uuid < varchar` operator-resolution failure) and was fixed in the smoke commit with `sa.cast(..., pg.UUID)`; the class of finding is structurally invisible to the unit tests' fake session and worth recording for the live-stack-as-required-complement methodology promotion candidate. The read-DTO-symmetry call (RunRecord-as-aggregate rather than the brief's drafted `RunWithCitations` wrapper) was the session-open structural-honesty finding the user-question resolved.

**S34 next**: build evidence at S33 points more strongly toward S34 pivoting to HTTP routes surfacing the run-history reader than staying on the ingestion-management track. Three reasons: the reader substrate is now complete and one more session closes the end-to-end Phase 2 UX consumer story; the cursor codec is the substrate the HTTP layer's request/response boundary needs; the four-filter vocabulary is the natural input to a query-string parser. The ingestion-management API carryover from P6 can land at S35 or P9 close without prejudice; the strategic-mode conversation at S34 framing settles the call against operator priority.

**S34 in flight.** D98 commits the four HTTP shapes: response DTO as 1:1 mirror of `RunRecord` per the storage-versus-render discipline from D96; query parameter vocabulary mapping the four `RunListFilters` fields plus cursor and page_size; error response body shape with the eleven-path map and the cursor-and-filter-mismatch policy honouring both independently; principal-derived tenant context per S29b precedent at `apps/api/routers/agent.py`. S34 closes the end-to-end Phase 2 UX consumer story for the entire P9 substrate: the reader port from S33 is now callable from any web frontend, the cursor codec is exercised at its intended HTTP boundary, the four-filter vocabulary surfaces as a usable query-string parser. The strategic-mode S34 framing conversation settled the pivot from the ingestion-management API carryover to the run-history HTTP routes on Kano-must-have-for-Phase-2-UX grounds; ingestion-management lands at S35 or P9 close without prejudice.

**S34 closed** at 2026-05-14 with the run-history HTTP read surface now in place end-to-end. Eight commits closed S34: D98 plus charter touch-points; four Pydantic response DTOs mirroring the domain records 1:1; query-string parser mapping six query params to `RunListFilters` and `RunListCursor` via a sentinel-cursor mechanism on the initial page; two FastAPI routes (`GET /runs/{run_id}` returning the run-with-citations aggregate; `GET /runs` returning the paginated, filtered list); error response body shape at `apps/api/_errors.py` with eleven verification paths covered (`malformed_cursor`, `invalid_filter_range`, `run_not_found`, `validation_error`, `internal_error`) plus `CorrelationIdMiddleware`; tenant-isolation contract harness extended to 20 scenarios with four HTTP-layer scenarios; live-stack smoke against tenant_a verified all eleven verification paths plus two happy paths end-to-end including the `X-Correlation-Id` header round-trip. One pre-write reconciliation finding required a session-open user-question: brief scenario 20 ("tenant-not-in-registry returns 401") contradicted the existing `get_tenant_context` dependency's 404 behaviour; resolved to 404 per consistency-with-precedent, recorded in D98 alternative (k) and Appendix D of the preserved brief.

**S35 closed** at 2026-05-14 with P9 closed via end-to-end demonstration against tenant_a. Five commits closed S35: charter updates ahead of code carrying two new deferred-decisions entries (integration-test slowness from real-LLM inference; HTTP API for ingestion management absorbed from P6 and not landed within P9) plus the investigation note `docs/notes/integration-test-slowness-investigation.md`; carryover fix for `test_agent_sse_endpoint.py` `_runtime_with_script` helper missing the `run_history_writer` field at `AgentRuntimeComposition`; end-to-end demonstration trigger against tenant_a producing run id `5226925f-bd76-47c2-8c9b-fdb4b370e3ab` with audit-chain hashes linked byte-for-byte between the runs row and `tenant_audit`, recorded at `docs/smoke/p9_s35_e2e_demo.md`; P9 retrospective addendum to `charter/packages/p9-epic.md` and P9 entry at `log/packages.md` per the P8 precedent (5/5 clean-close, 0/5 CFR, +128 net new tests pushing total to 883), plus archive of the epic to `docs/archive/packages/p9.md`; session-log entry and this final transition. Five pre-write reconciliation surfaces absorbed mechanically at session-open; one mid-session structural finding surfaced at demo trigger time (methodology-lineage-pointing-to-wiped-row sub-class of the S30b fixture-wipe; resolved by authoring a fresh agent via `create-from-role`). Two Phase 1 substrate-completeness limitations recorded for the Phase 1 close audit at P12: retrieval-not-exercised model-behaviour gap (already a P8→P9 carryover); trace-id propagation gap from agent runtime to runs row (the `trace_id` column is NULL despite Langfuse-web healthy; structural finding; substrate complete at column + read-port level, capture path needs revisit). No new D-entries at S35; all P9 substrate decisions landed at D94 through D98.

P9 delivered the run-history backend substrate as the Phase 2 UX consumer surface plus the end-to-end demonstration as the bet-proof artefact for Phase 1 substrate-completeness per D92. Every consumer surface Phase 2 UX needs for run-history is in place and verifiable end-to-end through a single trigger command: write path (S31), citation surface (S32), consumer-defined read port (S33), HTTP transport (S34), demonstrated execution (S35). P10 is the evaluation territory per the roadmap; the strategic-mode P10 framing conversation follows P9 close. Two P10 framing inputs surfaced from P9 execution worth weighing at the strategic conversation: the retrieval-not-exercised gap is more impactful at P10 than P9 because the evaluation harness needs reliable tool invocation as its foundation; the trace-id propagation gap impacts P10's evaluation harness more than P9's run history because cross-store correlation with Langfuse is part of how the harness will deep-dive into per-run cost and latency. Both are inputs for the P10 conversation; the strategic conversation owns P10 scope decisions.

## Carryovers active across the P8→P9 boundary

- **Audit context port-location asymmetry.** Write-side
  `AuditPort` sits at `contexts/audit/domain/ports.py` (pre-
  P9 convention). Read-side `AuditEventReader` sits at
  `contexts/audit/ports/reader.py` (P9-era convention
  established at run_history). Activation trigger: the next
  port added to the audit context (likely at P10 S37 HTTP
  work or P11 recommendation-engine consumers). At that
  point, decide whether to symmetrize by moving `AuditPort`
  to `contexts/audit/ports/writer.py` versus accepting the
  intra-context asymmetry as the audit-context convention.
  Recorded at S36 pre-write reconciliation; deferred on
  scope-discipline grounds because Option B (move now) has
  unbounded touch surface and adds incidental scope at
  S36 build time.
- **Per-invocation retrieval-constraint threading at ToolInvoker.** The Phase 1 `ToolInvokerAdapter` constructor accepts retrieval constants at composition time; per-role retrieval constraints from the effective bundle do not thread through to the tool invoker on each invocation. Phase 2 substrate refinement queued at the `apps/api/_agent_runtime_wiring.py` module docstring.
- **Cross-app adapter location cleanup.** S30b's production wiring imports adapter classes from `apps/cli/_cross_context.py` because both `apps/cli/` and `apps/api/` need them. Phase 2 cleanup relocates to a shared `apps/`-level module; Phase 1 cross-app import is the pragmatic call documented in the wiring module's docstring.
- **`psql` missing in padhanam-api image.** Two tests at `tests/contract/tenant_isolation/test_ingestion_isolation.py` shell out to `psql` to truncate chunks + sources; the image does not include `psql`. Tests pass only when tenant DBs happen to be empty; S30b's demo runs surfaced the latent issue. Pre-existing failure; P9 candidate.
- **TRUNCATE-without-CASCADE on chunks blocked by `run_chunk_citations` FK.** Two tests (`tests/integration/contexts/ingestion/test_concurrent_workers.py::test_two_concurrent_workers_no_double_processing_full_pipeline`, `tests/e2e/agent/test_create_from_methodology_flow.py::test_full_role_aware_clone_and_edit_flow_against_live_stack`) shell out to `psql -c "TRUNCATE TABLE chunks, sources;"`. The TRUNCATE fails with `cannot truncate a table referenced in a foreign key constraint` because S32 introduced `run_chunk_citations.chunk_id REFERENCES chunks(id)`. Latent at S35 close when tenant_a's chunks were empty; load-bearing at S35a close because the trace_id-propagation live demo populated chunks via the retrieval tool call path. Fix shape: TRUNCATE CASCADE or include `run_chunk_citations` in the truncate set; not on S35a's path.
- **Tenant registry fixture leak.** During S30b's demo work, the tenant registry got wiped between the recovery seed and the demo runs by some contract-test fixture path not yet identified. Same shape as the methodology fixture leak S30b fixed; same fix shape (`created_by_user_id NOT LIKE 'migration:%'` filter or equivalent guard). Activation trigger is the next pre-session smoke run that surfaces an empty registry.
- **Hierarchical multi-agent topology design.** Closed at strategic-mode commit 6f66f71 (D80 through D85). Role-first refinement (D86) closed at this commit.
- **Layer A policy authoring.** Follow-on strategic block authoring
  the ten policy scaffolds at `charter/compliance/` per the
  compliance-as-shared-responsibility principle. Scheduled at
  operator discretion between P7 build sessions or after P7 close;
  does not block any P7 build session because the substrate (D-entries
  D69-D73, the principle, the scaffold structure) is in place. Authoring
  effort estimated at one strategic block session.
- **Product methodology selection-space.** P7 commits to LVT as
  the first methodology per D68; the LVT methodology template
  lands at S25. Other methodologies in
  [charter/product-methodology.md](product-methodology.md)
  activate as evidence pulls them in (operator authors as needed);
  per-domain methodology selection surfaces at the framing of each
  domain-bearing package.
- **Production CLI tenant resolution via the registry.** Phase 2
  shape; `apps/cli/_runtime.py`'s hardcoded test-set mapping is
  honest about its dev-only scope. Activates when production
  deployment context arrives.
- **Multi-baseline regression reports.** Deferred per D58;
  single-baseline at S18. Activates at P11's recommendation
  engine when run-history infrastructure exists from P9.
- **PRFAQ phase-audit refresh.** Cadence per D45 (every phase audit). The v2 PRFAQ from the P4-post carryover-cleanup strategic session stands until the Phase 1 close audit at P12 close. The Phase 1 close audit refresh absorbs the dogfooding scenario acknowledgment per D77 and D78 (operator runs a private deployment for personal use as evidence of D14's customer-deployment scenario), and supersedes D51's voice-and-audience choice per D93 (PRFAQ v3 voice realigns to methodology-as-product audience: senior product leaders, CPOs, consultancies per `bet.md` line 57).
- **Personal-use deployment of public Padhanam (Phase C).**
  Operator-controlled deployment of public Padhanam as a real
  instance of the customer-deployment scenario per D78, exercising
  D14's configuration + tools + bounded-extensions model. Phase C
  activates concretely after P8 close (when agent runtime exists);
  preparatory work (operator-authored tool services and methodology
  template authoring) can start after P7 close in parallel with P8
  build, subject to operator capacity per the all-or-nothing
  posture. PRFAQ acknowledgment lands at the next phase audit.
- **Calendar tool service as platform capability.** Deferred-
  decisions entry per the P7 mid-package strategic block on
  consumer-direction placement; activation when public Padhanam
  needs a calendar integration for any package work or when the
  personal-use deployment Phase C activates per D78, whichever
  comes first.
- **Email tool service as platform capability.** Deferred-decisions
  entry, same activation shape as the calendar tool entry.
- **Scheduled-runs primitive.** Deferred-decisions entry; activates
  when public Padhanam needs scheduled agent execution (likely P11
  or P12 territory) or when personal-use deployment Phase C needs
  daily-review-style triggers, whichever comes first. Two
  implementation candidates (platform primitive versus external
  trigger); choice settles when implementation begins.
- **HTTP API for ingestion management.** Was deferred per the P6 out-of-scope to "when a UI consumer arrives at P9 or P10." Under D93 the UI consumer is Phase 2 consumer UX. The API surface lands at P9 or earlier as Phase 1 substrate completion so Phase 2 UX consumes it directly.
- **HTTP API for evaluation management.** Same shape; was deferred until a UI consumer arrives at P10 or P11; under D93 lands as Phase 1 substrate completion at P10 or P11.
- **Browser-based authentication.** Cookies, CSRF, session management. D23's signed-token backend is operator-only; consumer UX needs proper session handling. Lands as Phase 1 close substrate-completion session.
- **Frontend stack decision.** React 19 / Vite 6 / React Router 7 are pinned at D10 as pre-S1 baseline; Phase 2 open confirms or revisits, plus chooses a UI library or commits to an in-house design system grounded in `charter/brand-guidelines.md` and `charter/brand/tokens.css`. Could land at Phase 1 close or at Phase 2 open; cheap either way.

## Deferred items remaining visible

- **Per-tenant Neo4j topology.** Activated at S21 per D63 with
  Phase 1 shared-instance + property-based scoping; the
  deferred-decisions entry remains as the production-deployment
  revisit marker with three named triggers (residency, blast
  radius, security-review).
- **Within-tenant segmentation primitive.** Held in the P6-open
  strategic-block conversation; activates at the consumer-driven
  session that demands it (likely P8 agent runtime). No schema
  commitment at P6 beyond tenant.
- **Classification field on TenantContext.** Deferred per S15
  framing decision option C; lands at the package that genuinely
  consumes it (P7 or P8 per the P4 epic note's out-of-scope
  section). TenantContext at P6 close still carries three fields,
  not four; adding the field later is a one-line edit on the
  value object plus a registry-row column.
- **Cost-ceiling forward-affordance columns.** Configuration
  columns landed at S14 alongside the cost-attribution column per
  D41. Reading and enforcing the columns defers to Phase 2 per
  [charter/deferred-decisions.md](deferred-decisions.md).
- **Pricing-table monthly review.** Cadence in
  `ops/scheduled_checks.yaml` per D41; first run scheduled
  2026-06-05.
- **Pricing-table format evolution.** S14 reflection forward-note;
  the format-(b) Pydantic + dict shape evolves to YAML/TOML under
  `ops/` when multi-region rates, time-zoned rates, or rate-card
  complexity arrives. Phase 2 framing.
- **PRFAQ operator-voice rewrite.** Follow-on strategic
  conversation, queued at operator discretion.
- **Phase 1 PRD operator-review** of the problem-statement and
  target-user sections. Operator discretion.
- **Production-shaped tenant onboarding workflow** (full D13
  implementation): awaits production deployment context.
- **Cross-replica cache invalidation for the routing layer**
  (D36): single-replica dev makes this a non-issue.
- **Hash chain caching as a performance optimisation** (D37):
  deferred until measurement justifies.
- **Methodology mechanical-enforcement upgrades.** Tracked in
  [charter/deferred-decisions.md](deferred-decisions.md). The
  framing-prompt-as-recommendation and pre-write reconciliation
  promotions at this commit move two items off the
  Patterns-observed candidate list onto the prescriptive
  principle surface; the user-driven course-correction Patterns-
  observed entry lands at the same commit.
- **Platform-baseline scoring sheet library** (deferred per D53;
  activates at real onboarding flow or a cross-tenant curated
  library with a real consumer).
- **Human-review UI for evaluation** (deferred per D53; lands at
  P10 or P11 territory).
- **Multi-currency cost reporting** (deferred per the strategic
  commit `24561c9` deferred-decisions entry; activates at the
  Phase 2 multi-region deployment context).
- **Per-criterion cost breakdowns in
  `CostPerSuccessfulTaskResult`** (P11 territory).
- **Calibration learning loops over `automated_score` vs
  `human_score`** (P11 territory; data substrate lives at
  rubric_applications per D55).
- **Trace_id-based recommendation queries beyond
  cost-per-successful-task** (P11 territory).
- **HTTP API for ingestion management** (deferred per the P6
  out-of-scope; CLI is the user surface at P6; HTTP API ships
  when a UI consumer arrives at P9 or P10).
- **HTTP API for evaluation management** (deferred; activates
  when a UI consumer arrives at P10 or P11).
- **Sheet/interaction-set management commands in the CLI**
  (deferred; activates when CRUD UI is needed).
- **Personalization as a runtime concern.** Deferred-decisions
  entry from P6 mid-package absorption (Ask David external
  reference); activates at P8 agent runtime or whichever
  predecessor orchestration session demands it.
