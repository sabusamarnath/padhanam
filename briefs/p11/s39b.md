# S39b: Corpus re-ingest plus real-corpus discovery-mode authoring

## Goal at session close

The following artefacts exist at session close:

- Tenant_a's `chunks` table is non-empty; the S25 source corpus is re-ingested against the current DB instance via the existing ingestion path at `contexts/ingestion/`, with any drift in the ingestion CLI fixed in the same commit where it surfaces
- LVTGuide role is present on the control plane with `tool_allowlist` carrying the retrieval tool reference `[{tool_id: 00...01, revision_id: 00...02}]`; the S30b fixture-leak carryover that S39's smoke surfaced is closed at the control-plane layer
- Existing S39 gold-set "P11 retrieval baseline" is renamed to disambiguate it as the synthetic-chunk-id reference (per Finding 3 disposition); the hash-chain integrity evidence from S39 is preserved intact
- New gold-set "P11 retrieval baseline (real corpus)" authored against tenant_a via discovery-mode CLI end-to-end: three entries authored through `gold-set append-entry --query ... --top-k ...` against re-ingested corpus, finalized, hash-chain verified
- AC 10 from S39 closed at the discovery-mode CLI path: the `--query` mode through `RetrievalClient` and `PgVectorSearch` is exercised end-to-end against real retrieval candidates, not just at the typer-wiring import level
- `log/captures.md` captures the migration name-length convention (≤32 chars on Alembic revision strings to fit `alembic_version.version_num`)
- `charter/current-package.md` notes S39b in-flight then closed
- Smoke document at `docs/smoke/p11_s39b_corpus_reingest_and_real_authoring.md` walks the full re-ingest plus real-corpus authoring flow with inline executed-state evidence
- Session log entry at `log/sessions.md` as S39b

No new D-entries unless an architectural decision surfaces at reconciliation that the brief did not anticipate. The migration name-length convention is captured in `log/captures.md` as a project convention, not as a D-entry; it is a tooling constraint, not a strategic decision.

## Context to read first, in order

1. `charter/packages/p11-epic.md` — full file, P11 framing
2. `charter/decisions.md` — D109 (gold-set substrate including the appended S39 implementation correction), D105 (retrieval evaluation framing), D75 (platform-layer primitive promotion at S24), D26 (audit hash-chain), D31 (revision immutability)
3. `charter/current-package.md` — P11 framing block plus S39 close marker
4. `log/sessions.md` — S39 entry, full text including the three methodology lines and the carryovers to P12
5. `docs/smoke/p11_s39_retrieval_evaluation_substrate.md` — full document, particularly the Deviations and Smoke-evidence carryovers sections; the LVTGuide and corpus carryovers are the inputs to S39b's substantive work
6. `log/captures.md` — full file, particularly the 2026-05-13 S30b fixture-leak entry that names the LVTGuide wipe; the existing captures structure for the migration name-length addition
7. `contexts/ingestion/` — full directory, particularly the ingestion CLI command, the embedding model integration, the chunk-write path, and any S25-era source-document handling
8. `apps/cli/commands/retrieval_evaluation.py` — the discovery-mode `append-entry --query` path, the RetrievalClient invocation, the index-selection prompt shape
9. `apps/cli/commands/ingestion.py` (or wherever the ingestion CLI lives) — the S25-era ingestion command shape, source-discovery path, embedding-call path
10. `compose.yaml` — Ollama service configuration, embedding model availability, any image pins
11. The migration that seeds LVTGuide originally (smoke doc names migration 0007 `lvt_split` as the rename point from auto-migrated LVTRole; check whether the original seed of LVTGuide content lives at that migration or earlier)

## Pre-write reconciliation

Findings 1 through 3 require operator decision and pause for response. Findings 4 through 6 are autonomous-resolution unless surface state contradicts expectations.

**Finding 1: Re-ingestion source and path.** Report (a) where the S25 source documents live (repo path under `tests/fixtures/`, external location, or absent), (b) whether the ingestion CLI at `contexts/ingestion/` runs cleanly against the current codebase post-S38b drift check (specifically: does the CLI start without import errors, does the embedding call shape match the current Ollama integration, does the chunk-write path match the current per-tenant chunks schema), (c) what embedding model the ingestion uses by name and version. Recommend one of: (a) re-run the S25 ingestion CLI verbatim if it works without drift; (b) fix drift in the same commit and then re-ingest; (c) ingest a smaller substitute corpus if S25 sources are unavailable, with a captured note about the substitution. Operator selects.

**Finding 2: LVTGuide re-seed shape.** Report (a) whether the LVTGuide row exists in `role_templates` on the control plane (just the revision was wiped) or whether the template row itself was wiped, (b) what migration originally seeded LVTGuide's content (likely migration 0007 `lvt_split` per the smoke doc, but verify), (c) whether that migration is idempotent (re-run-safe) or destructive. Recommend one of: (a) re-run the original LVTGuide-seeding migration idempotently if it supports re-execution; (b) new forward-only migration that idempotently re-seeds LVTGuide plus applies the retrieval allowlist in a single transaction; (c) CLI-time authoring of LVTGuide with explicit `tool_allowlist` set to the retrieval pin (path-of-least-migrations, but produces a role-revision row whose origin is CLI rather than migration, which may not match the seven-McKinsey-roles audit shape). Operator selects.

**Finding 3: S39 synthetic gold-set handling.** The existing gold-set "P11 retrieval baseline" carries synthetic chunk_ids and has a verified hash-chain. Recommend (a) rename to "P11 retrieval baseline (synthetic, S39)" via direct repository update to preserve the aggregate intact and disambiguate from the real-corpus gold set authored at S39b. (b) Leave as-is and accept naming ambiguity in `gold-set list` output. (c) Delete; rejected by default given D31 immutability semantics on revisions and absence of any charter decision permitting aggregate deletion. Recommend (a). Operator confirms or selects alternative.

**Finding 4: Embedding model availability (autonomous unless deviation).** Confirm the embedding model that S25 ingestion uses (per Finding 1 (c)) is available on the current Ollama deployment via `docker compose exec ollama ollama list`. If the model is absent or has drifted to a different version, surface as a user-question before re-ingesting; embedding-vector incompatibility between S25-era ingestion and current model means retrieval candidates would be junk.

**Finding 5: Per-tenant chunks table shape (autonomous).** Confirm `chunks` table on tenant_a is empty (matches the smoke doc state) and that the schema the ingestion CLI writes to matches the schema the discovery-mode CLI reads from via `PgVectorSearch`. Surface if schema drift surfaces.

**Finding 6: Discovery-mode CLI runtime wiring (autonomous).** Confirm `gold-set append-entry --query ... --top-k ...` flows through `RetrievalClient` to `PgVectorSearch` correctly at runtime, not just at import-time. S39's smoke verified the typer-wiring imports; S39b is where runtime end-to-end gets confirmed. Surface any wiring divergence found between import-shape and runtime-shape.

## Pre-write reconciliation against current vendor state

Before re-ingesting, reconcile against the current Ollama deployment: list models available, confirm the embedding model named in Finding 1 (c) is present, confirm the model's embedding dimension matches the `chunks.embedding` vector column dimension on tenant_a's schema (S25-era ingestion may have used a different dimension; pgvector strict-typing rejects dimension mismatches at insert time). This pre-write step prevents the S4 Langfuse-shape miss: a session that drafts against memory and only discovers vendor-deployment drift mid-execution.

## Charter updates this session

One charter commit. No new D-entries.

**Commit 1 (charter):** `docs(charter): S39b in flight, migration name-length convention captured`.

`current-package.md` edit: append a paragraph after the S39 close marker and before the preserved P10 history section:

```
**S39b in flight.** Verification-and-hygiene bridge between S39 substrate close and S40 runner work. tenant_a corpus re-ingest from S25 sources (per S39 smoke carryover), LVTGuide re-seed plus retrieval-allowlist application (per S30b fixture-leak carryover surfaced again at S39 smoke), real-corpus discovery-mode gold-set authoring (closes S39 AC 10 verification gap), migration name-length convention captured in log/captures.md. No new D-entries; this is execution work closing S39's verification gaps.
```

`log/captures.md` addition (append to the appropriate captures section):

```
- **Migration name-length convention** (S39b, 2026-05-15)

  Alembic revision strings must stay ≤32 characters to fit the `alembic_version.version_num VARCHAR(32)` column. S39's initial revisions exceeded the ceiling (`0012_role_allowlist_retrieval_closure` at 37 chars, `0013_retrieval_evaluation_substrate` at 35 chars) and `make migrate` failed with `StringDataRightTruncation` on the `alembic_version` UPDATE. Shortened in place at smoke time to `0012_role_allowlist_retrieval` (29 chars) and `0013_retrieval_eval_substrate` (29 chars); transactional DDL rolled back cleanly, no partial state. Convention forward: revision-string components should fit `NNNN_<short_slug>` where `<short_slug>` stays under ~25 chars to leave headroom.
```

## Substantive work, commit-shaped

**Commit 1 (charter):** as drafted above. `docs(charter): S39b in flight, migration name-length convention captured`.

**Commit 2 (ingestion drift, conditional):** If Finding 1 (b) surfaces drift in the ingestion CLI that blocks re-ingest, fix the drift here. Conventional-commit message depends on the shape of the drift fix: `fix(ingestion): <specific drift>` or `chore(ingestion): align ingestion CLI with current per-tenant chunks schema`. Skip this commit entirely if the ingestion CLI runs cleanly without drift.

**Commit 3 (LVTGuide re-seed):** Per Finding 2 disposition. If (a): no new code, just re-run the original migration; capture evidence in the smoke document. If (b): new control-plane migration at `padhanam/persistence/migrations/control_plane/versions/NNNN_lvtguide_reseed_with_retrieval.py` (discover the next available number after 0012; keep revision string ≤32 chars per the convention captured at commit 1). If (c): no migration; CLI authoring of LVTGuide happens at execution time and is captured in the smoke document. Commit message depending on shape: `feat(p11/s39b): re-seed LVTGuide via control-plane migration` or `chore(p11/s39b): LVTGuide re-seeded via CLI authoring (no code change)`.

**Commit 4 (synthetic gold-set rename):** Per Finding 3 disposition. Likely a one-off CLI invocation against tenant_a's database that updates the `gold_sets.name` column on the S39 aggregate from "P11 retrieval baseline" to "P11 retrieval baseline (synthetic, S39)". The rename does not touch revisions (immutable per D31) and does not break hash-chain integrity (the revision's `this_event_hash` is over revision content, not gold-set name). No code change; captured as execution evidence in the smoke document. Commit message: `chore(p11/s39b): disambiguate S39 synthetic gold-set by rename`.

**Commit 5 (smoke):** `docs/smoke/p11_s39b_corpus_reingest_and_real_authoring.md` walks: pre-flight (vendor reconciliation against Ollama embedding model, image digest pin status); Stage 1 (corpus re-ingest evidence — chunk count post-ingest, sample chunk content, embedding dimension verification); Stage 2 (LVTGuide re-seed evidence — control-plane query confirming the row exists, `tool_allowlist` carries the retrieval pin, role-revision chain integrity passes); Stage 3 (S39 gold-set rename evidence — `gold-set list` showing the renamed aggregate); Stage 4 (real-corpus discovery-mode authoring — create gold-set "P11 retrieval baseline (real corpus)", three `append-entry --query` calls against re-ingested corpus with real queries, candidate indices selected by operator, finalize, hash-chain verification — recompute and compare, must match byte-identically). Commit message: `docs(p11/s39b): live-stack smoke for corpus re-ingest and real-corpus authoring`.

**Commit 6 (session log):** S39b entry at `log/sessions.md`. Commit message: `docs(log): S39b session log entry`.

## Acceptance criteria

1. `tenant_a`'s `chunks` table has at least 50 rows post-S39b (smoke evidence: `SELECT COUNT(*) FROM chunks` captured in the smoke document; exact threshold negotiable based on S25 corpus size, but non-zero is the hard floor).
2. `tenant_a`'s `chunks` table embedding dimension matches the embedding model's output dimension; `pgvector` accepts the inserts without dimension errors.
3. `LVTGuide` row exists in `role_templates` on the control plane post-Commit 3; the latest `role_revisions` row for LVTGuide carries `tool_allowlist` containing `[{"tool_id": "00000000-0000-0000-0000-000000000001", "revision_id": "00000000-0000-0000-0000-000000000002"}]`; role-revision chain integrity verifies on LVTGuide's chain.
4. The S39 gold-set on tenant_a is renamed to "P11 retrieval baseline (synthetic, S39)" or similar disambiguating name per Finding 3 disposition; the aggregate's revisions and entries are intact (hash-chain still verifies).
5. A new gold-set named "P11 retrieval baseline (real corpus)" exists on tenant_a with one finalized revision carrying three entries; each entry's `expected_chunk_ids` references real chunk rows in tenant_a's `chunks` table (not synthetic UUIDs); the revision's `this_event_hash` recomputes byte-identically against the persisted content.
6. The discovery-mode CLI path `gold-set append-entry --query ... --top-k ... --correct-indices ...` is exercised end-to-end at smoke time with real retrieval-surfaced candidates; the smoke document captures the retrieval candidates surfaced for each of the three queries and the indices the operator selected.
7. `log/captures.md` contains the migration name-length convention entry as drafted in Commit 1.
8. `charter/current-package.md` contains the "**S39b in flight**" paragraph as drafted in Commit 1.
9. All existing unit tests pass at session close; no regressions from S39 (test count baseline: 1047 per the S39 close report).
10. All import-linter contracts pass.
11. `git status` clean at session close.
12. Session log entry at `log/sessions.md` lands per the standard format including the `roles:` tag, `mode:` tag (build), commits landed, test count delta (likely zero or near-zero; this is execution work not feature work), and substantive reflection paragraphs against the reflection prompts where signal surfaced.

## Reflection prompts addressed in the session log entry

1. **Pre-write reconciliation findings outcomes.** Substantive paragraph on Findings 1, 2, 3 outcomes plus any deviations from the autonomous Findings 4, 5, 6. Did the S25 ingestion CLI run cleanly post-S38b drift check, or did drift fixes need to land? If drift was significant, what shape was it (schema, vendor API, embedding model, configuration)? Did LVTGuide's re-seed shape settle cleanly via the option (a) / (b) / (c) cut, or did a fourth option surface at execution time? Did the rename of the S39 synthetic gold-set raise any structural-honesty findings about the gold-set aggregate's name-mutation semantics that should land as a charter consideration at P12?

2. **Discovery-mode CLI end-to-end verification.** S39's smoke verified the typer-wiring imports but did not exercise the `--query` path against real retrieval candidates. S39b closes that verification gap. Did the runtime behaviour match the import-shape, or did the CLI surface runtime issues (tenant resolution, retrieval client wiring, candidate display formatting, index selection prompt) that the unit-test-level verification missed? Substantive paragraph if signal surfaced; one line if the runtime path was clean.

3. **Methodology candidate observations.** S39b is the first verification-and-hygiene bridge session in P11 (and probably in Phase 1; check the session log). Is this a methodology candidate pattern in itself? Specifically: "verification-and-hygiene bridge sessions between substrate sessions and feature sessions" as a session-shape distinct from substrate, feature, and audit shapes. The verification-debt-from-smoke-carryovers is the trigger condition; the bridge session is the resolution. One-line observation for the Phase 1 close audit at P12 if the pattern feels coherent at S39b close; substantive paragraph if it does not.

## Out of scope

- **Retrieval evaluation runner.** S40 territory. The runner consumes the real-corpus gold-set authored at S39b across the three retrieval strategies per D66 and computes metrics per D105.
- **Metric computation.** S40 territory.
- **Evaluation-result record schema.** S40 territory. S40 commits this.
- **Strategy comparison aggregation.** S40 territory.
- **`contexts/optimization/` substrate.** S41 territory.
- **HTTP transports for retrieval_evaluation.** S42 territory.
- **Tenant_b corpus re-ingestion.** Single-tenant smoke discipline; tenant_b's substrate is verified via the contract harness scenarios at S39's commit 9. If tenant_b's chunks table also empty post-fixture-cycles, that becomes a P12 carryover note, not S39b work.
- **Cross-tenant gold-set authoring.** Phase 2 territory per D105 alternative (f) and D53 precedent.
- **Re-running the S39 hash-chain integrity verification.** Already verified at S39 close; the renamed synthetic gold-set's hash-chain remains intact because the rename does not touch revisions, but no need to re-verify.
- **Manual-mode gold-set entry authoring.** Discovery mode is the only mode exercised at S39b; manual mode (operator directly inputs chunk IDs without retrieval invocation) remains S40-or-later territory per S39's out-of-scope commitment.
- **Image rebuild and digest pin commit.** Assumed already done at S39 close per the smoke doc; if not, surface and land as a hygiene commit before the smoke run.
- **New D-entries.** If reconciliation surfaces an architectural decision shape that the brief did not anticipate, surface as a user-question rather than land a D-entry within S39b. The session is execution work; architectural decisions route through Claude.ai strategic-mode conversations.

## Session log entry

Append a single entry at `log/sessions.md` after the S39 entry, as S39b. Format per the standard session log shape including the `roles:` tag (engineer primarily for execution; technical writer for smoke and log) and the `mode:` tag (build). Capture the commits landed (4 to 6 depending on Finding outcomes), test count delta versus session open (expected near-zero; this is execution work, not feature work), the Finding 1-6 outcomes with substantive paragraphs against 1, 2, 3 where signal surfaced, the reflection prompt outcomes, and any methodology candidate observations forwarded to the Phase 1 close audit. Particular flag for the P12 audit: whether S39b's verification-and-hygiene-bridge session shape is methodology-candidate worthy.
