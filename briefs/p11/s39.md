# S39: Allowlist closure plus `contexts/retrieval_evaluation/` substrate

## Goal at session close

The following artefacts exist at session close:

- Role-allowlist seed migrations updated so all seeded roles carry the retrieval tool reference in `tool_allowlist`; the no-retrieval gap that P8 and P9 surfaced and that D105 routed to P11 open is closed at the substrate layer
- New bounded context at `contexts/retrieval_evaluation/` per D108 and D105, with hexagonal layout matching the precedent at `contexts/run_history/` (S31) and `contexts/evaluation/` (P5)
- `GoldSet` aggregate root plus `GoldSetRevision` and `GoldSetEntry` value objects in the domain layer, with hash-chain audit on finalized revisions reusing `compute_event_hash` and `GENESIS_HASH` from `contexts/audit/domain/events.py` per D26
- Application use cases for gold-set authoring (create, append entry, finalize revision, list, get) at `contexts/retrieval_evaluation/application/`
- `GoldSetRepository` write-side port and `GoldSetReader` consumer-defined read port
- `PostgresGoldSetRepository` and `PostgresGoldSetReader` adapters against per-tenant databases
- Alembic migration creating three new tables (`gold_sets`, `gold_set_revisions`, `gold_set_entries`) on each tenant database
- Wiring at both composition roots (`apps/cli/_cross_context.py` and `apps/api/_agent_runtime_wiring.py`) following the consumer-port-plus-wiring-adapter pattern that reached its tenth instance at S36 and now extends to eleventh and twelfth
- CLI surface at `apps/cli/commands/retrieval_evaluation.py` walking the operator through discovery-mode gold-set authoring (query → retrieve top-K from per-tenant corpus → mark correct chunks in expected order → save entry)
- Tenant-isolation contract scenarios at `tests/contract/tenant_isolation/test_retrieval_evaluation_isolation.py` extending the harness; cross-tenant access through all surfaces must fail
- Live-stack smoke at `docs/smoke/p11_s39_retrieval_evaluation_substrate.md` walking the full discovery-mode authoring flow against tenant_a's corpus including hash-chain integrity verification
- D109 landed at `charter/decisions.md` committing the concrete gold-set domain shape and the hash-chain reuse pattern
- `charter/schema.md` gains the three new tables
- `charter/current-package.md` updated noting S39 in-flight
- Session log entry at `log/sessions.md` as S39

## Context to read first, in order

1. `charter/packages/p11-epic.md` — full file, P11 framing context including the S39 scope description
2. `charter/decisions.md` — D108 (P11 framing), D105 (retrieval evaluation framing with eight commitments), D26 (audit hash-chain), D53 (scoring-sheet aggregate as tenant-authored-with-revisions precedent), D66 (retrieval strategy registry), D31 (revision immutability), D14 (tenant-configured tool paths)
3. `charter/current-package.md` — P11 framing block plus the preserved "Per-invocation retrieval-constraint threading at ToolInvoker" carryover
4. `charter/principles.md` — Architectural section (hexagonal, bounded contexts, hybrid retrieval, jurisdiction-as-first-class) and Security posture section (audit logging, tenant isolation, audit hash-chain)
5. `charter/schema.md` — existing schema for the four producer contexts; migration shape conventions
6. `contexts/evaluation/` — full directory listing plus the scoring sheet aggregate's domain files (`domain/`), application use cases (`application/`), repository port, Postgres adapter, and CLI surface. Read for pattern reference; gold-set domain mirrors this shape per D105's sibling-contexts-sharing-patterns commitment
7. `contexts/run_history/` — directory listing plus the consumer-defined reader port at `ports/reader.py`. Read for new-context-substrate session shape from S31
8. `contexts/audit/domain/events.py` — `compute_event_hash`, `GENESIS_HASH`, hash-chain primitives for revision-finalization reuse
9. `contexts/ingestion/` — domain files referencing chunks; the chunk_id surface that gold-set entries reference per D105's indifferent point on test corpus shape
10. `contexts/methodology/` — existing role-seed migration location and current shape; the migration files seeded the default roles with empty `tool_allowlist` per the now-deleted carryover, and the allowlist closure extends those seeds
11. `apps/cli/commands/` — existing CLI command shapes (evaluation, methodology, agent, audit, etc.) for the gold-set authoring CLI pattern reference
12. `tests/contract/tenant_isolation/` — existing harness shape (current scenario count, fixture patterns) for the contract test extension

## Pre-write reconciliation

Findings 1 through 3 require operator decision and pause for response. Findings 4 through 8 are autonomous-resolution unless surface state contradicts expectations.

**Finding 1: Role-seed migration shape and location.** Report the migration file path that seeded the default roles, the current seeded role data shape (which roles seeded, current `tool_allowlist` values, role names and identifiers), and the retrieval tool reference shape expected in `tool_allowlist` (string ID, structured tuple, named-tool reference, or other). Recommend one of three migration shapes for the allowlist closure: (a) new forward-only Alembic migration that runs `UPDATE` against existing role rows to add the retrieval tool reference, with no change to the original seed-data source (the seed migration stays as historical record); (b) new forward-only migration plus a parallel seed-data file update so future seeded roles ship with the reference (the seed source becomes the new source of truth, the UPDATE handles existing rows); (c) something else surfaced at inspection time. Operator selects.

**Finding 2: Scoring-sheet aggregate shape at `contexts/evaluation/`.** Report the as-built domain shape: aggregate root class signature, revision value object shape, entry value object shape, hash-chain reuse pattern (does P5 reuse `contexts/audit/domain/events.py` primitives or carry its own?), revision status field and lifecycle, application use case shapes for create / append / finalize / list / get, repository port surface, and the chunk_id reference pattern if scoring-sheet entries reference chunks at all. The gold-set domain mirrors this shape where the patterns hold; divergences are where the data shape requires them (gold-set entries reference an ordered list of chunk IDs; scoring-sheet entries reference scoring criteria definitions). Report any structural-honesty findings about how D105's gold-set commitments fit or do not fit the scoring-sheet pattern as actually built; surface as a user-question before authoring the gold-set domain code if a meaningful divergence surfaces.

**Finding 3: Hash-chain primitive reuse confirmation.** Report whether `compute_event_hash` and `GENESIS_HASH` are still at `contexts/audit/domain/events.py` post-S38 (S36 reused them via the page-granularity chain integrity verifier per D102; the existing surface should still be live). Recommend the reuse shape for gold-set revision hashing: each finalized revision computes `this_event_hash` from its content (the revision's entry list serialized in canonical order, plus revision metadata) chained with `previous_event_hash` from the prior revision in the same gold-set; the genesis revision of each gold-set uses `GENESIS_HASH`. Surface any pattern deviation needed (e.g., if the audit context's hash primitive expects a specific input shape that requires adaptation).

**Finding 4: Chunk ID reference shape (autonomous unless deviation).** Confirm the chunk_id type (UUID) and the foreign-key relationship pattern. Gold-set entries' `expected_chunk_ids` is an ordered array of chunk_id values; storage shape is `text[]` or `uuid[]` depending on existing column conventions at `contexts/ingestion/` chunk references. Mirror the existing convention; surface if the chunk surface has shifted since S32.

**Finding 5: CLI command pattern (autonomous unless deviation).** Confirm the existing CLI command shape: which library (`click`, `typer`, or custom), how subcommands compose, how tenant context resolves at command invocation, how interactive prompts are structured. The gold-set authoring CLI follows the existing pattern.

**Finding 6: Tenant context propagation through CLI (autonomous).** Confirm the existing tenant resolution pattern at `apps/cli/_runtime.py` per the existing carryover at `charter/deferred-decisions.md` about hardcoded test-set mapping being Phase 2. The gold-set CLI inherits the existing dev-only tenant resolution.

**Finding 7: Composition root wiring patterns (autonomous).** Confirm the existing wiring pattern at `apps/cli/_cross_context.py` (the wiring adapter classes; tenth instance reached at S36 per the P10 retrospective addendum) and `apps/api/_agent_runtime_wiring.py` (the build_* factories). The `GoldSetRepositoryAdapter` and `GoldSetReaderAdapter` lands as eleventh and twelfth wiring classes respectively.

**Finding 8: Deferrable foreign-key support (autonomous unless deviation).** Confirm the existing Alembic migration surface supports `DEFERRABLE INITIALLY DEFERRED` foreign-key constraints. The `current_revision_id` column on `gold_sets` references `gold_set_revisions(id)`, and the inverse FK on `gold_set_revisions.gold_set_id` references `gold_sets(id)`; this circular dependency at insert time is what deferred constraints resolve. Confirm test-fixture patterns handle deferred-check timing (constraint checks fire at transaction commit, not at row insert). If deferrable constraints are not supported by the existing tooling or contradict an established migration convention, surface as a user-question before authoring Commit 6; the fallback is a nullable `current_revision_id` populated by a separate UPDATE after the first revision row inserts, which avoids the deferred constraint at the cost of an extra write step in the create_gold_set use case.

## Charter updates this session

Commit ahead of code. Two charter commits.

**Commit 1a (charter): `docs(charter): D109 retrieval_evaluation gold-set domain shape and hash-chain reuse`.** Lands D109 plus the schema-table additions plus the current-package marker.

D109 text for `charter/decisions.md` tail (revised at S39 pre-write reconciliation per Findings 2 and 3):

```
- **D109: Retrieval evaluation gold-set domain shape commits to revision-with-hash-chain aggregate following the methodology-context precedent; lifecycle ships at application-layer granularity for the first time in Phase 1** (P11 S39, 2026-05-15)

  **Choice.** Six coupled commitments. First, the gold-set aggregate root carries id (UUID primary key), tenant_id (UUID, jurisdiction-bearing per D12), jurisdiction (string), name (unique per tenant), created_by_user_id, created_at, current_revision_id (FK to the most recent finalized revision; nullable while no revisions exist). Second, the gold-set-revision value object carries id (UUID), gold_set_id (FK), revision_number (monotonic per gold-set), status (draft or finalized; immutable once finalized per D31), created_by_user_id, created_at, this_event_hash (computed at finalization), previous_event_hash (genesis or the prior finalized revision's hash); revisions are append-only and finalized revisions never mutate. Third, the gold-set-entry value object carries id (UUID), gold_set_revision_id (FK), entry_index (monotonic position within revision), query (text), expected_chunk_ids (ordered array of chunk_id values referencing the per-tenant chunks table from contexts/ingestion/); entry order within a revision is preserved by entry_index, expected_chunk_ids order encodes ranked relevance per D105. Fourth, hash-chain audit reuses a thin utility primitive extracted from contexts/audit/domain/events.py at the same module: compute_chained_payload_hash(payload: dict[str, Any], previous_hash: str) -> str — the canonical-JSON-sorted-keys-plus-SHA-256-with-chain-link mechanism. compute_event_hash continues to own audit-event input-shaping and calls the new helper internally; the existing audit chain on every tenant must verify bit-identical post-refactor per D26 chain-self-containment (verified at commit 2.5 by replaying tenant_a's existing audit log through the refactored compute_event_hash). At gold-set revision finalization, contexts/retrieval_evaluation/domain/hash_chain.py exposes compute_revision_hash, which constructs the revision's canonical payload and calls compute_chained_payload_hash. Canonical serialization is JSON with sorted keys, UTF-8 encoding, no whitespace; the revision-level payload shape is {"revision_number": int, "entries": [{"entry_index": int, "query": str, "expected_chunk_ids": [UUID strings in lowercase canonical form]}, ...]} with the entries array sorted by entry_index ascending; this payload is the input to compute_chained_payload_hash, which performs the canonicalization-and-hashing internally per the utility primitive's contract. The genesis revision of each gold-set uses GENESIS_HASH as previous_event_hash; chain integrity verifies on read at revision granularity by recomputing this_event_hash from the persisted revision content and comparing against the stored value. Fifth, the structural precedent for the revision-with-hash-chain shape is contexts/methodology/ (and the role/methodology aggregates more broadly), which carry chain-self-contained revision hashes per D26 even though their lifecycle is migration-time rather than application-time (e.g. migration 0010_role_tool_allowlist_pin recomputes role-revision hashes when allowlist content shifts). The gold-set aggregate is sibling-in-placement to contexts/evaluation/ and contexts/run_history/ per D105's first commitment, but it is not sibling-in-pattern to scoring-sheet — pre-write reconciliation at S39 surfaced that the scoring-sheet aggregate at contexts/evaluation/ is read-only at S16 with no hash-chain, no status lifecycle, and no write use cases. contexts/retrieval_evaluation/ ships the first context in Phase 1 to ship the full create/append/finalize/list/get lifecycle with revision-granularity hash-chain at application-layer granularity; when contexts/optimization/ lands at S41 with its own revision-with-hash-chain aggregate, it inherits from the gold-set pattern rather than mirroring methodology's migration-time approximation. Pattern mirroring follows D105's first commitment at the shape level without code substrate sharing per the principles.md commitment ("shared_kernel/ is tiny and policed: only types that must be referentially equal across contexts, never Pydantic"). Sixth, the application layer ships five use cases at S39: create_gold_set (creates aggregate root with draft initial revision), append_entry_to_revision (appends to the current draft revision), finalize_revision (marks the current draft revision finalized, computes this_event_hash via compute_revision_hash, opens a new draft revision if subsequent edits arrive), list_gold_sets (paginated read), get_gold_set_with_current_revision (read aggregate plus current revision plus entries).

  **Reasoning.** The aggregate root shape with current_revision_id FK plus revision-with-hash-chain mirrors the contexts/methodology/ precedent for chain-self-contained revisions per D26; mirroring at the shape level (not the code substrate level) reduces cognitive load architecturally without sharing data substrate. The methodology and role aggregates already carry revision hashes at the control plane, recomputed when content changes (e.g. migration 0010's allowlist pin); the gold-set extends this to application-layer authoring with explicit finalize_revision use case granularity, making revision finalization a deliberate operator action rather than a migration-time side-effect. The hash-chain audit reuse at revision finalization extends contexts/audit/domain/events.py's primitive via a thin extracted helper (compute_chained_payload_hash) rather than wholesale promotion to shared_kernel/; the audit context owns the chained-payload-hash utility primitive (canonical JSON sorted keys + SHA-256 + chain link), compute_event_hash continues to own audit-event input-shaping, and retrieval_evaluation owns the gold-set-revision input-shaping at compute_revision_hash. Chain integrity verification on read at revision granularity mirrors the audit context's S36 page-granularity verifier pattern. The append-only-revision shape per D31 ensures gold-set history is immutable; corrections happen as new revisions, never as in-place edits, which preserves the audit surface across tenant operator authoring sessions. The expected_chunk_ids ordered-array shape commits to D105's ranked-relevance unit of evaluation without committing to graded relevance (which D105 defers); the array order encodes ranking and the flat 0/1-style "is correct" semantics matches the starter metric set (recall@k, precision@k, MRR) per D105's seventh commitment. The five application use cases cover the CLI's authoring path completely; the runner at S40 adds its own use cases for reading gold sets in evaluation context (which reuses get_gold_set_with_current_revision or extends the reader port if a different read shape surfaces). contexts/retrieval_evaluation/ is sibling-in-placement to contexts/evaluation/ and contexts/run_history/ per D105's first commitment, but the structural precedent for the revision-with-hash-chain lifecycle is contexts/methodology/; D105's "sibling-contexts-sharing-patterns" framing rested on a scoring-sheet premise that did not survive S39 pre-write reconciliation. D105 is not revised at S39; the methodology line in the S39 session log entry flags the framing-versus-as-built divergence for the P12 audit, which decides whether D105's framing needs a successor D-entry or whether the gold-set's structural-first status absorbs the correction. The gold-set as Phase 1's first full application-layer lifecycle with revision-granularity hash-chain is the structurally honest framing forward; the optimization context at S41 inherits from this shape.

  **Alternatives considered.**

  (a) Inherit from contexts/methodology/ aggregate via parent class, mixin, or shared_kernel substrate. Rejected because cross-context substrate coupling violates principles.md ("shared_kernel/ is tiny and policed: only types that must be referentially equal across contexts, never Pydantic"). The structural correspondence between methodology revisions and gold-set revisions is conceptual not substrate; the gold-set context owns its aggregate root, its revision value object, its entry value object, and its hash-chain wiring; methodology owns its own. Sibling pattern mirroring at the shape level without code substrate sharing is the structurally honest cut. (The original D109 draft named contexts/evaluation/'s scoring-sheet aggregate as the inheritance candidate; pre-write reconciliation at S39 surfaced that the scoring-sheet aggregate has no hash-chain, no status lifecycle, and no write use cases at S16, so inheritance from it was structurally moot regardless of the substrate-coupling rejection.)

  (b) Three-aggregate model (gold-set, revision, entry as separate aggregate roots with their own ports and adapters). Rejected because revisions and entries do not have lifecycle outside their containing gold-set; one aggregate root with two value objects is the right granularity per the aggregate-root pattern in the existing contexts.

  (c) Reuse contexts/audit/domain/events.py primitives via direct import everywhere they are needed, with no retrieval_evaluation-context wrapper module and no audit-context refactor. Rejected on two grounds. First, the canonical-serialization rule for revision content is domain logic specific to retrieval_evaluation, not utility surface; placing it inside a wrapper module at contexts/retrieval_evaluation/domain/hash_chain.py keeps the domain-specific input-shaping inside the consuming context. Second, the contexts/audit/domain/events.py surface that needs reuse is the canonical-JSON-plus-SHA-256-with-chain-link mechanism, not compute_event_hash itself — compute_event_hash takes eleven audit-event-shaped named fields (actor, tenant_id, jurisdiction, timestamp, action_verb, resource_type, resource_id, before_state, after_state, correlation_id, previous_event_hash); calling it from retrieval_evaluation by stuffing gold-set content into after_state would semantically conflate audit events with gold-set revisions. The structurally honest cut is the audit-context refactor at commit 2.5: extract compute_chained_payload_hash(payload: dict[str, Any], previous_hash: str) -> str as a thin utility primitive inside contexts/audit/domain/events.py; refactor compute_event_hash to construct its eleven-field audit-event payload dict and call the new helper internally; expose compute_revision_hash at contexts/retrieval_evaluation/domain/hash_chain.py that constructs the gold-set-revision canonical payload per commitment 4's spec and calls compute_chained_payload_hash. The audit context owns the utility primitive; both wrappers own their domain-specific input-shaping. The refactor preserves bit-identical compute_event_hash output on the same input (verified at commit 2.5 by replaying tenant_a's existing audit log and confirming every stored this_event_hash recomputes identically); if any divergence surfaces, the refactor is wrong and commit 2.5 does not land. If a future structural-honesty finding surfaces that compute_chained_payload_hash has callers beyond audit and retrieval_evaluation, the helper's promotion to a shared utility module is the right move at that point; wholesale promotion to shared_kernel/ at S39 is the wrong cut per principles.md.

  (d) Persist hash-chain status as separate column on gold_set_revisions (verified/broken_at_row/partial) rather than computing on read. Rejected because the storage model is computed-on-read per the S36 audit reader pattern; persisting introduces a write-time computation and a stored-versus-recomputed divergence risk. Read-time verification is the audit-surface honest shape.

  (e) Mutable revisions with explicit "frozen" flag rather than append-only-with-status-finalized. Rejected because D31 commits revision immutability for tenant-authored aggregates; the append-only-with-status pattern is the existing methodology-context shape and gold-sets follow the same pattern.

  (f) Genesis-revision hashing via a per-tenant genesis hash rather than the audit context's GENESIS_HASH constant. Rejected because the chain anchor is logically a constant within the context; per-tenant anchoring adds no audit-defensibility value because the tenant_id is already on every row, and the GENESIS_HASH reuse is the simpler shape.

  **Kano.** Must-have on the aggregate root shape, the revision and entry value object shapes, the hash-chain reuse via the extracted utility primitive, the application-layer five-use-case lifecycle, the methodology-as-structural-precedent framing; these are what D105 commits and what the substrate the runner at S40 and the optimization engine at S41 depend on. Performance on the application use case shape at S39 (covers the authoring path; S40's runner may add reader-port methods if its read shape diverges from the get_gold_set_with_current_revision use case). Indifferent on specific field naming conventions (UUID column names, timestamp column names; mirror existing context conventions).
```

Schema additions at `charter/schema.md` (append to the appropriate per-tenant section):

```
### contexts/retrieval_evaluation/ (per-tenant database, P11 S39)

gold_sets
  id              UUID PRIMARY KEY
  tenant_id       UUID NOT NULL
  jurisdiction    VARCHAR NOT NULL
  name            VARCHAR NOT NULL
  created_by_user_id VARCHAR NOT NULL
  created_at      TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
  current_revision_id UUID NULL REFERENCES gold_set_revisions(id) DEFERRABLE INITIALLY DEFERRED
  UNIQUE (tenant_id, name)

gold_set_revisions
  id                   UUID PRIMARY KEY
  gold_set_id          UUID NOT NULL REFERENCES gold_sets(id) ON DELETE RESTRICT
  revision_number      INTEGER NOT NULL
  status               VARCHAR NOT NULL  -- 'draft' | 'finalized'
  created_by_user_id   VARCHAR NOT NULL
  created_at           TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
  finalized_at         TIMESTAMP WITH TIME ZONE NULL
  this_event_hash      VARCHAR NULL  -- populated at finalization
  previous_event_hash  VARCHAR NULL  -- GENESIS_HASH or prior revision's this_event_hash
  UNIQUE (gold_set_id, revision_number)

gold_set_entries
  id                       UUID PRIMARY KEY
  gold_set_revision_id     UUID NOT NULL REFERENCES gold_set_revisions(id) ON DELETE RESTRICT
  entry_index              INTEGER NOT NULL
  query                    TEXT NOT NULL
  expected_chunk_ids       UUID[] NOT NULL  -- ordered array
  UNIQUE (gold_set_revision_id, entry_index)
```

`current-package.md` edit: insert a new paragraph after the existing P11 framing block, before the "P10 — final history (preserved below for traceability)" section:

```
**S39 in flight.** Substrate work for the retrieval evaluation context underway per D108's first commitment and D105's substrate commitments. Allowlist closure as commit 1 (role-allowlist seed migrations extended with retrieval tool reference). Retrieval evaluation context substrate as subsequent commits per D109's six commitments: domain aggregate root plus revision and entry value objects, application use cases for authoring, GoldSetRepository write-side port plus GoldSetReader consumer-defined read port, Postgres adapter with hash-chain reuse from contexts/audit/domain/events.py per D26, Alembic migration creating three new tables on per-tenant databases, wiring at both composition roots, CLI surface for discovery-mode gold-set authoring, tenant-isolation contract scenarios, live-stack smoke against tenant_a.
```

## Substantive work, commit-shaped

**Commit 1 (charter):** as drafted above. `docs(charter): D109 retrieval_evaluation gold-set domain shape and hash-chain reuse`.

**Commit 2 (feature):** Allowlist closure. Forward-only Alembic migration `0012` on the control plane (next number after `0011_tenant_actor_provenance`) that UPDATEs the eight seeded role-revision rows — LVTGuide plus the seven McKinsey 7-Step roles (ProblemFramer, Disaggregator, Prioritiser, Planner, Analyst, Synthesiser, Communicator) — to set `tool_allowlist = [{"tool_id": "00000000-0000-0000-0000-000000000001", "revision_id": "00000000-0000-0000-0000-000000000002"}]` referencing the retrieval tool seeded at migration `0009_create_tools_tables`. The UPDATE step recomputes each role-revision's chain-self-contained hash per D26, reusing the recompute helper pattern established at migration `0010_role_tool_allowlist_pin` (canonical-payload JSON sort + SHA-256, with `previous_event_hash` chained from each revision's predecessor or `GENESIS_HASH` for revision-1 rows). The recompute is the structural integrity work that makes the UPDATE D26-honest, not a side-detail; without it, the chain integrity verification breaks on every UPDATEd role. Commit message body explicitly names the role-revision hash recompute step and references migration 0010 as the pattern precedent. Smoke verification in commit 10 queries each of the eight role rows post-migration to confirm the retrieval reference is present and runs chain integrity verification across each role's revision chain (no broken chain on any of the eight roles). `feat(p11/s39): allowlist closure migration with role-revision hash recompute`.

**Commit 2.5 (audit refactor precursor):** Extract `compute_chained_payload_hash(payload: dict[str, Any], previous_hash: str) -> str` as a thin utility primitive inside `contexts/audit/domain/events.py` that performs canonical-JSON-sorted-keys-plus-SHA-256-with-chain-link (the mechanism `compute_event_hash` currently performs inline). Refactor `compute_event_hash` to construct its eleven-field audit-event payload dict and call `compute_chained_payload_hash` internally. The refactor must produce bit-identical `compute_event_hash` output on the same eleven-field input as before; if any divergence surfaces, the refactor is wrong and the commit does not land. Verification evidence: (1) extend unit tests at `tests/unit/contexts/audit/domain/test_events.py` with known input/output fixtures covering pre-refactor `compute_event_hash` outputs; (2) replay tenant_a's existing audit log through the refactored function — read every event row from tenant_a's `tenant_audit` table, recompute each event's `this_event_hash` via the refactored `compute_event_hash` from the stored payload, compare against the stored `this_event_hash`, confirm bit-identical match across every event in the chain. Capture the replay verification evidence in the commit message body or as a referenced smoke document. The S36 page-granularity verifier at `contexts/audit/adapters/outbound/postgres/reader.py` continues to call `compute_event_hash` exactly as today; only the internals shift. No changes to the layers-audit `import-linter` contract (the helper stays inside `contexts/audit/domain/`). `feat(p11/s39): extract compute_chained_payload_hash utility primitive in contexts/audit/domain/events.py`.

**Commit 3 (domain):** `contexts/retrieval_evaluation/__init__.py`, `contexts/retrieval_evaluation/domain/gold_set.py` (aggregate root), `contexts/retrieval_evaluation/domain/gold_set_revision.py` (value object), `contexts/retrieval_evaluation/domain/gold_set_entry.py` (value object), `contexts/retrieval_evaluation/domain/hash_chain.py` — thin module that imports `compute_chained_payload_hash` and `GENESIS_HASH` from `contexts/audit/domain/events.py` (per the commit 2.5 refactor) and exposes `compute_revision_hash(revision: GoldSetRevision, entries: tuple[GoldSetEntry, ...], previous_event_hash: str) -> str` that constructs the gold-set-revision canonical payload per D109's commitment 4 spec and calls `compute_chained_payload_hash`. The wrapper owns the gold-set-specific input-shaping (the payload dict construction); the audit context owns the canonicalization-and-hashing mechanism. Unit tests at `tests/unit/contexts/retrieval_evaluation/domain/` cover the canonical payload shape (entries sorted by entry_index, UUID lowercase canonical form, sorted-keys JSON byte-identity) and at least one golden-vector test that pins the hash output for a known revision payload. `feat(p11/s39): retrieval_evaluation domain value objects and hash-chain wrapper`.

**Commit 4 (application):** `contexts/retrieval_evaluation/application/` with `create_gold_set.py`, `append_entry_to_revision.py`, `finalize_revision.py`, `list_gold_sets.py`, `get_gold_set.py`. `contexts/retrieval_evaluation/ports/repository.py` with `GoldSetRepository` Protocol (write-side: persist_aggregate, append_entry, finalize_revision). `contexts/retrieval_evaluation/ports/reader.py` with `GoldSetReader` Protocol (read-side: list_gold_sets with pagination, get_gold_set_with_current_revision, get_revision_with_entries). Unit tests against fake adapters. `feat(p11/s39): retrieval_evaluation application use cases and consumer-defined ports`.

**Commit 5 (adapter):** `contexts/retrieval_evaluation/adapters/outbound/postgres/repository.py` implementing `GoldSetRepository` against per-tenant database, with hash-chain on revision finalization. `contexts/retrieval_evaluation/adapters/outbound/postgres/reader.py` implementing `GoldSetReader` with cursor pagination matching the S33 base64-of-JSON shape. Unit tests against fake session for query-shape verification. `feat(p11/s39): PostgresGoldSetRepository and PostgresGoldSetReader adapters`.

**Commit 6 (migration):** Alembic migration at `padhanam/persistence/migrations/tenant/versions/NNNN_retrieval_evaluation_substrate.py` creating `gold_sets`, `gold_set_revisions`, `gold_set_entries` tables per the schema above. Discover the next available migration number from the existing per-tenant migrations directory before authoring the file; do not use a placeholder. Forward-only; downgrade not supported (per existing migration discipline). `feat(p11/s39): Alembic migration for retrieval_evaluation per-tenant tables`.

**Commit 7 (wiring):** `apps/cli/_cross_context.py` extended with `GoldSetRepositoryAdapter` (eleventh wiring class) and `GoldSetReaderAdapter` (twelfth). `apps/api/_agent_runtime_wiring.py` extended with `build_gold_set_repository` and `build_gold_set_reader` factories. Wiring tests at `tests/integration/apps/api/test_wiring.py` extended. If a parallel CLI-side wiring integration test surface exists, extend it in parallel; if no CLI-side wiring test pattern exists, surface the asymmetry in the session log entry as a methodology-candidate observation for the Phase 1 close audit. `feat(p11/s39): retrieval_evaluation wiring at CLI and API composition roots`.

**Commit 8 (CLI):** `apps/cli/commands/retrieval_evaluation.py` with subcommands: `create-gold-set` (name, tenant context resolved per existing pattern), `append-entry` (interactive: query prompt → retrieval surfaces top-K candidates from per-tenant corpus via `RetrievalClient` → operator marks correct chunks in expected order via index selection → save entry to current draft revision), `list-gold-sets` (paginated output), `get-gold-set` (display aggregate plus current revision plus entries), `finalize-revision` (marks current draft finalized, computes hash, opens new draft). Discovery-mode authoring uses the existing RetrievalClient port from D5; the CLI invokes retrieval directly without going through the agent loop. Unit tests against fake CLI fixtures. `feat(p11/s39): retrieval_evaluation CLI for discovery-mode gold-set authoring`.

**Commit 9 (contract tests):** `tests/contract/tenant_isolation/test_retrieval_evaluation_isolation.py` with scenarios: cross-tenant gold-set list returns empty / 404, cross-tenant gold-set get returns 404, cross-tenant revision access returns 404, cross-tenant entry access returns 404, cross-tenant append attempt fails, FK constraints prevent cross-tenant revision-to-gold-set association. Harness count extends; report new total in the session log entry. `test(p11/s39): tenant_isolation scenarios for retrieval_evaluation`.

**Commit 10 (smoke):** `docs/smoke/p11_s39_retrieval_evaluation_substrate.md` walking: pre-flight verification that tenant_a's role rows carry the retrieval tool reference post-allowlist-migration; create gold-set "P11 retrieval baseline" against tenant_a; append three entries via discovery mode (operator-authored queries, retrieval surfaces candidates, operator marks correct chunks in order); finalize revision; verify hash-chain integrity (this_event_hash present on finalized revision, previous_event_hash is GENESIS_HASH); list gold sets; get gold set with current revision; recompute the revision's hash and compare against stored hash. End-to-end commit message: `docs(p11/s39): live-stack smoke for retrieval_evaluation substrate against tenant_a`.

## Acceptance criteria

1. `charter/decisions.md` ends with D109 verbatim as drafted in this prompt, including the six commitments, the reasoning, the six alternatives considered, and the Kano section.
2. `charter/schema.md` contains the three new tables (`gold_sets`, `gold_set_revisions`, `gold_set_entries`) with column shapes matching D109's commitments and the schema text in this prompt.
3. `charter/current-package.md` contains the "**S39 in flight**" paragraph as drafted in this prompt, inserted after the existing P11 framing block and before the preserved P10 history section.
4. Post-commit-2, querying tenant_a's seeded role rows shows the retrieval tool reference present in `tool_allowlist`. Smoke evidence captured in commit 2's message body or referenced in commit 10's smoke document.
5. `contexts/retrieval_evaluation/` directory contains the hexagonal layout matching `contexts/run_history/` and `contexts/evaluation/`: `domain/`, `application/`, `ports/`, `adapters/outbound/postgres/`.
6. `GoldSet`, `GoldSetRevision`, `GoldSetEntry` are defined per D109's first three commitments and the column shapes in `charter/schema.md`.
7. The audit-context refactor at commit 2.5 lands: `compute_chained_payload_hash(payload: dict[str, Any], previous_hash: str) -> str` is exposed at `contexts/audit/domain/events.py` as a thin utility primitive; `compute_event_hash` is refactored to call the new helper internally and produces bit-identical output on the same eleven-field input as before (verified by replaying tenant_a's existing audit log through the refactored `compute_event_hash` and confirming every stored `this_event_hash` recomputes identically; chain integrity passes across the full audit chain). `compute_revision_hash` at `contexts/retrieval_evaluation/domain/hash_chain.py` imports `compute_chained_payload_hash` and `GENESIS_HASH` from `contexts/audit/domain/events.py`, constructs the gold-set-revision canonical payload per D109's commitment 4 spec, and calls `compute_chained_payload_hash`. No parallel canonical-JSON-plus-SHA-256 primitive ships at `contexts/retrieval_evaluation/`.
8. Alembic migration at `padhanam/persistence/migrations/tenant/versions/NNNN_retrieval_evaluation_substrate.py` applies cleanly to both tenant databases (tenant_a and tenant_b if seeded) via `make migrate` or the equivalent path.
9. `GoldSetRepositoryAdapter` and `GoldSetReaderAdapter` exist at `apps/cli/_cross_context.py` as the eleventh and twelfth wiring classes respectively; `build_gold_set_repository` and `build_gold_set_reader` factories exist at `apps/api/_agent_runtime_wiring.py`.
10. CLI subcommands `create-gold-set`, `append-entry`, `list-gold-sets`, `get-gold-set`, `finalize-revision` exist at `apps/cli/commands/retrieval_evaluation.py` and execute end-to-end against tenant_a per the smoke document.
11. `tests/contract/tenant_isolation/test_retrieval_evaluation_isolation.py` extends the harness; new scenario count reported in the session log entry. All scenarios pass.
12. All unit tests pass at session close. Net new test count reported in the session log entry.
13. All import-linter contracts pass; `tests/_enforcement/` verification clean. `contexts.retrieval_evaluation.ports` added to the layers-audit contract.
14. `docs/smoke/p11_s39_retrieval_evaluation_substrate.md` exists and walks the full discovery-mode authoring flow against tenant_a end-to-end including the hash-chain integrity verification step.
15. `git status` is clean at session close. No uncommitted changes.
16. Session log entry at `log/sessions.md` lands per the standard format including the `roles:` tag, `mode:` tag (build), commits landed, decisions (D109), test count delta, contract harness count delta, and substantive reflection paragraphs against the reflection prompts where signal surfaced.

## Reflection prompts addressed in the session log entry

1. **Pre-write reconciliation findings.** Substantive paragraph on Findings 1, 2, 3 outcomes. Did the existing role-seed migration shape match the brief's expectation? Did the scoring-sheet aggregate at `contexts/evaluation/` provide a clean pattern for the gold-set domain, or did structural-honesty findings surface that required D109 text revision before commit? Did the hash-chain primitive surface at `contexts/audit/domain/events.py` reuse cleanly, or did the audit context's expected input shape require an adapter wrapper at `contexts/retrieval_evaluation/domain/hash_chain.py`?

2. **Architectural-extension shape comparison to S31.** S31 was P9's first new-context substrate session for `contexts/run_history/`. S39 is P11's first new-context substrate session for `contexts/retrieval_evaluation/`. The methodology-candidate observation from the P10 retrospective addendum was that P10's mode shifted to architectural-extension (extending existing contexts) versus P5-through-P9's mode of greenfield-context-establishment. S39 returns to greenfield-context mode. Did the pattern-mirroring discipline from D105 (sibling-contexts-sharing-patterns) make greenfield-mode meaningfully easier than S31, or did the new-context-establishment work still surface its own structural questions? Substantive paragraph if signal surfaces; one line if not.

3. **Methodology candidate count update.** Consumer-port-plus-wiring-adapter pattern reaches eleventh and twelfth instances at S39 (`GoldSetRepositoryAdapter` and `GoldSetReaderAdapter` at `apps/cli/_cross_context.py`). Pre-write reconciliation discipline reinforced at S39 against a greenfield-context surface rather than the substrate-extension surface that characterised P10. Whether the pre-write reconciliation's value at greenfield mode differs from its value at extension mode is worth a one-line observation for the Phase 1 close audit at P12.

## Out of scope

- **Retrieval evaluation runner.** S40 territory. The runner exercises the agent loop against gold sets across the three retrieval strategies per D66 and computes recall@k, precision@k, MRR per D105. S39 ships the gold-set substrate; the runner consumes gold sets at S40.
- **Metric computation.** S40 territory. recall@k, precision@k, MRR at k of 1/3/5/10 plus latency recording land at S40 against an evaluation-result record schema that S40 commits.
- **Strategy comparison aggregation.** S40 territory. Comparing vector_only, graph_only, and parallel_rrf on the same gold set per D66's registry produces the substrate the optimization context reads at S41.
- **`contexts/optimization/` substrate.** S41 territory. Recommendation aggregate, four consumer-defined reader ports, recommendation generation per the four committed categories.
- **HTTP transports for retrieval_evaluation.** S42 territory. Phase 2 UX consumer surface lands at S42 alongside optimization and evaluation HTTP transports.
- **Per-invocation retrieval-constraint threading at ToolInvoker.** Phase 2 territory per the existing carryover at `charter/current-package.md`.
- **Online retrieval evaluation.** Phase 2 territory per D105 alternative (g).
- **nDCG and graded relevance.** Phase 2 territory per D105 alternatives (d) and (e). The `expected_chunk_ids` ordered-array shape is forward-compatible: a `relevance_grade` column defaulting to 1 can land when consumer evidence demands it.
- **Platform-curated cross-tenant gold sets.** Phase 2 territory per D105 alternative (f) and D53 precedent.
- **Manual-mode gold-set entry authoring.** Discovery mode (retrieve top-K, mark correct) is the only mode at S39. Manual entry (operator directly inputs chunk IDs without retrieval invocation) lands at S40 or later if authoring evidence demands.
- **Cross-tenant manual session against tenant_b.** Smoke runs against tenant_a only at S39. Cross-tenant verification via the contract harness scenarios is sufficient at substrate level.

## Session log entry

Append a single entry at `log/sessions.md` after the file header and the S38b entry, as S39. Format per the standard session log shape including the `roles:` tag (analyst, PM, architect, engineer, technical writer per the new-context-substrate session shape; greenfield-context work typically exercises all five) and the `mode:` tag (build). Capture the eleven commits landed (commits 1, 2, 2.5, 3, 4, 5, 6, 7, 8, 9, 10), D109 as the new decision, test count delta versus session open, contract harness scenario count delta, the pre-write reconciliation findings outcomes per Finding 1-8 with substantive paragraphs against Findings 1, 2, 3 (each surfaced real structural issues), the reflection prompt outcomes with substantive paragraphs where signal surfaced, any methodology candidate observations forwarded to the Phase 1 close audit, and any structurally honest findings at the smoke document that should inform S40's runner work.

The methodology line at session close MUST flag for the P12 audit that D105's "sibling-contexts-sharing-patterns" framing rested on a scoring-sheet premise that did not survive S39 pre-write reconciliation (`contexts/evaluation/` is read-only at S16 with no hash-chain, no status lifecycle, no write use cases; the actual structural precedent for revision-with-hash-chain is `contexts/methodology/`, with the gold-set as Phase 1's first application-layer lifecycle of this shape). **D105 is NOT revised at S39**; the P12 audit decides whether D105's framing needs a successor D-entry or whether the gold-set's structural-first status absorbs the correction. Pre-write reconciliation as architectural discovery fires its sixth-or-seventh instance at S39 (cross-reference the captures count); the methodology line should also note this for the Phase 1 close audit's recurrence threshold.
