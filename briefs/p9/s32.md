# Brief: P9 — S32 — citation linking, snapshot population, single-transaction completion seam, source_citation schema revision

**mode:** build

## Goal

At S32 close the following artefacts exist:

- Two new agent-context-owned domain value objects at `contexts/agent/domain/citation_candidates.py`: `ChunkCitationCandidate` carrying chunk_id, source_id, chunk_index, content snapshot, structured source-level attribution snapshot, plus tenant_id and jurisdiction; `EntityCitationCandidate` carrying the Neo4j composite key fields (entity_tenant_id, entity_name, entity_type), source_chunk_ids snapshot, plus tenant_id and jurisdiction. A discriminated union type `CitationCandidate` over the two.
- `ToolCallCompleted` event extended with one new field: `citation_candidates: tuple[CitationCandidate, ...] = ()` per D96.
- `AgentRetrievalClientAdapter` at `apps/cli/_cross_context.py` extended to translate `ChunkResult` and `EntityResult` from ingestion into agent-context citation candidate types and surface them through the tool invocation path.
- `AgentLoopExecutor` extended to populate `citation_candidates` on `ToolCallCompleted` events from the tool invocation result.
- `invoke_agent` accumulator extended to absorb citation candidates from `ToolCallCompleted` events across the run and pass them to `writer.record_run` alongside the run record.
- `RunRecord` domain object extended to carry `chunk_citations: tuple[ChunkCitationRecord, ...]` and `entity_citations: tuple[EntityCitationRecord, ...]`. The record domain types map cleanly from the candidate types; the use case is the structural authority on the mapping per the D54 precedent.
- `record_run` use case at `contexts/run_history/application/use_cases.py` extended to write runs + chunk citations + entity citations in a single transaction.
- `PostgresRunHistoryAdapter` extended to grow the `async with session.begin()` block to write all three tables together. Partial-failure semantics: any table-write failure rolls the whole transaction back; the runs row only persists if both citation tables persist successfully.
- Alembic revision `0012_revise_citation_snapshots` (or whichever number reconciliation surfaces) on the `alembic/tenant/` track: drops `source_citation text` from `run_chunk_citations`; adds `source_snapshot jsonb NOT NULL DEFAULT '{}'::jsonb` to `run_chunk_citations`; drops `entity_display_label text` from `run_entity_citations`; adds `source_chunk_ids text[] NOT NULL DEFAULT '{}'::text[]` to `run_entity_citations`. Both tenants migrated.
- Tenant-isolation contract harness extended at `tests/contract/tenant_isolation/test_run_history_isolation.py` to cover citation-row cross-tenant write isolation. Cross-tenant write of citation rows attempted through the adapter raises.
- D96 appended to `charter/decisions.md` per Appendix A.
- `charter/schema.md` updated for both citation tables per Appendix C.
- `charter/packages/p9-epic.md` S32 line replaced per Appendix B.
- `charter/current-package.md` transitioned per Appendix B.
- `briefs/p9/s32.md` preserved per convention.

## Context to read first, in order

1. `charter/decisions.md` D24 (tenant-isolation harness), D32 (database-per-tenant), D65 (RetrievalClient port), D89 (tool registry, tool revisions, retrieval tool seed), D90 (streaming runtime events), D94 (P9 substrate framing), D95 (D95-as-landed schema and write-timing).
2. `charter/principles.md` Engineering practice and User safety sections.
3. `charter/packages/p9-epic.md` and `charter/current-package.md`.
4. `charter/schema.md` per-tenant `run_chunk_citations` and `run_entity_citations` sections (current shape from S31).
5. `briefs/p9/s31.md` for the precedent at the runs-row write path and the accumulator shape.
6. `log/sessions.md` S31 entry for the structural-honesty deviations resolved at session-open and the methodology observations recorded.

## Pre-write reconciliation

Six surfaces to verify against current code before any commit lands. Findings appended to this brief at session close per the no-in-place-edits rule.

1. **Retrieval tool current return shape.** Read `contexts/ingestion/adapters/outbound/retrieval/pgvector_search.py` and `neo4j_traverse.py` to confirm `ChunkResult` and `EntityResult` field sets. Read whatever wraps these for tool consumption (likely `AgentRetrievalClientAdapter` at `apps/cli/_cross_context.py`) to confirm what the tool currently returns to the executor. The brief assumes the structured result objects are available to the adapter but get formatted to a string for LLM consumption via `_format_chunks_as_tool_result` or equivalent. If the adapter is structured already, the citation-candidate population is simpler. If the adapter discards structure, the adapter extends to preserve structure alongside the formatted string.
2. **ToolInvoker port surface.** Read `contexts/agent/application/ports/tool_invoker.py` (or wherever the S28b two-thin-ports landed) to confirm its return shape. The accumulator needs to thread citation candidates from the tool invocation result through to `ToolCallCompleted`. If the port returns a string, it extends to return a structured envelope. If the port returns structured already, the field for citation candidates is the extension point.
3. **AgentLoopExecutor's current ToolCallCompleted emission.** Read `contexts/agent/adapters/outbound/agent_loop_executor.py` at the tool-call yield site. The executor needs to populate the new `citation_candidates` field from the tool invocation result. Reconcile against the actual code path that handles tool results.
4. **Agent role allowlist with retrieval enabled.** Read migration `0008_agent_tables` and `0010_role_tool_allowlist_pin` (and any subsequent role-seeding migrations) to confirm which seeded roles carry retrieval in their `tool_allowlist`. The smoke at commit 9 needs an agent that exercises retrieval. If no seeded role enables retrieval, the smoke creates a new role with retrieval in its allowlist via the existing CLI commands, then creates an agent from that role. If a seeded role exists, use it. Either approach is acceptable; the reconciliation finding settles which.
5. **Migration revision number.** Read `alembic/tenant/versions/` to confirm the next revision number. Brief anticipates `0012_*`. The brief explicitly names this as a likely mechanical absorption.
6. **Existing citation-table state.** Confirm `run_chunk_citations` and `run_entity_citations` are empty across both tenants (S31 smoke verified this at S31 close; reconfirm at S32 session-open). The schema revision is structural-only because no rows exist; if rows have somehow appeared between S31 close and S32 session-open, the migration shape changes.

Resolve any mismatch as a user question with the resolution recorded in D96's Alternatives-considered section or in the relevant code module docstring.

## Charter updates (commit 1)

`docs(p9/s32): land D96 citation candidate types, event extension, single-transaction seam, schema revision; preserve S32 brief; transition current-package.md to S32 in flight`

This commit lands before any code commit.

- `charter/decisions.md`: append D96 per Appendix A.
- `charter/schema.md`: update `run_chunk_citations` and `run_entity_citations` sections per Appendix C.
- `charter/packages/p9-epic.md`: replace the S32 line per Appendix B.
- `charter/current-package.md`: transition "S31 closed; S32 next" to "S32 in flight" with one paragraph framing S32's scope (citation candidate domain types at agent context, ToolCallCompleted event extension, wiring adapter translation, executor and accumulator extension, single-transaction multi-table write, source_citation and entity_display_label schema revisions). Keep all four Carryovers-active bullets and the Deferred-items-remaining-visible block byte-for-byte.
- `briefs/p9/s32.md`: preserve this brief verbatim. Any pre-write reconciliation outcomes that change the brief's shape land as an Appendix to the preserved brief, not as in-place edits.

## Substantive work

### Commit 2. Citation candidate domain types at agent context

`feat(p9/s32): citation candidate domain types and discriminated union`

- Create `contexts/agent/domain/citation_candidates.py`.
- `ChunkCitationCandidate` frozen dataclass with fields: `chunk_id: UUID`, `source_id: UUID`, `chunk_index: int`, `content_snapshot: str`, `source_snapshot: Mapping[str, Any]`, `tenant_id: str`, `jurisdiction: str`. `__post_init__` validates: tenant_id non-empty, jurisdiction non-empty, chunk_index >= 0, content_snapshot non-empty.
- `EntityCitationCandidate` frozen dataclass with fields: `entity_tenant_id: str`, `entity_name: str`, `entity_type: str`, `source_chunk_ids: tuple[UUID, ...]`, `tenant_id: str`, `jurisdiction: str`. `__post_init__` validates: entity_tenant_id non-empty, entity_name non-empty, entity_type non-empty, tenant_id non-empty, jurisdiction non-empty, entity_tenant_id matches tenant_id.
- `CitationCandidate = ChunkCitationCandidate | EntityCitationCandidate` as the discriminated union.
- Unit tests fence every invariant (chunk: 4 invariants × accept/reject = 8 tests minimum; entity: 6 invariants × accept/reject = 12 tests minimum; union: 2 type-discrimination tests). Aim for 20-25 tests.

### Commit 3. ToolCallCompleted carries citation candidates

`feat(p9/s32): ToolCallCompleted gains citation_candidates field`

- Extend `ToolCallCompleted` at `contexts/agent/domain/events.py` with `citation_candidates: tuple[CitationCandidate, ...] = ()`. Default empty preserves backwards compatibility for tool calls that produce no citations.
- The field is the canonical surface citations ride on. The accumulator at `invoke_agent` reads from this field.
- Unit tests verify the field defaults to empty, accepts mixed chunk and entity candidates, and rejects non-CitationCandidate types via type system.

### Commit 4. Retrieval adapter translates ChunkResult and EntityResult to citation candidates

`feat(p9/s32): AgentRetrievalClientAdapter surfaces citation candidates alongside formatted tool result`

- Extend `AgentRetrievalClientAdapter` at `apps/cli/_cross_context.py` (or wherever the reconciliation step 1 finding locates it) to translate ingestion-context `ChunkResult` and `EntityResult` into agent-context `ChunkCitationCandidate` and `EntityCitationCandidate` types.
- The translation populates `source_snapshot` from whatever source-level metadata is available at retrieval time. At Phase 1 this is `{"file_name": <str>, "file_type": <str>}` from joining chunks to sources. Future ingestion enrichment fills more without code change.
- The tool-result surface the adapter returns to the executor extends to carry citation candidates alongside the existing formatted string. Reconciliation step 1 settles whether this is a new field on an existing return type or a new return type. The brief's recommendation: a structured tool result envelope with `formatted_text: str` and `citation_candidates: tuple[CitationCandidate, ...]`. The executor reads the formatted text for the LLM message stream and the candidates for the ToolCallCompleted event yield.
- Unit tests for the translation: ChunkResult input → ChunkCitationCandidate output with fields populated; EntityResult input → EntityCitationCandidate output with source_chunk_ids preserved.

### Commit 5. Executor populates citation_candidates on ToolCallCompleted

`feat(p9/s32): AgentLoopExecutor threads citation candidates from tool result to ToolCallCompleted event`

- Modify `AgentLoopExecutor` at `contexts/agent/adapters/outbound/agent_loop_executor.py` to read citation candidates from the tool-result envelope and populate them on the `ToolCallCompleted` event at the tool-call yield site.
- Tool calls that produce no citation candidates (any tool other than retrieval, or retrieval with empty results) yield ToolCallCompleted with `citation_candidates=()`.
- Unit tests verify: retrieval tool with chunk results produces ToolCallCompleted with non-empty chunk citation candidates; retrieval tool with entity results produces ToolCallCompleted with non-empty entity citation candidates; mixed results produce both; empty results produce empty tuple; non-retrieval tool calls (hypothetical, since retrieval is the only Phase 1 tool) produce empty tuple.

### Commit 6. invoke_agent accumulator absorbs citation candidates

`feat(p9/s32): invoke_agent accumulator threads citation candidates through to record_run`

- Extend the accumulator at `invoke_agent` in `contexts/agent/application/use_cases.py` to absorb citation candidates from each `ToolCallCompleted` event across the run.
- Two accumulator fields added: `chunk_citation_accumulator: list[ChunkCitationCandidate]` and `entity_citation_accumulator: list[EntityCitationCandidate]`. Each `ToolCallCompleted` extends them.
- Deduplication strategy at accumulation: a chunk is uniquely identified by `(chunk_id, run_id)` for the run; entities by `(entity_tenant_id, entity_name, entity_type, run_id)`. The accumulator deduplicates so a chunk retrieved by two separate retrieval calls within the same run produces one row, not two. The deduplication discards the second snapshot; the first-seen snapshot wins.
- At terminal event (post-yield, before writer call), the accumulator passes the deduplicated citation lists to `writer.record_run` alongside the run record. The `AgentRunRecord` DTO extends with the two citation tuples.
- Unit tests verify: single retrieval call with three chunks produces three citation candidates passed to writer; same chunk retrieved twice produces one citation candidate; chunks and entities together pass through cleanly; no retrieval call produces empty citation lists.

### Commit 7. Schema revision migration

`feat(p9/s32): alembic 0012 revise citation snapshot columns`

- New revision `0012_revise_citation_snapshots` (or whichever number reconciliation step 5 confirms) on `alembic/tenant/` track with `down_revision = "0011_create_run_history"`.
- Upgrade:
  - On `run_chunk_citations`: `ALTER TABLE DROP COLUMN source_citation`; `ALTER TABLE ADD COLUMN source_snapshot jsonb NOT NULL DEFAULT '{}'::jsonb`.
  - On `run_entity_citations`: `ALTER TABLE DROP COLUMN entity_display_label`; `ALTER TABLE ADD COLUMN source_chunk_ids text[] NOT NULL DEFAULT '{}'::text[]`.
- Downgrade reverses both. Down-migration reads as adding the dropped columns back with their original constraints and dropping the new ones.
- Migration applies to both `tenant_a` and `tenant_b` via `make migrate`.
- Tests: schema-level CHECK that the columns exist with the expected types after `make migrate`; that both tenants carry the revised schema.

### Commit 8. RunRecord and record_run use case extend; single-transaction multi-table write

`feat(p9/s32): RunRecord carries citations; record_run writes runs + citations in single transaction`

- Extend `RunRecord` at `contexts/run_history/domain/run_record.py` with `chunk_citations: tuple[ChunkCitationRecord, ...]` and `entity_citations: tuple[EntityCitationRecord, ...]`. The `ChunkCitationRecord` and `EntityCitationRecord` are run-history-context-owned types that mirror the agent-context candidate types one-for-one; the use case at `record_run` is the structural authority on the mapping per the D54 pattern.
- Extend `record_run` use case at `contexts/run_history/application/use_cases.py` to accept the citation lists alongside the run record and pass them to the repository's write method.
- Extend `PostgresRunHistoryAdapter` at `contexts/run_history/adapters/outbound/repository.py`: the `async with session.begin()` block grows to insert the runs row, then the chunk_citations rows (if any), then the entity_citations rows (if any), all within the same transaction. Any insert failure raises and rolls the whole transaction back.
- The `RunHistoryWriter` consumer port at the agent context extends to accept the citation tuples on its `record_run` method (the agent-side DTO `AgentRunRecord` carries them; the wiring adapter at `apps/cli/_cross_context.py` translates).
- Unit tests verify: successful three-table write commits; chunk-citation insert failure rolls back runs and any prior citation inserts; entity-citation insert failure rolls back the same way; empty citation lists produce no citation rows but the runs row commits cleanly.
- Integration tests against live per-tenant Postgres: run record with 3 chunk citations + 2 entity citations produces 1 + 3 + 2 = 6 rows in single transaction; row counts verified by separate queries post-write.

### Commit 9. Tenant-isolation contract harness extension

`feat(p9/s32): tenant-isolation harness covers citation rows`

- Extend `tests/contract/tenant_isolation/test_run_history_isolation.py` with scenarios for the two citation tables.
- New scenarios at minimum: cross-tenant write of `run_chunk_citations` rows through `PostgresRunHistoryAdapter` raises; cross-tenant write of `run_entity_citations` rows raises; the bound-tenant-id pre-routing defence fires on both tables; schema-level checks confirm citation tables exist on tenant DBs and not on the control plane.
- AST enforcement: any module under `contexts/run_history/adapters/` referencing the new column names must have isolation coverage per the D24 rule.

### Commit 10. Live-stack smoke against tenant_a with retrieval-enabled agent

`test(p9/s32): live-stack smoke writes citation rows through invoke_agent against tenant_a`

- Inside the container, set up an agent with retrieval in its tool_allowlist. The reconciliation step 4 finding determines whether this is a seeded role to reuse or a new role to create via CLI. The brief's recommendation if creation is needed: `padhanam role create` (or whichever CLI surface S26b/S28b landed) with a config carrying retrieval in the allowlist; `padhanam agent create-from-methodology` against an existing methodology that pairs with the role; ingest a small source corpus via `padhanam ingest run` and `padhanam ingest worker --max-iterations 5`.
- Invoke the agent against the live SSE endpoint with input that triggers retrieval (e.g., a question whose answer requires the ingested corpus).
- Verify via `docker compose exec postgres-tenant-a psql -U padhanam -d padhanam -c "SELECT count(*) FROM run_chunk_citations WHERE run_id = (SELECT id FROM runs ORDER BY created_at DESC LIMIT 1);"`: assert at least one row.
- Verify the corresponding `runs` row exists with hashes matching the SSE stream's terminal-event audit_chain_hashes.
- Verify a `source_snapshot` JSONB column on at least one citation row contains the expected source-level fields (`file_name`, `file_type`).
- This commit is a markdown record of the smoke verification at `docs/smoke/p9_s32_smoke.md` (one paragraph plus the captured SQL output and the agent invocation command), not a test file.

### Commit 11. Session log entry and current-package transition

`docs(p9/s32): session log entry; transition current-package.md to S33 next`

- Append `## P9 — S32 — citation linking, snapshot population, single-transaction completion seam, citation schema revision` entry to `log/sessions.md` per the established format. `roles:` (architect, engineer). `mode: build`.
- Reflection prompts addressed in substantive paragraphs per the four prompts below.
- Edit `charter/current-package.md`: transition "S32 in flight" to "S32 closed; S33 next" with one paragraph framing S32's contribution (citation surface end-to-end from retrieval through to per-tenant Postgres rows; single-transaction multi-table write; structured source snapshot anticipating Phase 2 ingestion enrichment) and brief framing for S33 (UX-shaped query port and Postgres adapter implementing it).

## Acceptance criteria

1. D96 appended to `charter/decisions.md` per Appendix A. The Choice text names the citation candidate types, the ToolCallCompleted extension, the schema revisions (source_citation → source_snapshot; entity_display_label → source_chunk_ids), the single-transaction multi-table write commitment, and the within-run deduplication policy.
2. `charter/schema.md` updated per Appendix C for both citation tables.
3. `charter/packages/p9-epic.md` S32 line replaced per Appendix B with as-landed scope.
4. `charter/current-package.md` transitioned twice: "S31 closed; S32 next" → "S32 in flight" at commit 1; "S32 in flight" → "S32 closed; S33 next" at commit 11.
5. `briefs/p9/s32.md` preserved per convention with any reconciliation findings appended.
6. `ChunkCitationCandidate` and `EntityCitationCandidate` value objects exist at `contexts/agent/domain/citation_candidates.py` with the field sets and invariants from commit 2; unit-test coverage fences every invariant.
7. `ToolCallCompleted` carries `citation_candidates: tuple[CitationCandidate, ...] = ()`. Existing tests pass without modification (default empty); new tests cover the populated path.
8. `AgentRetrievalClientAdapter` translates ingestion `ChunkResult` and `EntityResult` to agent-context citation candidate types. Unit tests cover the translation.
9. `AgentLoopExecutor` populates `citation_candidates` on `ToolCallCompleted` events. Unit tests cover the populated path for chunk-only, entity-only, mixed, and empty cases.
10. `invoke_agent` accumulator deduplicates citation candidates by `(chunk_id, run_id)` and `(entity_tenant_id, entity_name, entity_type, run_id)` and passes them to `writer.record_run`. Unit tests cover the deduplication path.
11. Alembic revision `0012_revise_citation_snapshots` applies cleanly to both tenants. Schema check confirms `source_citation` is gone and `source_snapshot jsonb` exists on `run_chunk_citations`; `entity_display_label` is gone and `source_chunk_ids text[]` exists on `run_entity_citations`.
12. `RunRecord` carries citation tuples. `record_run` use case writes runs + chunk citations + entity citations within `async with session.begin()`. Partial-failure rollback verified by integration tests.
13. Tenant-isolation contract harness covers citation-row cross-tenant write isolation. All cross-tenant write scenarios fail.
14. Live-stack smoke produces one runs row plus at least one citation row on tenant_a, with `source_snapshot` JSONB populated. The smoke document records the agent invocation command and the SQL verification output.
15. Session log entry appended. The four reflection prompts answered in substantive paragraphs.
16. `make lint` passes. All import-linter contracts kept. AST enforcement passes.
17. `uv run pytest` passes with no regressions. Test-count delta documented in session log entry.

## Reflection prompts addressed in session log entry

1. **Pre-write reconciliation, ninth instance.** Compare the six reconciliation surfaces against findings. Which absorbed without question, which forced a user question, which surfaced after the work began. Compare find-rate distribution against S31's "three structural-honesty plus three mechanical plus two post-work" baseline. S32's surfaces are smaller than S31's (no new bounded context, no new migration shape) so a smaller find-rate is expected and itself is methodology signal worth recording.

2. **Citation as defined versus citation as built.** Compare the storage-versus-render distinction the strategic conversation committed to against what the build actually shipped. Did the source_snapshot JSONB shape carry the load it was designed to carry, or did build evidence push back on the structured-versus-text decision. Did the within-run deduplication policy work cleanly or surface edge cases. Did the chunk-level granularity hold for the smoke's retrieval case, or did the substantive-data-point case (the structured extraction case the strategic conversation deferred) press against chunk-level boundaries.

3. **Single-transaction multi-table write at first product-form use.** The seam landed cleanly at S31's runs-only path. S32 grows it to three tables. Compare the implementation against the brief's commit 8 expectations. Did the partial-failure rollback semantics surface implementation surprises (the typical seam-grows-and-rolls-back-cleanly case is the cheap path; the seam-grows-and-something-leaks case is the methodology observation). The seam at three tables is the precedent every later run-history-related write inherits; surface any divergence from the brief's recommended shape and the reasoning.

4. **What S32 leaves for S33.** S33 lands the UX-shaped query port and the Postgres adapter implementing it. The substantive question for S33 framing is the shape of the read surface: get_run returns the run record alone or with citations attached; list_runs_with_filters supports which filter dimensions; get_citations_for_run is a separate method or folds into get_run. S32 should record any anticipation of S33's read-shape that emerged at build (e.g., did the citation-row structure raise questions about how the read surface should aggregate them; did the deduplication policy at write time anticipate or foreclose any read-side aggregation).

## Out of scope

- The UX-shaped query port and the read-side adapter. Lands at S33.
- The HTTP API for ingestion management absorbed from the P6 deferred carryover. Lands at S34.
- Sub-chunk precision for structured-extraction tools. Deferred until extraction-style tools enter the picture per the strategic conversation.
- Discard-explanation layer. Dropped entirely per the strategic conversation.
- Chunk-size revision at the ingestion layer. Flagged as a Phase 2 candidate; not S32 scope.
- NotebookLM-style ingestion with richer source metadata. Phase 2 work; the source_snapshot JSONB schema accommodates it additively when it lands.
- Methodology-as-product UX rendering of citations (Harvard, footnote, hover card). Phase 2 work; the storage substrate this brief commits supports any render shape.

## Session log entry instruction

Append a session log entry to `log/sessions.md` with the standard format. Include the metrics block at session close with `close_state: clean` or `close_state: with_caveats` depending on what surfaces. Record any methodology observations under a `methodology:` heading. The four reflection prompts above are addressed in substantive paragraphs in the entry's reflection section.

## Appendix A. D96 text

```
## D96: Citation surface — agent-context-owned candidate types, ToolCallCompleted event extension, single-transaction multi-table write, source_snapshot JSONB schema revision, within-run deduplication (Package P9, S32, 2026-05-13)

**Choice.** Citation linking from agent runtime to the per-tenant run-history substrate happens at four committed shapes. First, the agent context defines two citation candidate value objects at `contexts/agent/domain/citation_candidates.py`: `ChunkCitationCandidate` (chunk_id, source_id, chunk_index, content_snapshot, source_snapshot, tenant_id, jurisdiction) and `EntityCitationCandidate` (entity_tenant_id, entity_name, entity_type, source_chunk_ids, tenant_id, jurisdiction). A discriminated union `CitationCandidate` over the two is the field type the event carries. Second, `ToolCallCompleted` gains `citation_candidates: tuple[CitationCandidate, ...] = ()`. Default empty preserves backwards compatibility; tools producing attributable output fill the field. The retrieval tool is the only Phase 1 producer; future tools (web fetch, document parse, extraction) extend the same field. Third, the schema at `run_chunk_citations` revises from `source_citation text` to `source_snapshot jsonb` to keep render shape (Harvard, footnote, hover card, et al.) as a Phase 2 read-time concern over a structured input snapshot. At Phase 1 the snapshot carries `{"file_name": <str>, "file_type": <str>}`; richer ingestion enrichment fills more fields without schema change. The schema at `run_entity_citations` drops `entity_display_label text` (synthesised at render time from name + type) and adds `source_chunk_ids text[]` to snapshot the provenance trail from the Neo4j entity back to per-tenant Postgres chunks; the audit-evidence-fidelity argument from D94 holds for entity provenance the same way it holds for chunk content. Fourth, `PostgresRunHistoryAdapter.record_run` grows its `async with session.begin()` block to write runs + chunk citations + entity citations within a single transaction; partial failure rolls all three back. The accumulator at `invoke_agent` deduplicates citation candidates within the run by `(chunk_id, run_id)` and `(entity_tenant_id, entity_name, entity_type, run_id)`; first-seen snapshot wins.

**Reasoning.** [Full reasoning lands at commit time; the strategic conversation's positions are: storage-versus-render separation per the citation definition; audit-evidence-fidelity over render-commitment-at-storage; chunk-level granularity sufficient at Phase 1 with sub-chunk precision deferred until extraction tools enter; discard-explanation layer not built; structural JSONB on source_snapshot anticipates NotebookLM-style ingestion without bake-in; within-run deduplication keeps citation density meaningful without forcing the use-versus-discard layer at Phase 1.]

**Alternatives considered.** [To be filled at commit 1 against the strategic conversation. The list at minimum includes: keep `source_citation text` and commit a render format now (rejected per the storage-versus-render argument); leave `entity_display_label text` and synthesise at write rather than read (rejected per the same argument); discriminated single candidate type with a kind field (rejected per per-type-invariants clarity argument); record only what the LLM uses rather than everything retrieved (rejected per the LLM-does-not-emit-discard-signal honesty argument and the audit-floor commitment); discard-explanation layer with a second model pass (rejected per research-territory and Phase 1 scope arguments); sub-chunk precision now (rejected per no-Phase-1-tool-produces-this-case argument).]

**Kano.** Must-have on the candidate types and the event extension and the schema revisions; these are the substrate Phase 2 UX reads and P11 optimisation joins to. Performance on the within-run deduplication policy (the differentiator for citation surface usability; consumer evidence at Phase 2 refines the policy). Delighter on the structured JSONB source_snapshot's anticipation of NotebookLM-style ingestion enrichment.
```

## Appendix B. `charter/packages/p9-epic.md` S32 line replacement and `current-package.md` transition

Replace the existing S32 indicative line with: "**S32:** Citation surface lands end-to-end from retrieval through to per-tenant Postgres rows. `ChunkCitationCandidate` and `EntityCitationCandidate` value objects at `contexts/agent/domain/citation_candidates.py` carry chunk identity, content snapshot, structured source-level attribution snapshot, and the Neo4j composite key with `source_chunk_ids` provenance. `ToolCallCompleted` extends with `citation_candidates: tuple[CitationCandidate, ...] = ()`. The retrieval adapter at `apps/cli/_cross_context.py` translates ingestion `ChunkResult` and `EntityResult` to agent-context candidates. The accumulator at `invoke_agent` deduplicates within the run and passes citation lists to `writer.record_run`. Schema revision `0012_revise_citation_snapshots` drops `source_citation text` for `source_snapshot jsonb` and drops `entity_display_label text` for `source_chunk_ids text[]`. The `PostgresRunHistoryAdapter` writes all three tables within `async with session.begin()`; partial failure rolls all three back. Tenant-isolation contract harness extends for citation rows. Live-stack smoke verifies one runs row plus at least one citation row on `tenant_a` with `source_snapshot` populated."

## Appendix C. `charter/schema.md` updates

For `run_chunk_citations`: remove the `source_citation` row from the column table; add a `source_snapshot` row with type `jsonb`, constraint `not null; default '{}'::jsonb; structured snapshot of source-level metadata available at retrieval time (Phase 1 carries file_name and file_type; grows additively with ingestion enrichment per D96)`.

For `run_entity_citations`: remove the `entity_display_label` row from the column table; add a `source_chunk_ids` row with type `text[]`, constraint `not null; default '{}'::text[]; snapshot of the Neo4j entity's source_chunk_ids array preserving provenance back to per-tenant Postgres chunks per D96`.

Update the prose under both tables to reference D96 rather than the placeholder "population semantics at S32" language.

---

## Appendix D. Pre-write reconciliation outcomes (S32 session-open, 2026-05-13)

Six surfaces verified at session-open against current code.

**1. Retrieval tool return shape.** `ChunkResult` (ingestion domain at `contexts/ingestion/domain/chunk_result.py`) carries `chunk_id`, `source_id`, `tenant_id`, `jurisdiction`, `content`, `structural_metadata`, `similarity_score`, `created_at` — does NOT carry `chunk_index` or source-level fields (`file_name`, `file_type`). The pgvector SQL at `pgvector_search.py` joins `sources` but does not SELECT `s.file_name` / `s.file_type` / `c.chunk_index`. The `AgentRetrievalClient` consumer port at the agent context projects `ChunkResult` to `RetrievedChunk` (three fields: text, source_id, score) — the brief's "adapter discards structure" case applies. Resolution: extend `ChunkResult` with `chunk_index: int` and `source_snapshot: Mapping[str, Any]`; extend the SQL to project the additional columns; extend the `AgentRetrievalClient` port shape from `tuple[RetrievedChunk, ...]` to a `RetrievalResult` envelope carrying both `chunks` (the LLM-facing projection) and `citation_candidates` (the citation surface) so the same adapter (`AgentRetrievalClientAdapter`) produces both in a single pass. The structural finding is fully aligned with the brief's intent ("from joining chunks to sources"); absorbed without user-question per the framing-prompt-as-recommendation pattern.

**2. ToolInvoker port surface.** `ToolInvocationResult` at `contexts/agent/application/ports/tool_invoker.py` is already structured (carries `outcome`, `payload`, `message`, `invariant_index`). Resolution: add `citation_candidates: tuple[CitationCandidate, ...] = ()` to it; the wiring adapter populates the field from the retrieval result envelope.

**3. AgentLoopExecutor's ToolCallCompleted emission.** Yield site at `agent_loop_executor.py:386-393`. Resolution: populate the new `citation_candidates` field from `result.citation_candidates`. Mechanical absorption.

**4. Agent role allowlist with retrieval.** None of the eight seeded roles (LVTGuide, ProblemFramer, Disaggregator, Planner, Synthesiser, Prioritiser, Analyst, Communicator) carry retrieval in their allowlist; all have `tool_allowlist = []`. Resolution: smoke at commit 10 creates a new role with retrieval in allowlist via `padhanam role create` (or updates an existing one via `padhanam role update`).

**5. Migration revision number.** Latest is `0011_create_run_history`; next is `0012_revise_citation_snapshots`. Confirmed; absorbed without question.

**6. Existing citation-table state.** Both citation tables empty (0 rows on both tenants). The structural-only migration shape from the brief is correct. `runs` carries 1 row on tenant_a from S31's smoke; no citation rows to migrate.

Find-rate distribution at S32: one structural-honesty finding (surface 1 — `ChunkResult` extension shape), four mechanical absorptions (surfaces 2, 3, 5, 6), one operational reconciliation (surface 4 — no seeded role enables retrieval). Smaller than S31's "three structural-honesty plus three mechanical plus two post-work" by design — S32's surfaces are extensions of S31's substrate rather than new architectural primitives. Pre-write reconciliation continues to do load-bearing work; the find-rate compression is itself a methodology observation (the reconciliation pattern produces fewer findings as the substrate stabilises around the bounded context's commitments).
