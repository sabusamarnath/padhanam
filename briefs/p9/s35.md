# Brief: P9 — S35 — P9 close via end-to-end demonstration, test_agent_sse_endpoint carryover fix, deferred-decisions entries, package archive

**mode:** build

## Goal

At S35 close the following artefacts exist:

- End-to-end demonstration executed against tenant_a exercising the full P9 path: agent invocation through the SSE endpoint at `apps/api/routers/agent.py`; run completion writing record plus citations to the per-tenant Postgres tables via the S31/S32 substrate; HTTP routes from S34 retrieving the rendered surface; audit chain hashes on the run record matching the audit chain entries byte-for-byte. The path executes with no manual intervention beyond the trigger.
- `docs/smoke/p9_s35_e2e_demo.md` records the demonstration: framing paragraph naming what is being demonstrated and why it closes P9; the trigger command; the captured SSE event stream; the resulting runs row plus chunk_citation rows plus entity_citation rows queried via `psql`; the audit chain entries with hash-equality verification against the runs row; the HTTP responses from `GET /runs/{id}` and `GET /runs` rendering the same data through the read surface. Markdown record, not a test file.
- `test_agent_sse_endpoint.py` `run_history_writer` missing-field carryover from S31 resolved. The tests pass without regression elsewhere. The fix lands at the actual call site (likely a fixture or test setup that constructs the writer with an outdated field set) rather than working around the failure in the test bodies.
- `charter/deferred-decisions.md` extended with two entries: integration-test slowness disposition citing `docs/notes/integration-test-slowness-investigation.md`, activation trigger named, candidate landing site Phase 1 close substrate-completion or P10 framing; ingestion-management HTTP API disposition with activation trigger "a UI consumer that needs HTTP-driven ingestion management," candidate landing site early Phase 2 substrate-completion.
- `docs/notes/integration-test-slowness-investigation.md` committed to the repository so the deferred-decisions entry has a stable reference target. The note's content is preserved verbatim from the investigation; no edits.
- `charter/packages/p9-epic.md` retrospective addendum: a final section recording what P9 actually shipped (the five sessions S31 through S35) versus what the epic forecast at framing (four-to-five sessions, ingestion-management API inside P9). The delta is principled per the living-artefact discipline and documented for the Phase 1 close audit.
- `log/packages.md` extended with the P9 retrospective entry per the P8 precedent: sessions shipped, decisions landed (D94 through D98), substrate produced, methodology observations promoted (pre-write reconciliation tenth-plus reinforcement, consumer-port-plus-wiring-adapter eighth-plus reinforcement, mirror-types-at-context-boundaries second-plus reinforcement), what carries forward.
- `charter/packages/p9-epic.md` archived to `docs/archive/packages/p9.md` per the P8 archive precedent at S30b close.
- `charter/current-package.md` transitioned twice: "S34 closed; S35 next" → "S35 in flight (P9 close)" at commit 1; "S35 in flight" → "P9 closed; P10 framing next" or equivalent transition state at the closing commit. The final state surfaces the active package as "between packages" pending P10 framing rather than committing to P10 substance at S35 close.
- `briefs/p9/s35.md` preserved with reconciliation findings appended as Appendix D.

No new D-entries at S35 expected. All P9 substrate decisions landed at D94 through D98. If a structural surface forces a new commitment during the demo path (audit-chain-linkage verification revealing a gap, for instance), the session-open user-question protocol applies and D99 lands at commit 1.

## Context to read first, in order

1. `charter/bet.md` for the substrate-completion-as-bet-proof framing.
2. `charter/principles.md` for the living-artefact discipline (PRD-shaped documentation absorbs delta capture as audit deliverable), the audit-logging principle (state changes with hash chaining for tamper-evidence), and the reflection-density principle.
3. `charter/decisions.md` D92 through D98 (the full P9 chain).
4. `charter/packages/p9-epic.md` for the P9 framing and the as-built session forecast.
5. `charter/current-package.md` for the S34 close framing.
6. `charter/deferred-decisions.md` for the existing entry shape and the activation-trigger convention.
7. `log/sessions.md` S31 through S34 entries.
8. `log/packages.md` for the P8 retrospective entry shape (the P9 entry follows the same pattern).
9. `docs/archive/packages/p8.md` for the package-archive shape (the P9 archive follows the same pattern).
10. `docs/notes/integration-test-slowness-investigation.md` for the investigation findings the deferred-decisions entry references.
11. `apps/api/routers/agent.py` and `apps/api/routers/run_history.py` for the SSE plus HTTP surfaces the demo exercises.
12. `tests/integration/contexts/agent/test_agent_sse_endpoint.py` for the carryover failure shape and root cause.
13. `contexts/run_history/adapters/outbound/postgres/repository.py` and `contexts/run_history/adapters/outbound/postgres/reader.py` for the write plus read paths the demo exercises through.

## Pre-write reconciliation

Eight surfaces to verify against current code before writing. Resolve each before committing.

1. **`test_agent_sse_endpoint.py` actual failure mode.** Run the file in isolation. Read the failure traceback. The carryover description says "run_history_writer missing-field carryover." Verify the actual missing field, the call site that constructs the writer, and whether the fix is a test-fixture update, a writer-construction update, or a domain-record update. The fix shape changes what commits land and in what order.

2. **Agent templates plus role plus tool allowlist state on tenant_a.** Verify whether tenant_a's seeded data includes an agent template with a non-empty tool_allowlist that includes retrieval. Per the P8→P9 carryover at `charter/current-package.md`, all migration-seeded roles ship with empty tool_allowlist. The demo needs at least one role-plus-template combination that exercises retrieval end-to-end so the citations are non-empty. If the seeded state does not support the demo, surface the gap at session-open and decide between (a) seeding additional data ahead of the demo, (b) running the demo without retrieval citations and noting the limitation, (c) constructing the demo against a Phase-2-preview override that injects retrieval into the allowlist for the demo's invocation only. Recommendation: option (a) if the seeding is light, otherwise (c).

3. **Tenant registry state.** Verify the registry contains tenant_a with the post-S32 schema. The recurring registry-wipe class (S30b/S31 carryover) may have fired between S34 close and S35 open; if it has, the demo's first step is registry recovery. Document the recovery sequence in the smoke if it fires.

4. **Image pin state at compose.yaml.** Verify the current pinned digest at `compose.yaml` matches the S34 close image. If S34's hygiene work left any uncommitted image, the demo runs against the wrong image. Pin to the right digest before the demo.

5. **Langfuse trace correlation.** Per D27, the trace store is operational observability with the run record's `trace_id` column as the join key. The demo verification should optionally include a Langfuse trace lookup for the run to demonstrate the cross-store correlation. Verify Langfuse is healthy via the standard health probe pattern before the demo; if it is not, the demo proceeds without the Langfuse step and the note records the omission.

6. **Existing `docs/archive/packages/` directory state.** Verify the directory exists and contains `p8.md` per the P8 close precedent. If the archive convention has drifted since P8, surface it as a session-open question before commit 4.

7. **`log/packages.md` shape.** Read the P8 entry in full. The S35 P9 entry mirrors the structure: package goal, sessions shipped, decisions landed, substrate produced, methodology observations, what carries forward, measured outcomes.

8. **`charter/deferred-decisions.md` current shape.** Read the file. The S35 entries follow the existing convention: heading naming the deferred item; activation trigger naming what fires the work; candidate landing site naming where the work lands; one or two paragraphs of context. If the existing convention has evolved since the entries last landed, mirror the current shape.

If any surface reveals structural drift, surface as session-open question before commit 2. Mechanical adjustments absorb without question.

## Charter updates ahead of code

Commit 1 lands all charter touchpoints in a single commit ahead of code per the established discipline. No D-entry expected; if one surfaces from pre-write reconciliation, it lands here as D99.

**Commit message:** `docs(p9/s35): P9 close framing; deferred-decisions entries; current-package transition`

Files updated:
1. `charter/current-package.md` — transition from "S34 closed; S35 next" to "S35 in flight (P9 close)" with one framing paragraph naming the demo path and the closing materials.
2. `charter/deferred-decisions.md` — two new entries per Appendix A: integration-test slowness; ingestion-management HTTP API.
3. `docs/notes/integration-test-slowness-investigation.md` — commit the investigation note (it currently exists in the working tree uncommitted per the investigation brief).
4. `briefs/p9/s35.md` — preserve this brief.

## Commits

### Commit 1. Charter updates and investigation-note commit

`docs(p9/s35): P9 close framing; deferred-decisions entries; current-package transition`

Per the charter-updates-ahead-of-code section above.

### Commit 2. test_agent_sse_endpoint.py carryover fix

`fix(p9/s35): resolve run_history_writer missing-field carryover in test_agent_sse_endpoint`

- Apply the fix at the call site identified at reconciliation surface 1.
- Run `tests/integration/contexts/agent/test_agent_sse_endpoint.py` in isolation; verify it passes.
- Run the broader unit suite to confirm no regression: `uv run pytest tests/unit/`.
- If the fix touches more than the test file (writer construction, domain record), update the touched files with care and verify their unit tests still pass.

### Commit 3. End-to-end demo execution and smoke document

`test(p9/s35): end-to-end demo against tenant_a closing P9`

- Construct the demo trigger: a single command (curl, httpx, or `padhanam agent run`) that invokes the SSE endpoint against tenant_a with an agent configured to exercise retrieval. Per reconciliation surface 2, the agent template plus role plus tool_allowlist must permit retrieval so citations are non-empty.
- Execute the trigger. Capture the SSE event stream (the eleven event types from D90's vocabulary).
- After terminal event yield, query Postgres on tenant_a for: the runs row matching the invocation_id; chunk_citation rows linked to the run_id; entity_citation rows linked to the run_id.
- Verify audit chain integrity: the audit_start_hash and audit_end_hash on the runs row match the hash chain stored in `tenant_audit`. The verification is hash equality between the runs row's columns and the audit entries' corresponding columns.
- Hit `GET /runs/{run_id}` against the new HTTP routes. Verify the response renders the run plus its citations. Hit `GET /runs?termination_reason=content&page_size=10` to verify the list view returns the run.
- Optionally (per reconciliation surface 5): query Langfuse for the trace_id to demonstrate cross-store correlation.
- Markdown record at `docs/smoke/p9_s35_e2e_demo.md`: framing paragraph naming what is being demonstrated and why it closes P9; the trigger command; the captured SSE event stream (compressed if long); the Postgres queries and their outputs; the audit-chain verification with hash-equality assertion; the HTTP responses; the optional Langfuse trace correlation if exercised.
- This commit is a markdown record. The demo executes by following the document; the verification is whether the document records a successful execution end-to-end.

### Commit 4. P9 close retrospective and archive

`docs(p9/s35): P9 retrospective; archive p9-epic; measured outcomes`

- Append a retrospective addendum to `charter/packages/p9-epic.md`: a final section before the existing content recording what P9 shipped (five sessions S31 through S35; D94 through D98; substrate plus HTTP plus end-to-end demo) versus what the epic forecast (four-to-five sessions, ingestion-management API inside P9). Name the delta on ingestion-management API and reference the deferred-decisions entry.
- Append the P9 entry to `log/packages.md` per the P8 precedent: package goal restated; sessions shipped with one-line each; decisions landed (D94 through D98) with one-line each; substrate produced; methodology observations (the methodology promotion candidates this package generated); what carries forward to subsequent packages and what is captured at deferred-decisions; measured outcomes from the demo (timing, citation counts, audit-chain integrity).
- Archive `charter/packages/p9-epic.md` (with the retrospective addendum) to `docs/archive/packages/p9.md`. The charter file gets a stub at its original path linking to the archive, or the file itself moves and is replaced with a stub, per the P8 archive convention verified at reconciliation surface 6.

### Commit 5. Session log entry and current-package final transition

`docs(p9/s35): session log entry; P9 closed; P10 framing next`

- Append `## P9 — S35 — P9 close via end-to-end demonstration; test_agent_sse_endpoint carryover fixed; deferred-decisions extended; package archived` entry to `log/sessions.md`. `roles:` (architect, engineer, technical writer). `mode: build`. Reflection prompts addressed per the four below in substantive paragraphs.
- Edit `charter/current-package.md`: transition "S35 in flight (P9 close)" to "P9 closed; P10 framing next" with a closing paragraph framing what P9 delivered (the run-history substrate as Phase 2 UX consumer surface plus the end-to-end-demo as bet-proof artefact) and brief framing for the strategic-mode P10 conversation that follows.

## Acceptance criteria

1. End-to-end demo executes against tenant_a with no manual intervention beyond the trigger command. Run record plus citation rows land in per-tenant Postgres. Audit chain hashes match between runs row and tenant_audit entries. HTTP routes return the rendered surface.
2. `docs/smoke/p9_s35_e2e_demo.md` records the full execution with the captured outputs.
3. `test_agent_sse_endpoint.py` passes in isolation and as part of the broader test suite. No regression introduced.
4. `charter/deferred-decisions.md` carries two new entries per Appendix A. Existing entries unchanged.
5. `docs/notes/integration-test-slowness-investigation.md` is committed.
6. `charter/packages/p9-epic.md` carries the retrospective addendum.
7. `log/packages.md` carries the P9 retrospective entry per the P8 precedent.
8. `charter/packages/p9-epic.md` archived to `docs/archive/packages/p9.md`.
9. `charter/current-package.md` transitioned through "S35 in flight (P9 close)" to "P9 closed; P10 framing next."
10. `briefs/p9/s35.md` preserved with reconciliation findings as Appendix D.
11. Session log entry appended. Four reflection prompts answered in substantive paragraphs.
12. `make lint` passes. All import-linter contracts kept. AST enforcement passes.
13. `uv run pytest` passes with no regressions beyond the pre-existing 11 CLI failures and the integration-test slowness scope.

## Reflection prompts addressed in session log entry

1. **Pre-write reconciliation, twelfth instance.** Compare the eight reconciliation surfaces against findings. Which absorbed without question, which forced a user-question, which surfaced after the work began. Surfaces 1 (test_agent_sse_endpoint failure shape) and 2 (agent template plus tool_allowlist state on tenant_a) were the highest-risk-of-structural-finding entering the session; surface what they revealed. The find-rate trajectory across S31 through S35 is the substrate-stability proxy worth recording at the Phase 1 close audit.

2. **End-to-end demo as P9 close artefact.** The demo is methodology evidence for the Phase 1 close audit at P12. What did the execution surface that the substrate sessions S31 through S34 could not surface on their own? Did the path execute cleanly or did integration friction emerge? Did the audit-chain linkage hold byte-for-byte or did any hash mismatch surface? Did the SSE event stream match the documented eleven-event vocabulary from D90 across the full path?

3. **The audit-chain-to-run-record linkage as integrity claim.** D95's audit-chain-partial-state shape committed the hash linkage between the run record and the audit chain. The demo is the first time this linkage is verified end-to-end across a real invocation. What did the verification require operationally? What would a malicious actor need to do to break the linkage? Are there sharp edges in the verification protocol that need to be captured at the methodology document for Phase 1 close audit?

4. **What P9 leaves for P10 framing.** P9 closes the run-history substrate plus the HTTP read surface. P10 is the evaluation territory per the roadmap. What did P9's substrate produce that P10 can build on directly? What carryovers carry forward (the 11 CLI failures, the integration-test slowness, the ingestion-management HTTP API, the retrieval-aware role allowlists from the P8→P9 boundary)? Are there P10 framing inputs that emerged from P9's execution that the strategic-mode P10 conversation should weigh? Surface the framing inputs without committing to P10 substance; that is the strategic-mode conversation's work.

## Out of scope

- Ingestion-management HTTP API. Deferred to early Phase 2 substrate-completion per the new deferred-decisions entry at commit 1.
- Integration-test slowness fix (marker-based tier separation). Deferred per the new deferred-decisions entry at commit 1.
- The 11 methodology/tool CLI test failures (config shape drift carryover from S30b/S31). Pre-existing; not on the demo path; lands at its own session bundled with the broader config-shape-drift fix.
- P10 framing itself. P10 is strategic-mode work that follows P9 close. S35 closes P9 and transitions current-package to the "P10 framing next" state; the strategic-mode conversation produces the P10 framing artefacts.
- Any retrospective revision of S31 through S34 commits or session log entries. The investigation note's correction of the synthetic-DB hypothesis records the correction at the note itself rather than rewriting S34's session log entry; that pattern holds across S35.
- Methodology promotion to `charter/methodology.md`. Pre-write reconciliation tenth-plus reinforcement and consumer-port-plus-wiring-adapter eighth-plus reinforcement are promotion candidates for the Phase 1 close audit at P12; S35 records the reinforcement in the session log but does not promote.

## Appendix A — deferred-decisions.md entries

### Entry: integration-test slowness from real-LLM inference in `tests/integration/`

The top-ten slow integration tests (~695s combined at the S34-close durations probe) are dominated by real LLM inference via LiteLLM against `qwen2.5:7b` plus embedding models against Ollama. Module-scope fixtures are correctly applied; engine.dispose patterns honour module scope; the function-scoped truncation antipattern is small (~3% of cost). The cost is in test bodies executing real model calls, not in fixture overhead.

The recommended fix shape is two-tier test strategy: mark LLM-dependent integration tests with `@pytest.mark.live_llm` (or equivalent), exclude from default `uv run pytest` runs, expose through a separate Makefile target that runs at per-package close or weekly cadence. Preserves test fidelity; trades freshness of LLM-dependent verification against turnaround time on default hygiene runs.

**Activation trigger:** any session feeling fast-iteration pressure enough to invest in tier separation. The pressure is most likely to fire at P10 framing if P10's evaluation work requires fast hygiene loops, or at Phase 1 close substrate-completion if the substrate-completion territory bundles hygiene.

**Candidate landing site:** P10 framing or Phase 1 close substrate-completion territory. The work is small (~50 lines: pytest markers plus pyproject.toml collection config plus Makefile target) but warrants its own session for the D-entry that names the test-tier convention.

**Reference:** `docs/notes/integration-test-slowness-investigation.md` (committed at S35) for the full investigation and findings.

### Entry: HTTP API for ingestion management

The P6 deferred carryover absorbed into P9 at framing (per `charter/packages/p9-epic.md`) did not land at S35 in favour of the end-to-end demo closing P9 on Kano-must-have-for-bet grounds. The CLI works for ingestion at Phase 1; no UI consumer has materialised that needs HTTP-driven ingestion management.

**Activation trigger:** a UI consumer (Phase 2 frontend or external tool) that needs HTTP-driven ingestion management. Triggers at first concrete user story demanding it.

**Candidate landing site:** early Phase 2 substrate-completion territory. The substrate (the ingestion use cases at `contexts/ingestion/application/`) is fully in place; the work is purely the HTTP exposure plus principal-derived tenant context plus error response shape per the S34 D98 precedent.

**Reference:** D60 (the original P6 deferral); D98 (the HTTP shape precedent the ingestion API would mirror).

## Appendix D — Pre-write reconciliation outcomes

Eight reconciliation surfaces verified at session open per the brief's "Pre-write reconciliation" section. Find-rate distribution: zero structural-honesty at session-open; eight mechanical absorptions; zero no-impact verifications; one mid-session structural finding (downstream of surface 2) that surfaced at demo trigger execution rather than at session-open reconciliation. The trajectory across P9 (three structural at S31 → one at S32, S33, S34 → zero at S35 session-open) tracks substrate stability as the package matures.

**Surface 1 — `test_agent_sse_endpoint.py` actual failure mode.** Mechanical. Ran the file in isolation; the failure traceback at `tests/integration/api/test_agent_sse_endpoint.py:181-189` confirmed: `AgentRuntimeComposition.__init__() missing 1 required positional argument: 'run_history_writer'`. The fix is a test-helper update — adding `_FakeRunHistoryWriter` to the existing fakes list and threading it through `_runtime_with_script`. Not a writer-construction update; not a domain-record update. Landed at commit 2 (`fix(p9/s35)`, 16bb63e).

**Surface 2 — Agent templates plus role plus tool allowlist state on tenant_a.** Mechanical absorption with option (a) seeding. tenant_a has two existing agent templates (Flowstate ProblemFramer v1/v2 from S30b demos) but both have `tool_allowlist=[]`. Control plane has the `retrieval` tool registered. tenant_a has 79 chunks plus indexed sources for retrieval. Brief recommendation: option (a) if seeding is light. Seeding executed at commit 3: fresh agent authored via `agent create-from-role` against ProblemFramer role; `agent update` added revision 2 with `tool_allowlist=[retrieval]`. Light path; no user-question required. **Downstream mid-session finding** at trigger execution time: the existing agents have methodology lineage pointing to a methodology row wiped by the S30b fixture-leak class. Invoking such an agent fails at `methodology_overrides_lookup` with `LookupError: methodology template d13b6f03-... not found`. Resolved by using `create-from-role` (no methodology lineage) rather than reviving the methodology row. Recorded at the smoke document.

**Surface 3 — Tenant registry state.** Mechanical absorption. Registry contained zero rows at session open — the recurring wipe class from S30b/S31 carryover had fired since S34 close. Demo step 1 was `make seed-tenants` recovery; recorded in the smoke document.

**Surface 4 — Image pin state at compose.yaml.** Mechanical absorption. The pinned digest `padhanam-api:dev@sha256:583f986993f6...` at `compose.yaml:380` matches the S34 close image. No rebuild required at S35; the session changes are scoped to test helpers, charter files, and markdown documents — no production-wiring or context-boundary code changes.

**Surface 5 — Langfuse trace correlation.** Mechanical absorption with limitation finding at smoke time. Health probe via `httpx.get('http://langfuse-web:3000/api/public/health')` returned 200 OK with `{"status":"OK","version":"3.172.0"}`. The optional cross-store correlation step was planned. **Limitation finding at smoke:** the runs row's `trace_id` column is NULL on both demo invocations despite Langfuse being healthy. The trace-id propagation gap is structural — the agent-runtime accumulator path does not capture the OTel trace context into the run record. Documented at the smoke as a Phase 1 substrate-completeness limitation for the Phase 1 close audit; substrate complete at column + read-port level, capture path needs revisit.

**Surface 6 — `docs/archive/packages/` directory state.** Mechanical absorption. Directory exists at `/Users/sabu/padhanam/docs/archive/packages/` containing `p2.md` through `p8.md`. The P8 archive convention verified: charter file `charter/packages/p8-epic.md` remains at its original path AND a separate copy with retrospective addendum lives at `docs/archive/packages/p8.md`. S35 follows the same pattern for P9 at commit 4.

**Surface 7 — `log/packages.md` shape.** Mechanical absorption. P8 retrospective entry at line 796 read in full. The P9 entry at commit 4 mirrors the structure: package goal restated in opening line; sessions shipped with one-line each; archive link; measured outcomes section (deployment frequency, lead time for changes, change failure rate, MTTR, reliability, developer experience, contribution effectiveness); bet-native metrics (discipline-adherence, architectural-durability, bet-direction integrity); "what the numbers do not say" close.

**Surface 8 — `charter/deferred-decisions.md` current shape.** Mechanical absorption. Existing convention read in full: each entry has a `## Heading` line, an "Activates when..." line, body paragraphs typically including bold-prefix anchor lines, and a "**The specific D-entry lands at the activating session.**" closing convention. The two new entries at commit 1 mirror this shape: integration-test slowness from real-LLM inference; HTTP API for ingestion management.
