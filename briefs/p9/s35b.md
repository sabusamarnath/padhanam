# Brief: P9 carryover S35b — TRUNCATE+CASCADE fix, SQLAlchemy-based tenant-isolation truncation, streaming-isolation diagnostic

**mode:** build

## Goal

At S35b close the following artefacts exist:

- TRUNCATE-with-CASCADE fix applied to the two test fixtures that hit the chunks-to-run_chunk_citations FK boundary introduced at S32. The previously-failing tests pass without compromising the FK semantics elsewhere.
- Tenant-isolation contract test fixtures at `tests/contract/tenant_isolation/test_ingestion_isolation.py` migrated from `docker compose exec psql` shell-out to SQLAlchemy-based truncation matching the methodology fixture pattern. The two psql-missing tests pass against populated tenant data, not against empty tables. Harness integrity restored: tests are testing isolation rather than emptiness.
- `test_agent_streaming_isolation` failure shape diagnosed. If fixture-state class (the same empty-tables shape as the ingestion-isolation tests), fix folds into S35b as commit 4 (or absorbed alongside commit 3). If real isolation gap, surface as session-open question; the workstream stops at the diagnostic and a new session frames the structural fix.
- All six pre-existing carryover failures resolved (assuming streaming-isolation is fixture-state class; otherwise five plus a structural-gap entry on the carryover list with clearer framing).
- Tenant-isolation contract harness still at 20 scenarios with all passing; if reconciliation surfaces additional scenarios that should land to enforce non-empty-tables-at-test-time, they extend the harness accordingly.
- `charter/current-package.md` transitioned twice: from "P9 closed; P10 framing next" to "S35b in flight (P9 carryover hygiene continued)" at commit 1; back to "P9 closed; P10 framing next" at the closing commit. The carryovers section updates to reflect resolved entries.
- `briefs/p9/s35b.md` preserved with reconciliation findings as Appendix D.

No new D-entries expected. D24 (tenant isolation contract test discipline) already commits the principle the fixes restore. If the streaming-isolation diagnostic reveals structural ground, the user-question protocol applies and D102 may land at commit 1.

## Context to read first, in order

1. `charter/principles.md` for the Security posture commitment to tenant-isolation contract tests being red-team shaped and Engineering practice "Tests are part of the build, not a follow-up."
2. `charter/decisions.md` D24 (tenant isolation contract tests), D32 (database-per-tenant), D95 (run record schema and FKs).
3. `charter/current-package.md` for the carryovers section and the "P9 closed; P10 framing next" state.
4. `log/sessions.md` S35a entry for the carryover list as it stood at S35a close.
5. The methodology fixture at `tests/contract/tenant_isolation/test_methodology_isolation.py` (or wherever the SQLAlchemy-based truncation pattern lives; reconciliation surface 2 settles the path) as the template for the new ingestion-isolation fixture.
6. `tests/contract/tenant_isolation/test_ingestion_isolation.py` for the two psql-missing failures.
7. The two TRUNCATE-without-CASCADE failing tests (identified at reconciliation surface 1).
8. `tests/integration/contexts/agent/test_agent_streaming_isolation.py` (or equivalent path) for the streaming-isolation diagnostic target.
9. `alembic/tenant/versions/0011_*` for the FK shape on `run_chunk_citations.chunk_id → chunks.id` introduced at S32.

## Pre-write reconciliation

Five surfaces to verify before writing. Resolve each before commit 2.

1. **Exact identity of the six failing tests.** Run the integration suite (default tier, live-LLM excluded) and capture the six failure traces. Confirm two are TRUNCATE-without-CASCADE shape, two are psql-missing shape, two are test_agent_streaming_isolation. If the distribution differs, surface as session-open question; the brief's scope assumes the stated distribution.

2. **Methodology fixture SQLAlchemy-truncation pattern.** Identify the existing fixture that performs SQLAlchemy-based truncation against tenant-scoped tables. Read it in full. The pattern dictates: session construction, transaction scoping, table-list ordering for FK-respecting cascade, teardown idempotency. The new ingestion-isolation fixture mirrors this exactly so the two harnesses converge.

3. **`test_agent_streaming_isolation` diagnostic.** Read the failing tests. Compare the failure mode against the two known shapes (FK CASCADE missing; psql-missing). If the failure is fixture-state class (same empty-tables shape as ingestion-isolation, or same FK shape as TRUNCATE), fold the fix into the brief's scope at commit 4. If the failure exposes a real isolation gap (cross-tenant data actually leaking through the streaming endpoint), surface as session-open question and stop the streaming workstream pending operator decision on structural treatment.

4. **FK shape on chunks-to-run_chunk_citations.** Verify the FK on `run_chunk_citations.chunk_id` references `chunks(id)` with `ON DELETE SET NULL` (S31 D95 commitment). The TRUNCATE+CASCADE fix needs to either include `run_chunk_citations` in the truncate set or use `CASCADE` keyword; the FK semantics elsewhere (a chunk deletion sets the citation's chunk_id to NULL, preserving the audit-evidence row) must not be affected by the test-fixture change. The fixture truncates both tables in the same statement; production data deletion still goes through ORM with SET NULL semantics intact.

5. **Tenant data state at session-open.** The recurring registry-wipe class was closed at S35a via D101. Verify tenant_a and tenant_b are populated as expected. If the registry is wiped (D101's guard fired or didn't), document the recovery sequence in the smoke and proceed. Verify the test fixtures will populate sufficient data to make isolation tests pass-because-isolated rather than pass-because-empty.

If any surface reveals structural drift, surface as session-open question before commit 2.

## Charter updates ahead of code

Commit 1 lands the charter touchpoints in a single commit ahead of code.

**Commit message:** `docs(p9/s35b): current-package transition; brief preserved`

Files updated:
1. `charter/current-package.md` — transition from "P9 closed; P10 framing next" to "S35b in flight (P9 carryover hygiene continued)" with one framing paragraph naming the three workstreams.
2. `briefs/p9/s35b.md` — preserve this brief.

If the streaming-isolation diagnostic surfaces structural ground at reconciliation surface 3, D102 lands at this commit; the appendix text settles at session-open via the user-question protocol.

## Commits

### Commit 1. Charter updates

`docs(p9/s35b): current-package transition; brief preserved`

Per the charter-updates-ahead-of-code section above.

### Commit 2. TRUNCATE+CASCADE fix

`fix(p9/s35b): TRUNCATE with CASCADE on chunks-to-run_chunk_citations FK boundary`

- Identify the two failing fixtures at reconciliation surface 1.
- Apply the fix: either `TRUNCATE TABLE chunks, sources, run_chunk_citations` (extending the truncate set) or `TRUNCATE TABLE chunks, sources CASCADE` (using CASCADE keyword). Reconciliation surface 4 settles which is cleaner against the FK semantics; the recommendation is the explicit-table-list shape because CASCADE is non-obvious to readers and the explicit list documents the intent.
- Verify the two previously-failing tests pass.
- Verify the FK semantics elsewhere are intact: a production deletion of a chunk through the ORM still sets the corresponding `run_chunk_citations.chunk_id` to NULL per D95.
- Unit-test coverage: the fixture's truncation path is exercised; the ORM deletion path is exercised separately and unaffected.

### Commit 3. SQLAlchemy-based tenant-isolation truncation

`refactor(p9/s35b): SQLAlchemy-based truncation in tenant-isolation fixtures, matching methodology pattern`

- Migrate the two psql-missing fixtures at `tests/contract/tenant_isolation/test_ingestion_isolation.py` from `docker compose exec psql` shell-out to the SQLAlchemy-based truncation pattern identified at reconciliation surface 2.
- The fixture acquires a per-tenant session, truncates the test-relevant tables in FK-respecting order, commits, releases the session.
- Populate the fixture with non-empty test data so the isolation tests pass-because-isolated rather than pass-because-empty. The harness is now red-team shaped per D24.
- Verify the two previously-failing tests pass.
- Verify they pass for the right reason: introduce a deliberate-violation test (a temporary commit that breaks isolation; verify the contract tests catch it; revert).

### Commit 4. Streaming-isolation fix (fold-in if fixture-state class)

`fix(p9/s35b): test_agent_streaming_isolation fixture-state correction` (subject to diagnostic outcome at reconciliation surface 3)

- If the streaming-isolation diagnostic at reconciliation surface 3 surfaced fixture-state class: apply the analogous fix (SQLAlchemy-based truncation, populated test data, deliberate-violation verification).
- If the diagnostic surfaced a real isolation gap: this commit does not land. The streaming-isolation work stops at the diagnostic. The session log records the finding; a new session frames the structural fix.

### Commit 5. Session log entry and current-package final transition

`docs(p9/s35b): session log entry; transition back to P9 closed; P10 framing next`

- Append `## P9 carryover — S35b — TRUNCATE+CASCADE fix; SQLAlchemy-based tenant-isolation truncation; streaming-isolation [diagnosed/fixed]` entry to `log/sessions.md`. `roles:` (architect, engineer). `mode: build`. Reflection prompts addressed per the four below.
- Edit `charter/current-package.md`: transition "S35b in flight" back to "P9 closed; P10 framing next" with a closing paragraph framing what S35b closed (harness integrity restored across tenant-isolation contract tests) and the carryover list state after S35b close.

## Acceptance criteria

1. `charter/current-package.md` transitioned through "S35b in flight" and back to "P9 closed; P10 framing next." Carryovers section reflects resolved entries.
2. `briefs/p9/s35b.md` preserved with reconciliation findings as Appendix D.
3. The two TRUNCATE-without-CASCADE failing tests pass.
4. The two psql-missing tests at `tests/contract/tenant_isolation/test_ingestion_isolation.py` pass under SQLAlchemy-based truncation. No `docker compose exec` shell-out remains in the file.
5. The two test_agent_streaming_isolation tests either pass (if fixture-state class) or remain on the carryover list with a clearer framing (if structural gap).
6. The tenant-isolation contract harness tests pass because isolation is enforced, not because tables are empty. A deliberate-violation cycle on the new fixtures catches the violation.
7. The chunks-to-run_chunk_citations FK semantics in production are unaffected. ORM deletions of chunks still set citation chunk_id to NULL per D95.
8. `make lint` passes. All import-linter contracts kept.
9. `uv run pytest` (default tier) passes with no regressions and four-to-six fewer pre-existing failures.
10. Session log entry appended. Four reflection prompts answered.

## Reflection prompts addressed in session log entry

1. **Pre-write reconciliation, fourteenth instance, second carryover-hygiene altitude.** Compare the five reconciliation surfaces against findings. Which absorbed without question, which forced user-question (specifically: did the streaming-isolation diagnostic surface fixture-state class or structural gap, and how did the resolution shape the session). The S35a observation that hygiene-session reconciliation has a different shape from substrate sessions (less architectural-drift, more pattern-recurrence and operational-state) gets a second data point. Surface whether the carryover-hygiene altitude is stable as a methodology variant.

2. **Harness integrity as a class of finding.** The "pass-because-empty rather than pass-because-isolated" framing surfaced during S35a close conversation as the load-bearing reason to do S35b before P10 framing. What did the deliberate-violation verification reveal about the harness's red-team posture? Are there other contract tests in the harness (beyond ingestion-isolation and streaming-isolation) that might harbour the same shape — tests that pass for the wrong reason because of fixture state? Worth recording a methodology observation: hygiene sessions should include a deliberate-violation pass as standard verification, not just an existence check.

3. **Fixture pattern unification.** The methodology fixture's SQLAlchemy-based truncation pattern became the harness standard at S35b. The convergence is architectural symmetry similar to the apps/cli-to-apps/api convergence at D100. What other fixture patterns across the harness might benefit from the same unification pass? Surface candidates for the Phase 1 close audit at P12.

4. **What S35b leaves for P10 framing.** P10 is evaluation territory. The harness now actually enforces isolation claims; P10's evaluation work builds on infrastructure that earns its security commitments. What does the carryover list look like after S35b close? Are any remaining entries blocking or shaping P10? Surface the framing inputs for the P10 strategic-mode conversation that follows.

## Out of scope

- Other unenumerated carryovers in the `charter/current-package.md` carryovers section beyond the six this brief targets. They remain inventory pending P10 framing audit or activation.
- P10 framing itself. Strategic-mode work that follows S35b close.
- Real isolation gap in `test_agent_streaming_isolation` if the diagnostic surfaces one. That work lands at its own session with structural framing.
- Migration of other contract test fixtures (beyond ingestion-isolation and possibly streaming-isolation) to the SQLAlchemy pattern. If reflection prompt 3 surfaces additional candidates, they land at their own session or fold into Phase 1 close audit hygiene at P12.
- New D-entries beyond the contingent D102 if the streaming-isolation diagnostic forces structural commitment.
- Adding `postgresql-client` to the padhanam-api Dockerfile. Option (a) rejected at S35a-close framing in favour of option (b) per the comprehensive-solution-over-patch logic from D100 and D101.
