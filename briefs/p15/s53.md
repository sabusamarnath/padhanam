# S53 — P15 opens: Outbound initiation substrate

## Identification

**Package:** P15 — Outbound initiation plus intake-side integrations (medium scope: outbound + calendar-read + email-read)
**Session:** S53 (first build session in P15; foundational substrate for outbound initiation)
**Mode:** Build (substrate foundation; subsequent P15 sessions consume this substrate)
**Brief authored:** 2026-05-27 at P15 framing close (six-surface framing strategic-mode session)
**Predecessors:** P14 close at S52 (2026-05-27); P15 framing strategic-mode session (2026-05-27)
**Successors:** S54 (HTTP trigger endpoint plus daily-briefing BroadcastFlow implementer); S55 (calendar-read substrate plus calendar-conversation cell); S56 (email-read substrate plus email-conversation cell); S57 (threshold-briefing plus ThresholdEvaluator plus P15 close)

---

## Goal (artefacts at session close)

At S53 close, the repo state carries:

1. **BroadcastFlow Protocol** at `shared_kernel/broadcast_flow.py` carrying the single fire method plus TriggerContext value object plus BroadcastResponse satisfying CitedResponse Protocol.

2. **TriggerContext typed value object** at `shared_kernel/broadcast_flow.py` (single file per the shared_kernel convention from S52's Finding 2) carrying trigger_type discriminator plus per-type metadata via discriminated union.

3. **BroadcastDispatch port** at `contexts/messaging/application/ports/broadcast_dispatch.py` plus the InProcessBroadcastDispatchAdapter at `contexts/messaging/adapters/dispatch/`. Symmetric to S47's CellDispatch port.

4. **BroadcastFlow registry mechanism** at the contract harness, mirroring the S45 ConversationFlow registry pattern (the harness globs `test_*_broadcast_flow.py` for registration modules).

5. **BroadcastFlow contract harness** at `tests/contract/broadcast_flow/` with conformance scenarios (signature; CitedResponse conformance for BroadcastResponse; tenant-scoping at fire method).

6. **ChannelResolver Protocol** at `contexts/messaging/application/ports/channel_resolver.py` plus StaticConfigChannelResolverAdapter at `contexts/messaging/adapters/` reading the extended MessagingSettings.

7. **MessagingSettings extension** with `operator_default_channel: ChannelType = ChannelType.WHATSAPP` field plus the existing operator phone number reused.

8. **MessageIntent enum** at `shared_kernel/` (or `contexts/messaging/domain/`; path lands at pre-write reconciliation) carrying the three Phase 2-A values: BROADCAST_DAILY_BRIEFING, BROADCAST_THRESHOLD_BRIEFING, REACTIVE_RESPONSE.

9. **ChannelDestination value object** at `contexts/messaging/domain/channel_destination.py` carrying channel_type plus channel_address.

10. **Reactive outbound refactor** at `contexts/messaging/application/`: the existing outbound use case from S45 now consults ChannelResolver before send. Identity routing at Phase 2-A (resolves to the same channel the inbound came from); multi-channel adapter swap defers to second-channel activation.

11. **Procedural smoke** at `docs/smoke/p15_s53_outbound_substrate.md` walking the BroadcastFlow plus ChannelResolver substrate end-to-end against tenant_a (without a registered BroadcastFlow implementer yet; smoke validates the dispatch substrate and resolver work).

12. **Session log entry** at `log/sessions.md` plus **current-package.md transition** marking S53 close and naming S54 next.

---

## Brief preserved per D43.

The full brief text authored at P15 framing close (2026-05-27) is preserved here for audit-trail traceability. Substantive prose from the authored brief is reproduced verbatim above (sections 1-12) and continues across the brief's full body covering Context-to-read-first, Pre-write reconciliation, Charter updates (commit 1 prose for D142-D145 + architecture.md additions + packages.md revision + deferred-decisions.md updates + p15-epic.md content + captures.md entries), Substantive work (commits 2-7), Acceptance criteria, Reflection prompts, Out of scope, and Session log entry instruction. The session executes against the brief in full; this preservation file is the immutable record of what the brief committed at authoring time.

The brief's surfaces:
- Context to read first (eleven items)
- Pre-write reconciliation: substrate-inheritance survey first, interface-versus-implementation fifth recurrence test instance, schema.md hygiene check, standing pre-write reconciliation surfaces
- Charter updates (commit 1) covering D142 (BroadcastFlow Protocol at shared_kernel), D143 (BroadcastDispatch substrate at messaging context), D144 (ChannelResolver Protocol with static-configuration adapter at Phase 2-A), D145 (HTTP trigger endpoint substrate with audit chain integration)
- Architecture.md additions covering BroadcastFlow Protocol, BroadcastDispatch substrate, ChannelResolver Protocol, HTTP trigger endpoint sub-sections
- packages.md revision plus p15-epic.md (new file) plus deferred-decisions.md eight updates plus log/captures.md five entries
- Substantive work commits 2-7
- Acceptance criteria (15 items)
- Reflection prompts (8 prompts)
- Out of scope (18 items)
- Session log entry instruction

End of S53 brief.
