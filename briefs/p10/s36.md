# S36 — P10 open + audit read substrate

## Package and session

P10 session 1. Opens P10 (audit log viewer, backend-only). Ships
charter updates ahead of code per discipline, then the audit
context's read side.

## Goal

At session close:

- `charter/packages/p10-epic.md` exists and commits the P10 epic.
- `charter/current-package.md` transitions from "P9 closed; P10
  framing next" to "P10 open" with the corrective note on the
  misrouted P10 framing inputs from S35 close.
- `charter/decisions.md` carries the new D-entry (next available
  number, forecast D102) anchoring the P10 substrate.
- `contexts/audit/` carries the read side: `AuditEventReader`
  consumer-defined port, the supporting domain value objects,
  the `PostgresAuditEventReader` adapter against both
  destinations, and wiring at both composition roots.
- Tenant-isolation contract harness extends with cross-tenant and
  cross-destination scenarios on the read port.
- Live-stack smoke verifies the three reads against `tenant_a`'s
  per-tenant chain and the control-plane chain end-to-end.
- Session log entry at `log/sessions.md` per the standing
  reflection-density convention.

## Pre-write reconciliation

Three reconciliation passes happen before any code lands. Each
surfaces findings as questions to the operator if structural
drift appears.

1. **Existing `contexts/audit/` tree.** Read the directory layout
   and the hexagonal-layer convention it follows. The P10 epic
   forecast assumes a layered shape with `domain/`, `application/`,
   `ports/`, and `adapters/` subtrees. Confirm against as-built.
   The brief assumes the hash-chain helpers live at
   `contexts/audit/domain/events.py`; confirm signature and
   reuse contract.

2. **Audit table schema versus `charter/schema.md`.** The brief
   assumes two destinations sharing schema column-for-column: the
   per-tenant `tenant_audit` table (12 columns) and the
   control-plane `tenant_audit` table (12 columns, with
   `tenant_id = ''` sentinel). Confirm against the live migration
   state on both destinations before adapter work begins. Any
   drift gets surfaced before the read port shape commits.

3. **Principal-derived tenant context pattern.** S34 settled this
   at `apps/api/routers/agent.py` and `apps/api/routers/
   run_history.py`. Confirm the dependency function signature and
   the 404 behaviour on tenant-not-in-registry. The S37 work
   (next session) extends the resolver with a platform-operator
   branch; the S36 work consumes the existing per-tenant resolver
   only.

Findings from reconciliation surface as questions or as in-line
corrections to the brief. The discipline is: ask before adjusting
shape, do not silently adapt.

## Charter updates (commit 1)

Single commit, conventional-commit subject:

```
docs(charter): open P10 audit log viewer (D102)
```

[Charter update details per the operator's original brief; the
content landed at commit 1 mirrors the brief with the column-
count and chain-helper-reuse corrections from Appendix D below
applied at write time.]

## Substantive work (commits 2 onward)

[Eight-commit plan per the operator's original brief.]

## Acceptance criteria

1. `charter/packages/p10-epic.md` exists.
2. `charter/current-package.md` reads "P10 open" with the
   corrective note attached and the new audit-port-asymmetry
   carryover entry.
3. `charter/decisions.md` carries D102 with the alternatives
   and Kano category.
4. `contexts/audit/ports/reader.py` defines `AuditEventReader`
   as Protocol with three methods.
5. `contexts/audit/domain/` carries `audit_event_record.py`,
   `query_filters.py`, `chain_integrity.py`, and `destination.py`
   with the value objects defined.
6. `AuditEventListFilters` validator rejects `resource_id`
   without `resource_type`; unit test covers it.
7. `contexts/audit/adapters/outbound/postgres/reader.py`
   implements the port; chain integrity verification reuses
   `compute_event_hash` and `GENESIS_HASH` as primitives from
   `contexts/audit/domain/events.py`; the page-granularity
   verifier is new logic on top.
8. Wiring at `apps/cli/_cross_context.py` and
   `apps/api/_agent_runtime_wiring.py` exposes the reader
   symmetrically with the run-history reader from S33.
9. Tenant-isolation contract harness extends with the
   scenarios listed at commit 6; all new scenarios pass.
10. All unit and integration tests pass; the live-stack
    smoke document at `docs/smoke/p10_s36_audit_reader.md`
    walks the documented scenarios end-to-end.
11. Import-linter contracts stay green.
12. Session log entry at `log/sessions.md` lands.

## Reflection prompts addressed in the session log entry

[Four prompts per the operator's original brief.]

## Appendix D — Pre-write reconciliation outcomes (2026-05-14)

Reconciliation fired its fifteenth instance at S36 session-open
(first P10 instance). Three reconciliation passes per the
brief's three-surface framing plus four brief-shape corrections
captured at write time.

**Finding 1 — Audit context port-location asymmetry (operator
question).** The brief at "Commit 3: read port" specifies a new
`contexts/audit/ports/reader.py` mirroring `contexts/run_history/
ports/`. The existing write-side `AuditPort` sits at
`contexts/audit/domain/ports.py` (pre-P9 convention). Three
options framed at the operator user-question: (A) brief's call —
new `ports/reader.py` only, write-side port unchanged
(asymmetry within audit, symmetry with run_history at the read
port); (B) move existing `AuditPort` to `contexts/audit/ports/
writer.py` simultaneously, full structural symmetry; (C)
co-locate both ports in `contexts/audit/domain/ports.py`,
consistent within audit but asymmetric with run_history.
Operator selected option (A) on minimal-touch grounds. The
intra-context asymmetry is recorded as a carryover at
`charter/current-package.md` with activation trigger named (next
port added to the audit context).

**Finding 2 — Chain helper reuse is partial, not wholesale.**
The brief at "Commit 4: Postgres adapter" reads "Chain
verification reuses existing helpers at `contexts/audit/domain/
events.py`." The existing `verify_chain(events)` walker at
`contexts/audit/domain/events.py:95` walks from `GENESIS_HASH`
and reports `break_index=0` for any input whose first row's
`previous_event_hash` differs. For a page-granularity verifier
that may start mid-chain, this is structurally inapplicable.
The reusable primitives are `compute_event_hash` (the per-row
recomputation) and `GENESIS_HASH` (the sentinel for head-of-
chain detection); the walker itself is new logic on top. The
correction lands at write time in D102 alternative (h) and in
the commit 4 adapter docstring; no brief shape change.

**Finding 3 — Read-side `AuditEventRecord` is a new value object
distinct from `AuditEvent`.** The existing domain `AuditEvent`
dataclass at `contexts/audit/domain/events.py:21` omits `id`
(Postgres generates server-side via `gen_random_uuid()` and the
write-path adapter never reads it back). The brief's read DTO
`AuditEventRecord` carries `id` per the 13-column table shape.
Two coexisting value objects: write-side `AuditEvent` without
`id`, read-side `AuditEventRecord` with `id`. Same pattern as
`AgentRunRecord` versus `RunRecord` at run_history. No brief
change.

**Finding 4 — Schema column count is 13, not 12.** Brief
language at the P10 epic forecast and at "Pre-write
reconciliation" surface 2 names "12 columns." Actual schema at
`charter/schema.md:54-68` (control plane) and `:326-340` (per-
tenant) carries 13 columns including the `id` UUID primary key.
Brief-shape miscount; corrected at write time in the P10 epic
and D102.

**Finding 5 — D-entry numbering clean at D102.** Verified at
`charter/decisions.md` tail; D101 is the latest entry. No
collision.

**Finding 6 — Principal-derived tenant context 404 behaviour
confirmed.** `get_tenant_context` at `apps/api/routers/
inference.py:81-124` returns 404 for tenant-not-in-registry,
400 for non-UUID `tenant_id`, 503 for "registry not configured."
Brief assumption holds.

**Finding 7 — Tuple-comparison defence-in-depth pattern exists
live.** S33's `sa.cast(..., pg.UUID)` pattern is in the run-
history Postgres reader; the audit reader will mirror.

Find-rate distribution at S36 session-open: one operator-
decision finding (finding 1) plus two clarifications to brief
language (findings 2, 3) plus one brief-shape correction
(finding 4) plus three confirmations (findings 5, 6, 7).
Substrate-shaping altitude at session-open — first P10 session
extends an existing context with a new substrate surface, mid-
to-low structural drift expected. The trajectory continues to
hold: pre-write reconciliation surfaces drift the brief's
prose-review pass cannot catch, and the structural-honesty
finding is the load-bearing one (finding 1) while the brief-
shape findings absorb mechanically.
