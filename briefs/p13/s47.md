# P13 S47 — UX convergence substrate

Strategic-mode convergence between P13 Wave 1 close and P14 epic
framing settled five architectural concerns at the user-experience
surface. S47 lands the convergence: charter commits as commit 1, then
substrate build work as commits 2 through 7. The substrate enables
P14 ConversationFlow implementers (audit-conversation per 5.1,
mirror-conversation per 4.1) to inherit working multi-turn confidence-
aware composition, dispatch-port-shaped webhook handling, and the
domain-content channel-format rendering pattern rather than half-
built infrastructure under P14 time pressure.

S47 ships seven commits across charter and substrate. No new
bounded context. The new domain entity (PendingClarification) lands
at `contexts/messaging/`. The new ports (CellDispatch,
ConfidenceCalculator) land per the hexagonal discipline. Static
configuration updates absorb concern 5's Response A.

## Package and session identification

- **Package.** P13 (Wave 1 close hygiene, bridging to P14 epic
  framing).
- **Session.** S47.
- **Mode.** Build session producing charter commits first
  (`docs(charter): ...`) then substrate code (`feat(p13/s47): ...`,
  `docs(p13/s47): ...`, `chore(p13/s47): ...`).
- **Predecessors.** S46 (manual entry cell first ConversationFlow
  implementer); S46 smoke (live-stack verification surfacing the
  two structural-honesty findings the convergence absorbed).
- **Convergence input.** The strategic conversation between S46
  smoke close and S47 framing settled concerns 4, 5, 3, 2, 1 in
  that order. The architectural commitments below are the
  convergence's settled position; this session implements them.

## Goal at session close

Eight artefacts exist at session close.

1. Four new D-entries (D133 through D136) land at `charter/decisions.md`
   with the prose specified in the **Charter updates** section.
2. Architecture.md prose additions land at `charter/architecture.md`
   for each new primitive.
3. Principles.md addition lands at `charter/principles.md` under Phase
   2 principles: the channel-shaped rendering of channel-agnostic
   domain output principle.
4. Deferred-decisions updates land at `charter/deferred-decisions.md`
   reflecting activation-trigger inheritance forward.
5. Captures entry lands at `log/captures.md` promoting the
   interface-versus-implementation methodology candidate at four-
   instance evidence.
6. CellDispatch port plus Path A in-process adapter ship; the
   Twilio webhook returns 2xx promptly via the dispatch port; cell-
   failure logging at the dispatch boundary closes the bare-`except`
   gap from S46 smoke.
7. ConfidenceCalculator port at shared_kernel ships with the self-
   reported placeholder adapter; D130's StructuredOutputPort gains
   the parse-failure-as-distinct-exception extension.
8. PendingClarification domain entity ships with Postgres persistence,
   audit lifecycle (create / resolve / expire), and a consumer port
   for cells; the manual entry cell consumes confidence plus
   PendingClarification through ports and runs three-case decision
   logic; WhatsApp channel-adapter renders shape-aware clarification
   for the new middle case; live-stack smoke against tenant_a verifies
   a full multi-turn cascade (medium-confidence classification,
   PendingClarification persisted, user confirmation resolves the
   pending, orchestration runs, cited confirmation reply renders).

Full brief content carried over to this preserved copy from the
S47 framing handoff document; the prompt above is the canonical
session-of-record artefact per D43 briefs/ preservation discipline.
See the original S47 prompt artefact for the full Charter updates,
substantive work commit by commit, acceptance criteria,
reflection prompts, out-of-scope deferrals, and session-log entry
instruction sections.
