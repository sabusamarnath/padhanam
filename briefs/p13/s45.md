# P13 — S45 — Messaging substrate (WhatsApp via Twilio Sandbox) plus ConversationFlow Protocol plus structured output plus provenance-aware response composition

## Brief lineage

This brief supersedes the prior `briefs/p13/s45.md` (the original 284-line draft). The updates absorb the S45 pre-write reconciliation findings and the subsequent disposition conversation at Claude.ai. Material changes from the original: SMS replaced with WhatsApp throughout per D119's existing channel commitment (the original brief's most load-bearing miss); D-number sequence corrected to D129 (messaging substrate), D130 (structured-output), D131 (provenance-aware response composition) given D119 is already taken; orchestration placement settled at intake per D127(d); import-linter contract count corrected to 32 (adds layers-messaging plus twilio-confined plus twilio-confined-apps); CLI scope explicitly deferred; ConversationFlow no separate shape D-entry; new architecture.md prose for provenance-aware response composition; new deferred-decisions entries for multi-channel UX readiness and structured-output-to-agent-tool convergence; new captures.md entry for the UX convergence forward strategic block.

## Goal stated as artefacts at session close

At session close the repository carries:

- A new `contexts/messaging/` bounded context: `domain/message.py` defining Message aggregate root plus MessageDirection, MessageChannel (WhatsApp at Phase 2-A), MessageStatus enums; `ports/message_repository.py` for persistence; `ports/message_delivery_port.py` for outbound vendor send; `application/send_message.py`, `application/get_message.py`, `application/list_messages.py`, `application/record_inbound_message.py` (plain use case for inbound Message persistence; invoked from the intake-context orchestration); `adapters/outbound/twilio/twilio_message_delivery.py` implementing MessageDeliveryPort via Twilio Python SDK (WhatsApp via `whatsapp:`-prefixed addresses); `adapters/outbound/local_echo/local_echo_message_delivery.py` implementing the same port for local-first development; `adapters/outbound/postgres/message_repository.py` implementing persistence.
- A new orchestration at `contexts/intake/application/record_intake_and_record_inbound_message.py` with dual decorators (`intake.record.create` plus `messaging.message.receive`) following the S44b PortfolioWriter consumer-port precedent. The orchestration imports a new `MessageWriter` consumer port at `contexts/intake/application/ports/message_writer.py` with its own intake-owned result DTO. The apps/ layer provides the MessageWriter adapter calling `contexts.messaging.application.record_inbound_message`.
- Alembic 0019 creating the `messages` table per-tenant. Alembic 0020 (revision string `0020_intake_source_whatsapp` per the captures-documented Migration name-length convention) extending the IntakeSource enum at the existing `intakes` table with `WHATSAPP_INBOUND`.
- A new `shared_kernel/conversation_flow.py` defining ConversationFlow Protocol with three methods (open, turn, close) plus the five value object shapes (ConversationInvocation, ConversationState, ConversationInput, ConversationClosure, ConversationOutcome). No separate shape D-entry; implementation lands D115 directly per the disposition (Revisable's D125 existed because shape had alternatives; ConversationFlow's shape has no contested alternatives).
- A new `shared_kernel/structured_output.py` defining StructuredOutputRequest, StructuredOutputResponse[T] generic, StructuredOutputPort Protocol.
- Extension of the existing inference adapter at `contexts/inference/` to implement StructuredOutputPort. Existing inference shape preserves; the extension is additive.
- Two new contract-test harnesses: `tests/contract/conversation_flow/` and `tests/contract/structured_output/`. Both follow the Revisable precedent: registration mechanism plus parametrised contract scenarios. No ConversationFlow implementer registers at S45; StructuredOutputPort harness registers the inference adapter as the first implementer.
- HTTP routes for messaging at `apps/api/routers/messaging.py`: POST `/api/v1/messaging/send` (outbound); POST `/api/v1/messaging/inbound` (Twilio WhatsApp webhook receiver with X-Twilio-Signature verification; route synthesises operator ActorContext after signature verification per the CLI synthesis precedent because Twilio webhooks carry no Padhanam Principal); GET `/api/v1/messaging/messages/{message_id}`; GET `/api/v1/messaging/messages`.
- DTOs and query parser at `apps/api/routers/_messaging_dto.py` and `apps/api/routers/_messaging_query.py` (paths corrected from original brief per pre-write reconciliation surface 6).
- New `apps/api/_messaging_errors.py` carrying messaging-specific HTTP error handlers per the redirect-away-from-over-budget-files discipline (`apps/api/_errors.py` at 828 lines per S44a finding is not extended).
- Four new permissions added to `shared_kernel/authorisation.py`: `messaging.message.send`, `messaging.message.receive`, `messaging.message.get`, `messaging.message.list`. Operator role authorisation_set grows from eight to twelve.
- Composition root wiring at `apps/api/main.py` registering the messaging router. Cross-context wiring at `apps/api/_messaging_wiring.py` (new file per the S44b `_intake_wiring.py` precedent) carrying messaging adapter selection (MESSAGING_ADAPTER environment variable; default `local_echo`; production `twilio`), use case factories, orchestration factories, plus the MessageWriter adapter that the intake orchestration consumes. Adapter selection via a new MessagingSettings Pydantic class at `padhanam/config/messaging.py` mirroring the existing InferenceSettings precedent.
- Webhook signature verification helper at the Twilio adapter; invoked by the webhook receiver before processing.
- Three new D-entries at `charter/decisions.md`: D129 (messaging substrate plus orchestration plus LocalEcho adapter; references existing D119 for channel and vendor commitments rather than restating them), D130 (structured-output shared_kernel primitive plus inference adapter extension), D131 (provenance-aware response composition as the read-side counterpart to D128's intake-canonical write-side commitment).
- Architecture.md prose addition adjacent to the existing intake commitment (provenance-aware response composition framing).
- Schema additions at `charter/schema.md`: Message field table; messages table schema; intake_source enum extension with WHATSAPP_INBOUND; structured-output value object shapes; ConversationFlow value object shapes.
- Two new entries at `charter/deferred-decisions.md`: structured-output-to-agent-tool convergence; multi-channel UX architectural readiness.
- New entry at `log/captures.md`: forward strategic block for the UX convergence session, sitting between S46 close and P14 epic framing.
- `charter/packages/p13-epic.md` Session 3 line updates.
- `charter/current-package.md` transitions.
- Live-stack smoke at `docs/smoke/p13_s45_messaging_substrate.md` walking the full propagation paths end-to-end against tenant_a on freshly-rebuilt image with 0019 and 0020 migrations deployed. Smoke includes Twilio WhatsApp Sandbox setup as stage 0.
- Test deltas across domain, application, adapter, HTTP, and contract layers per the precedents from S44a and S44b.
- Session log entry at `log/sessions.md`.
- `briefs/p13/s45.md` preserved (this updated brief).

S45 does not ship: SMS, voice, email channel adapters (all defer to P14+ per the channel enum extension trigger); ConversationFlow implementations at audit or portfolio contexts (defer to future packages); Gate entity from D116 (defers to P14); learned-pattern-based salience per D117 (defers to Phase 2-B Cluster B4); information-vector decay per D118 (defers to Phase 2-B Cluster B3); WABA migration (defers to Phase 2-B+ at first non-operator customer arrival); CLI messaging commands (out of scope per pre-write reconciliation surface 5); multi-channel UX implementation; provenance-aware response composition implementation (D131 commits the architectural posture; first implementation at P14 when ConversationFlow implementers land).

## Pre-write reconciliation outcomes (from the prior cycle)

The eight pre-write reconciliation surfaces were verified in the prior cycle and their findings dispositioned at the Claude.ai disposition conversation. Summary:

1. **D-number sequence**: D119 already taken (WhatsApp via Twilio for Phase 2). Next free numbers D129 onward. Assigned: D129 messaging substrate, D130 structured-output, D131 provenance-aware response composition.
2. **Channel commitment**: WhatsApp throughout per D119, p13-epic, and S46 substrate dependency. SMS in the original brief was a load-bearing miss; corrected throughout this brief.
3. **Composition root**: confirmed; MessagingSettings Pydantic class at `padhanam/config/messaging.py`.
4. **Orchestration placement**: settled at intake per D127(d) ("intake-then-X orchestrations remain the same pattern and stay at contexts/intake/"). MessageWriter consumer port at intake; messaging context exposes plain use case; apps/ provides the adapter.
5. **CLI scope**: deferred (out of scope for S45).
6. **File topology**: budget holds; path corrections applied (`apps/api/routers/_messaging_dto.py` not `apps/api/_messaging_dto.py`); new `_messaging_errors.py` instead of growing `_errors.py`.
7. **Type-identifier collisions**: Message exists at `contexts/inference/domain/completion.py` (chat-completion message); messaging aggregate Message is in a distinct bounded context with no cross-context import path; no structural collision.
8. **Cross-context contracts**: option (a) placement keeps contracts clean. Twilio import-linter confinement: add twilio-confined plus twilio-confined-apps pair for structural consistency with the litellm/langfuse/neo4j precedent; total contract count grows from 29 to 32.

Session-open should verify nothing has shifted since the disposition conversation (working tree clean, no intervening commits). If anything has changed, surface to operator before commit 1 drafts.

## Charter updates ahead of code (commit 1)

Single commit, conventional-commit subject:

```
docs(p13/s45): D129 messaging substrate, D130 structured-output, D131 provenance-aware response composition; p13-epic Session 3; schema additions; deferred-decisions and captures for forward UX work
```

Files updated at commit 1: `charter/decisions.md` (D129, D130, D131); `charter/architecture.md` (provenance-aware response composition prose); `charter/packages/p13-epic.md` (Session 3 line); `charter/schema.md` (Message field table, messages table, intake_source enum extension, structured-output and ConversationFlow value objects); `charter/deferred-decisions.md` (two new entries); `log/captures.md` (UX convergence forward strategic block); `charter/current-package.md` (S45 in flight); `briefs/p13/s45.md` (this updated brief).

## Substantive work (commits 2 onward)

Twelve build commits. Detailed commit-by-commit shapes follow the established precedent from S44a and S44b.

- **Commit 2**: messaging domain layer plus import-linter `layers-messaging` contract. Message aggregate root plus MessageDirection/MessageChannel/MessageStatus enums. `contexts.messaging.*` added to the three cross-context independence contracts and the domain-purity contract.
- **Commit 3**: messaging ports plus Postgres adapter plus Alembic 0019. MessageRepository and MessageDeliveryPort ports; Postgres adapter with bound-tenant-id pattern; Alembic 0019 messages table per-tenant; contract tests.
- **Commit 4**: Twilio and LocalEcho message delivery adapters plus `twilio-confined` plus `twilio-confined-apps` import-linter contracts. Twilio adapter via Twilio Python SDK (WhatsApp via `whatsapp:`-prefixed addresses) including `verify_twilio_signature` helper; LocalEcho adapter.
- **Commit 5**: messaging use cases (send_message, get_message, list_messages, record_inbound_message), four new permissions at `shared_kernel/authorisation.py`, MessagingSettings Pydantic class at `padhanam/config/messaging.py`.
- **Commit 6**: Alembic 0020 (`0020_intake_source_whatsapp`) extending intake_source with WHATSAPP_INBOUND; IntakeSource enum update; ManualEntryPayload reuse for inbound payloads.
- **Commit 7**: inbound orchestration `record_intake_and_record_inbound_message` at the intake context plus MessageWriter consumer port at `contexts/intake/application/ports/`.
- **Commit 8**: `shared_kernel/conversation_flow.py` ConversationFlow Protocol plus five value objects; `shared_kernel/structured_output.py` structured-output discipline.
- **Commit 9**: inference adapter implements StructuredOutputPort additively.
- **Commit 10**: contract harnesses `tests/contract/conversation_flow/` (no implementer) and `tests/contract/structured_output/` (inference adapter registered).
- **Commit 11**: messaging HTTP routes, DTOs, query parser, `_messaging_errors.py`, `_messaging_wiring.py` (including the MessageWriter adapter), composition-root registration, tenant-isolation contract scenarios.
- **Commit 12**: live-stack smoke `docs/smoke/p13_s45_messaging_substrate.md` with WhatsApp Sandbox setup as stage 0.
- **Commit 13**: session log entry plus `charter/current-package.md` close marker.

## Acceptance criteria

1. `charter/decisions.md` carries D129, D130, D131 with their alternatives-considered and Kano sections.
2. `charter/architecture.md` carries the provenance-aware response composition prose.
3. `charter/packages/p13-epic.md` Session 3 line updated.
4. `charter/schema.md` carries messaging schema, intake_source enum extension with WHATSAPP_INBOUND, structured-output value objects, ConversationFlow value objects.
5. `charter/deferred-decisions.md` carries the multi-channel UX readiness entry and the structured-output-to-agent-tool convergence entry.
6. `log/captures.md` carries the UX convergence forward strategic block entry.
7. `charter/current-package.md` transitions cleanly across commits 1 and 13.
8. `contexts/messaging/` bounded context exists with domain entity, two ports, two delivery adapters (Twilio plus LocalEcho), Postgres persistence adapter, four use cases.
9. `contexts/intake/application/record_intake_and_record_inbound_message.py` exists with dual decorators and MessageWriter consumer port at `contexts/intake/application/ports/`.
10. Alembic 0019 creates messages table; Alembic 0020 extends intake_source with WHATSAPP_INBOUND.
11. `shared_kernel/conversation_flow.py` and `shared_kernel/structured_output.py` exist with the three new value objects plus two Protocols per the framing.
12. Inference adapter implements StructuredOutputPort additively.
13. Two contract-test harnesses exist; ConversationFlow with no implementers; StructuredOutputPort with inference adapter registered.
14. Four messaging HTTP routes pass integration tests; webhook signature verification tested for valid and invalid signatures; cross-tenant scenarios pass; webhook synthesises ActorContext correctly.
15. `shared_kernel/authorisation.py` carries twelve permissions for operator role.
16. AST enforcement passes; import-linter contracts grow from 29 to 32 (adds layers-messaging, twilio-confined, twilio-confined-apps); cross-context contracts hold.
17. File topology budget verification confirms projected sizes.
18. Live-stack smoke walks the stages including Twilio Sandbox setup and signature verification scenarios.
19. Session log entry appended; current-package transition committed.

## Out of scope explicitly

SMS/voice/email channel adapters; ConversationFlow implementations; Gate entity (D116); learned-pattern-based salience (D117); information-vector decay (D118); Twilio MCP server integration; WABA migration; CLI messaging commands; multi-channel UX implementation; provenance-aware response composition implementation; confidence-aware response composition; the UX convergence session itself; methodology document promotions; YAGNI monitoring discipline addition.

## Session log entry instruction

After commit 12, append the S45 entry to `log/sessions.md` at commit 13. Use real SHA for prior commit plus "this commit" for self-reference. Transition `charter/current-package.md` close marker within commit 13.
