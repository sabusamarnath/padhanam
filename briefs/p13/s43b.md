# P13 — S43b — Portfolio context HTTP transport surface plus live-stack smoke

## Bridge-session classification

S43b is a *planned* bridge session at the substrate-and-transport split. The prior bridge instances at P11 (S39 → S39b verification-and-hygiene; S40 → S40b methodologically-clean-artefact authoring) were *unplanned* corrections discovered at substrate close. S43b is the first instance of a deliberate substrate-and-transport split taken because the substrate alone consumed full session scope.

This sub-variant of the bridge-session pattern is first-instance at S43b close. Promotion to a methodology line requires a second instance at a future bounded-context substrate session. The session log entry captures the candidate; no methodology.md commit lands at S43b.

## Goal stated as artefacts at session close

At session close the repository carries:

- `apps/api/routers/portfolio.py` plus `_portfolio_dto.py` plus `_portfolio_query.py` carrying two GET routes (`/api/v1/portfolio/cases`, `/api/v1/portfolio/cases/{case_id}`) wired through `get_tenant_context` and the use case layer.
- HTTP-level wiring at the API composition root (`AppCompositions` field plus `_agent_runtime_wiring.py` factory) per the established pattern.
- HTTP error handlers registered per the eleven-path map from D98.
- HTTP contract tests at `tests/contract/http/portfolio/` covering cursor pagination plus error-response body shape canonicalisation per the S42 precedent.
- Integration tests at `tests/integration/api/` covering the full read-surface lifecycle against in-memory and live-stack fakes.
- HTTP-level tenant-isolation contract scenarios extending the harness from the post-S43 count (verify at pre-write reconciliation). Cross-tenant HTTP attempts return 404 plus security event; same-tenant queries succeed.
- Image rebuild plus migration verification confirming the 0016 portfolio migration is applied to both tenant containers.
- Live-stack smoke exercising the full Case-DataPoint-Assertion chain end-to-end via CLI write plus HTTP read against tenant_a, with audit events appearing for each write.
- `log/sessions.md` carries the S43b session log entry.
- `charter/current-package.md` transitions from "S43 closed at 7/10 commits per substrate-and-transport split; S43b lands HTTP transport plus live smoke" to "S43b closed; S44 next."

No new D-entries land at S43b. D124 (portfolio context schema plus entities) and D125 (Revisable Protocol shape) already commit the architectural surface. The HTTP transport pattern is mechanical absorption from the run_history and optimization router precedents.

## Pre-write reconciliation (verify against current sources before code lands)

Six surfaces to verify before any code commits:

1. **S43 substrate state.** Verify `contexts/portfolio/` carries the seven-commit substrate landed at S43 (commits d0ada39 through 3ddff81). Verify the application layer's use cases and the CLI subcommand are present and tested. Verify import-linter at 28 of 28 contracts kept. Surface any drift since S43 close as a session-open question.

2. **HTTP router pattern.** Verify the existing router precedent at `apps/api/routers/run_history.py` and `apps/api/routers/optimization.py` for DTO shape, query parser pattern, error handler registration, dependency injection of `get_tenant_context`, and route prefix convention. The portfolio router mirrors this pattern mechanically. Surface any divergence as a session-open question.

3. **AppCompositions plus _agent_runtime_wiring.py shape.** Verify the current composition root pattern at `apps/api/main.py` (or equivalent). The optimization wiring landed at S41 as composition-root entries #15-#18; the portfolio wiring extends this. Verify the convention for adding a new context wiring slot, including the factory pattern in `_agent_runtime_wiring.py`.

4. **Error handler registration.** Verify the eleven-path error response map from D98 is registered at the API composition root. The portfolio router's error responses map to the existing path families; no new error code families land at S43b.

5. **Image rebuild plus migration runner.** Verify the `make build-api` target and the `make migrate` target. Verify the `padhanam-api` image build inputs include the contexts/portfolio/ tree (the COPY directive at `apps/api/Dockerfile` should pick up the new context per the S39 retrieval_evaluation precedent). Verify the migration runner applies 0016_portfolio_substrate to both tenant_a and tenant_b containers. The S43 commit 9 deferral on the structural-honesty deployment check resolves at this surface.

6. **HTTP contract test harness.** Verify the test harness conventions at `tests/contract/http/` including cursor pagination canonicalisation tests and error-response body shape tests per the S42 precedent. Verify the tenant-isolation HTTP scenario shape from the S41 close (cross-tenant HTTP request returns 404 plus security event; same-tenant succeeds).

Findings surface as questions before any code commits. The discipline is unchanged from S36 onward: ask before adjusting shape, do not silently adapt.

## Charter updates ahead of code (commit 1)

Single commit, conventional-commit subject:

```
docs(p13/s43b): current-package transition; brief preserved
```

Files updated at commit 1:

1. `charter/current-package.md` — transition the S43 close marker to "S43b in flight."
2. `briefs/p13/s43b.md` — preserve this brief at its canonical path for audit-trail reference.

No new D-entries. No schema.md additions. No principles.md additions. The S43b scope is mechanical extension of established patterns.

## Substantive work (commits 2 onward)

### Commit 2: HTTP transport surface

```
feat(p13/s43b): portfolio HTTP transport surface (read-side)
```

Add `apps/api/routers/portfolio.py` carrying two route trees:

- `GET /api/v1/portfolio/cases` — list cases for the authenticated tenant. Query params: `case_type`, `status`, `cursor`, `page_size`. Returns paginated CaseListDTO. Cursor encoding per the canonical cursor codec from `charter/schema.md` cross-cutting binding shapes section. Error responses per the eleven-path map from D98.
- `GET /api/v1/portfolio/cases/{case_id}` — case detail with embedded data points and latest assertions. 404 for not-found-in-tenant. Cross-tenant returns 404 plus security event per S34 convention.

DTOs at `apps/api/routers/_portfolio_dto.py`: `CaseDTO`, `CaseListDTO`, `DataPointDTO`, `AssertionDTO` per the run_history precedent.

Query parser at `apps/api/routers/_portfolio_query.py` parsing `case_type`, `status`, `cursor`, `page_size` per the S42 query parser pattern. Validation errors map to D98 error-code family.

Wiring extends the API composition root: `AppCompositions` field for portfolio plus `_agent_runtime_wiring.py` factory entry. Error handler registration follows the existing pattern; no new error code families land.

Unit tests at `tests/unit/apps/api/routers/test_portfolio.py` cover happy paths, validation paths, cursor encoding, error response shapes against fake repositories.

### Commit 3: HTTP contract tests plus tenant-isolation scenarios

```
feat(p13/s43b): portfolio HTTP contract tests plus tenant-isolation scenarios
```

Add `tests/contract/http/portfolio/` carrying:

- Cursor pagination canonicalisation tests (encode-decode round-trip; `next_cursor` opacity; pagination across boundaries) per the S42 precedent.
- Error-response body shape tests covering all D98 paths the router can return.
- Tenant-isolation HTTP scenarios (cross-tenant attempts return 404 plus security event; same-tenant queries succeed) extending the harness count from the post-S43 baseline. Scenario count verifies at pre-write reconciliation surface 6.

Integration tests at `tests/integration/api/test_portfolio_lifecycle.py` exercise the full read-surface lifecycle against an in-memory composition: create Case via use case; create DataPoint via use case; revise DataPoint via use case; GET on the case detail endpoint returns the case plus the data point plus both assertions in revision-history order.

All tenant-isolation contract scenarios pass. Cumulative tenant-isolation contract count update verifies at acceptance criteria.

### Commit 4: Image rebuild plus migration verification plus live-stack smoke

```
feat(p13/s43b): live-stack smoke against tenant_a with image rebuild plus migration verification
```

Sequence:

1. `make build-api` rebuilds the `padhanam-api` image picking up the contexts/portfolio/ tree per the Dockerfile COPY directive.
2. Container recreate via `docker compose up -d --force-recreate padhanam-api` (or equivalent per the established convention).
3. `make migrate` applies the 0016_portfolio_substrate migration to both tenant_a and tenant_b containers. Verify the three portfolio tables (`cases`, `data_points`, `assertions`) exist in both tenant databases. The structural-honesty deployment check deferred at S43 commit 9 lands here.
4. Multi-stage smoke against tenant_a:
   - CLI creates a Case (`padhanam portfolio create-case`) and records the case_id.
   - CLI creates a DataPoint with an INITIAL Assertion (`padhanam portfolio create-data-point`) and records the data_point_id.
   - CLI revises the DataPoint (`padhanam portfolio revise-data-point`) producing a REVISION Assertion.
   - HTTP GET on `/api/v1/portfolio/cases` lists the case via the read surface.
   - HTTP GET on `/api/v1/portfolio/cases/{case_id}` returns the case plus the data point plus both assertions in revision-history order.
   - Audit-context query confirms audit events appear for each write per the P3 audit-context convention.

The smoke produces a real Case-DataPoint-Assertion chain exercising the full substrate end-to-end including the Revisable Protocol contract at runtime against a live container stack with a freshly-rebuilt image and a freshly-applied migration.

Smoke output records the case_id, data_point_id, and assertion_ids for citation in the session log entry.

### Commit 5: Session log entry

```
docs(p13/s43b): session log entry plus current-package close
```

Append the S43b entry to `log/sessions.md` per the established shape. Include `roles:` tag (architect, engineer, technical writer). Mode: build (bridge session). Substantive paragraphs addressing the reflection prompts below. Transition `charter/current-package.md` from "S43b in flight" to "S43b closed; S44 next."

The session log entry's structural shape mirrors S39b and S40b but explicitly identifies the bridge-session sub-variant: planned substrate-and-transport split, not unplanned correction work.

## Acceptance criteria

1. `charter/current-package.md` transitions cleanly across commit 1 and commit 5.
2. `briefs/p13/s43b.md` preserved at its canonical path.
3. `apps/api/routers/portfolio.py` plus `_portfolio_dto.py` plus `_portfolio_query.py` carry two GET routes wired through `get_tenant_context` and the use case layer with passing unit tests.
4. HTTP-level wiring at the API composition root extends `AppCompositions` and `_agent_runtime_wiring.py` per the established pattern.
5. Error handlers registered for all D98 paths the portfolio router can return.
6. HTTP contract tests at `tests/contract/http/portfolio/` cover cursor pagination canonicalisation plus error-response body shape plus tenant-isolation scenarios.
7. Integration tests at `tests/integration/api/test_portfolio_lifecycle.py` exercise the full read-surface lifecycle.
8. `make build-api` rebuilds the `padhanam-api` image successfully.
9. `make migrate` applies the 0016 migration to both tenant_a and tenant_b containers; verification confirms the three portfolio tables exist in both databases.
10. Live-stack smoke against tenant_a completes successfully: CLI creates Case plus DataPoint plus REVISION Assertion; HTTP GET on the case detail endpoint returns the full chain in revision-history order; audit events appear for each write.
11. Import-linter at 28 of 28 contracts kept (no contract changes at S43b; verify at pre-write reconciliation).
12. AST enforcement passes.
13. All previously-passing tests still pass; the new tests added at S43b pass.
14. Session log entry appended; current-package transition committed.

## Reflection prompts addressed in the session log entry

1. **Pre-write reconciliation surfaces.** What did the six reconciliation surfaces reveal? Mechanical absorptions versus structural-honesty findings versus user-question resolutions. Specifically surface the migration-deployed-to-tenant-containers structural-honesty check outcome at reconciliation surface 5. Cumulative reconciliation count update.

2. **Planned-bridge-session sub-variant, first-instance evidence.** S43b is the first instance of a deliberate substrate-and-transport split at a bounded-context introduction session. The two prior bridge instances (S39 → S39b verification-and-hygiene; S40 → S40b methodologically-clean-artefact authoring) were unplanned corrections discovered at substrate close. Did the planned-bridge sub-variant produce signal worth promoting to a methodology line at second-instance threshold? Specifically: did the substrate work at S43 plus the transport work at S43b feel more controlled, less fatigue-taxed, and structurally cleaner than a single ten-commit session would have? First-instance evidence captured at S43b close; second-instance threshold at a future bounded-context substrate session.

3. **Image-rebuild structural-honesty check outcome.** The S43 commit 9 deferral on whether the 0016 migration was actually deployed to the real tenant containers resolved at S43b commit 4. What did the check reveal? If the image was indeed stale at S43 close, that is a methodology data point on substrate-completion-versus-deployment honesty. The pattern (migration committed to source tree but not deployed to running containers) is the kind of drift the structural-honesty discipline exists to catch.

4. **Operator-pause-at-substrate-close as methodology candidate.** S43 closed at 7/10 commits at operator decision after the Claude Code check-in surfaced the remaining commits' scope. The S41 mid-execution scope check at the substrate-application boundary was a different shape (mid-session pause). S43's pause was at session-close boundary (substrate complete; HTTP transport remaining). The two shapes (mid-session boundary pause; session-close boundary pause) are sub-variants of the same scope-versus-fatigue-management discipline. Worth flagging as paired methodology candidates: scope checks at substrate-application boundaries (mid-session) plus scope checks at substrate-completion boundaries (session-close). Second-instance promotion threshold for each sub-variant.

5. **HTTP transport mechanical-absorption verification.** S43b's HTTP transport followed the run_history and optimization router precedents. Were the precedents truly mechanical absorption, or did pre-write reconciliation surface drift between the brief's assumed shape and the actual codebase convention? If drift surfaced, the methodology candidate from S43 (brief drafts naming adapter paths must reconcile against actual `adapters/outbound/{vendor}/` convention before commit 1) extends to router paths and composition-root patterns.

## Out of scope explicitly

- Write-side HTTP routes for portfolio (POST `/portfolio/cases`, PATCH `/portfolio/data_points/{id}`). Defers to S44.
- ActorContext extension replacing the ActorReference placeholder. Defers to S44.
- Authorisation decorator. Defers to S44.
- Intake record. Defers to S44.
- Messaging substrate. Defers to S45.
- Manual entry cell. Defers to S46 (or S47 per session ordering verified at S45 close).
- Latency-tier routing extension. Defers to S46 bridge session.
- Four-layer model ontology shape. Defers to S46 bridge session.
- Structured output object. Defers to S45.
- Two-vector decay and tiered-by-salience architecture-primitive charter commits. Defer to S45 (alongside ConversationFlow Protocol).
- Three-tier consent-and-awareness architecture-primitive charter commit. Defers to S44 (alongside authorisation decorator).
- Methodology.md commit for the planned-bridge-session sub-variant. First-instance at S43b; promotion threshold at second instance.

## Session log entry instruction

After commit 4, append the S43b entry to `log/sessions.md` per the conventions established at S39b and S40b. The entry explicitly identifies the bridge-session sub-variant as *planned* substrate-and-transport split. Use real-SHA-for-prior-commit plus "this commit" for the session log entry's self-reference per the established discipline.

The entry's `metrics:` block carries the bridge-session classification field:

```
metrics:
  classification: planned-bridge-session (substrate-and-transport split)
  brief_started: [DATE]
  session_started: [DATE]
  session_closed: [DATE]
  merged: [DATE]
  close_state: clean
  tests_passing: yes
  principles_intact: yes
  charter_touchpoints: charter/current-package.md (S43b close marker); briefs/p13/s43b.md
  corrects:
  corrected_by:
```

The `corrects:` field is empty because S43b does not correct prior work; it completes the planned substrate-and-transport split. The classification field distinguishes this instance from the verification-and-hygiene and methodologically-clean-artefact-authoring sub-variants previously evidenced.

---

## Operator amendments at S43b open (recorded for audit trail)

Two loose-end resolutions landed at S43b open, after the brief above was drafted:

1. **current-package.md state.** The brief's commit 1 "transition the S43 close marker" assumed a clean S43 close that did not happen — S43 paused at 7/10 with `current-package.md` reading "S43 in flight". The actual commit-1 reconciliation appends two consecutive markers: the S43 close marker (substrate-and-transport split) and the S43b in-flight marker.

2. **Retroactive S43 session log entry.** The seven-commit S43 substrate is too substantive to fold into the S43b entry's narrative. Per the S39/S39b precedent each half gets its own entry. The retroactive S43 entry lands at S43b **commit 1** (with the current-package housekeeping), not commit 5, freeing commit 5 to be S43b's own entry without conflation. Commit 1's subject becomes `docs(p13/s43b): retroactive S43 session log entry; current-package S43-close plus S43b-in-flight markers; brief preserved`. The S43 and S43b entries name each other via a new `paired_with` metrics field (S43b does not *correct* S43; it completes the planned split). The `metrics:` block is a new session-log-entry shape introduced at S43/S43b — no prior entry carries one.
