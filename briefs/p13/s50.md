# S50 — Resolver disambiguation on duplicate-title cases

Short operational-substrate build session closing the S48a forward
finding on resolver behaviour when multiple cases share the same
title. Two commits: feat commit adding multi-match resolution with
discriminator-aware rendering plus tests; docs commit landing the
live-stack smoke against tenant_a's three existing "Q3 portfolio
review" cases plus the captures-entry recording the resolution-
ambiguity disposition plus session-log close.

No new D-entries. No new bounded contexts. The cell's behaviour
change is an application of the existing D134 confidence-aware
composition primitive at a sub-case (resolution-ambiguity routes
to the same shape-aware clarification surface as medium-confidence
intent-ambiguity). The architectural commitment is small enough
that captures-entry disposition suffices; architecture.md prose
addition deferred to P14 framing if ConversationFlow implementers
surface a need for explicit cross-implementer commitment language.

## Package and session identification

- **Package.** P13 (close hygiene, post-S48, post-S49, operational-
  substrate altitude).
- **Session.** S50.
- **Mode.** Build session producing operational substrate at two
  commits.
- **Predecessor.** S48a (operational unblock at `gpt-4o-mini`; the
  smoke surfaced three "Q3 portfolio review" cases accumulated in
  tenant_a from S46/S47/S48a cascades; the resolver currently
  resolves to one match without surfacing the disambiguation
  question).
- **Successor.** P14 epic framing.

## Goal at session close

Four artefacts exist at session close.

1. The manual entry cell's case-resolution layer extended to handle
   multi-match cases: when the operator's input matches multiple
   existing cases by significant-token-overlap, the cell renders a
   disambiguation clarification surfacing each candidate with
   disambiguating signals (creation time, last activity, data-point
   count) rather than silently selecting one.

2. PendingClarification persists the resolution-ambiguity state per
   D134's existing entity shape (no schema change); the operator's
   reply ("1", "2") resolves the pending and executes the proposed
   action against the selected case. (S50 framing decision A:
   positional only at Phase 2-A; descriptive forms defer.)

3. Live-stack smoke at tenant_a verifies the cascade end-to-end
   against the three existing "Q3 portfolio review" cases.

4. `log/captures.md` carries a captures entry recording the
   resolution-ambiguity-routes-to-shape-aware-clarification
   disposition. `log/sessions.md` carries the S50 close entry.
   `charter/current-package.md` carries the S50 close marker.

## Context to read first

Read in this order.

1. `log/captures.md` — the S48a forward finding entry on resolver
   disambiguation. The finding identifies three discriminators
   worth surfacing (created_at, last-activity, data-point count).
2. `contexts/messaging/application/manual_entry_cell.py` — the
   cell's current resolution logic.
3. `contexts/messaging/application/ports/portfolio_gateway.py` —
   the case-lookup port the cell consumes for resolution.
4. `contexts/portfolio/domain/case.py` — the Case entity carrying
   `created_at` directly. Last-activity and data-point count are
   computed surfaces, not Case fields.
5. `contexts/messaging/domain/pending_clarification.py` — the
   entity's current shape. Resolution-ambiguity uses the existing
   shape via a `resolution_candidates` sidecar in `proposed_intent`
   that `parse_intent` ignores.
6. `charter/decisions.md` — D134 (confidence-aware composition;
   the existing three-case discipline that resolution-ambiguity
   extends as a sub-case of Case 2's shape-aware clarification).

## Pre-write reconciliation surfaces

Six surfaces apply. The interface-versus-implementation surface
from S49's methodology promotion binds standing forward; S50 is the
first session that applies it.

1. **Resolver placement verification.** The cell's significant-
   token-overlap matching lives at `contexts/messaging/application/
   resolve_target.py`. The case-lookup port's current shape
   determines the extension surface. Interface-versus-implementation
   check binds: commit the port's extension as the interface change
   (the port returns multi-match shape); the cell's handling is the
   implementation behind the port.

2. **Discriminator availability verification.**
   - `created_at` lives on the Case entity.
   - Last-activity timestamp: MAX(data_point.created_at) per case,
     falling back to Case.created_at if no data points.
   - Data-point count via the existing reader.
   - Computation surface trade-off: eager (compute always in
     find_cases, N+1 case-detail loads per AddDataPointIntent) vs
     lazy (compute only on AMBIGUOUS). Recommended eager at Phase
     2-A scale.

3. **PendingClarification reuse verification.** Hypothesis: the
   existing `proposed_intent: dict[str, Any]` plus
   `proposed_action_summary: str` accommodate the sub-case without
   schema change. Verify at framing.

4. **Path-naming verification.** Per S49's promoted methodology.
5. **Disambiguation rendering verification against pattern 2.**
   The shape-aware clarification surface honours suggestion-as-
   question; phrase as a question.
6. **(Standing surface from S49) Interface-versus-implementation
   check.** Apply at every architectural-commitment-shape sentence
   in the brief and at commit-draft time.

## Framing decisions (operator-confirmed)

- **Decision A — Operator reply pattern coverage.** Positional only
  at Phase 2-A ("1", "2", "3"). Descriptive forms ("the older one")
  defer to second-instance need at P14 or operator-dogfooding
  signal.
- **Decision B — "Or start a new one?" option.** Defer per the
  captures entry's own framing (detect-at-creation discipline lands
  at P14 symmetrically with CreateCase-side detection).

## Acceptance criteria

1. The case-lookup port returns multi-match results with
   disambiguating signals (creation time, last activity, data-point
   count); single-match case preserves current behaviour exactly.
2. The cell detects multi-match and routes to D134's shape-aware
   clarification surface; renders each candidate numbered with
   discriminators; phrases as a question per pattern 2.
3. PendingClarification persists the resolution-ambiguity state
   with no schema change.
4. The cell's reply-handling interprets positional integer
   selections; the pending resolves; the proposed action executes
   against the selected case; the cited confirmation reply renders
   per D131.
5. Live-stack smoke against tenant_a's three "Q3 portfolio review"
   cases verifies the cascade end-to-end.
6. `log/captures.md` carries the S50 captures entry.
7. `log/sessions.md` carries the S50 session-log entry.
8. `charter/current-package.md` carries the S50 close marker.
9. No new D-entries land at S50.
10. No architecture.md prose addition at S50.
11. All existing tests pass; new tests for the multi-match cascade
    pass.
12. 33/33 import-linter contracts hold.

## Out of scope

- Architecture.md prose addition for resolution-ambiguity disposition
  (defer to P14 second-instance trigger).
- Cross-implementer resolution-ambiguity contract (P14).
- Cancel-then-fresh-turn double-cost finding from S48a (separate
  captures; future hygiene).
- Auto-expire-prior-pending positive observation from S48a (no S50
  action needed).
- Confidence-calibration substrate (Phase 2-B+).
- Multi-tenant resolution-ambiguity at scale (Phase 2-B+).
- Per-tenant disambiguator-set configuration (future Phase).
- DataPoint resolution-ambiguity multi-match disposition (build-at-
  second-instance discipline; activates if operator dogfooding
  surfaces duplicate-label data points).

---

End of S50 prompt (preserved per D43 briefs/ discipline).
