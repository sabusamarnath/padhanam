# S49 — Post-S48 hygiene: methodology promotions and captures formalizations

Charter-only strategic-mode hygiene session sized at two commits.
The session promotes three methodology candidates from accumulated
evidence to `charter/methodology.md`'s Patterns observed section and
formalizes two captures observations. No substrate work; no code
commits; no D-entries; no new tests. The session inherits the
S48a/S48b parallel-thread close inventory and closes the discipline-
altitude work before the resolver-disambiguation build session and
P14 epic framing open.

## Package and session identification

- **Package.** P13 (close hygiene, post-S48, charter-only).
- **Session.** S49.
- **Mode.** Strategic-mode hygiene producing charter commits only.
  Two commits: methodology.md promotions first, then captures
  formalizations plus session-log close.
- **Predecessors.** S48a close (operational unblock at
  `gpt-4o-mini`); S48b close (component-quality substrate at D137);
  the convergence session settlement at S47.
- **Successors.** Short resolver-disambiguation build session
  closing the S48a duplicate-titles finding, then P14 epic framing.

## Goal at session close

Four artefacts exist at session close.

1. `charter/methodology.md` carries three new sub-sections under
   the existing **Patterns observed** section:
   - Interface-versus-implementation discipline (seven instances
     across three surfaces).
   - Component-quality-versus-integration-smoke discipline (two-
     instance evidence plus first structural implementation D137).
   - Structural-test single-source-of-truth binding (two-instance
     evidence).
2. `log/captures.md` carries two new formalized entries:
   - Scope-discipline-at-brief-authoring at first-instance
     observation.
   - Gold-set authoring plus confidence-aware composition
     connection observation.
3. `log/captures.md` carries resolution-line updates on the
   captures entries that the three methodology promotions
   absorb, marking each as resolved at S49 via promotion.
4. `log/sessions.md` carries the S49 close entry; `charter/current-package.md`
   carries the S49 close marker.

## Context to read first

Read in this order.

1. `charter/methodology.md` — focus on the **Patterns observed**
   section (lines 305 forward in the version after the most recent
   P12-audit promotions). The three new sub-sections mirror the
   existing entries' style: dated header, "What recurs" paragraph,
   "Recurrence count" paragraph, mechanism or mitigation
   paragraph, forward-relevance paragraph.
2. `log/captures.md` — the entries that motivate the three
   promotions:
   - "[S47] Interface-versus-implementation framing drift" at five-
     instance evidence with the pattern-density observation; updated
     at the S47 addendum to five instances; the S48a session log
     extended to seven instances across three surfaces.
   - "[S48b] Component-quality evaluation distinct from integration
     smoke" at two-instance evidence with the structural-
     implementation reference to D137.
   - The structural-test SSOT binding observation from the S47
     addendum (no-numeric-threshold-literals AST test) and from the
     S48b commit 2 session log (symbol-identity test for cell-
     runner shared prompt).
3. `charter/decisions.md` — D137 (the structural implementation of
   the component-quality discipline) and D134 (the confidence-aware
   composition primitive that connects to gold-set authoring).
4. `log/sessions.md` — S47 close, S47 addendum close, S48a close,
   S48b close entries. Each session-log entry's methodology line
   contributes evidence to the promotions.

## Acceptance criteria

Numbered list. Every criterion is independently verifiable.

1. `charter/methodology.md` carries three new sub-sections under
   **Patterns observed** with the prose specified above (or with
   post-pre-write-reconciliation refinements explicitly recorded
   in the session-log entry).
2. The three methodology entries match the existing Patterns
   observed style (dated header; "What recurs" paragraph;
   "Recurrence count" paragraph; mechanism paragraph; forward-
   relevance paragraph).
3. `log/captures.md` carries the three resolution-line updates on
   the captures entries the promotions absorb.
4. `log/captures.md` carries the two new captures entries
   (scope-discipline-at-brief-authoring; gold-set authoring plus
   confidence-aware composition).
5. `log/sessions.md` carries the S49 close entry.
6. `charter/current-package.md` carries the S49 close marker.
7. No new D-entries land at S49 (verify charter/decisions.md
   unchanged outside of any housekeeping).
8. No code changes, no test changes, no Alembic migrations.
9. All 1776 unit and contract tests pass (baseline from S48b
   close).
10. 33/33 import-linter contracts hold (no new contracts; no
    contract changes).

## Out of scope

Explicit deferral with the sessions where deferred work lands.

- **Resolver-disambiguation on duplicate-title cases.** Closes at
  the next short build session per the post-S48 forward sequence.
  S49 is charter-only; the build work activates separately.
- **Cancel-then-fresh-turn double-cost from S48a forward
  findings.** Stays in captures pending captures-promotion or
  build-session activation at future hygiene work. Not in S49
  scope.
- **Model-registry-vs-LiteLLM-gateway-routing drift structural
  test from S47 smoke.** Stays in captures pending future hygiene
  or build session. Not in S49 scope.
- **D-entries supplementing the promotions.** The three
  methodology entries do not require new D-entries; they reference
  existing D-entries (D133, D134, D137) for their structural
  bindings. Any future D-entry that operationalizes a discipline
  (e.g., D137 operationalizes the component-quality discipline as
  the first structural implementation) lands at its own activation
  session per the standard discipline.
- **P14 epic framing inheritance preparation.** P14 framing
  inherits the three methodology promotions plus the convergence
  commitments at D133-D136 plus the substrate at D137 plus
  whatever the resolver-disambiguation build session ships. S49
  produces the methodology surface; P14 framing consumes it.
  Forward sequencing is the strategic-mode block after the
  resolver-disambiguation build session closes.

---

End of S49 prompt (preserved per D43 briefs/ discipline).
