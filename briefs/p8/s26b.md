# S26b — McKinsey 7-Step methodology authoring (control plane)

## Identification

Package: P8 (Agent Runtime)
Session: S26b
Mode: build
Branch: operator-selected at session open

## Goal at session close

The McKinsey 7-Step methodology and its seven role aggregates exist in the control-plane Postgres at deterministic hashes, authored against D86's role-first model and D87's per-field override-mode framing. The brief at `briefs/p8/mckinsey-7-step.md` is the content source; D87 is the substrate refinement that surfaced at pre-write reconciliation. Artefacts present at session close:

- D87 commits the override-mode space (augment, replace, tighten), the per-field default-mode table, the structured on-disk shape for `RoleRef.overrides`, the authoring projection from flat to structured, and the validation rule.
- The methodology context's substrate carries `RoleRef.overrides` as `dict[str, dict[str, Any]]` with the canonical `{mode, value}` shape. The parser at the methodology authoring path accepts flat values and projects per the default-mode table.
- Seven `role_templates` rows plus seven `role_revisions` rows (revision 1) on control-plane Postgres for ProblemFramer, Disaggregator, Prioritiser, Planner, Analyst, Synthesiser, Communicator, each carrying the constraint bundle the brief specifies.
- One `methodology_templates` row plus one `methodology_revisions` row (revision 1) for McKinsey 7-Step, with `role_refs` JSONB referencing the seven role templates in the brief's sequential order and carrying per-role overrides on `system_prompt` only, mode `augment`, value verbatim from the brief's overrides table.
- Hash chains intact. Each role's revision 1 chains from `GENESIS_REVISION_HASH` over the role's payload. The methodology's revision 1 chains from `GENESIS_REVISION_HASH` over `name + description + role_refs` sorted by `role_id`, with overrides in canonical structured form.
- Integration test exercising end-to-end resolution against the live control-plane Postgres: the methodology resolves through `MethodologyLookupAdapter`, yields seven `RoleView` instances matching the brief, and each role-ref's overrides expose the structured `{mode, value}` shape. Each role's hash matches the use-case-path computation (golden-hash assertion).
- `briefs/p8/s26b.md` preserves this session prompt verbatim. `briefs/p8/mckinsey-7-step.md` remains the content brief.

## Context to read first

In order. Read in ranges where files exceed 200 lines.

1. `briefs/p8/mckinsey-7-step.md`. Content brief: each role's constraint bundle, the methodology's overrides table, the workflow specification.
2. `charter/decisions.md` entries D74, D75, D79, D86. Methodology aggregate shape, agent lineage, cross-context port pattern, role-first model. D87 lands at this session's commit 1.
3. `charter/current-package.md`. State at S26a close.
4. `charter/schema.md` sections "Methodology tables (control plane)" and "Role aggregate (control plane)". V3 shape post-S26a-1.
5. `charter/principles.md`. The light-UX-as-authoring-discipline rule and the structural-enforcement-of-authoring-discipline rule under Engineering practice.
6. `contexts/methodology/domain/methodology.py` and `contexts/methodology/domain/role.py`. Aggregate dataclass shapes including the current `RoleRef`.
7. `contexts/methodology/application/use_cases.py`. The `_methodology_content_payload` helper and the methodology create-and-update use cases.
8. `padhanam/security/hash_chain.py`. Field-set-agnostic helper promoted at S26a-1 commit 8.
9. `apps/cli/_methodology.py` and `apps/cli/_role.py`. The current YAML config shape the CLI accepts; the parser entry points.
10. Alembic revisions `0005_role_tables`, `0006_methodology_role_refs`, `0007_lvt_split` on the control plane. The v3 substrate to extend; the hash-anchoring pattern from the LVT split.
11. `apps/cli/_cross_context.py`. `MethodologyLookupAdapter`'s role-aware resolution shape, for the integration test.
12. `log/sessions.md` entries S26a-1 and S26a-2. Immediately preceding work; cross-context port pattern; methodology v3 content surface.

## Pre-write reconciliation

The brief is content-shaped; D87 is substrate-shaped. Reconcile both against the codebase before writing migration content. Promote any structural mismatch to a user-question rather than editing the brief in place per the methodology document's no-in-place-brief-edits rule.

1. **Current `RoleRef.overrides` shape.** Inspect `contexts/methodology/domain/methodology.py`. If `overrides` is already a generic `Mapping[str, Any]`, the substrate accommodates D87 without dataclass change and commit 2 lands the parser projection plus validation only. If `overrides` is absent or typed in a way that forbids the structured `{mode, value}` shape, commit 2 also lands the dataclass refactor with backward-compatibility handling for the LVT methodology authored at S25 (LVT has one `role_ref` with empty overrides per S26a-1 commit 5, so the refactor's data impact is zero).

2. **Existing methodology config parser.** Inspect `apps/cli/_methodology.py`. The S26a-1 refactor moved the config shape to `role_refs`. Confirm how overrides are currently parsed. The D87 projection inserts at this layer: flat string maps to default mode for that field; structured `{mode, value}` passes through; unsupported mode-field pairs raise. If the parser does not currently process overrides, commit 2 adds the projection.

3. **Hash payload composition.** Confirm the methodology revision's hash spans `name + description + role_refs` sorted by `role_id`, with `role_refs` serialised in canonical JSON (each entry as `{"role_id", "role_version", "overrides"}` with overrides itself canonically serialised). The role revision's hash spans `name + description + constraint-bundle-fields` per the LVT split's anchoring pattern.

4. **Constraint-bundle field shapes.** Map each brief-described field to its substrate-stored type before writing the migration:
   - `tool_allowlist`: substrate type and value at S26b given no tool registry exists. Conservative path: empty list per role, repopulated at a future methodology revision after S28b. If a placeholder string list is expected instead, surface to user-question.
   - `source_filter`: substrate type and whether tag-based filtering ("context" or "background") is supported at the source layer today. If not supported, conservative path: empty filter, deferred to a source-tagging session.
   - `retrieval_strategy`: substrate type (string enum vs. structured config) and concrete value for "vector primary, graph secondary".
   - `filter_tree`: substrate type and empty-state representation.
   - `top_k`, `min_score`, `cost_ceiling`: numeric defaults match the brief verbatim.
   - `model_selection`: substrate type and concrete value for "default (LiteLLM-routed)".

5. **Alembic data migration shape.** Confirm the pattern from `0007_lvt_split` for inserting `role_templates` plus `role_revisions` plus `methodology_templates` plus `methodology_revisions` rows directly via SQLAlchemy core within a data migration. Confirm `compute_revision_hash` imports cleanly into a migration module. Verify the canonical JSON serialisation in the migration produces bytes byte-equivalent to the use-case-path output for the same payload.

6. **Brief presence on disk.** Confirm `briefs/p8/mckinsey-7-step.md` is committed at session open. If absent, the charter commit includes it alongside the session-brief preservation.

## Charter updates

Single commit ahead of code.

**Commit 1.** `docs(p8/s26b): land D87 override-mode framing, preserve S26b brief, transition current-package.md for S26b in flight`

- Append D87 to `charter/decisions.md` with the structure below.
- Preserve this prompt at `briefs/p8/s26b.md` verbatim.
- Verify `briefs/p8/mckinsey-7-step.md` is committed; if absent, commit it as part of this charter commit.
- Edit `charter/current-package.md`. Transition the active-session block from "S26a closed on 2026-05-12 ... S26b next" to "S26b in flight" with one paragraph framing matching the structure used at the "S26a active" block authored at S26a-1 commit 1. The framing names D87's surfacing through pre-write reconciliation as the architectural anchor.
- Update `charter/schema.md` "Methodology tables (control plane)" section to reflect the structured `role_refs[i].overrides` shape per D87.

### D87 text

```
- **D87: Override-mode space and structured on-disk overrides** (Package P8, Session S26b)
  - Choice. `RoleRef.overrides` is `dict[str, dict[str, Any]]` with each entry the canonical `{"mode": <str>, "value": <any>}` shape. Mode space: augment, replace, tighten. Default mode per field is committed at the substrate layer:
    - system_prompt (soft): augment.
    - tool_allowlist (hard): tighten.
    - source_filter (hard): tighten.
    - retrieval_strategy (soft): replace.
    - filter_tree (hard): tighten.
    - top_k (hard): tighten.
    - min_score (hard): tighten.
    - model_selection (soft): replace.
    - cost_ceiling (hard): tighten.
  Authoring projection: methodology config parsers accept a flat value for each field and expand to the structured form using the default mode; an author writing the structured form overrides the default. The on-disk JSONB always carries the structured form so canonical JSON serialisation and hash determinism stay byte-stable. Validation: substrate rejects writes that pair an inadmissible mode with a field type (augment is admissible only for free-text soft fields; tighten is admissible only for hard fields; replace is admissible for all soft fields and for hard fields where structural replacement is meaningful). Resolver semantics defer to S27b's agent runtime; S26b commits storage plus validation only.
  - Reasoning. D86's "soft fields replace" framing held against architectural intent but did not survive the first authoring evidence. The McKinsey 7-Step brief's "system_prompt addition" semantics for each of the seven roles read as augment, not replace. Forcing the brief to rewrite each addition into a full overridden prompt (concatenating role base plus methodology specialisation by hand at authoring time) doubles authoring cost on every methodology and dilutes the role-first-model's separation between standalone role identity and methodology context. The cleaner refinement admits a richer mode space at the substrate layer and ties default modes to fields so authoring stays terse for the common case. The authoring projection means "augment" never reaches the methodology author for routine overrides; the structural form surfaces only when the author deliberately overrides the default. Hash determinism is preserved by canonical-on-disk storage. The light-UX-as-authoring-discipline principle from `charter/principles.md` is satisfied: flat string for default mode, structured object only on deliberate override.
  - Revisions to prior decisions. D86's sub-commitment (b) "per-role overrides for the methodology's context (hard fields tighten only; soft fields replace)" extends to "soft fields support augment or replace; methodology chooses per instance via the override-mode space; defaults committed per field at the substrate layer". D86 sub-commitment (e)'s structural-enforcement framing extends to mode validation: the substrate rejects inadmissible (field, mode) pairs at methodology write time.
  - Alternatives considered. (a) Option A, keyed addition (`{"system_prompt_addition": "..."}`): encodes mode in key names; works for one (field, mode) combination but breaks when a methodology needs replace on system_prompt or augment on tool_allowlist; key space explodes; binding-mode taxonomy hides inside string conventions. Rejected. (b) Option C, flat field-keyed with implicit mode (`{"system_prompt": "..."}`, mode = default for the field globally): simplest authoring shape but ties mode to field globally, foreclosing per-methodology mode choice. Rejected; a future methodology wanting to replace `system_prompt` entirely would need a substrate change. (c) Hold D86, rewrite the McKinsey brief to expand each addition into a full replacement prompt: defensible but doubles authoring cost on every methodology and breaks the role-first-model's clean separation between standalone role identity and methodology overlay. Rejected on authoring-discipline grounds.
  - Kano. Must-have for the brief's authoring fidelity at the first authoring-evidence moment. Performance for the architectural commitment: the mode space is explicit, extensible, and validated.
```

## Substantive work

### Commit 2. Substrate refinement for D87

`feat(p8/s26b): land D87 substrate — structured RoleRef overrides, parser projection, mode validation`

- Refactor `RoleRef.overrides` per pre-write reconciliation step 1. The on-disk shape is `dict[str, dict[str, Any]]` with the `{mode, value}` structure.
- Land the default-mode table from D87 as a constant at `contexts/methodology/domain/overrides.py` (new module): `DEFAULT_MODE_BY_FIELD` mapping field name to default mode string.
- Land the admissible-mode validation as a function at `contexts/methodology/domain/overrides.py`: `validate_override(field, mode)` raises on inadmissible pairs.
- Land the authoring projection at `apps/cli/_methodology.py` (or wherever the methodology config parser lives per pre-write reconciliation step 2): flat values project to the structured form using `DEFAULT_MODE_BY_FIELD`; structured values pass through; inadmissible pairs raise at parse time.
- Update the `_methodology_content_payload` helper in `contexts/methodology/application/use_cases.py` to serialise overrides canonically per D87 so hashing remains byte-stable.
- LVT methodology's auto-migrated empty overrides verify byte-equivalent under the refactor (LVT has one `role_ref` with empty overrides per S26a-1 commit 5; the empty case is the trivial no-op for canonical serialisation).
- Unit tests covering: default mode lookup per field; admissible and inadmissible (field, mode) pair validation; parser projection from flat to structured; canonical serialisation of overrides in the methodology hash payload; LVT methodology hash byte-equivalent under the refactor.
- Existing methodology and role tests pass without modification (the refactor is additive at the type level; LVT's empty overrides exercise the no-op path).

### Commit 3. Alembic data migration on control plane

`feat(p8/s26b): add 0008_create_mckinsey_7_step data migration on control plane`

- Create Alembic revision `0008_create_mckinsey_7_step` on the control-plane track, chaining after `0007_lvt_split`. Keep the revision ID under 32 chars per the S23 fix precedent at commit `bc969a9`.
- Insert seven `role_templates` rows plus seven `role_revisions` rows (revision 1 each) for the brief's seven roles. Constraint bundle per role matches the substrate-mapped values from pre-write reconciliation step 4. System prompts match the brief verbatim.
- Insert one `methodology_templates` row plus one `methodology_revisions` row (revision 1) for McKinsey 7-Step. The `role_refs` JSONB carries seven entries in the brief's sequential order. Each entry's `overrides` field carries `{"system_prompt": {"mode": "augment", "value": "<verbatim from brief>"}}`. The "other" column entries from the brief are "none" for all seven roles; the migration omits those keys from each entry's `overrides`.
- Hashes computed via `padhanam.security.hash_chain.compute_revision_hash` with the field-set-agnostic API. `previous_revision_hash` = `GENESIS_REVISION_HASH` for each genesis revision.
- Forward-and-back-verified against the live control-plane Postgres. Downgrade DROPs the eight rows by template id; no schema changes (data-only).
- `make migrate` head moves to `0008_create_mckinsey_7_step`. Idempotent on re-run.

### Commit 4. Integration test

`feat(p8/s26b): add integration test exercising McKinsey methodology resolution end-to-end`

- Test at `tests/integration/contexts/methodology/test_mckinsey_resolution.py`.
- Asserts the methodology row exists on control-plane Postgres at the template id the migration writes.
- Resolves the methodology via the role-aware `MethodologyLookupAdapter` from `apps/cli/_cross_context.py` against the live control-plane Postgres.
- Asserts the lookup returns a `MethodologyView` whose `role_refs` resolve to seven `RoleView` instances with role names matching the brief in sequential order.
- Asserts each role_ref's `overrides` exposes the structured `{mode, value}` shape with `mode = "augment"` and value matching the brief verbatim for `system_prompt`. Asserts no other override keys appear on any role_ref.
- Asserts each role's revision-1 hash equals the hash the use-case path would compute (`compute_revision_hash` over the role's content payload). Golden-hash assertion, not just non-null.
- Asserts the methodology's revision-1 hash equals the hash the use-case path would compute over `name + description + role_refs` with canonical override serialisation.
- Test runs against the live stack via the existing integration-test fixtures.

### Commit 5. Session-log entry

`docs(p8/s26b): session log entry; transition current-package.md to S26b closed`

- Append `## Session 26b (Package P8)` entry to `log/sessions.md` per the established format. `roles:` tag covers `architect, technical writer, engineer` (the work spans D87 architectural refinement, McKinsey content authoring, and substrate code). `mode: build`.
- Reflection prompts answered in the entry per the five prompts below.
- Edit `charter/current-package.md`. Transition from "S26b in flight" to "S26b closed; S27b next" with one paragraph framing the McKinsey content landed (seven roles plus methodology plus hash anchoring), D87 as the architectural extension that surfaced through brief reconciliation, and the substrate's first authoring-evidence-driven refinement as the discipline anchor.

## Acceptance criteria

1. D87 lands at `charter/decisions.md` with the structure specified in commit 1, including the default-mode table, the on-disk shape commitment, the authoring projection, the validation rule, the revisions to D86, and the Kano classification.
2. `charter/schema.md` "Methodology tables (control plane)" section documents the structured `role_refs[i].overrides` shape.
3. `RoleRef.overrides` is `dict[str, dict[str, Any]]` at the domain layer. The default-mode table and admissible-mode validation live at `contexts/methodology/domain/overrides.py`.
4. Methodology config parser projects flat values to the structured form per the default-mode table and raises on inadmissible (field, mode) pairs.
5. `_methodology_content_payload` serialises overrides canonically; the LVT methodology's revision-1 hash post-refactor equals the hash computed pre-refactor (byte-equivalent serialisation for empty overrides).
6. `make migrate` applies `0008_create_mckinsey_7_step` forward against the live control-plane Postgres; head is at `0008_create_mckinsey_7_step`; idempotent on re-run.
7. `alembic downgrade -1` reverses the migration cleanly; the eight inserted rows are gone; `alembic upgrade head` re-applies cleanly.
8. Seven `role_templates` rows on control-plane Postgres carry names ProblemFramer, Disaggregator, Prioritiser, Planner, Analyst, Synthesiser, Communicator. Each has exactly one row in `role_revisions` at `version = 1`.
9. Each role's revision-1 row carries a constraint bundle whose field values match the substrate-mapped resolution from pre-write reconciliation step 4. Each role's revision-1 `system_prompt` matches the brief verbatim.
10. The McKinsey 7-Step `methodology_templates` row exists with exactly one row in `methodology_revisions` at `version = 1`.
11. The methodology revision's `role_refs` JSONB carries seven entries in the brief's sequential order, each entry referencing one of the seven role templates by id.
12. Each entry's `overrides` carries exactly one key, `system_prompt`, with value `{"mode": "augment", "value": "<verbatim from brief>"}`.
13. Each role's revision-1 hash equals `compute_revision_hash` applied to the role's content payload with `previous_revision_hash = GENESIS_REVISION_HASH`. Computed at test time; asserted against the persisted value.
14. The methodology's revision-1 hash equals `compute_revision_hash` applied to the methodology's content payload (`name`, `description`, `role_refs` sorted by `role_id`, overrides canonically serialised) with `previous_revision_hash = GENESIS_REVISION_HASH`. Computed at test time; asserted against the persisted value.
15. `tests/integration/contexts/methodology/test_mckinsey_resolution.py` passes against the live control-plane Postgres.
16. Existing 561 unit plus integration plus tenant-isolation plus AST tests still pass at session close. The new D87 substrate tests, the new McKinsey integration test, and the new LVT byte-equivalence test add to the count; report at the session-log entry.
17. 23 import-linter contracts kept; no new contracts; no contracts broken.
18. `briefs/p8/s26b.md` exists with this prompt verbatim. `briefs/p8/mckinsey-7-step.md` exists with the content brief.
19. `charter/current-package.md` ends the session at "S26b closed; S27b next" with the framing paragraph described at commit 5.

## Reflection prompts addressed in the session-log entry

1. **D87 surfacing through brief reconciliation.** D87 surfaced at pre-write reconciliation when Claude Code asked how `RoleRef.overrides` should shape per-entry mode information. The McKinsey brief's "system_prompt addition" semantics did not match D86's "soft fields replace" framing literally. What does the pattern of D86 → first authoring evidence → D86 refinement say about the discipline of committing architectural decisions ahead of authoring evidence versus committing them when authoring evidence arrives? Does this argue for tighter pre-authoring architectural commitments, or for tolerance of refinement when first authoring evidence surfaces gaps?

2. **Content authoring against a stable substrate, refined mid-session.** S26b started as content-only authoring against a stable substrate (the planned shape from the original prompt). D87's surfacing turned it into content authoring plus a substrate refinement. Did the refinement scope stay disciplined (commits 1 and 2 land D87 cleanly; commits 3 and 4 land the content), or did D87 force scope creep into adjacent architectural work? What does this say about the substrate's authoring affordances at the first authoring-evidence moment?

3. **Role authoring discipline at scale.** D86's structural-enforcement commitment specifies role system prompts stay function-focused; procedural content lives in skills (Phase 2). Did the brief's seven function-focused prompts hold against the temptation to include procedural content? Do the per-role overrides specialise the roles for the McKinsey context without diluting standalone-role identity? Does the augment-mode-as-default for system_prompt encourage authors to keep the role base and methodology overlay structurally separate?

4. **Hash computation in migrations and serialisation determinism.** Computing deterministic hashes inside an Alembic data migration is a new shape relative to the use-case-driven hash computation pattern. The D87 refactor adds canonical override serialisation to the hash payload. What did the byte-equivalence check between migration-computed and use-case-computed hashes surface, especially for the LVT no-op case (empty overrides under canonical serialisation)? Is the canonical JSON serialisation truly platform-stable across the migration boundary?

5. **Workflow specification without runtime.** The methodology declares a sequential workflow per D83, but the workflow context is Phase 2. Did the brief's workflow specification translate cleanly into the methodology's storage shape, or did the workflow-not-yet-runtime asymmetry produce friction at the authoring boundary? Combined with the augment-mode-defers-to-resolver pattern (S27b consumes the override), does the broader "substrate stores authored content; consumer layer resolves at runtime" framing hold up at first scale?

## Out of scope

- AgentExecutor port and AgentLoopExecutor adapter. Lands at S27b.
- Tool registry bounded context, tool aggregate, classification taxonomy, invariant enforcement. Lands at S28b.
- SSE streaming and trace capture for agent runtime. Lands at S29b.
- End-to-end agent runtime invocation of a McKinsey role. Lands at S27b plus S30b. The S30b demonstration content is the LVT PM agent, not the McKinsey roles.
- Workflow context implementation. Phase 2 per D83. The workflow specification declared in the methodology's `role_refs` ordering is not runtime-executed at Phase 1.
- Skills aggregate. Phase 2 per D86 sub-commitment (d). The methodology aggregate's schema reserves `recommended_skills` structurally always soft; Phase 1 does not populate it.
- Resolver semantics for override modes at agent invocation. S26b commits storage plus validation only per D87. The agent runtime at S27b consumes the role plus methodology-overrides and composes the effective constraint bundle at invocation time; the composition algorithm (how augment concatenates, how tighten intersects, how replace substitutes) lands at S27b.
- Output aggregates for intermediate artefacts (issue trees, scored branches, workplans, findings, storylines, communications). Phase 2 design per the brief's out-of-scope.
- Tenant-scoped surfacing of the McKinsey methodology. Authoring is platform-managed (control plane) per the epic; tenant-scoped operations defer until a consumer needs them.
- YAML config files at `examples/methodologies/mckinsey-7-step/`. The migration is the source of truth for S26b. If future sessions need the content as YAML for CLI authoring exercises, extraction is a small follow-on.
- Adjacent mode-space extensions beyond augment, replace, tighten. D87 commits these three; any future mode (e.g., a "merge" mode for structured collection fields) lands at the session that surfaces the need.

## Session log entry instruction

At the final commit, append a `## Session 26b (Package P8)` entry to `log/sessions.md` matching the format of S26a-1 and S26a-2. Include the `roles:` tag per D46 (architect, technical writer, engineer this session). Include the `mode:` tag per D47 (build). Cover Produced (D87 plus substrate plus migration plus integration test), Decisions (D87), Tests (test counts at open and close including the new D87 substrate tests, the McKinsey integration test, and the LVT byte-equivalence test; migration forward-and-back verification; golden-hash assertions in the integration test), and the five reflection prompts. Hash any deterministic values inline so the entry is self-contained for the phase audit.
